/**
 * NOT the active Tailwind config. HeroUI requires a JS config with the
 * heroui() plugin — see `tailwind.config.js`, loaded via the `@config`
 * directive in `src/styles/globals.css`. Tailwind 4 does not auto-load this
 * `.ts` file; it's kept only to avoid a dangling reference and is safe to
 * delete.
 */
export {};
