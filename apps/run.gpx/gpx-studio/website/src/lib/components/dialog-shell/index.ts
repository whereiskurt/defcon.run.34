export { default as DialogShell } from './DialogShell.svelte';
export { default as HintBar } from './HintBar.svelte';

/** Hint-bar copy shown when nothing hinted is hovered or focused. */
export const DEFAULT_HINT = 'Hover a row for details';
