config:
  target: "{{ $processEnvironment.TARGET_URL }}"
  phases:
    - duration: 1800       # 30 minutes
      arrivalRate: 1       # 1 VU per second (throttled by maxVusers)
      name: "Auth probe — full OIDC login + MFA + browse + logout"
  plugins:
    ensure: {}
  maxVusers: 1              # Only 1 probe at a time (each takes 30-90s)
  engines:
    playwright:
      launchOptions:
        headless: true
        args:
          - "--no-sandbox"
          - "--disable-dev-shm-usage"
  processor: "../scenarios/index.ts"

scenarios:
  - name: "Full auth probe (login + MFA + browse + logout)"
    engine: playwright
    flowFunction: "authProbe"
    flow: []
