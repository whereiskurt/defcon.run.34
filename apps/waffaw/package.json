{
  "name": "waffaw",
  "version": "1.0.0",
  "description": "WAF testing platform for DEF CON 34 — Artillery + Playwright real-browser traffic generation",
  "private": true,
  "scripts": {
    "test": "artillery run templates/low-and-slow.yml --count 1 --duration 10",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "artillery": "^2.0.22",
    "artillery-engine-playwright": "^1.27.0",
    "playwright": "^1.50.0",
    "@aws-sdk/client-s3": "^3.679.0",
    "@aws-sdk/client-ssm": "^3.679.0",
    "altcha-lib": "^1.4.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
