---
phase: 20-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/services/run.bib/service.hcl
  - infra/terraform/live/site/region/us-east-1/bib-secrets/terragrunt.hcl
autonomous: true
requirements: [BIB-01, BIB-02, BIB-04]
user_setup:
  - service: stripe
    why: "Stripe test-mode payment intents + webhook signature verification (Phase 22)"
    env_vars:
      - name: "/dc34/secrets/use1/bib/stripe/secret_key"
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (test mode, sk_test_*)"
      - name: "/dc34/secrets/use1/bib/stripe/webhook_signing_secret"
        source: "Stripe Dashboard -> Developers -> Webhooks -> {endpoint} -> Signing secret (whsec_*)"
    dashboard_config:
      - task: "Create test-mode webhook endpoint for https://bib.defcon.run/use1/api/payments/stripe/webhook after Phase 22 deploy"
        location: "Stripe Dashboard -> Developers -> Webhooks -> Add endpoint"
  - service: anthropic
    why: "Haiku Lambda parses Venmo/CashApp receipt emails in Phase 22"
    env_vars:
      - name: "/dc34/secrets/use1/bib/anthropic/api_key"
        source: "console.anthropic.com -> API Keys -> new key (sk-ant-*)"

must_haves:
  truths:
    - "bib.defcon.run resolves to a CloudFront distribution in us-east-1 with a valid ACM cert"
    - "ECR repos dc34-run-bib-nginx and dc34-run-bib-app exist in us-east-1 and accept `docker push`"
    - "Five SSM params exist at /dc34/secrets/use1/bib/{stripe/secret_key, stripe/webhook_signing_secret, venmo/handle, cashapp/handle, anthropic/api_key} — stripe/anthropic as SecureString with placeholder value, venmo/cashapp as String with real defaults"
    - "`terragrunt plan` in region/us-east-1/{ecr,secrets,cloudfront,certs} is clean for existing services after this plan applies"
    - "Kurt can run `aws ssm put-parameter --overwrite` on stripe_*/anthropic_* params and a subsequent `terragrunt plan` does NOT try to revert the value (lifecycle ignore_changes)"
  artifacts:
    - path: "infra/terraform/live/site/services/run.bib/service.hcl"
      provides: "run.bib service config skeleton with ecr_repositories block (task/service added in Plan 20-02)"
      contains: "run-bib-nginx"
    - path: "infra/terraform/live/site/region/us-east-1/bib-secrets/terragrunt.hcl"
      provides: "Inline aws_ssm_parameter resources for the 5 bib secrets with nested paths + lifecycle ignore_changes on stripe/anthropic"
      contains: "aws_ssm_parameter"
  key_links:
    - from: "site.hcl locals.dns.subdomains"
      to: "region/us-east-1/certs/ and cloudfront/"
      via: "for_each over subdomains"
      pattern: "\"bib\""
    - from: "site.hcl locals.cloudfront.domains"
      to: "CloudFront distribution for bib.defcon.run"
      via: "domain list drives cert + distribution creation"
      pattern: "\"bib\""
    - from: "site.hcl locals.service_conf.bib"
      to: "site.hcl locals.ecr.repositories concat()"
      via: "read_terragrunt_config in Plan 20-02 wires this; Plan 20-01 only creates the file the read expects"
      pattern: "service_conf.bib"
---

<objective>
Provision the routable + secret surface for bib.defcon.run in us-east-1: DNS subdomain, ACM cert, CloudFront distribution, two ECR repositories (nginx + app), and five SSM parameters. Split from the service+data surface (Plan 20-02) so the boilerplate lands cleanly and the SSM params exist BEFORE Plan 20-02's ECS task tries to consume them.

Purpose: Reviewer can pattern-match this diff against the flash.defcon.run precedent and land it fast. Post-apply, Kurt overrides the placeholder stripe/anthropic secrets via the AWS CLI, and TF's `ignore_changes = [value]` prevents subsequent applies from clobbering the real values.

Output: `site.hcl` recognizes `bib` as a subdomain + CloudFront domain, a skeleton `services/run.bib/service.hcl` exists (with just `ecr_repositories`), and a new `region/us-east-1/bib-secrets/` terragrunt unit provisions the 5 SSM parameters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# v1.5 Bib Registration — Phase 20 Plan 01

Read these before starting:

- `.planning/workstreams/v1-5-bib/ROADMAP.md` — authoritative design contract (Kurt 2026-07-02). Especially "Payments — SSM configuration" section for the 5 param paths.
- `.planning/workstreams/v1-5-bib/phases/20-infrastructure-foundation/20-CONTEXT.md` — locked gray-area decisions. Especially:
  - Decision #1 (region scope): `regions = ["us-east-1"]` only — drop cac1/apse1 wherever bib appears.
  - Decision #2 (plan boundaries): this plan is the "routable + secret surface" half.
  - Decision #3 (SSM provisioning): placeholder + `lifecycle { ignore_changes = [value] }` for stripe/anthropic; real defaults for venmo/cashapp handles.

## Pattern references (READ, do not modify)

- `infra/terraform/live/site/services/run.flash/service.hcl` — two-container ECS pattern to mirror. **Bib's regions list is `["us-east-1"]` only** (not the flash 3-region list).
- `infra/terraform/live/site/site.hcl` lines 21-26 (dns.subdomains), lines 120-162 (cloudfront.domains), lines 210-221 (ecr.repositories concat) — the three sites this plan edits.

## Interfaces used

From `site.hcl`:
```hcl
dns = {
  subdomains = ["email", "run", "auth", "cms", "gpx", "flash", "mqtt"]  # ADD "bib"
}
cloudfront = {
  domains = ["auth", "run", "cms", "gpx", "flash"]  # ADD "bib"
}
service_conf = {
  # existing entries...
  # ADD: bib = read_terragrunt_config("./services/run.bib/service.hcl")
}
ecr = {
  repositories = concat(
    # existing service_conf.*.locals.ecr_repositories...
    # ADD: local.service_conf.bib.locals.ecr_repositories
  )
}
```

Existing ECR entry shape (from `run.flash/service.hcl`):
```hcl
ecr_repositories = [
  { name = "run-flash-nginx", regions = [...], image_tag_mutability = "IMMUTABLE",
    lifecycle_policy = { max_image_count = 10, expire_days = 30 } },
  { name = "run-flash-app",   regions = [...], image_tag_mutability = "IMMUTABLE",
    lifecycle_policy = { max_image_count = 10, expire_days = 30 } },
]
```

The ecr module prepends the site label — repo `run-flash-nginx` in HCL becomes `dc34-run-flash-nginx` on AWS. So bib entries `run-bib-nginx` / `run-bib-app` produce `dc34-run-bib-nginx` / `dc34-run-bib-app` matching CONTEXT.md acceptance.

## SSM param naming — why an inline terragrunt unit

The existing `secrets` module (`region/us-east-1/secrets/`) generates params at `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/{service}/{key}` — 2 levels deep after the region. CONTEXT.md requires 3 levels deep (e.g. `.../bib/stripe/secret_key`). Rather than fork the shared module, create a **new terragrunt unit** `region/us-east-1/bib-secrets/` with inline `aws_ssm_parameter` resources. This keeps the shared module unchanged and gives us full control over lifecycle + type per param.

Kurt's post-apply override commands (already templated in his notes):
```
aws ssm put-parameter --overwrite --name /dc34/secrets/use1/bib/stripe/secret_key             --value "sk_test_..."   --type SecureString --key-id alias/dc34-ssm-use1
aws ssm put-parameter --overwrite --name /dc34/secrets/use1/bib/stripe/webhook_signing_secret --value "whsec_..."     --type SecureString --key-id alias/dc34-ssm-use1
aws ssm put-parameter --overwrite --name /dc34/secrets/use1/bib/anthropic/api_key             --value "sk-ant-..."    --type SecureString --key-id alias/dc34-ssm-use1
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `bib` in site.hcl DNS + CloudFront + service_conf + ECR</name>
  <files>infra/terraform/live/site/site.hcl</files>
  <action>Four surgical edits to `site.hcl` — additive only, do not touch existing subdomains/services:
    1. Add `"bib"` to `locals.dns.subdomains` (line ~23) — final list: `["email", "run", "auth", "cms", "gpx", "flash", "mqtt", "bib"]`.
    2. Add `"bib"` to `locals.cloudfront.domains` (line ~126) — final list: `["auth", "run", "cms", "gpx", "flash", "bib"]`.
    3. Add `bib = read_terragrunt_config("./services/run.bib/service.hcl")` to `locals.service_conf` (line ~62, after the `mqtt` entry).
    4. Add `local.service_conf.bib.locals.ecr_repositories` to the `local.ecr.repositories` concat (line ~218, after `local.service_conf.mqtt.locals.ecr_repositories`).
    Do NOT add bib to `locals.urls.subdomains` or `local_ports` — those maps are consumed by run.human/run.gpx for internal wiring bib doesn't need in Phase 20. Do NOT add bib to `locals.secrets.definitions` — bib SSM params are provisioned by the new `bib-secrets` terragrunt unit (Task 3), not the shared secrets module. Do NOT add bib to `ecs_tasks.tasks` or `ecs_services.services` — that's Plan 20-02 once `service.hcl` grows a task/service block.</action>
  <verify>
    <automated>cd infra/terraform/live/site &amp;&amp; grep -c '"bib"' site.hcl | awk '$1 &gt;= 2 { exit 0 } { exit 1 }' &amp;&amp; grep -q 'service_conf.bib.locals.ecr_repositories' site.hcl &amp;&amp; grep -q 'bib.*=.*read_terragrunt_config.*run.bib/service.hcl' site.hcl</automated>
  </verify>
  <done>site.hcl parses (`terragrunt hcl format --check` clean), `"bib"` appears in `dns.subdomains` AND `cloudfront.domains`, and `service_conf.bib` + ecr concat entry are present. No other services touched.</done>
</task>

<task type="auto">
  <name>Task 2: Create services/run.bib/service.hcl skeleton with ecr_repositories</name>
  <files>infra/terraform/live/site/services/run.bib/service.hcl</files>
  <action>Create a new file with ONLY the `locals.ecr_repositories` block for now. Task/service blocks are Plan 20-02. The file must be `read_terragrunt_config`-loadable (valid HCL with a `locals { }` wrapper) so Task 1's `site.hcl` edit does not fail on plan.

Structure (mirror `services/run.flash/service.hcl` lines 1-27 but single-region):

```hcl
locals {
  ecr_repositories = [
    {
      name                 = "run-bib-nginx"
      regions              = ["us-east-1"]
      image_tag_mutability = "IMMUTABLE"
      lifecycle_policy = {
        max_image_count = 10
        expire_days     = 30
      }
    },
    {
      name                 = "run-bib-app"
      regions              = ["us-east-1"]
      image_tag_mutability = "IMMUTABLE"
      lifecycle_policy = {
        max_image_count = 10
        expire_days     = 30
      }
    },
  ]

  # task = {}    -- populated in Plan 20-02
  # service = {} -- populated in Plan 20-02
}
```

Do NOT add VERSION.nginx / VERSION.app files yet — those come with the actual container builds in Phase 23. The `locals.versions` block from flash is absent here on purpose (Plan 20-02 adds it when the task block references image versions).</action>
  <verify>
    <automated>test -f infra/terraform/live/site/services/run.bib/service.hcl &amp;&amp; grep -q 'run-bib-nginx' infra/terraform/live/site/services/run.bib/service.hcl &amp;&amp; grep -q 'run-bib-app' infra/terraform/live/site/services/run.bib/service.hcl &amp;&amp; ! grep -q 'ca-central-1\|ap-southeast-1' infra/terraform/live/site/services/run.bib/service.hcl</automated>
  </verify>
  <done>File exists, defines `run-bib-nginx` and `run-bib-app` ECR repos scoped to `["us-east-1"]` only, and is loadable by `read_terragrunt_config` (verified via `terragrunt hcl format --check`).</done>
</task>

<task type="auto">
  <name>Task 3: Provision 5 bib SSM parameters via new terragrunt unit</name>
  <files>infra/terraform/live/site/region/us-east-1/bib-secrets/terragrunt.hcl</files>
  <action>Create a new terragrunt unit that inline-defines the 5 SSM parameters with the nested paths required by ROADMAP.md and the placeholder-then-override lifecycle from CONTEXT.md decision #3.

The unit uses inline Terraform (`terraform { source = "." }` pattern — see `region/us-east-1/mqtt/` for precedent) with `aws_ssm_parameter` resources. It must include the `skip.hcl` guard and regional provider like the neighboring `secrets/terragrunt.hcl`:

```hcl
include "skip" {
  path   = "${find_in_parent_folders("region")}/skip.hcl"
  expose = true
}

exclude {
  if      = include.skip.locals.should_skip
  actions = ["all"]
}

include "providers" {
  path = "${find_in_parent_folders("providers")}/regional.hcl"
}

terraform {
  source = "."
}

inputs = {}
```

Then create `main.tf` (or equivalent) in the same directory with:

- `data "aws_kms_alias" "ssm"` for `alias/dc34-ssm-use1` (so we don't hardcode the key id).
- 3 x `aws_ssm_parameter` SecureString with placeholder + ignore_changes:
  - `/dc34/secrets/use1/bib/stripe/secret_key` — value `"PLACEHOLDER_SET_BY_KURT"`, `type = "SecureString"`, `key_id = data.aws_kms_alias.ssm.target_key_id`, `lifecycle { ignore_changes = [value] }`.
  - `/dc34/secrets/use1/bib/stripe/webhook_signing_secret` — same shape.
  - `/dc34/secrets/use1/bib/anthropic/api_key` — same shape.
- 2 x `aws_ssm_parameter` String with real defaults (NO ignore_changes — the default IS the operational value):
  - `/dc34/secrets/use1/bib/venmo/handle`   — value `"@defconrun"`, `type = "String"`.
  - `/dc34/secrets/use1/bib/cashapp/handle` — value `"$defconrun"`, `type = "String"`.

Every resource must include `tags = { site = "dc34", service = "run-bib", region = "use1" }` for consistency with the site conventions.

Documented operator handoff (must land in the plan SUMMARY): after `terragrunt apply`, Kurt runs the three `aws ssm put-parameter --overwrite` commands templated in his 2026-07-02 notes to set real `sk_test_*` / `whsec_*` / `sk-ant-*` values. Subsequent `terragrunt plan` MUST show zero drift on those three parameters (proves `ignore_changes` works).</action>
  <verify>
    <automated>test -f infra/terraform/live/site/region/us-east-1/bib-secrets/terragrunt.hcl &amp;&amp; test -f infra/terraform/live/site/region/us-east-1/bib-secrets/main.tf &amp;&amp; grep -c 'aws_ssm_parameter' infra/terraform/live/site/region/us-east-1/bib-secrets/main.tf | awk '$1 == 5 { exit 0 } { exit 1 }' &amp;&amp; grep -c 'ignore_changes' infra/terraform/live/site/region/us-east-1/bib-secrets/main.tf | awk '$1 == 3 { exit 0 } { exit 1 }' &amp;&amp; grep -q '/dc34/secrets/use1/bib/stripe/secret_key' infra/terraform/live/site/region/us-east-1/bib-secrets/main.tf</automated>
  </verify>
  <done>Unit exists, defines exactly 5 `aws_ssm_parameter` resources, exactly 3 have `ignore_changes = [value]` (stripe/anthropic), venmo/cashapp use real handle defaults, KMS key alias resolved dynamically, `terragrunt plan` in this unit shows 5 additions and nothing else on first run.</done>
</task>

</tasks>

<verification>
End-to-end verification (executor must confirm all of these before declaring plan done):

1. **`terragrunt plan --all` — no drift on existing services.** From `infra/terraform/live/site/`, run `terragrunt plan --all` and confirm:
   - `region/us-east-1/ecr/` shows 2 additions (`dc34-run-bib-nginx`, `dc34-run-bib-app`) and 0 modifications to existing repos.
   - `region/us-east-1/certs/` shows the new bib subdomain cert + 0 modifications to existing certs.
   - `region/us-east-1/cloudfront/` shows one new distribution for bib.defcon.run + 0 modifications to existing distributions.
   - `region/us-east-1/bib-secrets/` shows 5 additions and nothing else.
   - No plan changes surface in `services/*` other than concat expansion into ecr.

2. **Post-apply resolvability.** After `terragrunt apply --all`:
   - `dig bib.defcon.run` returns a CloudFront distribution address.
   - `aws acm list-certificates --region us-east-1` includes a cert covering `bib.defcon.run`.
   - `aws ecr describe-repositories --region us-east-1 --repository-names dc34-run-bib-nginx dc34-run-bib-app` returns 2 repos.
   - `aws ssm get-parameters --with-decryption --names /dc34/secrets/use1/bib/stripe/secret_key /dc34/secrets/use1/bib/stripe/webhook_signing_secret /dc34/secrets/use1/bib/anthropic/api_key /dc34/secrets/use1/bib/venmo/handle /dc34/secrets/use1/bib/cashapp/handle` returns 5 params.
   - Stripe/Anthropic params show `PLACEHOLDER_SET_BY_KURT`; venmo/cashapp show `@defconrun` / `$defconrun`.

3. **Placeholder override survives replan.** Executor demonstrates (in dev, if Kurt hasn't set real values yet) that manually running `aws ssm put-parameter --overwrite --name /dc34/secrets/use1/bib/stripe/secret_key --value "sk_test_dummy" --type SecureString --key-id alias/dc34-ssm-use1` and then `terragrunt plan` on `bib-secrets/` shows NO drift on that param (confirms `ignore_changes = [value]`).
</verification>

<success_criteria>
- Executor confirms Success Criteria 1, 2, and 4 from Phase 20 (from ROADMAP.md): bib subdomain in `dns.subdomains`, ACM + CloudFront resolve in use1, ECR repos exist and accept push, and 5 SSM params exist with correct type + encryption.
- Plan 20-02 can start immediately: `services/run.bib/service.hcl` is a valid HCL file that Plan 20-02 will expand with `task` + `service` blocks, and the 5 SSM param paths exist for Plan 20-02's ECS task `secrets` block to reference via `valueFrom`.
- Operator handoff (Kurt sets real secrets) is documented in the plan SUMMARY.
</success_criteria>

<output>
Create `.planning/workstreams/v1-5-bib/phases/20-infrastructure-foundation/20-01-SUMMARY.md` when done, capturing:
- Confirmed file diffs (site.hcl 4-line delta, new service.hcl skeleton, new bib-secrets/ unit).
- `terragrunt plan --all` output showing zero drift on existing services.
- The three `aws ssm put-parameter --overwrite` commands Kurt must run before Phase 22 deploy (verbatim, so a future session can find them).
- Any deviations from this plan and why.
</output>
