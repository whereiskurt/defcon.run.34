---
phase: 20-infrastructure-foundation
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - infra/terraform/live/site/services/run.bib/service.hcl
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/services/run.human/service.hcl
  - infra/terraform/live/site/region/us-east-1/email/email.hcl
autonomous: true
requirements: [BIB-03, BIB-05]

must_haves:
  truths:
    - "services/run.bib/service.hcl defines a 2-container (nginx + app) ECS task and ALB load_balancer scoped to us-east-1 only"
    - "site.hcl ecs_tasks.tasks and ecs_services.services concat include the bib task/service"
    - "`terragrunt plan` on region/us-east-1/{ecs-task,ecs-service} shows bib additions and 0 modifications to existing services (flash, run-human, auth, cms, gpx, mqtt)"
    - "run-human-electro table has a `runnerCode-index` GSI with Projection.ProjectionType = ALL, primary key + existing GSIs unchanged"
    - "SES receipt rule set for run.defcon.run has a new rule matching bibpayment@run.defcon.run that writes to s3://ses-inbox-dc34-use1/bib-payments/"
    - "ECS task's secrets block references all 5 SSM param paths from Plan 20-01 via `valueFrom`"
  artifacts:
    - path: "infra/terraform/live/site/services/run.bib/service.hcl"
      provides: "Full task+service blocks (nginx + Next.js app containers, ALB listener on 443, service discovery entry `run-bib`)"
      contains: "run-bib-app"
    - path: "infra/terraform/live/site/services/run.human/service.hcl"
      provides: "Adds runnerCode-index GSI to the run-human-electro table entry (dynamodb.tables list)"
      contains: "runnerCode-index"
    - path: "infra/terraform/live/site/region/us-east-1/email/email.hcl"
      provides: "New receive rule for bibpayment@run.defcon.run with S3 action + bib-payments/ prefix"
      contains: "bibpayment"
  key_links:
    - from: "services/run.bib/service.hcl locals.task.containers[app].secrets"
      to: "Plan 20-01's /dc34/secrets/use1/bib/... SSM params"
      via: "valueFrom string references"
      pattern: "/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/"
    - from: "services/run.bib/service.hcl locals.task.containers[app].environment"
      to: "run-human-electro DDB table"
      via: "SSM valueFrom for RUN_ELECTRO_ID/SECRET/DBNAME (existing SSM populated by shared dynamodb module)"
      pattern: "/{{SITE_LABEL}}/dynamodb/{{REGION_LABEL}}/run-human-electro/"
    - from: "SES receipt rule s3_action.object_key_prefix"
      to: "Phase 22 Haiku Lambda S3 event filter"
      via: "prefix `bib-payments/` is a load-bearing contract"
      pattern: "bib-payments/"
    - from: "run-human-electro runnerCode-index GSI"
      to: "Phase 22 reconciliation Lambda (looks up Bib by comment code)"
      via: "GSI hash-key on runnerCode"
      pattern: "runnerCode-index"
---

<objective>
Complete Phase 20 by wiring the service+data surface: the `services/run.bib/service.hcl` ECS task + service blocks (two-container, us-east-1 only), the `runnerCode-index` GSI on the shared `run-human-electro` DDB table, and the SES receipt rule for `bibpayment@run.defcon.run`. On merge, all 6 Phase 20 success criteria from ROADMAP.md are satisfied and `services/run.bib/service.hcl` is ready for the Phase 21 app scaffold to point its container images at.

Purpose: This is the actually-new bits reviewer needs to look at carefully — a GSI addition on a hot shared table, a new SES rule appended to an existing rule set, and a new ECS service definition. Isolating them from Plan 20-01's boilerplate makes review manageable.

Output: `service.hcl` grows a `task` + `service` block, `site.hcl` picks it up in `ecs_tasks`/`ecs_services` concat, `run.human/service.hcl` grows a GSI on the existing table entry, and `email.hcl` grows one receive rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# v1.5 Bib Registration — Phase 20 Plan 02

Read these before starting:

- `.planning/workstreams/v1-5-bib/ROADMAP.md` — authoritative design contract. Data model + reconciliation queue + SSM configuration + auth pattern sections.
- `.planning/workstreams/v1-5-bib/phases/20-infrastructure-foundation/20-CONTEXT.md` — locked decisions. Especially:
  - Decision #4 (SES): append to existing rule set (SES caps active rule sets at 1/region), dedicated `bib-payments/` prefix.
  - Decision #5 (GSI): Projection = ALL (Haiku Lambda updates the whole item, KEYS_ONLY would force a follow-up GetItem — waste). Additive-only diff on the shared table — do NOT alter primary key or existing GSIs.
- `.planning/workstreams/v1-5-bib/phases/20-infrastructure-foundation/20-01-SUMMARY.md` — Plan 20-01's actual outcome (read to confirm the 5 SSM param paths landed as expected).

## Pattern references (READ, do not modify wholesale)

- `services/run.flash/service.hcl` — full pattern for task + service blocks (containers, secrets, port_mappings, health_check, ALB load_balancer, service_discovery). **Bib mirrors this exactly with these swaps:**
  - `regions = ["us-east-1"]` (single element) — never `ca-central-1` or `ap-southeast-1`.
  - Service name / container names / images: `run-bib-*`.
  - Host header: `bib.{{SITE_DOMAIN}}`.
  - App URL: `https://bib.{{SITE_DOMAIN}}`.
  - `secrets` block references the 5 bib SSM paths from Plan 20-01 + JWT + auth secrets.
- `services/run.human/service.hcl` lines 246-315 — existing `dynamodb.tables` list entry for `run-human-electro`. Edit ADDITIVELY to add `global_secondary_indexes` block.
- `services/run.human/service.hcl` lines 189-199 — env wiring pattern for `RUN_ELECTRO_ID/SECRET/DBNAME` via SSM `/{{SITE_LABEL}}/dynamodb/{{REGION_LABEL}}/run-human-electro/*`. Bib reuses the same three SSM refs.
- `services/run.gpx/service.hcl` — auth cookie/claim env vars (`AUTH_URL`, `NEXTAUTH_URL`, `AUTH_COOKIE_DOMAIN`) — bib mirrors this pattern with `bib.{{SITE_DOMAIN}}` domain.

## Interfaces used

From Plan 20-01 (Wave 1):
- `services/run.bib/service.hcl` has `locals.ecr_repositories`. This plan ADDS `locals.task` and `locals.service` blocks.
- 5 SSM parameter paths exist:
  - `/dc34/secrets/use1/bib/stripe/secret_key` (SecureString, placeholder)
  - `/dc34/secrets/use1/bib/stripe/webhook_signing_secret` (SecureString, placeholder)
  - `/dc34/secrets/use1/bib/venmo/handle` (String, `@defconrun`)
  - `/dc34/secrets/use1/bib/cashapp/handle` (String, `$defconrun`)
  - `/dc34/secrets/use1/bib/anthropic/api_key` (SecureString, placeholder)

Shared infra to reference (existing, do NOT recreate):
- `run-human-electro` DDB table (primary key `pk` HASH + `sk` RANGE, PAY_PER_REQUEST, stream enabled).
- SSM DDB creds at `/dc34/dynamodb/use1/run-human-electro/{access_key_id,secret_access_key,table_name}`.
- SSM JWT at `/dc34/secrets/use1/jwt/{secret,internal_secret}`.
- SES receipt rule set (exact name in `region/us-east-1/email/email.hcl`).
- S3 bucket `ses-inbox-dc34-use1` (already provisioned by email module).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand services/run.bib/service.hcl with task + service blocks (us-east-1 only)</name>
  <files>infra/terraform/live/site/services/run.bib/service.hcl</files>
  <action>Edit the skeleton file from Plan 20-01. Preserve the existing `locals.ecr_repositories` block. Add `locals.versions` (reading `VERSION.nginx` and `VERSION.app` — files are stubbed by Phase 23; use `try(file(...), "0.0.0")` fallback so Phase 20 plan does not require them), then add `locals.task` and `locals.service` mirroring `services/run.flash/service.hcl` structure with these bib-specific swaps:

**Task block:**
- `name = "run-bib"`, `regions = ["us-east-1"]`, `cluster_name = "app"`, `task_cpu = 256`, `task_memory = 512`.
- Container `run-bib-nginx`: identical shape to flash's nginx container. `image = "run-bib-nginx:${local.versions.nginx}"`, `environment[APP_URL] = "https://bib.{{SITE_DOMAIN}}"`, port 443, health check `curl -k -f https://localhost/hello`.
- Container `run-bib-app`: identical shape to flash's app container. `image = "run-bib-app:${local.versions.app}"`, port 3000. Environment block includes `NODE_ENV=production`, `HOSTNAME=0.0.0.0`, `REGION_SHORT={{REGION_LABEL}}`, `AWS_REGION={{REGION}}`, `SITE_DOMAIN={{SITE_DOMAIN}}`, `AUTH_URL=https://bib.{{SITE_DOMAIN}}/{{REGION_LABEL}}`, `NEXTAUTH_URL=https://bib.{{SITE_DOMAIN}}/{{REGION_LABEL}}`, `AUTH_COOKIE_DOMAIN=.{{SITE_DOMAIN}}`, `AUTH_PUBLIC_URL=https://auth.{{SITE_DOMAIN}}/{{REGION_LABEL}}`, `AUTH_INTERNAL_URL=http://run-auth.app-{{REGION_LABEL}}-{{SITE_LABEL}}.local:3000/{{REGION_LABEL}}`, `BIB_PUBLIC_URL=https://bib.{{SITE_DOMAIN}}/{{REGION_LABEL}}`, `RUN_HUMAN_INTERNAL_URL=http://run-human.app-{{REGION_LABEL}}-{{SITE_LABEL}}.local:3000/{{REGION_LABEL}}`.
- Container `run-bib-app` `secrets` block — 10 entries:
  - `AUTH_JWT_SECRET`      → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/jwt/secret`
  - `AUTH_INTERNAL_SECRET` → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/jwt/internal_secret`
  - `OIDC_CLIENT_ID`       → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/client_id`   ← **Phase 21 will register bib OIDC client + provision this via shared secrets module. Placeholder param path is fine for Phase 20 plan; note in SUMMARY that Phase 21 must provision it before deploy.**
  - `OIDC_CLIENT_SECRET`   → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/client_secret` (same note)
  - `RUN_ELECTRO_ID`       → `/{{SITE_LABEL}}/dynamodb/{{REGION_LABEL}}/run-human-electro/access_key_id`
  - `RUN_ELECTRO_SECRET`   → `/{{SITE_LABEL}}/dynamodb/{{REGION_LABEL}}/run-human-electro/secret_access_key`
  - `RUN_ELECTRO_DBNAME`   → `/{{SITE_LABEL}}/dynamodb/{{REGION_LABEL}}/run-human-electro/table_name`
  - `STRIPE_SECRET_KEY`             → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/stripe/secret_key`
  - `STRIPE_WEBHOOK_SIGNING_SECRET` → `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/stripe/webhook_signing_secret`
  - Plus `VENMO_HANDLE`, `CASHAPP_HANDLE` (from bib/venmo/handle, bib/cashapp/handle) — even though they're String type, use `secrets` block so the ECS task role gets read access without extra IAM plumbing.

  Note: `ANTHROPIC_API_KEY` is NOT referenced here — the Haiku Lambda in Phase 22 consumes that param, not the web app.

  Health check path: `curl -f http://localhost:3000/{{REGION_LABEL}}/`.

**Service block:**
- `name = "run-bib"`, `regions = ["us-east-1"]`, `cluster_name = "app"`, `task_family = "run-bib"`, `desired_count = 1`.
- `service_discovery = { name = "run-bib", container_name = "run-bib-app" }`.
- ALB `load_balancers` entry: HTTPS on 443, container `run-bib-nginx`, health check path `/hello`, host header `["bib.{{SITE_DOMAIN}}"]` (no wildcard — bib has no subroutes).
- Autoscaling disabled (`enabled = false`, min 1 max 2) — matches flash.

**Add both blocks additively** — do not touch `locals.ecr_repositories` from Plan 20-01.</action>
  <verify>
    <automated>cd infra/terraform/live/site &amp;&amp; grep -q 'name * = *"run-bib"' services/run.bib/service.hcl &amp;&amp; grep -q 'bib.{{SITE_DOMAIN}}' services/run.bib/service.hcl &amp;&amp; grep -q '/dc34/secrets/{{REGION_LABEL}}/bib/stripe/secret_key\|{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/bib/stripe/secret_key' services/run.bib/service.hcl &amp;&amp; ! grep -E 'ca-central-1|ap-southeast-1' services/run.bib/service.hcl</automated>
  </verify>
  <done>service.hcl contains task + service + existing ecr_repositories blocks. `regions` is `["us-east-1"]` everywhere. Task references all 5 bib SSM paths + 3 run-human-electro SSM paths + JWT paths. Service ALB listener host_header is `bib.{{SITE_DOMAIN}}`. No cac1/apse1 references.</done>
</task>

<task type="auto">
  <name>Task 2: Wire run.bib task + service into site.hcl concat lists</name>
  <files>infra/terraform/live/site/site.hcl</files>
  <action>Two additive edits (single-line each):
    1. Add `local.service_conf.bib.locals.task` to `locals.ecs_tasks.tasks` (line ~233, after `local.service_conf.mqtt.locals.task`).
    2. Add `local.service_conf.bib.locals.service` to `locals.ecs_services.services` (line ~245, after `local.service_conf.mqtt.locals.service`).
    Do NOT touch `dynamodb.tables` concat — bib does NOT create a new table; its GSI is added inside the existing `run.human` service.hcl entry in Task 3. Do NOT touch `user_uploads` or `upload_processors` — bib has no S3 user upload surface in Phase 20.</action>
  <verify>
    <automated>cd infra/terraform/live/site &amp;&amp; grep -c 'service_conf.bib.locals.task' site.hcl | awk '$1 &gt;= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c 'service_conf.bib.locals.service' site.hcl | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>site.hcl's `ecs_tasks.tasks` and `ecs_services.services` concat both include the bib service. `terragrunt hcl format --check` clean. No other locals mutated.</done>
</task>

<task type="auto">
  <name>Task 3: Add runnerCode-index GSI to run-human-electro table (additive-only)</name>
  <files>infra/terraform/live/site/services/run.human/service.hcl</files>
  <action>Locate the `run-human-electro` entry in `locals.dynamodb.tables` (starting around line 249 with `table_name = "run-human-electro"`). ADD a `global_secondary_indexes` block additively — do NOT touch `hash_key`, `range_key`, `replica_regions`, `billing_mode`, `stream_enabled`, `stream_view_type`, or existing GSIs.

The dynamodb module's GSI schema (verify by reading `infra/terraform/modules/dynamodb/` or an existing GSI on another table entry) — typical shape:

```hcl
global_secondary_indexes = [
  {
    name            = "runnerCode-index"
    hash_key        = "runnerCode"
    hash_key_type   = "S"
    projection_type = "ALL"
  },
  # ... any existing GSIs preserved verbatim
]
```

**Important:** If the module requires the item schema to also declare `runnerCode` in a top-level `attributes` list, add it there too:
```hcl
attributes = [
  { name = "runnerCode", type = "S" },
]
```

**Do not add sort key** — the GSI is hash-only (comment-code lookup, no range needed). Projection MUST be `ALL` (per CONTEXT.md decision #5).

**Additive-only check:** Before committing, run `terragrunt plan` on `region/us-east-1/dynamodb/` and confirm the plan for `run-human-electro` shows ONLY the new GSI + the new attribute — zero modifications to `pk`, `sk`, replica config, or streams. If plan surfaces any drift on those fields, STOP and investigate before proceeding (destructive change to a hot shared table used by run.human).</action>
  <verify>
    <automated>cd infra/terraform/live/site &amp;&amp; grep -q 'runnerCode-index' services/run.human/service.hcl &amp;&amp; grep -q 'runnerCode' services/run.human/service.hcl</automated>
  </verify>
  <done>GSI declared with hash `runnerCode` and Projection.ProjectionType = ALL. `terragrunt plan` on the DDB unit shows only additive changes on `run-human-electro`. Backfill is instant on empty index. Primary key + existing GSIs untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Append SES receive rule for bibpayment@run.defcon.run</name>
  <files>infra/terraform/live/site/region/us-east-1/email/email.hcl</files>
  <action>Locate the existing SES receipt rule set config for the `run.defcon.run` zone (search for `run.defcon.run` and `receipt_rule` or the module's rules-list input). APPEND a new rule to the END of the existing rules list (SES evaluates rules in order; append is safest — do NOT reorder existing rules).

Expected shape (adapt to whatever schema the shared email module accepts):

```hcl
{
  name       = "bib-payment-inbound"
  recipients = ["bibpayment@run.defcon.run"]
  enabled    = true
  scan_enabled = true
  tls_policy = "Optional"

  s3_action = {
    bucket_name       = "ses-inbox-dc34-use1"
    object_key_prefix = "bib-payments/"
    position          = 1
  }
}
```

**Load-bearing contract:** the object_key_prefix `bib-payments/` MUST match exactly — Phase 22's Haiku Lambda S3 event trigger filters on this prefix to scope its work to bib emails only. Get it wrong and the Lambda either misses events or picks up unrelated traffic.

**Additive-only check:** Before committing, run `terragrunt plan` on `region/us-east-1/email/`. The plan must show only the new rule addition on the existing rule set — no delete/recreate of the rule set, no modifications to existing rules (admin@, no-reply@, catch-all). If plan shows a rule-set replacement, STOP — that would take down existing mail flow.

If the `run.defcon.run` zone's rule set is defined via a data source or dependency reference rather than in this file directly, follow the pattern used by the existing rules (they're the ground truth for how to hook into the existing set).</action>
  <verify>
    <automated>cd infra/terraform/live/site &amp;&amp; grep -q 'bibpayment@run.defcon.run\|bibpayment@run' region/us-east-1/email/email.hcl &amp;&amp; grep -q 'bib-payments/' region/us-east-1/email/email.hcl</automated>
  </verify>
  <done>Rule added to existing rule set, matches `bibpayment@run.defcon.run`, S3 action writes to `ses-inbox-dc34-use1` with prefix `bib-payments/`. `terragrunt plan` shows one rule addition + zero modifications to existing rules or the rule set itself.</done>
</task>

</tasks>

<verification>
End-to-end verification (executor must confirm all before declaring plan done):

1. **`terragrunt plan --all` — additive-only on shared infra.** From `infra/terraform/live/site/`:
   - `region/us-east-1/ecs-task/` shows `run-bib` task addition + 0 modifications to existing tasks (auth, run-human, cms master/worker, gpx, flash, mqtt).
   - `region/us-east-1/ecs-service/` shows `run-bib` service addition + 0 modifications to existing services.
   - `region/us-east-1/dynamodb/` shows `run-human-electro` gaining `runnerCode-index` GSI + attribute — 0 modifications to `pk`, `sk`, replica config, streams, existing GSIs.
   - `region/us-east-1/email/` shows one new SES rule addition + 0 modifications to existing rules or rule-set structure.
   - No plan changes surface in `region/us-east-1/{ecr,secrets,certs,cloudfront,bib-secrets}` — those were finalized in Plan 20-01.

2. **Post-apply reachability.** After `terragrunt apply --all`:
   - `aws ecs describe-task-definition --task-definition run-bib --region us-east-1` returns 2 container definitions (nginx + app) with all expected env vars and secrets valueFroms.
   - `aws ecs describe-services --cluster app --services run-bib --region us-east-1` returns a service with desiredCount 1 (task will fail health check until app image exists — expected; Phase 21 delivers the image).
   - `aws dynamodb describe-table --table-name run-human-electro --region us-east-1` shows `runnerCode-index` under `GlobalSecondaryIndexes` with `Projection.ProjectionType == "ALL"` and `IndexStatus == "ACTIVE"`.
   - `aws ses describe-active-receipt-rule-set --region us-east-1` includes a rule named `bib-payment-inbound` matching `bibpayment@run.defcon.run` with S3 action pointing at `ses-inbox-dc34-use1` + prefix `bib-payments/`.

3. **Run.human unaffected.** Executor confirms `run.defcon.run` still serves normally (`curl -I https://run.defcon.run/use1/` returns 200/302 as before) — proves the shared-table GSI addition did not disrupt the primary consumer.

4. **Existing mail unaffected.** Send a test message to `admin@run.defcon.run` (or whatever the primary existing recipient is) and confirm it still routes correctly — proves the SES rule-set edit did not break existing traffic.
</verification>

<success_criteria>
- All 6 Phase 20 success criteria from ROADMAP.md are satisfied (Plan 20-01 covered #1, #2, #4; this plan covers #3, #5, #6).
- Phase 21 can start immediately: `services/run.bib/service.hcl` is a complete task+service definition; Phase 21 only needs to build/push the app + nginx images to the ECR repos and set VERSION files, and register the `bib` OIDC client to populate `/dc34/secrets/use1/bib/{client_id,client_secret}`.
- `run-human-electro` GSI is active and empty, ready for Phase 21 to write `Bib` items with `runnerCode`.
- `bibpayment@run.defcon.run` receives + persists to S3, ready for Phase 22 Haiku Lambda's S3 event trigger.
</success_criteria>

<output>
Create `.planning/workstreams/v1-5-bib/phases/20-infrastructure-foundation/20-02-SUMMARY.md` when done, capturing:
- Final diff summary (service.hcl grew by ~180 lines, site.hcl 2-line delta, run.human/service.hcl GSI addition, email.hcl 1 rule addition).
- `terragrunt plan --all` output confirming additive-only on `run-human-electro` and the SES rule set.
- Post-apply verification screenshots/JSON extracts for the DDB GSI and SES rule.
- Confirmation that run.defcon.run and existing mail flow are unaffected.
- Note that Phase 21 must register the `bib` OIDC client (populating `/dc34/secrets/use1/bib/client_id` and `.../client_secret`) before deploying the app — the ECS task's `secrets` block already references those paths but they don't exist yet in Phase 20.
</output>
