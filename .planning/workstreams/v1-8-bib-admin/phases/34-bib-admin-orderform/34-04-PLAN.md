---
phase: 34-bib-admin-orderform
plan: 04
type: execute
wave: 2
depends_on: [34-02, 34-03]
files_modified:
  - apps/run.bib/webapp/src/lib/altcha-overlay.ts
  - apps/run.bib/webapp/src/__tests__/altcha-overlay.test.ts
  - apps/run.bib/webapp/src/lib/altcha-client.ts
  - apps/run.bib/webapp/src/components/AltchaOverlay.tsx
  - apps/run.bib/webapp/src/app/providers.tsx
  - apps/run.bib/webapp/src/components/BibForm.tsx
  - apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx
  - apps/run.bib/webapp/src/app/orderform/page.tsx
  - apps/run.bib/webapp/src/components/GetYourBib.tsx
  - apps/run.bib/webapp/src/components/BibPreview.tsx
autonomous: true
requirements: [BIB-ADM-07, BIB-ADM-08, BIB-ADM-09]

must_haves:
  truths:
    - "Every ALTCHA proof-of-work shows a once-mounted HeroUI blur-overlay Spinner ('Checking you're human…') that auto-dismisses when the in-flight counter returns to 0 (SC34.7)"
    - "The inline 'verifying' text is removed from BibForm and the checkbox (SC34.7)"
    - "The overlay in-flight store is unit-tested for increment/decrement (SC34.7)"
    - "The bib tear-off stubs render the runner's social-QR (/r?h=<hash>) enlarged when available, else the runner-code QR (SC34.8)"
    - "A missing hash never blanks a stub — runner-code fallback always renders (SC34.8)"
  artifacts:
    - "apps/run.bib/webapp/src/lib/altcha-overlay.ts exports begin/end/subscribe (+ useAltchaBusy)"
    - "apps/run.bib/webapp/src/components/AltchaOverlay.tsx"
    - "apps/run.bib/webapp/src/components/BibPreview.tsx accepts socialQrUrl and renders enlarged QR"
  key_links:
    - "solveAltcha begin()/end() (finally) → altcha-overlay store → AltchaOverlay (mounted in providers.tsx)"
    - "orderform/page.tsx getSocialQrHash+buildSocialQrUrl → GetYourBib → BibForm → BibPreview socialQrUrl"
    - "BibPreview: socialQrUrl present → enlarged QrBadge; absent → runnerCode QrBadge"
---

<objective>
Slice B ALTCHA overlay + Slice C QR render. Replace inline "verifying" text with a
once-mounted blur-overlay Spinner driven by an in-flight counter inside `solveAltcha`,
and render the runner's real social-QR (enlarged) on both bib tear-off stubs with a
runner-code fallback.

Purpose: The proof-of-work has no unmistakable global affordance, and the bib prints only
the runner-code QR. Implements CONTEXT D-09 (B-T4) and D-10 (C-T3, C-T4). This is the
wave-2 half of the orderform work: it re-touches BibForm / WillPayInPersonCheckbox /
orderform/page / GetYourBib / BibPreview after plan 03 has landed, and consumes plan 02's
`social-qr.ts`.
Output: `altcha-overlay.ts` + `AltchaOverlay.tsx` mounted in providers, `solveAltcha`
wrapped, inline hints removed, threaded `socialQrUrl`, enlarged stub QR.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-CONTEXT.md
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-PATTERNS.md
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md
@apps/run.bib/webapp/src/lib/pending-bib-save.ts
@apps/run.bib/webapp/src/lib/altcha-client.ts
@apps/run.bib/webapp/src/app/providers.tsx
</context>

## Artifacts this plan produces

- New file `apps/run.bib/webapp/src/lib/altcha-overlay.ts` — in-flight counter store: `begin()`, `end()`, `subscribe(cb)`, `useAltchaBusy()`
- New file `apps/run.bib/webapp/src/components/AltchaOverlay.tsx` — dimmed/blurred fixed overlay with HeroUI `<Spinner>`
- New prop on `BibPreview`: `socialQrUrl?: string` (enlarged QR + reposition)
- `orderform/page.tsx` computes + threads `socialQrUrl`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ALTCHA in-flight store + blur overlay, remove inline hints</name>
  <files>apps/run.bib/webapp/src/lib/altcha-overlay.ts, apps/run.bib/webapp/src/__tests__/altcha-overlay.test.ts, apps/run.bib/webapp/src/lib/altcha-client.ts, apps/run.bib/webapp/src/components/AltchaOverlay.tsx, apps/run.bib/webapp/src/app/providers.tsx, apps/run.bib/webapp/src/components/BibForm.tsx, apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/lib/pending-bib-save.ts (module-singleton subscribe shape)
    - apps/run.bib/webapp/src/lib/altcha-client.ts (solveAltcha(level) entry + return; AltchaLevel)
    - apps/run.bib/webapp/src/components/CashRain.tsx (fixed-overlay client component + z-index reference)
    - apps/run.bib/webapp/src/app/providers.tsx (HeroUIProvider nesting; where to mount once)
    - apps/run.bib/webapp/src/components/BibForm.tsx (SaveStateHint "Checking you're human… (~5s)" branch to remove; keep saved/error/quota/rename-count)
    - apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx (the PoW "Saving…" hint now covered by overlay)
    - .planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md (IC-2: dim rgba(10,10,15,0.55) + blur(6px); Spinner label "Checking you're human…"; z-index 9999; copy-removal list)
  </read_first>
  <behavior>
    - begin() increments the count; a subscriber is notified with busy=true on 0→1
    - end() decrements; subscriber notified busy=false on 1→0
    - nested begin/begin/end/end stays busy until the final end (count-based, not boolean)
    - end() never drives the count below 0
  </behavior>
  <action>
    Create `lib/altcha-overlay.ts` (module singleton, mirror `pending-bib-save.ts`):
    `let count = 0; const subs = new Set<(busy:boolean)=>void>();` export `begin()` (count++,
    notify), `end()` (count = Math.max(0, count-1), notify), `subscribe(cb)` (returns unsubscribe),
    and `useAltchaBusy(): boolean` (a React hook subscribing via useState/useEffect). Notify passes
    `count > 0`.
    In `lib/altcha-client.ts` `solveAltcha`: call `begin()` at entry and `end()` in a `finally` so
    every caller (BibForm save, checkbox toggle, checkout flush) drives the overlay. Keep the
    returned solution contract + `AltchaLevel` unchanged.
    Create `components/AltchaOverlay.tsx` (`"use client"`): subscribe via `useAltchaBusy()`; when
    busy, render a `position:fixed` full-viewport layer, `background: rgba(10,10,15,0.55)`,
    `backdropFilter: "blur(6px)"`, `zIndex: 9999`, non-dismissable (no close affordance,
    pointer-events capture), centered HeroUI `<Spinner label="Checking you're human…" />` (mint
    tint). Optional framer-motion ~150ms fade gated by prefers-reduced-motion. Auto-hides at count 0.
    Mount `<AltchaOverlay />` ONCE in `app/providers.tsx` inside `<HeroUIProvider>` (keep the
    NextThemes/HeroUI/Session/SilentSSO nesting + basePath handling intact).
    Remove inline verifying copy: in `BibForm`'s `SaveStateHint`, drop the "Checking you're human…
    (~5s)" branch (keep saved/error/quota/locked/rename-count). In `WillPayInPersonCheckbox`, drop
    the PoW "Saving…" hint (overlay covers it); keep the rest.
    Create `__tests__/altcha-overlay.test.ts` asserting increment/decrement, nested begin/end
    staying busy until final end, and the no-negative-floor guard.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/altcha-overlay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function begin" apps/run.bib/webapp/src/lib/altcha-overlay.ts` and `end` + `subscribe`
    - `grep -q "begin()" apps/run.bib/webapp/src/lib/altcha-client.ts` and a `finally` calling `end()`
    - `grep -q "backdrop" apps/run.bib/webapp/src/components/AltchaOverlay.tsx` and `Checking you're human`
    - `grep -q "AltchaOverlay" apps/run.bib/webapp/src/app/providers.tsx`
    - BibForm SaveStateHint no longer contains the "Checking you're human… (~5s)" branch
    - altcha-overlay test proves count stays busy through nested begin/end and never goes negative
    - vitest altcha-overlay suite green
  </acceptance_criteria>
  <done>Every PoW drives a once-mounted blur-overlay Spinner; inline verifying text removed; store unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Thread social-QR URL + render enlarged stub QR</name>
  <files>apps/run.bib/webapp/src/app/orderform/page.tsx, apps/run.bib/webapp/src/components/GetYourBib.tsx, apps/run.bib/webapp/src/components/BibForm.tsx, apps/run.bib/webapp/src/components/BibPreview.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/lib/social-qr.ts (getSocialQrHash + buildSocialQrUrl — produced by plan 02)
    - apps/run.bib/webapp/src/app/orderform/page.tsx (server bootstrap; ownerSub available from the session/bib; where runnerCode is threaded down)
    - apps/run.bib/webapp/src/components/GetYourBib.tsx (props passed to BibForm)
    - apps/run.bib/webapp/src/components/BibForm.tsx (props passed to BibPreview)
    - apps/run.bib/webapp/src/components/BibPreview.tsx (the two QrBadge calls at x=200/x=660 y=596 size=76; the number boxes / smiley badge geometry; the vector QrBadge renderer)
    - .planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md (IC-5: enlarge 76→~112 SVG units; reposition x/y; fallback to runnerCode; silent miss)
  </read_first>
  <action>
    In `orderform/page.tsx` (server component): best-effort resolve the hash —
    `const hash = ownerSub ? await getSocialQrHash(ownerSub) : null;`
    `const socialQrUrl = hash ? buildSocialQrUrl(hash) : null;` (getSocialQrHash is already
    null-safe). Thread `socialQrUrl` down alongside the existing `runnerCode` (through GetYourBib →
    BibForm → BibPreview).
    In `GetYourBib.tsx` and `BibForm.tsx`: add a `socialQrUrl?: string` prop and pass it through to
    `<BibPreview>` (no logic — pure pass-through).
    In `BibPreview.tsx`: add `socialQrUrl?: string` to `BibPreviewProps`. For each of the two
    tear-off stubs, encode `socialQrUrl` via the existing vector `QrBadge` when present, else fall
    back to `runnerCode` (never render a blank stub — SC34.8). Enlarge the badge from 76 → ~112 SVG
    user-units and reposition the two stubs' QR `x/y` so the larger badges sit fully inside the stub
    area without overlapping the number boxes or smiley badges. Keep the crisp vector `QrBadge`
    renderer (print-safe) and the rest of the SVG untouched.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "buildSocialQrUrl" apps/run.bib/webapp/src/app/orderform/page.tsx`
    - `grep -q "socialQrUrl" apps/run.bib/webapp/src/components/BibPreview.tsx`
    - BibPreview encodes socialQrUrl when present and falls back to runnerCode when absent (both stubs)
    - the enlarged QrBadge size (~112) is applied and stub x/y repositioned (no blank stub on miss)
    - `next build` green in apps/run.bib/webapp
  </acceptance_criteria>
  <done>Orderform resolves + threads socialQrUrl; both stubs render the enlarged social-QR with a runner-code fallback; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run.bib server (orderform) → run.human internal endpoint | Server-side hash resolution during SSR (via plan-02 social-qr.ts); a failure must degrade, not error |
| browser → ALTCHA proof-of-work | Existing anti-abuse boundary; the overlay is presentation only and does not weaken the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-07 | Denial of Service | orderform SSR hash fetch | medium | mitigate | `getSocialQrHash` catches all errors → null; orderform renders the runner-code QR fallback; a miss/timeout never 500s the page (SC34.8) |
| T-34-10 | Tampering | ALTCHA overlay | low | mitigate | Overlay is cosmetic; PoW verification still occurs server-side; overlay is non-dismissable so it cannot be used to imply a bypass |
| T-34-11 | Information disclosure | printed social-QR value | low | accept | The QR encodes the same public `/r?h=<hash>` value run.human already prints on the profile QR; no new secret is surfaced |
| T-34-SC | Tampering | npm installs | low | accept | No new packages (HeroUI Spinner + framer-motion already installed) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/altcha-overlay.test.ts` green (increment/decrement + nesting).
- `npx next build` green in apps/run.bib/webapp.
- Manual: any save/toggle/checkout shows the blur overlay Spinner and it auto-dismisses; the bib
  stub QR resolves to the same `/r?h=` target as the run.human profile QR, and falls back to the
  runner-code QR (never blank) when there is no hash.
</verification>

<success_criteria>
SC34.7 (ALTCHA blur overlay + inline text removed + store tested), SC34.8 render half (enlarged
social-QR on both stubs with runner-code fallback), and the SC34.9 build/test gate for this slice.
</success_criteria>

<output>
Create `.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-04-SUMMARY.md` when done.
</output>
