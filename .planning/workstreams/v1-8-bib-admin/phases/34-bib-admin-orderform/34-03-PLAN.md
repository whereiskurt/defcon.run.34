---
phase: 34-bib-admin-orderform
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.bib/webapp/src/lib/rain-store.ts
  - apps/run.bib/webapp/src/app/orderform/page.tsx
  - apps/run.bib/webapp/src/components/GetYourBib.tsx
  - apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx
  - apps/run.bib/webapp/src/components/BibForm.tsx
  - apps/run.bib/webapp/src/components/BibPreview.tsx
  - apps/run.bib/webapp/src/components/SponsorForm.tsx
autonomous: true
requirements: [BIB-ADM-04, BIB-ADM-05, BIB-ADM-06, BIB-ADM-09]

must_haves:
  truths:
    - "The '$20 in person' checkbox renders between Sponsor and Donate on mobile and full-width below both on desktop (SC34.4)"
    - "Checking the checkbox still hides the Sponsor tile (hideBuyBib, D-05) and still fires cash-rain over the bib preview (SC34.4)"
    - "A dirty name makes the Save button glow + enlarge (SC34.5)"
    - "A dirty name renders an UNSAVED stamp in the same slot as PAID; UNSAVED outranks PAID while dirty (SC34.5)"
    - "Clicking Sponsor or Donate commits the current bib name before checkout for both bib and general variants (SC34.6)"
  artifacts:
    - "apps/run.bib/webapp/src/lib/rain-store.ts exports setRaining + subscribe"
    - "apps/run.bib/webapp/src/components/BibPreview.tsx accepts a `dirty` prop and renders an UNSAVED stamp"
  key_links:
    - "WillPayInPersonCheckbox.onCheckedChange → rain-store.setRaining → BibForm subscribes → CashRain"
    - "BibForm dirty → BibPreview dirty prop → UNSAVED stamp priority over PAID"
    - "SponsorForm.onSubmit → flushPendingBibName() awaited before checkout redirect (both variants)"
---

<objective>
Slice B core (orderform UX). Move the in-person checkbox to a responsive placement,
bridge its cash-rain across the new component boundary, make the unsaved-name state loud
(Save glow/enlarge + UNSAVED stamp), and verify/harden the implicit save-on-checkout.

Purpose: Runners miss the unsaved state and the checkbox placement is not responsive.
Implements CONTEXT D-05…D-08 (B-T1, B-T2, B-T3). This plan owns the shared orderform
component files; the ALTCHA overlay + social-QR render (which also touch these files)
follow in wave 2 (plan 04) to avoid concurrent edits.
Output: `rain-store.ts`, responsive tile grid, dirty Save glow, UNSAVED stamp, verified
flush bridge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-CONTEXT.md
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-PATTERNS.md
@.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md
@apps/run.bib/webapp/src/lib/pending-bib-save.ts
</context>

## Artifacts this plan produces

- New file `apps/run.bib/webapp/src/lib/rain-store.ts` exporting `setRaining(v: boolean)` + `subscribe(cb)`
- New prop on `BibPreview`: `dirty?: boolean` (renders the UNSAVED rubber stamp)
- Responsive Tailwind tile-grid wrapper in `orderform/page.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Responsive checkbox placement + cash-rain bridge</name>
  <files>apps/run.bib/webapp/src/lib/rain-store.ts, apps/run.bib/webapp/src/app/orderform/page.tsx, apps/run.bib/webapp/src/components/GetYourBib.tsx, apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx, apps/run.bib/webapp/src/components/BibForm.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/lib/pending-bib-save.ts (module-singleton register/subscribe shape to mirror)
    - apps/run.bib/webapp/src/app/orderform/page.tsx (server bootstrap: getBib/createBib, hideBuyBib, the tiles region, showCheckbox pass-through)
    - apps/run.bib/webapp/src/components/GetYourBib.tsx (client wrapper: local `raining` useState + showCheckbox it currently owns)
    - apps/run.bib/webapp/src/components/WillPayInPersonCheckbox.tsx (debounce + ALTCHA PATCH + router.refresh; onCheckedChange)
    - apps/run.bib/webapp/src/components/CashRain.tsx (the `raining` prop consumer)
    - .planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md (Spacing: `grid gap-5 sm:grid-cols-2`; cell `order-2 sm:order-3 sm:col-span-2`; IC-3)
  </read_first>
  <action>
    Create `lib/rain-store.ts` (`"use client"`) mirroring `pending-bib-save.ts`: a module
    singleton with `let raining = false; const subs = new Set<(v:boolean)=>void>();`
    `export function setRaining(v: boolean)` (sets + notifies subscribers),
    `export function subscribe(cb)` returning an unsubscribe fn.
    In `orderform/page.tsx`: stop passing the checkbox through `GetYourBib` (drop `showCheckbox`);
    render the tiles region as a Tailwind grid wrapper `grid gap-5 sm:grid-cols-2` with DOM order
    `Sponsor → WillPayInPersonCheckbox → Donate`, giving the checkbox cell
    `order-2 sm:order-3 sm:col-span-2` (between on mobile, full-width below on desktop). Only this
    wrapper migrates to Tailwind classes; the Sponsor/Donate tiles keep their inline styles. Keep
    `hideBuyBib` (checked still hides the Sponsor tile — D-05) and the big THANK YOU logic.
    In `WillPayInPersonCheckbox.tsx`: in `onCheckedChange`, call `setRaining(checked)` (replacing
    the old prop callback that reached `GetYourBib`); keep the debounce, ALTCHA PATCH, and
    `router.refresh()`.
    In `GetYourBib.tsx`: remove the local `raining` useState and the `showCheckbox` prop; leave it
    the thin client wrapper around `BibForm`.
    In `BibForm.tsx`: `subscribe` to `rain-store` in an effect and pass the current `raining` value
    to `<CashRain>`. Keep CashRain's prefers-reduced-motion handling unchanged.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function setRaining" apps/run.bib/webapp/src/lib/rain-store.ts`
    - `grep -q "order-2 sm:order-3 sm:col-span-2" apps/run.bib/webapp/src/app/orderform/page.tsx`
    - `grep -q "grid gap-5 sm:grid-cols-2" apps/run.bib/webapp/src/app/orderform/page.tsx`
    - WillPayInPersonCheckbox calls `setRaining` from rain-store; GetYourBib no longer holds a `raining` useState or `showCheckbox`
    - `next build` green in apps/run.bib/webapp
  </acceptance_criteria>
  <done>Checkbox is responsive (between mobile / below desktop), still hides Sponsor, and cash-rain fires via the shared store; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Loud unsaved state (Save glow + UNSAVED stamp)</name>
  <files>apps/run.bib/webapp/src/components/BibForm.tsx, apps/run.bib/webapp/src/components/BibPreview.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/BibForm.tsx (the `dirty = name !== savedName` computation, the Save button, and where it renders `<BibPreview>`)
    - apps/run.bib/webapp/src/components/BibPreview.tsx (BibPreviewProps; the `hasSponsored` `sponsor-charm` PAID stamp group at transform `rotate(-11 806 208)`)
    - apps/run.bib/webapp/src/__tests__/bib-preview.test.tsx (existing preview test style, for adding a dirty-stamp assertion)
    - .planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-UI-SPEC.md (IC-1; Color: glow `#6CCDB8`; UNSAVED fill `#C2410C`, stroke `#ffe9df`, text `#fff`)
  </read_first>
  <action>
    In `BibForm.tsx`: when `dirty`, style the Save button with a glow ring in `#6CCDB8`
    (e.g. `boxShadow: "0 0 0 3px #6CCDB855, 0 0 16px #6CCDB8AA"`) and enlarge it (padding
    `10px 16px → 12px 22px`, font `13 → 15px`); revert to base style when clean. Any pulse
    animation must be gated by `@media (prefers-reduced-motion: reduce)`. Pass a new `dirty={dirty}`
    prop into `<BibPreview>`.
    In `BibPreview.tsx`: add `dirty?: boolean` to `BibPreviewProps`. Render an "UNSAVED" rotated
    rubber-stamp group in the SAME `transform="rotate(-11 806 208)"` slot/position as the existing
    `sponsor-charm` PAID group, styled fill `#C2410C`, stroke `#ffe9df`, text `#fff`, with
    `data-testid="unsaved-charm"`. Priority: if `dirty` render UNSAVED and DO NOT render the PAID
    group; else if `hasSponsored` render PAID. Keep the auto-shrink `fitFontSize`, the SVG
    structure, and the vector `QrBadge` untouched.
    Add a `bib-preview.test.tsx` assertion: `dirty` renders `unsaved-charm` and suppresses
    `sponsor-charm` even when `hasSponsored` is true.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/bib-preview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "dirty" apps/run.bib/webapp/src/components/BibPreview.tsx` (prop added)
    - `grep -q "unsaved-charm" apps/run.bib/webapp/src/components/BibPreview.tsx`
    - BibForm passes `dirty` to BibPreview and applies a `#6CCDB8` glow when dirty
    - bib-preview test asserts UNSAVED outranks PAID while dirty
    - vitest bib-preview suite green
  </acceptance_criteria>
  <done>Save button glows/enlarges when dirty and an UNSAVED stamp outranks PAID in the preview; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + harden implicit save on checkout</name>
  <files>apps/run.bib/webapp/src/components/SponsorForm.tsx, apps/run.bib/webapp/src/components/BibForm.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/SponsorForm.tsx (onSubmit; the `await flushPendingBibName()` call site relative to the checkout fetch/redirect; both `variant="bib"` and `variant="general"` paths)
    - apps/run.bib/webapp/src/lib/pending-bib-save.ts (registerBibFlusher / flushPendingBibName contract)
    - apps/run.bib/webapp/src/components/BibForm.tsx (registerBibFlusher(onSave) registration; onSave self-guards when not dirty)
    - apps/run.bib/webapp/src/__tests__/sponsor-form.test.ts (existing flush-path test style)
  </read_first>
  <action>
    Confirm and, if needed, harden the existing bridge so BOTH SponsorForm variants commit the
    current bib name before leaving for checkout: (a) `await flushPendingBibName()` MUST precede
    the checkout `fetch`/redirect in `onSubmit`; (b) the flush path is variant-agnostic (fires for
    `bib` and `general`); (c) `BibForm.registerBibFlusher(onSave)` is registered on mount and the
    flusher is cleared on unmount. If all three already hold, make it a no-op verification and rely
    on the test; if any gap exists, move the `await flushPendingBibName()` ahead of the redirect
    and/or fix the registration lifecycle. Do NOT surface a separate visible "saving" hint here —
    the ALTCHA overlay (plan 04) covers the flush's proof-of-work. Keep slider/custom/provider
    logic and checkout endpoints unchanged.
    Add/extend a `sponsor-form.test.ts` case asserting `flushPendingBibName` is awaited before the
    checkout call for both variants (mock the flusher + the checkout fetch and assert call order).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/sponsor-form.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "flushPendingBibName" apps/run.bib/webapp/src/components/SponsorForm.tsx`
    - sponsor-form test asserts flush is awaited before the checkout fetch for both `bib` and `general` variants
    - BibForm registers the flusher on mount and clears it on unmount
    - vitest sponsor-form suite green
  </acceptance_criteria>
  <done>Both Sponsor/Donate variants commit the current name before checkout; flush ordering unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client state → bib name PATCH | Existing boundary (unchanged) — the name save + checkbox toggle already cross to the server-gated PATCH with ALTCHA; this plan only moves client wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08 | Tampering | rain-store singleton | low | accept | Client-only cosmetic state (cash-rain visual); no server trust decision depends on it |
| T-34-09 | Spoofing | implicit bib-name save | low | accept | The flush reuses the existing ALTCHA-gated, session-scoped PATCH; this plan changes only call ordering, not the server gate |
| T-34-SC | Tampering | npm installs | low | accept | No new packages introduced by this plan |
</threat_model>

<verification>
- `npx vitest run src/__tests__/bib-preview.test.tsx src/__tests__/sponsor-form.test.ts` green.
- `npx next build` green in apps/run.bib/webapp.
- Manual: resize the orderform — checkbox sits between the tiles on mobile and full-width below
  on desktop; checking it hides Sponsor and rains cash over the bib; a dirty name glows Save and
  stamps UNSAVED (outranking PAID); clicking Sponsor/Donate saves the name first.
</verification>

<success_criteria>
SC34.4 (responsive checkbox + rain bridge), SC34.5 (loud unsaved state), SC34.6 (hardened
implicit save), and the SC34.9 build/test gate for this slice.
</success_criteria>

<output>
Create `.planning/workstreams/v1-8-bib-admin/phases/34-bib-admin-orderform/34-03-SUMMARY.md` when done.
</output>
