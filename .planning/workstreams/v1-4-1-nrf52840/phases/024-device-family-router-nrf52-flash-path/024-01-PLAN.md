---
phase: 024-device-family-router-nrf52-flash-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.flash/webapp/src/types/device.ts
  - apps/run.flash/webapp/src/config/devices.ts
  - apps/run.flash/webapp/src/hooks/use-flash.ts
  - apps/run.flash/webapp/src/hooks/use-flash-esp32.ts
  - apps/run.flash/webapp/src/hooks/use-flash-nrf52.ts
  - apps/run.flash/webapp/Dockerfile.webapp
autonomous: true
requirements:
  - DEVC-07
  - DPLY-07
must_haves:
  truths:
    - "getDeviceFamily(device) returns 'esp32' for esp32/esp32-s3/esp32-c3/esp32-c6 and 'nrf52' for nrf52840"
    - "useFlash is a router: given an ESP32 device it delegates to useFlashEsp32; given an nRF52 device it delegates to useFlashNrf52"
    - "useFlashEsp32 is a byte-identical extract of the pre-phase useFlash body — reviewer can eyeball git-diff --stat as pure move"
    - "useFlashNrf52 is a typed stub that throws 'nRF52 flash not yet implemented' (Plan 24-02 replaces the body)"
    - "A clean docker build resolves nrf52840 from api.meshtastic.org/resource/deviceHardware and lands the T-1000E slug in /public/data/hardware-list.json"
    - "Dockerfile Stage 1 downloads firmware-nrf52840-{version}.zip and unzips *.uf2 alongside the existing *.factory.bin extraction"
    - "Recommended set is unchanged (5 ESP32 slugs) — no T-1000E promotion in this phase"
    - "DPLY-06 offline-guarantee grep gate still passes (no api.meshtastic.org / github.com/meshtastic strings in .next/standalone or .next/static)"
  artifacts:
    - path: apps/run.flash/webapp/src/types/device.ts
      provides: "getDeviceFamily helper + NRF52_ARCHITECTURES constant"
      contains: "NRF52_ARCHITECTURES"
    - path: apps/run.flash/webapp/src/hooks/use-flash-esp32.ts
      provides: "ESP32 flash pipeline — verbatim move of prior useFlash body"
      min_lines: 180
    - path: apps/run.flash/webapp/src/hooks/use-flash-nrf52.ts
      provides: "nRF52 flash pipeline stub with UseFlashReturn shape"
      min_lines: 20
    - path: apps/run.flash/webapp/src/hooks/use-flash.ts
      provides: "Family-aware router hook"
    - path: apps/run.flash/webapp/Dockerfile.webapp
      provides: "Stage 1 extended for nrf52840 architecture + .uf2 extract + hardware-list filter"
  key_links:
    - from: apps/run.flash/webapp/src/hooks/use-flash.ts
      to: apps/run.flash/webapp/src/types/device.ts
      via: "getDeviceFamily(device) import + dispatch"
      pattern: "getDeviceFamily"
    - from: apps/run.flash/webapp/src/hooks/use-flash.ts
      to: apps/run.flash/webapp/src/hooks/use-flash-esp32.ts
      via: "delegate hook call when family === 'esp32'"
      pattern: "useFlashEsp32"
    - from: apps/run.flash/webapp/src/hooks/use-flash.ts
      to: apps/run.flash/webapp/src/hooks/use-flash-nrf52.ts
      via: "delegate hook call when family === 'nrf52'"
      pattern: "useFlashNrf52"
    - from: apps/run.flash/webapp/Dockerfile.webapp
      to: apps/run.flash/webapp/public/data/hardware-list.json
      via: "Stage 1 jq filter admits nrf52840 → hardware-list contains T-1000E slug"
      pattern: "nrf52840"
---

<objective>
Split `useFlash` into a family-aware router (dispatching by `deviceHardware.architecture`) and extend `Dockerfile.webapp` Stage 1 so the T-1000E slug and a `firmware-t1000-e-{version}.uf2` artifact ship alongside the ESP32 `.factory.bin` set — with zero regression on the current ESP32 flash pipeline and no violation of the DPLY-06 offline gate.

Purpose: Establishes the router contract Plan 24-02 fills in, and puts the nRF52 firmware bytes on disk so the Web USB DFU write path in Plan 24-02 has something to send. This is the "wire the socket" plan.

Output:
- `src/types/device.ts` extended with `NRF52_ARCHITECTURES` + `getDeviceFamily`
- `src/hooks/use-flash-esp32.ts` (new, verbatim extract of prior `useFlash` body)
- `src/hooks/use-flash-nrf52.ts` (new, typed stub matching `UseFlashReturn`)
- `src/hooks/use-flash.ts` (rewritten as thin dispatch router)
- `src/config/devices.ts` (adds `TODO(v1.4.1 close-out)` above `RECOMMENDED_SLUGS`)
- `Dockerfile.webapp` Stage 1 (jq filter + download loop + `.uf2` unzip)
- Hardware-list JSON regenerated at build now surfaces T-1000E
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Phase 24 CONTEXT.md locks all architectural decisions for this plan. Read it first:

@.planning/workstreams/v1-4-1-nrf52840/phases/024-device-family-router-nrf52-flash-path/024-CONTEXT.md
@.planning/workstreams/v1-4-1-nrf52840/ROADMAP.md

Existing source files this plan touches or reads:

@apps/run.flash/webapp/src/hooks/use-flash.ts
@apps/run.flash/webapp/src/types/device.ts
@apps/run.flash/webapp/src/types/serial.ts
@apps/run.flash/webapp/src/config/devices.ts
@apps/run.flash/webapp/src/config/firmware.ts
@apps/run.flash/webapp/Dockerfile.webapp

<interfaces>
<!-- Extracted from the codebase. Executor uses these directly; no exploration needed. -->

From src/types/device.ts (existing):
```typescript
export interface DeviceHardware {
  hwModel: number;
  hwModelSlug: string;
  platformioTarget: string;
  architecture: string;
  activelySupported: boolean;
  displayName: string;
  supportLevel?: number;
  tags?: string[];
  images?: string[];
  partitionScheme?: string;
  requiresDfu?: boolean;
  hasMui?: boolean;
  hasInkHud?: boolean;
}

export const ESP32_ARCHITECTURES = ["esp32", "esp32-s3", "esp32-c3", "esp32-c6"];
export function isEsp32Device(device: DeviceHardware): boolean;
```

From src/hooks/use-flash.ts (existing — the shape both delegates must conform to):
```typescript
interface UseFlashReturn {
  progress: FlashProgress;
  isFlashing: boolean;
  isComplete: boolean;
  isError: boolean;
  flash: (
    espLoader: ESPLoader,
    device: DeviceHardware,
    appendLog: (text: string) => void
  ) => Promise<void>;
  reset: () => void;
}
```

Note: the ESP32 delegate keeps the `ESPLoader` transport argument literal. The nRF52 delegate will take a different transport shape (Plan 24-02 defines it). The router's exported `flash` signature stays permissive — see Task 2 below.

From src/types/serial.ts (existing):
`FlashProgress`, `FlashStage`, and `INITIAL_FLASH_PROGRESS` are reused unchanged. Do not add nRF52-specific stages here; reuse `writing` → `verifying` → `complete` with `eraseComplete: true` seeded by the nRF52 hook (Plan 24-02).

From src/config/devices.ts (existing):
```typescript
export const RECOMMENDED_SLUGS = new Set([
  "HELTEC_V3", "TBEAM", "TLORA_V2_1_1P6", "RAK4631", "STATION_G2",
]);
```
Do NOT add T-1000E to this set — CONTEXT Decision 5 explicitly freezes it at 5 ESP32 slugs pending Phase 25 SC4 hardware verify.

From Dockerfile.webapp Stage 1 (existing shape to extend):
- jq architecture filter at line 38 currently: `select(.architecture == "esp32" or .architecture == "esp32-s3" or .architecture == "esp32-c3" or .architecture == "esp32-c6")`
- Firmware download loop at line 23: `for ARCH in esp32 esp32s3 esp32c3 esp32c6`
- Unzip step at line 27: `unzip -q -o "/tmp/${ZIP}" "firmware-*.factory.bin" -d /firmware/`
- Offline gate at line 95: `grep -rE 'api\.meshtastic\.org|github\.com/meshtastic' .next/standalone .next/static`

Note that the firmware zip naming convention Meshtastic uses is `firmware-{arch-with-no-dash}-{version}.zip` — so `nrf52840` matches the loop variable directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device-family discriminator + router extraction (per D-01, D-03, D-05)</name>
  <files>
    apps/run.flash/webapp/src/types/device.ts,
    apps/run.flash/webapp/src/config/devices.ts,
    apps/run.flash/webapp/src/hooks/use-flash.ts,
    apps/run.flash/webapp/src/hooks/use-flash-esp32.ts,
    apps/run.flash/webapp/src/hooks/use-flash-nrf52.ts
  </files>
  <action>
    Step A — extend `src/types/device.ts`: add `export const NRF52_ARCHITECTURES = ["nrf52840"] as const;` immediately below `ESP32_ARCHITECTURES`. Add `export function isNrf52Device(device: DeviceHardware): boolean` mirroring `isEsp32Device`. Add `export type DeviceFamily = "esp32" | "nrf52";` and `export function getDeviceFamily(device: DeviceHardware): DeviceFamily` that returns `"esp32"` when `isEsp32Device(device)`, `"nrf52"` when `isNrf52Device(device)`, and throws `Error("Unsupported device architecture: ${device.architecture}")` otherwise. Fail-fast per CONTEXT Decision 3 — no silent ESP32 fallback.

    Step B — annotate `src/config/devices.ts`: insert a single-line comment above `RECOMMENDED_SLUGS`: `// TODO(v1.4.1 close-out): promote T-1000E (hwModelSlug SEEED_TRACKER_T1000_E) after Phase 25 SC4 hardware verify.` Do NOT modify the set contents. Per CONTEXT Decision 5.

    Step C — create `src/hooks/use-flash-esp32.ts` as a VERBATIM extract of the current `useFlash` implementation from `src/hooks/use-flash.ts`. Rename the exported function to `useFlashEsp32`. Keep all imports, the `UseFlashReturn` interface (renamed to `UseFlashEsp32Return`), the state, refs, `flash` callback body (all three stages including the `tlora-t3s3 → 'dio'` quirk at lines 104–106), the `beforeunload` effect, and the `reset` callback IDENTICAL to the pre-phase version. The reviewer must be able to run `git log -p` and see this file's content is a pure move. Do not "clean up" or "modernise" anything.

    Step D — create `src/hooks/use-flash-nrf52.ts` as a typed stub. Export `useFlashNrf52(): UseFlashNrf52Return` that returns the same-shape object as the ESP32 hook but with `flash` implemented as `async () => { throw new Error("nRF52 flash not yet implemented — Plan 24-02"); }`. Use `INITIAL_FLASH_PROGRESS` for state. Include a top-of-file comment: `// STUB — Plan 24-02 replaces the flash body with the Web USB DFU write path.` The transport argument type is `unknown` in the stub (Plan 24-02 replaces with the concrete `DfuDevice` type).

    Step E — rewrite `src/hooks/use-flash.ts` as a thin router. Delete the previous body. New content:
    - Imports: `useFlashEsp32`, `useFlashNrf52`, `getDeviceFamily`, `DeviceHardware`, `FlashProgress`, `ESPLoader`.
    - Export `interface UseFlashReturn` with the SAME public shape as before (`progress`, `isFlashing`, `isComplete`, `isError`, `flash`, `reset`) so no consumer changes are required. The `flash` argument type widens: `flash: (transport: ESPLoader | unknown, device: DeviceHardware, appendLog: (text: string) => void) => Promise<void>` — Plan 24-02 will narrow `unknown` to the concrete DFU device type.
    - Inside `useFlash()`, always call BOTH `useFlashEsp32()` and `useFlashNrf52()` unconditionally at the top level (React rules-of-hooks — no conditional hook calls). The router's returned `progress`/`isFlashing`/`isComplete`/`isError` derive from whichever delegate is currently active (track "which family last called flash" in a ref, default to ESP32 shape until first call). The router's `flash(transport, device, appendLog)` reads `getDeviceFamily(device)` and delegates to the matching hook's `flash`. The router's `reset` calls both delegates' `reset`.

    Do NOT modify `RECOMMENDED_SLUGS`, `hardware-list.json` source, or any callers of `useFlash` — the public shape is preserved by design.

    After the edit: run `cd apps/run.flash/webapp && npx tsc --noEmit` to prove types are clean.
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q 'NRF52_ARCHITECTURES' src/types/device.ts &amp;&amp; grep -q 'getDeviceFamily' src/types/device.ts &amp;&amp; grep -q 'useFlashEsp32' src/hooks/use-flash.ts &amp;&amp; grep -q 'useFlashNrf52' src/hooks/use-flash.ts &amp;&amp; grep -q 'nRF52 flash not yet implemented' src/hooks/use-flash-nrf52.ts &amp;&amp; grep -q "TODO(v1.4.1 close-out)" src/config/devices.ts &amp;&amp; grep -cE '"HELTEC_V3"|"TBEAM"|"TLORA_V2_1_1P6"|"RAK4631"|"STATION_G2"' src/config/devices.ts | grep -q '^5$'</automated>
  </verify>
  <done>
    `tsc --noEmit` is clean; `NRF52_ARCHITECTURES` + `getDeviceFamily` exported from `types/device.ts`; `use-flash.ts` no longer contains the erase/write/verify stages inline (it now only routes); `use-flash-esp32.ts` contains the full pipeline including the `tlora-t3s3 → 'dio'` quirk; `use-flash-nrf52.ts` throws "not yet implemented" from `flash`; `RECOMMENDED_SLUGS` still contains exactly the 5 ESP32 slugs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile Stage 1 extension — nrf52840 architecture, .uf2 extract, hardware-list filter (per D-04, DPLY-07)</name>
  <files>apps/run.flash/webapp/Dockerfile.webapp</files>
  <action>
    Extend `Dockerfile.webapp` Stage 1 (the `FROM node:current-alpine AS firmware` stage). Three edits, all inside that stage:

    1. Firmware download loop — extend the `for ARCH in esp32 esp32s3 esp32c3 esp32c6` clause to `for ARCH in esp32 esp32s3 esp32c3 esp32c6 nrf52840`. Meshtastic ships `firmware-nrf52840-{version}.zip` at the same GitHub release URL as the ESP32 zips.

    2. After the existing `unzip -q -o "/tmp/${ZIP}" "firmware-*.factory.bin" -d /firmware/ 2>/dev/null || true;` line, add a PARALLEL unzip step in the same `for` iteration for `.uf2` artifacts: `unzip -q -o "/tmp/${ZIP}" "firmware-*.uf2" -d /firmware/ 2>/dev/null || true;`. This runs for every architecture but only produces files where a `.uf2` is actually present in the zip (nrf52840). The `.uf2` files land in `/firmware/` alongside the `.factory.bin` set; both get copied to `public/firmware/` in Stage 2 (line 66 of Dockerfile).

    3. Extend the hardware-list jq architecture filter (currently at line 38): change the `select()` expression to include `or .architecture == "nrf52840"`. Full new filter: `[.[] | select(.architecture == "esp32" or .architecture == "esp32-s3" or .architecture == "esp32-c3" or .architecture == "esp32-c6" or .architecture == "nrf52840")]`. This causes `hardware-list.json` regenerated at build to include the T-1000E slug (`SEEED_TRACKER_T1000_E`).

    Do NOT modify: the offline-gate grep at Stage 2 line 95 (DPLY-06 — verified untouched by CONTEXT Decision 4 rationale, since `.uf2` binaries land in `/firmware` → `public/firmware/` not in `.next/standalone` / `.next/static`), Stage 2 build steps, Stage 3 runtime layer, or the `FIRMWARE_VERSION` build-arg plumbing.

    After the edit: run `docker build --no-cache -f apps/run.flash/webapp/Dockerfile.webapp -t dc34-run-flash-app:phase24-p1 apps/run.flash/webapp/` in a scratch shell. Confirm the Stage 1 log line reports at least one `.uf2` extracted and that `hardware-list.json` contains the T-1000E slug. Confirm the offline-gate grep at Stage 2 still passes (build succeeds).
  </action>
  <verify>
    <automated>grep -q 'nrf52840' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'firmware-\*.uf2' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'or .architecture == "nrf52840"' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; docker build --no-cache -f apps/run.flash/webapp/Dockerfile.webapp -t dc34-run-flash-app:phase24-p1 apps/run.flash/webapp/ 2>&amp;1 | tee /tmp/build24-1.log | grep -qE 'Extracted [0-9]+ firmware binaries' &amp;&amp; docker run --rm dc34-run-flash-app:phase24-p1 sh -c 'grep -q SEEED_TRACKER_T1000_E /app/public/data/hardware-list.json &amp;&amp; ls /app/public/firmware/ | grep -qE "\.uf2$"'</automated>
  </verify>
  <done>
    `docker build` succeeds on the current Meshtastic stable release; Stage 1 log shows `.uf2` artifacts extracted; the built image's `/app/public/data/hardware-list.json` contains `SEEED_TRACKER_T1000_E`; at least one `firmware-*.uf2` is present in `/app/public/firmware/`; Stage 2 offline-gate grep passes; the ESP32 factory-bin extraction is not broken (Recommended slugs still ship binaries).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api.meshtastic.org → Docker build | Build-time fetch of firmware release list and hardware-device catalog (already in scope from v1.4). |
| github.com/meshtastic → Docker build | Build-time download of firmware zip artifacts. |
| Docker Hub → base image | `node:current-alpine` pulled from Docker Hub. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24.1-01 | Tampering | api.meshtastic.org firmware-list JSON | accept | Same trust posture as v1.4 (shipped). Build reproducibility is bounded by upstream honesty — no new attack surface introduced. |
| T-24.1-02 | Tampering | github.com/meshtastic zip artifacts | accept | Same trust posture as v1.4 (shipped). Adding `nrf52840` zip does not change the trust model. |
| T-24.1-03 | Denial-of-service | Dockerfile Stage 1 fails on unreachable nrf52840 zip | mitigate | Existing `|| { echo "Warning: ${ZIP} not found"; continue; }` guard applies to the extended loop unchanged — a missing `nrf52840` zip warns and continues rather than failing the build. |
| T-24.1-04 | Information disclosure | `.uf2` artifacts leak meshtastic hostnames into client bundle | mitigate | DPLY-06 grep gate at Stage 2 continues to scan `.next/standalone` + `.next/static`. `.uf2` binaries land in `public/firmware/` (Stage 2 line 66) which is outside the grep scope but also outside the concern — binaries are opaque device firmware, not JS code. |
| T-24.1-SC | Tampering | npm supply chain | accept | No new npm packages installed in this plan. Existing lockfile applies. |
</threat_model>

<verification>
1. `tsc --noEmit` clean in `apps/run.flash/webapp/`.
2. `git log -p -- apps/run.flash/webapp/src/hooks/use-flash-esp32.ts` shows the file was created with the pre-phase `useFlash` body verbatim (renamed hook, no logic edits).
3. `git diff --stat apps/run.flash/webapp/src/hooks/use-flash.ts` shows the file shrunk substantially (router only).
4. Clean `docker build` completes; built image's `hardware-list.json` contains `SEEED_TRACKER_T1000_E`.
5. Built image's `public/firmware/` contains at least one `firmware-*.uf2`.
6. Stage 2 offline-gate grep still passes.
</verification>

<success_criteria>
- ESP32 flash pipeline continues to work byte-identically (extract-and-move, no logic change).
- Family discriminator `getDeviceFamily(device)` returns correct family for all ESP32 architectures and `nrf52840`; throws on unknown.
- `useFlash` exports the same public shape as before (no consumer changes required).
- Recommended set is unchanged (5 ESP32 slugs). T-1000E slug appears in `hardware-list.json` but is NOT in Recommended.
- Dockerfile Stage 1 admits `nrf52840` in both the firmware download loop and the hardware-list jq filter.
- `.uf2` firmware artifacts ship in `/app/public/firmware/` alongside `.factory.bin` set.
- DPLY-06 offline-gate grep passes at Stage 2.
- `next build` + `tsc --noEmit` clean (SC5 targeted).
</success_criteria>

<output>
Create `.planning/workstreams/v1-4-1-nrf52840/phases/024-device-family-router-nrf52-flash-path/024-01-SUMMARY.md` when done, recording:
- The router shape (function signature, dispatch logic)
- The extracted `useFlashEsp32` LOC count (should match pre-phase `useFlash` body ± the interface rename)
- Confirmation of `docker build` success + hardware-list contents
- Any deviations from CONTEXT.md decisions and the reason
</output>
