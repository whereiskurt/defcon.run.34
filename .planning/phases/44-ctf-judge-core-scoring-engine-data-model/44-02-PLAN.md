---
phase: 44-ctf-judge-core-scoring-engine-data-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/lib/ctf-scoring.ts
  - apps/run.human/webapp/src/lib/ctf-hash.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-scoring.test.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-hash.test.ts
autonomous: true
requirements: [CTF-02, CTF-04]
must_haves:
  truths:
    - "computePoints(n, ctf, now) applies the active time-tier ceiling as the curve max, declines linearly to the floor across N, adds firstBloodBonus at n==1, and returns 0 for n>N."
    - "activeTierCeiling(now, tiers) returns the first half-open [from,to) match's ceiling, else null, and never throws on empty/garbage tiers."
    - "hashAnswer(raw) is a deterministic salted hash; verifyAnswer(guess, answerHash) is a constant-time compare; the raw answer is not recoverable from the stored hash."
  artifacts:
    - "apps/run.human/webapp/src/lib/ctf-scoring.ts"
    - "apps/run.human/webapp/src/lib/ctf-hash.ts"
    - "apps/run.human/webapp/src/lib/__tests__/ctf-scoring.test.ts"
    - "apps/run.human/webapp/src/lib/__tests__/ctf-hash.test.ts"
  key_links:
    - "computePoints must take an injectable `now` so tier-boundary tests are deterministic."
    - "The linear decline is isolated in computePoints so a curved decline is a one-function swap later."
---

<objective>
Build the two pure, unit-testable primitives the judge composes: the scoring engine (`computePoints` + `activeTierCeiling`, CTF-02) and the answer-hashing seam (`hashAnswer` + `verifyAnswer`, CTF-04). Both are structural/pure — they take plain objects, own no DynamoDB, and are fully covered by boundary tests. This plan is parallel-safe with 44-01 (zero file overlap).

Purpose: the judge (44-03) calls `computePoints(n, ctf, now)` after allocating an ordinal and calls `verifyAnswer(guess, ctf.answerHash)` to validate; the admin CRUD in Phase 47 will call `hashAnswer` on save. Isolating the linear curve and the hashing choice in one function each keeps the later swap (curved decline, KDF upgrade) a single-file change.

Output: `src/lib/ctf-scoring.ts`, `src/lib/ctf-hash.ts`, and their test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md
@apps/run.human/webapp/src/lib/live-lockout.test.ts
@apps/run.human/webapp/src/entities/run-user.ts
</context>

<artifacts_this_phase_produces>
This plan (44-02) produces:
- `src/lib/ctf-scoring.ts`: `activeTierCeiling(now, timeTiers)`, `computePoints(n, ctf, now?)`, and a structural `ScoringConfig` type (`pointMax`, `pointFloor`, `maxSolves`, `firstBloodBonus`, `timeTiers`). NO import of the entities — the type is structural so this stays parallel with 44-01; the loaded `Ctf` row satisfies it by shape.
- `src/lib/ctf-hash.ts`: `hashAnswer(raw: string): string`, `verifyAnswer(guess: string, answerHash: string): boolean` (constant-time). Uses Node `crypto` only (no new dependency).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoring engine — activeTierCeiling + computePoints</name>
  <files>apps/run.human/webapp/src/lib/ctf-scoring.ts</files>
  <read_first>
    - .planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md (§Scoring — the LOCKED formula and tier semantics; copy the math exactly)
    - apps/run.human/webapp/src/lib/live-lockout.test.ts (the injectable-clock DI convention used across this codebase — mirror it: pass `now` as a parameter, default to Date.now())
  </read_first>
  <behavior>
    - activeTierCeiling: `now` inside a tier's half-open `[from,to)` → that tier's `ceiling`; overlapping tiers → first match wins; no match / empty / undefined tiers → null; garbage/invalid ISO in a tier → treated as non-matching, never throws.
    - computePoints(n, ctf, now): n>maxSolves → 0; ceiling = activeTierCeiling(now, timeTiers) ?? pointMax; span = ceiling - pointFloor; frac = maxSolves==1 ? 1 : 1-(n-1)/(maxSolves-1); base = pointFloor + span*frac; +firstBloodBonus when n==1; return round(base)+bonus.
    - Boundary cases proven: n==1 first-blood adds bonus and sits at the ceiling; n==N lands on pointFloor (before bonus); n==N+1 → 0; N==1 → full ceiling + bonus; in-window tier ceiling overrides pointMax vs. out-of-window falls back to pointMax.
  </behavior>
  <action>
    Create `src/lib/ctf-scoring.ts`. Define a structural `ScoringConfig` type with `pointMax` (number), `pointFloor` (number), `maxSolves` (number), `firstBloodBonus` (number), and `timeTiers` (array of `{ from: string; to: string; ceiling: number }` — optional). Export `activeTierCeiling(now: Date | number, tiers?: TimeTier[]): number | null` returning the `ceiling` of the first tier whose half-open `[from, to)` interval (UTC-ISO parsed) contains `now`, else `null`; it MUST be total (wrap the `Date.parse` so an unparseable `from`/`to` is skipped, never thrown). Export `computePoints(n: number, ctf: ScoringConfig, now?: Date | number): number` implementing the LOCKED formula from CONTEXT §Scoring exactly (early-return 0 when `n > ctf.maxSolves`; ceiling via `activeTierCeiling(...) ?? ctf.pointMax`; linear `frac`; `Math.round(base)` + first-blood bonus). Default `now` to `Date.now()`. Keep the linear-decline line isolated with a short comment marking it as the future curved-swap point. Do NOT import the entities — the type is structural (the loaded Ctf row satisfies it). No I/O. Reference: CTF-02.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ctf-scoring.ts` exports `activeTierCeiling`, `computePoints`, and `ScoringConfig`.
    - `computePoints` returns 0 for `n > maxSolves` and adds `firstBloodBonus` only when `n==1`.
    - `activeTierCeiling` returns `null` on no-match/empty/garbage and does not throw.
    - No entity import; no network I/O; `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The scoring math is a pure, clock-injectable function matching the locked formula.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Answer hashing seam — hashAnswer + verifyAnswer</name>
  <files>apps/run.human/webapp/src/lib/ctf-hash.ts</files>
  <read_first>
    - .planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md (§Hashing & hygiene — salted SHA-256, document the salt choice, no password-grade KDF required)
    - apps/run.human/webapp/src/entities/run-user.ts (existing `createHash("sha256")` usage from Node `crypto` — reuse this dependency, add nothing new)
  </read_first>
  <behavior>
    - hashAnswer(raw): trims + lowercases the input (challenge answers are case-insensitive to match the lowercase-normalized challenge convention), applies a salt, returns a stable hex digest; same input → same output; different input → different output.
    - verifyAnswer(guess, answerHash): recomputes hashAnswer(guess) and compares to answerHash in constant time (crypto.timingSafeEqual over equal-length buffers); returns false (never throws) on empty/malformed answerHash.
    - The raw answer is not present in the digest output (a leaked hash does not hand over the flag).
  </behavior>
  <action>
    Create `src/lib/ctf-hash.ts` using Node `crypto` only (`createHash`, `timingSafeEqual`). Export `hashAnswer(raw: string): string` = salted SHA-256 hex of the normalized answer: normalize with the same trim+lowercase rule used for challenge names, prepend/append a salt sourced from `process.env.CTF_ANSWER_SALT` with a documented in-code default fallback (a per-app static salt is acceptable per CONTEXT — the goal is "a table leak doesn't hand over flags", not password-grade KDF; add a comment stating the choice and that upgrading to a KDF later is a single-file change). Export `verifyAnswer(guess: string, answerHash: string): boolean` that recomputes `hashAnswer(guess)` and compares constant-time via `timingSafeEqual` on equal-length `Buffer`s, returning `false` (not throwing) when `answerHash` is empty or a different length. Do NOT invent crypto; do NOT log `guess`. Reference: CTF-04.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ctf-hash.ts` exports `hashAnswer` and `verifyAnswer`; imports only from Node `crypto`.
    - Salt source + choice is documented in-code; no new npm dependency added.
    - `verifyAnswer` uses `timingSafeEqual` and returns false (no throw) on malformed `answerHash`.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Answers are stored/validated as salted hashes with a constant-time compare; the raw answer never appears in output.</done>
</task>

<task type="auto">
  <name>Task 3: Boundary tests for scoring and hashing</name>
  <files>apps/run.human/webapp/src/lib/__tests__/ctf-scoring.test.ts, apps/run.human/webapp/src/lib/__tests__/ctf-hash.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/ctf-scoring.ts (functions under test — Task 1)
    - apps/run.human/webapp/src/lib/ctf-hash.ts (functions under test — Task 2)
    - apps/run.human/webapp/src/lib/__tests__/qr-admin.test.ts (the `describe`/`it.each` vitest style used in this app)
    - .planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md (§Success Criteria 1 + 3 — the exact boundary set to cover)
  </read_first>
  <action>
    Create `src/lib/__tests__/ctf-scoring.test.ts` covering SC-1 with a fixed injected `now`: a two-tier `ctf` fixture; assert `activeTierCeiling` returns the in-window ceiling, `null` outside all windows, first-match-wins on overlap, and no-throw on an empty and a garbage-ISO tier. Assert `computePoints` for: `n==1` first-blood (ceiling + bonus), `n==N` (pointFloor, no bonus), `n==N+1` (0), `N==1` (full ceiling + bonus), in-window tier ceiling overriding `pointMax`, and out-of-window falling back to `pointMax`. Use exact numeric fixtures so the rounding is asserted, not approximated.
    Create `src/lib/__tests__/ctf-hash.test.ts`: `hashAnswer` is deterministic (same input twice → equal), case/space-insensitive (`"  FLAG "` == `"flag"`), and different inputs differ; `verifyAnswer(correct, hashAnswer(correct))` is true, a wrong guess is false, an empty/short `answerHash` returns false without throwing; assert the raw answer string is NOT a substring of its own hash output (leak check). Reference: CTF-02, CTF-04, SC-1, SC-3.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp &amp;&amp; nvm use 23.6.0 &gt;/dev/null 2>&amp;1; npx vitest run src/lib/__tests__/ctf-scoring.test.ts src/lib/__tests__/ctf-hash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Scoring test covers all five+ `computePoints` boundaries and both tier paths with a fixed `now`.
    - Hash test proves determinism, case-insensitivity, correct/incorrect verify, malformed-hash safety, and the substring leak check.
    - `vitest run` on both files is green.
  </acceptance_criteria>
  <done>SC-1 and the hashing half of SC-3 are proven by green unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored answerHash → attacker with table read | A table/backup leak must not reveal flags. |
| untrusted guess → verifyAnswer | Guess is attacker-controlled; comparison must not leak timing or throw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-03 | Information Disclosure | stored `answerHash` | high | mitigate | Salted SHA-256; raw answer never in output; substring leak check test (Task 2/3). |
| T-44-04 | Information Disclosure | `verifyAnswer` timing | medium | mitigate | `crypto.timingSafeEqual` constant-time compare (Task 2). |
| T-44-05 | Tampering | scoring input `n` | low | mitigate | `computePoints` caps at `n>N → 0`; pure + boundary-tested (Task 1/3). |
| T-44-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies — Node `crypto` only; no install task. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both new lib files.
- `npx vitest run` green on both test files (`nvm use 23.6.0` first).
- Neither lib file imports the entities or performs I/O (grep confirms no `electroClient`).
</verification>

<success_criteria>
- CTF-02 satisfied: `computePoints`/`activeTierCeiling` correct across all boundaries (SC-1).
- CTF-04 (hashing half) satisfied: salted-hash storage + constant-time verify, raw answer unrecoverable (SC-3).
</success_criteria>

<output>
Create `.planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-02-SUMMARY.md` when done.
</output>
