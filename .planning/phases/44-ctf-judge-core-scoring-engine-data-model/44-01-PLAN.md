---
phase: 44-ctf-judge-core-scoring-engine-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/entities/qr.ts
  - apps/run.human/webapp/src/entities/run-user.ts
  - apps/run.human/webapp/src/entities/ctf.ts
  - apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts
  - apps/run.human/webapp/src/entities/__tests__/ctf-key-parity.test.ts
autonomous: true
requirements: [CTF-01]
must_haves:
  truths:
    - "The Ctf entity carries answerHash, pointMax, pointFloor, maxSolves, firstBloodBonus, timeTiers[], solveCount while still loading legacy rows that only have the old `answer`."
    - "A per-(user,challenge) CtfSolve row is expressible and is unique per user within a challenge partition; 'all my solves' resolves via gsi1."
    - "A CtfPending park row (nonce + submittedFlagHash + TTL) and a short-TTL CtfAttempt counter are expressible."
    - "RunUser carries ctfScore and ctfSolves numeric rollups."
  artifacts:
    - "apps/run.human/webapp/src/entities/qr.ts (Ctf extended)"
    - "apps/run.human/webapp/src/entities/run-user.ts (ctfScore/ctfSolves)"
    - "apps/run.human/webapp/src/entities/ctf.ts (CtfSolve, CtfPending, CtfAttempt)"
    - "apps/run.human/webapp/src/entities/__tests__/ctf-key-parity.test.ts"
  key_links:
    - "Ctf primary key MUST still encode $run#challenge_<c> / $ctf_1 (resolver reads it on the hot path — parity-locked)."
    - "CtfSolve sk attribute_not_exists(sk) is the idempotency key the Phase-44-03 judge relies on."
---

<objective>
Establish the Phase-44 CTF data model on the shared `run-human-electro` table (CTF-01): extend the existing `Ctf` entity with the scoring/secret fields, add the three new run.human-only entities (`CtfSolve`, `CtfPending`, `CtfAttempt`), add the `ctfScore`/`ctfSolves` rollups to `RunUser`, and lock every key shape with offline key-parity tests.

Purpose: everything else in v2.1 (the judge in 44-03, the visible claim page in Phase 45, the covert channel in Phase 46, the admin CRUD + leaderboard in Phase 47) reads or writes these rows. The `Ctf` key stays byte-parity-locked with the resolver mirror; the three new entities are run.human-only (no `.mjs` mirror).

Output: extended `Ctf` + `RunUser`, new `src/entities/ctf.ts`, and key-parity tests proving the encoded DynamoDB keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md
@apps/run.human/webapp/src/entities/qr.ts
@apps/run.human/webapp/src/entities/run-user.ts
@apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts
@apps/run.qr/lambda/resolver/lib/entities.mjs
</context>

<artifacts_this_phase_produces>
This plan (44-01) produces:
- `Ctf` new attributes: `answerHash` (string), `pointMax` (number), `pointFloor` (number), `maxSolves` (number), `firstBloodBonus` (number), `timeTiers` (list of `{ from, to, ceiling }`), `solveCount` (number). Legacy `answer` kept as an optional attribute (migration is Phase 47).
- `RunUser.ctfScore` (number, default 0) and `RunUser.ctfSolves` (number, default 0); mirrored into the `RunUserItem` type.
- New entities in `src/entities/ctf.ts`: `CtfSolve`, `CtfPending`, `CtfAttempt` (+ their exported item types).
- `src/entities/__tests__/ctf-key-parity.test.ts`.
Downstream (44-02, 44-03) add `src/lib/ctf-scoring.ts`, `ctf-hash.ts`, `ctf-log.ts`, `ctf-judge.ts` — NOT in this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the Ctf entity and add RunUser ctf rollups</name>
  <files>apps/run.human/webapp/src/entities/qr.ts, apps/run.human/webapp/src/entities/run-user.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/qr.ts (the `Ctf` entity you extend + the load-bearing parity docstring — read fully before editing)
    - apps/run.qr/lambda/resolver/lib/entities.mjs (the resolver `Ctf` mirror — its KEY shape stays locked; attribute additions do not need mirroring because the resolver forwards without reading these fields)
    - apps/run.human/webapp/src/entities/run-user.ts (attribute + `RunUserItem` patterns; note the atomic default-0 counter `checkInCount`)
    - .planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md (§Entities — the locked attribute list)
  </read_first>
  <action>
    In `src/entities/qr.ts`, ADD to the existing `Ctf` entity attributes (do NOT remove `answer`, `points`, `effect`, `maxAttempts`, `rateLimitWindow`, `enabled`, `createdAt`, `updatedAt`, and do NOT change any `model` field or the `primary` index — the resolver reads this key):
    `answerHash` (string), `pointMax` (number), `pointFloor` (number), `maxSolves` (number), `firstBloodBonus` (number), `solveCount` (number, default 0 — the atomic ordinal allocator), and `timeTiers` as a list whose items are a map with properties `from` (string), `to` (string), `ceiling` (number). Keep `answer` as an optional legacy attribute so existing rows still load; add a short comment that `answerHash` supersedes `answer` and the plaintext→hash migration lands in Phase 47.
    In `src/entities/run-user.ts`, ADD two attributes to `RunUser`: `ctfScore` (number, default 0) and `ctfSolves` (number, default 0) — patterned on the existing `checkInCount` default-0 counter (these are atomic-`ADD` rollups, per CTF-03). ADD both fields (optional numbers) to the exported `RunUserItem` type. Do NOT add any read/write helper here — the judge (44-03) owns the atomic `ADD`.
    Reference: CTF-01, CONTEXT §Entities.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Ctf` attributes include `answerHash`, `pointMax`, `pointFloor`, `maxSolves`, `firstBloodBonus`, `solveCount`, and a `timeTiers` list of `{from,to,ceiling}` maps; `answer` still present and optional.
    - `Ctf.model` (entity/version/service) and the `primary` index are byte-identical to before (unchanged).
    - `RunUser` has `ctfScore` and `ctfSolves` numeric attributes with default 0; `RunUserItem` includes both as optional numbers.
    - `tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>Ctf and RunUser carry the new CTF fields; the Ctf key is unchanged; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Create CtfSolve, CtfPending, and CtfAttempt entities</name>
  <files>apps/run.human/webapp/src/entities/ctf.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/qr.ts (entity authoring style + `electroClient`/`ELECTRO_TABLE` import + gsi1 `gsi1pk-gsi1sk-index` usage on `Qr.byOwner`)
    - apps/run.human/webapp/src/entities/run-user.ts (a second gsi1 example: `byHash`)
    - apps/run.human/webapp/src/entities/client.ts (the `electroClient` + `ELECTRO_TABLE` exports)
    - .planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-CONTEXT.md (§Entities — the locked key composites for CtfSolve/CtfPending)
  </read_first>
  <action>
    Create `src/entities/ctf.ts` importing `electroClient`, `ELECTRO_TABLE` from `./client` (mirror the header of `qr.ts`). Define three entities, all `model.version: "1"`, `model.service: "run"`:
    - `CtfSolve` (entity `"CtfSolve"`) — one row per `(user, challenge)`, the idempotency key. Attributes: `challenge` (string, required), `user` (string, required), `ordinal` (number), `points` (number), `firstBlood` (boolean), `tierCeiling` (number), `channel` (string — values `"qr"`|`"covert"`), `solvedAt` (string), `createdAt`/`updatedAt` (string, same default/`watch:"*"` pattern as `Qr`). Indexes: `primary` pk `field:"pk"` composite `["challenge"]`, sk `field:"sk"` composite `["user"]` (so all solvers of a challenge share a partition and each user is one row); `gsi1` index `"gsi1pk-gsi1sk-index"` pk `field:"gsi1pk"` composite `["user"]`, sk `field:"gsi1sk"` composite `["challenge"]` ("all my solves").
    - `CtfPending` (entity `"CtfPending"`) — park-and-claim for unauth covert hits (Phase 46 consumes). Attributes: `nonce` (string, required), `challenge` (string), `submittedFlagHash` (string — a hash, NEVER the raw guess), `createdAt` (string), `ttl` (number — DynamoDB TTL epoch seconds). Index: `primary` pk `field:"pk"` composite `["nonce"]`, sk `field:"sk"` composite `[]`. CONTEXT documents the intended shape as `$run#ctfpending_<nonce>` / `$ctfpending_1`; because CtfPending has NO resolver mirror, its exact encoding is run.human-internal — Task 3 pins whatever ElectroDB emits as the contract Phase 46 reads.
    - `CtfAttempt` (entity `"CtfAttempt"`) — the short-TTL per-user attempt counter that CTF-03 step 2 requires. Attributes: `challenge` (string, required), `user` (string, required), `count` (number, default 0), `expiresAt` (number), `ttl` (number — TTL epoch seconds). Index: `primary` pk `field:"pk"` composite `["challenge"]`, sk `field:"sk"` composite `["user"]`.
    Export item types (`CtfSolveItem`, `CtfPendingItem`, `CtfAttemptItem`) hand-authored like `RunUserItem`. Do NOT add DB-mutating helpers here — the judge (44-03) owns the conditional-put / atomic-ADD orchestration behind its store seam. Keep the file schema-only (single file, boring, well under the simplicity budget).
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/entities/ctf.ts` exports `CtfSolve`, `CtfPending`, `CtfAttempt` entities plus `CtfSolveItem`/`CtfPendingItem`/`CtfAttemptItem` types.
    - `CtfSolve` has a `gsi1pk-gsi1sk-index` gsi1 keyed pk=`user`, sk=`challenge`.
    - `CtfPending` carries `submittedFlagHash` and `ttl`; no raw-guess attribute exists.
    - File contains no `electroClient` write calls (schema only); `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The three new entities compile and are importable; the judge in 44-03 can build its store on them.</done>
</task>

<task type="auto">
  <name>Task 3: Key-parity tests for the extended Ctf and the new entities</name>
  <files>apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts, apps/run.human/webapp/src/entities/__tests__/ctf-key-parity.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts (the `.params({table}).Key` offline-encoding assertion pattern — no DB call; extend it)
    - apps/run.human/webapp/src/entities/ctf.ts (the entities under test — created in Task 2)
    - apps/run.human/webapp/src/entities/qr.ts (the extended `Ctf` under test — Task 1)
    - apps/run.human/webapp/vitest.config.mts (the `@` alias so tests resolve app imports)
  </read_first>
  <action>
    Extend `src/entities/__tests__/qr-key-parity.test.ts`: keep the existing assertion that `Ctf.get({challenge:"sao"})` still encodes `pk: "$run#challenge_sao", sk: "$ctf_1"` (proves the extension did not move the key). Add one assertion that a `Ctf.put({...})` payload including the new scoring attributes (`answerHash`, `pointMax`, `pointFloor`, `maxSolves`, `firstBloodBonus`, `solveCount`, a one-entry `timeTiers`) encodes offline via `.params({table})` without error (validates the entity accepts the new shape — `.params()` performs no network I/O).
    Create `src/entities/__tests__/ctf-key-parity.test.ts` importing the three entities from `@/entities/ctf`. Assert the offline-encoded `.params({table}).Key` for:
    - `CtfSolve.get({challenge:"sao", user:"user-123"})` → pk `$run#challenge_sao`, sk `$ctfsolve_1#user_user-123`.
    - `CtfSolve.query.gsi1(...)` (or the named gsi1 accessor) uses `IndexName` `gsi1pk-gsi1sk-index` and the `:pk` value `$run#user_user-123`.
    - `CtfPending.get({nonce:"n-abc"})` and `CtfAttempt.get({challenge:"sao", user:"user-123"})` — assert against the ACTUAL ElectroDB-emitted `.params({table}).Key` (compute it once, then hard-code the expected string) so the test documents the exact key contract Phase 46 (CtfPending) and the judge (CtfAttempt) depend on.
    Reference: CTF-01 SC-4.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp &amp;&amp; nvm use 23.6.0 &gt;/dev/null 2>&amp;1; npx vitest run src/entities/__tests__/ctf-key-parity.test.ts src/entities/__tests__/qr-key-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `qr-key-parity.test.ts` still passes the `$run#challenge_sao`/`$ctf_1` assertion after the extension.
    - `ctf-key-parity.test.ts` asserts CtfSolve primary `$run#challenge_sao` / `$ctfsolve_1#user_user-123` and gsi1 index name + `$run#user_user-123`.
    - CtfPending and CtfAttempt keys are pinned to their exact emitted encoding.
    - `vitest run` on both files is green.
  </acceptance_criteria>
  <done>All CTF key shapes are locked by passing offline key-parity tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver (q.defcon.run) → shared table | The resolver reads `Ctf` on its hot path; a drifted key silently 404s every scan. |
| future front doors → CtfSolve partition | Solve rows are the auditable source of truth for scoring; their key shape gates idempotency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Tampering | `Ctf` primary key drift | high | mitigate | Key-parity test asserts `$run#challenge_<c>`/`$ctf_1` unchanged after the attribute extension (Task 3). |
| T-44-02 | Information Disclosure | `CtfPending.submittedFlagHash` | high | mitigate | Store a hash, never the raw guess; entity has no raw-guess attribute (Task 2). |
| T-44-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies — entities use existing `electrodb` + Node `crypto`; no install task in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the three edited/created entity files.
- `npx vitest run` on both key-parity test files green (`nvm use 23.6.0` first).
- Grep confirms the `Ctf.model` block and `primary` index in `qr.ts` are unchanged from the resolver mirror.
</verification>

<success_criteria>
- CTF-01 satisfied: `Ctf` extended (legacy `answer` retained), `CtfSolve`/`CtfPending`/`CtfAttempt` created, `RunUser.ctfScore`/`ctfSolves` added, and all key shapes proven by tests.
</success_criteria>

<output>
Create `.planning/phases/44-ctf-judge-core-scoring-engine-data-model/44-01-SUMMARY.md` when done.
</output>
