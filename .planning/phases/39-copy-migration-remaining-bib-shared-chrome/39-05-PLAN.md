---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 05
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - apps/run.human/webapp/src/components/header/header.tsx
  - apps/run.human/webapp/src/components/header/dropdown-user.tsx
  - apps/run.human/webapp/src/components/header/dropdown-menu.tsx
  - apps/run.human/webapp/src/components/footer.tsx
autonomous: true
requirements: [MIGR-03]
user_setup: []

must_haves:
  truths:
    - run.human's header nav, mobile menu, profile menu, and footer render their labels through useCopy()/t() from the SAME common.* keys run.bib reads (MIGR-03, D-03 — the de-dup win)
    - Any bounded human.* easy-win labels authored in 39-02 render from the catalog (D-06)
    - No raw dotted key renders (run.human snapshot floor from 39-02 guarantees it)
  artifacts:
    - apps/run.human/webapp/src/components/header/header.tsx (nav + donate -> common.*)
    - apps/run.human/webapp/src/components/header/dropdown-user.tsx (profile menu -> common.profileMenu.*)
    - apps/run.human/webapp/src/components/header/dropdown-menu.tsx (mobile nav -> common.*)
    - apps/run.human/webapp/src/components/footer.tsx (FAQ/Credits -> common.*)
  key_links:
    - useCopy() (CopyProvider mounted in both layouts by 39-02) -> common.* keys shared with run.bib
---

<objective>
Wire run.human's shared chrome (header nav, mobile dropdown-menu, profile dropdown-user, footer) to the SAME common.* catalog keys run.bib reads, making the copy-paste de-dup win LIVE (D-01, D-03, SC-3): one CMS edit to a shared common.* row now changes wording in BOTH run.human and run.bib with no shared React component and no deploy. Also wire any bounded human.* easy-win labels authored in 39-02 (D-06). Words-only: per-variant keys keep each app's current word, so no visible label changes.

Purpose: This is the second reader that makes SC-3 demonstrable rather than latent.
Output: Four run.human chrome components reading labels from common.* (+ optional human.*) via useCopy().
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

# Toolkit + keys installed by 39-02; canonical key table in 39-01
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-01-PLAN.md
@apps/run.human/webapp/src/lib/copy-snapshot.json
@apps/run.human/webapp/src/components/CopyProvider.tsx

# Wiring targets
@apps/run.human/webapp/src/components/header/header.tsx
@apps/run.human/webapp/src/components/header/dropdown-user.tsx
@apps/run.human/webapp/src/components/header/dropdown-menu.tsx
@apps/run.human/webapp/src/components/footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run.human header + mobile dropdown-menu to common.* keys</name>
  <files>apps/run.human/webapp/src/components/header/header.tsx, apps/run.human/webapp/src/components/header/dropdown-menu.tsx</files>
  <read_first>
    apps/run.human/webapp/src/components/header/header.tsx (client; navItems has literal labels + "Donate $" sibling),
    apps/run.human/webapp/src/components/header/dropdown-menu.tsx (client; Who Am I / Maps / Meshtastic / Bib / Donate $ / FAQ),
    apps/run.human/webapp/src/components/CopyProvider.tsx (useCopy), the &lt;canonical_key_table&gt; in 39-01-PLAN.md
  </read_first>
  <action>
    In header/header.tsx add useCopy() and resolve the nav labels via t() from the shared keys: Maps -> common.header.maps, Meshtastic -> common.header.meshtastic, Bib -> common.header.bib, and the "Donate $" sibling button -> common.header.donate (the same key bib reads — SC-3 surface). The navItems array holds literal `label`s; resolve each via t() at render, leaving icon/href/external structure intact. Do not touch the brand wordmark or the dynamic Login skeleton fallback.
    In header/dropdown-menu.tsx add useCopy() and resolve item labels via t(): Who Am I -> common.header.whoami, Maps/Meshtastic -> common.header.maps / common.header.meshtastic, Bib -> common.header.bib, Donate $ -> common.header.donate, FAQ -> common.header.faq. Keep icons/onClick/navigation behavior unchanged; textValue attributes stay literal or mirror the t() value.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q "common.header.donate" src/components/header/header.tsx && grep -q "common.header.faq" src/components/header/dropdown-menu.tsx && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - header.tsx resolves Maps/Meshtastic/Bib and the donate sibling via t() from common.* keys (common.header.donate is the shared key bib also reads).
    - dropdown-menu.tsx resolves its visible item labels via t() from common.* keys.
    - Guard test green; wordmark + Login skeleton untouched; no rendered word changed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire run.human profile dropdown-user + footer to common.* (+ bounded human.* easy wins)</name>
  <files>apps/run.human/webapp/src/components/header/dropdown-user.tsx, apps/run.human/webapp/src/components/footer.tsx</files>
  <read_first>
    apps/run.human/webapp/src/components/header/dropdown-user.tsx (client; Profile / My Bib / CMS / GPS Check-in / Show My QR / Logout — note "Logout" not "Sign out"),
    apps/run.human/webapp/src/components/footer.tsx (client; FAQ / brand wordmark / Credits),
    the &lt;canonical_key_table&gt; in 39-01-PLAN.md
  </read_first>
  <action>
    In header/dropdown-user.tsx add useCopy() and resolve the visible profile-menu item labels via t(): Profile -> common.profileMenu.profile, My Bib -> common.profileMenu.myBib, CMS -> common.profileMenu.cms, GPS Check-in -> common.profileMenu.gpsCheckin, Show My QR -> common.profileMenu.showQr, and the "Logout" menu ITEM -> common.profileMenu.logout (the per-variant key that keeps run.human's current word "Logout" — NO visible change, honoring words-only + 37 D-05 per-variant rule). Leave the Logout MODAL ("Logout?" header + body sentence) and the QR modal prose LITERAL — those are deeper modal/client-state surfaces scoped to MIGR-04 (D-06 guardrail). Leave aria-labels and user data literal. Do not change any onPress/href/modal-open behavior.
    In footer.tsx add useCopy() and resolve FAQ -> common.header.faq (the shared de-duped key) and Credits -> common.footer.credits. Leave the brand wordmark ("defcon.run 34", teal dot) literal (D-02 brand token). If 39-02 authored any bounded human.* easy-win keys for a static label in these files, wire them here; otherwise author nothing new.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q "common.profileMenu.logout" src/components/header/dropdown-user.tsx && grep -q "common.footer.credits" src/components/footer.tsx && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts && npx next build 2>&1 | tail -3 | grep -qiE "Compiled|Route|✓"</automated>
  </verify>
  <acceptance_criteria>
    - dropdown-user.tsx resolves the five shared profile labels + the per-variant Logout item via t() from common.profileMenu.* (Logout modal + QR modal stay literal, MIGR-04).
    - footer.tsx resolves FAQ + Credits via t() from common.* keys; brand wordmark stays literal.
    - Guard test green and production build succeeds; no rendered word changed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value -> DOM | Chrome labels resolved via t() at render |
| server -> client bundle | useCopy is client-safe; server-only resolver never imported by these client components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-11 | Information Disclosure | run.human chrome (client) | low | mitigate | Components import only client-safe useCopy (copy-core), never the server-only @/lib/copy resolver; token boundary preserved (Test C in 39-02 guard) |
| T-39-12 | Tampering | chrome label rendering | low | mitigate | Plain t() string reads; renderCopy escape-first whitelist applies to any markdown value; no raw-HTML path |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — wiring only |
</threat_model>

<verification>
- `cd apps/run.human/webapp && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts` green.
- `cd apps/run.human/webapp && npx next build` succeeds.
- Grep confirms header/menu/profile/footer read common.* keys via useCopy(); common.header.donate + common.profileMenu.* are the SAME keys bib reads (SC-3 surface).
- Manual: run.human chrome renders identical words to before (words-only).
</verification>

<success_criteria>
- run.human chrome renders labels through t() from the same common.* keys run.bib reads (MIGR-03, SC-2/SC-3 surface).
- Per-variant keys keep each app's current word; Logout/QR modals stay MIGR-04.
- No visible word changed; no raw dotted key renders.
</success_criteria>

<artifacts>
This plan produces:
- Edited `apps/run.human/webapp/src/components/header/header.tsx` (nav + donate -> common.*).
- Edited `apps/run.human/webapp/src/components/header/dropdown-menu.tsx` (mobile nav -> common.*).
- Edited `apps/run.human/webapp/src/components/header/dropdown-user.tsx` (profile menu -> common.profileMenu.* via useCopy).
- Edited `apps/run.human/webapp/src/components/footer.tsx` (FAQ + Credits -> common.*).
- Any bounded human.* easy-win labels (from 39-02) wired, or none (bias to defer).
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-05-SUMMARY.md` when done
</output>