---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 04
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/run.bib/webapp/src/components/TransactionHistory.tsx
  - apps/run.bib/webapp/src/components/AdminActions.tsx
autonomous: true
requirements: [MIGR-02]
user_setup: []

must_haves:
  truths:
    - The remaining bib transaction-history and admin-action surfaces render their visible prose from catalog keys (MIGR-02), not inline literals
    - Aria-labels and error-DETAIL/interpolation tokens stay literal (D-04)
  artifacts:
    - apps/run.bib/webapp/src/components/TransactionHistory.tsx (server component -> bib.txn.* via loadCopy+t)
    - apps/run.bib/webapp/src/components/AdminActions.tsx (client component -> bib.admin.* via useCopy)
  key_links:
    - TransactionHistory async server component -> loadCopy('default')+t (37-04 ContributionChip pattern)
    - AdminActions client component -> useCopy() (CopyProvider mounted in bib root layout)
---

<objective>
Migrate run.bib's remaining copy surfaces — TransactionHistory (server) and AdminActions (client) — off inline literals onto the bib.txn.* / bib.admin.* keys authored in 39-01 (MIGR-02, D-04). Visible prose migrates; aria-labels and error-DETAIL tokens stay literal. Words-only: values byte-identical to current text.

Purpose: Close SC-1 ("no inline string literals left on migrated surfaces") for the bib remaining copy that Phase 37 deferred here.
Output: TransactionHistory + AdminActions reading prose from the catalog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

# Keys authored in 39-01
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-01-PLAN.md
@apps/run.bib/webapp/src/lib/copy-snapshot.json

# Toolkit consumption patterns: server loadCopy+t (37-04 ContributionChip), client useCopy
@apps/run.bib/webapp/src/lib/copy.ts
@apps/run.bib/webapp/src/components/CopyProvider.tsx

# Wiring targets
@apps/run.bib/webapp/src/components/TransactionHistory.tsx
@apps/run.bib/webapp/src/components/AdminActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TransactionHistory visible prose to bib.txn.* (async server component)</name>
  <files>apps/run.bib/webapp/src/components/TransactionHistory.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/components/TransactionHistory.tsx (currently a sync default-export server component),
    apps/run.bib/webapp/src/lib/copy.ts (loadCopy + t), the 37-04 ContributionChip async-server-component pattern noted in STATE.md
  </read_first>
  <action>
    Convert TransactionHistory to an async server component that calls `const copy = await loadCopy("default")` and resolves its visible prose with t(copy, key): the "Total contributed" label -> bib.txn key, the row kind labels "Bib"/"Donation" -> the bib.txn kind keys, the "In progress" pending badge -> bib.txn key, and the "Venmo & Cash App contributions appear here once an organizer confirms them." note -> its bib.txn key (use renderCopy if the authored value carries markdown; otherwise plain t()). Keep literal (D-04): the aria-label "Your contributions", the "· {provider}" provider data value, and all usd()/fmtDate() numeric formatting. Do not change layout/styles/props shape. If a caller relies on this being sync, confirm it is rendered within an async server tree (it is a server component today) — the await is the only structural change.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "loadCopy" src/components/TransactionHistory.tsx && grep -q "bib.txn" src/components/TransactionHistory.tsx && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TransactionHistory is async and resolves all four visible-prose strings via t(copy, "bib.txn...").
    - aria-label "Your contributions", provider data, and numeric formatting remain literal.
    - Guard test green; no rendered word changed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate AdminActions visible prose to bib.admin.* (client component)</name>
  <files>apps/run.bib/webapp/src/components/AdminActions.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/components/AdminActions.tsx (a "use client" component),
    apps/run.bib/webapp/src/components/CopyProvider.tsx (useCopy)
  </read_first>
  <action>
    Add useCopy() from @/components/CopyProvider and resolve the visible prose via t(): FAIL_TEXT ("Couldn't apply — try again."), DEDUPED_TEXT ("Already reconciled — amount unchanged."), the "Approve" button label, the "PAID" button label, the "Already booked." status, the "Reject" button label, and the window.confirm() reject-confirmation sentence -> their bib.admin.* keys. The module-level FAIL_TEXT / DEDUPED_TEXT constants must move inside the components (or become t() calls at render) since useCopy is a hook — do not read copy at module scope. Keep literal (D-04): every aria-label ("Amount in cents", "Approve payment", "Mark paid in person", "Reject bib") and any HTTP/{detail} error-detail token. Do not change any fetch URL, request body, router.refresh, or dedupe logic.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "useCopy" src/components/AdminActions.tsx && grep -q "bib.admin" src/components/AdminActions.tsx && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AdminActions imports useCopy and resolves all listed visible-prose strings via t() from bib.admin.* keys (no module-scope copy reads).
    - All aria-labels remain literal; fetch/refresh/dedupe logic unchanged.
    - Guard test green; no rendered word changed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value -> DOM | Admin/txn prose resolved via t()/renderCopy |
| server -> client bundle | AdminActions (client) uses only client-safe useCopy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-09 | Information Disclosure | AdminActions.tsx (client) | low | mitigate | Imports only useCopy (copy-core), never the server-only @/lib/copy resolver (token boundary; Test C guard) |
| T-39-10 | Tampering | TransactionHistory renderCopy | low | mitigate | Any markdown value rendered escape-first via renderCopy whitelist; plain labels are inert string reads |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — wiring only |
</threat_model>

<verification>
- `cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts` green.
- Grep confirms both surfaces read bib.txn.* / bib.admin.* via the toolkit.
- Manual: total-contributed panel + admin action prose render identical words to before.
</verification>

<success_criteria>
- TransactionHistory + AdminActions visible prose resolves from catalog keys (MIGR-02 / SC-1 for these surfaces).
- Aria-labels + error-detail tokens stay literal (D-04); no rendered word changed.
</success_criteria>

<artifacts>
This plan produces:
- Edited `apps/run.bib/webapp/src/components/TransactionHistory.tsx` (async server component -> bib.txn.*).
- Edited `apps/run.bib/webapp/src/components/AdminActions.tsx` (client component -> bib.admin.* via useCopy).
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-04-SUMMARY.md` when done
</output>
