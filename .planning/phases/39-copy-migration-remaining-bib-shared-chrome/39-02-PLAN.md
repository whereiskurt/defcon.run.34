---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/lib/copy.ts
  - apps/run.human/webapp/src/lib/copy-core.ts
  - apps/run.human/webapp/src/lib/copy-markdown.tsx
  - apps/run.human/webapp/src/components/CopyProvider.tsx
  - apps/run.human/webapp/src/lib/copy-snapshot.json
  - apps/run.human/webapp/scripts/import-copy.mjs
  - apps/run.human/webapp/package.json
  - apps/run.human/webapp/src/app/(protected)/layout.tsx
  - apps/run.human/webapp/src/app/(public)/layout.tsx
  - apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts
autonomous: true
requirements: [MIGR-03]
user_setup: []

must_haves:
  truths:
    - run.human has the copy toolkit installed (loadCopy/t server-side, useCopy client-side) reading its existing CMS_INTERNAL_URL + STRAPI_API_TOKEN config (D-05)
    - CopyProvider wraps the header/footer tree in BOTH run.human group layouts (protected + public), so chrome client components can call useCopy() (D-05)
    - run.human's copy-snapshot.json carries the full common.* union (byte-identical to run.bib's) as its offline floor plus any bounded human.* easy-win keys (D-06, D-07)
    - The server-only copy resolver never crosses to the client bundle (token stays server-side)
  artifacts:
    - apps/run.human/webapp/src/lib/{copy.ts,copy-core.ts,copy-markdown.tsx} (ported toolkit)
    - apps/run.human/webapp/src/components/CopyProvider.tsx (ported)
    - apps/run.human/webapp/src/lib/copy-snapshot.json (new: common.* union + human.* easy wins)
    - apps/run.human/webapp/scripts/import-copy.mjs (namespace-aware) + package.json copy:import script
    - apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts (key-floor + token-boundary guard)
  key_links:
    - loadCopy('default') in each group layout -> CopyProvider value -> useCopy() in chrome components (mount is what makes 39-05 wiring resolvable)
---

<objective>
Install the Phase 36 copy toolkit into run.human by copy-paste port (D-05 — repo per-app convention, NOT a shared package), author run.human's offline snapshot floor with the shared common.* union (identical to run.bib) plus bounded human.* easy-win keys (D-06), port the namespace-aware import script, and mount CopyProvider in BOTH run.human group layouts (run.human has NO root layout). This is the gate that makes run.human's chrome wiring (39-05) possible and demonstrates the de-dup win LIVE (D-01, SC-3).

Purpose: run.human already carries config.cms.internalUrl (CMS_INTERNAL_URL) + config.cms.apiToken (STRAPI_API_TOKEN) — the exact env the ported copy.ts reads — so this is largely a copy job over existing infra (D-05 de-risk).
Output: Ported toolkit (4 files), run.human snapshot, import script + package script, CopyProvider mounted in both layouts, guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

# Port SOURCES (copy verbatim; run.human @/* alias -> ./src/* matches bib, resolveJsonModule on, Next 16)
@apps/run.bib/webapp/src/lib/copy.ts
@apps/run.bib/webapp/src/lib/copy-core.ts
@apps/run.bib/webapp/src/lib/copy-markdown.tsx
@apps/run.bib/webapp/src/components/CopyProvider.tsx
@apps/run.bib/webapp/scripts/import-copy.mjs
@apps/run.bib/webapp/src/app/layout.tsx

# run.human MOUNT targets + existing config
@apps/run.human/webapp/src/config/index.ts
@apps/run.human/webapp/src/app/(protected)/layout.tsx
@apps/run.human/webapp/src/app/(public)/layout.tsx

# The canonical common.* table lives in 39-01 (identical subset required here)
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the 4 toolkit files verbatim into run.human</name>
  <files>apps/run.human/webapp/src/lib/copy.ts, apps/run.human/webapp/src/lib/copy-core.ts, apps/run.human/webapp/src/lib/copy-markdown.tsx, apps/run.human/webapp/src/components/CopyProvider.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/lib/copy.ts, apps/run.bib/webapp/src/lib/copy-core.ts,
    apps/run.bib/webapp/src/lib/copy-markdown.tsx, apps/run.bib/webapp/src/components/CopyProvider.tsx
  </read_first>
  <action>
    Copy the four bib toolkit files into run.human at the matching paths, unchanged. The @/lib/copy-core and @/lib/copy-snapshot.json imports resolve identically because run.human's tsconfig maps @/* -> ./src/* with resolveJsonModule enabled. Confirm copy.ts reads process.env.CMS_INTERNAL_URL + process.env.STRAPI_API_TOKEN directly (it does today) — these match run.human's existing config.cms env names (D-05). Do NOT add `import 'server-only'` (Next 16 vendors it internally; the server-only contract is convention-enforced exactly as in bib). Do NOT alter renderCopy's escape-first whitelist. The snapshot import in copy.ts + CopyProvider.tsx will resolve once Task 2 creates copy-snapshot.json.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/(lib/copy|components/CopyProvider)" ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - The 4 files exist at the run.human paths and are byte-equivalent to the bib sources (aside from being separate copies).
    - copy.ts references CMS_INTERNAL_URL and STRAPI_API_TOKEN (the run.human config env names).
    - No `import 'server-only'` literal is added; renderCopy whitelist is unchanged.
    - Typecheck reports no errors originating in the ported files (snapshot present after Task 2).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author run.human snapshot (common.* union + human.* easy wins), guard test, and namespace-aware import</name>
  <files>apps/run.human/webapp/src/lib/copy-snapshot.json, apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts, apps/run.human/webapp/scripts/import-copy.mjs, apps/run.human/webapp/package.json</files>
  <read_first>
    the &lt;canonical_key_table&gt; in 39-01-PLAN.md (the common.* subset MUST be byte-identical here),
    apps/run.bib/webapp/scripts/import-copy.mjs, apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts,
    apps/run.human/webapp/src/components/header/header.tsx, apps/run.human/webapp/src/components/header/dropdown-user.tsx,
    apps/run.human/webapp/src/components/header/dropdown-menu.tsx, apps/run.human/webapp/src/components/footer.tsx
  </read_first>
  <action>
    Create copy-snapshot.json with a `default` map containing (a) the FULL common.* union from 39-01's canonical key table with byte-identical values (both apps carry the shared floor — D-07; 39-06 diffs the two subsets), and (b) a BOUNDED set of human.* easy-win keys per the D-06 guardrail: clearly-static, low-risk, visible, NO interpolation, NO deep client-state coupling — top-level static labels only. BIAS TO DEFER: author zero human.* keys if none qualify cleanly; anything touching CheckInModal, dashboard, profile map/history, or the QR modal stays MIGR-04. Record which human.* keys (if any) you authored and why each meets the guardrail in the SUMMARY.
    Create copy-catalog-human.test.ts (mirror bib's copy-catalog guard, placed where run.human's vitest include picks it up): Test A key-floor asserting every common.* union key + each authored human.* key resolves from the snapshot `default`; Test C token boundary asserting NO "use client" component imports the server-only @/lib/copy resolver.
    Port import-copy.mjs into run.human/webapp/scripts with the SAME namespace-aware derivation authored in 39-01 (namespace = key prefix, enum-guarded; reads CMS_INTERNAL_URL + STRAPI_WRITE_TOKEN at run time only; missing env -> exit 1 before any CMS call; never logs the token; never uses the reserved `locale` query key). Add `"copy:import": "node scripts/import-copy.mjs"` to package.json scripts.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('./apps/run.human/webapp/src/lib/copy-snapshot.json').default; ['common.header.maps','common.header.donate','common.profileMenu.profile','common.profileMenu.logout','common.footer.credits'].forEach(k=>{if(!(k in s))throw new Error('missing '+k)})"` exits 0.
    - Guard test passes (common.* floor + token-boundary Test C green).
    - package.json has a copy:import script; import-copy.mjs exits 1 with env unset (token-safety guard).
    - Any authored human.* keys are static/no-interpolation and justified against the D-06 guardrail in the SUMMARY (or zero authored).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mount CopyProvider in both run.human group layouts</name>
  <files>apps/run.human/webapp/src/app/(protected)/layout.tsx, apps/run.human/webapp/src/app/(public)/layout.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/app/layout.tsx (reference: server loadCopy + &lt;CopyProvider value={copy}&gt; wrapping the header/footer tree),
    apps/run.human/webapp/src/app/(protected)/layout.tsx, apps/run.human/webapp/src/app/(public)/layout.tsx
  </read_first>
  <action>
    In each group layout (run.human has NO root layout, so both must mount it — D-05), call `const copy = await loadCopy("default")` in the async server component and wrap the existing Header/main/Footer tree in `<CopyProvider value={copy}>...</CopyProvider>`, mirroring bib's layout.tsx placement (inside SessionProvider, around the flex column that holds Header + main + Footer). Import loadCopy from @/lib/copy and CopyProvider from @/components/CopyProvider. Change nothing else in the layouts — the silent-SSO redirect logic in (public)/layout.tsx and the version meta tags stay exactly as-is; the CopyProvider wrap is purely additive. Only the server-resolved copy MAP crosses to the client provider — never the resolver, token, or CMS URL.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx next build 2>&1 | tail -5 | grep -qiE "Compiled|Route|✓" </automated>
  </verify>
  <acceptance_criteria>
    - Both (protected) and (public) layouts import loadCopy + CopyProvider and wrap the Header/Footer tree with `<CopyProvider value={copy}>`.
    - `grep -rl "CopyProvider" apps/run.human/webapp/src/app/(protected)/layout.tsx apps/run.human/webapp/src/app/(public)/layout.tsx` returns both files.
    - The (public) layout's silent-SSO/redirect logic is unchanged (diff shows only additive copy wiring).
    - Production build succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (copy.ts) -> client bundle | Only the resolved copy map may cross; STRAPI_API_TOKEN + CMS_INTERNAL_URL must NOT |
| operator -> Strapi (import time) | STRAPI_WRITE_TOKEN seeds rows; manual/CI only |
| catalog value -> DOM (renderCopy) | Editor-controlled string is the XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04 | Information Disclosure | ported copy.ts + layouts | high | mitigate | copy.ts stays server-only by convention (no `server-only` literal, reads token via call-time env, never NEXT_PUBLIC_*); layouts pass ONLY the resolved map to CopyProvider; copy-catalog-human Test C grep-gates that no "use client" file imports @/lib/copy |
| T-39-05 | Information Disclosure | scripts/import-copy.mjs | high | mitigate | STRAPI_WRITE_TOKEN read at run time only, distinct from runtime read-only STRAPI_API_TOKEN; missing env -> exit 1 pre-call; never logged/committed/NEXT_PUBLIC_* |
| T-39-06 | Tampering | ported copy-markdown.tsx | medium | mitigate | renderCopy ported verbatim: escape-first (React text nodes, no raw-HTML prop) + http/https/mailto scheme whitelist; no new markdown/sanitizer dep |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — toolkit port reuses next/cache + react already present; import uses global fetch |
</threat_model>

<verification>
- `cd apps/run.human/webapp && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts` green.
- `cd apps/run.human/webapp && npx next build` succeeds with CopyProvider mounted in both group layouts.
- No client component imports the server-only resolver (Test C).
</verification>

<success_criteria>
- run.human has the toolkit installed and CopyProvider mounted in both group layouts.
- run.human snapshot carries the full common.* union (byte-identical to bib) + bounded human.* easy wins.
- Namespace-aware import + copy:import script present; token stays server-side/import-time only.
</success_criteria>

<artifacts>
This plan produces:
- Ported `apps/run.human/webapp/src/lib/copy.ts`, `copy-core.ts`, `copy-markdown.tsx`, and `src/components/CopyProvider.tsx`.
- New `apps/run.human/webapp/src/lib/copy-snapshot.json` (common.* union + bounded human.* easy-win keys).
- Ported namespace-aware `apps/run.human/webapp/scripts/import-copy.mjs` + `copy:import` package script.
- CopyProvider mount edits in `(protected)/layout.tsx` and `(public)/layout.tsx` (loadCopy + provider wrap).
- New `apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts` guard.
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-02-SUMMARY.md` when done
</output>
