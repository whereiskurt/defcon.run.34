---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 06
type: execute
wave: 3
depends_on: ["39-03", "39-04", "39-05"]
files_modified:
  - apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts
  - apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts
autonomous: false
requirements: [MIGR-02, MIGR-03]
user_setup:
  - service: strapi-cms
    why: "One-shot import of the migrated common.*/bib.*/human.* rows into the shared live catalog + the live cross-app edit demo (SC-3) require a write-capable CMS token and access to the live/staging CMS admin."
    env_vars:
      - name: STRAPI_WRITE_TOKEN
        source: "Strapi admin -> Settings -> API Tokens (write-capable; distinct from the runtime read-only STRAPI_API_TOKEN). Supplied only at import time — never committed, never added to runtime env."
      - name: CMS_INTERNAL_URL
        source: "The reachable Strapi base URL for the import run"
    dashboard_config:
      - task: "Edit one shared common.* row (e.g. common.header.maps) and confirm the wording changes in BOTH bib.defcon.run and run.defcon.run within the revalidate window"
        location: "Strapi admin -> Copy Catalog page (ADMN-01)"

must_haves:
  truths:
    - Both apps build; bib remaining copy renders from bib.* keys and run.human chrome renders from common.* keys (no raw dotted keys)
    - The two apps' common.* snapshot subsets are byte-identical (shared floor, D-07)
    - Editing one shared common.* CMS row changes the wording in BOTH bib and run.human live (SC-3)
    - CMS-down fallback in BOTH apps renders from the snapshot `default` map, never `{}` and never a raw dotted key (FALL-04)
  artifacts:
    - A common.* cross-snapshot equality assertion in each app's copy-catalog guard test
  key_links:
    - snapshot floors <-> shared CMS catalog rows <-> both apps' live renders
---

<objective>
Prove the phase against real builds and the live catalog: (1) bib remaining copy renders from catalog keys; (2) run.human chrome renders from common.* keys; (3) a single live CMS common.* edit changes wording in BOTH bib and run.human with no shared component and no deploy (SC-3 — the headline de-dup proof, D-01); (4) CMS-down fallback in BOTH apps renders from each app's snapshot floor (FALL-04). Also lock the two apps' common.* snapshot subsets to byte-equality so they can never drift (D-07).

Purpose: Convert SC-3 from latent to demonstrated and guard the shared-floor invariant.
Output: Cross-snapshot equality assertions + a verified live cross-app edit + fallback demonstration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

@apps/run.bib/webapp/src/lib/copy-snapshot.json
@apps/run.human/webapp/src/lib/copy-snapshot.json
@apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts
@apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts
@apps/run.bib/webapp/scripts/import-copy.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the two apps' common.* snapshot subsets to byte-equality + full build/test gate</name>
  <files>apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts, apps/run.human/webapp/src/lib/__tests__/copy-catalog-human.test.ts</files>
  <read_first>
    apps/run.bib/webapp/src/lib/copy-snapshot.json, apps/run.human/webapp/src/lib/copy-snapshot.json,
    both copy-catalog guard tests
  </read_first>
  <action>
    Add a cross-snapshot equality check to BOTH guard tests: read the sibling app's copy-snapshot.json (relative path from the test file), select every key starting with "common." from each `default` map, and assert the two common.* subsets are deeply equal (same keys AND same values) — this makes any future divergence of the shared floor a red test in both apps (D-07 invariant). Then run the full test + build gate for both apps to confirm the wiring holds end-to-end.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts && cd ../../run.human/webapp && npx vitest run src/lib/__tests__/copy-catalog-human.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both guard tests pass INCLUDING the new cross-snapshot common.* deep-equality assertion.
    - `diff <(node -e "const s=require('./apps/run.bib/webapp/src/lib/copy-snapshot.json').default;console.log(JSON.stringify(Object.fromEntries(Object.entries(s).filter(([k])=>k.startsWith('common.')).sort()),null,1))") <(node -e "const s=require('./apps/run.human/webapp/src/lib/copy-snapshot.json').default;console.log(JSON.stringify(Object.fromEntries(Object.entries(s).filter(([k])=>k.startsWith('common.')).sort()),null,1))")` prints nothing (identical).
    - Both apps' production builds succeed (run `npx next build` in each; capture pass/fail in the SUMMARY).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator import + live SC-3 cross-app edit + FALL-04 fallback verification</name>
  <action>Operator runs the copy:import in both apps with a write token, then verifies SC-1/SC-2 renders, the live SC-3 shared-key edit reflected in both apps, and the CMS-down fallback — per how-to-verify below.</action>
  <what-built>
    Both apps now read shared common.* chrome keys and bib reads its remaining bib.* keys from the catalog. The offline snapshot floors are byte-identical for the common.* subset. The import scripts (bib + run.human) are namespace-aware and seed common.*/bib.*/human.* rows into the single shared Strapi catalog.
  </what-built>
  <how-to-verify>
    IMPORT (operator, once): with CMS_INTERNAL_URL + a WRITE-capable STRAPI_WRITE_TOKEN exported in the shell (never committed), run `npm run copy:import` in apps/run.bib/webapp AND in apps/run.human/webapp. Confirm each prints a created/updated tally and exits 0. The common.* rows land once in the shared catalog (idempotent upsert by key).
    SC-1 / SC-2 (renders from catalog): load bib.defcon.run (or staging) — header nav, mobile menu, profile menu, and the transaction-history + admin surfaces show normal words, NO raw dotted keys (e.g. never a literal "common.header.maps"). Load run.defcon.run — header nav, mobile menu, profile menu, footer show normal words, no raw dotted keys.
    SC-3 (the de-dup proof): in the Strapi Copy Catalog admin, edit ONE shared common.* row (e.g. common.header.maps "Maps" -> "Maps!" or common.profileMenu.profile). After the revalidate window (~5 min), confirm the changed wording appears in BOTH bib.defcon.run AND run.defcon.run — with no code deploy and no shared React component touched. Revert the row afterwards.
    FALL-04 (fallback floor): with the CMS unreachable (or the write token pointed at a down instance / offline build), confirm both apps still render the chrome from their snapshot `default` floor — normal words, never `{}` and never a raw dotted key.
  </how-to-verify>
  <resume-signal>Type "approved" once SC-1/SC-2/SC-3 + fallback are confirmed in both apps, or describe which surface failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> shared Strapi catalog | Write-token import + live edit; import-time only |
| CMS/S3 down -> app render | Fallback must reach the committed snapshot floor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-13 | Information Disclosure | copy:import runs (both apps) | high | mitigate | STRAPI_WRITE_TOKEN exported only for the import shell, distinct from runtime read-only token; never committed/logged/NEXT_PUBLIC_*; blocking human checkpoint gates the write path |
| T-39-14 | Denial of Service | CMS/S3 unreachable | low | accept | Cached 3-layer fallback (Strapi -> S3 -> snapshot) proven live in FALL-04 step; snapshot floor guarantees a render |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — assertions + operator import only |
</threat_model>

<verification>
- Both guard tests pass including the cross-snapshot common.* equality assertion.
- Operator import seeds both apps' rows; live SC-3 edit reflected in both apps; fallback renders from snapshot floor in both apps (human-verified).
- Both apps build.
</verification>

<success_criteria>
- SC-1: bib remaining copy resolves from catalog keys, no raw dotted keys on migrated surfaces.
- SC-2: shared chrome keyed under common.*; each app renders those labels via t() from the same keys.
- SC-3: one common.* CMS edit changes wording in bib AND run.human live, no shared component, no deploy.
- FALL-04: CMS-down fallback renders from each app's snapshot floor, never a raw dotted key.
- D-07 invariant: the two apps' common.* snapshot subsets are byte-identical (guarded).
</success_criteria>

<artifacts>
This plan produces:
- Cross-snapshot common.* deep-equality assertions added to both `copy-catalog-bib.test.ts` and `copy-catalog-human.test.ts`.
- A verified operator import + live SC-3 cross-app edit + FALL-04 fallback demonstration (recorded in the SUMMARY).
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-06-SUMMARY.md` when done
</output>