---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/run.bib/webapp/src/components/header.tsx
  - apps/run.bib/webapp/src/components/menu-dropdown.tsx
  - apps/run.bib/webapp/src/components/user-dropdown.tsx
  - apps/run.bib/webapp/src/components/footer.tsx
autonomous: true
requirements: [MIGR-03]
user_setup: []

must_haves:
  truths:
    - run.bib's header nav, mobile menu, profile menu, and footer render their labels through t()/useCopy() from common.* keys (MIGR-03)
    - No raw dotted key renders on any bib chrome surface (snapshot floor from 39-01 guarantees it — FALL-04)
  artifacts:
    - apps/run.bib/webapp/src/components/header.tsx (nav + admin link -> common.*)
    - apps/run.bib/webapp/src/components/menu-dropdown.tsx (mobile nav -> common.*)
    - apps/run.bib/webapp/src/components/user-dropdown.tsx (profile menu -> common.profileMenu.*)
    - apps/run.bib/webapp/src/components/footer.tsx (footer -> common.footer.* where applicable)
  key_links:
    - useCopy() (CopyProvider already mounted in bib root layout) -> common.* keys authored in 39-01 snapshot
---

<objective>
Wire run.bib's shared chrome (header nav, mobile menu-dropdown, profile user-dropdown, footer) to the common.* catalog keys authored in 39-01, so bib reads the SAME keys run.human will read (MIGR-03, D-03). Words-only: every value is byte-identical to the current label; no pixels move, no component restructured.

Purpose: Establish bib as the first reader of the unified common.* chrome namespace — half of the SC-3 de-dup proof (the other half is run.human via 39-05).
Output: Four bib chrome components reading labels from common.* via useCopy().
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

# Keys authored in 39-01 (this plan references them; the canonical key table lives in 39-01)
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-01-PLAN.md
@apps/run.bib/webapp/src/lib/copy-snapshot.json

# Wiring targets + toolkit consumption pattern (useCopy already used for bib.donate.trigger here)
@apps/run.bib/webapp/src/components/header.tsx
@apps/run.bib/webapp/src/components/menu-dropdown.tsx
@apps/run.bib/webapp/src/components/user-dropdown.tsx
@apps/run.bib/webapp/src/components/footer.tsx
@apps/run.bib/webapp/src/components/CopyProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire bib header + mobile menu-dropdown to common.* keys</name>
  <files>apps/run.bib/webapp/src/components/header.tsx, apps/run.bib/webapp/src/components/menu-dropdown.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/components/header.tsx (already calls useCopy() for bib.donate.trigger),
    apps/run.bib/webapp/src/components/menu-dropdown.tsx (already calls useCopy()),
    the &lt;canonical_key_table&gt; in 39-01-PLAN.md
  </read_first>
  <action>
    In header.tsx replace the inline nav labels with t() reads from the common.* keys: the Maps/Meshtastic/Bib nav item labels -> common.header.maps / common.header.meshtastic / common.header.bib, and the "Admin" desktop link -> common.header.admin. Re-point the existing donate trigger read from bib.donate.trigger to common.header.donate (the re-homed shared key from 39-01). The navItems array currently holds literal `label` strings; resolve each label via t() at render (keep the icon/href/external structure untouched). Do NOT touch aria-labels or the brand wordmark ("defcon.run" with teal dot).
    In menu-dropdown.tsx replace the mobile item labels with t() reads: "My bib" -> common.header.myBibMobile, "Maps"/"Meshtastic" -> common.header.maps / common.header.meshtastic, the donate item -> common.header.donate, and the admin item "Admin reports" -> common.profileMenu.adminReports. Keep textValue attributes as-is (they are a11y/list-key values, not visible chrome prose — leave literal per D-04) OR set them from the same t() value; do not change any icon or onPress behavior.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "common.header.maps" src/components/header.tsx && grep -q "common.header.donate" src/components/menu-dropdown.tsx && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - header.tsx resolves Maps/Meshtastic/Bib/Admin and the donate trigger via t() from common.* keys (no inline nav label literals remain on these elements).
    - menu-dropdown.tsx resolves its visible item labels via t() from common.* keys.
    - Guard test still green (all referenced common.* keys resolve from the snapshot floor).
    - Wordmark and aria-labels untouched.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire bib profile user-dropdown and footer to common.* keys</name>
  <files>apps/run.bib/webapp/src/components/user-dropdown.tsx, apps/run.bib/webapp/src/components/footer.tsx</files>
  <read_first>
    apps/run.bib/webapp/src/components/user-dropdown.tsx (currently all literal; no useCopy yet),
    apps/run.bib/webapp/src/components/footer.tsx (server component; only brand wordmark + version),
    the &lt;canonical_key_table&gt; in 39-01-PLAN.md
  </read_first>
  <action>
    In user-dropdown.tsx (a "use client" component) add useCopy() from @/components/CopyProvider and replace the visible profile-menu item labels with t() reads: Profile -> common.profileMenu.profile, My Bib -> common.profileMenu.myBib, CMS -> common.profileMenu.cms, "Admin reports" -> common.profileMenu.adminReports, GPS Check-in -> common.profileMenu.gpsCheckin, Show My QR -> common.profileMenu.showQr, Sign out -> common.profileMenu.signOut. Leave literal (D-04): the aria-label attributes ("User menu", section aria labels) and the runner's name/email data. textValue attributes may stay literal or mirror the t() value — do not change any href/onPress/icon.
    In footer.tsx: the only text is the brand wordmark ("defcon.run 34", teal dot) and the injected version string. Per D-02, the brand wordmark stays a literal (brand token) — bib's footer has no FAQ/Credits prose to migrate, so leave footer.tsx unchanged unless the sweep in 39-01 identified footer-adjacent prose; document in the SUMMARY that bib footer carries only the brand token (nothing to migrate) so SC-1's footer coverage is explicitly accounted for.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "common.profileMenu.signOut" src/components/user-dropdown.tsx && grep -q "useCopy" src/components/user-dropdown.tsx && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - user-dropdown.tsx imports useCopy and resolves all seven visible profile-menu labels via t() from common.profileMenu.* keys.
    - No aria-label or user data value was migrated (stays literal per D-04).
    - footer decision (brand-token-only, nothing to migrate) documented in the SUMMARY.
    - Guard test green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value -> DOM | Chrome labels resolved via t()/renderCopy at render |
| server -> client bundle | CopyProvider (already mounted in bib root layout) passes only the resolved map |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-07 | Tampering | chrome label rendering | low | mitigate | Labels are plain t() string reads; where markdown is possible renderCopy's escape-first whitelist applies; no raw-HTML path introduced |
| T-39-08 | Information Disclosure | user-dropdown.tsx (client) | low | mitigate | Component imports only the client-safe useCopy (copy-core), never the server-only @/lib/copy resolver; token boundary preserved (Test C in 39-01 guard) |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — wiring only |
</threat_model>

<verification>
- `cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts` green.
- Grep confirms header/menu/profile chrome reads common.* keys via t().
- Manual: bib header/menu/profile/footer render identical words to before (words-only).
</verification>

<success_criteria>
- run.bib chrome (header nav, mobile menu, profile menu) renders labels through t() from common.* keys.
- No visible word changed; no raw dotted key renders (snapshot floor from 39-01).
</success_criteria>

<artifacts>
This plan produces:
- Edited `apps/run.bib/webapp/src/components/header.tsx` (nav + admin + donate trigger -> common.*).
- Edited `apps/run.bib/webapp/src/components/menu-dropdown.tsx` (mobile nav -> common.*).
- Edited `apps/run.bib/webapp/src/components/user-dropdown.tsx` (profile menu -> common.profileMenu.* via useCopy).
- Footer coverage decision (brand-token-only) documented; footer.tsx unchanged unless sweep found prose.
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-03-SUMMARY.md` when done
</output>
