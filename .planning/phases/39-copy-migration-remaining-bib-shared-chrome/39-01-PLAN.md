---
phase: 39-copy-migration-remaining-bib-shared-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.bib/webapp/src/lib/copy-snapshot.json
  - apps/run.bib/webapp/scripts/import-copy.mjs
  - apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts
autonomous: true
requirements: [MIGR-02, MIGR-03]
user_setup: []

must_haves:
  truths:
    - Every remaining bib.* key and every shared common.* key the Wave 2 wiring plans will consume resolves from run.bib copy-snapshot.json `default` (offline floor, D-07)
    - The bib import script seeds each row into the shared Strapi catalog under the namespace derived from the key prefix (common.* -> "common", bib.* -> "bib"), not a hardcoded "bib"
  artifacts:
    - apps/run.bib/webapp/src/lib/copy-snapshot.json (extended default map: common.* union + remaining bib.* keys)
    - apps/run.bib/webapp/scripts/import-copy.mjs (namespace-aware)
    - apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts (key-floor contract extended)
  key_links:
    - copy-snapshot.json `default` map -> the guard test REQUIRED key list -> Wave 2 t()/useCopy() reads (a missing key fails the guard test first, never renders a raw dotted key)
---

<objective>
Author the offline source-of-truth (D-07) for everything Phase 39 migrates on the run.bib side: the remaining bib.* copy (MIGR-02) plus the full shared common.* chrome union (MIGR-03), written into run.bib's `copy-snapshot.json` `default` map. Make the bib import script namespace-aware so one import seeds both `common.*` and `bib.*` rows into the single shared Strapi catalog under correct namespaces. Extend the existing key-floor guard test so it is the red/green contract the Wave 2 wiring plans build against.

Purpose: The server `t` has no floor of its own; the CMS-down fallback must render from the snapshot `default` map, never `{}` (D-07). Keys must exist before components reference them, or the UI renders raw dotted keys (violates FALL-04 / SC-1).
Output: Extended bib snapshot, namespace-aware import script, extended guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-CONTEXT.md

# Toolkit + prior-phase authoring pattern (this plan extends 37-01's snapshot/import/test triad)
@apps/run.bib/webapp/src/lib/copy-snapshot.json
@apps/run.bib/webapp/scripts/import-copy.mjs
@apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts
@apps/run.bib/webapp/src/lib/copy-core.ts

# The literals being catalogued (read to enumerate exact current strings)
@apps/run.bib/webapp/src/components/TransactionHistory.tsx
@apps/run.bib/webapp/src/components/AdminActions.tsx
@apps/run.bib/webapp/src/components/header.tsx
@apps/run.bib/webapp/src/components/menu-dropdown.tsx
@apps/run.bib/webapp/src/components/user-dropdown.tsx
@apps/run.bib/webapp/src/components/footer.tsx
</context>

<canonical_key_table>
The shared `common.*` chrome union (authored here into run.bib's snapshot; plan 39-02 authors the IDENTICAL common.* subset into run.human's snapshot — the two common.* subsets MUST be byte-identical, verified in 39-06). Apply the locked 37 D-05 key rules: identical strings across apps de-dupe to ONE shared key (these are the SC-3 demo surface); strings that differ get per-variant keys (NO visible word change anywhere — words-only migration). Exact leaf names are executor discretion against `<namespace>.<area>.<element>`; the inventory below is the required coverage.

Shared, byte-identical across bib + run.human (de-dup -> one key each; SC-3 demo surface):
- common.header.maps = "Maps"
- common.header.meshtastic = "Meshtastic"
- common.header.bib = "Bib"
- common.header.donate = "Donate $"   (re-home of bib.donate.trigger per D-07 discretion — see Task 2)
- common.profileMenu.profile = "Profile"
- common.profileMenu.myBib = "My Bib"
- common.profileMenu.cms = "CMS"
- common.profileMenu.gpsCheckin = "GPS Check-in"
- common.profileMenu.showQr = "Show My QR"

Per-variant (words differ between apps -> distinct keys, each keeps its current word):
- common.header.admin = "Admin"                (bib desktop admin link)
- common.header.myBibMobile = "My bib"         (bib mobile menu, lowercase b — distinct from profileMenu.myBib)
- common.profileMenu.adminReports = "Admin reports"  (bib dropdown + bib mobile menu admin item)
- common.profileMenu.signOut = "Sign out"      (bib sign-out item)
- common.profileMenu.logout = "Logout"         (run.human logout item — authored here as floor; run.human wires it in 39-05)

run.human-only chrome (still under common.* per D-03; authored here as bib's floor too so both snapshots carry the full union):
- common.header.whoami = "Who Am I"            (run.human mobile menu)
- common.header.faq = "FAQ"                    (run.human mobile menu + footer; one shared key, de-dup)
- common.footer.credits = "Credits"           (run.human footer)

Brand token: the footer wordmark "defcon.run 34" stays a literal (brand token, D-02 discretion — do NOT catalog it).
</canonical_key_table>

<tasks>

<task type="auto">
  <name>Task 1: Sweep run.bib surfaces and author remaining bib.* keys into the snapshot floor</name>
  <files>apps/run.bib/webapp/src/lib/copy-snapshot.json, apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts</files>
  <read_first>
    apps/run.bib/webapp/src/components/TransactionHistory.tsx (server component visible prose),
    apps/run.bib/webapp/src/components/AdminActions.tsx (client component visible prose),
    apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts (the REQUIRED_BIB_KEYS contract to extend)
  </read_first>
  <action>
    Do the D-04 full sweep of remaining run.bib user-facing literals and add a key/value pair for each to the `default` map in copy-snapshot.json, then add each new key to the REQUIRED_BIB_KEYS list in copy-catalog-bib.test.ts (that inline list is the contract Wave 2 depends on).
    TransactionHistory.tsx visible prose -> bib.txn.* group: the "Total contributed" label, the row kind labels ("Bib" / "Donation"), the "In progress" pending badge, and the "Venmo & Cash App contributions appear here once an organizer confirms them." note. Leave literal (do NOT catalog): the aria-label "Your contributions", the "· {provider}" data value, and the usd()/fmtDate() formatted numerics.
    AdminActions.tsx visible prose -> bib.admin.* group: FAIL_TEXT ("Couldn't apply — try again."), DEDUPED_TEXT ("Already reconciled — amount unchanged."), the "Approve" button label, the "PAID" button label, the "Already booked." status, the "Reject" button label, and the window.confirm() reject-confirmation sentence. Leave literal (D-04): all aria-labels (e.g. amount/approve/mark-paid/reject aria strings) and any HTTP/{detail}/error-DETAIL interpolation tokens.
    Also grep the rest of the bib app surface (src/app/**, remaining components) for stray user-facing JSX text / heading / empty / loading / error prose that Phases 36–37 did not migrate; catalog each visible one under the appropriate bib.* area and record any string DELIBERATELY left literal (aria, brand wordmark, error-detail token, numeric) in the plan SUMMARY so SC-1 has an auditable exclusion list.
    Keep values byte-identical to the current on-screen text so no rendered pixels move.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Guard test passes: every newly-listed bib.txn.* and bib.admin.* key resolves from copy-snapshot.json `default` (key-floor Test A green).
    - `node -e "const s=require('./apps/run.bib/webapp/src/lib/copy-snapshot.json').default; ['bib.txn','bib.admin'].forEach(p=>{if(!Object.keys(s).some(k=>k.startsWith(p)))throw new Error('missing '+p)})"` exits 0.
    - The plan SUMMARY lists every literal intentionally left un-migrated (aria / brand / error-detail / numeric) as the SC-1 exclusion set.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the shared common.* chrome union into the bib snapshot floor</name>
  <files>apps/run.bib/webapp/src/lib/copy-snapshot.json, apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts</files>
  <read_first>
    apps/run.bib/webapp/src/components/header.tsx, apps/run.bib/webapp/src/components/menu-dropdown.tsx,
    apps/run.bib/webapp/src/components/user-dropdown.tsx, apps/run.bib/webapp/src/components/footer.tsx,
    the &lt;canonical_key_table&gt; in this plan
  </read_first>
  <action>
    Add every key in the &lt;canonical_key_table&gt; (common.header.*, common.profileMenu.*, common.footer.*) to the `default` map in copy-snapshot.json with the exact values shown, applying the de-dup (identical -> one shared key) vs per-variant (differ -> distinct key) rule so no visible word changes. Add a REQUIRED_COMMON_KEYS list (mirroring REQUIRED_BIB_KEYS) to copy-catalog-bib.test.ts asserting each common.* key resolves from the snapshot `default` floor.
    Re-home the donate trigger (D-07 discretion): introduce common.header.donate = "Donate $" as the shared chrome key that both apps' header + mobile menu will read in Wave 2. Keep the existing bib.donate.trigger row in the snapshot (leave it seeded — a bib test and possibly other surfaces still reference it) so nothing breaks; the 39-04 wiring plan re-points bib's header + menu-dropdown at common.header.donate.
    Because both apps carry the full union as their offline floor (D-07), include the run.human-only chrome keys (common.header.whoami, common.header.faq, common.footer.credits, common.profileMenu.logout) in the bib snapshot too even though bib does not render them.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('./apps/run.bib/webapp/src/lib/copy-snapshot.json').default; ['common.header.maps','common.header.donate','common.profileMenu.profile','common.profileMenu.signOut','common.profileMenu.logout','common.footer.credits'].forEach(k=>{if(!(k in s))throw new Error('missing '+k)})"` exits 0.
    - bib.donate.trigger row is still present in the snapshot (unchanged value "Donate $").
    - Guard test passes with the new REQUIRED_COMMON_KEYS assertions green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Make the bib import script namespace-aware</name>
  <files>apps/run.bib/webapp/scripts/import-copy.mjs</files>
  <read_first>apps/run.bib/webapp/scripts/import-copy.mjs (currently hardcodes NAMESPACE = "bib" on every POST)</read_first>
  <action>
    Replace the single hardcoded NAMESPACE constant used in the POST body with a per-key derivation: the namespace is the key's first dotted segment (e.g. "common.header.maps" -> "common", "bib.txn.total" -> "bib"). Guard it against the Phase 35 namespace enum [common, human, auth, gpx, bib, flash]; if a key's prefix is not in the enum, skip that row and log the key (never POST an invalid namespace). Preserve every existing safety property verbatim: reads CMS_INTERNAL_URL + STRAPI_WRITE_TOKEN at run time only, exits 1 without any CMS call when either is missing, never logs/echoes the token, never filters by the reserved Strapi `locale` query key. This one import now seeds both the common.* and bib.* rows correctly.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -vE '^\s*(\*|//)' scripts/import-copy.mjs | grep -q "split('.')" || { echo "FAIL: no split('.') namespace derivation found"; exit 1; }</automated>
    <automated>cd apps/run.bib/webapp && env -u STRAPI_WRITE_TOKEN -u CMS_INTERNAL_URL node scripts/import-copy.mjs; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The POST body namespace is computed per key from the key prefix (grep confirms a `split('.')` derivation outside comments), not a fixed literal.
    - Running the script with STRAPI_WRITE_TOKEN and CMS_INTERNAL_URL unset prints a one-line reason and exits 1 WITHOUT any CMS call (token-safety guard intact).
    - The reserved Strapi `locale` query key is still never used in any request URL.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Strapi (import time) | Write-capable token seeds catalog rows; runs manually/CI only |
| snapshot (committed source) -> app bundle | Static copy data only; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Information Disclosure | scripts/import-copy.mjs | high | mitigate | Write token read from STRAPI_WRITE_TOKEN at run time only; never committed, never in runtime env, never NEXT_PUBLIC_*, never logged/echoed; missing env -> exit 1 before any CMS call (preserved verbatim from 37-01) |
| T-39-02 | Tampering | namespace derivation | medium | mitigate | Namespace derived from key prefix is validated against the fixed Phase 35 enum; an unknown prefix is skipped + logged, never POSTed |
| T-39-03 | Tampering | copy-snapshot.json values | low | accept | Snapshot is editor-authored plain copy rendered escape-first by renderCopy (whitelist); no raw HTML path exists |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — snapshot is data, import uses global fetch, no package installs in this plan |
</threat_model>

<verification>
- `cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-catalog-bib.test.ts` green (key floor for all new common.* + bib.* keys, plus the unchanged server->client token-boundary test).
- Import script namespace-derivation grep passes and the missing-env guard exits 1.
- No rendered string values changed (values byte-identical to current on-screen text).
</verification>

<success_criteria>
- Remaining bib.* (bib.txn.*, bib.admin.*, sweep) and the full common.* union resolve from run.bib copy-snapshot.json `default`.
- Import script seeds common.* and bib.* rows under correct namespaces from a single run.
- Guard test is the extended red/green contract Wave 2 wiring plans depend on.
</success_criteria>

<artifacts>
This plan produces:
- Extended `apps/run.bib/webapp/src/lib/copy-snapshot.json` `default` map: common.header.*, common.profileMenu.*, common.footer.* union + bib.txn.*, bib.admin.*, and any sweep-discovered bib.* keys.
- Namespace-aware `apps/run.bib/webapp/scripts/import-copy.mjs` (per-key namespace from prefix, enum-guarded).
- Extended `apps/run.bib/webapp/src/__tests__/copy-catalog-bib.test.ts` (REQUIRED_BIB_KEYS + REQUIRED_COMMON_KEYS floor contract).
- SC-1 exclusion list (literals intentionally left un-migrated) recorded in the plan SUMMARY.
</artifacts>

<output>
Create `.planning/phases/39-copy-migration-remaining-bib-shared-chrome/39-01-SUMMARY.md` when done
</output>