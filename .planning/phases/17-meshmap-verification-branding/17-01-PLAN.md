---
phase: 17-meshmap-verification-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mqtt/nginx/index.html
  - apps/mqtt/nginx/site.webmanifest
  - apps/mqtt/nginx/Dockerfile.nginx
autonomous: true
requirements:
  - MESH-01
  - MESH-02
  - MESH-03
  - MESH-04
  - MESH-05
  - MESH-06
  - MESH-07
  - MESH-08
  - MESH-09
  - MESH-10
  - MESH-11

must_haves:
  truths:
    - "Meshmap HTML is the full DC33 port, not the placeholder"
    - "All dc33 string references are updated to dc34"
    - "nodes.json fetch path is /nodes.json (not /map/nodes.json)"
    - "Node color prefixes check dc34.* instead of dc33.*"
    - "Ghost mode activation does NOT open /qr/ URL"
    - "Ghost mode activation shows flash message and calls accomplishment API silently"
    - "Dark mode toggle persists via localStorage"
    - "Manifest and preload paths have no /map/ prefix"
    - "Dockerfile copies all asset files into container"
  artifacts:
    - path: "apps/mqtt/nginx/index.html"
      provides: "Full meshmap with DC34 branding"
      min_lines: 500
      contains: "dc34"
    - path: "apps/mqtt/nginx/site.webmanifest"
      provides: "PWA manifest with updated paths"
      contains: "android-chrome"
    - path: "apps/mqtt/nginx/Dockerfile.nginx"
      provides: "Container build with asset COPY lines"
      contains: "COPY nginx/"
  key_links:
    - from: "apps/mqtt/nginx/index.html"
      to: "/nodes.json"
      via: "fetch in drawMap"
      pattern: "fetch.*'/nodes.json'"
    - from: "apps/mqtt/nginx/index.html"
      to: "run.defcon.run accomplishment API"
      via: "fetch in toggleMobileMode"
      pattern: "hostname.replace.*mqtt.*run.*api"
    - from: "apps/mqtt/nginx/Dockerfile.nginx"
      to: "apps/mqtt/nginx/*.webp, *.png, *.ico"
      via: "COPY directive"
      pattern: "COPY nginx/"
---

<objective>
Port the DC33 meshmap single-page HTML application to DC34 and update Dockerfile to serve all assets.

Purpose: Replace the placeholder index.html with the full meshmap from DC33, applying all DC34 branding changes, path fixes, and ghost mode behavior updates specified in the context decisions.

Output: Working meshmap HTML, asset files, updated manifest, and Dockerfile ready for container build.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-meshmap-verification-branding/17-CONTEXT.md

@apps/mqtt/nginx/Dockerfile.nginx
@apps/mqtt/nginx/nginx.conf

Source to port from (read-only reference):
@~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port meshmap HTML and assets from DC33 to DC34</name>
  <files>apps/mqtt/nginx/index.html, apps/mqtt/nginx/site.webmanifest</files>
  <action>
1. Copy the DC33 meshmap index.html from `~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/index.html` to `apps/mqtt/nginx/index.html`, applying these modifications:

   **Fetch path updates (MESH-01, MESH-08):**
   - Change `'/map/nodes.json'` to `'/nodes.json'` in the fetch call inside drawMap()
   - Change preload link href from `/map/nodes.json` to `/nodes.json`

   **Branding updates (MESH-08, MESH-09):**
   - Change node color prefix checks from `dc33.east` to `dc34.east`, `dc33.bigstar` to `dc34.bigstar`, `dc33.` to `dc34.` in getMarkerColorFromNodeName()
   - Change logo image src from `dc33-logo-transp.webp` to `dc34-logo-transp.webp`
   - Change logo alt text from "DefCon 33 Logo" to "DefCon 34 Logo"
   - Update the DC Meshtastic link href from `defcon.meshtastic.org/` -- keep as-is unless a DC34 page is known (per user decision, keep same pattern)

   **Ghost mode updates (MESH-08):**
   - In the toggleMobileMode function, REMOVE the QR redirect behavior. Specifically:
     - Remove the `window.open(...)` call inside the `.then()` callback (line ~247 in DC33)
     - Remove the `window.open(...)` call inside the `.catch()` callback (line ~251 in DC33)
     - Keep the accomplishment API fetch call (the `fetch(...)` with credentials: 'include') -- this stays as a silent call
     - The accomplishment API pattern `${location.protocol}//${location.hostname.replace('mqtt', 'run')}/api/user/accomplishments` is correct and should remain
   - Keep ghost mode flash messages (the atob-encoded "GHOST MODE ACTIVATED" / "DEACTIVATED" text)
   - Keep both triggers: Konami code sequence and 10x rapid theme toggle in 4s

   **Manifest path updates:**
   - Change manifest link href from `/map/site.webmanifest` to `/site.webmanifest`

2. Copy the DC33 site.webmanifest from `~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/site.webmanifest` to `apps/mqtt/nginx/site.webmanifest`, applying:
   - Change icon src paths from `/map/android-chrome-192x192.png` to `/android-chrome-192x192.png`
   - Change icon src paths from `/map/android-chrome-512x512.png` to `/android-chrome-512x512.png`
   - Update name from "MeshMap.net" to "run.defcon.run"
   - Update short_name from "MeshMap" to "meshmap"

3. Copy all asset files from DC33 source directory to `apps/mqtt/nginx/`:
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/android-chrome-192x192.png apps/mqtt/nginx/`
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/android-chrome-512x512.png apps/mqtt/nginx/`
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/apple-touch-icon.png apps/mqtt/nginx/`
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/favicon.ico apps/mqtt/nginx/`
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/dc33-logo-transp.webp apps/mqtt/nginx/dc34-logo-transp.webp` (rename to dc34)
   - `cp ~/working/defcon.run.33/apps/mqtt/nginx/meshmap/meshmap.net/website/dc33-logo.webp apps/mqtt/nginx/dc34-logo.webp` (rename to dc34)

   Note: The logo images are copied as-is (same visual content) with dc34 filenames per user's discretion decision. The actual DC34 logo image can be swapped later if a new design is created.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/defcon.run.34 && grep -c 'dc33' apps/mqtt/nginx/index.html | grep '^0$' && grep -c 'dc34' apps/mqtt/nginx/index.html && grep -q "'/nodes.json'" apps/mqtt/nginx/index.html && grep -q 'dc34-logo-transp.webp' apps/mqtt/nginx/index.html && ! grep -q 'window.open' apps/mqtt/nginx/index.html && grep -q '/site.webmanifest' apps/mqtt/nginx/index.html && ! grep -q '/map/' apps/mqtt/nginx/index.html && test -f apps/mqtt/nginx/favicon.ico && test -f apps/mqtt/nginx/dc34-logo-transp.webp && test -f apps/mqtt/nginx/site.webmanifest && echo "PASS"</automated>
  </verify>
  <done>
    - index.html is the full 570+ line meshmap (not placeholder)
    - Zero occurrences of "dc33" in index.html
    - All fetch/preload paths use /nodes.json (no /map/ prefix)
    - Ghost mode has no window.open calls (QR redirect removed)
    - All 8 asset files present in apps/mqtt/nginx/
    - site.webmanifest paths updated (no /map/ prefix)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile to copy all meshmap assets</name>
  <files>apps/mqtt/nginx/Dockerfile.nginx</files>
  <action>
Update `apps/mqtt/nginx/Dockerfile.nginx` to copy all meshmap files into the container:

1. Replace the single placeholder COPY line:
   ```
   # Copy meshmap HTML placeholder
   # TODO: Phase 17 will port the actual meshmap HTML from DC33
   COPY nginx/index.html /var/www/html/index.html
   ```
   With COPY lines for all meshmap assets:
   ```
   # Copy meshmap files (ported from DC33, updated for DC34)
   COPY nginx/index.html /var/www/html/index.html
   COPY nginx/site.webmanifest /var/www/html/site.webmanifest
   COPY nginx/favicon.ico /var/www/html/favicon.ico
   COPY nginx/apple-touch-icon.png /var/www/html/apple-touch-icon.png
   COPY nginx/android-chrome-192x192.png /var/www/html/android-chrome-192x192.png
   COPY nginx/android-chrome-512x512.png /var/www/html/android-chrome-512x512.png
   COPY nginx/dc34-logo-transp.webp /var/www/html/dc34-logo-transp.webp
   COPY nginx/dc34-logo.webp /var/www/html/dc34-logo.webp
   ```

   Note: Individual COPY lines preferred over wildcard glob to make the asset list explicit in the Dockerfile. The Dockerfile build context is `apps/mqtt/` (see apps/build.sh), so paths are relative to that.

2. Remove the TODO comment referencing Phase 17.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/defcon.run.34 && grep -q 'dc34-logo-transp.webp' apps/mqtt/nginx/Dockerfile.nginx && grep -q 'site.webmanifest' apps/mqtt/nginx/Dockerfile.nginx && grep -q 'favicon.ico' apps/mqtt/nginx/Dockerfile.nginx && grep -c 'COPY nginx/' apps/mqtt/nginx/Dockerfile.nginx | grep -q '^8$' && ! grep -q 'TODO' apps/mqtt/nginx/Dockerfile.nginx && echo "PASS"</automated>
  </verify>
  <done>
    - Dockerfile has 8 COPY lines for all meshmap files
    - No TODO comments remain
    - All asset filenames use dc34 naming
  </done>
</task>

</tasks>

<verification>
All meshmap requirements verified:
- MESH-01: Live node position display -- index.html fetches /nodes.json, creates Leaflet markers from lat/lon
- MESH-02: Node identity in popups -- longName, shortName, hwModel, role displayed in popup HTML
- MESH-03: Device telemetry in popups -- battery, voltage, chUtil, airUtilTx in popup table
- MESH-04: Neighbor topology lines -- polyline drawn between neighbors with SNR/distance tooltips
- MESH-05: AES-CTR decryption -- handled by meshobserv binary (Go code), not in meshmap HTML
- MESH-06: Node search -- leaflet-search plugin with searchString property
- MESH-07: Marker clustering -- markerClusterGroup with disableClusteringAtZoom: 10
- MESH-08: Ported from DC33 with label updates -- all dc33 refs changed to dc34
- MESH-09: Color-coded markers -- getMarkerColorFromNodeName with dc34.east/bigstar/default colors
- MESH-10: Dark mode toggle -- body.dark class with localStorage persistence
- MESH-11: Opacity fade -- 1.0 - (elapsed / 129600) gives 36-hour fade (129600s = 36h)
</verification>

<success_criteria>
1. `apps/mqtt/nginx/index.html` is the full ~572-line meshmap (not the 22-line placeholder)
2. Zero occurrences of string "dc33" anywhere in index.html
3. All asset files (favicon, logos, manifest, chrome icons) present in apps/mqtt/nginx/
4. Dockerfile copies all 8 files into /var/www/html/
5. No /map/ path prefix remains in any file
6. Ghost mode works without QR redirect (no window.open calls)
</success_criteria>

<output>
After completion, create `.planning/phases/17-meshmap-verification-branding/17-01-SUMMARY.md`
</output>
