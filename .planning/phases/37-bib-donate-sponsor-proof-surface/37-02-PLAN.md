---
phase: 37-bib-donate-sponsor-proof-surface
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - apps/run.bib/webapp/src/components/SponsorInstructions.tsx
  - apps/run.bib/webapp/src/app/sponsor/venmo/page.tsx
  - apps/run.bib/webapp/src/app/sponsor/cashapp/page.tsx
  - apps/run.bib/webapp/src/app/orderform/page.tsx
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "The sponsor instruction copy (Pay via / Send to / Required comment / Open provider / reconcile note) and the Venmo/CashApp page titles+subheads render from bib.instructions.* catalog keys (SC-1)."
    - "The orderform landing h1 + intro render from bib.landing.* keys (SC-1)."
    - "With the CMS unavailable these server surfaces still render the wording from the snapshot floor — never a raw dotted key (SC-4)."
  artifacts:
    - apps/run.bib/webapp/src/components/SponsorInstructions.tsx
    - apps/run.bib/webapp/src/app/orderform/page.tsx
  key_links:
    - "server component -> loadCopy('default') -> t()/renderCopy() (D-02, mirrors layout.tsx)"
    - "SponsorInstructions reconcile note -> reconcileNoteBefore + <code>{runnerCode}</code> + reconcileNoteAfter"
---

<objective>
Migrate the SERVER-rendered sponsor/instruction/landing surface to the catalog. These are true server components/pages, so they resolve copy via the cached `loadCopy('default')` + `t()` / `renderCopy()` path (D-02), exactly as `layout.tsx` already does.

Purpose: Proves the server half of the toolkit end-to-end (SC-1/SC-4) on the sponsor instructions pages and the orderform landing header.
Output: Four server files reading `bib.instructions.*` and `bib.landing.*` instead of inline JSX literals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md

@apps/run.bib/webapp/src/lib/copy.ts
@apps/run.bib/webapp/src/lib/copy-markdown.tsx
@apps/run.bib/webapp/src/app/layout.tsx
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SponsorInstructions server copy (bib.instructions.*)</name>
  <files>apps/run.bib/webapp/src/components/SponsorInstructions.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/SponsorInstructions.tsx (current literals: the "Pay via {provider}" eyebrow, "Send to", "Required comment" + its hint, "Open {provider}" button, and the reconcile note wrapping `<code>{runnerCode}</code>`)
    - apps/run.bib/webapp/src/lib/copy.ts (loadCopy + t)
  </read_first>
  <action>
    Make SponsorInstructions resolve copy via `loadCopy('default')` (await it — the component becomes async server) and `t(copy, key, vars)`:
    - The provider eyebrow → `t('bib.instructions.payVia', { provider: providerLabel })`.
    - The "Send to" row label → `t('bib.instructions.sendTo')`.
    - The "Required comment" row label → `t('bib.instructions.requiredComment')`, its hint prop value → `t('bib.instructions.requiredCommentHint')`.
    - The deep-link button label → `t('bib.instructions.openProvider', { provider: providerLabel })`.
    - The closing reconcile paragraph: render `t('bib.instructions.reconcileNoteBefore')`, then keep the existing `<code>{runnerCode}</code>` element, then `t('bib.instructions.reconcileNoteAfter')` — preserving the monospace `<code>` treatment around the runner code.
    Leave the `aria-label={\`${providerLabel} payment instructions\`}` and the InstructionRow uppercase styling untouched (aria stays literal, D-03). Pass `runnerCode` through as data, never concatenated into markup.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.instructions.payVia" src/components/SponsorInstructions.tsx && ! grep -q "Required comment<" src/components/SponsorInstructions.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>SponsorInstructions is async, resolves all five instruction strings via t(), keeps `<code>{runnerCode}</code>` intact, and typechecks. No visible instruction literal remains inline.</done>
</task>

<task type="auto">
  <name>Task 2: Venmo + Cash App instruction pages (titles, subheads, back link)</name>
  <files>apps/run.bib/webapp/src/app/sponsor/venmo/page.tsx, apps/run.bib/webapp/src/app/sponsor/cashapp/page.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/app/sponsor/venmo/page.tsx (h1, subhead paragraph, "← Back to bib" link)
    - apps/run.bib/webapp/src/app/sponsor/cashapp/page.tsx (mirror; different title/subhead wording)
  </read_first>
  <action>
    Both pages are already async server components — add `const copy = await loadCopy('default');` (import loadCopy + t from @/lib/copy) near the other awaits.
    - Venmo: h1 → `t(copy,'bib.instructions.venmoTitle')`; subhead paragraph → `t(copy,'bib.instructions.venmoSubhead')`; the back link text → `t(copy,'bib.instructions.backToBib')`.
    - Cash App: h1 → `t(copy,'bib.instructions.cashappTitle')`; subhead → `t(copy,'bib.instructions.cashappSubhead')`; back link → `t(copy,'bib.instructions.backToBib')` (shared key, D-09).
    Leave the `providerLabel="Venmo"` / `"Cash App"` props passed into SponsorInstructions as-is (those drive interpolation there). Do not touch the deep-link / handle / redirect logic.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.instructions.venmoTitle" src/app/sponsor/venmo/page.tsx && grep -c "bib.instructions.cashappTitle" src/app/sponsor/cashapp/page.tsx && ! grep -q "Sponsor via Venmo<" src/app/sponsor/venmo/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Both pages resolve title/subhead/back-link from bib.instructions.* and typecheck; the shared back-link key is used on both.</done>
</task>

<task type="auto">
  <name>Task 3: Orderform landing header (bib.landing.*)</name>
  <files>apps/run.bib/webapp/src/app/orderform/page.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/app/orderform/page.tsx (the `<h1>` "Bibs &amp; Donation" and the intro `<p>` about the free daily event — around lines 215-230)
    - apps/run.bib/webapp/src/lib/copy-markdown.tsx (renderCopy, in case the intro is treated as formatted)
  </read_first>
  <action>
    Add `const copy = await loadCopy('default');` in the Home server component (import loadCopy + t from @/lib/copy). Replace the landing `<h1>` text with `t(copy,'bib.landing.title')` and the intro paragraph text with `t(copy,'bib.landing.intro')`. Leave every child component render (StripeStatusBanner, ContributionChip, GetYourBib, PledgeTagline, ContributionChoice, ContributionTiles) and all data-fetching logic untouched — those components own their own copy in other Wave 2 plans. Only the h1 + intro belong to this file.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.landing.title" src/app/orderform/page.tsx && grep -c "bib.landing.intro" src/app/orderform/page.tsx && ! grep -q "Bibs &amp; Donation" src/app/orderform/page.tsx && ! grep -q "remains a FREE daily event" src/app/orderform/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The landing h1 + intro resolve from bib.landing.* and the page typechecks; no other copy in this file is changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → rendered copy | Editor-controlled strings flow into server-rendered JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering (XSS) | SponsorInstructions / pages | high | mitigate | Copy values render as React text children (auto-escaped) or via `renderCopy` (escape-first + scheme whitelist). No `dangerouslySetInnerHTML`. Interpolated values (`provider`, `runnerCode`) pass as `vars`, never concatenated into markup. |
| T-37-03 | Info-disclosure | loadCopy usage | high | mitigate | These are server components; loadCopy reads the token server-side only. Nothing new crosses to the client. |
| T-37-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for run.bib/webapp.
- Positive greps confirm bib.instructions.* / bib.landing.* usage; negative greps confirm the removed visible literals are gone from each file.
- `npm test` (existing suites) stays green — no test files changed here.
</verification>

<success_criteria>
- All server sponsor/instruction/landing copy resolves from bib.instructions.* / bib.landing.* via loadCopy+t/renderCopy (SC-1).
- `<code>{runnerCode}</code>` treatment preserved in the reconcile note.
- Snapshot floor guarantees these render even with the CMS down (SC-4).
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-02-SUMMARY.md` when done
</output>
