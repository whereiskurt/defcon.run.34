---
phase: 37-bib-donate-sponsor-proof-surface
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - apps/run.bib/webapp/src/components/DonateModal.tsx
  - apps/run.bib/webapp/src/components/StripeStatusBanner.tsx
  - apps/run.bib/webapp/src/components/PledgeTagline.tsx
  - apps/run.bib/webapp/src/components/header.tsx
  - apps/run.bib/webapp/src/components/menu-dropdown.tsx
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "The DonateModal title/subhead/labels/provider-note/error and its interpolated submit CTA resolve through useCopy() inside the client component (SC-1/SC-2)."
    - "The Stripe status banner success/cancel sentences resolve through useCopy() (SC-2 interpolated-confirmation surface)."
    - "The header desktop + mobile menu 'Donate $' trigger resolves from bib.donate.trigger (D-07)."
  artifacts:
    - apps/run.bib/webapp/src/components/DonateModal.tsx
    - apps/run.bib/webapp/src/components/StripeStatusBanner.tsx
  key_links:
    - "client component -> useCopy() -> t(key, vars) at render/handler time (SC-2)"
    - "DonateModal submit CTA -> t('bib.checkout.cta', { label, amount }) (interpolated)"
---

<objective>
Migrate the "Just donate" quick-give modal and its two status surfaces (the Stripe redirect banner, the pledge tagline) to the catalog via `useCopy()`, and repoint the single `"Donate $"` chrome trigger label to `bib.donate.trigger` in both the desktop header and the mobile menu (D-07). This is the hardest proof case: a client, interpolated, modal-heavy surface resolving copy inside render/handlers (SC-2).

Purpose: Proves the client half of the toolkit including runtime-interpolated CTA and confirmation banner (SC-2).
Output: Five client files reading bib.donate.* / bib.checkout.* / bib.status.* via useCopy(), plus the trigger label.

NOTE (D-09 caveat): `DonateModal.tsx` is currently byte-for-byte duplicated into run.human and run.flash. Migrating run.bib's copy intentionally diverges run.bib's DonateModal from the other two. That is expected — do NOT re-sync the three files afterward; the CMS catalog is now run.bib's source of truth for this modal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md

@apps/run.bib/webapp/src/components/CopyProvider.tsx
@apps/run.bib/webapp/src/lib/copy-core.ts
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DonateModal copy via useCopy (bib.donate.* + shared bib.checkout.*)</name>
  <files>apps/run.bib/webapp/src/components/DonateModal.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/DonateModal.tsx (literals: title, subhead, "Donation amount" label, the slider helper with `${MIN_CENTS/100}`/`${MAX_CENTS/100}`, "Payment method", the Card/Cash App/Venmo pill labels, the organizer-confirm note, the submit `Donate ${displayAmount}` / "Redirecting…", and the error sentence with `{submit.detail}`)
    - apps/run.bib/webapp/src/components/CopyProvider.tsx (useCopy signature)
  </read_first>
  <action>
    Add `const { t } = useCopy();` (import from @/components/CopyProvider) and resolve, at render time:
    - modal title → `t('bib.donate.title')`; subhead → `t('bib.donate.subhead')`; amount label → `t('bib.donate.amountLabel')`.
    - slider helper → `t('bib.checkout.sliderHelper', { min: MIN_CENTS / 100, max: MAX_CENTS / 100 })`.
    - "Payment method" → `t('bib.checkout.paymentMethod')`; provider pill labels → `t('bib.checkout.providerCard')` / `providerCashApp` / `providerVenmo` (map the existing `[value,label]` tuples through t).
    - organizer-confirm note → `t('bib.checkout.providerNote')`.
    - submit button: `disabled ? t('bib.checkout.redirecting') : t('bib.checkout.cta', { label: t('bib.contribution.donateVerb'), amount: displayAmount })`.
    - error alert → `t('bib.checkout.error', { detail: submit.detail })` (the `{detail}` token stays raw per D-03).
    Leave `aria-label="Make a donation"` / `aria-label="Close"` / the amount input aria untouched (aria stays literal). Do not change any checkout/fetch/redirect logic.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "useCopy" src/components/DonateModal.tsx && grep -c "bib.checkout.cta" src/components/DonateModal.tsx && ! grep -q "Just donate" src/components/DonateModal.tsx && ! grep -q "Payment method" src/components/DonateModal.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>DonateModal resolves every visible label/sentence/CTA through useCopy() with correct interpolation; aria stays literal; typechecks. The visible "Just donate" / "Payment method" literals are gone from the file.</done>
</task>

<task type="auto">
  <name>Task 2: StripeStatusBanner + PledgeTagline via useCopy (bib.status.*)</name>
  <files>apps/run.bib/webapp/src/components/StripeStatusBanner.tsx, apps/run.bib/webapp/src/components/PledgeTagline.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/StripeStatusBanner.tsx (the two `message` branches: success sentence, cancel sentence)
    - apps/run.bib/webapp/src/components/PledgeTagline.tsx (the "OK! You promised 🙏" line)
  </read_first>
  <action>
    StripeStatusBanner: add `const { t } = useCopy();`; set `message = isSuccess ? t('bib.status.paymentSuccess') : t('bib.status.paymentCancel')`. Keep the dismiss-on-interaction effect and the styling untouched.
    PledgeTagline: add `const { t } = useCopy();`; render `t('bib.status.pledgeTagline')` in place of the inline line. Keep the rain-store subscription + the null-when-not-raining behavior untouched.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.status.paymentSuccess" src/components/StripeStatusBanner.tsx && grep -c "bib.status.pledgeTagline" src/components/PledgeTagline.tsx && ! grep -q "Checkout cancelled" src/components/StripeStatusBanner.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Both banners resolve their sentences from bib.status.* via useCopy() and typecheck; the inline success/cancel/pledge literals are gone.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate ONLY the "Donate $" trigger label (D-07)</name>
  <files>apps/run.bib/webapp/src/components/header.tsx, apps/run.bib/webapp/src/components/menu-dropdown.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/header.tsx (the desktop `<button>Donate $</button>` around line 116; header is already "use client")
    - apps/run.bib/webapp/src/components/menu-dropdown.tsx (the mobile `<span className="text-base">Donate $</span>` around line 91)
  </read_first>
  <action>
    In BOTH files add `const { t } = useCopy();` (import from @/components/CopyProvider) and replace the visible `Donate $` label text with `t('bib.donate.trigger')`. Change NOTHING else in these two files — every other nav label, wordmark, Admin link, and menu item stays literal for Phase 39 (D-07 documented seam). Leave the DropdownItem `textValue="Donate"` prop as-is (it is an a11y/selection value, not visible copy).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.donate.trigger" src/components/header.tsx && grep -c "bib.donate.trigger" src/components/menu-dropdown.tsx && ! grep -q ">Donate \$<" src/components/menu-dropdown.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Both chrome files resolve the donate trigger from bib.donate.trigger; no other chrome copy changed; typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → client render | Editor copy resolves in the client via useCopy() |
| server → client | Only the resolved map crosses via CopyProvider; never the token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering (XSS) | DonateModal / banners | high | mitigate | useCopy() returns plain strings rendered as React text children (auto-escaped). Interpolated `amount`/`detail` pass as `vars`, never concatenated into markup; no `dangerouslySetInnerHTML`. |
| T-37-03 | Info-disclosure | client imports | high | mitigate | Client files import only @/components/CopyProvider (useCopy) — never @/lib/copy. Enforced by 37-01 Test C. |
| T-37-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Positive greps confirm useCopy() + bib.donate.*/bib.checkout.*/bib.status.* usage; negative greps confirm removed visible literals are gone.
- SC-2 interpolated CTA (`bib.checkout.cta`) and the StripeStatusBanner both resolve at runtime through the client handler/render path.
</verification>

<success_criteria>
- DonateModal + status banners fully catalog-driven via useCopy(), including the interpolated submit CTA (SC-2).
- The one chrome trigger label migrated (D-07); no other chrome touched.
- No "use client" file imports the server resolver (T-37-03).
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-03-SUMMARY.md` when done
</output>
