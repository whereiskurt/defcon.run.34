---
phase: 37-bib-donate-sponsor-proof-surface
plan: 04
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - apps/run.bib/webapp/src/components/BibForm.tsx
  - apps/run.bib/webapp/src/components/user-dropdown.tsx
  - apps/run.bib/webapp/src/components/menu-dropdown.tsx
  - apps/run.bib/webapp/src/__tests__/bibform-copy.test.tsx
autonomous: true
requirements: [MIGR-01]

must_haves:
  truths:
    - "BibForm name-entry copy (label, placeholder, Save/Cancel/Saving/Verifying, save-state hints, and the pluralized rename-count) resolves through useCopy() (SC-1, D-04)."
    - "The profile/QR/logout menu (user-dropdown) and the mobile nav menu (menu-dropdown) resolve their visible item labels through useCopy() (SC-2 modals)."
    - "Offline (no CMS) the committed snapshot floor guarantees no raw dotted key renders (SC-4)."
  artifacts:
    - apps/run.bib/webapp/src/__tests__/bibform-copy.test.tsx
  key_links:
    - "useCopy() works because CopyProvider is mounted in layout; BibForm and both dropdowns are client descendants."
    - "The rename-count keeps its singular/plural wording via two keys (bib.name.changesLeftOne / bib.name.changesLeftOther) selected by count in code; {detail} in the save-error sentence stays raw (D-03)."
---

<objective>
Migrate the remaining CLIENT surface — BibForm name-entry copy (in scope per D-04) and the profile/QR/logout + mobile nav menus (the SC-2 "modals") — to resolve visible copy through useCopy(). aria/textValue accessibility strings and the raw {detail} save-error token stay literal (D-03).

Purpose: Complete the donate/sponsor vertical top-to-bottom (SC-1, D-04) and prove modal-copy resolution via useCopy (SC-2).
Output: Three migrated client components + a vitest pinning BibForm's catalog-driven copy incl. the pluralized rename-count and the offline floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-SUMMARY.md

# Toolkit (client hook) — consume, do not modify
@apps/run.bib/webapp/src/components/CopyProvider.tsx
@apps/run.bib/webapp/src/lib/copy-core.ts
@apps/run.bib/webapp/src/lib/copy-snapshot.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate BibForm name-entry copy to useCopy (D-04, D-03)</name>
  <files>apps/run.bib/webapp/src/components/BibForm.tsx, apps/run.bib/webapp/src/__tests__/bibform-copy.test.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/BibForm.tsx (literals: the name label, placeholder, Save/Cancel buttons, Saving…/Verifying… states, and the SaveStateHint strings incl. locked/quota/saved/saved-with-count/error and the pluralized "N name change(s) left")
    - apps/run.bib/webapp/src/components/CopyProvider.tsx (useCopy())
  </read_first>
  <behavior>
    - Test: BibForm renders the name label ("Name on bib") and placeholder from the catalog.
    - Test: with renamesRemaining=1 the low-count hint reads "1 name change left" (singular key); with 2 it reads "2 name changes left" (plural key).
    - Test: with an empty CopyProvider value the same render still shows real words (snapshot floor), never a substring "bib." dotted key.
  </behavior>
  <action>
    Add `const { t } = useCopy();` to the BibForm function body, and pass `t` into SaveStateHint (accept it as a prop) OR call useCopy() inside SaveStateHint directly — planner's call; both are client. Replace:
    - label → t('bib.name.label'); placeholder → t('bib.name.placeholder').
    - Save button: busy ? (verifying ? t('bib.name.verifying') : t('bib.name.saving')) : t('bib.name.save'); Cancel button → t('bib.name.cancel').
    - SaveStateHint strings: locked → t('bib.name.locked'); quota-empty → t('bib.name.quotaEmpty'); the "saving" hint → t('bib.name.saving'); saved → t('bib.name.saved'); error → t('bib.name.saveError', { detail: state.detail }) ({detail} stays raw, D-03).
    - Rename-count `left`: compute via renamesRemaining === 1 ? t('bib.name.changesLeftOne', { count: renamesRemaining }) : t('bib.name.changesLeftOther', { count: renamesRemaining }). The "Saved · {left}" case → t('bib.name.savedWithCount', { left }) using that resolved `left`.
    - Keep aria/id/role attributes literal (bib-name-hint id, role="status", aria-describedby) — D-03.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "useCopy" src/components/BibForm.tsx && for k in bib.name.label bib.name.placeholder bib.name.save bib.name.saveError bib.name.changesLeftOne bib.name.changesLeftOther bib.name.savedWithCount; do grep -q "$k" src/components/BibForm.tsx || { echo "missing $k"; exit 1; }; done && npx vitest run src/__tests__/bibform-copy.test.tsx</automated>
  </verify>
  <done>BibForm resolves label/placeholder/buttons/hints via useCopy(); the singular/plural rename-count is preserved via the two keys; {detail} + aria stay literal; the copy vitest passes incl. the snapshot-floor no-raw-key case.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the profile/QR/logout + mobile nav menus to useCopy (SC-2 modals)</name>
  <files>apps/run.bib/webapp/src/components/user-dropdown.tsx, apps/run.bib/webapp/src/components/menu-dropdown.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/user-dropdown.tsx (visible item children: Profile, My Bib, CMS, Admin reports, GPS Check-in, Show My QR, Sign out; textValue attrs are a11y/typeahead — keep literal)
    - apps/run.bib/webapp/src/components/menu-dropdown.tsx (visible item children: My bib, Maps, Meshtastic, Admin reports)
  </read_first>
  <action>
    In user-dropdown.tsx add `const { t } = useCopy();` (client component) and replace the VISIBLE DropdownItem child text: Profile → t('bib.menu.profile'); My Bib → t('bib.menu.myBib'); CMS → t('bib.menu.cms'); Admin reports → t('bib.menu.adminReports'); GPS Check-in → t('bib.menu.gpsCheckin'); Show My QR → t('bib.menu.showQr'); Sign out → t('bib.menu.signOut'). Leave the `textValue=` attributes, aria-labels, and the "Runner" name fallback literal (a11y/data, D-03).

    In menu-dropdown.tsx add `const { t } = useCopy();` and replace the visible span text: My bib → t('bib.mobileMenu.myBib'); Maps → t('bib.mobileMenu.maps'); Meshtastic → t('bib.mobileMenu.meshtastic'); Admin reports → t('bib.mobileMenu.adminReports'). Leave textValue + aria-labels literal.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -q "useCopy" src/components/user-dropdown.tsx && grep -q "useCopy" src/components/menu-dropdown.tsx && for k in bib.menu.profile bib.menu.signOut bib.menu.showQr; do grep -q "$k" src/components/user-dropdown.tsx || { echo "missing $k"; exit 1; }; done && for k in bib.mobileMenu.myBib bib.mobileMenu.maps bib.mobileMenu.meshtastic; do grep -q "$k" src/components/menu-dropdown.tsx || { echo "missing $k"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Both menus resolve their visible item labels via useCopy() (SC-2 modals); textValue/aria-labels stay literal; user-dropdown covers the profile/QR/logout items and menu-dropdown covers the mobile nav items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client | CopyProvider carries only the resolved copy map into the browser; the CMS token/URL never cross |
| editor/CMS → client render | ui-string values render inside client components via useCopy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04a | Information Disclosure | BibForm/user-dropdown/menu-dropdown imports | high | mitigate | Client components import only useCopy (CopyProvider) + copy-core; none imports @/lib/copy (server resolver) — grep-gated in verification |
| T-37-04b | Injection (XSS) | interpolated save-state + count strings | high | mitigate | Copy renders as React text children (auto-escaped); count/detail/left pass as vars into t(), never concatenated into markup; no raw-HTML injection prop used |
| T-37-04c | Tampering | editor-supplied menu labels | low | accept | Text-only labels; malformed values degrade to the snapshot floor via useCopy |
</threat_model>

<verification>
- npx vitest run src/__tests__/bibform-copy.test.tsx passes (label/placeholder + singular/plural count + snapshot-floor no-raw-key).
- Key-presence greps pass for BibForm, user-dropdown, menu-dropdown.
- Client-boundary check: `grep -l "@/lib/copy\"" src/components/BibForm.tsx src/components/user-dropdown.tsx src/components/menu-dropdown.tsx` returns nothing.
- npx tsc --noEmit clean; npm run build succeeds.
</verification>

<success_criteria>
BibForm name-entry copy and both menus render all visible copy from bib.* keys via useCopy() (SC-1, SC-2, D-04); the pluralized rename-count keeps its wording; the snapshot floor guarantees no raw dotted key offline (SC-4); aria/textValue and raw {detail} tokens remain literal (D-03).
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-04-SUMMARY.md` when done.
</output>