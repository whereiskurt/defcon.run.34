---
phase: 37-bib-donate-sponsor-proof-surface
plan: 04
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - apps/run.bib/webapp/src/components/ContributionTiles.tsx
  - apps/run.bib/webapp/src/components/ContributionChoice.tsx
  - apps/run.bib/webapp/src/components/ContributionChip.tsx
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "The Sponsor/Donate tile kickers, titles, bodies, and the CTA verbs passed to SponsorForm resolve through useCopy() (SC-1)."
    - "The ContributionChoice checkbox labels, three hint lines, 30-rain limit note, and error sentence resolve through useCopy() (SC-1)."
    - "The server-rendered ContributionChip thank-you pill + its interpolated aria resolve via loadCopy()+t() (SC-1/SC-2)."
  artifacts:
    - apps/run.bib/webapp/src/components/ContributionTiles.tsx
    - apps/run.bib/webapp/src/components/ContributionChip.tsx
  key_links:
    - "ContributionTiles CTA verb -> t('bib.contribution.sponsorVerb'|'donateVerb') passed as SponsorForm ctaLabel"
    - "ContributionChip (server) -> loadCopy('default') -> t('bib.contribution.chipAria', { amount, providers })"
---

<objective>
Migrate the contribution/donate tile cluster copy: the client-reactive tile grid (`ContributionTiles`), the 3-way pledge/burn control (`ContributionChoice`), and the server-rendered thank-you pill (`ContributionChip`). Tiles + choice are client components (useCopy); the chip is a true server component rendered directly by the orderform page, so it uses `loadCopy('default')` + `t()` (D-02).

Purpose: Covers the largest copy group (bib.contribution.*) and proves both mechanisms in one cluster (SC-1/SC-2).
Output: Three files reading bib.contribution.* from the catalog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md

@apps/run.bib/webapp/src/components/CopyProvider.tsx
@apps/run.bib/webapp/src/lib/copy.ts
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContributionTiles copy via useCopy (bib.contribution.* + verbs)</name>
  <files>apps/run.bib/webapp/src/components/ContributionTiles.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/ContributionTiles.tsx (literals: donate tile title "Just donate" + body; sponsor tile title "Sponsor this bib" + body; the kicker eyebrows "Support"/"This"/"or That"; the `ctaLabel="Donate"` / `ctaLabel="Sponsor"` props passed to SponsorForm)
  </read_first>
  <action>
    Add `const { t } = useCopy();` (import from @/components/CopyProvider) and resolve:
    - donate tile title → `t('bib.donate.title')` (shared with the modal, D-09); donate body → `t('bib.contribution.donateBody')`.
    - sponsor tile title → `t('bib.contribution.sponsorTitle')`; sponsor body → `t('bib.contribution.sponsorBody')`.
    - kicker eyebrows: the `donateTile(kicker)` call-sites pass `t('bib.contribution.kickerSupport')` / `kickerThis` / `kickerOrThat` in place of the "Support"/"This"/"or That" literals.
    - the `ctaLabel` passed to SponsorForm → `t('bib.contribution.donateVerb')` for the donate tile and `t('bib.contribution.sponsorVerb')` for the sponsor tile.
    Leave the `aria-label={title}` on the Tile section (it now receives the resolved title — fine) and all rain/burn store logic untouched.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.contribution.sponsorTitle" src/components/ContributionTiles.tsx && grep -c "bib.contribution.donateVerb" src/components/ContributionTiles.tsx && ! grep -q "Sponsor this bib" src/components/ContributionTiles.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Tile titles/bodies/kickers + the CTA verbs resolve from the catalog via useCopy(); typechecks; visible tile literals gone.</done>
</task>

<task type="auto">
  <name>Task 2: ContributionChoice copy via useCopy (labels, hints, limit, error)</name>
  <files>apps/run.bib/webapp/src/components/ContributionChoice.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/ContributionChoice.tsx (the limit note "That's plenty of cash…", the two checkbox labels, the `hintFor()` three branches, and the error `Couldn't save (${state.detail}) — try again.`)
  </read_first>
  <action>
    Add `const { t } = useCopy();` in the component. Resolve:
    - limit note → `t('bib.contribution.limitNote')`.
    - in-person checkbox label → `t('bib.contribution.optInPerson')`; burn checkbox label → `t('bib.contribution.optBurn')`.
    - the three `hintFor` branches: because `hintFor` is a module-scope helper, either inline the hint resolution in the component using the choice/state, or pass `t` into `hintFor(choice, state, t)`. Map: inperson → `t('bib.contribution.hintInPerson')`, burn → `t('bib.contribution.hintBurn')`, nothing → `t('bib.contribution.hintNothing')`, and the error branch → `t('bib.contribution.saveError', { detail: state.detail })` (`{detail}` raw, D-03).
    Leave `aria-label="Contribution options"` literal. Keep all PATCH / debounce / store logic untouched.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.contribution.hintInPerson" src/components/ContributionChoice.tsx && grep -c "bib.contribution.optBurn" src/components/ContributionChoice.tsx && ! grep -q "Free bib without customization" src/components/ContributionChoice.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>All choice labels/hints/limit/error resolve via useCopy() with the detail token raw; typechecks; inline hint literals gone.</done>
</task>

<task type="auto">
  <name>Task 3: ContributionChip server copy via loadCopy (thanks + interpolated aria)</name>
  <files>apps/run.bib/webapp/src/components/ContributionChip.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/ContributionChip.tsx (the visible "Thank you" span, and the `aria-label={\`Thank you — you've contributed ${amount} via ${provs.join(", ")}\`}`)
    - apps/run.bib/webapp/src/lib/copy.ts (loadCopy + t)
    - apps/run.bib/webapp/src/app/orderform/page.tsx (renders `<ContributionChip totalCents providers />` — the chip stays a server component; making it async is compatible)
  </read_first>
  <action>
    ContributionChip is rendered directly by the server orderform page, so make it `async` and resolve `const copy = await loadCopy('default');` (import loadCopy + t from @/lib/copy).
    - visible pill text → `t(copy,'bib.contribution.thanks')`.
    - the aria-label → `t(copy,'bib.contribution.chipAria', { amount, providers: provs.join(', ') })` (this aria is explicitly in-scope per 37-CONTEXT — the chip's interpolated aria migrates).
    Keep the brand SVG map, the `usd()` formatter, and the early `totalCents <= 0` return untouched. The orderform already awaits/renders server children, so no orderform edit is needed.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.contribution.chipAria" src/components/ContributionChip.tsx && grep -c "loadCopy" src/components/ContributionChip.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>ContributionChip is async, resolves the thank-you text + interpolated aria from the catalog via loadCopy+t, and typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → render | Editor copy resolves in both client (tiles/choice) and server (chip) renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering (XSS) | tiles / choice / chip | high | mitigate | Copy renders as React text children (auto-escaped); interpolated `amount`/`providers`/`detail` pass as `vars`. No `dangerouslySetInnerHTML`. |
| T-37-03 | Info-disclosure | client vs server imports | high | mitigate | Client tiles/choice import only useCopy; only the server chip imports @/lib/copy. Enforced by 37-01 Test C. |
| T-37-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Positive greps confirm bib.contribution.* usage across the three files; negative greps confirm removed visible literals are gone.
- `npm test` existing suites stay green.
</verification>

<success_criteria>
- Contribution tile + choice + chip copy fully catalog-driven; correct mechanism per component (client useCopy vs server loadCopy).
- Interpolated chip aria resolves via t() with amount+providers (SC-2).
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-04-SUMMARY.md` when done
</output>
