---
phase: 37-bib-donate-sponsor-proof-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.bib/webapp/src/lib/copy-snapshot.json
  - apps/run.bib/webapp/scripts/import-copy.mjs
  - apps/run.bib/webapp/package.json
  - apps/run.bib/webapp/src/__tests__/copy-import.test.ts
autonomous: true
requirements: [MIGR-01]

must_haves:
  truths:
    - "Every bib donate/sponsor key the migration plans consume exists in copy-snapshot.json under `default`, so useCopy()/loadCopy() resolve them offline (SC-4 floor)."
    - "A dependency-free import script can POST the same (key, locale='default', value, namespace='bib') rows into Strapi using a write-capable token that is never part of the app runtime (D-01)."
  artifacts:
    - apps/run.bib/webapp/src/lib/copy-snapshot.json
    - apps/run.bib/webapp/scripts/import-copy.mjs
    - apps/run.bib/webapp/src/__tests__/copy-import.test.ts
  key_links:
    - "copy-snapshot.json is the authored source of truth (D-01); the import script derives CMS rows FROM it (never the reverse), and `copy:snapshot` round-trips CMS back into it."
    - "The write token env var is distinct from the runtime read-only STRAPI_API_TOKEN and is read only inside scripts/import-copy.mjs."
---

<objective>
Author every bib donate/sponsor copy key into the committed `copy-snapshot.json` (the offline floor and, per D-01, the authored source of truth), and add a one-shot, dependency-free import script that POSTs the same rows into Strapi via a write-capable token. This is the foundation Wave — Wave 2 migration plans (37-02/03/04) consume these keys; they must exist in the snapshot first so the toolkit resolves them offline (satisfying SC-4's "never a raw dotted key" floor) and so a live CMS edit (SC-3) has rows to edit.

Purpose: Satisfy D-01 (snapshot-as-source-of-truth + CMS import) and pre-seed SC-4 (fallback floor) in one authoring pass.
Output: Extended `copy-snapshot.json`, `scripts/import-copy.mjs`, a `copy:import` npm script, and a vitest that pins the snapshot key set + the script's fail-closed behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md

# Toolkit this phase consumes (already live — do NOT modify)
@apps/run.bib/webapp/src/lib/copy-snapshot.json
@apps/run.bib/webapp/src/lib/copy-core.ts
@apps/run.bib/webapp/scripts/copy-snapshot.mjs
@apps/run.cms/app/src/api/ui-string/content-types/ui-string/schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author all bib donate/sponsor keys into copy-snapshot.json (D-01, D-05)</name>
  <files>apps/run.bib/webapp/src/lib/copy-snapshot.json</files>
  <read_first>
    - apps/run.bib/webapp/src/lib/copy-snapshot.json (existing shape: `{ "default": { "<key>": "<value>" } }`; PRESERVE the two existing `bib.selftest.*` keys)
    - apps/run.bib/webapp/src/lib/copy-core.ts (confirms `{placeholder}` interpolation token syntax so authored values use `{amount}` / `{detail}` / etc.)
    - docs/superpowers/specs/2026-07-05-cms-copy-catalog-design.md §"Key convention" (`<namespace>.<area>.<element>`, namespace `bib`)
  </read_first>
  <action>
    Extend the `default` object in copy-snapshot.json with EVERY key below, authored per D-05 (`bib.<area>.<element>`, per-variant where the words differ). Values MUST reproduce the current on-screen wording verbatim so the migration is a 1:1 swap. Interpolation tokens (`{amount}`, `{detail}`, `{provider}`, `{min}`, `{max}`, `{count}`, `{left}`, `{runnerCode}`) use the copy-core `{placeholder}` syntax. Preserve the existing `bib.selftest.*` keys (do not delete — layout.tsx still references one; harmless hidden span).

    SponsorForm (client): `bib.sponsor.amountLabel`="Sponsor amount"; `bib.donate.amountLabel`="Donation amount"; `bib.sponsor.cta`="Sponsor {amount}"; `bib.donate.cta`="Donate {amount}"; `bib.checkout.amountRangeHint`="Slide or type any amount from {min} up to {max}."; `bib.checkout.paymentMethodLabel`="Payment method"; `bib.checkout.providerCard`="Card"; `bib.checkout.providerCashApp`="Cash App"; `bib.checkout.providerVenmo`="Venmo"; `bib.checkout.offlineProviderNote`="Venmo & Cash App are confirmed by an organizer — your contribution appears once approved."; `bib.checkout.redirecting`="Redirecting…"; `bib.checkout.error`="Could not start checkout ({detail}) — try again."

    RunnerCodeBadge (client): `bib.checkout.runnerCodeLabel`="Runner code"; `bib.checkout.runnerCodeCopy`="Copy"; `bib.checkout.runnerCodeCopied`="Copied!"

    SponsorInstructions + sponsor pages (server): `bib.instructions.payVia`="Pay via {provider}"; `bib.instructions.sendToLabel`="Send to"; `bib.instructions.requiredCommentLabel`="Required comment"; `bib.instructions.requiredCommentHint`="This BIB-XXXX code must be in the payment note so we can match your sponsorship to your bib."; `bib.instructions.openApp`="Open {provider}"; `bib.instructions.postPaymentNote`="After you send the payment, come back here — reconciliation is automatic and usually finishes within a few minutes. Payments without {runnerCode} in the note are flagged for manual review."; `bib.instructions.backLink`="← Back to bib"; `bib.venmoPage.title`="Sponsor via Venmo"; `bib.venmoPage.subtitle`="DEF CON 34 — send the amount below with your runner code in the note."; `bib.cashappPage.title`="Sponsor via Cash App"; `bib.cashappPage.subtitle`="DEF CON 34 — send the amount below and paste your runner code as the note before you tap send."

    Orderform page (server): `bib.orderform.title`="Bibs & Donation"; `bib.orderform.subtitle`="defcon.run remains a FREE daily event — if you'd like to financially support, we would appreciate it."; `bib.orderform.thankYou`="Thank You"; `bib.orderform.payInPersonReserved`="You're paying in person — your bib is reserved."; `bib.orderform.sponsorKicker`="This"; `bib.orderform.sponsorTitle`="Sponsor this bib"; `bib.orderform.sponsorBody`="Contributions attach to your bib and help fund defcon.run 34."; `bib.orderform.donateKicker`="or That"; `bib.orderform.donateKickerSupport`="Support"; `bib.orderform.donateTitle`="Just donate"; `bib.orderform.donateBody`="Not running? Contribute anyway — support goes directly to defcon.run 34."; `bib.orderform.donateBodySupport`="Contribute anyway — support goes directly to defcon.run 34."; `bib.orderform.statusSuccess`="Payment received — thanks for supporting defcon.run 34! Reconciliation may take a moment."; `bib.orderform.statusCancel`="Checkout cancelled. No charge was made — you can try again below."

    BibForm name-entry (client, D-04): `bib.name.label`="Name on bib"; `bib.name.placeholder`="Enter the name to print on your bib"; `bib.name.save`="Save"; `bib.name.saving`="Saving…"; `bib.name.verifying`="Verifying…"; `bib.name.cancel`="Cancel"; `bib.name.locked`="Name locked for print — contact organizers to change it."; `bib.name.quotaEmpty`="No name changes left."; `bib.name.saved`="Saved."; `bib.name.savedWithCount`="Saved · {left}"; `bib.name.saveError`="Save failed ({detail}) — try again."; `bib.name.changesLeftOne`="{count} name change left"; `bib.name.changesLeftOther`="{count} name changes left"

    Profile/QR/logout menu (client modals): `bib.menu.profile`="Profile"; `bib.menu.myBib`="My Bib"; `bib.menu.cms`="CMS"; `bib.menu.adminReports`="Admin reports"; `bib.menu.gpsCheckin`="GPS Check-in"; `bib.menu.showQr`="Show My QR"; `bib.menu.signOut`="Sign out"; `bib.mobileMenu.myBib`="My bib"; `bib.mobileMenu.maps`="Maps"; `bib.mobileMenu.meshtastic`="Meshtastic"; `bib.mobileMenu.adminReports`="Admin reports"

    Keep the file valid JSON, 2-space indented, trailing newline (match `copy:snapshot` output shape so a later regenerate diffs cleanly).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && node -e "const s=require('./src/lib/copy-snapshot.json').default; const need=['bib.sponsor.amountLabel','bib.donate.amountLabel','bib.sponsor.cta','bib.donate.cta','bib.checkout.amountRangeHint','bib.checkout.paymentMethodLabel','bib.checkout.providerCard','bib.checkout.providerCashApp','bib.checkout.providerVenmo','bib.checkout.offlineProviderNote','bib.checkout.redirecting','bib.checkout.error','bib.checkout.runnerCodeLabel','bib.checkout.runnerCodeCopy','bib.checkout.runnerCodeCopied','bib.instructions.payVia','bib.instructions.sendToLabel','bib.instructions.requiredCommentLabel','bib.instructions.requiredCommentHint','bib.instructions.openApp','bib.instructions.postPaymentNote','bib.instructions.backLink','bib.venmoPage.title','bib.venmoPage.subtitle','bib.cashappPage.title','bib.cashappPage.subtitle','bib.orderform.title','bib.orderform.subtitle','bib.orderform.thankYou','bib.orderform.payInPersonReserved','bib.orderform.sponsorKicker','bib.orderform.sponsorTitle','bib.orderform.sponsorBody','bib.orderform.donateKicker','bib.orderform.donateKickerSupport','bib.orderform.donateTitle','bib.orderform.donateBody','bib.orderform.donateBodySupport','bib.orderform.statusSuccess','bib.orderform.statusCancel','bib.name.label','bib.name.placeholder','bib.name.save','bib.name.saving','bib.name.verifying','bib.name.cancel','bib.name.locked','bib.name.quotaEmpty','bib.name.saved','bib.name.savedWithCount','bib.name.saveError','bib.name.changesLeftOne','bib.name.changesLeftOther','bib.menu.profile','bib.menu.myBib','bib.menu.cms','bib.menu.adminReports','bib.menu.gpsCheckin','bib.menu.showQr','bib.menu.signOut','bib.mobileMenu.myBib','bib.mobileMenu.maps','bib.mobileMenu.meshtastic','bib.mobileMenu.adminReports']; const miss=need.filter(k=>!(k in s)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('all',need.length,'keys present')"</automated>
  </verify>
  <done>All listed keys exist in copy-snapshot.json `default` with the verbatim current wording and correct `{placeholder}` tokens; existing `bib.selftest.*` keys preserved; file is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dependency-free CMS import script + copy:import npm script (D-01)</name>
  <files>apps/run.bib/webapp/scripts/import-copy.mjs, apps/run.bib/webapp/package.json</files>
  <read_first>
    - apps/run.bib/webapp/scripts/copy-snapshot.mjs (mirror its style: global `fetch`, no deps, fail-closed exit 1 when env missing, `{ "default": { key: value } }` shape)
    - apps/run.cms/app/src/api/ui-string/content-types/ui-string/schema.json (POST target fields: key, locale, value, namespace; draftAndPublish=false so a create is immediately live)
    - apps/run.bib/webapp/package.json (add the script alongside `copy:snapshot`)
  </read_first>
  <action>
    Create `scripts/import-copy.mjs` (ESM `.mjs`, NOT `.ts` — matches the existing dependency-free `copy-snapshot.mjs` and avoids adding a tsx/ts-node dependency, honoring the project's no-new-deps rule and D-05's minimal-tooling spirit; this is the planner's transport/location call under D-01's Claude's-Discretion).

    Behavior:
    - Read `CMS_INTERNAL_URL` and a WRITE-capable token from `STRAPI_WRITE_TOKEN` (a NEW env var, deliberately distinct from the runtime read-only `STRAPI_API_TOKEN`). If either is missing, print a one-line reason and `process.exit(1)` WITHOUT any network call (fail-closed — never partial-writes, never falls back to the read token).
    - Load the local `src/lib/copy-snapshot.json` `default` map — this is the authored source of truth; the CMS is a derived import.
    - For each `(key, value)`: GET `${CMS_INTERNAL_URL}/api/ui-strings?filters[key][$eq]=<key>&filters[locale][$eq]=default` with the write token. If a row exists, PUT `/api/ui-strings/<documentId>` with `{ data: { value } }`; else POST `/api/ui-strings` with `{ data: { key, locale: "default", value, namespace: "bib" } }`. This respects the (key,locale) uniqueness lifecycle guard (35-02) so re-runs are idempotent.
    - Log a per-row created/updated tally and a final summary; a single row failure logs and continues (best-effort), exiting non-zero if any row failed so CI surfaces it.
    - Uses ONLY global `fetch` — no imports beyond node built-ins + the JSON snapshot. Never reads `NEXT_PUBLIC_*`. Never imports anything from `src/` app runtime.

    Add to package.json scripts: `"copy:import": "node scripts/import-copy.mjs"`. Do NOT wire it into `build` (build must never couple to CMS or need a write token).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && env -u CMS_INTERNAL_URL -u STRAPI_WRITE_TOKEN node scripts/import-copy.mjs; test $? -eq 1 && node -e "const p=require('./package.json'); if(p.scripts['copy:import']!=='node scripts/import-copy.mjs'){console.error('copy:import script missing/wrong');process.exit(1)} if((p.scripts.build||'').includes('import-copy')){console.error('import must not be in build');process.exit(1)} console.log('script wired, not in build')"</automated>
  </verify>
  <done>`scripts/import-copy.mjs` exits 1 with a reason when CMS_INTERNAL_URL or STRAPI_WRITE_TOKEN is unset (no network call); `copy:import` is registered and absent from `build`; the script imports no `src/` runtime module and no new npm dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the snapshot key set and the script's security boundary in vitest</name>
  <files>apps/run.bib/webapp/src/__tests__/copy-import.test.ts</files>
  <read_first>
    - apps/run.bib/webapp/src/__tests__/copy.test.ts (existing vitest style for the copy toolkit)
    - apps/run.bib/webapp/scripts/import-copy.mjs (asserting its env/import boundary)
  </read_first>
  <behavior>
    - Test 1: copy-snapshot.json `default` contains all bib donate/sponsor keys (import the same key list; assert none missing) — guards SC-4 (every migrated key has an offline floor).
    - Test 2: interpolation-token sanity — `bib.sponsor.cta` contains `{amount}`, `bib.checkout.error` contains `{detail}`, `bib.instructions.payVia` contains `{provider}`, `bib.instructions.postPaymentNote` contains `{runnerCode}` — so migration call-sites can interpolate.
    - Test 3: security boundary — read scripts/import-copy.mjs as text; assert it references `STRAPI_WRITE_TOKEN`, does NOT reference `NEXT_PUBLIC_`, and contains no `import ... from "@/` or relative `../src/` app-runtime import (T-37-01).
  </behavior>
  <action>
    Create `src/__tests__/copy-import.test.ts` implementing the three behaviors above with vitest. Test 3 reads the script file with `node:fs` and asserts on its text (no execution needed). Keep the key list in one shared array reused across Task 1's spirit.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/copy-import.test.ts</automated>
  </verify>
  <done>`vitest run src/__tests__/copy-import.test.ts` passes all three tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → import script | A write-capable Strapi token crosses into the one-shot import at script runtime only |
| local snapshot → CMS | Authored copy values are pushed into the CMS collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure / Elevation | scripts/import-copy.mjs write token | high | mitigate | Token read only from `STRAPI_WRITE_TOKEN` inside the script; never `NEXT_PUBLIC_*`, never committed, never imported by any `src/` runtime path; script exits 1 when unset (Task 2/3, grep-gated in Test 3) |
| T-37-02 | Tampering | copy-snapshot.json authored values | low | accept | Values are committed source under code review; the runtime renderers (37-02/03/04) escape editor content via renderCopy — see those plans' threat models |
| T-37-SC | Tampering | npm installs | low | accept | Zero new packages this plan (script uses global `fetch` + node built-ins); no package-legitimacy gate required |
</threat_model>

<verification>
- `npx vitest run src/__tests__/copy-import.test.ts` passes.
- Snapshot key-presence node check (Task 1 verify) passes.
- `env -u CMS_INTERNAL_URL -u STRAPI_WRITE_TOKEN node scripts/import-copy.mjs` exits 1 (fail-closed).
- `npx tsc --noEmit` still clean (JSON + .mjs add no type surface).
</verification>

<success_criteria>
All 63 bib donate/sponsor keys are present in copy-snapshot.json with verbatim wording; the import script is dependency-free, fail-closed, and uses a write token isolated from app runtime; the vitest pins the key set and the security boundary.
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-SUMMARY.md` when done.
</output>
