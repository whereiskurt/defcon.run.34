---
phase: 37-bib-donate-sponsor-proof-surface
plan: 05
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - apps/run.bib/webapp/src/components/SponsorForm.tsx
  - apps/run.bib/webapp/src/components/BibForm.tsx
  - apps/run.bib/webapp/src/components/BibPreview.tsx
  - apps/run.bib/webapp/src/components/RunnerCodeBadge.tsx
  - apps/run.bib/webapp/src/components/BurningBib.tsx
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "The SponsorForm amount labels, slider helper, payment method, provider pills+note, interpolated submit CTA, and error resolve through useCopy() (SC-1/SC-2)."
    - "The BibForm Save/Verifying/Saving/Cancel, locked hint, and error resolve through useCopy(); the RunnerCodeBadge eyebrow + Copy/Copied resolve through useCopy() (SC-1)."
    - "The BibPreview rubber-stamp copy (UNSAVED/DRAFT/PAID!/THANK YOU!) and the BurningBib alt text resolve through useCopy() — both render inside the BibForm client tree (D-08)."
  artifacts:
    - apps/run.bib/webapp/src/components/SponsorForm.tsx
    - apps/run.bib/webapp/src/components/BibPreview.tsx
  key_links:
    - "SponsorForm submit -> t('bib.checkout.cta', { label, amount }) (interpolated, SC-2)"
    - "BibPreview stamps -> useCopy() (client tree via BibForm — NOT loadCopy)"
---

<objective>
Migrate the client checkout form (`SponsorForm`, both bib + general variants) and the bib name-entry proof (`BibForm`, `BibPreview` stamps D-08, `RunnerCodeBadge`, `BurningBib` alt). All five render inside the client tree, so they resolve copy via `useCopy()`.

Purpose: Completes the client checkout + bib name-entry vertical (D-04/D-08), including the interpolated Sponsor/Donate CTA (SC-2). Reuses the shared `bib.checkout.*` keys de-duped in 37-01 (D-09).
Output: Five client files reading bib.sponsor.* / bib.donate.* / bib.checkout.* / bib.bibform.* / bib.status.* via useCopy().

MECHANISM NOTE (corrects the 37-CONTEXT "server" labels for two files): `BibPreview` and `BurningBib` are imported and rendered by `BibForm` (a "use client" component), so they run in the client bundle and MUST use `useCopy()` — they cannot call the server-only `loadCopy`. Their stamp/alt strings are static (no interpolation), so useCopy() resolves them O(1) from the provider/snapshot floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md

@apps/run.bib/webapp/src/components/CopyProvider.tsx
@apps/run.bib/webapp/src/lib/copy-core.ts
@apps/run.bib/webapp/src/__tests__/sponsor-form.test.ts
@apps/run.bib/webapp/src/__tests__/bib-preview.test.tsx
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SponsorForm copy via useCopy (shared bib.checkout.* + per-variant amount label)</name>
  <files>apps/run.bib/webapp/src/components/SponsorForm.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/SponsorForm.tsx (amount label `variant === "bib" ? "Sponsor amount" : "Donation amount"`; slider helper with `${minCents/100}`/`${AMOUNT_MAX_CENTS/100}`; "Payment method"; the Card/Cash App/Venmo ProviderPill labels; the organizer-confirm note; submit `${resolvedCtaLabel} ${displayAmount}` / "Redirecting…"; the error sentence with `{submit.detail}`)
    - apps/run.bib/webapp/src/__tests__/sponsor-form.test.ts (the exported pure helpers `clampForVariant`/`performSponsorCheckout` must keep working — do not change their signatures)
  </read_first>
  <action>
    Add `const { t } = useCopy();` in the SponsorForm component (import from @/components/CopyProvider). Resolve at render:
    - amount label → `variant === 'bib' ? t('bib.sponsor.amountLabel') : t('bib.donate.amountLabel')`.
    - slider helper → `t('bib.checkout.sliderHelper', { min: minCents / 100, max: AMOUNT_MAX_CENTS / 100 })`.
    - "Payment method" → `t('bib.checkout.paymentMethod')`; the three ProviderPill `label` props → `t('bib.checkout.providerCard')` / `providerCashApp` / `providerVenmo`.
    - organizer-confirm note → `t('bib.checkout.providerNote')`.
    - submit button: `submit.kind === 'submitting' ? t('bib.checkout.redirecting') : t('bib.checkout.cta', { label: resolvedCtaLabel, amount: displayAmount })`. `resolvedCtaLabel` default becomes `variant === 'bib' ? t('bib.contribution.sponsorVerb') : t('bib.contribution.donateVerb')` (the `ctaLabel` prop, when passed from ContributionTiles, is already a resolved string).
    - error alert → `t('bib.checkout.error', { detail: submit.detail })` (`{detail}` raw, D-03).
    Leave the two form `aria-label`s ("Sponsor a bib" / "Make a general donation") and the amount-input aria literal. Do NOT touch the exported pure helpers or the checkout/fetch logic.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.checkout.cta" src/components/SponsorForm.tsx && grep -c "bib.sponsor.amountLabel" src/components/SponsorForm.tsx && ! grep -q '"Payment method"' src/components/SponsorForm.tsx && npm test -- sponsor-form && npx tsc --noEmit</automated>
  </verify>
  <done>SponsorForm resolves all visible checkout copy via useCopy() incl. the interpolated CTA; the existing sponsor-form unit test still passes; typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: BibForm + RunnerCodeBadge copy via useCopy (bib.bibform.*)</name>
  <files>apps/run.bib/webapp/src/components/BibForm.tsx, apps/run.bib/webapp/src/components/RunnerCodeBadge.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/BibForm.tsx (Save/Verifying…/Saving… button text, Cancel button, the SaveStateHint locked line "Name locked for print…", and the error "Save failed (${state.detail}) — try again.")
    - apps/run.bib/webapp/src/components/RunnerCodeBadge.tsx ("Runner code" eyebrow, "Copy"/"Copied!" button)
  </read_first>
  <action>
    BibForm: add `const { t } = useCopy();` in the component. Resolve the Save-button ternary → `t('bib.bibform.verifying')` / `t('bib.bibform.saving')` / `t('bib.bibform.save')`; Cancel → `t('bib.bibform.cancel')`. In `SaveStateHint` (a nested helper), resolve the locked line → `t('bib.bibform.lockedHint')` and the error line → `t('bib.bibform.saveError', { detail: state.detail })` — pass `t` into SaveStateHint or inline it into the component so useCopy() is available. Leave `aria-label="Name on bib"`, `aria-describedby`, and `placeholder="1337"` untouched (placeholder is a design token per D-05 discretion).
    RunnerCodeBadge: add `const { t } = useCopy();`; eyebrow → `t('bib.bibform.runnerCodeLabel')`; button → `copied ? t('bib.bibform.copied') : t('bib.bibform.copy')`. Leave `aria-label="Your runner code"` / `aria-label="Copy runner code"` literal.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.bibform.save" src/components/BibForm.tsx && grep -c "bib.bibform.copied" src/components/RunnerCodeBadge.tsx && ! grep -q "Name locked for print" src/components/BibForm.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>BibForm buttons/hints + RunnerCodeBadge labels resolve via useCopy() with detail raw; aria/placeholder untouched; typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: BibPreview stamps + BurningBib alt via useCopy (D-08)</name>
  <files>apps/run.bib/webapp/src/components/BibPreview.tsx, apps/run.bib/webapp/src/components/BurningBib.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/components/BibPreview.tsx (the four rubber-stamp `<text>` values UNSAVED / DRAFT / PAID! / THANK YOU! — around lines 371/403/436/447; note the component currently has no "use client" but renders inside BibForm's client tree)
    - apps/run.bib/webapp/src/components/BurningBib.tsx (the `<img alt="Your bib is a dumpster fire." />`)
    - apps/run.bib/webapp/src/__tests__/bib-preview.test.tsx (renders BibPreview directly — it will need the useCopy floor; the snapshot floor makes useCopy resolve outside a provider, so the test still gets the stamp text; keep the test green)
  </read_first>
  <action>
    BibPreview: add `const { t } = useCopy();` in the component (import from @/components/CopyProvider — this pulls BibPreview explicitly into the client boundary, which is correct since BibForm already renders it client-side). Replace the four stamp `<text>` contents with `t('bib.status.stampUnsaved')` / `t('bib.status.stampDraft')` / `t('bib.status.stampPaid')` / `t('bib.status.stampThankYou')`. Leave every `aria-label` on the stamp `<g>` groups literal, and leave `PRIMARY_PLACEHOLDER = "1337"` as the design constant (D-05 discretion). Because useCopy() floors to the committed snapshot even outside a provider, the existing bib-preview.test.tsx keeps resolving the stamp text.
    BurningBib: add `const { t } = useCopy();`; set the img `alt` → `t('bib.status.burningBibAlt')` (this alt is explicitly in-scope per 37-CONTEXT). Keep the static GIF import + sizing untouched.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && grep -c "bib.status.stampPaid" src/components/BibPreview.tsx && grep -c "bib.status.burningBibAlt" src/components/BurningBib.tsx && npm test -- bib-preview && npx tsc --noEmit</automated>
  </verify>
  <done>All four BibPreview stamps + the BurningBib alt resolve via useCopy(); bib-preview.test.tsx still passes (snapshot floor); typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS editor → client render | Editor copy resolves via useCopy() in the checkout + bib-preview client tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering (XSS) | SponsorForm / BibForm / BibPreview | high | mitigate | Copy renders as React text children (auto-escaped), including inside SVG `<text>`. Interpolated `amount`/`detail` pass as `vars`; no `dangerouslySetInnerHTML`. |
| T-37-03 | Info-disclosure | client imports | high | mitigate | All five files import only useCopy from @/components/CopyProvider — never @/lib/copy. BibPreview is explicitly a client component here; enforced by 37-01 Test C. |
| T-37-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test -- sponsor-form` and `npm test -- bib-preview` stay green.
- Positive greps confirm bib.checkout.*/bib.bibform.*/bib.status.stamp* usage; negative greps confirm removed visible literals are gone.
</verification>

<success_criteria>
- Client checkout + bib name-entry + stamps + burning alt fully catalog-driven via useCopy() (SC-1), incl. the interpolated Sponsor/Donate CTA (SC-2).
- Shared bib.checkout.* keys reused (not re-authored) — D-09 de-dupe holds across DonateModal (37-03) and SponsorForm.
- BibPreview/BurningBib correctly use useCopy() (not loadCopy), matching their client-tree render context.
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-05-SUMMARY.md` when done
</output>
