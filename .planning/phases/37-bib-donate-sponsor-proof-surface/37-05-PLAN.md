---
phase: 37-bib-donate-sponsor-proof-surface
plan: 05
type: execute
wave: 3
depends_on: ["37-02", "37-03", "37-04"]
files_modified: []
autonomous: false
requirements: [MIGR-01]

user_setup:
  - service: strapi-cms
    why: "The D-01 import needs a WRITE-capable Strapi token to POST the authored copy rows; the runtime read-only STRAPI_API_TOKEN cannot create rows."
    env_vars:
      - name: STRAPI_WRITE_TOKEN
        source: "Strapi admin -> Settings -> API Tokens -> create a Full-access (or custom write) token for ui-string. Supply it ONLY at script runtime (shell env / CI secret) — never add it to the app's runtime env, .env committed files, or NEXT_PUBLIC_*."
      - name: CMS_INTERNAL_URL
        source: "The Strapi base URL reachable from where you run the import (e.g. http://localhost:1337 in dev)."

must_haves:
  truths:
    - "The import script has pushed every bib.* key into the live CMS, and `copy:snapshot` round-trips cleanly (SC-3 seeding proof, D-01)."
    - "Editing a bib donate/sponsor row in the CMS changes the rendered wording within the propagation window with no deploy (SC-3)."
    - "With the CMS unavailable the bib donate/sponsor copy still renders via the fallback chain — never a raw dotted key (SC-4)."
    - "Against a real build the sponsor/donate surface, its interpolated CTA, and the profile/QR/logout menus render from the catalog (SC-1, SC-2)."
  artifacts: []
  key_links:
    - "The write token is operator-supplied at import time only and never enters app runtime (T-37-01)."
---

<objective>
Verify all four Phase 37 success criteria against a real build — the parts that cannot be automated in the executor sandbox because they need a running Strapi with a write-capable token (SC-3 seed + live edit) and a CMS-down condition (SC-4). This is the phase's human-verify gate.

Purpose: Close SC-1..SC-4 end-to-end on the proof surface.
Output: Operator confirmation (or defect notes) covering import round-trip, live edit, fallback, and catalog render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-CONTEXT.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-01-SUMMARY.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-02-SUMMARY.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-03-SUMMARY.md
@.planning/phases/37-bib-donate-sponsor-proof-surface/37-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify SC-1..SC-4 against a real build (import round-trip, live edit, fallback, catalog render)</name>
  <action>Operator performs the how-to-verify steps below with a running Strapi + the write token from user_setup. This is a blocking human-verify gate: do not mark the phase complete until every step passes or defects are recorded.</action>
  <what-built>
    Waves 1-2 authored all bib donate/sponsor keys into copy-snapshot.json + an import script (37-01) and migrated the server surface (37-02), client checkout (37-03), and name-entry + menus (37-04) to resolve copy from the catalog. This checkpoint confirms the runtime behaviors that need a live CMS + a write token.
  </what-built>
  <how-to-verify>
    Run from apps/run.bib/webapp with a running Strapi reachable at CMS_INTERNAL_URL.

    1. SC-3 seed + round-trip (needs the WRITE token — see user_setup):
       - Export the write token: `export STRAPI_WRITE_TOKEN=... CMS_INTERNAL_URL=...`
       - `npm run copy:import` — confirm it reports created/updated rows and exits 0. Re-run it once and confirm it is idempotent (updates, not duplicates).
       - `npm run copy:snapshot` — confirm `git diff src/lib/copy-snapshot.json` shows no change (the CMS now round-trips back to the authored values). Unset STRAPI_WRITE_TOKEN afterward.
    2. SC-3 live edit: `npm run dev`, sign in, open /orderform. In the Strapi admin edit the value of `bib.orderform.sponsorTitle` (e.g. to "Back this bib"). Wait the revalidate window (~300s) or restart dev, reload /orderform, and confirm the Sponsor tile title changed with NO code deploy.
    3. SC-1 + SC-2 catalog render (real build): `npm run build && npm start`. On /orderform confirm the title, subtitle, both tiles, the Sponsor/Donate CTAs (interpolated amount, e.g. "Sponsor $20.00"), the payment-method pills, and the RunnerCodeBadge all render. Open the profile avatar menu (Profile/My Bib/Show My QR/Sign out) and the mobile hamburger menu — confirm labels render. Visit /sponsor/venmo?amount_cents=2500 and confirm "Pay via Venmo", "Send to", "Required comment", "Open Venmo", and the post-payment note with the runner code in a code element.
    4. SC-4 fallback (CMS down, no raw keys): stop Strapi (or point CMS_INTERNAL_URL at an unreachable host) and clear the Next data cache (restart the server). Reload /orderform and /sponsor/venmo and confirm EVERY string still shows real words from the snapshot floor — NO raw dotted key like `bib.orderform.title` appears anywhere on screen.
  </how-to-verify>
  <resume-signal>Type "approved" when SC-1..SC-4 all pass, or describe any surface that showed a raw dotted key, failed to update, or failed to fall back.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → import script | The write-capable Strapi token crosses into `copy:import` at shell runtime only |
| editor/CMS → live render | Edited ui-string values propagate to the app via the revalidate window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure / Elevation | STRAPI_WRITE_TOKEN | high | mitigate | Token exported in the operator's shell for the import only, never added to app runtime env / committed files / NEXT_PUBLIC_*; unset after import; the app runtime keeps using the read-only STRAPI_API_TOKEN |
| T-37-05a | Denial of Service | CMS-down render path | low | accept | SC-4 verifies the app renders from the cached snapshot floor when Strapi is unreachable — a CMS outage never breaks the surface |
</threat_model>

<verification>
- SC-3: copy:import round-trips (copy:snapshot shows no diff); a live CMS edit changes rendered wording with no deploy.
- SC-4: CMS-down reload shows real words, never a raw dotted key.
- SC-1/SC-2: the real build renders the whole donate/sponsor surface, the interpolated CTA, and the menus from the catalog.
</verification>

<success_criteria>
Operator confirms all four success criteria pass against a real build, with the write token used only at import time and never entering app runtime.
</success_criteria>

<output>
Create `.planning/phases/37-bib-donate-sponsor-proof-surface/37-05-SUMMARY.md` when done.
</output>
