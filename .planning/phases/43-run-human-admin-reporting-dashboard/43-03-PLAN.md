---
phase: 43-run-human-admin-reporting-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/entities/run-user.ts
  - apps/run.human/webapp/src/entities/auth-user.ts
  - apps/run.human/webapp/src/entities/bib.ts
  - apps/run.human/webapp/src/lib/quota-client.ts
autonomous: true
requirements: [ADMN-02, ADMN-03, ADMN-04, ADMN-05]
must_haves:
  truths:
    - "scanAllRunUsers() returns every RunUser row (full-table scan, ElectroDB auto-filters by entity) with displayName/createdAt/updatedAt/lastLoginAt/lastCheckInAt/checkInCount/hash/eqr."
    - "getAuthUserEmail(userId) returns the full email from the run.human Auth.js adapter USER# record (server-side only), or null."
    - "scanAccountSubs() returns an adapterUserId → OIDC-sub map from ONE authjs ACCOUNT# scan (bridges the run.human adapter-id ↔ run.bib OIDC-sub namespace gap)."
    - "scanRunnerCodesBySub() returns an ownerSub → runnerCode map from ONE Bib scan; joined with scanAccountSubs it yields per-user bib codes with no per-row fan-out."
    - "getQuotaByType(quotaId) fetches the run.auth bulk endpoint and returns [{ userId, consumptionCount, remaining, updatedAt }]."
  artifacts:
    - apps/run.human/webapp/src/entities/run-user.ts (scanAllRunUsers export)
    - apps/run.human/webapp/src/entities/auth-user.ts (getAuthUserEmail, getAuthUserEmails, scanAccountSubs exports)
    - apps/run.human/webapp/src/entities/bib.ts (scanRunnerCodesBySub export)
    - apps/run.human/webapp/src/lib/quota-client.ts (getQuotaByType export)
  key_links:
    - "Email source = run-human-authjs USER# record via dynamodbClient (NOT RunUser, NOT a cross-app call)."
    - "Bib code join = scanAccountSubs (adapterId→sub) ∘ scanRunnerCodesBySub (sub→runnerCode); two scans total, no per-row resolveOidcSub."
    - "getQuotaByType calls /api/internal/quota/by-type/{quotaId} via the existing quotaRequest (X-Internal-Secret + base URL)."
  prohibitions:
    - "These helpers MUST NOT be imported into any client component — emails and the scan are server-side only."
    - "No new DynamoDB schema/GSI is defined; run-user.ts and bib.ts changes are read-only scan/map wrappers only."
    - "Bib code resolution MUST NOT call getRunnerCode/resolveOidcSub once per row (no N fan-out) — build the maps once and join."
---

<objective>
Build the read-only data helpers the admin API/page will join: a RunUser full-table
scan (ADMN-02), a server-side email lookup from run.human's own Auth.js adapter table
(ADMN-03 source), the two namespace maps that resolve bib runner codes without per-row
fan-out (ADMN-05), and a bulk gpx-usage client for the new run.auth endpoint (ADMN-04
join). All are independent, server-only reads with concrete analogs.

Purpose: Supply the raw signals + join maps; assembly/masking/CSV live in Plan 04.
Output: `scanAllRunUsers`, `getAuthUserEmail`/`getAuthUserEmails`, `scanAccountSubs`,
`scanRunnerCodesBySub`, `getQuotaByType`.

NO schema change: the run-user.ts and bib.ts edits add scan/map wrappers next to the
existing readers; they do NOT alter any entity definition (the `src/entities/**`
schema gate is a false positive here — see plan note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-run-human-admin-reporting-dashboard/43-CONTEXT.md
@.planning/phases/43-run-human-admin-reporting-dashboard/43-PATTERNS.md
@apps/run.human/webapp/src/entities/run-user.ts
@apps/run.human/webapp/src/entities/client.ts
@apps/run.human/webapp/src/entities/bib.ts
@apps/run.human/webapp/src/lib/quota-client.ts
</context>

<artifacts>
This plan produces:
- `scanAllRunUsers(): Promise<RunUserItem[]>` — new export in run-user.ts
- `apps/run.human/webapp/src/entities/auth-user.ts` (NEW): `getAuthUserEmail(userId): Promise<string | null>`, `getAuthUserEmails(userIds): Promise<Record<string,string|null>>`, `scanAccountSubs(): Promise<Record<string,string>>` (adapterUserId → OIDC sub)
- `scanRunnerCodesBySub(): Promise<Record<string,string>>` — new export in bib.ts (ownerSub → runnerCode)
- `getQuotaByType(quotaId: QuotaId)` — new export in quota-client.ts returning `Array<{ userId; consumptionCount; remaining; updatedAt }>`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Add scanAllRunUsers read-only scan wrapper</name>
  <files>apps/run.human/webapp/src/entities/run-user.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/run-user.ts:236-239 (getRunUser — where to add the sibling export)
    - apps/run.human/webapp/src/entities/run-user.ts:326-350 (RunUserItem exported row shape)
    - apps/run.bib/webapp/src/lib/admin-reports.ts:421-429 (Bib.scan.go({ pages: "all" }) idiom to mirror)
  </read_first>
  <action>
    Add `export async function scanAllRunUsers(): Promise<RunUserItem[]>` beside getRunUser. Body: `const result = await RunUser.scan.go({ pages: "all" }); return result.data;`. ElectroDB auto-filters the scan to the RunUser entity, so no manual entity filter is needed. Do NOT modify the entity schema, GSIs, or attributes — this is purely a read wrapper. Add a one-line comment that this is a full-table scan acceptable at event scale (hundreds–low-thousands), per the phase decision. Reuse the already-exported RunUserItem type for the return.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q "export async function scanAllRunUsers" src/entities/run-user.ts && grep -q "RunUser.scan.go" src/entities/run-user.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - scanAllRunUsers exported, returns RunUserItem[] via RunUser.scan.go({ pages: "all" }).
    - No change to the RunUser entity schema/GSIs (diff limited to the new function + comment).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add authjs-table reads — email lookup + adapterId→sub map</name>
  <files>apps/run.human/webapp/src/entities/auth-user.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/bib.ts:14-23 (namespace note: run.human session.user.id = adapter uuid; bib keyed by OIDC sub via ACCOUNT# providerAccountId)
    - apps/run.human/webapp/src/entities/bib.ts:59-72 (resolveOidcSub — proven ACCOUNT# query: pk=USER#{id}, begins_with(sk, ACCOUNT#run.defcon.run#), ProjectionExpression providerAccountId)
    - apps/run.human/webapp/src/entities/client.ts (dynamodbClient + DYNAMODB_TABLE exports)
  </read_first>
  <action>
    Create auth-user.ts. Export `getAuthUserEmail(userId: string): Promise<string | null>`: import `dynamodbClient`/`DYNAMODB_TABLE` from ./client, do a direct `dynamodbClient.get({ TableName: DYNAMODB_TABLE, Key: { pk: \`USER#${userId}\`, sk: \`USER#${userId}\` }, ProjectionExpression: "email" })`, return `(res.Item?.email as string | undefined) ?? null`. VERIFY the USER# sk shape against a real run-human-authjs record before finalizing (adapter default is pk=sk=USER#{id}, but confirm — the ACCOUNT# query in bib.ts:60-69 is the proven read in this table); if the live record differs, fall back to the adapter GSI1 (GSI1PK=USER#{id}) query. Also export `getAuthUserEmails(userIds: string[]): Promise<Record<string,string|null>>` (batched/sequential get) so Plan 04 resolves the whole list without duplicating key logic. Finally export `scanAccountSubs(): Promise<Record<string,string>>` that scans the authjs table for `ACCOUNT#${OIDC_PROVIDER}#` items (OIDC_PROVIDER = "run.defcon.run"), projecting the ACCOUNT# record's user pk + providerAccountId, and returns an adapterUserId → OIDC-sub map in ONE scan — this is the fan-out-free bridge Plan 04 composes with the Bib map. All exports return FULL emails/subs and are server-only; never import into a client component.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q "export async function getAuthUserEmail" src/entities/auth-user.ts && grep -q "export async function scanAccountSubs" src/entities/auth-user.ts && grep -q "DYNAMODB_TABLE" src/entities/auth-user.ts && grep -q "USER#" src/entities/auth-user.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getAuthUserEmail + getAuthUserEmails + scanAccountSubs exported; all read the authjs table via dynamodbClient.
    - scanAccountSubs builds the adapterId→sub map in a single scan (no per-user query loop).
    - USER# key shape confirmed against a live record (or GSI1 fallback wired) — noted in the SUMMARY.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add bib runner-code map + bulk gpx-usage client</name>
  <files>apps/run.human/webapp/src/entities/bib.ts, apps/run.human/webapp/src/lib/quota-client.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/bib.ts:29-84 (Bib entity + getRunnerCode — reuse the Bib entity; add a scan-based map beside getRunnerCode)
    - apps/run.human/webapp/src/lib/quota-client.ts:82-118 (quotaRequest helper — sets X-Internal-Secret + base URL; getUserQuotas analog)
    - apps/run.human/webapp/src/lib/quota-client.ts:18-29 (QuotaId union incl. gpx_upload/gpx_save/gpx_share)
    - apps/run.auth/webapp/src/app/api/internal/quota/by-type/[quotaId]/route.ts (Plan 01 endpoint contract — returns the bare array)
  </read_first>
  <action>
    (a) In bib.ts add `export async function scanRunnerCodesBySub(): Promise<Record<string,string>>`: `const result = await Bib.scan.go({ pages: "all" });` then reduce `result.data` to an `ownerSub → runnerCode` map (skip rows without a runnerCode). Keep getRunnerCode untouched. Do NOT change the Bib entity definition. Plan 04 composes this with scanAccountSubs (adapterId→sub) to get per-user bib codes with zero per-row resolveOidcSub calls (avoids N fan-out per CONTEXT 76-79). (b) In quota-client.ts add `export async function getQuotaByType(quotaId: QuotaId)` returning `quotaRequest<Array<{ userId: string; consumptionCount: number; remaining: number; updatedAt: number }>>(\`/api/internal/quota/by-type/${quotaId}\`)`. Reuse quotaRequest verbatim (already attaches config.auth.internalSecret + config.urls.privateAuthServer); do not hand-roll a fetch. Degrade to empty on failure the way getUserQuotas does so a quota-service outage does not crash the page.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q "export async function scanRunnerCodesBySub" src/entities/bib.ts && grep -q "Bib.scan.go" src/entities/bib.ts && grep -q "export async function getQuotaByType" src/lib/quota-client.ts && grep -q "by-type" src/lib/quota-client.ts && grep -q "quotaRequest" src/lib/quota-client.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - scanRunnerCodesBySub returns an ownerSub→runnerCode map from ONE Bib scan; getRunnerCode + Bib entity unchanged.
    - getQuotaByType calls /api/internal/quota/by-type/{quotaId} via quotaRequest and degrades to empty on failure.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-only data helpers → client | Emails and the full user scan must never cross to the browser except via the masked/gated surfaces in Plans 04/05. |
| run.human → run.auth bulk quota endpoint | Fetch carries X-Internal-Secret via quotaRequest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-03 | Information disclosure (PII) | getAuthUserEmail | high | mitigate | Returns full email only server-side; helpers live in entities/lib (server modules), never imported by client components. Masking/reveal enforced downstream (Plan 04). ProjectionExpression limits the read to the email attribute. |
| T-43-04 | Information disclosure (enumeration) | scanAllRunUsers + email map | medium | mitigate | Scan + email resolution are server-only building blocks; no unauthenticated entry point. The only caller is the admin-gated API (Plan 04). |
| T-43-05 | Spoofing | getQuotaByType | medium | mitigate | Reuses quotaRequest which attaches the internal secret; no new auth surface introduced. |
| T-43-SC | Tampering (supply chain) | dependencies | n/a | accept | No new npm packages; no legitimacy gate required. |
</threat_model>

<verification>
- All three helpers are server-only reads with the documented shapes.
- No entity schema/GSI change; run-user.ts diff is the scan wrapper only.
- Typecheck passes; USER# key shape confirmed against a live record.
</verification>

<success_criteria>
- ADMN-02/03/04/05 (data layer): user scan, server-side email lookup + email map, adapterId→sub + sub→runnerCode maps (fan-out-free bib codes), and bulk gpx-usage fetch exist and typecheck, ready for join in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/43-run-human-admin-reporting-dashboard/43-03-SUMMARY.md` when done.
</output>
