---
phase: 43-run-human-admin-reporting-dashboard
plan: 05
type: execute
wave: 3
depends_on: [43-02, 43-04]
files_modified:
  - apps/run.human/webapp/src/app/admin/page.tsx
autonomous: false
requirements: [ADMN-01, ADMN-06, ADMN-07]
must_haves:
  truths:
    - "The /admin page is a server component (runtime=nodejs, dynamic=force-dynamic) that gates with requireAdmin + revalidateAdmin on entry; a non-admin or stale-admin sees notFound() (404)."
    - "It renders summary tiles (total users · new signups 7d · active 7d · gpx-active) and a sortable, paginated user table defaulting to most-recently-active."
    - "Emails render masked by default; a per-row reveal shows one full email; a search box filters by full email server-side (via ?q); no unrevealed full-email column is rendered."
    - "A Download CSV control links to /api/admin/users?format=csv with the current filter/sort, region-basePath-prefixed."
  artifacts:
    - apps/run.human/webapp/src/app/admin/page.tsx (server component page + inline tiles/table)
  key_links:
    - "Gate = requireAdmin + revalidateAdmin (Plan 02); data = buildUserReport/summaryTiles/sortRows/filterByEmail/maskEmail (Plan 04)."
    - "CSV link target = /api/admin/users?format=csv (Plan 04), prefixed with the region basePath in production."
  prohibitions:
    - "Non-admin / unauthenticated / stale-admin entry MUST render notFound() (404) — never a 403 page and never the dashboard."
    - "The page MUST NOT render an unrevealed full-email column; only masked emails, plus the one explicitly revealed row."
    - "The page MUST NOT ship full emails to the client except for the row explicitly revealed via ?reveal."
---

<objective>
The user-facing surface: a boring, single-file `/admin` server component that gates on
entry with fresh-claims revalidation (ADMN-01), renders summary tiles + a
sortable/paginated user table (ADMN-06) with masked emails + per-row reveal + full-email
search, and a Download CSV control (ADMN-07). All state is URL-param driven
(searchParams) and rendered server-side; the only client-privileged data is the one
explicitly revealed email.

Purpose: Give admins the who-signed-up / who-uses-gpx view.
Output: `app/admin/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-run-human-admin-reporting-dashboard/43-CONTEXT.md
@.planning/phases/43-run-human-admin-reporting-dashboard/43-PATTERNS.md
@apps/run.bib/webapp/src/app/admin/page.tsx
@apps/run.human/webapp/src/lib/admin-gate.ts
@apps/run.human/webapp/src/lib/admin-report.ts
</context>

<artifacts>
This plan produces:
- `apps/run.human/webapp/src/app/admin/page.tsx` (NEW): default async server component `AdminPage({ searchParams })` with inline summary tiles + user table, per-row reveal link, email search form, sort header links, pagination, and a Download CSV anchor.
- Route `/admin` (region-prefixed in production), admin-gated, 404 for everyone else.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: /admin server component — gate, tiles, table, reveal, search, CSV link</name>
  <files>apps/run.human/webapp/src/app/admin/page.tsx</files>
  <read_first>
    - apps/run.bib/webapp/src/app/admin/page.tsx:31-57 (server component skeleton + gate — replace Forbidden with notFound; add revalidation)
    - apps/run.bib/webapp/src/app/admin/page.tsx:39-43 and :689-702 (apiBase region-basePath idiom for the CSV anchor)
    - apps/run.bib/webapp/src/app/admin/page.tsx:352-359 (Chip / tile) and :712-805 (Table with sticky first column + horizontal scroll)
    - apps/run.human/webapp/src/lib/admin-gate.ts (requireAdmin + revalidateAdmin — Plan 02)
    - apps/run.human/webapp/src/lib/admin-report.ts (buildUserReport, summaryTiles, sortRows, filterByEmail, maskEmail, runnerQrUrl — Plan 04)
  </read_first>
  <action>
    Create page.tsx with `export const runtime = "nodejs"` and `export const dynamic = "force-dynamic"`. `export default async function AdminPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> })`. Flow: `const session = await auth()`; `const gate = requireAdmin(session)`; if `!gate.ok && gate.reason === "no_session"` you MAY redirect to signin, otherwise on `!gate.ok` call `notFound()` (import from "next/navigation"). Then `if (!(await revalidateAdmin(session.user.id))) notFound()` — stale admin also 404. Load `const rows = await buildUserReport()`; read searchParams `q`, `sort` (default lastActivity), `page`/`pageSize`, `reveal`. Apply `filterByEmail(rows, q)` then `sortRows(..., sort)`; compute `summaryTiles(filtered)` for the tiles; slice the page. Render: (a) four summary tiles reusing the bib Chip/tile inline style (total users · new 7d · active 7d · gpx-active); (b) a GET `<form>` search box that sets `?q=` (server-side full-email filter); (c) the table reusing the bib inline Table (sticky first col + horizontal scroll) with columns displayName · email · bib code · runner QR URL · signed-up · last login · last activity · check-ins · gpx routes/saves/shares · uploads · services. Render each email via `maskEmail(row.emailFull)` UNLESS `reveal === row.userId`, in which case show the full email; each masked cell links to `?reveal=<userId>` (preserving q/sort/page) to reveal that one row. Sort headers for last-activity / gpx-usage / signup link to `?sort=`. QR URL cell renders `runnerQrUrl(row.qrUrl or hash)` as a clickable/copyable link; bib code blank when absent. (d) A "Download CSV" anchor `<a href={\`${apiBase()}/api/admin/users?format=csv&...\`}>` carrying the current q/sort so the export matches the view — reuse the bib apiBase() region-basePath idiom (plain <a> is NOT auto-prefixed in production). Keep everything inline/single-file per the boring-first convention; do not add a table library unless proven insufficient. Do NOT render any column that would show unrevealed full emails.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -q 'runtime = "nodejs"' src/app/admin/page.tsx && grep -q 'dynamic = "force-dynamic"' src/app/admin/page.tsx && grep -q "notFound" src/app/admin/page.tsx && grep -q "revalidateAdmin" src/app/admin/page.tsx && grep -q "buildUserReport" src/app/admin/page.tsx && grep -q "maskEmail" src/app/admin/page.tsx && grep -q "format=csv" src/app/admin/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Non-admin / unauthenticated / stale-admin entry renders notFound() (404); admins get the dashboard.
    - Tiles (total · new 7d · active 7d · gpx-active) + sortable (last-activity default, gpx-usage, signup) paginated table render.
    - Emails masked by default; per-row reveal shows one full email; search filters by full email via ?q; no unrevealed full-email column.
    - Download CSV anchor targets /api/admin/users?format=csv with the current filter/sort, region-basePath-prefixed.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the /admin dashboard (gate + UX + CSV)</name>
  <action>Human verifies the assembled dashboard end to end per the steps below; this checkpoint gates phase completion (no code change in this task).</action>
  <what-built>The `/admin` dashboard end to end: admin-gated page with summary tiles, sortable/paginated user table, masked emails with per-row reveal, full-email search, runner QR URL + bib code columns, and CSV export.</what-built>
  <how-to-verify>
    1. Run run.human locally: from `apps/run.human/webapp`, `PORT=3001 npm run dev` (ensure AUTH_INTERNAL_SECRET + run.auth reachable so revalidateAdmin + getQuotaByType resolve).
    2. As an admin (Kurt/Jesse — a user with "admin" in services), visit `http://localhost:3001/admin`. Confirm the tiles show counts and the table lists users defaulting to most-recently-active.
    3. Click a masked email → that one row reveals the full email; others stay masked. Type a full email in the search box → the list filters to that user server-side.
    4. Click the gpx-usage and signup sort headers → order changes. Page through the table.
    5. Confirm the runner QR URL column links to `https://run.<domain>/<region>/r?h=...` and bib code shows for users with a bib (blank otherwise).
    6. Click Download CSV → a `run-users-YYYY-MM-DD.csv` downloads with FULL emails/QR/bib codes matching the current filter/sort.
    7. Sign in as a NON-admin (or a session whose admin was just revoked in run.auth) and visit `/admin` → confirm a 404 (not a 403, not the dashboard) on BOTH the page and `GET /api/admin/users`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any user → /admin page | Entry must pass requireAdmin + fresh-claims revalidation. |
| server render → browser DOM | Only masked emails cross, except the single explicitly revealed row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Elevation of privilege | page gate | high | mitigate | requireAdmin (services) + synchronous revalidateAdmin on entry; both the page and its data route (Plan 04) gate identically. |
| T-43-06 | Info disclosure (staleness) | page entry | high | mitigate | revalidateAdmin on entry denies a revoked-but-cached admin (notFound). |
| T-43-02 | Info disclosure (route existence) | denial path | medium | mitigate | Denial renders `notFound()` (404), not a 403 page — route not advertised. |
| T-43-03 | Info disclosure (PII) | rendered table | high | mitigate | Emails rendered via maskEmail; full email only on explicit per-row reveal; no unrevealed full-email column. Full-data lives only in the gated CSV. |
| T-43-SC | Tampering (supply chain) | dependencies | n/a | accept | No new npm packages (inline tiles/table reuse existing bib idioms). No legitimacy gate required. |
</threat_model>

<verification>
- Admin sees the dashboard; non-admin/stale-admin gets 404 on page + API.
- Masking + reveal + search behave; sort/pagination work; CSV matches the current view.
- Typecheck passes; human checkpoint confirms the end-to-end UX.
</verification>

<success_criteria>
- ADMN-01/06/07 (surface): the gated dashboard with tiles, sortable/paginated masked table, reveal, search, and CSV export renders for admins and 404s for everyone else.
</success_criteria>

<output>
Create `.planning/phases/43-run-human-admin-reporting-dashboard/43-05-SUMMARY.md` when done.
</output>
