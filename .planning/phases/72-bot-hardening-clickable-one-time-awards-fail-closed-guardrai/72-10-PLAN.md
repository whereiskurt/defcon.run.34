---
phase: 72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai
plan: 10
type: execute
wave: 6
depends_on: ["72-09"]
files_modified:
  - .planning/todos/pending/2026-07-31-llm-rate-limiting-bedrock-ceiling.md
  - .planning/todos/pending/2026-07-31-per-bot-mint-secret-scoping.md
  - .planning/todos/pending/2026-07-31-bots-cac1-deployment.md
autonomous: false
requirements: [BOT-01]

must_haves:
  truths:
    - "The static interstitial that published a claim code in plaintext is GONE — the URL returns a non-200 through CloudFront."
    - "The Qr row that pointed at it is deleted."
    - "A hardware UAT confirms the end-to-end shape: DM ricky, 58 numbered lines, a tappable award, first tap awards 100, second tap no-ops."
    - "The three deliberately-deferred items are recorded as todos rather than lost."
  artifacts:
    - .planning/todos/pending/2026-07-31-llm-rate-limiting-bedrock-ceiling.md
    - .planning/todos/pending/2026-07-31-per-bot-mint-secret-scoping.md
    - .planning/todos/pending/2026-07-31-bots-cac1-deployment.md
  key_links:
    - "rotate script teardown stage -> Qr row delete + S3 object delete -> CloudFront invalidation -> old URL dead"
---

<objective>
Tear down the old, freely-shareable ricky path now that the new one is deployed and
verified, run the hardware UAT, and record the three items the owner deliberately
deferred.

This is the irreversible half of the rotation and it runs LAST on purpose. 72-08 already
made the old code stop awarding; this plan removes the page that published it and the
short-link row that pointed there. Doing it in this order means there was never a window
where the old path was dead and the new one was not yet live.

Purpose: close the loop and leave nothing dangling.
Output: a dead legacy path, a hardware-confirmed award flow, and three filed todos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-31-bot-hardening-design.md
@.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-08-SUMMARY.md
@.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-09-SUMMARY.md
</context>

<interface_context>
Verified against live AWS on 2026-07-31:

- The static interstitial object lives at key `qr/rick_astley_loves_desert_running` in
  bucket `defcon-run-static-20240523-v1` (474 bytes, last modified 2026-07-30), readable
  and deletable with `AWS_PROFILE=sudo-management` — NOT the application profile.
- It is fronted by the landing CloudFront distribution `ETHVMDHQC21EG`; deleting the
  object without invalidating leaves it served from edge cache.
- The DynamoDB row to delete: `pk = "$run#code_rick_astley_loves_desert_running"`,
  `sk = "$qr_1"`, entity `Qr`, in table `run-human-electro` (us-east-1), reachable with
  `AWS_PROFILE=dc34-application`.
- That row does NOT resolve on q.defcon.run — `q.defcon.run/rick_astley_loves_desert_running`
  is a 404. The live path is the static S3 page on `defcon.run`, which is a different
  distribution and a different origin. Both must go.
- `apps/run.human/webapp/scripts/rotate-ricky-flag.mts` (from 72-08) already contains the
  teardown stage behind its own explicit flag, DRY-RUN by default.
- Deferred items to file as todos, from the design's Out of Scope section:
  (a) LLM rate limiting / Bedrock cost ceiling — `llm.go` has NO limiter of any kind and
      the only throttle anywhere is the 30-second byte-identical dedup window, so abuse
      and spend are both unbounded;
  (b) per-bot scoping of the mint internal secret — one shared secret can mint any flag;
  (c) cac1 deployment — the bots stay single-region us-east-1.
</interface_context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review the teardown DRY-RUN, then authorise the deletions</name>
  <files>apps/run.human/webapp/scripts/rotate-ricky-flag.mts</files>
  <read_first>
    - apps/run.human/webapp/scripts/rotate-ricky-flag.mts (the teardown stage written in 72-08)
    - .planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-09-SUMMARY.md (deployment evidence)
  </read_first>
  <action>
Re-probe the live award namespace to confirm the NEW path works before destroying the old
one, then run the teardown stage without its confirm flag and present the DRY-RUN to the
operator for the blocking review below.

On approval, re-run the teardown stage with its confirm flag, then invalidate the landing
CloudFront distribution for the affected path and wait for the invalidation to reach
Completed before re-probing. Deleting the origin object without invalidating leaves the
page alive at the edge, which would defeat the purpose entirely.

This is irreversible. Do not run it if the award-namespace probe does not return a 302.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' --max-time 10 https://defcon.run/qr/rick_astley_loves_desert_running | grep -qv '^200$' && echo "legacy interstitial is dead"</automated>
  </verify>
  <done>The award namespace was confirmed live, the DRY-RUN was reviewed and approved, both deletions completed, the invalidation reached Completed, and the old URL no longer returns 200.</done>
  <what-built>
    The teardown stage of `scripts/rotate-ricky-flag.mts` deletes the `Qr` row and the
    static S3 interstitial. This is IRREVERSIBLE. The new award path is already deployed
    and verified by 72-09.
  </what-built>
  <how-to-verify>
    1. Re-confirm the new path works before destroying the old one. From 72-09's
       evidence, the ghosts container is running the new revision. Confirm the live
       resolver still serves the award namespace:
       `curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' https://q.defcon.run/a/probe`
       must be a 302 to the claim page carrying `nonce=probe`.
    2. Run the teardown stage WITHOUT the confirm flag and read the DRY-RUN. It must name
       exactly two targets: the one DynamoDB row by its composite key, and the one S3
       object by bucket and key. Confirm the bucket is the landing bucket and the profile
       is `sudo-management` for the S3 half and `dc34-application` for the DynamoDB half.
    3. Confirm the DRY-RUN reports it deleted nothing.
    4. On approval, re-run with the confirm flag.
    5. Invalidate the landing CloudFront distribution for the affected path, then wait for
       the invalidation to report Completed before probing. Deleting the origin object
       without invalidating leaves the page alive at the edge, which would defeat the
       whole point.
    6. Probe the old URL and confirm it no longer returns the interstitial:
       `curl -s -o /dev/null -w '%{http_code}\n' https://defcon.run/qr/rick_astley_loves_desert_running`
       must NOT be 200.
  </how-to-verify>
  <acceptance_criteria>
    - The award namespace probe returned 302 BEFORE any deletion.
    - The DRY-RUN named exactly two targets and deleted nothing.
    - Both deletions completed under the confirm flag.
    - The CloudFront invalidation reached Completed.
    - The old URL returns a non-200 status.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorise the irreversible teardown, or describe what looks wrong in the DRY-RUN.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hardware UAT — DM ricky end to end</name>
  <files>.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-09-SUMMARY.md (what shipped and at which versions)
    - docs/superpowers/specs/2026-07-31-bot-hardening-design.md (the UAT section)
  </read_first>
  <action>
Hand the ten-step hardware script below to Kurt and hold. This step cannot be automated —
it requires a real radio on the dc.run mesh and a signed-in browser session, and the
specific defect being retested (a line-01 drop) was only ever observed on hardware,
downstream of MQTT at the iOS-proxy-to-BLE hop.

Record the result verbatim in the SUMMARY. If a step fails, write down exactly what was
seen rather than paraphrasing it — the failure detail is the whole diagnostic value.
  </action>
  <verify>
    <human-check>Kurt runs the ten-step script on hardware and reports per-step results.</human-check>
  </verify>
  <done>All required UAT steps pass on hardware, or the failures are recorded verbatim in the SUMMARY.</done>
  <what-built>
    The complete phase: ricky's song ends at 58 numbered lines on the real closing lyric
    followed by two reliable award DMs carrying a 35-character single-use claim link;
    guardrails run fail-closed with an in-persona degradation line; concurrent
    performances are capped at 12.
  </what-built>
  <how-to-verify>
    Kurt, on real hardware, on the dc.run mesh:

    1. DM ricky to trigger the song.
    2. Confirm line `01:` ARRIVES. This is the specific drop that was observed live and
       that the reliable path is meant to fix — if line 01 is missing, the fix did not work.
    3. Let the song play. Confirm it ends at line `58:` with the real closing lyric, and
       that NO line contains a bare slash-prefixed path.
    4. Confirm two further messages arrive after the song: an award headline, then a link
       on its own line. Confirm the link is short (roughly 35 characters) and that your
       client renders it as TAPPABLE.
    5. Tap it while signed in to run.defcon.run. Confirm 100 points are awarded.
    6. Tap the SAME link again. Confirm it no-ops — no second award. This is the
       "genuinely single-use" property; a forwarded link must award at most once.
    7. Forward the link to a second signed-in account and tap. Confirm no award.
    8. DM ricky again immediately. Confirm you get the encore-cooldown notice, not silence
       and not a second song.
    9. Confirm the old page is dead: open
       `https://defcon.run/qr/rick_astley_loves_desert_running` and confirm it does not
       serve the interstitial.
    10. Optional, if convenient: have several radios request the song at once and confirm
        that beyond the cap a requester gets a stage-is-full reply rather than silence.
  </how-to-verify>
  <acceptance_criteria>
    - Line 01 arrived.
    - The song ended at line 58 on the closing lyric, with no path-like line.
    - Two award messages arrived; the link was short and tappable.
    - First tap awarded 100; second tap no-opped; a forwarded tap awarded nothing.
    - An immediate repeat request produced the encore notice, not silence.
    - The old interstitial URL no longer serves the page.
    - Any failure is recorded verbatim in the SUMMARY rather than summarised away.
  </acceptance_criteria>
  <resume-signal>Type "approved" if every step passed, or describe exactly which step failed and what you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: File the three deferred items as todos</name>
  <files>.planning/todos/pending/2026-07-31-llm-rate-limiting-bedrock-ceiling.md, .planning/todos/pending/2026-07-31-per-bot-mint-secret-scoping.md, .planning/todos/pending/2026-07-31-bots-cac1-deployment.md</files>
  <read_first>
    - .planning/todos/pending/ (an existing todo file, for the format this repo uses)
    - docs/superpowers/specs/2026-07-31-bot-hardening-design.md (the Out of Scope section)
  </read_first>
  <action>
Write one todo per deferred item, matching the existing format in the pending directory.
Each carries the concrete evidence gathered during this phase so a future session does not
have to re-derive it.

For the LLM rate limiting item, be blunt about the state: there is no limiter of any kind
in the model-call path, and the only throttle anywhere is a 30-second window that
collapses byte-identical repeats only. Every unlocked DM is a model call with no per-radio
budget and no global concurrency cap, so both abuse and spend are unbounded. Note that the
lyric semaphore added in this phase bounds LYRIC fan-out only and does nothing for model
calls. Note that it was raised during design and consciously deferred by the owner, so a
future reader understands this is a decision rather than an oversight.

For the mint secret item, record that one shared internal secret authorises minting for
any flag, and point at the mint route and the ghosts container's secret wiring.

For the cac1 item, record that the bots remain single-region and name what would have to
move: the ghosts container env, the fallback secret, and the region's skip status.

Do not re-open any of these in this phase.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/2026-07-31-llm-rate-limiting-bedrock-ceiling.md .planning/todos/pending/2026-07-31-per-bot-mint-secret-scoping.md .planning/todos/pending/2026-07-31-bots-cac1-deployment.md && grep -lc . .planning/todos/pending/2026-07-31-*.md | wc -l | grep -qx 3 && echo "3 todos filed"</automated>
  </verify>
  <acceptance_criteria>
    - All three todo files exist and are non-empty.
    - Each matches the format of the existing files in the pending directory.
    - Each names the specific code location the future session should start from.
    - None of them contains a flag code, claim URL or nonce.
  </acceptance_criteria>
  <done>The three deliberately-deferred items are recorded with the evidence gathered during this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> landing S3 bucket (sudo-management profile) | Irreversible deletion of a public asset under an elevated profile |
| operator workstation -> production DynamoDB | Irreversible row deletion |
| CloudFront edge cache | A deleted origin object stays served until invalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-54 | Denial of service | tearing down before the new path is proven | high | mitigate | Sequenced after 72-09's verification, and the live award-namespace probe is re-run as the first step of the teardown checkpoint |
| T-72-55 | Information disclosure | code still served from the CloudFront edge after origin deletion | high | mitigate | Explicit invalidation of the landing distribution, waited to Completed, then re-probed for a non-200 |
| T-72-56 | Tampering | deleting the wrong object or row under an elevated profile | high | mitigate | DRY-RUN names exactly two targets with bucket, key, table and composite key, reviewed at a blocking checkpoint before the confirm flag |
| T-72-57 | Repudiation | UAT failures being summarised away | medium | mitigate | Acceptance requires failures to be recorded verbatim in the SUMMARY |
| T-72-58 | Elevation of privilege | unbounded model spend and abuse | high | accept | Explicitly deferred by the owner during design; filed as a todo with the evidence so the acceptance is recorded rather than forgotten |
</threat_model>

<verification>
1. Award-namespace probe returned 302 before any deletion.
2. Teardown DRY-RUN reviewed at a blocking checkpoint; confirm run completed.
3. CloudFront invalidation Completed; the old URL returns a non-200.
4. Hardware UAT passed all nine required steps.
5. Three todo files exist under `.planning/todos/pending/`.

PHASE CLOSE: after this plan, update STATE.md and mark Phase 72 complete in ROADMAP.md.
Remember AGENTS.md "Landing the Plane" — the work is not complete until `git push`
succeeds and `git status` reports up to date with origin.
</verification>

<success_criteria>
- The static interstitial and its Qr row are gone; the old URL is dead through CloudFront.
- Hardware UAT confirms line 01 arrives, the song ends at 58, the award is tappable, the
  first tap awards and every subsequent tap does not.
- The three deferred items are filed as todos.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| Deleted row | `$run#code_rick_astley_loves_desert_running` / `$qr_1` | DynamoDB `run-human-electro` |
| Deleted object | key `qr/rick_astley_loves_desert_running` | s3 `defcon-run-static-20240523-v1` (profile `sudo-management`) |
| CloudFront invalidation | landing distribution `ETHVMDHQC21EG` | us-east-1 |
| New todo | LLM rate limiting / Bedrock cost ceiling (unbounded abuse + spend) | `.planning/todos/pending/2026-07-31-llm-rate-limiting-bedrock-ceiling.md` |
| New todo | per-bot scoping of the mint internal secret | `.planning/todos/pending/2026-07-31-per-bot-mint-secret-scoping.md` |
| New todo | cac1 deployment for the bots | `.planning/todos/pending/2026-07-31-bots-cac1-deployment.md` |
| Evidence | hardware UAT transcript | plan SUMMARY |

No new symbols, env vars or dependencies. This plan only removes and records.
</artifacts_produced>

<output>
Create `.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-10-SUMMARY.md` when done. The SUMMARY must NOT contain any flag code, claim URL or nonce.
</output>
