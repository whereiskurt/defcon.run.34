---
phase: 72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/terraform/live/site/services/run.mqtt/service.hcl
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/.secrets.sops.json.template
  - infra/terraform/modules/admin-reports/v1.0.0/variables.tf
  - infra/terraform/modules/admin-reports/v1.0.0/metrics.tf
  - infra/terraform/modules/admin-reports/v1.0.0/alarms.tf
  - infra/terraform/modules/admin-reports/v1.0.0/outputs.tf
  - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl
autonomous: true
requirements: [BOT-02]

must_haves:
  truths:
    - "The ghosts container env declares MESHTK_GUARDRAIL_FAILMODE=closed, so an unreachable guardrail sidecar blocks rather than silently passing every jailbreak/toxicity/PII check."
    - "The ghosts container can read MESHTK_RICKY_FALLBACK_URL from SSM without the value ever appearing in git or a terraform plan."
    - "A guardrail-sidecar outage raises a CloudWatch alarm on the existing admin-reports SNS tripwire topic, so fail-closed is observable rather than mysterious."
  artifacts:
    - infra/terraform/live/site/services/run.mqtt/service.hcl
    - infra/terraform/modules/admin-reports/v1.0.0/metrics.tf
    - infra/terraform/modules/admin-reports/v1.0.0/alarms.tf
  key_links:
    - "meshtk guard outage log line -> ghosts /ecs log group -> metric filter -> GuardrailOutages metric -> alarm -> dcr-admin-reports-tripwire SNS topic -> operator email"
    - "site.hcl mqtt secret keys -> secrets unit -> SSM /dc34/secrets/use1/mqtt/ricky-fallback-url -> service.hcl valueFrom -> ghosts container env"
---

<objective>
Author every infrastructure change Workstream C needs: flip the guardrail failmode to
`closed`, plumb the (not-yet-populated) ricky fallback secret, and add the CloudWatch
alarm that makes a sidecar outage visible.

This plan AUTHORS ONLY — it applies nothing. Applies are sequenced deliberately in
72-08 (secrets) and 72-09 (ecs-task + admin-reports), because flipping fail-closed
before the in-persona degradation line from 72-07 is deployed would turn a sidecar
outage into a confusing blanket refusal.

Purpose: at a hacker con, a jailbreak check that silently passes when the checker is
down is worse than no checker at all.
Output: service env changes, secret key plumbing, and a metric-filter-backed alarm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-31-bot-hardening-design.md
@.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-CONTEXT.md
</context>

<interface_context>
Verified against live source and live AWS on 2026-07-31:

- `infra/terraform/live/site/services/run.mqtt/service.hcl:337-340` — the ghosts
  container (`name = "run-mqtt-ghosts"`, NOT `ghosts`) declares
  `MESHTK_GUARDRAIL_FAILMODE` with value `open`. Prod task def
  `run-mqtt-use1-dc34:122` was confirmed carrying `open` during pre-flight.
- `service.hcl:351-377` — the `secrets` block; every entry is
  `{name, valueFrom = "/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/mqtt/<key>"}`.
- `service.hcl:383-396` — ghosts `depends_on` the guardrails container with
  `condition = "START"`, NOT `HEALTHY`. **The design spec's C4 line claiming
  "the task already declares depends_on HEALTHY" is INCORRECT.** The existing comment
  explains the choice: a slow or crash-looping sidecar must never block the ghosts from
  starting.
- `service.hcl:399-402` — a comment block asserting `MESHTK_GUARDRAIL_FAILMODE=open`
  degrades to un-guarded. This becomes stale on flip and must be rewritten.
- `service.hcl:422-428` — the guardrails container DOES have a `health_check` on
  `/healthz` with `start_period = 90`.
- `infra/terraform/live/site/site.hcl:366-369` — the `mqtt` secret group and its
  `keys` list (9 keys today, ending `flag-challenges`).
- `infra/terraform/live/site/.secrets.sops.json.template:60-62` — the mqtt placeholder
  block that mirrors those keys.
- `infra/terraform/modules/admin-reports/v1.0.0/metrics.tf:16` — the metric-filter
  pattern to model (`aws_cloudwatch_log_metric_filter`, `log_group_name` from
  `var.log_group_names[...]`, `metric_transformation` with `default_value = "0"`).
- `.../alarms.tf:14` `aws_sns_topic.tripwire` + `.../alarms.tf:97` `alb_5xx` — the
  simplest alarm to model (statistic Sum, period 300, `treat_missing_data =
  "notBreaching"`, `alarm_actions = [aws_sns_topic.tripwire.arn]`).
- `.../retention.tf:17-43` — `aws_cloudwatch_log_group.app` `for_each`s
  `var.log_group_names` AND an `import{}` block adopts each one with
  `prevent_destroy`. **Adding the ghosts group to `log_group_names` would therefore
  also adopt it and force 90-day retention on it.** Use a SEPARATE variable instead.
- The ghosts log group name follows `ecs-task/v1.0.0/main.tf:266`
  (`/ecs/{container.name}-{family}`) with `main.tf:74`
  (`family = {task.name}-{region.label}-{site.label}`), giving
  `/ecs/run-mqtt-ghosts-run-mqtt-use1-dc34`. VERIFY this against live AWS before
  wiring it — an alarm bound to a nonexistent group sits in INSUFFICIENT_DATA forever.
- `.github/workflows/deploy.yml:129` applies ONLY `modules: ecs-task,ecs-service`.
  Anything else needs `terragrunt-apply.yml` with an explicit `modules=` value.
- `apps/local/configui/templates/services/run.mqtt.service.hcl.tmpl` has ALREADY
  drifted (it carries neither `MESHTK_GUARDRAIL_FAILMODE` nor `MESHTK_RUN_INTERNAL_URL`).
  Do NOT chase it — it is local-dev tooling, not a deploy input.

CROSS-PLAN CONTRACT: 72-07 makes meshtk emit a stable marker token
`MESHTK_GUARDRAIL_OUTAGE` on its guard-outage log line. The metric filter here keys on
that token. Until 72-07 ships the metric reads zero, which is correct and harmless.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the ghosts guardrail failmode to closed and correct the stale rationale</name>
  <files>infra/terraform/live/site/services/run.mqtt/service.hcl</files>
  <read_first>
    - infra/terraform/live/site/services/run.mqtt/service.hcl lines 290-435 (the ghosts container block, its depends_on, and the guardrails sidecar block)
  </read_first>
  <action>
Change the ghosts container's guardrail failmode environment value from the fail-open
setting to the fail-closed one. That single value is the whole behavioural flip: meshtk's
`guard.go` reads it and treats any transport error, non-200 status or decode failure as a
block instead of an allow.

Then rewrite the two now-stale comments so the file stops asserting the opposite of what
it configures:

(a) The comment above the guardrails sidecar container currently justifies the sidecar
being non-essential by saying meshtk fail-opens. Replace that reasoning: the sidecar
stays non-essential so a crash-looping model load cannot take down the whole task, but
state that guarded chat now REFUSES while the sidecar is unavailable and that meshtk
answers those refusals with an in-persona degradation line rather than silence.

(b) The ghosts `depends_on` comment currently justifies `condition = "START"` by
appealing to fail-open. KEEP the `START` condition — do not change it to `HEALTHY` —
but replace the justification. Record the real trade explicitly: with `HEALTHY`, a
permanently unhealthy sidecar would stop the ghosts container from starting at all,
which also kills nodeinfo beacons and position updates, so the ghosts would vanish from
the map entirely. With `START`, only guarded chat degrades while beaconing continues.
Note the sidecar's own health check declares a 90-second start period, so the boot window
is bounded and visible, and the new CloudWatch alarm covers a sustained outage. Note
that the design spec's claim that this already reads `HEALTHY` was inaccurate.

Add the ricky fallback URL to the ghosts container's `secrets` block, following the exact
shape of the sibling entries: the meshtk env name paired with a `valueFrom` pointing at
the mqtt secret path templated on site and region labels, using a `ricky-fallback-url`
key. Comment it as the mint-failure fallback only — never published on any page, never
logged — and note that meshtk tolerates it being empty by sending the award line without
a link.

ORDERING WARNING to record in that comment: ECS refuses to start a task whose `valueFrom`
parameter does not exist. The SSM parameter is created by 72-08 before the ecs-task apply
in 72-09; this file must not be applied before then.
  </action>
  <verify>
    <automated>cd infra/terraform/live/site && grep -A1 'MESHTK_GUARDRAIL_FAILMODE' services/run.mqtt/service.hcl | grep -q '"closed"' && grep -c 'name      = "MESHTK_RICKY_FALLBACK_URL"' services/run.mqtt/service.hcl | grep -qx 1 && grep -c 'condition      = "START"' services/run.mqtt/service.hcl | grep -qvx 0 && BLK=$(grep -B22 'container_name = "run-mqtt-guardrails"' services/run.mqtt/service.hcl) && echo "$BLK" | grep -q 'fail-closed' && echo "$BLK" | grep -q 'BLOCKED' && echo "service.hcl OK"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `service.hcl OK`.
    - `MESHTK_GUARDRAIL_FAILMODE` has exactly one occurrence and its value is the
      fail-closed setting.
    - The `MESHTK_RICKY_FALLBACK_URL` DECLARATION line appears exactly once, inside the
      ghosts `secrets` block, with a `valueFrom` ending in `/mqtt/ricky-fallback-url`.
      (Match the declaration, not the bare token — the explanatory comment this task
      requires may legitimately name the variable too.)
    - The ghosts `depends_on` for the guardrails container still uses the START condition.
    - No stale comment remains claiming the failmode degrades to un-guarded:
      `grep -c 'degrades to un-guarded' infra/terraform/live/site/services/run.mqtt/service.hcl` equals 0.
    - The `depends_on` comment block (the 22 lines preceding
      `container_name = "run-mqtt-guardrails"`) DESCRIBES the post-flip reality: it
      contains both `fail-closed` and `BLOCKED`. Assert this POSITIVELY. Do NOT assert
      the absence of `fail-opens` — a bare negative grep cannot tell a stale claim from a
      deliberate quotation of the superseded rationale, and enforcing it would force
      deletion of the sentence that explains what changed.
  </acceptance_criteria>
  <done>The ghosts container is configured fail-closed, can read the fallback secret, and its comments describe the configuration that is actually present.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the ricky-fallback-url secret key</name>
  <files>infra/terraform/live/site/site.hcl, infra/terraform/live/site/.secrets.sops.json.template</files>
  <read_first>
    - infra/terraform/live/site/site.hcl lines 340-375 (the `secrets.groups` map, especially the `mqtt` group)
    - infra/terraform/live/site/.secrets.sops.json.template lines 50-70 (the mqtt placeholder block)
  </read_first>
  <action>
Append a `ricky-fallback-url` key to the `mqtt` secret group's `keys` list in `site.hcl`,
keeping the existing ordering and adding it at the end so the diff is one entry. This is
what causes the secrets unit to provision the SSM parameter that `service.hcl` now
references.

Add a matching placeholder entry to the SOPS template so a fresh environment bootstraps
with a recognisable non-secret sentinel rather than a missing key. Use a CHANGEME-style
placeholder consistent with its neighbours.

Do NOT write a real value anywhere. The real URL is generated by the rotation script in
72-08 and placed in the encrypted `.secrets.sops.json` by the operator. It contains a
live flag code and must never reach git, a terraform plan, or a log.

Add a short comment next to the new key explaining what consumes it (the ghosts
container's mint-failure fallback) and that an empty value is tolerated by meshtk.
  </action>
  <verify>
    <automated>cd infra/terraform/live/site && grep -c 'ricky-fallback-url' site.hcl | grep -qx 1 && grep -c 'ricky-fallback-url' .secrets.sops.json.template | grep -qx 1 && ! grep -q 'nggyu' site.hcl .secrets.sops.json.template && echo "secret key declared, no live code present"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `secret key declared, no live code present`.
    - The `mqtt` group `keys` list contains `ricky-fallback-url` exactly once.
    - The SOPS template carries a CHANGEME-style placeholder for the same key.
    - No real claim URL or flag code appears in either file.
    - `git diff --stat infra/terraform/live/site/site.hcl` shows 1 insertion, 1 deletion
      (the single keys line) plus at most the comment lines added.
  </acceptance_criteria>
  <done>The mqtt secret group declares ricky-fallback-url and the SOPS template has a placeholder, with no real value committed.</done>
</task>

<task type="auto">
  <name>Task 3: Guardrail-outage metric filter and CloudWatch alarm</name>
  <files>infra/terraform/modules/admin-reports/v1.0.0/variables.tf, infra/terraform/modules/admin-reports/v1.0.0/metrics.tf, infra/terraform/modules/admin-reports/v1.0.0/alarms.tf, infra/terraform/modules/admin-reports/v1.0.0/outputs.tf, infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl</files>
  <read_first>
    - infra/terraform/modules/admin-reports/v1.0.0/metrics.tf lines 1-45 (filter conventions)
    - infra/terraform/modules/admin-reports/v1.0.0/alarms.tf (all 113 lines — SNS topic + the alb_5xx alarm to model)
    - infra/terraform/modules/admin-reports/v1.0.0/variables.tf lines 1-30 and 80-119
    - infra/terraform/modules/admin-reports/v1.0.0/retention.tf (all 43 lines — WHY the ghosts group must NOT join log_group_names)
    - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl (all 102 lines)
  </read_first>
  <action>
First CONFIRM the ghosts log group actually exists and note its exact name:
`aws logs describe-log-groups --log-group-name-prefix /ecs/run-mqtt-ghosts --profile dc34-application --region us-east-1`.
Use whatever name that returns. An alarm bound to a nonexistent group sits in
INSUFFICIENT_DATA forever, which is precisely the silent failure this alarm exists to
prevent. If the group does not exist, stop and report rather than guessing.

Add to `variables.tf`:
- a `guardrail_log_group_name` string defaulting to empty, documented as the ghosts
  container ECS log group. It is DELIBERATELY separate from `log_group_names`: that map
  is `for_each`ed by `retention.tf` into adopted log groups with `prevent_destroy` and a
  90-day retention override, and pulling the ghosts group into that would be an
  unrelated, riskier change than this alarm needs.
- a `threshold_guardrail_outages_per_5min` number defaulting to 3, documented as
  tolerating a transient blip (including the sidecar's 90-second boot window) while
  catching a sustained outage.

Add to `metrics.tf` a metric filter, created only when the log group variable is
non-empty (use `count`), attached to that group. meshtk's guard logs are logrus
unstructured text, NOT the structured JSON the other filters key on, so use a plain-text
filter pattern matching the stable marker token that 72-07 adds to the outage log line —
not a `$.evt` JSON selector, which would never match. Publish into the module's existing
metric namespace as a count metric with value 1 and default value 0, matching its
siblings so "no outages" reads as a real zero.

Add to `alarms.tf` an alarm, guarded by the same count condition, modelled on the
`alb_5xx` alarm: Sum statistic, 300-second period, one evaluation period, threshold from
the new variable with a greater-than-or-equal comparison, `treat_missing_data` set to
notBreaching, and `alarm_actions` pointing at the EXISTING tripwire SNS topic so it
reaches the same operator email as every other tripwire. Give it an alarm description
that names the consequence in plain words: the guardrail sidecar is unreachable and,
because the ghosts now run fail-closed, guarded chat is refusing. Add `ok_actions` on
this one — unlike a spike alarm, "the sidecar came back" is genuinely useful news.

Export the alarm name from `outputs.tf` alongside the existing outputs.

In the live terragrunt unit, pass both new inputs, sourcing them from `site.hcl` under
the existing `admin_reports` local so con-week tuning stays a one-file edit — add the log
group name and the threshold there next to the other thresholds.

Do NOT add the ghosts group to `log_group_names`. Do NOT create a new module version
directory: this module is edited in place by established convention (see its git history
— the signups threshold and ALB anomaly gate were both in-place edits to v1.0.0).
  </action>
  <verify>
    <automated>cd infra/terraform/modules/admin-reports/v1.0.0 && terraform fmt -check -recursive . && terraform init -backend=false >/dev/null 2>&1 && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform fmt -check -recursive` and `terraform validate` both exit 0 for the module.
    - `grep -c 'guardrail_log_group_name' infra/terraform/modules/admin-reports/v1.0.0/variables.tf` equals 1.
    - `grep -c 'aws_cloudwatch_log_metric_filter' infra/terraform/modules/admin-reports/v1.0.0/metrics.tf` is 9
      (8 existing plus the new one).
    - `grep -c 'aws_cloudwatch_metric_alarm' infra/terraform/modules/admin-reports/v1.0.0/alarms.tf` equals 5
      (4 existing plus the new one).
    - The ghosts log group is NOT in `log_group_names`:
      `grep -c 'run-mqtt-ghosts' infra/terraform/live/site/site.hcl` equals 1 and that
      one occurrence is under the new alarm setting, not inside the `log_group_names` map.
    - The confirmed live log-group name from the AWS describe call is recorded in the
      plan SUMMARY.
  </acceptance_criteria>
  <done>A count-gated metric filter and alarm exist, wired to the existing SNS tripwire topic, with the ghosts log group kept out of the retention adoption set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| radio DM -> ghosts container -> guardrail sidecar -> Bedrock | Untrusted attacker-authored prompt text crosses into an LLM |
| SOPS/SSM -> ECS task env | A live flag code transits this boundary and must never leak into git, plan output or logs |
| ghosts log group -> CloudWatch metric -> SNS -> operator email | Detection path for a fail-closed outage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-17 | Elevation of privilege | `guard.go` failmode | critical | mitigate | Flip to fail-closed so an unreachable sidecar blocks jailbreak/toxicity/PII checks instead of passing every one straight to Bedrock |
| T-72-18 | Denial of service | fail-closed with a down sidecar | high | mitigate | Sidecar stays non-essential and `depends_on` stays START so beaconing and position continue; only guarded chat refuses, and 72-07 makes that refusal an in-persona line |
| T-72-19 | Information disclosure | ricky fallback URL in git or plan output | critical | mitigate | Value lives only in encrypted SOPS and SSM; the template carries a CHANGEME placeholder; `valueFrom` keeps it out of terraform plan output entirely |
| T-72-20 | Repudiation | silent guardrail outage | high | mitigate | Metric filter on the marker token plus a >=3-per-5-minutes alarm to the existing tripwire topic, with ok_actions so recovery is also reported |
| T-72-21 | Tampering | alarm bound to a wrong/nonexistent log group | medium | mitigate | Log group name confirmed against live AWS before wiring; count-gated so an empty name provisions nothing rather than creating a dead alarm |
| T-72-22 | Denial of service | accidental retention adoption of the ghosts log group | medium | mitigate | Dedicated variable instead of joining `log_group_names`, keeping the `import{}` + `prevent_destroy` set unchanged |
</threat_model>

<verification>
1. `terraform fmt -check -recursive` + `terraform validate` clean on the admin-reports module.
2. `terragrunt hclfmt --check` (or `terragrunt hcl fmt --check`) clean on the changed
   live files. NOTE: the command ignores its path argument and scans the whole
   `live/site` tree, which has 9 files of pre-existing format drift. Scope the result to
   the files this plan changed rather than running the formatter tree-wide.
3. Confirm no secret material was COMMITTED — check committed content, not the working
   diff, which is empty once the task commits are made and so would pass unconditionally:
   `! git grep -qiE 'nggyu|ctf/claim\?c=ricky' HEAD -- infra/terraform/live/site`

APPLY IS DELIBERATELY NOT IN THIS PLAN. Sequencing, all via GitHub Actions:
  - `secrets` unit -> 72-08 (after the rotation generates the URL), via
    `terragrunt-apply.yml -f region=us-east-1 -f modules=secrets`
  - `admin-reports` unit -> 72-09, via
    `terragrunt-apply.yml -f region=us-east-1 -f modules=admin-reports`
  - `ecs-task`/`ecs-service` -> 72-09, via `deploy.yml`
Never run `terragrunt apply` locally (AGENTS.md Essential Rule 4), and never apply-all.
</verification>

<success_criteria>
- The ghosts container env declares the fail-closed failmode and the fallback secret.
- The mqtt secret group declares `ricky-fallback-url`; no real value is committed.
- A count-gated metric filter and alarm exist and validate, wired to the existing SNS topic.
- The ghosts `depends_on` still uses START, with the trade recorded in-file.
- The ghosts log group is not pulled into the retention adoption set.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| Env value change | `MESHTK_GUARDRAIL_FAILMODE`: `open` -> `closed` | `services/run.mqtt/service.hcl` (~line 338) |
| Container secret | `MESHTK_RICKY_FALLBACK_URL` -> `/{{SITE_LABEL}}/secrets/{{REGION_LABEL}}/mqtt/ricky-fallback-url` | `services/run.mqtt/service.hcl` secrets block |
| Secret key | `ricky-fallback-url` in the `mqtt` group `keys` list | `live/site/site.hcl` (~line 368) |
| SOPS placeholder | `ricky-fallback-url` CHANGEME entry | `live/site/.secrets.sops.json.template` |
| Terraform variable | `guardrail_log_group_name` (string, default "") | `modules/admin-reports/v1.0.0/variables.tf` |
| Terraform variable | `threshold_guardrail_outages_per_5min` (number, default 3) | `modules/admin-reports/v1.0.0/variables.tf` |
| Metric filter | guardrail-outage plain-text filter on the marker token | `modules/admin-reports/v1.0.0/metrics.tf` |
| CloudWatch alarm | guardrail-outage alarm -> `dcr-admin-reports-tripwire` SNS topic | `modules/admin-reports/v1.0.0/alarms.tf` |
| Terraform output | the new alarm's name | `modules/admin-reports/v1.0.0/outputs.tf` |
| site.hcl settings | ghosts log group name + outage threshold under `admin_reports` | `live/site/site.hcl` |

Cross-plan contract: the metric filter keys on the token `MESHTK_GUARDRAIL_OUTAGE`,
emitted by meshtk's guard module as of 72-07.
Log group (verify live before wiring): `/ecs/run-mqtt-ghosts-run-mqtt-use1-dc34`.
</artifacts_produced>

<output>
Create `.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-04-SUMMARY.md` when done.
</output>
