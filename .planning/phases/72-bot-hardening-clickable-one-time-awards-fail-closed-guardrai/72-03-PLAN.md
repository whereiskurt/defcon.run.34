---
phase: 72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.mqtt/meshtk/meshtk.dc34.yaml
  - apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts
autonomous: true
requirements: [BOT-03]

must_haves:
  truths:
    - "ricky's LRC blob decodes to exactly 58 timed lyric entries, down from 59."
    - "The final entry is the real closing lyric at timestamp [03:28.40], not a QR path."
    - "No remaining lyric entry's text begins with a slash."
    - "run.human's committed fleet snapshot is byte-identical to the canonical YAML."
  artifacts:
    - apps/run.mqtt/meshtk/meshtk.dc34.yaml
    - apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts
  key_links:
    - "meshtk.dc34.yaml -> node scripts/sync-meshtk-fleet.mjs -> src/data/meshtk-fleet-yaml.ts -> mesh-ghosts.test.ts byte-parity assertion"
---

<objective>
Drop the trailing QR-path entry from ricky's base64 LRC blob so the song ends on the
real closing lyric at 58 numbered lines, clearing the way for the two reliable award
DMs that 72-06 appends after playback.

Today `numberLyric(i)` prints `i+1`, so the 59th entry renders as a bare literal path
line that is neither clickable nor single-use — it is the exact defect this phase exists
to remove.

Purpose: the flag stops riding a lyric line at all.
Output: a 58-entry LRC blob plus the regenerated, byte-parity-checked run.human snapshot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-31-bot-hardening-design.md
@.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-CONTEXT.md
</context>

<interface_context>
Verified against live source on 2026-07-31:

- `apps/run.mqtt/meshtk/meshtk.dc34.yaml:478` begins the `ghost.ricky` fleet entry.
  Line 503 is the `chatmode_lyrics` ChatBot entry; line 506 is the single-element
  `Message` list holding the base64 LRC blob (3412 characters, one unwrapped line).
- Decoding that blob today yields 2559 bytes: a `[length: 03:34]` header, 59 timed
  entries, and a trailing newline. Entry index 58 (the 59th) carries timestamp
  `[03:30.34]` and its text is a QR path beginning with a slash. Entry index 57 is
  `[03:28.40]Never gonna tell a lie and hurt you`.
- `meshtk.dc34.yaml` is a MONOREPO-ONLY overlay: upstream `~/working/meshtk` carries
  `meshtk.defcon.yaml`, `meshtk.bak.yaml`, `meshtk.liamcottle.yaml` and
  `meshtk.sslexample.yaml` but NO `meshtk.dc34.yaml`. Confirmed by directory listing.
  So this edit needs NO upstream PR — it lands in the monorepo only.
- `apps/run.human/webapp/scripts/sync-meshtk-fleet.mjs` reads the canonical YAML at
  `../../run.mqtt/meshtk/meshtk.dc34.yaml` (relative to the webapp dir) and writes
  `src/data/meshtk-fleet-yaml.ts` as a banner plus `JSON.stringify(yaml)`.
- `src/lib/__tests__/mesh-ghosts.test.ts:23` asserts the committed snapshot is
  byte-identical to the canonical file, skipping only when the canonical path is
  unreachable (it IS reachable in this worktree, so the assertion is live).
- Go parses the blob with `base64.StdEncoding.DecodeString` (`cmd.go:1004`), which
  rejects line-wrapped base64 — the replacement string must be a single unwrapped line.

EXPECTED POST-EDIT VALUES (precomputed, use as exact acceptance targets):
  decoded bytes 2559 -> 2512; base64 length 3412 -> 3352; timed entries 59 -> 58;
  decoded text ends with `[03:28.40]Never gonna tell a lie and hurt you` plus a newline.

ENVIRONMENT NOTE: node v22.1.0 is the default here but vitest needs >= v22.12. Run
`source "$HOME/.nvm/nvm.sh" && nvm use 22.12.0 && npm install` from
`apps/run.human/webapp` before the parity test.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the LRC blob without the trailing QR-path entry</name>
  <files>apps/run.mqtt/meshtk/meshtk.dc34.yaml</files>
  <read_first>
    - apps/run.mqtt/meshtk/meshtk.dc34.yaml lines 476-510 (the `ghost.ricky` fleet entry and its `chatmode_lyrics` blob)
  </read_first>
  <action>
Write a throwaway node script under the scratchpad (NOT in the repo) that performs the
edit deterministically rather than hand-editing a 3412-character string:

1. Read the YAML as UTF-8 and locate the single base64 run that begins with the encoded
   length header (it is the only base64 blob in the file).
2. Decode it, split on newline, and drop exactly the one part whose text portion — the
   remainder after the closing bracket of its timestamp — begins with a slash. Assert
   exactly one part matched before continuing; if the count is not one, abort without
   writing.
3. Rejoin with newline. The decode splits into 61 parts with an empty final part because
   the payload ends in a trailing newline — preserve that, so the rejoined text still
   ends with a newline exactly as it does today.
4. Re-encode with standard base64 as ONE unwrapped line. Go's decoder rejects wrapped
   input, so no line breaks may be introduced.
5. Replace the old run with the new one via a single exact string substitution and write
   the file back. Change nothing else in the file — same indentation, same surrounding
   YAML flow-sequence punctuation, same trailing newline at end of file.

Verify the arithmetic before moving on: decoded length must go 2559 -> 2512, base64
length 3412 -> 3352, timed entry count 59 -> 58.

Do not touch any other fleet entry, and do not reformat the YAML — the run.human parity
test compares bytes, so an incidental whitespace change is a failure.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const y=fs.readFileSync("apps/run.mqtt/meshtk/meshtk.dc34.yaml","utf8");const m=y.match(/W2xlbmd0aDogMDM6MzRd[A-Za-z0-9+\/=]+/);if(!m)throw new Error("blob not found");const b=m[0];const t=Buffer.from(b,"base64").toString("utf8");const e=t.split("\n").filter(s=>s.startsWith("[")&&!s.startsWith("[length:"));const slash=e.filter(s=>s.slice(s.indexOf("]")+1).startsWith("/"));if(b.length!==3352)throw new Error("b64 len "+b.length);if(t.length!==2512)throw new Error("decoded len "+t.length);if(e.length!==58)throw new Error("entries "+e.length);if(slash.length!==0)throw new Error("slash-prefixed entries remain: "+slash.length);if(e[57]!=="[03:28.40]Never gonna tell a lie and hurt you")throw new Error("tail "+e[57]);if(!t.endsWith("\n"))throw new Error("lost trailing newline");console.log("OK 58 entries, ends on the closing lyric")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `OK 58 entries, ends on the closing lyric` and exits 0.
    - `git diff --stat apps/run.mqtt/meshtk/meshtk.dc34.yaml` shows exactly 1 file,
      1 insertion, 1 deletion (the single blob line).
    - No throwaway script is committed:
      `git status --porcelain apps/run.mqtt/meshtk/ | grep -c '^??'` equals 0.
  </acceptance_criteria>
  <done>ricky's LRC blob decodes to 58 timed entries ending on the real closing lyric, with no slash-prefixed entry left and the trailing newline intact.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the run.human fleet snapshot and prove byte parity</name>
  <files>apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts</files>
  <read_first>
    - apps/run.human/webapp/scripts/sync-meshtk-fleet.mjs (all 34 lines)
    - apps/run.human/webapp/src/lib/__tests__/mesh-ghosts.test.ts lines 1-40 (the parity assertion and the ghost-count assertion)
  </read_first>
  <action>
From `apps/run.human/webapp`, select node 22.12.0 via nvm, install dependencies if
`node_modules` is absent, then run the fleet sync script. It rewrites
`src/data/meshtk-fleet-yaml.ts` from the canonical YAML and prints the new byte count —
confirm the printed count matches the canonical file's size on disk.

This step is NOT optional and is NOT deferrable: any edit to the canonical YAML that is
not followed by this sync turns the run.human byte-parity vitest red, and that test is
live in this worktree because the canonical path resolves.

Then run the mesh-ghosts suite. Beyond parity it asserts the fleet still contains ten
`ghost.*` entries — a guard that the blob substitution did not corrupt YAML structure.
Commit the regenerated snapshot together with the YAML edit so the two never diverge in
history.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx vitest run src/lib/__tests__/mesh-ghosts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/mesh-ghosts.test.ts` passes, including the
      snapshot-parity case (it must RUN, not skip — the canonical path resolves here).
    - `node -e 'const{MESHTK_FLEET_YAML}=0' ` is not needed; instead confirm parity
      directly: `node -e 'const fs=require("fs");const s=fs.readFileSync("apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts","utf8");const y=fs.readFileSync("apps/run.mqtt/meshtk/meshtk.dc34.yaml","utf8");const m=s.slice(s.indexOf("export const MESHTK_FLEET_YAML: string =")+41).trim().replace(/;\s*$/,"");if(JSON.parse(m)!==y)throw new Error("snapshot drift");console.log("byte parity OK")'`
      prints `byte parity OK`.
    - `git diff --name-only` lists both `apps/run.mqtt/meshtk/meshtk.dc34.yaml` and
      `apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts`.
  </acceptance_criteria>
  <done>The committed snapshot is byte-identical to the edited canonical YAML and the mesh-ghosts suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meshtk fleet config -> ghost DMs over LoRa/MQTT | Config content is broadcast verbatim to radios |
| canonical YAML -> committed run.human snapshot | A silent drift here changes what the admin roster reports vs what bots actually run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-13 | Information disclosure | LRC blob broadcasting a static claim path | high | mitigate | The QR-path entry is removed entirely, so the freely-shareable static route stops being announced to every listener |
| T-72-14 | Tampering | blob corruption during substitution | medium | mitigate | Scripted edit with pre/post assertions on decoded length, base64 length, entry count and tail text; abort-before-write if the match count is not exactly one |
| T-72-15 | Tampering | snapshot/canonical drift | medium | mitigate | `sync-meshtk-fleet.mjs` regenerates the snapshot and `mesh-ghosts.test.ts` byte-compares it in the same commit |
| T-72-16 | Denial of service | Go rejecting wrapped base64 at runtime | medium | mitigate | Re-encode as a single unwrapped line; asserted by decoding the file's blob in the verify command |
</threat_model>

<verification>
1. Blob arithmetic check (Task 1 verify command) exits 0.
2. `cd apps/run.human/webapp && npx vitest run src/lib/__tests__/mesh-ghosts.test.ts` green.
3. `git diff --stat` shows exactly two files changed and no stray scratch files.

NOT in this plan: the award DMs that replace the removed line. Those are 72-06. Between
this plan and 72-06 landing, ricky's song would end with no award at all — which is why
72-06 depends on this plan and both ship in the same release (72-09). Nothing is
deployed until then.
</verification>

<success_criteria>
- ricky's LRC decodes to exactly 58 timed entries.
- The last entry is the closing lyric at `[03:28.40]`.
- No entry text begins with a slash.
- The run.human snapshot is byte-identical to the canonical YAML and its vitest is green.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| Config change | `ghost.ricky` LRC blob: 59 -> 58 timed entries | `apps/run.mqtt/meshtk/meshtk.dc34.yaml` (line ~506) |
| Regenerated file | `MESHTK_FLEET_YAML` snapshot (2559 -> 2512 decoded bytes) | `apps/run.human/webapp/src/data/meshtk-fleet-yaml.ts` |

No new symbols, env vars, files or dependencies. The base64 blob length goes 3412 -> 3352.
</artifacts_produced>

<output>
Create `.planning/phases/72-bot-hardening-clickable-one-time-awards-fail-closed-guardrai/72-03-SUMMARY.md` when done.
</output>
