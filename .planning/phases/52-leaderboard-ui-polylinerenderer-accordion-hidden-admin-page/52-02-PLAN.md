---
phase: 52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - apps/run.human/webapp/src/lib/leaderboard-ui.ts
  - apps/run.human/webapp/src/lib/leaderboard-ui.test.ts
  - apps/run.human/webapp/src/components/leaderboard/LeaderboardTable.tsx
autonomous: true
requirements: [LDBR-10]

must_haves:
  truths:
    - "A HeroUI Accordion renders one row per runner: rank / globalScore 🥕 / displayName / count chips, with search + fast-filter chips and pagination (SC #3, SC #4)."
    - "The current admin's own row is highlighted (DC33 green highlight)."
    - "Expanding a row lazy-fetches GET /api/leaderboard/[userId]/accomplishments and renders each run with a PolylineRenderer thumbnail from metadata.polyline (SC #3)."
    - "Rank/score/count chips match the API globalScore/globalRank/counts; runner-class emoji reflect mqttUsertype (SC #4)."
  artifacts:
    - "apps/run.human/webapp/src/lib/leaderboard-ui.ts"
    - "apps/run.human/webapp/src/lib/leaderboard-ui.test.ts"
    - "apps/run.human/webapp/src/components/leaderboard/LeaderboardTable.tsx"
  key_links:
    - "LeaderboardTable imports runnerClassEmoji + deriveCountChips from lib/leaderboard-ui (tested seam)."
    - "LeaderboardTable imports PolylineRenderer (plan 01) and feeds it metadata.polyline objects."
    - "Fetches are region-prefixed via an apiBase prop (prod /use1, bare in dev) — the basePath landmine."
---

<objective>
Port DC33's `LeaderboardTable` to run.human (LDBR-10): a HeroUI `Accordion selectionMode="multiple" variant="bordered" isCompact` where each row is a runner (rank / `globalScore` 🥕 / displayName / count chips), the current admin's own row highlighted; a keyword search + fast-filter chips; pagination (default 25); and expand → lazy `GET /api/leaderboard/[userId]/accomplishments` → each run rendered with a `PolylineRenderer` thumbnail.

Two PURE seams are extracted and unit-tested first: the runner-class emoji mapping (`mqttUsertype` → emoji) and the count-chip derivation (`activityCounts.checkin + gpx`, `ctfSolves`, rendering 0 gracefully).

Purpose: This is the interactive heart of the board. Consumes the Phase-51 API AS SHIPPED (no backend change).
Output: One pure helper module + its test + the client accordion component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-CONTEXT.md

# DC33 source to reproduce (different repo, read-only):
@/Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/leaderboard/LeaderboardTable.tsx

# Phase-51 API shapes this component CONSUMES (do not modify):
@apps/run.human/webapp/src/lib/leaderboard-data.ts
@apps/run.human/webapp/src/app/api/leaderboard/route.ts
@apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/route.ts

# Plan-01 outputs this component imports:
@apps/run.human/webapp/src/components/leaderboard/PolylineRenderer.tsx

# HeroUI usage reference + region-prefix (apiBase) pattern:
@apps/run.human/webapp/src/components/profile/CheckInHistory.tsx
@apps/run.human/webapp/src/app/(protected)/admin/AdminConsole.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure leaderboard-ui helpers + tests</name>
  <files>apps/run.human/webapp/src/lib/leaderboard-ui.ts, apps/run.human/webapp/src/lib/leaderboard-ui.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/leaderboard-data.ts (the LeaderboardRow DTO: globalRank, globalScore, activityCounts:{checkin,gpx}, ctfSolves, mqttUsertype)
    - /Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/leaderboard/LeaderboardTable.tsx (getDisplayNameWithEmoji: wildhare ⭐️, og 🤠; the count-chip type/color mapping)
    - apps/run.human/webapp/src/lib/leaderboard-data.test.ts (vitest style)
  </read_first>
  <behavior>
    - runnerClassEmoji('wildhare') === '⭐️'; runnerClassEmoji('og') === '🤠' (DC33 parity).
    - runnerClassEmoji('rabbit') and ('admin') return sensible non-empty emoji (Claude's discretion, documented); runnerClassEmoji(undefined) returns '' (no trailing emoji).
    - deriveCountChips({activityCounts:{checkin:3,gpx:2}, ctfSolves:4}) yields an activity chip count 5 (success/green) and a ctf chip count 4 (warning/orange).
    - deriveCountChips with missing/absent activityCounts and absent ctfSolves yields activity 0 and ctf 0 (renders 0 gracefully — SC #4).
  </behavior>
  <action>
    Create `lib/leaderboard-ui.ts` exporting two PURE functions. `runnerClassEmoji(mqttUsertype?: 'rabbit'|'admin'|'wildhare'|'og')` returns the DC33 emoji for wildhare (⭐️) and og (🤠), a sensible emoji for rabbit and admin (your discretion — document the choice in a short doc-comment), and '' for undefined/unknown. `deriveCountChips(row: { activityCounts?: {checkin?:number; gpx?:number}; ctfSolves?: number })` returns a small typed array of `{ key: 'activity'|'ctf'; count: number; color: 'success'|'warning' }` where activity = (checkin ?? 0) + (gpx ?? 0) and ctf = ctfSolves ?? 0. No React, no JSX — pure data. Write `leaderboard-ui.test.ts` covering every behavior bullet with plain fixtures, mirroring leaderboard-data.test.ts.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/lib/leaderboard-ui.test.ts</automated>
  </verify>
  <done>leaderboard-ui.test.ts passes; the module is pure (no React/DOM import).</done>
</task>

<task type="auto">
  <name>Task 2: LeaderboardTable client accordion component</name>
  <files>apps/run.human/webapp/src/components/leaderboard/LeaderboardTable.tsx</files>
  <read_first>
    - /Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/leaderboard/LeaderboardTable.tsx (Accordion config itemClasses; search + fast-filter chips; lazy fetchUserAccomplishments on onSelectionChange; two-column run layout with the thumbnail; current-user green highlight bg-green-400/20)
    - apps/run.human/webapp/src/app/api/leaderboard/route.ts and .../[userId]/accomplishments/route.ts (the exact JSON shapes: { rows, total, page, limit } and { accomplishments: [...] })
    - apps/run.human/webapp/src/app/(protected)/admin/AdminConsole.tsx (apiBase prop + fetch pattern)
    - apps/run.human/webapp/src/lib/leaderboard-ui.ts and .../components/leaderboard/PolylineRenderer.tsx (the two seams this component consumes)
  </read_first>
  <action>
    Create the `'use client'` component `components/leaderboard/LeaderboardTable.tsx`. Props: `currentUserId: string` (the current admin's session.user.id — used to highlight their own row) and `apiBase: string` (region prefix from the server page; ALL fetches must be `${apiBase}/api/leaderboard...` — this is the basePath landmine that 404s prod fetches if omitted). State: rows, total/page/limit, filter/searchInput, per-user accomplishments cache, loading + loadingAccomplishments set. On mount and whenever page/filter change, fetch `${apiBase}/api/leaderboard?page&limit&filter` and set rows/pagination from `{ rows, total, page, limit }` (limit default 25). Render the DC33 search Input + Search/Clear buttons and the fast-filter Chips (Claude's discretion for the set: a "you" chip filtering to the current admin's displayName, plus ⭐️ Wild Hares and 🤠 OG). Render the HeroUI `Accordion selectionMode="multiple" variant="bordered" isCompact` with the DC33 itemClasses. For each row build the title: `#{globalRank}`, a `{globalScore} 🥕` chip when globalScore > 0, the displayName + `runnerClassEmoji(row.mqttUsertype)` (from lib/leaderboard-ui), and the count chips from `deriveCountChips(row)` (activity green, ctf orange, render 0 gracefully). Apply the DC33 green highlight class when `row.userId === currentUserId`. On `onSelectionChange`, lazy-fetch `${apiBase}/api/leaderboard/${userId}/accomplishments` once per user (cache by userId), showing a Spinner while loading. Render each accomplishment: name, a source badge (e.g. STRAVA when source === 'strava'), the completedAt date, and — when `metadata.polyline` is a non-empty array — a `<PolylineRenderer points={metadata.polyline} theme={...} />` in the DC33 two-column layout; single-column when no polyline; an empty-state line ("no runs yet" — your copy) when the list is empty. Pass the current theme through to PolylineRenderer so the dark-mode filter fires (read the theme the same way run.human components already do). Add a bottom HeroUI `Pagination` when total exceeds one page. Inherit run.human's HeroUI/Tailwind tokens; keep DC33's semantic colors (green highlight, 🥕, success/warning chips).
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit 2>&1 | grep -E "leaderboard/LeaderboardTable|leaderboard-ui" | grep -v "dropdown-user.tsx|__tests__/checkin.test.ts" | head; echo "tsc-scan-done"</automated>
  </verify>
  <done>tsc reports NO error in LeaderboardTable.tsx or leaderboard-ui.ts (only the 2 known pre-existing out-of-scope errors may remain); the component imports both plan-52 seams (PolylineRenderer + leaderboard-ui) and fetches via the apiBase-prefixed URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/leaderboard(+/[userId]/accomplishments) | client fetches the already admin-gated Phase-51 endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Elevation of Privilege | client fetch of admin API | low | accept | Both endpoints re-gate server-side (requireAdmin + revalidateAdmin → 404). The client cannot bypass; a non-admin's fetch 404s. No new trust granted here. |
| T-52-04 | Information Disclosure | LeaderboardRow render | low | accept | The row DTO already carries NO PII (Phase-51 T-51-01); the component only renders score/count/name/class fields it receives. |

No package installs this phase — no supply-chain (SC) threat row.
</threat_model>

<verification>
- `npx vitest run src/lib/leaderboard-ui.test.ts` passes (Node ≥22.12).
- `npx tsc --noEmit` introduces no NEW errors in LeaderboardTable.tsx or leaderboard-ui.ts.
- Manual/local (orchestrator browser check, plan 03): expand a row → canvas thumbnails render; own row highlighted; chips/emoji match API.
</verification>

<success_criteria>
- Accordion renders rank/globalScore/name/count chips + emoji from the API, current admin's row highlighted (SC #3, SC #4).
- Expand lazy-loads accomplishments and renders PolylineRenderer thumbnails from metadata.polyline (SC #3).
- Search, fast-filter chips, and pagination work against the Phase-51 API unchanged.
</success_criteria>

<output>
Create `.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-02-SUMMARY.md` when done
</output>
