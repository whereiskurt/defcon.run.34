---
phase: 52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page
plan: 03
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - apps/run.human/webapp/src/app/(protected)/leaderboard/page.tsx
  - apps/run.human/webapp/src/lib/leaderboard-hidden.test.ts
autonomous: true
requirements: [LDBR-11]

must_haves:
  truths:
    - "A non-admin (or signed-out) visitor to /leaderboard gets a 404 — never a 403, never the page (SC #1)."
    - "An admin sees the ranked accordion (SC #1)."
    - "The string /leaderboard appears in NO navigation component — header, dropdown, menu — grep/test-verifiable (SC #2)."
  artifacts:
    - "apps/run.human/webapp/src/app/(protected)/leaderboard/page.tsx"
    - "apps/run.human/webapp/src/lib/leaderboard-hidden.test.ts"
  key_links:
    - "page.tsx gate mirrors (protected)/admin/page.tsx: requireAdmin → notFound(); revalidateAdmin(session.user.authUserId) → notFound() (authUserId, NOT session.user.id — the identity landmine)."
    - "page.tsx renders <LeaderboardTable currentUserId={session.user.id} apiBase={apiBase()} /> and is linked from no nav."
---

<objective>
Add the hidden admin page at `app/(protected)/leaderboard/page.tsx` (LDBR-11): a server component that gates exactly like the Phase-43 `(protected)/admin/page.tsx` — `requireAdmin(session)` → `notFound()`, then `revalidateAdmin(session.user.authUserId)` → `notFound()` on failure — and renders `<LeaderboardTable>` inside the real run.human chrome. It is linked from NO navigation (ships hidden until launch), and a unit test proves the `/leaderboard` string appears in no nav component.

Purpose: The gate is the non-disclosure guarantee (SC #1) and the "no nav" test is the hidden guarantee (SC #2).
Output: The page + a hidden-nav assertion test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-CONTEXT.md

# The gate to mirror EXACTLY (requireAdmin → notFound, revalidateAdmin(authUserId), runtime/dynamic, apiBase):
@apps/run.human/webapp/src/app/(protected)/admin/page.tsx

# The client this page renders (plan 02):
@apps/run.human/webapp/src/components/leaderboard/LeaderboardTable.tsx

# Nav components the hidden-nav test must scan (must NOT reference the route):
@apps/run.human/webapp/src/components/header/header.tsx
@apps/run.human/webapp/src/components/header/dropdown-user.tsx
@apps/run.human/webapp/src/components/header/dropdown-menu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hidden admin leaderboard page (gate + render)</name>
  <files>apps/run.human/webapp/src/app/(protected)/leaderboard/page.tsx</files>
  <read_first>
    - apps/run.human/webapp/src/app/(protected)/admin/page.tsx (copy the gate spine verbatim: runtime="nodejs"; dynamic="force-dynamic"; const session = await auth(); requireAdmin → notFound(); const authUserId = session?.user?.authUserId; if (!authUserId || !(await revalidateAdmin(authUserId))) notFound(); plus the apiBase() region-prefix helper)
    - apps/run.human/webapp/src/components/leaderboard/LeaderboardTable.tsx (its props: currentUserId, apiBase)
  </read_first>
  <action>
    Create the server component `app/(protected)/leaderboard/page.tsx`. Set `export const runtime = "nodejs"` and `export const dynamic = "force-dynamic"`. Copy the admin page's `apiBase()` region-prefix helper (prod → `/${NEXT_PUBLIC_REGION_SHORT || "use1"}`, dev → ""). In the default async component: `const session = await auth();` then `const gate = requireAdmin(session); if (!gate.ok) notFound();` then `const authUserId = session?.user?.authUserId; if (!authUserId || !(await revalidateAdmin(authUserId))) notFound();` — use `authUserId` (the OIDC sub), NEVER `session.user.id` (the adapter uuid), for revalidateAdmin (the identity landmine). Every denial path collapses to `notFound()` (404) — never a 403, never a rendered page. Then render `<LeaderboardTable currentUserId={session.user.id} apiBase={apiBase()} />` (session.user.id IS the correct value for the own-row highlight — it matches RunUser.userId per 52-CONTEXT). Add a heading consistent with the app (e.g. "🥕 Leaderboard 🥕"). Import auth from "@/config/auth" and requireAdmin/revalidateAdmin from "@/lib/admin-gate", mirroring the admin page's imports. Do NOT add any nav link, menu entry, or header change anywhere.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit 2>&1 | grep -E "\(protected\)/leaderboard/page" | grep -v "dropdown-user.tsx|__tests__/checkin.test.ts" | head; echo "tsc-scan-done"</automated>
  </verify>
  <done>page.tsx compiles (no NEW tsc error in the file); gate mirrors the admin page (requireAdmin → notFound, revalidateAdmin(authUserId) → notFound); renders LeaderboardTable with currentUserId + apiBase; no nav entry added.</done>
</task>

<task type="auto">
  <name>Task 2: Hidden-nav assertion test</name>
  <files>apps/run.human/webapp/src/lib/leaderboard-hidden.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/leaderboard-data.test.ts (vitest style; how tests import/run)
    - apps/run.human/webapp/src/components/header/ (the set of nav/header/dropdown/menu source files to scan)
  </read_first>
  <action>
    Create `lib/leaderboard-hidden.test.ts` — a vitest test that reads every nav/header source file under `src/components/header/` (header.tsx, dropdown-user.tsx, dropdown-menu.tsx, and any icon/menu siblings) from disk (Node `fs` + `path`, resolved from the webapp root / this file's dirname) and asserts NONE of them contains the route path string for the leaderboard page (build the needle at runtime from the segment name so the literal is not hard-baked as a self-tripping constant — e.g. concatenate "/" with the word). The test's intent: the leaderboard route is reachable by URL only, never surfaced in navigation (SC #2). Also assert the header directory scan actually found > 0 files (guard against a silent empty glob that would vacuously pass).
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/lib/leaderboard-hidden.test.ts</automated>
  </verify>
  <done>leaderboard-hidden.test.ts passes: it scanned ≥1 header file and none reference the leaderboard route (the hidden guarantee, SC #2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous/non-admin → /leaderboard | untrusted visitor requests the hidden admin route |
| session.user.authUserId → run.auth revalidateAdmin | live fresh-claims admin check (keyed by OIDC sub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-05 | Elevation of Privilege / Info Disclosure | page gate | high | mitigate | requireAdmin(session) → notFound() AND revalidateAdmin(authUserId) → notFound(); every denial is a bare 404, never a 403, never a rendered page (SC #1). |
| T-52-06 | Spoofing (identity mismatch) | revalidateAdmin arg | high | mitigate | Pass session.user.authUserId (OIDC sub), NOT session.user.id (adapter uuid) — the Phase-43 landmine that would 404 a real admin or, worse, mis-key the claims check. |
| T-52-07 | Information Disclosure (feature reveal) | navigation | medium | mitigate | Page linked from NO nav; leaderboard-hidden.test.ts asserts the route string is absent from every header/dropdown/menu component (SC #2). |

No package installs this phase — no supply-chain (SC) threat row.
</threat_model>

<verification>
- `npx vitest run src/lib/leaderboard-hidden.test.ts` passes (≥1 nav file scanned, none reference the route).
- `npx tsc --noEmit` introduces no NEW error in page.tsx.
- Orchestrator local browser check (end-of-phase): run.human against local DynamoDB — an admin session sees the ranked accordion at /leaderboard; a non-admin/signed-out session gets 404. Expand a row → canvas thumbnails render; own row highlighted; chips/emoji match the API.
</verification>

<success_criteria>
- Non-admin/signed-out → 404 at /leaderboard; admin → ranked accordion (SC #1).
- The route appears in no navigation component, proven by test (SC #2).
- revalidateAdmin is called with authUserId (OIDC sub), not the adapter id.
</success_criteria>

<output>
Create `.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-03-SUMMARY.md` when done
</output>
