---
phase: 52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/lib/polyline-geometry.ts
  - apps/run.human/webapp/src/lib/polyline-geometry.test.ts
  - apps/run.human/webapp/src/components/leaderboard/PolylineRenderer.tsx
autonomous: true
requirements: [LDBR-09]

must_haves:
  truths:
    - "A <canvas> thumbnail draws one OSM tile + the route polyline + a green start dot and a red end dot from a points:{lat,lng}[] input (SC #3)."
    - "In dark mode the tile is inverted via the DC33 canvas filter; on tile load error the route still draws (DC33 fallback)."
    - "The pure bounds/zoom/center-tile geometry is unit-tested with no DOM/canvas."
  artifacts:
    - "apps/run.human/webapp/src/lib/polyline-geometry.ts"
    - "apps/run.human/webapp/src/lib/polyline-geometry.test.ts"
    - "apps/run.human/webapp/src/components/leaderboard/PolylineRenderer.tsx"
  key_links:
    - "PolylineRenderer imports computeBounds/calculateZoomLevel/centerTile from lib/polyline-geometry (the tested seam)."
    - "Renderer input is points:{lat,lng}[] OBJECTS (Accomplishment.metadata.polyline) — NO Google-polyline decode."
---

<objective>
Port DC33's `PolylineRenderer` to run.human as a client `<canvas>` route thumbnail (LDBR-09), and extract its bounds → zoom → center-tile math into a PURE, unit-tested `lib/polyline-geometry.ts` seam.

Faithful DC33 port with ONE deliberate DC34 delta: the input is already an array of `{lat,lng}` OBJECTS (from `Accomplishment.metadata.polyline`, produced in Phase 50), so DC33's `decodePolyline` (Google-polyline decode) is DROPPED. Everything else — single-OSM-tile fetch, white-halo route, green-start/red-end dots, dark-mode canvas filter, tile-error fallback — is reproduced.

Purpose: The renderer is the visual payoff of the leaderboard (SC #3). Extracting the geometry keeps the map math testable without a canvas/DOM.
Output: One pure helper module + its test + the client component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-CONTEXT.md

# DC33 source to reproduce (different repo, read-only):
@/Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/routes/PolylineRenderer.tsx

# DC34 data shape the renderer consumes (metadata.polyline = {lat,lng}[]):
@apps/run.human/webapp/src/entities/accomplishment.ts

# Test convention to mirror (pure vitest, no mocks):
@apps/run.human/webapp/src/lib/leaderboard-data.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure polyline geometry seam + tests</name>
  <files>apps/run.human/webapp/src/lib/polyline-geometry.ts, apps/run.human/webapp/src/lib/polyline-geometry.test.ts</files>
  <read_first>
    - /Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/routes/PolylineRenderer.tsx (functions latLngToTile, tileToLatLng, calculateZoomLevel, and the bounds loop inside renderPolylineOnly — reproduce their math EXACTLY)
    - apps/run.human/webapp/src/lib/leaderboard-data.test.ts (vitest style: describe/it/expect, plain fixtures, no mocks)
  </read_first>
  <behavior>
    - computeBounds([]) returns null; computeBounds(points) returns {minLat,maxLat,minLng,maxLng} covering all points.
    - latLngToTile(lat,lng,zoom) reproduces DC33's floored slippy-map formula exactly (x = floor((lng+180)/360 * 2^zoom); y from the mercator log/tan formula).
    - calculateZoomLevel(bounds) walks zoom 15→10 and returns the first where the bounds span ≤1 tile in x and y; fallback 12 when none fit.
    - centerTile(bounds) returns {zoom,x,y} = latLngToTile(centerLat,centerLng,calculateZoomLevel(bounds)) where center = midpoint of bounds.
    - Degenerate case: a single-point bounds (min==max) yields a valid zoom (15) and a finite tile.
  </behavior>
  <action>
    Create `lib/polyline-geometry.ts` exporting the PURE types and functions the renderer needs: a `LatLng = { lat: number; lng: number }` type, a `Bounds = { minLat; maxLat; minLng; maxLng }` type, and `computeBounds`, `latLngToTile`, `calculateZoomLevel`, `centerTile`. Reproduce DC33's math verbatim (floored tile coords; zoom loop 15→10 with fallback 12; center = bounds midpoint). No DOM, no fetch, no React — this module must be importable in a Node vitest run with zero side effects. `computeBounds` returns null for an empty array so the caller can early-out. Write `polyline-geometry.test.ts` alongside covering every behavior bullet with plain fixtures (a two-point Vegas-area route, a single-point degenerate route, an empty array). Mirror the leaderboard-data.test.ts convention (describe/it/expect, no mocks). Do NOT port DC33's decodePolyline/parseGPX — those are out of scope for the object-input DC34 renderer.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/lib/polyline-geometry.test.ts</automated>
  </verify>
  <done>polyline-geometry.test.ts passes (all behavior bullets green); the module has no React/DOM/network import.</done>
</task>

<task type="auto">
  <name>Task 2: PolylineRenderer client canvas component</name>
  <files>apps/run.human/webapp/src/components/leaderboard/PolylineRenderer.tsx</files>
  <read_first>
    - /Users/khundeck/working/defcon.run.33/apps/nx/apps/webapp/src/components/routes/PolylineRenderer.tsx (reproduce the useEffect draw pipeline, white-halo route, green #10B981 start / red #EF4444 end markers, dark-mode filter `invert(1) hue-rotate(180deg) brightness(1.2) contrast(0.9)`, and the tile onerror → drawPolyline fallback)
    - apps/run.human/webapp/src/lib/polyline-geometry.ts (the seam this component consumes — from Task 1)
  </read_first>
  <action>
    Create the `'use client'` component `components/leaderboard/PolylineRenderer.tsx`. Props: `points: {lat,lng}[]`, optional `width=200`, `height=120`, `strokeColor='#3B82F6'`, `strokeWidth=2`, `padding=10`, `showMapTile=true`, `theme?: 'light'|'dark'`. Import `computeBounds` and `centerTile` from `@/lib/polyline-geometry` — do NOT re-implement the math inline. In a `useEffect` keyed on the inputs: get the 2D context, clear, and if points.length < 2 draw the DC33 "No route data" text and return. Otherwise compute bounds, and when `showMapTile` fetch the single OSM tile `https://tile.openstreetmap.org/{zoom}/{x}/{y}.png` (from `centerTile`) with `crossOrigin='anonymous'`; on load draw it (applying the DC33 dark-mode canvas filter when theme is dark, then reset filter), overlay the DC33 semi-transparent wash, then draw the route; on tile error call the same drawPolyline fallback so the route still renders. drawPolyline uses DC33's LINEAR lat/lng→canvas scaling (NOT mercator-projected onto the tile — keep DC33 fidelity), a white halo under the stroke, then the colored stroke, then a white-ringed green start dot and red end dot. Reproduce DC33's canvas className/border/rounded styling adapted to run.human's default-token classes. Convert the object input to the [lat,lng] pairs the draw loop expects at the top of the effect (map `{lat,lng}` → the local shape) — the incoming polyline is already objects, so there is NO decode step.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit 2>&1 | grep -E "leaderboard/PolylineRenderer|polyline-geometry" | grep -v "dropdown-user.tsx|__tests__/checkin.test.ts" | head; echo "tsc-scan-done"</automated>
  </verify>
  <done>tsc reports NO error in PolylineRenderer.tsx or polyline-geometry.ts (the two known pre-existing errors in dropdown-user.tsx and __tests__/checkin.test.ts are the only permitted remainder); component compiles as a client component and imports the geometry seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tile.openstreetmap.org | client fetches one map tile per rendered run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Information Disclosure | OSM tile fetch | low | accept | Route coords are already visible to the admin viewing them; tile host is a fixed public endpoint (DC33 parity). Admin-only surface. |
| T-52-02 | Denial of Service | canvas draw on malformed polyline | low | mitigate | Guard points.length < 2 (draw "No route data" and return); computeBounds returns null on empty input; tile onerror falls back to drawing the route only. |

No package installs this phase (heroui/lucide-react already deps) — no supply-chain (SC) threat row.
</threat_model>

<verification>
- `npx vitest run src/lib/polyline-geometry.test.ts` passes (from apps/run.human/webapp with Node ≥22.12).
- `npx tsc --noEmit` introduces no NEW errors in PolylineRenderer.tsx or polyline-geometry.ts (only the 2 known pre-existing out-of-scope errors may remain).
</verification>

<success_criteria>
- Pure geometry module exists and is unit-tested (bounds/zoom/center-tile).
- PolylineRenderer draws OSM tile + route + green-start/red-end dots + dark-mode filter from a {lat,lng}[] input, with a tile-error fallback — no Google-polyline decode.
</success_criteria>

<output>
Create `.planning/phases/52-leaderboard-ui-polylinerenderer-accordion-hidden-admin-page/52-01-SUMMARY.md` when done
</output>
