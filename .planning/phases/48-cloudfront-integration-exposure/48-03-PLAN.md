---
phase: 48-cloudfront-integration-exposure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ctf-score-integration.md
autonomous: true
requirements: [CTF-14]
must_haves:
  truths:
    - "`docs/ctf-score-integration.md` gives the DC33 total-score mapper the exact CTF read: table, entities, pk/sk composites, the byUser GSI, and a runnable sample query — per CTF-14."
    - "Every key shape in the doc matches the committed entities (`apps/run.human/webapp/src/entities/ctf.ts` CtfSolve + `run-user.ts` RunUser.ctfScore/ctfSolves), confirmed by grep cross-check."
    - "The doc explicitly states the global/total leaderboard is NOT built here and that our writes are NOT coupled to the DC33 schema — we expose a signal, they map it."
  artifacts:
    - docs/ctf-score-integration.md
  key_links:
    - "CtfSolve GSI `gsi1pk-gsi1sk-index` (pk = user) is the 'all my solves' read; RunUser.ctfScore is the rollup leaderboard key; CtfSolve rows are the auditable source of truth."
    - "Table `run-human-electro` (env `RUN_ELECTRO_DBNAME`); ElectroDB service `run` / version `1` drives the `$run#...` key encoding."
---

<objective>
Document and confirm-queryable the CTF signal (CTF-14): a short markdown doc that gives the separate DC33 total-score migration worktree the exact read it consumes — `RunUser.ctfScore`/`ctfSolves` (the cheap rollup) + `CtfSolve` rows (the auditable source of truth) — with entity names, table, pk/sk composites, the byUser GSI, and a sample query, all confirmed against the committed entities. Do NOT build the global/total board; do NOT couple our writes to their schema.

This is a pure-documentation plan with zero infra/app code changes. It runs in parallel with the two infra plans (no shared files).

Purpose: give the DC33 mapper a stable, correct integration contract so it can roll CTF points into its global total without reverse-engineering our data layer.
Output: `docs/ctf-score-integration.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-cloudfront-integration-exposure/48-CONTEXT.md
@apps/run.human/webapp/src/entities/ctf.ts
@apps/run.human/webapp/src/entities/run-user.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the entity/key reference + sample query in docs/ctf-score-integration.md</name>
  <files>docs/ctf-score-integration.md</files>
  <read_first>
    - apps/run.human/webapp/src/entities/ctf.ts — `CtfSolve` entity: `model.service = "run"`, `version = "1"`; primary `pk composite ["challenge"]` / `sk composite ["user"]`; GSI `byUser` = index `"gsi1pk-gsi1sk-index"`, `pk composite ["user"]` / `sk composite ["challenge"]`; attributes ordinal, points, firstBlood, tierCeiling, channel (`"qr"|"covert"`), solvedAt. Confirm the exact strings.
    - apps/run.human/webapp/src/entities/run-user.ts — `RunUser` entity: `service "run"` / `version "1"`; primary `pk composite ["userId"]` / `sk composite []`; attributes `ctfScore` (number, rollup total via atomic ADD) and `ctfSolves` (number, count). Confirm the exact field names + the "rollup vs source of truth" comment.
    - apps/run.human/webapp/src/entities/client.ts — `ELECTRO_TABLE = process.env.RUN_ELECTRO_DBNAME || "run-human-electro"`.
    - 48-CONTEXT.md `<decisions>` CTF-14 line — the confirmed raw key encodings: RunUser pk `$run#userid_<userId>` / sk `$runuser_1`; CtfSolve pk `$run#challenge_<challenge>` / sk `$ctfsolve_1#user_<user>`; byUser gsi1 pk `$run#user_<user>` / gsi1 sk `$ctfsolve_1#challenge_<challenge>`. (ElectroDB encodes keys as `$<service>#<attr>_<value>` and `$<entity>_<version>#...`; state the encoding so a non-ElectroDB reader can build keys, but flag ElectroDB as the authoritative encoder.)
  </read_first>
  <action>
    Create `docs/ctf-score-integration.md`. Structure it as a concise integration contract for the DC33 total-score mapper:

    (1) Header/intent — "CTF score integration for the DC33 total-score migration": we own + expose the CTF contribution; the mapper reads it and rolls it into the global total. State the table `run-human-electro` (env `RUN_ELECTRO_DBNAME`), region us-east-1, and that all entities use ElectroDB `service: "run"`, `version: "1"`.

    (2) The fast read — `RunUser.ctfScore` (running total, atomic ADD) + `ctfSolves` (count). Give the RunUser key: primary pk composite `["userId"]` → `$run#userid_<userId>`, sk `[]` → `$runuser_1`. Note `ctfScore` is a cheap rollup for leaderboard reads.

    (3) The source of truth — `CtfSolve` rows (one per challenge+user). Give the primary key: pk composite `["challenge"]` → `$run#challenge_<challenge>`, sk composite `["user"]` → `$ctfsolve_1#user_<user>`; and the `byUser` GSI `gsi1pk-gsi1sk-index`: gsi1pk composite `["user"]` → `$run#user_<user>`, gsi1sk composite `["challenge"]` → `$ctfsolve_1#challenge_<challenge>`. List the scored attributes: ordinal, points, firstBlood, tierCeiling, channel (`qr`|`covert`), solvedAt. State that `ctfScore` for a user == `sum(CtfSolve.points)` (rebuildable), so a mapper may trust `ctfScore` for speed OR recompute from CtfSolve for audit.

    (4) Sample query — show BOTH forms in fenced code blocks (fenced code is fine in a doc file — the `<action>` no-fenced-code rule applies to plan actions, not to this authored artifact): (a) ElectroDB idiomatic "all solves for a user" via `CtfSolve.query.byUser({ user }).go()`; (b) raw DynamoDB Query on `gsi1pk-gsi1sk-index` with `gsi1pk = "$run#user_<user>"` for a mapper not using ElectroDB. Also show reading `ctfScore` via a GetItem on the RunUser primary key.

    (5) Integration boundary — explicit non-goals: the global/total leaderboard is NOT built here (a separate worktree owns it); we do NOT couple our writes to the DC33 schema; the q admin leaderboard is CTF-only. Mirror design spec §11.

    Ground every key string against the entities as read above — do not invent attribute names. Keep it tight and skimmable.
  </action>
  <verify>
    <automated>test -f docs/ctf-score-integration.md && grep -q 'run-human-electro' docs/ctf-score-integration.md && grep -q 'gsi1pk-gsi1sk-index' docs/ctf-score-integration.md && grep -q 'ctfScore' docs/ctf-score-integration.md && grep -q 'CtfSolve' docs/ctf-score-integration.md && grep -q 'gsi1pk-gsi1sk-index' apps/run.human/webapp/src/entities/ctf.ts && grep -q 'ctfScore' apps/run.human/webapp/src/entities/run-user.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ctf-score-integration.md` exists and names the table `run-human-electro`, both entities (`RunUser`, `CtfSolve`), the primary + `gsi1pk-gsi1sk-index` key composites, and `ctfScore`/`ctfSolves`.
    - The doc's key strings match the committed entities (grep cross-check on ctf.ts + run-user.ts passes).
    - The doc contains at least one runnable sample query (ElectroDB `byUser` and/or raw DynamoDB Query on the GSI) plus a RunUser GetItem for `ctfScore`.
    - The doc explicitly states the global/total board is NOT built here and that our writes are not coupled to the DC33 schema.
  </acceptance_criteria>
  <done>A tight integration contract exists giving the DC33 mapper the exact, entity-confirmed CTF read (rollup + source of truth + sample query) with the non-goals stated; no app/infra code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run.human data layer → DC33 mapper (separate worktree) | The mapper reads our `run-human-electro` CTF signal to compute a global total. |
| documentation vs implementation | The doc must reflect the committed entity keys, or the mapper builds wrong keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-09 | Information disclosure | documenting internal key shapes | low | accept | Keys are internal identifiers already derivable from the shipped entities; exposing them to the sibling DC33 worktree is the intended integration, not a leak. No secrets/PII in the doc. |
| T-48-10 | Tampering / integrity | doc drifting from the real entities | medium | mitigate | The task verify grep-cross-checks every documented key token against `ctf.ts`/`run-user.ts`; the doc states ElectroDB is the authoritative key encoder and `ctfScore` is rebuildable from `CtfSolve` (audit path). |
| T-48-11 | Repudiation / scope creep | mapper coupling to our write schema | low | mitigate | The doc explicitly declares the boundary: we expose a read signal, we do not own the global board, and our writes are not coupled to the DC33 schema. |
</threat_model>

<verification>
- `docs/ctf-score-integration.md` exists and documents table, both entities, primary + GSI key composites, `ctfScore`/`ctfSolves`, and a sample query.
- Every documented key token cross-checks against the committed entities (`ctf.ts`, `run-user.ts`).
- The non-goal (no global board; no write-schema coupling) is stated.
- No app or infra code was modified.
</verification>

<success_criteria>
CTF-14 is satisfied: `docs/ctf-score-integration.md` documents the exact `ctfScore`/`CtfSolve` read (entity, table, keys, GSI, sample query) confirmed against the committed entities, with the global board explicitly NOT built here.
</success_criteria>

<output>
Create `.planning/phases/48-cloudfront-integration-exposure/48-03-SUMMARY.md` when done.
</output>