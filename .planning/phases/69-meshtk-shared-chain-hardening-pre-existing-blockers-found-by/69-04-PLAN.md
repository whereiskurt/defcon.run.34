---
phase: 69-meshtk-shared-chain-hardening-pre-existing-blockers-found-by
plan: 04
type: execute
wave: 4
depends_on: ["69-03"]
files_modified:
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish_test.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe_test.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_connect_fail_test.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go
  - /Users/khundeck/working/meshtk/internal/app/server/inspect.go
  - /Users/khundeck/working/meshtk/internal/app/server/inspect_v5.go
  - /Users/khundeck/working/meshtk/internal/app/server/rules.go
  - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_publish_test.go
autonomous: true
requirements: [MQFX-04]
must_haves:
  truths:
    - "A hand-parsed v5 PUBLISH carrying a Topic Alias property is Blocked exactly as the codec-parsed path Blocks it, so the documented decision-for-decision parity is true"
    - "Detecting the alias never gates inspection: a frame whose first property id is unknown is still fully inspected, decided and hop-clamped"
    - "Every v5 SUBSCRIBE reaches PacketDecider with its topic filters recorded, including one the codec cannot parse, and it is matched by the same rule by NAME as a 3.1.1 SUBSCRIBE"
    - "An unparseable v5 CONNECT is answered with a version-correct CONNACK instead of a silent TCP close, so the client cannot hot-retry against a mute socket"
    - "No test name in the suite still describes the retired fail-open PUBLISH posture"
    - "The 3.1.1 forwarded-byte behavior is unchanged: proxy_v4_golden_test.go is byte-unedited and green"
  artifacts:
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe.go
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe_test.go
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_connect_fail_test.go
  key_links:
    - "parseV5PublishFrame alias scan -> v5RawPublish.HasTopicAlias -> handleV5PublishUplink Block on reason=topic_alias_uplink"
    - "parseV5SubscribeFrame -> inspectV5RawSubscribe -> RawPacket.MQTT5RawSub -> rules.go AllowMQTTControl third branch -> PacketDecider.Decide"
    - "handleProxyV5 CONNECT failure branches -> writeMqtt5Connack(conn, v5.ConnackMalformedPacket)"
---

<objective>
Close the four remaining lower-severity `68-REVIEW.md` warnings that Phase 68's own fixes
introduced, plus the misleading test name (MQFX-04, items a, b, c and e; item d shipped in
69-03).

- **(a) WR-01** — the CR-04 hand-parse PUBLISH path documents itself as mirroring its codec
  sibling "decision for decision" and does not: the sibling Blocks on
  `Properties.TopicAlias != nil`, the hand-parse path skips the property block whole and
  cannot see it. PROVEN: a PUBLISH carrying `TopicAlias=7` plus an unmodelled id was ALLOWED
  on the hand-parsed path.
- **(b) WR-04** — a SUBSCRIBE the codec cannot parse is relayed without reaching
  `inspectV5Subscribe` or `PacketDecider`, re-opening codec-dependent inspection one layer up.
  The exemption is bought with the same three client-chosen bytes as CR-04, and the first
  topic Block rule anyone adds will silently not apply to those frames.
- **(c) WR-02** — an unparseable v5 CONNECT is dropped with NO CONNACK, so the client gets a
  silent TCP close and hot-retries. That is the exact failure mode Phase 68 existed to remove
  (0x84 retry-looping), reintroduced one layer down.
- **(e)** — `TestV5PublishParseFailureForwardsRaw` still names the retired fail-open posture.

Purpose: remove the asymmetries that let a client pick which inspection path judges it.
Output: an alias-aware hand parser that never gates inspection on a property table, a
property-agnostic SUBSCRIBE seam, version-correct CONNECT failures, and honest test names.

**Execution location.** Upstream `/Users/khundeck/working/meshtk` only, on the phase branch.
Never `apps/run.mqtt/meshtk`; never `internal/embedded/`.

**Scope fence.** WR-03 (both new relay paths handing mosquitto frames the proxy knows are
malformed, after which the broker kills the session) is NOT in MQFX-04 and is NOT in scope for
the codec-parse-fail-but-hand-parse-OK relay. Leave that relay as it is; the one place this
plan chooses fail-closed is a SUBSCRIBE whose own length prefixes contradict its bytes, which
is the same bar `MQTT5_PUBLISH_HEADER_FAIL` already applies. WR-06 through WR-13 (other than
item e) are also out of scope, including the `gofmt` wart (WR-12).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-REVIEW.md
@.planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-VERIFICATION.md

Source of truth: `68-REVIEW.md` **WR-01**, **WR-02**, **WR-04** and the Anti-Patterns row for
the misleading test name; `68-VERIFICATION.md` warnings **W1** and **W2** for the impact
assessment (why these are warnings and not blockers).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the hand-parsed PUBLISH path a real topic-alias guard, without a property table in the inspection path</name>
  <files>/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go, /Users/khundeck/working/meshtk/internal/app/server/inspect_v5.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish_test.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_publish_test.go</files>
  <read_first>
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish.go — read the whole file: the `v5RawPublish` struct, `parseV5PublishFrame` (note it already computes the property-block bounds and then skips them whole, and its doc comment states that reading one property id would put a property table back in the inspection path), `spliceV5PublishPayload`, `decodeV5Varint`.
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go — read `handleV5PublishUplink` and compare its two guards side by side: the codec path Blocks on `(p.Properties != nil && p.Properties.TopicAlias != nil) || p.Topic == ""`; the hand-parse arm checks only the empty topic. That difference IS the defect.
    - /Users/khundeck/working/meshtk/internal/app/server/inspect_v5.go — `inspectV5RawPublish`'s doc comment, which claims it "mirrors its sibling decision for decision"; that claim must become true or be corrected.
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish_test.go — the existing `TestParseV5PublishFrame*` and `TestSpliceV5PublishPayload*` cases; extend, do not replace.
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_publish_test.go — read `TestV5PublishParseFailureForwardsRaw` (its fixture, its assertions and why they remain correct under the fail-closed posture) plus `TestV5TopicAliasBlocked` for the codec-path expectations.
    - .planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-REVIEW.md — WR-01, including the PROVEN divergence output and both suggested fix shapes.
  </read_first>
  <behavior>
    - A hand-parsed PUBLISH whose property block contains the Topic Alias property (id 0x23) at a position reachable by a sound walk is Blocked with the existing `reason=topic_alias_uplink`, matching the codec path.
    - A hand-parsed PUBLISH whose property block starts with an id the walk does not model returns an INDETERMINATE alias result, is logged once, and is still fully inspected: topic recovered, ServiceEnvelope decoded, `PacketDecider` run, hop clamp applied to the forwarded bytes.
    - Ordering coverage: alias-property-before-unknown-id yields Block; unknown-id-before-alias-property yields indeterminate-plus-inspected. Both orders are tested.
    - A PUBLISH with a modelled, alias-free property block (message expiry, user property) is walked to completion, reported alias-free, and behaves exactly as today.
    - Existing behavior is unchanged for every fixture already in the suite: the 68-07 clamp, splice and byte-identity tests keep passing unedited.
  </behavior>
  <action>
    Add a bounded, sound-until-unknown alias scan to `proxy_v5_rawpublish.go` — a function that
    walks the property block reading id bytes and skipping each modelled value by its MQTT 5.0
    wire type, and returns two booleans: whether a Topic Alias was found, and whether the walk
    completed. The modelled PUBLISH property ids and their wire shapes are: payload format
    indicator (one byte), message expiry interval (four bytes), content type (length-prefixed
    string), response topic (length-prefixed string), correlation data (length-prefixed
    binary), subscription identifier (variable byte integer), topic alias (two bytes), user
    property (two length-prefixed strings). On the topic-alias id, return found-and-complete
    immediately. On ANY id outside that set, or on a length prefix that runs past the block
    end, return not-found-and-incomplete — never an error, never a partial view, and never a
    reason to skip inspection.

    Have `parseV5PublishFrame` call it with the block bounds it already computes and record
    both results on `v5RawPublish` as two new fields. Keep the struct's existing
    report-never-judge contract: the parser reports the alias, the caller Blocks.

    The distinction from the CR-04 defect is the whole point and must be written into the doc
    comment: the CR-04 defect was that an unmodelled property id GATED inspection. Here an
    unmodelled id only abandons one optional refinement — the frame is still parsed,
    inspected, decided and clamped. Say that explicitly, and say that an id table is
    acceptable only because of that property.

    In `handleV5PublishUplink`'s hand-parse arm, extend the existing empty-topic guard to also
    Block when the alias was found, reusing the existing `action=BLOCK, reason=topic_alias_uplink`
    line so production greps and any prior evidence keep working. When the walk was
    incomplete, log once through `InspectorLogger` with a new distinguishable action —
    `action=MQTT5_ALIAS_SCAN_INDETERMINATE` — and CONTINUE into the inspector; do not Block.
    State the compensating control in the comment: 68-01 strips `TopicAliasMaximum` from both
    the CONNECT and the CONNACK, so the broker grants a zero alias budget and would treat any
    alias as a protocol error anyway; the residual is therefore bounded and now observable.

    Correct `inspectV5RawPublish`'s doc comment so it states precisely which decisions it
    mirrors and names the residual (an incomplete alias walk) instead of claiming unqualified
    parity.

    Rename `TestV5PublishParseFailureForwardsRaw` to a name that describes the shipped
    fail-closed posture — the frame is inspected and THEN forwarded byte-identically because no
    rule mutated it. Keep every assertion in that test unchanged; this is a rename plus a
    comment, not a rewrite. Update its doc comment to say what it now proves.

    Extend `proxy_v5_rawpublish_test.go` with unit cases for the scan (each modelled wire type
    skipped correctly, alias found, alias absent, unknown id short-circuits to incomplete,
    truncated value short-circuits to incomplete) and `proxy_v5_publish_test.go` with the two
    ordering cases from `<behavior>`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run 'TestParseV5PublishFrame|TestV5TopicAlias|TestV5UnmodelledProperty|TestSpliceV5PublishPayload' -count=1 -v && go test ./internal/app/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'HasTopicAlias' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish.go` returns the new struct field and its assignment.
    - `grep -n 'topic_alias_uplink' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go` returns 2 matches (codec path and hand-parse path), and the hand-parse guard's condition includes the alias field — verified by reading it.
    - `grep -c 'action=MQTT5_ALIAS_SCAN_INDETERMINATE' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go` returns 1.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run TestV5UnmodelledPropertyPublishIsClamped -count=1` exits 0 — the 68-07 clamp behavior is unchanged, proving the scan does not gate inspection.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run TestV5UnmodelledPropertyPublishBlockRuleFires -count=1` exits 0.
    - A test exists asserting an unknown property id BEFORE the alias yields a full inspection (decider reached, not Blocked for alias) and another asserting the alias BEFORE an unknown id yields a Block — both pass.
    - `grep -c 'TestV5PublishParseFailureForwardsRaw' /Users/khundeck/working/meshtk/internal/app/server/*.go` returns 0 (the misleading name is gone), and the replacement test passes.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -count=1` exits 0.
  </acceptance_criteria>
  <done>The hand-parsed PUBLISH path Blocks a detectable topic alias with the same reason as the codec path, an unknown property id degrades to a logged indeterminate result without ever skipping inspection, the parity comment is true, and the misleading test name is gone with its assertions intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a property-agnostic SUBSCRIBE seam so every SUBSCRIBE reaches the decider</name>
  <files>/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe_test.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go, /Users/khundeck/working/meshtk/internal/app/server/inspect.go, /Users/khundeck/working/meshtk/internal/app/server/rules.go</files>
  <read_first>
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go — read the `v5.SUBSCRIBE` arm of `handleProxyV5`'s frame switch: on a codec parse failure it logs `MQTT5_PARSE_FAIL` and relays without ever building an InspectorPacket (accepted risk T-68-06-05). That arm is what changes.
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawpublish.go — the parser this one mirrors: length-prefix-only walking, cap check before trusting a length, error and NO partial view on any inconsistency, and the deliberate never-read-a-property-id posture.
    - /Users/khundeck/working/meshtk/internal/app/server/inspect_v5.go — `inspectV5Subscribe` (what a parseable SUBSCRIBE produces: `MQTT.Type` set to the literal SUBSCRIBE, `MQTT.Topics` in filter order, `SetConnTrack` for the tracked original username) and `inspectV5RawPublish` (the pattern for a hand-parsed inspector).
    - /Users/khundeck/working/meshtk/internal/app/server/inspect.go — the `RawPacket` struct and its at-most-one-field doc comment, which enumerates `MQTT`, `MQTT5` and `MQTT5Raw` and will need a fourth member.
    - /Users/khundeck/working/meshtk/internal/app/server/rules.go — `AllowMQTTControl`'s two existing branches and the explicit neither-codec-populated tail. The 3.1.1 branch must stay first and unedited.
    - /Users/khundeck/working/meshtk/internal/app/server/decider.go — `Decide` returns `NoMatch` when nothing matches, and `WriteDecisionLog` prints `action=ALLOW` for `NoMatch`; that is exactly why the parity bar is the matching rule NAME, not "not Blocked".
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_parity_test.go — `startV5Session`, `waitFrames`, `v5ParityServer`, `matchingRuleName`, `allowMQTTControlRule`, `TestV5SubscribeMatchesSameRuleAsV4`, `TestV5SubscribeParseFailureRelaysRaw`: the harness and the existing parity assertions this task must extend rather than duplicate.
    - .planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-REVIEW.md — WR-04 and its suggested fix.
  </read_first>
  <behavior>
    - A v5 SUBSCRIBE the codec refuses to parse (unmodelled property id) has its packet id and every topic filter recovered by the hand parser, in filter order.
    - That frame produces an InspectorPacket carrying `MQTT.Type` SUBSCRIBE and `MQTT.Topics`, reaches `PacketDecider.Decide`, and is matched by `AllowMQTTControl` — the SAME rule by NAME that matches a parseable v5 SUBSCRIBE and a 3.1.1 SUBSCRIBE.
    - The CAPTURED frame is what gets relayed, byte-identical; the hand parse is read-only.
    - A SUBSCRIBE whose own length prefixes contradict its bytes is refused: logged as `action=MQTT5_SUBSCRIBE_HEADER_FAIL`, answered with a v5 DISCONNECT carrying the malformed-packet reason code, and the connection ends.
    - `AllowMQTTControl`'s 3.1.1 answers are unchanged, asserted by the existing matcher-level v4 test and by the golden.
  </behavior>
  <action>
    Create `proxy_v5_rawsubscribe.go` holding the complete raw-SUBSCRIBE seam — the parser and
    the inspector together, so the property-agnostic parse and the InspectorPacket it feeds are
    read as one unit.

    The parser walks only what the wire format guarantees is skippable: the fixed-header byte
    (type 8), the remaining-length varint, the two-byte packet identifier (always present on a
    SUBSCRIBE), the property-block length varint, the property block SKIPPED WHOLE, then a
    sequence of length-prefixed topic filters each followed by a one-byte subscription-options
    byte. Apply the same discipline as the PUBLISH parser: check the packet cap before trusting
    a length, validate every prefix against the bytes actually present, require the walk to end
    exactly at the frame end, and return an error with NO partial view on any inconsistency.
    Reject a filter list that is empty (a SUBSCRIBE with no filters is malformed per MQTT 5.0).
    Read no property id.

    Add a `v5RawSubscribe`-shaped view carrying the packet id and the ordered filters, and a
    matching new field on `RawPacket` for it. Update `RawPacket`'s doc comment: the at-most-one
    invariant now spans four fields, and every reader must nil-guard the one it wants.

    The inspector mirrors `inspectV5RawPublish`: build the InspectorPacket with only the new
    raw-subscribe field set, call `SetConnTrack` (load-bearing — the forwarded CONNECT carries
    the swapped proxy identity, so without it `Track.Username` is empty and
    `RequireMQTTUserName` would Block a subscribe on an authenticated session), set `MQTT.Type`
    to the same literal `inspectV5Subscribe` uses, and fill `MQTT.Topics` in filter order.
    Decode nothing meshtastic — a SUBSCRIBE has no payload.

    Add a third dispatch branch to `AllowMQTTControl` for the new field, returning true for a
    hand-parsed SUBSCRIBE. The 3.1.1 branch stays FIRST and unedited so the golden's decision
    sequence cannot move. Record the rejected alternative in a comment: synthesizing a
    `v5.Subscribe` for a frame the codec refused to parse would make `Raw.MQTT5` lie about
    where the data came from, and the RawPacket doc's never-synthesize invariant exists because
    a synthesized packet is one a rule can mutate without the mutation reaching the wire.

    Rewire the `v5.SUBSCRIBE` arm: keep the `MQTT5_PARSE_FAIL` line (it is the signal that the
    path was taken), then hand-parse. On success, inspect, run the SAME decide/log switch the
    parseable branch runs — do not write a second, softer switch — and relay the CAPTURED
    frame. On a hand-parse failure, log `action=MQTT5_SUBSCRIBE_HEADER_FAIL`, write a v5
    DISCONNECT with the malformed-packet reason code and end the connection. State in the
    comment that this retires accepted risk T-68-06-05 and why: the frames that now fail closed
    are only those whose own length prefixes contradict their bytes, which mosquitto refuses
    too, and the client now gets a reason code instead of a silent close.

    Extract the decide/log switch if that is what it takes to avoid duplicating it — the same
    argument that made `decideV5Publish` a shared function applies here, and two copies would
    be two chances for the exempt path to drift into a quieter decision.

    Create `proxy_v5_rawsubscribe_test.go` covering every bullet in `<behavior>`. For the
    same-rule-by-name assertion, reuse `matchingRuleName` from the parity test rather than
    asserting on the decision alone — `AllowMQTTControl` is first among inspect rules and
    short-circuits, so a decision-only assertion would pass whether or not the inspector ran.
    Do not edit `proxy_v5_parity_test.go`.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run 'TestParseV5SubscribeFrame|TestV5RawSubscribe|TestV5Subscribe|TestAllowMQTTControl' -count=1 -v && go test ./internal/app/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe.go` exists and contains a frame parser and an inspector; `grep -c 'Prop' proxy_v5_rawsubscribe.go` shows the property block is described and skipped, and no property-id constant is compared in it (verified by reading).
    - `grep -n 'MQTT5RawSub' /Users/khundeck/working/meshtk/internal/app/server/inspect.go` returns the new `RawPacket` field, and the struct's doc comment enumerates four fields.
    - `grep -c 'MQTT5RawSub' /Users/khundeck/working/meshtk/internal/app/server/rules.go` returns at least 1, and the 3.1.1 branch is still the first branch in `AllowMQTTControl` (verified by reading).
    - `grep -c 'action=MQTT5_SUBSCRIBE_HEADER_FAIL' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go` returns 1.
    - A test asserts the matching rule NAME for a hand-parsed v5 SUBSCRIBE equals the name for a parseable v5 SUBSCRIBE and for a 3.1.1 SUBSCRIBE; it passes.
    - A test asserts the relayed bytes for a hand-parsed SUBSCRIBE are byte-identical to the captured frame; it passes.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run 'TestAllowMQTTControlV4Unchanged|TestAllowMQTTControlV5|TestV5SubscribeMatchesSameRuleAsV4' -count=1` exits 0.
    - `cd /Users/khundeck/working/meshtk && git diff --stat origin/main -- internal/app/server/proxy_v5_parity_test.go` prints nothing.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -count=1` exits 0.
  </acceptance_criteria>
  <done>Every v5 SUBSCRIBE, parseable or not, reaches the decider with its filters recorded and is matched by the same rule by name across both codecs; a malformed SUBSCRIBE fails closed with a reason code; the captured frame is what relays; the 3.1.1 allowlist answers are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Answer every v5 CONNECT failure with a version-correct CONNACK</name>
  <files>/Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go, /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_connect_fail_test.go</files>
  <read_first>
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go — read `handleProxyV5`'s three CONNECT failure branches: the `readFrame` error, the not-a-CONNECT first packet, the `v5.ReadPacket` error, and the CONNECT-parsed-as-something-else assertion failure. Each currently logs `MQTT5_PARSE_FAIL` and returns, leaving `handleProxy`'s deferred `conn.Close()` to drop the socket with nothing written.
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go — `writeMqtt5Connack` and `writeMqtt5Disconnect`, the existing answer helpers.
    - /Users/khundeck/working/meshtk/vendor/github.com/eclipse/paho.golang/packets/connack.go — the reason-code constants; the malformed-packet code is 0x81 and the unsupported-protocol-version code 0x84 is reserved for levels above 5 (answering 0x84 to a level-5 CONNECT is what made mqttastic retry-loop).
    - /Users/khundeck/working/meshtk/internal/app/server/proxy_v5_test.go — `connackFrom`, `mustHex`, `v5TestServer` and `TestV5ConnackReasonCodes`: the harness and the byte-level CONNACK expectations. Read only; put new tests in the new file.
    - .planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-REVIEW.md — WR-02, including the PROVEN routing detail that `peekConnectProtocolVersion` still returns level 5 for a CONNECT `v5.ReadPacket` refuses, so this branch is genuinely reachable.
  </read_first>
  <behavior>
    - A level-5 CONNECT carrying a property id the codec does not model receives a CONNACK whose reason byte is the malformed-packet code, then the socket closes.
    - The same holds on the other CONNECT failure branches: a frame-read failure, a first packet that is not a CONNECT, and a CONNECT frame that parses as another type.
    - The existing four CONNACK reason-code expectations are unchanged: bad credentials, enhanced auth, level above 5 and the 3.1.1 four-byte CONNACK all still produce exactly the bytes `TestV5ConnackReasonCodes` and the committed production probe pin.
    - Each failure still emits its `action=MQTT5_PARSE_FAIL` line, now carrying the answered reason code so production can distinguish an answered failure from the old silent close.
  </behavior>
  <action>
    On each of `handleProxyV5`'s CONNECT failure branches, write a CONNACK with the
    malformed-packet reason code before returning, using the existing `writeMqtt5Connack`
    helper and the vendored constant rather than a hand-rolled literal. Ignore the write error
    the same way the existing rejection paths do — the socket is about to close either way.

    Extend each branch's existing `action=MQTT5_PARSE_FAIL` line with an `answered=` field
    carrying the reason code, so 69-06 can tell an answered failure from the pre-fix silent
    close in production telemetry, and so the line remains one grep.

    Do not touch the credential, enhanced-auth or level-above-5 answers: those reason codes are
    pinned by test AND by the committed `mqtt5_probe.py` regression check, and changing any of
    them would be a regression, not a fix. State in a comment why the malformed-packet code is
    the right answer here and why the unsupported-protocol-version code is not: the proxy DOES
    speak level 5, so the honest complaint is about the frame, not the version — and answering
    the version code to a level-5 CONNECT is the retry loop Phase 68 existed to remove.

    Create `proxy_v5_connect_fail_test.go` with the tests from `<behavior>`, asserting on the
    captured client-bound BYTES (the full CONNACK frame in hex, as the existing reason-code
    test does) rather than on a helper's return value. Do not edit `proxy_v5_test.go`.

    Then run the whole gate: full package, `-race`, and the golden. Commit as `fix(69-04):`
    per item plus `test(69-04):` for the suites, on the phase branch.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run 'TestV5Connect|TestV5Connack' -count=1 -v && go test ./internal/app/server/ -count=1 && go test -race ./internal/app/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'writeMqtt5Connack' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go` increased by the number of CONNECT failure branches, and each branch's call uses the vendored malformed-packet constant (verified by reading).
    - `grep -c 'answered=' /Users/khundeck/working/meshtk/internal/app/server/proxy_v5.go` equals the number of CONNECT failure branches.
    - A test asserts the captured client-bound bytes for an unmodelled-property level-5 CONNECT are the five-byte CONNACK ending in the malformed-packet reason code plus an empty properties byte; it passes.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run 'TestV5ConnackReasonCodes|TestWriteMqtt5ConnackMatchesUnsupportedLiteral|TestV5EnhancedAuthRejected|TestV5ConnectInvalidCredsRejected' -count=1` exits 0 — no existing reason code moved.
    - `cd /Users/khundeck/working/meshtk && git diff --stat origin/main -- internal/app/server/proxy_v5_test.go` prints nothing.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -count=1` and `go test -race ./internal/app/server/ -count=1` both exit 0.
    - `cd /Users/khundeck/working/meshtk && go test ./internal/app/server/ -run TestV4SessionForwardBytesGolden -count=1` exits 0 and `git diff --stat origin/main -- internal/app/server/proxy_v4_golden_test.go` prints nothing.
    - `cd /Users/khundeck/working/meshtk && git diff --stat origin/main -- go.mod go.sum vendor/` prints nothing.
  </acceptance_criteria>
  <done>Every v5 CONNECT failure branch answers with the malformed-packet CONNACK and says so in its log line; the four pinned reason codes are unmoved; full package green under `-count=1` and `-race`; golden unedited and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted MQTT client → v5 frame switch | the client chooses which inspection path judges it by choosing property bytes the codec cannot read |
| hand-parsed view → PacketDecider | anything the hand parser fails to report is a control the rules cannot apply |
| proxy → v5 client answer channel | a client that gets no answer cannot distinguish refusal from a network fault and retries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04-01 | Tampering | a Topic Alias property invisible to the hand-parsed PUBLISH path while the codec path Blocks it (WR-01, PROVEN divergence) | medium | mitigate | Task 1's bounded alias scan Blocks on a detectable alias with the same reason string as the codec path; both property orderings are tested |
| T-69-04-02 | Tampering | reintroducing a property table that GATES inspection — the CR-04 defect class | high | mitigate | the scan never returns an error and never skips inspection: an unmodelled id abandons the walk only, and the 68-07 clamp and block-rule tests must still pass unedited as proof |
| T-69-04-03 | Tampering | an unparseable v5 SUBSCRIBE relayed uninspected, so the first topic Block rule silently will not apply to it (WR-04) | medium | mitigate | Task 2's property-agnostic parser plus inspector plus the third `AllowMQTTControl` branch; parity asserted on the matching rule NAME, not on "not Blocked" |
| T-69-04-04 | Denial of Service | an unparseable v5 CONNECT dropped with no CONNACK, so the client hot-retries against a mute socket — the 0x84 retry loop reintroduced one layer down (WR-02) | high | mitigate | Task 3 answers the malformed-packet reason code on every CONNECT failure branch and records `answered=` in the log line |
| T-69-04-05 | Denial of Service | the new SUBSCRIBE fail-closed path tearing down a live session where 68-06 deliberately relayed (retires T-68-06-05) | medium | accept | only a frame whose own length prefixes contradict its bytes reaches it — mosquitto refuses those too — and the client now receives a reason code; 69-06 asserts the counter is zero on real client ids in production |
| T-69-04-06 | Information Disclosure | answering an unauthenticated client a specific reason code reveals proxy internals | low | accept | the malformed-packet code is the MQTT 5.0 answer for exactly this condition and discloses nothing about credentials or topology |
| T-69-04-07 | Tampering | adding a fourth `RawPacket` member or a third `AllowMQTTControl` branch perturbs the 3.1.1 decision sequence | high | mitigate | the 3.1.1 branch stays first and unedited; `TestAllowMQTTControlV4Unchanged` plus the byte-and-decision golden are both re-run and must pass with the golden file unedited |
| T-69-04-08 | Tampering | a synthesized `v5.Subscribe` making `Raw.MQTT5` lie about provenance, enabling a mutation that never reaches the wire (meshtk#22 class) | medium | mitigate | rejected by design: the hand-parsed SUBSCRIBE gets its own `RawPacket` field, and the rejected alternative is recorded in a comment |
| T-69-04-SC | Tampering | dependency substitution via a package-manager install | high | mitigate | nothing is installed; `git diff --stat origin/main -- go.mod go.sum vendor/` must print nothing |
</threat_model>

<artifacts_this_phase_produces>
- `/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe.go` (new file: property-agnostic SUBSCRIBE parser + hand-parsed inspector)
- `/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_rawsubscribe_test.go` (new file)
- `/Users/khundeck/working/meshtk/internal/app/server/proxy_v5_connect_fail_test.go` (new file)
- a topic-alias scan function and two new `v5RawPublish` fields (alias found, walk complete)
- a `v5RawSubscribe` view type, a new fourth `RawPacket` field for it, and a third `AllowMQTTControl` branch
- `action=MQTT5_ALIAS_SCAN_INDETERMINATE` and `action=MQTT5_SUBSCRIBE_HEADER_FAIL` (new log action strings)
- an `answered=` field added to the existing `action=MQTT5_PARSE_FAIL` CONNECT lines
- the renamed replacement for `TestV5PublishParseFailureForwardsRaw` (assertions unchanged)
</artifacts_this_phase_produces>

<verification>
- `go build ./...`, `go vet ./internal/app/server/`, `go test ./internal/app/server/ -count=1` and `go test -race ./internal/app/server/ -count=1` all exit 0 in `/Users/khundeck/working/meshtk`.
- Pre-existing test files `proxy_v5_test.go`, `proxy_v5_parity_test.go`, `proxy_v5_rawpublish_test.go` (assertions), `proxy_v4_golden_test.go` and the e2e file keep every existing assertion — read `git diff origin/main -- internal/app/server/*_test.go` and confirm removals are limited to the renamed test's old name, corrected comments and additive cases.
- `MESHTK_E2E=1 go test ./internal/app/server/ -run TestE2EDualCodec -count=1` passes against a local mosquitto if one is available; it SKIPs cleanly if not (it is env-gated), and a SKIP is not a pass — record which happened.
- `go.mod` / `go.sum` / `vendor/` diff against `origin/main` is empty.
- The gate is `./internal/app/server/`; `internal/credcache`'s singleflight flake is pre-existing and out of scope.
</verification>

<success_criteria>
- The hand-parsed PUBLISH path and the codec path Block the same frames for the same stated reasons, and the parity comment is true.
- Every v5 SUBSCRIBE reaches the decider with its filters recorded, matched by the same rule by name as a 3.1.1 SUBSCRIBE.
- No v5 CONNECT failure ends in a silent close.
- No test name still describes the retired fail-open posture.
- The 3.1.1 golden is unedited and green; no dependency changed.
- Work is committed on the phase branch; nothing pushed, PR'd or vendor-synced by this plan.
</success_criteria>

<output>
Create `.planning/phases/69-meshtk-shared-chain-hardening-pre-existing-blockers-found-by/69-04-SUMMARY.md` when done.
Record: commit shas, the exact new log action strings and the `answered=` field format as shipped
(69-06 greps for them in production), which property ids the alias walk models, the retired
accepted risk T-68-06-05, and whether the e2e ran or skipped.
</output>
