---
phase: 69-meshtk-shared-chain-hardening-pre-existing-blockers-found-by
plan: 05
type: execute
wave: 5
depends_on: ["69-04"]
files_modified:
  - apps/run.mqtt/meshtk/internal/app/server/inspect.go
  - apps/run.mqtt/meshtk/internal/app/server/inspect_v5.go
  - apps/run.mqtt/meshtk/internal/app/server/rules.go
  - apps/run.mqtt/meshtk/internal/app/server/proxy.go
  - apps/run.mqtt/meshtk/internal/app/server/proxy_v5.go
  - apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawpublish.go
  - apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawsubscribe.go
  - apps/run.mqtt/meshtk/internal/app/server/logsafe.go
  - apps/run.mqtt/meshtk/internal/app/server/cmd.go
autonomous: false
requirements: [MQFX-05]
must_haves:
  truths:
    - "The four hardening plans are merged to whereiskurt/meshtk main and the merge state passes build, vet, the server-package tests and the live-mosquitto e2e"
    - "The monorepo overlay is byte-identical to the upstream merge commit for every tracked file that exists in both, with only the two known-divergent files excepted"
    - "internal/embedded/gpx/embedded.go is byte-unchanged by the vendor-sync, so the ghost-fleet GPX regression class (#1009) is not reintroduced"
    - "The CI overlay composition (fresh upstream clone plus the tracked overlay untarred on top) compiles locally before anything is merged"
    - "The monorepo vendor-sync PR is merged only after explicit human approval"
  artifacts:
    - "whereiskurt/meshtk PR and its merge commit sha"
    - "monorepo vendor-sync PR and its merge commit sha"
    - apps/run.mqtt/meshtk/internal/app/server/logsafe.go
    - apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawsubscribe.go
  key_links:
    - "upstream main merge sha -> per-file sha256 parity of the monorepo overlay (git show <sha>:<path>, never a working tree)"
    - "apps/build.sh resolve_meshtk -> fresh upstream clone + git ls-files overlay tar -> the image tree the release builds"
    - "monorepo origin/main -> buildpub VERSION bump -> immutable ECR tag (plan 69-06)"
---

<objective>
Land the four hardening plans upstream and vendor-sync them into the monorepo overlay — the
first half of MQFX-05. No release and no deploy happen here; that is 69-06.

The upstream repository has no CI, so the four local gates (`go build ./...`,
`go vet ./internal/app/server/`, `go test ./internal/app/server/`, and the env-gated
live-mosquitto e2e) ARE the entire gate. They must be run from the branch tip AND re-run from
the merged state, because "green before the merge" and "green after the merge" are different
claims.

Purpose: get the fixes into the tree the release actually builds from, without reintroducing
either of the two regressions this repo has already shipped twice — a stale-overlay sync that
reverts upstream work, and an overlay sync that clobbers the monorepo-authoritative GPX embed.
Output: an upstream merge commit, a byte-parity-verified monorepo overlay, and a merged
vendor-sync PR.

**Landmines this plan must actively avoid.**
1. The `release/2026-07-26-230957` checkout carries a stale v0.0.66 overlay with **no v5 files
   at all**. Reading or syncing from it produces a silent revert of meshtk#22/#23/#27. Work from
   `origin/main`.
2. `vendor/` is git-tracked in BOTH repos and `Dockerfile.meshtk` does `COPY . .`, so a `go.mod`
   change without a matching `vendor/` update fails the image build with `inconsistent
   vendoring`. This phase adds zero dependencies, so the correct state is "no change at all" —
   assert it rather than assume it.
3. `internal/embedded/gpx/embedded.go` is **monorepo-authoritative** and upstream's version
   embeds only `example/*.gpx`. Overwriting it is exactly the #1009 regression that stranded all
   24 GPX-driven sim nodes, twice. Never sync it; prove it unchanged by hash.
4. `VERSION` is monorepo-only and buildpub owns the bump. Do not hand-edit it.
5. In this checkout the `main` branch is pinned by another worktree; `local-main-track` is the
   local tracking branch for `origin/main`. Branch from `origin/main` explicitly rather than
   assuming `main` is checked out or current.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-08-SUMMARY.md

`68-08-SUMMARY.md` is the pattern this plan reuses: the per-file sha256 parity sweep against
upstream merge-sha BLOBS (`git show <sha>:<path>`, never a working tree), the local reproduction
of the CI overlay composition, and the `embedded.go` hash check.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open, gate and merge the upstream meshtk PR</name>
  <files>/Users/khundeck/working/meshtk (branch, PR and merge only — no source edits)</files>
  <read_first>
    - .planning/phases/69-meshtk-shared-chain-hardening-pre-existing-blockers-found-by/69-01-SUMMARY.md, 69-02-SUMMARY.md, 69-03-SUMMARY.md, 69-04-SUMMARY.md — the commit shas, the exact new log action strings and any deviations recorded by the four hardening plans. The PR body must describe what actually shipped, not what was planned.
    - .planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-08-SUMMARY.md — the four-gate discipline and the "re-run green FROM the merged main" requirement.
    - AGENTS.md — the branch-and-PR workflow rules.
  </read_first>
  <action>
    In `/Users/khundeck/working/meshtk`, confirm the phase branch is based on `origin/main` and
    that `origin/main` has not moved since 69-01 branched (if it has, rebase and re-run the
    gates before anything else). Confirm the working tree is clean and that every plan's commits
    are present.

    Run the full gate from the branch tip and record the exact output of each: `go build ./...`,
    `go vet ./internal/app/server/`, `go test ./internal/app/server/ -count=1`,
    `go test -race ./internal/app/server/ -count=1`, and the env-gated live-mosquitto e2e with
    `MESHTK_E2E=1` (it SKIPs without a broker — a SKIP is not a pass, so if no mosquitto is
    available say so explicitly in the summary rather than claiming coverage). Also confirm
    `git diff --stat origin/main -- go.mod go.sum vendor/` prints nothing and
    `git diff --stat origin/main -- internal/app/server/proxy_v4_golden_test.go` prints nothing.

    Push the branch and open the PR against `whereiskurt/meshtk` main with `gh pr create`. The
    body must enumerate, one section per defect: the review finding it closes (CR-01, CR-02,
    CR-03 and the four MQFX-04 warnings, using the `68-REVIEW.md` ids so the mapping is
    checkable), what changed, and the new log action strings a production operator will start
    seeing. Explicitly state that the 3.1.1 path is unchanged and that
    `proxy_v4_golden_test.go` has an empty diff.

    Merge the PR. Then re-run all four gates from the merged `main` with a clean tree and record
    the merge commit sha — that sha is the parity target for Task 2 and the state
    `apps/build.sh` will clone in CI.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/meshtk && git fetch origin && git rev-parse origin/main && git status --porcelain && go build ./... && go vet ./internal/app/server/ && go test ./internal/app/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/khundeck/working/meshtk && gh pr list --state merged --limit 3` shows the phase PR as merged.
    - After the merge, `cd /Users/khundeck/working/meshtk && git checkout main && git pull && git status --porcelain` prints nothing (clean tree at the merge sha).
    - From the merged main: `go build ./...`, `go vet ./internal/app/server/`, `go test ./internal/app/server/ -count=1` and `go test -race ./internal/app/server/ -count=1` all exit 0.
    - `cd /Users/khundeck/working/meshtk && git show --stat HEAD -- internal/embedded/` prints no file (the upstream PR touched nothing under `internal/embedded/`).
    - `cd /Users/khundeck/working/meshtk && git diff --stat <pre-merge-main-sha>..HEAD -- go.mod go.sum vendor/` prints nothing.
    - `cd /Users/khundeck/working/meshtk && git log --follow --oneline internal/app/server/proxy_v4_golden_test.go | wc -l` still reports 1 (the golden was authored once and never adjusted).
    - The merge commit sha is recorded verbatim in the summary.
  </acceptance_criteria>
  <done>The phase PR is merged to upstream main, all four gates are green from the merged state with a clean tree, `internal/embedded/` and the dependency files are untouched, and the merge sha is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor-sync the overlay and prove byte parity plus an untouched GPX embed</name>
  <files>apps/run.mqtt/meshtk/internal/app/server/inspect.go, apps/run.mqtt/meshtk/internal/app/server/inspect_v5.go, apps/run.mqtt/meshtk/internal/app/server/rules.go, apps/run.mqtt/meshtk/internal/app/server/proxy.go, apps/run.mqtt/meshtk/internal/app/server/proxy_v5.go, apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawpublish.go, apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawsubscribe.go, apps/run.mqtt/meshtk/internal/app/server/logsafe.go, apps/run.mqtt/meshtk/internal/app/server/cmd.go</files>
  <read_first>
    - apps/build.sh — read `resolve_meshtk` in full. In CI it snapshots every repo-tracked file under `apps/run.mqtt/meshtk` with `git ls-files`, tars it, deletes the directory, does a shallow clone of `whereiskurt/meshtk`, then untars the overlay on top. That composition — clone first, overlay second — is why the overlay's copy of a file always wins, and why `embedded.go` must stay monorepo-authoritative.
    - apps/run.mqtt/meshtk/VERSION — currently `v0.0.74`; monorepo-only, buildpub owns the bump. Do not edit.
    - apps/run.mqtt/meshtk/internal/embedded/gpx/embedded.go — the monorepo-authoritative GPX embed. Capture its sha256 BEFORE any file is copied.
    - apps/run.mqtt/meshtk/Dockerfile.meshtk — the MQFX-06 build-time assertion (every `GPXFile:` in the shipped config must resolve under `/app`) and the `COPY . .` that makes a `go.mod`/`vendor` mismatch a build failure. Monorepo-only; the sync must not remove it.
    - .planning/phases/68-mqtt-v5-support-in-meshtk-proxy-dual-codec-android-2-8-compa/68-08-SUMMARY.md — the parity-sweep method (compare against `git show <merge-sha>:<path>` blobs, never a working tree) and the local CI-composition reproduction.
  </read_first>
  <action>
    Create a monorepo feature branch from `origin/main` (not from whatever is checked out; this
    checkout's main tracking branch is `local-main-track` because `main` is pinned by another
    worktree). Never branch from `release/2026-07-26-230957`.

    Record the sha256 of `apps/run.mqtt/meshtk/internal/embedded/gpx/embedded.go` before
    touching anything.

    Copy from the upstream MERGE COMMIT — extract blobs with `git show <merge-sha>:<path>`, not
    from the upstream working tree, so a stray local edit upstream cannot leak in. Copy only the
    files this phase changed or added, as recorded in the four plan summaries; the rest of the
    overlay is already at parity and must not be rewritten. The new files include the sanitizer
    and the raw-SUBSCRIBE seam.

    Then run a per-file parity sweep over EVERY tracked overlay file that also exists in the
    upstream merge commit: compare sha256 of the monorepo file against sha256 of
    `git show <merge-sha>:<path>`. Exactly two mismatches are permitted and both must be named
    in the output: `VERSION` (monorepo-only; upstream has no such file) and
    `internal/embedded/gpx/embedded.go` (monorepo-authoritative by design). Any third mismatch
    is a defect — stop and report rather than "fixing" it by copying.

    Re-check the `embedded.go` sha256 against the pre-sync value and assert equality. Assert
    `embedded.go` is absent from the branch's diffstat.

    Reproduce the CI overlay composition locally into a scratch directory: `git archive` the
    upstream merge sha, untar the monorepo overlay (`git ls-files` on the overlay path, same as
    `resolve_meshtk`) on top, then run `go build ./...` in the composed tree and confirm exit 0
    and that the composed `embedded.go` is the monorepo copy by hash. This is the release gate;
    do it BEFORE opening the PR, because it is what catches an `inconsistent vendoring` failure
    while it is still cheap.

    Open the monorepo PR with `gh pr create`. Body: the upstream merge sha, the defect-to-change
    mapping, the parity-sweep result (file count, the two expected mismatches), the
    `embedded.go` hash before and after, and the composed-tree build result. State that `VERSION`
    is deliberately untouched so buildpub owns the bump.

    Do NOT merge in this task.
  </action>
  <verify>
    <automated>cd /Users/khundeck/working/defcon.run.34 && git diff --name-only origin/main -- apps/run.mqtt/meshtk/internal/embedded/ apps/run.mqtt/meshtk/VERSION apps/run.mqtt/meshtk/go.mod apps/run.mqtt/meshtk/go.sum apps/run.mqtt/meshtk/vendor/ | wc -l | tr -d ' ' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only origin/main -- apps/run.mqtt/meshtk/internal/embedded/` prints nothing, and the sha256 of `apps/run.mqtt/meshtk/internal/embedded/gpx/embedded.go` equals the value captured before the sync.
    - `git diff --name-only origin/main -- apps/run.mqtt/meshtk/VERSION apps/run.mqtt/meshtk/go.mod apps/run.mqtt/meshtk/go.sum apps/run.mqtt/meshtk/vendor/` prints nothing.
    - The parity sweep reports exactly two mismatches and names them as `VERSION` and `internal/embedded/gpx/embedded.go`; its per-file comparison used `git show <merge-sha>:<path>` and not an upstream working tree — evidenced by the recorded command.
    - `git merge-base --is-ancestor <branch-base-sha> origin/main` exits 0, and `git merge-base --is-ancestor release/2026-07-26-230957 HEAD` exits NON-zero (the stale overlay branch is not in this branch's history).
    - The locally composed tree (`git archive <upstream-merge-sha>` plus the `git ls-files` overlay untarred on top) runs `go build ./...` to exit 0, and the composed `internal/embedded/gpx/embedded.go` sha256 equals the monorepo copy's.
    - `apps/run.mqtt/meshtk/Dockerfile.meshtk` still contains its `GPXFile:` build-time assertion: `grep -c 'GPXFile:' apps/run.mqtt/meshtk/Dockerfile.meshtk` returns at least 1.
    - `gh pr view --json number,title,body` shows an open PR whose body contains the upstream merge sha, the parity file count and the `embedded.go` before/after hashes.
    - The PR is NOT merged at the end of this task.
  </acceptance_criteria>
  <done>The overlay carries this phase's upstream changes, is byte-parity-verified against the upstream merge commit with exactly the two known exceptions, leaves the GPX embed and the dependency files untouched, composes and builds the way CI will, and is open as a PR awaiting approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner approval before merging the vendor-sync PR</name>
  <what-built>
    The four hardening plans are merged upstream at a recorded merge sha, and the monorepo
    overlay has been vendor-synced from that sha on a feature branch with an open PR. Byte parity
    over the overlay is verified, `internal/embedded/gpx/embedded.go` is proven unchanged by hash,
    `go.mod`/`go.sum`/`vendor/`/`VERSION` are untouched, and the CI overlay composition has been
    reproduced locally and builds.
  </what-built>
  <how-to-verify>
    1. Open the monorepo PR and read the body: it must state the upstream merge sha, the
       defect-to-change mapping (CR-01, CR-02, CR-03 and the four MQFX-04 warnings), the parity
       sweep result with exactly two named mismatches, and the `embedded.go` hash before and after.
    2. Review the diff: it should be confined to `apps/run.mqtt/meshtk/internal/app/server/`.
       Nothing under `internal/embedded/`, no `VERSION` change, no `go.mod` / `go.sum` / `vendor/`
       change.
    3. Confirm the new production log strings you will start seeing are acceptable:
       `action=WILL_STRIPPED` (both codecs), `action=PANIC_RECOVERED`,
       `action=MQTT5_SUBSCRIBE_HEADER_FAIL`, `action=MQTT5_ALIAS_SCAN_INDETERMINATE`, and an
       `answered=` field on the existing `action=MQTT5_PARSE_FAIL` CONNECT lines.
    4. Confirm the behavior changes that will affect live clients: a CONNECT carrying a Will loses
       its Will; a malformed v5 SUBSCRIBE now ends the connection with a reason code instead of
       being relayed; a v5 CONNECT the codec cannot parse now gets a CONNACK instead of a silent
       close.
    5. On approval, the executor merges the PR. Merging is what unblocks 69-06's release and
       deploy — nothing is released by approving alone.
  </how-to-verify>
  <resume-signal>Type "approved" to merge the vendor-sync PR, or describe what must change first.</resume-signal>
  <files>(no source edits — a merge decision on the open monorepo vendor-sync PR)</files>
  <action>
    Present the open monorepo PR to the owner with the evidence Task 2 produced: the upstream
    merge sha, the defect-to-change mapping, the parity sweep result with its two named
    mismatches, the `embedded.go` sha256 before and after, and the composed-tree build result.
    Then present the two lists in `how-to-verify` — the new production log strings and the
    behavior changes that will affect live clients — and STOP. Do not merge, do not release, do
    not deploy until the owner replies. On an explicit approval, merge the PR with `gh pr merge`
    and record the merge commit sha; on anything else, record what must change and stop the plan.
  </action>
  <verify>
    <human-check>The owner has read the PR body and the two lists above and has replied with an explicit approval or with required changes.</human-check>
  </verify>
  <acceptance_criteria>
    - The owner's reply is recorded verbatim in the summary.
    - On approval: `gh pr view --json state,mergedAt` reports the vendor-sync PR merged, and the merge commit sha is recorded and is an ancestor of `origin/main`.
    - On anything other than approval: the PR remains open, nothing was merged, and the required changes are recorded.
    - No release or deploy was triggered by this task.
  </acceptance_criteria>
  <done>The owner has explicitly approved and the vendor-sync PR is merged with its sha recorded, or the PR remains open with the requested changes recorded and the plan stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream `whereiskurt/meshtk` main → the monorepo overlay | the release clones upstream and layers the overlay, so upstream is code the image runs even though it lives in a different repository with no CI |
| monorepo overlay → the built image tree | `resolve_meshtk` decides which copy of a file wins; an overlay file silently shadows its upstream counterpart |
| local working tree → what actually gets synced | copying from a working tree instead of a commit blob can smuggle in uncommitted local edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-05-01 | Tampering | vendor-sync clobbering `internal/embedded/gpx/embedded.go`, reverting it to upstream's `example/*.gpx`-only embed and silently stranding all 24 GPX-driven sim nodes (#1009, shipped twice) | high | mitigate | sha256 captured before the sync and asserted equal after; `internal/embedded/` asserted absent from the branch diffstat; the composed CI tree's copy re-hashed |
| T-69-05-02 | Tampering | syncing from the stale `release/2026-07-26-230957` overlay (v0.0.66, no v5 files), silently reverting meshtk#22/#23/#27 | high | mitigate | branch explicitly from `origin/main`; assert the branch base is an ancestor of `origin/main` and that the stale branch is NOT an ancestor of HEAD |
| T-69-05-03 | Tampering | overlay drifting from the upstream merge state, so the image runs code nobody reviewed | high | mitigate | per-file sha256 parity over every tracked overlay file against `git show <merge-sha>:<path>` blobs; exactly two named exceptions; a third mismatch stops the plan |
| T-69-05-04 | Tampering | uncommitted upstream working-tree edits leaking into the overlay | medium | mitigate | files are extracted from the merge commit's blobs, never copied from the upstream working directory; upstream tree asserted clean at the merge sha |
| T-69-05-05 | Denial of Service | a `go.mod` change without a matching `vendor/` update failing the image build with `inconsistent vendoring` after the release has started | medium | mitigate | zero dependency change asserted by diff on `go.mod`/`go.sum`/`vendor/`; the CI overlay composition is reproduced locally and built BEFORE the PR opens |
| T-69-05-06 | Elevation of Privilege | merging a change to production infrastructure without owner review | high | mitigate | Task 3 is a blocking human checkpoint; the vendor-sync PR is merged only on explicit approval, per the repository's branch-workflow rule |
| T-69-05-07 | Tampering | hand-editing `VERSION` and colliding with an immutable ECR tag | medium | mitigate | `VERSION` asserted unchanged; buildpub owns the bump in 69-06 |
| T-69-05-SC | Tampering | dependency substitution during the sync | high | mitigate | no package-manager install runs; `go.mod`, `go.sum` and `vendor/` are asserted byte-unchanged in both repositories |
</threat_model>

<artifacts_this_phase_produces>
- an upstream `whereiskurt/meshtk` PR and its merge commit (new)
- a monorepo vendor-sync PR and its merge commit (new)
- `apps/run.mqtt/meshtk/internal/app/server/logsafe.go` (new in the overlay)
- `apps/run.mqtt/meshtk/internal/app/server/proxy_v5_rawsubscribe.go` (new in the overlay)
- the overlay copies of the new test files created by 69-01 through 69-04
</artifacts_this_phase_produces>

<verification>
- Upstream: PR merged; `go build ./...`, `go vet ./internal/app/server/`, `go test ./internal/app/server/ -count=1` and `-race` all green FROM the merged main with a clean tree; the golden file's history is still a single commit.
- Overlay parity: exactly two mismatches (`VERSION`, `internal/embedded/gpx/embedded.go`) across every tracked overlay file compared against upstream merge-sha blobs.
- `internal/embedded/gpx/embedded.go` sha256 identical before and after, and identical in the locally composed CI tree.
- `go.mod`, `go.sum`, `vendor/` and `VERSION` all show an empty diff against `origin/main`.
- Locally composed CI tree builds (`go build ./...` exit 0).
- `Dockerfile.meshtk` still carries the MQFX-06 `GPXFile:` build-time assertion.
- The monorepo PR is merged only after the blocking checkpoint returns approval.
</verification>

<success_criteria>
- The hardening work is on upstream main and in the monorepo overlay, byte-parity-verified.
- The GPX embed and the dependency set are provably untouched.
- The tree CI will build has been composed and compiled locally before the merge.
- The merge happened with explicit owner approval.
- Nothing has been released or deployed by this plan.
</success_criteria>

<output>
Create `.planning/phases/69-meshtk-shared-chain-hardening-pre-existing-blockers-found-by/69-05-SUMMARY.md` when done.
Record: the upstream PR number and merge sha, the monorepo PR number and merge sha, the parity
sweep's file count and the two named mismatches, the `embedded.go` sha256 before and after, the
composed-tree build result, whether the live-mosquitto e2e ran or skipped, and the checkpoint
approval.
</output>
