---
phase: 71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio
plan: 12
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs
  - .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-pre.txt
autonomous: true
requirements: [HEAT-06]

must_haves:
  truths:
    - "Assertion 8 can now tell 'unreachable' from 'reached and rejected' — it requires the edge block's marker header, which the application cannot produce."
    - "Assertions 1 and 2 can now see that the CDN ignores s-maxage — they require a cache HIT on a repeat request, not merely the presence of a cache-control header."
    - "A new assertion proves the gpx internal path family is blocked at the edge across all three of its routes and in the no-region form."
    - "A new assertion proves the edge block did NOT catch the internal paths that legitimately travel over public HTTPS — meshtk's run.human claim-link mint and run.auth's quota family."
    - "A new assertion fails if any live DC33 feature is degenerate."
    - "A new assertion fails if the two DC34 schedules can fire in the same minute."
    - "A new assertion reads the live DC33 layer's line-opacity and requires the D-13 value."
    - "TOTAL is a fixed literal of 19 and the file contains no skip helper — every new assertion fails closed."
  artifacts:
    - "71-08-probes/heatmap-probe.cjs (extended, not rewritten)"
    - "71-08-probes/transcript-gap-pre.txt"
  key_links:
    - "assertion 8 -> the x-dc34-edge-block marker header emitted by the CloudFront function plan 71-13 adds"
    - "assertion 16 -> run.mqtt's MESHTK_RUN_INTERNAL_URL public-HTTPS call path (the blast-radius regression gate)"
  prohibitions:
    - "NO renumbering, deletion or weakening of the existing thirteen assertions. Their labels and predicates may only become STRICTER."
    - "NO skip() / skipped / vacuous-pass helper. A probe that cannot read a thing must score FAIL for that thing."
    - "NO synthetic or injected con-day data to turn assertions 5 or 11 green. Both are calendar-bound (0 of 133 rows carry a conDay; the con is 2026-08-05..10) and must stay red until real data exists."
    - "NO softening of assertion 5 by passing a permissive flag to verify-heatmap-artifact.mjs."

---

<objective>
The 13-assertion probe scored 11/13 post-deploy and was cited as the ship gate — but it
could not see either of the two structural failures the verifier later found by hand.
Assertion 8's "non-2xx" predicate is satisfied identically by an unreachable path and by
the application's own rejection, which is exactly how CR-01 hid. Assertions 1 and 2 check
that a cache-control header is PRESENT, which it is, and therefore could not see that the
distribution ignores it, which is exactly how CR-03 hid.

Extend the probe so both blind spots become visible, and add four gates for the fixes the
other plans land. One of the new assertions is a blast-radius regression gate: the edge
block in plan 71-13 must NOT catch the internal paths that legitimately travel over public
HTTPS, and the probe is where that gets proven rather than asserted.

Purpose: this file is written PRE-deploy so its failures are the contrast evidence. A
probe extended after the fix, against the fix, proves nothing.

Output: a 19-assertion probe and a pre-fix transcript in which every new assertion is red
for a reason that is written down.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-VERIFICATION.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten assertions 1, 2 and 8 so the two structural failures become visible</name>

  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs`
      in full (764 lines). Specifically: the header docstring (lines 1-40, which says
      "Thirteen assertions" and states the fail-closed discipline), `const TOTAL = 13`
      (line 48), the module constants block (lines 50-88), `httpGet`/`httpPost`
      (lines 115-143), assertions 1/2 (lines 298-317) and assertion 8 (lines 461-472).
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-VERIFICATION.md`
      — the gap entries for truths #6 and #24, which contain the reproduced curls and the
      exact evidence these assertions must be able to produce.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-post-deploy.txt`
      — the current 11/13 output, so the new transcript's shape is comparable.
  </read_first>

  <files>.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs</files>

  <action>
Extend in place. Do not rewrite the file, do not renumber, and do not relax any existing
predicate — the existing thirteen may only become stricter.

(a) Add a `httpGetRaw`-style helper (or extend `httpGet`) that also surfaces the response
headers for a repeat-request sequence. `httpGet` already returns headers, so this may be
purely a loop in the assertion body — prefer that over a new helper if it reads clearly.

(b) **Tighten assertions 1 and 2 (CR-03).** Keep every current sub-check: status 200, a
JSON content type, and a cache-control header containing an s-maxage directive. ADD a
CDN-behaviour sub-check: issue the same URL a small number of additional times in sequence
(4 total requests is enough), collect each response's `x-cache` header, and require that at
least one of the requests AFTER the first reports a cache hit — accept both the plain hit
and the refresh-hit spellings CloudFront emits, matched case-insensitively. Record every
observed `x-cache` value in the assertion's note so the transcript shows the sequence, not
just the verdict. Add a short comment above the assertion explaining that header presence
was the original predicate and that it could not distinguish a working cache from a
distribution that ignores the header outright — which is what shipped.

(c) **Tighten assertion 8 (CR-01).** Keep the existing non-2xx requirement. ADD two
sub-checks that together distinguish an edge block from an application rejection:
  - the response must carry a marker header emitted only by the CloudFront function plan
    71-13 adds. Define the header name once as a module constant near the other constants —
    use `x-dc34-edge-block` with the value `1`. The application cannot produce this header;
    that is the whole point.
  - the response body must NOT contain the application guard's error payload. Define the
    forbidden body token as a module constant so it appears exactly once in the file.
Update the assertion's label so it states what is actually being proven — that the request
is refused at the edge and never reaches the application — rather than merely that the
status is not a success.

(d) Update the header docstring: the count word, and a new paragraph recording that
assertions 1, 2 and 8 were STRENGTHENED after 71-VERIFICATION.md found that their original
predicates were satisfied by the failing deployment. Name both blind spots explicitly so
the next reader understands why the predicates are shaped the way they are.

(e) Change `TOTAL` from 13 to 19. Task 2 adds assertions 14-19; set the literal now so an
incomplete Task 2 scores red rather than passing a shrunken denominator.
  </action>

  <acceptance_criteria>
    - `grep -c "const TOTAL = 19" .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs` returns 1.
      (Can go red: it is 13 today.)
    - The edge marker constant exists and is used by assertion 8:
      `grep -c "x-dc34-edge-block" .../heatmap-probe.cjs` returns >= 2 (declaration + use).
      (Can go red: today the count is 0.)
    - Assertion 8 inspects the body, not just the status:
      read the assertion and confirm it compares the response body against the forbidden
      token constant. `grep -cE "FORBIDDEN_GUARD_BODY|forbiddenGuardBody" .../heatmap-probe.cjs` returns >= 2.
    - Assertions 1 and 2 inspect `x-cache`:
      `grep -c "x-cache" .../heatmap-probe.cjs` returns >= 1.
      (Can go red: today the count is 0 — that is exactly the blind spot.)
    - No skip helper was introduced:
      `grep -cE "function skip|skip\(|SKIP" .../heatmap-probe.cjs` returns 0.
    - The thirteen existing labels survive verbatim. Diff the file against its HEAD version
      and confirm no line matching `bad\(|pass\(` for assertions 1-13 had its predicate
      LOOSENED. Record the diff summary in the SUMMARY.
    - `node -c` equivalent syntax check passes: `node --check .../heatmap-probe.cjs` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>node --check .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs</automated>
  </verify>

  <done>TOTAL is 19; assertions 1, 2 and 8 carry the new sub-checks; the marker header and forbidden-body constants each appear once as a declaration; the docstring records why the three were strengthened; the file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add assertions 14-19, including the edge-block blast-radius regression gate</name>

  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs`
      — the assertion idiom (label constant, try/catch, `problems` array, `info()` lines,
      `pass`/`bad`), the `SCHEDULES` constant (lines 74-79) and assertion 13's
      `aws scheduler get-schedule` shell-out (lines 478-507), and assertion 11's
      `page.evaluate` block that reads `getPaintProperty` (lines 615-640).
    - `infra/terraform/live/site/services/run.mqtt/service.hcl` around the
      `MESHTK_RUN_INTERNAL_URL` environment variable — this is the proof that run.human's
      claim-link mint path is called over PUBLIC HTTPS and therefore must NOT be edge-blocked.
      Read the surrounding comment; it says so explicitly.
    - `apps/run.gpx/webapp/src/app/api/gpx/internal/` — the three route directories the edge
      block must cover (`reconcile`, `strava-sync`, `heatmap-build`).
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md`
      sections WR-04 (the schedule collision) and WR-06 (the degenerate features).
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-CONTEXT.md`
      **D-13** for the opacity value assertion 19 gates.
  </read_first>

  <files>.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs</files>

  <action>
Add six assertions, numbered 14-19, each following the file's existing idiom exactly: a
label constant, a `problems` array, `info()` lines carrying the raw observation, and a
single `pass`/`bad` at the end. Every one must fail closed — an unreadable input scores
FAIL, never a silent pass. Place the HTTP-only ones with the other HTTP assertions (before
the browser block) and the browser one inside the existing `try` that owns the page.

**14 — CDN cache-key separation.** Fetch the bare artifact URL and the `?meta=1` URL for
dc33, repeatedly enough to warm the cache, then require: both return 200; the meta response
is small (reuse the existing `META_MAX_BYTES` constant) while the bare response is large;
and the two bodies are NOT byte-identical. This proves the cache policy's query-string
whitelist actually separates the two entries — a cache policy that ignores query strings
would serve one for the other, which is a silent correctness bug the header checks cannot
see.

**15 — the gpx internal family is blocked at the edge.** For each of the three gpx internal
paths (`heatmap-build`, `strava-sync`, `reconcile`) issue an unauthenticated POST to the
region-prefixed public URL, and separately issue one to the no-region form (the same path
without the region segment — the ALB rule carries no path patterns and the default
behaviour forwards, so this form must be proven too). Every one must be non-2xx AND carry
the edge marker header. Report each path's status and marker presence on its own `info()`
line so a partial block is diagnosable.

**16 — BLAST-RADIUS REGRESSION: the block did not catch what must keep working.** This is
the most important new assertion. Issue a GET (not a POST — do not poke a mint endpoint) to
each of:
  - `https://run.defcon.run/use1/api/internal/ctf/mint` — run.mqtt's meshtk calls this over
    public HTTPS to mint ghost claim links; blocking it silently kills a con-critical CTF
    flow.
  - `https://auth.defcon.run/use1/api/internal/quota/probe-nonexistent-user` — the run.auth
    internal family.
For each, require that the edge marker header is ABSENT. The status may be anything
(405, 404, 403 are all fine — the routes are secret-gated and some export only POST); the
assertion is solely that the edge block did not reach them. Add a comment above this
assertion naming why it exists: a naive block on every internal path family across every
distribution would have broken these, and the plan deliberately scoped the block to the gpx
distribution and the gpx internal prefix.

**17 — no degenerate geometry in the live DC33 artifact.** Parse the bare dc33 artifact and
count features whose coordinates are all identical to the feature's first coordinate.
Require zero, and report the count and the first offending index. This is currently 20 of
110; it goes green only when plan 71-15 republishes the rebuilt artifact.

**18 — the two DC34 schedules cannot fire in the same minute.** Re-use assertion 13's
`aws scheduler get-schedule` shell-out (fail closed on any non-zero exit, as 13 does).
Parse the MINUTE field out of both cron expressions and require that they differ. The
hourly expression fires at minute 0 of every hour across the con days; a daily expression
whose minute field is also 0 therefore coincides with it once per con day, invoking the
Lambda twice with no concurrency control. Report both parsed minute fields.

**Also update assertion 13's `SCHEDULES` constant.** Assertion 13 pins the EXACT schedule
expressions, and plan 71-14 moves the daily schedule off the top of the hour to minute 20
(that is the WR-04 fix). Change the daily entry in the `SCHEDULES` constant from the
top-of-hour spelling to `cron(20 4 * * ? *)`, leaving the hourly entry untouched. This is a
STRENGTHENING, not a renumbering: assertion 13 now pins the de-collided schedule and goes
red until 71-14 applies. Assertion 18 is the durable companion — it pins the INVARIANT
(the two can never coincide) so a future expression change cannot silently reintroduce the
collision. Keep both; they are not redundant.

**19 — the live DC33 layer paints at the D-13 opacity.** Inside the existing browser block,
after the DC33 layer is built, read `line-opacity` from the map for the dc33 layer id and
require the D-13 value. Define the expected value as a module constant next to
`HEAT_COLOR`. Fail closed: an unresolvable map or an absent layer scores FAIL. Do NOT fold
this into assertion 11 — assertion 11 is calendar-bound red because dc34 has no features,
and a paint check hidden inside it would be masked by that redness.

Do not touch the browser setup, the route stubs, the session shape or the screenshot
helpers.
  </action>

  <acceptance_criteria>
    - Six new assertion numbers are present and each has exactly one terminal verdict call:
      `grep -cE "^\s*(pass|bad)\(1[4-9]," .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs` returns 12 (one pass + one bad per assertion).
      Verify the exact count by reading; the point is that each of 14-19 can both pass and fail.
    - Assertion 16 targets both hosts:
      `grep -c "run.defcon.run" .../heatmap-probe.cjs` returns >= 1 AND
      `grep -c "auth.defcon.run" .../heatmap-probe.cjs` returns >= 1.
      (Can go red: today the file only knows `gpx.defcon.run`.)
    - Assertion 16 asserts the marker is ABSENT, not present — read the code and confirm the
      predicate is a negation. This is the criterion that distinguishes a real regression gate
      from a copy of assertion 15.
    - Assertion 15 covers all three internal routes:
      `grep -c "strava-sync" .../heatmap-probe.cjs` returns >= 1 AND
      `grep -c "reconcile" .../heatmap-probe.cjs` returns >= 1.
    - Assertion 19's expected opacity is a named constant equal to the D-13 value:
      `grep -cE "0\.7" .../heatmap-probe.cjs` returns >= 1.
    - Assertion 13's daily expression was moved to the de-collided value:
      `grep -c "cron(20 4 \* \* ? \*)" .../heatmap-probe.cjs` returns 1 AND
      `grep -c "cron(0 4 \* \* ? \*)" .../heatmap-probe.cjs` returns 0.
      (Can go red: the top-of-hour spelling is what ships today, and leaving it would make
      assertion 13 fail permanently after 71-14 applies.)
    - The hourly expression is UNCHANGED:
      `grep -c "cron(0 \* 5-10 8 ? 2026)" .../heatmap-probe.cjs` returns 1.
    - No skip helper: `grep -cE "function skip|SKIP" .../heatmap-probe.cjs` returns 0.
    - `node --check .../heatmap-probe.cjs` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>node --check .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs</automated>
  </verify>

  <done>Assertions 14-19 exist, each fails closed, assertion 16 asserts marker ABSENCE on two non-gpx hosts, assertion 19 is independent of the calendar-bound assertion 11, and the file parses.</done>
</task>

<task type="auto">
  <name>Task 3: Run the extended probe PRE-fix and record the contrast transcript</name>

  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs`
      header, lines 25-39 — the SSM invocation with the decryption flag (load-bearing: without
      it the value is KMS ciphertext, mapbox never fires its load event, and every DOM
      assertion times out) and the `PROBE_NOTES` header-only escape hatch.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-pre-deploy.txt`
      — the format of the previous contrast transcript.
  </read_first>

  <files>.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-pre.txt</files>

  <action>
Run the extended probe against production BEFORE any fix from plans 71-09..71-15 has been
deployed, and save the output to `transcript-gap-pre.txt` in the probes directory.

Read the mapbox public token from SSM with the decryption flag exactly as the file header
documents, and set `PROBE_NOTES` to record: that this is the gap-closure PRE run, the live
version at the time (currently run.gpx v0.0.109), and the git sha. The token is never
printed and never written to disk.

The run WILL score well below 19. That is the evidence. In the SUMMARY, list every red
assertion with a one-line reason, and state explicitly which reds are expected to go green
from which plan:
  - assertions 1, 2 red -> plan 71-13 (CloudFront cache behaviour)
  - assertion 8, 15 red -> plan 71-13 (edge block) and plan 71-11 (application 404)
  - assertion 14 red or green depending on current behaviour -> record which and why
  - assertion 13 red -> plan 71-14 (schedule de-collision; its expected daily expression was
    moved forward in Task 2 and does not match production until 71-14 applies)
  - assertion 17 red -> plan 71-15 (DC33 rebuild)
  - assertion 18 red -> plan 71-14 (schedule de-collision)
  - assertion 19 red -> plan 71-09 (D-13 opacity)
  - assertion 16 EXPECTED GREEN NOW and must STAY green -> it is a regression gate, not a
    fix gate. If it is red on this run, something is already broken and that is a finding.
  - assertions 5 (dc34 leg) and 11 red -> calendar-bound, not addressable before 5 Aug 2026.

Do NOT modify the probe after seeing this run. The byte-identical-script property is what
makes the pre/post contrast evidence rather than decoration.
  </action>

  <acceptance_criteria>
    - `transcript-gap-pre.txt` exists in the probes directory, contains a `RESULT: N/19` line,
      and N is strictly less than 19.
      (Can go red: a pre-run that already scores 19/19 would mean the new assertions are not
      testing anything — that is a hard failure of this task, not a success.)
    - Assertion 16 is PASS in this transcript. (Can go red: if it is not, an existing
      production path is already broken and the plan set must stop and report.)
    - Assertions 1, 2, 8, 13, 15, 17, 18 and 19 are all FAIL in this transcript. Each must be
      red for the reason recorded above; a green one means that assertion is not testing
      what it claims.
    - The transcript contains no string matching `^pk\.` — the mapbox token was never printed.
      `grep -c "pk\." transcript-gap-pre.txt` returns 0.
    - The transcript's `git sha` header line is present and non-`unknown`.
  </acceptance_criteria>

  <verify>
    <automated>cd .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes && grep -q "RESULT: " transcript-gap-pre.txt && test "$(grep -c 'pk\.' transcript-gap-pre.txt)" = "0" && echo PRE-TRANSCRIPT-OK</automated>
  </verify>

  <done>`transcript-gap-pre.txt` is committed, scores below 19/19, shows assertion 16 green and assertions 1/2/8/15/17/18/19 red, and leaks no token. The probe file is unchanged after the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe process -> SSM | Reads a decrypted public mapbox token into process memory |
| probe artifacts -> git | Transcripts and screenshots are committed to the repo |
| probe -> production endpoints | Issues unauthenticated requests to three live hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-12-01 | Information disclosure | Mapbox token leaking into a committed transcript | medium | mitigate | The token is read from env, never printed and never written; pinned by an acceptance grep on the transcript for the public-token prefix. |
| T-71-12-02 | Tampering | A probe softened after the fact so the ship gate becomes reachable | high | mitigate | `TOTAL` is a fixed literal set to 19 in Task 1, before the new assertions exist; the pre-run transcript is committed unchanged; the post-run in plan 71-16 uses the byte-identical script. |
| T-71-12-03 | Denial of service | The probe POSTs to live internal endpoints | low | accept | Every such request is unauthenticated and rejected; assertion 16 deliberately uses GET so no mint endpoint is exercised. Volume is a handful of requests. |
| T-71-12-04 | Spoofing | Assertion 8 accepting an application rejection as an edge block | high | mitigate | The marker header is emitted only by the CloudFront function; the application cannot produce it. Assertion 8 additionally requires the guard's body token to be absent. |
</threat_model>

<verification>
1. `node --check` on the probe exits 0.
2. `git diff` on the probe shows only additions and strengthenings — no existing assertion's
   predicate was loosened, no assertion was renumbered, no skip helper introduced.
3. `transcript-gap-pre.txt` exists, scores below 19, and its red set matches the mapping
   recorded in Task 3.
4. Assertion 16 is GREEN pre-fix. If it is red, STOP and report — an already-broken
   production path is a finding that outranks this plan set.
</verification>

<success_criteria>
- Assertions 1, 2 and 8 can each go red for the exact failure that previously hid from them.
- Six new assertions exist, all fail closed, covering the cache key, the edge block, the
  edge block's blast radius, degenerate geometry, the schedule collision, and the D-13 paint.
- `TOTAL` is a fixed literal of 19; no skip helper exists.
- A committed pre-fix transcript records the contrast, with every red mapped to the plan
  that will turn it green.
- Assertions 5 and 11 remain red and untouched — calendar-bound, not faked.
</success_criteria>

<output>
Create `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-12-SUMMARY.md` when done.

Record in the SUMMARY the exact pre-fix score and the red/green map — plan 71-16 compares
its post-deploy run against it.

## Artifacts this phase produces (this plan)

| Kind | Name | File |
|------|------|------|
| new constant | `x-dc34-edge-block` marker header name + value | `71-08-probes/heatmap-probe.cjs` |
| new constant | forbidden application-guard body token | `71-08-probes/heatmap-probe.cjs` |
| new constant | expected D-13 line-opacity | `71-08-probes/heatmap-probe.cjs` |
| changed literal | `TOTAL` 13 -> 19 | `71-08-probes/heatmap-probe.cjs` |
| strengthened assertions | 1, 2 (x-cache hit), 8 (edge marker + body) | `71-08-probes/heatmap-probe.cjs` |
| new assertions | 14 cache-key separation, 15 gpx internal family blocked, 16 blast-radius regression, 17 no degenerate features, 18 schedules disjoint, 19 D-13 opacity | `71-08-probes/heatmap-probe.cjs` |
| new transcript | pre-fix contrast run | `71-08-probes/transcript-gap-pre.txt` |
</output>
