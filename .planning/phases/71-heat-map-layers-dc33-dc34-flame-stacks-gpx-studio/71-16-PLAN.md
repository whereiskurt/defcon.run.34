---
phase: 71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio
plan: 16
type: execute
wave: 4
gap_closure: true
depends_on: ["71-09", "71-10", "71-11", "71-12", "71-13", "71-14", "71-15"]
files_modified:
  - apps/run.gpx/webapp/VERSION
  - .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-post.txt
  - .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-phase70-regression-gap.txt
  - .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/SCREENSHOTS.md
autonomous: false
requirements: [HEAT-01, HEAT-02, HEAT-03, HEAT-04, HEAT-05, HEAT-06]

must_haves:
  truths:
    - "The gap-closure code is live in production on a NEW version tag, deployed by the deploy.yml workflow — never by a local terragrunt apply."
    - "The byte-identical extended probe scores strictly higher post-deploy than the pre-fix transcript, and every remaining red has a written, calendar-bound reason."
    - "Probe assertion 16 — the blast-radius regression gate — is GREEN post-deploy, proving the edge block did not catch meshtk's claim-link mint or the run.auth internal family."
    - "The Phase 70 dialog-shell probe still scores 16/16, unmodified — the studio changes did not regress the dialog."
    - "Fresh visual evidence exists, captured with every non-heat layer hidden and the camera on the measured density hotspot, showing a legible DC33 flame stack at the D-13 opacity."
    - "A human has looked at the live map on real hardware and confirmed the stack reads."
  artifacts:
    - "apps/run.gpx/webapp/VERSION (bumped)"
    - "a buildpub.yml run URL and a deploy.yml run URL"
    - "71-08-probes/transcript-gap-post.txt"
    - "71-08-probes/transcript-phase70-regression-gap.txt"
    - "fresh capture-heat-visual.cjs screenshots + an updated SCREENSHOTS.md"
  key_links:
    - "transcript-gap-pre.txt (plan 71-12) -> transcript-gap-post.txt: the contrast is the evidence, produced by a byte-identical script"
    - "deploy.yml invalidate_cache -> plan 71-13's new heat-map cache behaviour"
  prohibitions:
    - "NO local `terragrunt apply` and NO `--with-terragrunt` on the release. The ECS/CloudFront deploy ALWAYS goes through deploy.yml. Essential Rule 4."
    - "NO `--skip-bump`. ECR repos are immutable; the release must produce a NEW tag. Live is v0.0.109."
    - "NO edit to heatmap-probe.cjs during or after this plan. The byte-identical-script property is what makes the pre/post contrast evidence rather than decoration."
    - "NO synthetic or injected con-day data to turn assertions 5 or 11 green. They are calendar-bound until 2026-08-05."
    - "NO default-zoom screenshot with the public layer groups on. The visual record must be captured the way 71-08 finally did it — every non-heat layer hidden, camera on the measured hotspot."

---

<objective>
Ship the gap closure and prove it against production with the same script that scored it
before the fix.

Seven plans landed changes across three surfaces: the studio paint and empty-year handling,
the artifact guard and serve route, the internal route and builder bounds, the CloudFront
edge block and cache behaviour, the scheduler's schedules and timeouts, and the republished
DC33 artifact. The infrastructure halves are already applied by their own scoped CI runs;
this plan releases the application code, deploys it, and closes the loop with evidence.

Purpose: DEF CON 34 runs 2026-08-05..10. This is the last planned deploy before the con, so
the verification has to be the strict kind — a byte-identical probe, a Phase 70 regression
re-run, controlled visual capture, and a human looking at the real thing.

Output: a live version carrying every fix, two transcripts whose contrast is the proof, and
fresh visual evidence of the thing the phase promised.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./AGENTS.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-SUMMARY.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-12-SUMMARY.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every gate on the assembled tree</name>

  <read_first>
    - `./AGENTS.md` — the Essential Rules, the Releases & Deploys section, and the
      `env.local.sh` worktree landmine.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-SUMMARY.md`
      — the exact gate commands and the recorded baselines used the last time this phase
      shipped, including the svelte-check baseline shape.
    - `apps/run.gpx/build-frontend.sh` — the gpx-studio build the release depends on.
  </read_first>

  <files>(no repo files modified — gate output recorded in the SUMMARY)</files>

  <action>
On the assembled phase branch with all seven prior plans merged in, run every gate and
record the raw output.

  - `cd apps/run.gpx/webapp && npx vitest run` — Node >= 22.12 (`nvm use 22.12.0`). The
    baseline before this gap-closure set was 303 passed / 1 skipped; the new total must be
    STRICTLY HIGHER, with zero failures and no newly skipped tests.
  - `cd apps/run.gpx/webapp && npx tsc --noEmit` — exit 0.
  - `cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json` —
    a DELTA gate, not an absolute count. The recorded baseline is 26 errors plus 4
    environment-dependent ones (D-71-E). Zero NEW errors attributable to the files this
    gap-closure set touched.
  - `./apps/run.gpx/build-frontend.sh` — exit 0. A svelte-check pass does not prove the Vite
    build, and the bundle is what ships.
  - `cd apps/run.gpx/webapp && node scripts/verify-heatmap-artifact.mjs --selftest` — exit 0.
  - `terraform fmt -check` on both touched module directories.

Do NOT run any eslint or prettier gate. eslint is non-functional in both packages (D-71-A/C)
and prettier has a pre-existing baseline failure on an untouched studio file (D-71-D). Both
were proven pre-existing on a clean tree and are correctly scoped out; running them here
would produce noise that is not this phase's.
  </action>

  <acceptance_criteria>
    - vitest total passed is strictly greater than 303, failures 0, skipped 1 (unchanged).
      (Can go red: any of the four code plans could have broken a sibling suite.)
    - `tsc --noEmit` exits 0.
    - svelte-check delta is 0 new errors — record both the baseline and the current counts,
      and if the current count differs, name each new diagnostic and the file it is in.
    - `build-frontend.sh` exits 0.
    - `verify-heatmap-artifact.mjs --selftest` exits 0 and reports four fixtures.
    - `terraform fmt -check` clean on `modules/cloudfront/v1.0.0` and
      `modules/heatmap-scheduler/v1.0.0`.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/webapp && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>

  <done>Every gate green with the raw counts recorded; the vitest total is above baseline; the svelte-check delta is zero.</done>
</task>

<task type="auto">
  <name>Task 2: Bump, release through CI, deploy through deploy.yml, invalidate</name>

  <read_first>
    - `./AGENTS.md` "Releases & Deploys" — the local-builds-images / CI-does-the-apply split,
      the immutable-ECR rule, and the `env.local.sh` worktree landmine (the profile silently
      collapses in a fresh worktree and the build dies at the S3 static-asset sync with exit
      255 AFTER the images already succeeded).
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-SUMMARY.md`
      — the exact release path this phase used last time (buildpub, then deploy.yml) and the
      recorded run ids, so the same path is followed rather than a different one improvised.
    - `apps/run.gpx/webapp/VERSION` — the current value. Live is v0.0.109.
    - `.github/workflows/buildpub.yml` and `.github/workflows/deploy.yml` input lists.
  </read_first>

  <files>apps/run.gpx/webapp/VERSION</files>

  <action>
(a) **Bump the version.** Increment `apps/run.gpx/webapp/VERSION` to the next patch. ECR
repositories are immutable, so a build against an already-released tag fails loudly — never
skip the bump, and never reuse v0.0.109.

(b) **Release through CI.** Use the same path 71-08 used: dispatch `buildpub.yml` for the
gpx app targeting use1. This builds and publishes the image from CI, which sidesteps the
`env.local.sh` worktree landmine entirely. If you fall back to the local
`apps/release-all.sh --apps run.gpx --pr` path for any reason, copy `env.local.sh` from the
main checkout into this worktree root FIRST — it is gitignored, a fresh worktree does not
have it, and without it the AWS profile collapses and the build dies at the post-build S3
sync after the images already pushed.

(c) **Deploy through deploy.yml.** Dispatch it for us-east-1 with cache invalidation
enabled. This is the only path that applies ECS and invalidates CloudFront; never run
terragrunt locally to deploy and never pass a terragrunt flag to a release.

The invalidation matters more than usual this time: plan 71-13 put a real cache in front of
the public heat-map path, so without it the edge would keep serving pre-deploy bytes.

(d) **Verify the version actually rolled.** CI going green is not proof the new task is
serving — ECS does a rolling replace while the old task drains. Poll the live version until
it reports the new tag, and record the command and its output.
  </action>

  <acceptance_criteria>
    - `apps/run.gpx/webapp/VERSION` is strictly greater than the pre-plan value and is not
      v0.0.109.
    - A buildpub.yml run URL is recorded and concluded successfully.
    - A deploy.yml run URL is recorded, with region us-east-1 and invalidation enabled, and
      concluded successfully.
    - The live version reports the new tag:
      `curl -s https://gpx.defcon.run/use1/ | grep -oE 'v0\.0\.[0-9]+'` returns the new value.
      (Can go red: a green CI run with an old version still serving is the exact false-verify
      trap this repo has hit before.)
    - The SUMMARY states that no local terragrunt apply was run and that no release flag
      requested one.
  </acceptance_criteria>

  <verify>
    <automated>curl -s https://gpx.defcon.run/use1/ | grep -oE 'v0\.0\.[0-9]+' | head -1</automated>
  </verify>

  <done>A new version tag is built by CI, deployed by deploy.yml with invalidation, and confirmed live by polling the served version — not by trusting the workflow's exit status.</done>
</task>

<task type="auto">
  <name>Task 3: Post-deploy probe, Phase 70 regression, and the contrast record</name>

  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/heatmap-probe.cjs`
      header — the SSM invocation with the decryption flag (load-bearing) and the
      header-only `PROBE_NOTES` escape hatch, which exists precisely so one byte-identical
      script can serve both runs.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-pre.txt`
      — the pre-fix score and red/green map this run is compared against.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-12-SUMMARY.md`
      — the mapping of each red assertion to the plan expected to turn it green.
    - The Phase 70 dialog-shell probe referenced by
      `71-08-probes/transcript-phase70-regression.txt` — locate the script it was produced by
      and re-run it UNMODIFIED.
  </read_first>

  <files>
.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-post.txt,
.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-phase70-regression-gap.txt
  </files>

  <action>
(a) **Run the extended probe, unmodified.** Confirm first that
`git diff transcript-gap-pre.txt's commit..HEAD -- heatmap-probe.cjs` shows NO change since
the pre run — the script must be byte-identical or the contrast proves nothing. Then run it
with the SSM-read token and `PROBE_NOTES` recording the new live version, the buildpub run
URL and the deploy.yml run URL. Save to `transcript-gap-post.txt`.

(b) **Compare, assertion by assertion.** Produce a table in the SUMMARY with one row per
assertion: pre verdict, post verdict, and — for anything still red — the reason. The expected
transitions are:
  - 1, 2 red -> green (the cache behaviour from 71-13)
  - 8, 15 red -> green (the edge block from 71-13 plus the application 404 from 71-11)
  - 13, 18 red -> green (the de-collided schedules from 71-14)
  - 17 red -> green (the republished DC33 artifact from 71-15, or still red WITH a recorded
    deferral if the 71-15 checkpoint was deferred)
  - 19 red -> green (the D-13 opacity from 71-09)
  - 16 green -> green. This one must NOT change. A green-to-red transition here means the
    edge block caught something it must not, and it is a stop-the-line finding.
  - 5 (dc34 leg) and 11 stay red. Calendar-bound: zero of the run rows carry a con day and
    DEF CON 34 is 2026-08-05..10. Do not fake data and do not soften either assertion.

Any assertion that does NOT follow this map is a finding. Investigate and record it; do not
paper over it and do not edit the probe.

(c) **Re-run the Phase 70 dialog-shell probe UNMODIFIED** and save to
`transcript-phase70-regression-gap.txt`. It scored 16/16 before; the studio changes in 71-09
touch the same dialog subtree, so this is the regression gate for them. Note in the SUMMARY
that two of that probe's assertions were previously recorded as not failing closed — treat a
16/16 as necessary but not sufficient, and say so rather than citing it as a clean bill.
  </action>

  <acceptance_criteria>
    - `transcript-gap-post.txt` exists with a `RESULT: N/19` line, and N is STRICTLY GREATER
      than the pre-fix N. Record both.
    - The probe script is byte-identical to the version that produced the pre transcript:
      `git diff --stat <pre-commit>..HEAD -- .../heatmap-probe.cjs` shows no change.
      (Can go red: any post-hoc edit invalidates the contrast.)
    - Assertion 16 is PASS in the post transcript. (Can go red — and if it does, STOP: the
      edge block is too wide and a con-critical CTF flow is broken.)
    - Assertions 1, 2, 8, 13, 15, 18 and 19 are all PASS.
    - Assertion 17 is PASS, or is red with the 71-15 deferral recorded as its reason.
    - Assertions 5 and 11 are red with the calendar-bound reason restated.
    - `grep -c "pk\." transcript-gap-post.txt` returns 0 — no token leaked.
    - `transcript-phase70-regression-gap.txt` shows 16/16.
    - The per-assertion pre/post table is in the SUMMARY.
  </acceptance_criteria>

  <verify>
    <automated>cd .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes && grep "RESULT: " transcript-gap-pre.txt transcript-gap-post.txt && test "$(grep -c 'pk\.' transcript-gap-post.txt)" = "0" && echo NO-TOKEN-LEAK</automated>
  </verify>

  <done>A post-deploy transcript from the byte-identical script scores above the pre-fix run; assertion 16 stayed green; every transition matches the expected map or is recorded as a finding; the Phase 70 probe re-run is 16/16 with its known weakness restated.</done>
</task>

<task type="auto">
  <name>Task 4: Re-capture the controlled visual evidence at the D-13 opacity</name>

  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/capture-heat-visual.cjs`
      in full. Read the header: it already turns every other section's master checkbox OFF,
      parks the camera on a density hotspot DERIVED from the live artifact rather than
      guessed, and shoots at two zooms. It asserts nothing and gates nothing by design.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/SCREENSHOTS.md`
      — the existing index and the naming convention, including
      `shot-dc33-SHIPPED-0.25-invisible.png` and `shot-dc33-DIAG-0.70-legible.png` which are
      the two controlled captures that produced D-13.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-15-SUMMARY.md`
      — the republished artifact's coordinate count, since the hotspot is derived from the
      live artifact and the artifact changed.
  </read_first>

  <files>.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/SCREENSHOTS.md</files>

  <action>
Re-run `capture-heat-visual.cjs` against the newly deployed studio with the SSM-read mapbox
token. Do NOT take a default-zoom screenshot with the public layer groups on — that capture
method is exactly what made the previous shots useless, and the script already fixes it.

Because plan 71-15 republished the DC33 artifact, re-derive the density hotspot from the NEW
live artifact rather than reusing the old coordinates, if the script computes it at runtime;
if it is hardcoded, confirm the recorded hotspot still peaks on the new data and update it if
not. Record the hotspot cell, its point count, and how many runs pass through it.

Name the new captures so they cannot be confused with the pre-fix ones — include the shipped
opacity in the filename, mirroring the existing convention. Then update `SCREENSHOTS.md` with:
  - the new files, what each shows, and the camera/zoom used;
  - an explicit side-by-side pointer to `shot-dc33-SHIPPED-0.25-invisible.png` so the
    before/after is one click apart;
  - an honest statement that DC34 still draws nothing because it has no runs, that this is a
    calendar gap and not a render gap, and that no synthetic data was injected;
  - a note that these are headless captures on a software rasteriser, which is why Task 5
    asks a human to confirm on real hardware.
  </action>

  <acceptance_criteria>
    - At least two new screenshot files exist in the probes directory whose names carry the
      shipped opacity, at the two zooms the capture script shoots.
    - The DC33 stack is visible in them. Verify by opening the images and describing what is
      there — the corridor structure should read, with busier corridors visibly heavier than
      one-off spurs, which is the effect the previous 0.70 diagnostic capture showed and the
      0.25 shipped capture did not.
      (Can go red: if the new capture is still indistinguishable from a bare basemap, the
      D-13 change did not reach production and Task 2's deploy verification was wrong.)
    - `SCREENSHOTS.md` names the new files, the hotspot cell and its measured point/run counts,
      and points at the pre-fix capture for comparison.
    - `SCREENSHOTS.md` states plainly that DC34 draws nothing for calendar reasons and that no
      synthetic data was injected.
    - No mapbox token appears in any committed file:
      `grep -rc "pk\." .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/*.md` returns 0.
  </acceptance_criteria>

  <verify>
    <automated>ls -la .planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/*.png | tail -6</automated>
  </verify>

  <done>Fresh controlled captures exist at the shipped opacity with all non-heat layers hidden and the camera on the re-derived hotspot; the DC33 stack is legible in them; SCREENSHOTS.md indexes them honestly, including what still cannot be shown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Blocking human verification of the live heat map on real hardware (D-13 / D-12)</name>
  <files>apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts</files>
  <read_first>
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/transcript-gap-post.txt` — the automated result this human check sits on top of.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-08-probes/SCREENSHOTS.md` — the fresh controlled captures and the pre-fix comparison pointer.
  </read_first>
  <action>Present the material below to the developer and BLOCK until they reply. Do not mark the gap-closure set complete before approval.</action>
  <what-built>
The full gap closure is live on a new run.gpx version:

  - **D-13** — the DC33/DC34 heat lines paint at opacity 0.70 instead of 0.25. Colours are
    unchanged. This is the fix for the phase's headline failure: at 0.25 the stack was not
    faint, it was invisible.
  - **CR-03** — the public heat-map path now has a real CloudFront cache behaviour, so repeat
    fetches are absorbed at the edge instead of costing the single run.gpx task an S3 read
    and a full response body each.
  - **CR-01** — `/api/gpx/internal/*` on the gpx host is refused at the edge with a marked
    404, in both the region-prefixed and no-region forms, closing the inherited strava-sync
    exposure at the same time. The application also now compares its secret in constant time
    and answers 404 rather than 403.
  - **WR-04 / WR-03** — the two DC34 schedules no longer collide at 04:00 PT on con days, and
    the builder / invoker / Lambda timeout chain is strictly increasing at 240 / 300 / 420 s.
  - **WR-06** — the DC33 artifact was republished without its 20 zero-length decode artefacts
    (subject to your earlier approval in plan 71-15).
  - **WR-01 / WR-02 / WR-05 / WR-07 / WR-09 / IN-01 / IN-02 / IN-04 / IN-05** — guard widened,
    serve path validated, truncation made loud, the empty DC34 row made honest, logs cleaned.
  - **D-14** — CR-02 is recorded in the code as an accepted risk. Nothing was trimmed and the
    coordinate precision is unchanged, per your call.

Evidence: `transcript-gap-post.txt` (byte-identical script, scored higher than the pre run),
`transcript-phase70-regression-gap.txt` (16/16), and fresh controlled captures indexed in
`SCREENSHOTS.md`.
  </what-built>

  <how-to-verify>
Three things, none of which a headless browser can settle.

1. **Look at the flame stack on real hardware.** Open
   `https://gpx.defcon.run/use1/studio/app` in a normal browser (not headless), open Map
   Layers, turn OFF the DEF CON 34 Routes, Rabbit Routes and User Check-ins sections, turn ON
   `🔥 DC33 — the classic`, and zoom to the Strip.
   EXPECT: a visible orange flame stack whose busy corridors read heavier than one-off spurs.
   All the captures in this phase were headless Chromium on a software rasteriser; the 0.70
   and opacity-1 renders prove low-alpha blending works there, so a pure headless artifact is
   unlikely — but confirming on real hardware before the con is cheap.

2. **Check the DC33 run count in the hint bar.** Hover the DC33 row. If you approved the
   plan-71-15 rebuild, the count is now the corrected one (about 90, down from 110) and
   the distance is essentially unchanged. Confirm that reads right to you.

3. **Sanity-check one thing that must still work.** The ghost claim-link mint is called by
   meshtk over public HTTPS against run.human, and this phase added an edge block on a
   neighbouring path family. Probe assertion 16 gates it and is green, but if you have a
   quick way to exercise a ghost claim link, do. A silent break here would only surface
   during the con.

Known and deliberately still open, so they are not a surprise:
  - **DC34 draws nothing.** Zero of the run rows carry a con day and DEF CON 34 is
    2026-08-05..10. Probe assertions 5 and 11 are red for this reason alone. No synthetic
    data was injected — you declined that, and the dated 5-10 August re-probe todo covers it.
  - The hourly con-window schedule has still never fired; it cannot before 5 August.

Reply `approved` to close the gap-closure set, or describe what is wrong.
  </how-to-verify>

  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI release pipeline -> production ECS | buildpub publishes the image, deploy.yml applies it |
| probe artifacts -> git | Transcripts and screenshots are committed |
| production -> probe | Live unauthenticated requests across three hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-16-01 | Tampering | A probe edited after the deploy so the ship gate becomes reachable | high | mitigate | Task 3(a) proves the script is byte-identical to the pre-run version via a git diff before running it; the prohibition forbids editing it in this plan at all. |
| T-71-16-02 | Denial of service | Deploying an edge block that catches a con-critical path | critical | mitigate | Probe assertion 16 must be green post-deploy and a green-to-red transition is a stop-the-line finding; the human checkpoint asks for a ghost claim-link spot-check as well. |
| T-71-16-03 | Information disclosure | Mapbox token leaking into a committed transcript or index | medium | mitigate | Read from env, never printed; pinned by grep on both the transcript and the markdown index. |
| T-71-16-04 | Tampering | A green CI run with the old task still serving, read as a successful deploy | high | mitigate | Task 2(d) polls the live served version rather than trusting the workflow conclusion — the repo has hit this false-verify trap before. |
| T-71-16-05 | Repudiation | Remaining red assertions absorbed silently into a headline score | medium | mitigate | Task 3(b) requires a per-assertion pre/post table with a written reason for every remaining red, and forbids softening assertions 5 and 11. |
</threat_model>

<verification>
1. Full gate sweep on the assembled tree (Task 1), with raw counts.
2. New version tag built by buildpub, deployed by deploy.yml with invalidation, confirmed by
   polling the live served version.
3. Byte-identical extended probe: post score strictly above pre, per-assertion table, no
   token leak.
4. Phase 70 dialog-shell probe re-run unmodified at 16/16, with its two
   not-failing-closed assertions restated as a known weakness rather than cited as proof.
5. Controlled visual re-capture with all non-heat layers hidden and the camera on the
   re-derived hotspot.
6. Blocking human verification on real hardware.
7. NO local terragrunt apply. NO `--with-terragrunt`. NO `--skip-bump`.

**Carried forward, not closed here:**
- Probe assertions 5 (dc34 leg) and 11 remain red. Calendar-bound; the dated 5-10 August 2026
  re-probe todo already exists and covers them.
- The DC34 hourly cadence has never fired and cannot before the con.
- The three deferrals named in plans 71-11, 71-13 and 71-14 (rate limiting on the internal
  route, the strava-sync comment, count-gating the VPC managed policy) are post-con items.
- IN-03 (an unreachable finite check in `polyline-decode.ts`) is deliberately untouched:
  it is dead code with zero behavioural effect, and editing the DC33 decode path five days
  before the con is risk without benefit.
</verification>

<success_criteria>
- A new run.gpx version is live and confirmed by polling the served version.
- The byte-identical extended probe scores strictly higher than the pre-fix run.
- Assertion 16 stayed green; assertions 1, 2, 8, 13, 15, 18, 19 turned green; 17 turned green
  or carries a recorded deferral; 5 and 11 remain red with the calendar reason restated.
- The Phase 70 probe is 16/16 unmodified.
- Fresh controlled captures show a legible DC33 flame stack at the shipped opacity.
- A human has confirmed on real hardware.
</success_criteria>

<output>
Create `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-16-SUMMARY.md` when done.

Include the per-assertion pre/post table and the live version string.

## Artifacts this phase produces (this plan)

| Kind | Name | File |
|------|------|------|
| version bump | new run.gpx patch version | `apps/run.gpx/webapp/VERSION` |
| CI run | buildpub.yml (gpx, use1) | recorded in SUMMARY |
| CI run | deploy.yml (us-east-1, invalidate) | recorded in SUMMARY |
| transcript | post-deploy extended probe | `71-08-probes/transcript-gap-post.txt` |
| transcript | Phase 70 dialog-shell regression re-run | `71-08-probes/transcript-phase70-regression-gap.txt` |
| screenshots | controlled captures at the shipped opacity | `71-08-probes/shot-*.png` |
| updated index | screenshot index with before/after pointer | `71-08-probes/SCREENSHOTS.md` |
</output>
