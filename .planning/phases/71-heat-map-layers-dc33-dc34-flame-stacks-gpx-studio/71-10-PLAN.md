---
phase: 71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio
plan: 10
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - apps/run.gpx/webapp/src/lib/heatmap-artifact.ts
  - apps/run.gpx/webapp/src/lib/heatmap-artifact.test.ts
  - apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs
  - apps/run.gpx/webapp/src/app/api/gpx/public/heatmap/[year]/route.ts
autonomous: true
requirements: [HEAT-01, HEAT-03]

must_haves:
  truths:
    - "assertNonAttributable rejects an unexpected key inside `meta`, a non-numeric or non-pair coordinate, and a root `type` that is not FeatureCollection — the three holes WR-01 found, each pinned by a test that fails if the check is removed."
    - "assembleHeatmapArtifact drops a track whose surviving coordinates are all identical, so a null-island or never-moved run cannot inflate runCount (WR-06)."
    - "The public serve route runs the same structural guard on the way OUT, so an object written by anything other than the two known writers cannot be echoed to the internet unchecked (WR-02)."
    - "`?meta=1` is an exact-equality test, so `?meta=0` no longer projects meta (IN-02)."
    - "No AWS SDK error object reaches a log line from the serve route — only a name/tag (WR-09)."
    - "verify-heatmap-artifact.mjs FAILS a degenerate artifact, and its --selftest proves that with a doctored fixture (WR-06 third consequence)."
    - "The reason the endpoint-privacy exposure is NOT being fixed is recorded in the code at normalizeTrack, citing D-14, so a future reader does not 'fix' it as an oversight."
  artifacts:
    - "apps/run.gpx/webapp/src/lib/heatmap-artifact.ts"
    - "apps/run.gpx/webapp/src/lib/heatmap-artifact.test.ts"
    - "apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs"
    - "apps/run.gpx/webapp/src/app/api/gpx/public/heatmap/[year]/route.ts"
  key_links:
    - "[year]/route.ts -> assertNonAttributable (new import; serve-path defence in depth)"
    - "assembleHeatmapArtifact -> degeneracy filter (fixes both years at the single assembly point)"
    - "verify-heatmap-artifact.mjs --selftest -> doctored degenerate fixture (stops the verifier going vacuous again)"
  prohibitions:
    - "NO endpoint trimming. NO ENDPOINT_TRIM_M constant, no trimEnds helper, no change to the first/last-point-preserving stride. D-14 is user-locked: CR-02 is an ACCEPTED RISK."
    - "NO change to COORD_PRECISION. It stays 5. D-14."
    - "NO restoration of an includeInAggregate / opt-in predicate anywhere. D-03 stays locked and is not re-litigated by this plan."
    - "NO relaxation of verify-heatmap-artifact.mjs's `runCount > 0` check. The dc34 leg of probe assertion 5 is calendar-bound and must stay red until real con-day runs exist; softening the verifier to turn it green is exactly the anti-pattern this phase already recorded."

---

<objective>
Make the phase's single non-attributability chokepoint actually cover what its docstring
claims, stop the assembler publishing degenerate geometry, put a structural check on the
way OUT as well as the way IN, and record D-14 in the code so the accepted risk is not
re-opened as a bug.

Closes WR-01 (guard never inspects `meta`, `coordinates` contents or the root `type`),
WR-02 (serve path echoes S3 unvalidated; `{}` yields a 200 with an empty body), WR-06 code
half (20 of 110 live DC33 features are entirely `[[0,0],[0,0]]`, and the verifier certified
them), WR-09 route half (raw SDK error objects logged), and IN-02 (`?meta=` truthiness).

Purpose: the guard is the compensating control that was accepted in exchange for dropping
the owner opt-in gate. A control with holes exactly where a future change would put data is
worse than an honest one, because the phase's own comments cite it as proof.

Output: a guard that would stop the leak it exists to stop, an assembler that cannot
publish a zero-length "run", a verifier that cannot certify one, and a serve route that
proves its own output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-CONTEXT.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the guard, filter degenerate geometry, and record D-14 at normalizeTrack</name>

  <read_first>
    - `apps/run.gpx/webapp/src/lib/heatmap-artifact.ts` in full (291 lines). Specifically:
      lines 55-65 (`MAX_TRACK_POINTS` / `MAX_RUNS` / `COORD_PRECISION` and the comment that
      sizes them on the CDN premise), 113-143 (`normalizeTrack` and its first/last-preserving
      stride), 174-212 (`assembleHeatmapArtifact`), 214-291 (`assertNonAttributable`,
      `ROOT_KEYS`/`FEATURE_KEYS`/`GEOMETRY_KEYS`, `isPlainObject`).
    - `apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs` lines 35-36 and 121-152 —
      the standalone verifier is currently STRICTER than the runtime guard. Port its
      `META_KEYS` set and its coordinate walk; do not invent a second spelling of either.
    - `apps/run.gpx/webapp/src/lib/heatmap-artifact.test.ts` — read the existing
      `assertNonAttributable` describe block (the review notes it has no `meta` case) so the
      new tests match the file's existing arrange/act/assert idiom.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-CONTEXT.md`
      section `## Gap-Closure Decisions` -> **D-14**. This is the binding text for the
      comment you will write at `normalizeTrack`.
  </read_first>

  <files>apps/run.gpx/webapp/src/lib/heatmap-artifact.ts, apps/run.gpx/webapp/src/lib/heatmap-artifact.test.ts</files>

  <behavior>
    - `assertNonAttributable({type:"Feature", meta:{...}, features:[]})` throws — root type wrong.
    - `assertNonAttributable` with `meta` carrying a fifth key (e.g. a `generatedBy`) throws, naming that key.
    - `assertNonAttributable` with `meta` absent or not a plain object throws.
    - `assertNonAttributable` with a feature whose `geometry.coordinates` is not an array throws.
    - `assertNonAttributable` with a coordinate that is a string, a 3-element array, or a pair containing a non-number throws, naming the feature index.
    - A clean artifact produced by `assembleHeatmapArtifact` still passes (regression: the widened guard must not reject its own output).
    - `assembleHeatmapArtifact("dc34", iso, [[[0,0],[0,0]]])` returns `runCount: 0` and `features: []`.
    - `assembleHeatmapArtifact` with a track whose points are all the SAME non-zero coordinate also returns 0 features (the filter is "never moved", not "is null island").
    - `assembleHeatmapArtifact` with a genuine two-point track that moves still returns 1 feature (the filter must not eat real runs).
  </behavior>

  <action>
Write the tests in `heatmap-artifact.test.ts` FIRST, watch them fail, then implement.

(a) **Widen `assertNonAttributable` (WR-01).** Add, alongside the existing checks and in
this order so the error messages stay useful:
  - root `type` must equal the FeatureCollection literal; throw naming the actual value.
  - `meta` must be a plain object, and every one of its own keys must be a member of a new
    module-level `META_KEYS` set whose contents match `verify-heatmap-artifact.mjs`'s
    (`year`, `generatedAt`, `runCount`, `totalKm`); throw naming the offending key.
  - per feature, `geometry.coordinates` must be an array; every element must be a
    2-element array of two numbers. Throw naming the feature index (and the coordinate
    index where cheap). Do NOT range-check here — the verifier does that and the assembler
    already filters out-of-range points; the point of this check is that the one field
    which actually carries data is inspected at all.
Update the function's docstring so it enumerates what it now checks and, honestly, what it
still does not (it does not range-check coordinates and it does not run on the serve path
in this module — 71-10 Task 3 wires that call from the route).

(b) **Filter degenerate geometry in `assembleHeatmapArtifact` (WR-06).** After
`normalizeTrack` and after the existing `coordinates.length < 2` drop, also `continue` when
every coordinate equals the first. Add a short comment: a track that never moves is not a
run; the DC33 export produced 20 of these out of 110, all entirely at null island, because
a short or degenerate `summary_polyline` decodes to zero accumulated deltas. Note in the
comment that this filter runs at the single assembly point, so it fixes BOTH years, and
that the DC33 artifact must be rebuilt (plan 71-15) for the fix to reach production.

(c) **Record D-14 at `normalizeTrack`.** Add a clearly-labelled block comment immediately
above `normalizeTrack` stating, in prose: the stride deliberately preserves each run's
first and last surviving point; combined with `COORD_PRECISION` of 5 (~1.1 m) and D-03's
removal of the owner opt-in gate, that publishes every con-day run's exact start and end
point, unauthenticated, at a stable public URL, for runners who never consented. State that
Kurt reviewed exactly this on 2026-07-31 with the exposure named plainly and ACCEPTED the
risk (**D-14**), that `assertNonAttributable` proves "no identifier fields" which is a
different property from "not re-identifiable from geometry", and that this distinction is
on the record rather than implied away. Close with an explicit instruction that the
first/last preservation and the precision constant must NOT be changed as a drive-by fix,
and that reversing D-14 requires a new user decision.

(d) **Correct the CDN premise comment.** The block above `MAX_TRACK_POINTS` sizes these
bounds for a CDN-cached route, which was not true when it was written. Rewrite it to name
the dependency explicitly: the caching is delivered by a dedicated ordered cache behaviour
for the public heat-map path in `infra/terraform/modules/cloudfront/v1.0.0/main.tf` (added
by plan 71-13), because the catch-all region behaviour uses the managed caching-disabled
policy; if that dedicated behaviour is ever removed these bounds are no longer correctly
sized and every hit becomes an origin S3 read. Do not delete the bounds.

Do NOT add any trimming helper, any trim constant, or any change to `COORD_PRECISION`.
  </action>

  <acceptance_criteria>
    - New tests exist and pass. `cd apps/run.gpx/webapp && npx vitest run src/lib/heatmap-artifact.test.ts`
      reports MORE passing tests than the recorded pre-change count, and every behavior bullet
      above has a corresponding test. (Can go red: run the suite against the unmodified
      `heatmap-artifact.ts` first — the meta/coordinate/root-type and degeneracy tests must
      FAIL before the implementation lands. Record that pre-implementation failure count in
      the SUMMARY; a test that passes before the fix is not testing the fix.)
    - Guard inspects meta: `grep -v '^\s*[/*]' apps/run.gpx/webapp/src/lib/heatmap-artifact.ts | grep -c "META_KEYS"` returns >= 2 (declaration + use).
    - Guard inspects coordinates: `grep -v '^\s*[/*]' .../heatmap-artifact.ts | grep -cE "geometry.coordinates|coords\b"` returns >= 1 inside `assertNonAttributable` (confirm by reading, not by count alone).
    - Degeneracy filter present in the assembler: `grep -v '^\s*[/*]' .../heatmap-artifact.ts | grep -cE "every\(" ` returns >= 1.
    - **D-14 prohibitions hold** — all three of these greps return 0 on comment-stripped source:
      `grep -v '^\s*[/*]' .../heatmap-artifact.ts | grep -ciE "trimEnds|ENDPOINT_TRIM"` -> 0
      `grep -v '^\s*[/*]' .../heatmap-artifact.ts | grep -c "COORD_PRECISION = 4"` -> 0
      `grep -v '^\s*[/*]' .../heatmap-artifact.ts | grep -c "includeInAggregate"` -> 0
      (All three can go red: they are the exact fixes CR-02 proposed and D-14 declined.)
    - `grep -c "COORD_PRECISION = 5" .../heatmap-artifact.ts` returns 1 — the value is unchanged.
    - `grep -c "D-14" .../heatmap-artifact.ts` returns >= 1 — the decision is cited in-file.
    - The CDN comment names its dependency: `grep -c "cloudfront" .../heatmap-artifact.ts` returns >= 1.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/webapp && npx vitest run src/lib/heatmap-artifact.test.ts src/lib/heatmap-build.test.ts src/lib/polyline-decode.test.ts</automated>
  </verify>

  <done>All three heatmap suites green (previously 51/51, now higher); the widened guard rejects each of the three WR-01 holes with a named error; a never-moving track yields zero features; a real track still yields one; D-14 is recorded at `normalizeTrack`; no trim helper, no precision change, no opt-in predicate.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the verifier to reject degenerate geometry, and prove it with a doctored fixture</name>

  <read_first>
    - `apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs` in full (294 lines).
      Specifically the `geometry is bounded LineString` check (lines 121-152), the
      `runCount agrees with features` check (lines 83-94), and the `--selftest` block
      (lines 193-267) with its three fixtures — `CLEAN_FIXTURE`, `withFeatureProperty`,
      `withSmuggledAtSign`.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md`
      section WR-06, third consequence: this verifier PASSED the live artifact in which 18%
      of features are junk, because its range check verifies bounds but not degeneracy.
  </read_first>

  <files>apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs</files>

  <action>
Add a degeneracy check to the `geometry is bounded LineString` entry in `CHECKS`: after the
existing per-coordinate range walk for a feature, compare every coordinate against the
feature's first; if all are identical, `fail()` naming the feature index and the repeated
coordinate. Extend that check's returned detail string so a passing run reports something
that would have differed on the current live artifact (for example, that zero features are
degenerate) — the detail line is the audit trail.

Add a FOURTH self-test fixture: a clone of `CLEAN_FIXTURE` whose single feature's
coordinates are two identical pairs. Register it in the doctored-fixture loop with a label
naming degeneracy, so `--selftest` fails loudly if the new check is ever removed. This is
what stops the verifier going vacuous a second time.

Do NOT touch the `runCount > 0` check. That check is why probe assertion 5's dc34 leg is
red, and that redness is calendar-bound (zero con-day runs exist until 5 Aug 2026).
Relaxing it here would turn a genuine liveness gate green with no data behind it — see the
prohibition.

Do NOT touch the `FORBIDDEN` byte-sweep list or the `META_KEYS` / `ROOT_KEYS` /
`FEATURE_KEYS` constants; Task 1 ports FROM them, so they are the reference spelling.
  </action>

  <acceptance_criteria>
    - `cd apps/run.gpx/webapp && node scripts/verify-heatmap-artifact.mjs --selftest` exits 0
      and its output names FOUR doctored/clean fixtures, including a degeneracy one.
      (Can go red: with the new fixture added but the check absent, the doctored fixture
      passes and `--selftest` exits 1 with "the verifier is vacuous".)
    - The verifier rejects a real degenerate artifact. Build a scratch file containing a
      two-feature FeatureCollection where one feature is `[[0,0],[0,0]]` (write it under the
      session scratch directory, NOT into the repo) and confirm
      `node scripts/verify-heatmap-artifact.mjs <scratch>.json` exits NON-ZERO with a message
      naming the degenerate feature index.
      (Can go red: this exact shape passes today — that is WR-06.)
    - The verifier still ACCEPTS the current clean fixture — `--selftest` reports the clean
      fixture passing every check. (Can go red: an over-broad degeneracy test that rejects
      any 2-point line would break the clean fixture, which is a legitimate 2-point run.)
    - `grep -c "runCount <= 0" apps/run.gpx/webapp/scripts/verify-heatmap-artifact.mjs` returns 1
      — the liveness check is untouched. (Can go red if softened.)
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/webapp && node scripts/verify-heatmap-artifact.mjs --selftest</automated>
  </verify>

  <done>`--selftest` exits 0 with four fixtures; a hand-built degenerate artifact exits non-zero naming the feature; the clean fixture still passes; the `runCount > 0` liveness check is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the serve path, tighten `?meta=1`, and stop logging raw SDK errors</name>

  <read_first>
    - `apps/run.gpx/webapp/src/app/api/gpx/public/heatmap/[year]/route.ts` in full (104
      lines). Note line 71 / 84 / 98 (the three `console.error` calls that pass the whole
      error object), line 81 (`JSON.parse(body) as HeatmapArtifact` — an unchecked
      assertion), line 91 (the truthiness `?meta=` test), line 92 (`artifact.meta` used with
      no existence check), and lines 26-43 (the CDN-premise comments and `CACHE_SECONDS`).
    - `apps/run.gpx/webapp/src/lib/heatmap-artifact.ts` — the `assertNonAttributable` export
      you are about to import here, AFTER Task 1 has widened it. Task 1 must land first
      within this plan.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md`
      sections WR-02, WR-09 and IN-02 for the exact failure shapes.
  </read_first>

  <files>apps/run.gpx/webapp/src/app/api/gpx/public/heatmap/[year]/route.ts</files>

  <action>
Four edits to the public serve route. Keep the existing allowlist-first ordering and the
404-on-missing-object behaviour exactly as they are — both were verified correct.

(a) **WR-02 — prove the output.** Import `assertNonAttributable` from
`@/lib/heatmap-artifact`. After the successful `JSON.parse`, run it inside a try/catch;
on a throw, log a message that names the year and the fact of the failure but NOT the error
text, and return a 500 with the existing generic body. Separately, existence-check
`artifact.meta` before the `?meta` branch: an object that parses to an empty object
currently yields a 200 with an empty body and a JSON content type, which the studio then
fails to parse. Return the generic 500 instead. Add a comment explaining that the write-path
guard is the only thing between a bad object and the internet, so any write that bypasses
the two known writers — a manual object copy during an incident, a restore from backup, a
future second builder, a compromised uploads keypair — would otherwise publish unchecked;
and that the cost is one structural walk per ORIGIN request, which the dedicated cache
behaviour reduces to a handful per cache period.

(b) **IN-02 — exact meta test.** Change the `?meta=` branch from a truthiness test to an
exact string equality against the literal `1`. Update the neighbouring comment to state the
contract precisely.

(c) **WR-09 — log hygiene.** None of the three `console.error` calls may pass a caught
error object. Log a stable tag plus, at most, the error's `name` when it is an Error
instance and a fixed unknown token otherwise. Node's console inspects enumerable own
properties, and S3 exceptions carry request ids, response metadata and for several shapes
the bucket name and key — which is the exact pattern that leaks a per-user key the next
time this file is copied. Also drop the log line severity for the ordinary
unbuilt-year miss: that path is unauthenticated and an outsider can otherwise drive
CloudWatch error volume at will. Use a warn-level line, or none, for the plain
object-not-found case while keeping an error line for parse and guard failures.

(d) **CR-03 — correct the CDN comments.** The module comment claims each distinct query
value is its own CDN cache entry, and the `CACHE_SECONDS` comment claims the header buys
full CDN absorption. Neither was true when written: the catch-all region behaviour uses the
managed caching-disabled policy, so the origin's s-maxage was ignored and three consecutive
identical requests all missed. Rewrite both so they state WHERE the caching actually comes
from — a dedicated ordered cache behaviour for this path in
`infra/terraform/modules/cloudfront/v1.0.0/main.tf`, added by plan 71-13, whose cache key
whitelists exactly the `meta` query string — and state the consequence if that behaviour is
removed: the header becomes decorative again and every hit is an origin S3 read plus a
full-artifact response body off a single ECS task.
  </action>

  <acceptance_criteria>
    - Serve-path guard is wired: `grep -v '^\s*[/*]' 'apps/run.gpx/webapp/src/app/api/gpx/public/heatmap/[year]/route.ts' | grep -c "assertNonAttributable"` returns 2 (import + call).
      (Can go red: today the count is 0.)
    - Exact meta test: `grep -v '^\s*[/*]' '.../[year]/route.ts' | grep -cE 'get\("meta"\) === "1"'` returns 1.
      (Can go red: today the test is a bare truthiness check.)
    - `artifact.meta` is existence-checked: read the code and confirm a guard precedes the
      meta projection; `grep -v '^\s*[/*]' '.../[year]/route.ts' | grep -cE "!artifact.meta|artifact.meta ==" ` returns >= 1.
    - **No raw error object reaches a log call.** Comment-stripped source must contain zero
      `console.` calls whose final argument is a bare `error`:
      `grep -v '^\s*[/*]' '.../[year]/route.ts' | grep -cE "console\.(error|warn|log)\([^)]*, *error\)"` returns 0.
      (Can go red: three such calls exist today at lines 71, 84 and 98.)
    - `error.name` (or equivalent narrowing) is used instead:
      `grep -v '^\s*[/*]' '.../[year]/route.ts' | grep -c "instanceof Error"` returns >= 1.
    - The allowlist ordering is unchanged — `isHeatmapYear` is still called before
      `heatmapArtifactKey`. Confirm by reading; and
      `grep -v '^\s*[/*]' '.../[year]/route.ts' | grep -c "isHeatmapYear"` returns **2**
      (the import at :5 and the guard call at :58 — this task removes neither).
      🚨 SECURITY: this criterion asserts the allowlist gate is STILL PRESENT. If it reads
      anything other than 2, investigate — do NOT delete an occurrence to reach a number.
      Removing the `:58` call would drop the year allowlist that must run before
      `heatmapArtifactKey` constructs an S3 key from an untrusted URL segment.
    - The CDN comments now name their dependency:
      `grep -ci "cloudfront" '.../[year]/route.ts'` returns >= 1 AND
      `grep -c "71-13" '.../[year]/route.ts'` returns >= 1.
    - `npx tsc --noEmit` from `apps/run.gpx/webapp` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/webapp && npx tsc --noEmit && npx vitest run src/lib/heatmap-artifact.test.ts</automated>
  </verify>

  <done>The route asserts non-attributability on read and 500s on failure; an empty parsed object 500s instead of returning a 200 with no body; `?meta=1` is exact; no SDK error object is logged; the CDN comments name the behaviour that makes them true and the consequence of its removal; `tsc --noEmit` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 object -> public HTTP response | The serve route reads an object and echoes it to the open internet |
| assembler input tracks -> published artifact | Untrusted-shaped geometry from DynamoDB rows and a foreign-account export |
| public internet -> serve route | Unauthenticated GET with an attacker-chosen query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-10-01 | Information disclosure | `assertNonAttributable` blind spots (`meta`, `coordinates`, root `type`) | high | mitigate | Task 1 ports the three missing checks from the standalone verifier into the runtime guard and pins each with a test that fails when the check is removed. |
| T-71-10-02 | Information disclosure | Any write that bypasses the two known writers is served unvalidated | high | mitigate | Task 3 runs the guard on the serve path; a failing object returns 500 instead of publishing. |
| T-71-10-03 | Information disclosure | Exact start/end coordinates at ~1.1 m for non-consenting runners (CR-02) | high | **accept** | Kurt's explicit call 2026-07-31 (**D-14**) with the exposure stated plainly. NOT mitigated by design. Task 1(c) records the acceptance, the residual property gap ("no identifier fields" != "not re-identifiable"), and an instruction that reversal needs a new user decision. |
| T-71-10-04 | Information disclosure | Raw SDK error objects in CloudWatch carry request ids, response metadata, bucket and key | medium | mitigate | Task 3(c) logs a tag plus `error.name` only; pinned by a negative grep on comment-stripped source. |
| T-71-10-05 | Denial of service | Unauthenticated path emits an error log per miss; outsider drives log volume | low | mitigate | Task 3(c) demotes the ordinary unbuilt-year miss below error level. |
| T-71-10-06 | Tampering | Degenerate geometry inflates a publicly-served `runCount` by 22% | medium | mitigate | Task 1(b) filters at the single assembly point; Task 2 makes the verifier able to catch it; plan 71-15 rebuilds the frozen DC33 artifact. |
</threat_model>

<verification>
1. `cd apps/run.gpx/webapp && npx vitest run` — full suite. Baseline is 303 passed /
   1 skipped; the new count must be strictly higher with zero failures and zero newly
   skipped. Requires Node >= 22.12 (`nvm use 22.12.0`).
2. `cd apps/run.gpx/webapp && npx tsc --noEmit` — exit 0.
3. `cd apps/run.gpx/webapp && node scripts/verify-heatmap-artifact.mjs --selftest` — exit 0.
4. Run the verifier against the CURRENT live DC33 artifact:
   `node scripts/verify-heatmap-artifact.mjs https://gpx.defcon.run/use1/api/gpx/public/heatmap/dc33`
   It MUST now exit NON-ZERO, naming a degenerate feature. That non-zero exit is the proof
   the check works against real production bytes, and it is the trigger for plan 71-15.
   Record the exit code and the first failing feature index in the SUMMARY.
5. Do NOT run any lint gate — eslint is non-functional in both packages (D-71-A/C).
</verification>

<success_criteria>
- The runtime guard rejects an unexpected meta key, a malformed coordinate and a wrong root type, each with a test.
- A never-moving track produces zero features; a real track still produces one.
- The serve route validates before it echoes and 500s on a bad object or a missing meta.
- `?meta=0` no longer returns meta.
- No `console.*` call in the serve route passes a caught error object.
- The verifier fails the live DC33 artifact (proving WR-06) and passes its own clean fixture.
- D-14 is recorded at `normalizeTrack`; no trim helper, no precision change, no opt-in predicate.
</success_criteria>

<output>
Create `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-10-SUMMARY.md` when done.

Record in the SUMMARY: (a) the pre-implementation test failure count for the new tests,
(b) the verifier's exit code and first degenerate feature index against the live DC33 URL —
plan 71-15 consumes both.

## Artifacts this phase produces (this plan)

| Kind | Name | File |
|------|------|------|
| new constant | `META_KEYS` (runtime guard) | `src/lib/heatmap-artifact.ts` |
| widened function | `assertNonAttributable` (+meta, +coordinates, +root type) | `src/lib/heatmap-artifact.ts` |
| new filter | degenerate-track drop in `assembleHeatmapArtifact` | `src/lib/heatmap-artifact.ts` |
| new comment block | D-14 accepted-risk record at `normalizeTrack` | `src/lib/heatmap-artifact.ts` |
| new check | degeneracy clause in `geometry is bounded LineString` | `scripts/verify-heatmap-artifact.mjs` |
| new fixture | degenerate doctored self-test fixture | `scripts/verify-heatmap-artifact.mjs` |
| new import + call | `assertNonAttributable` on the serve path | `src/app/api/gpx/public/heatmap/[year]/route.ts` |
</output>
