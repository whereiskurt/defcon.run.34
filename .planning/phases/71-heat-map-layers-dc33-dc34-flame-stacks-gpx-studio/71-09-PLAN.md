---
phase: 71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio
plan: 09
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts
  - apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/HeatMap.svelte
autonomous: true
requirements: [HEAT-04, HEAT-05]

must_haves:
  truths:
    - "HEAT_STROKE line-opacity is 0.70 in the shipped source, line-width is still 3, and both HEAT_PAINT colours are byte-identical to what shipped (#ff8c00 dc33, #ff0000 dc34) — D-13."
    - "Toggling a year whose artifact is valid but has zero features latches `built` for that year, so a second toggle issues NO second network fetch (WR-07)."
    - "A year with runCount 0 renders its row with visible 'no runs yet' wording in the hint, so a checked box that draws nothing is explained rather than silently broken (WR-07)."
    - "HeatmapLayer.remove() leaves heatmapState blank rather than stale, and whenStyleReady() always settles (IN-05)."
  artifacts:
    - "apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts"
    - "apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/HeatMap.svelte"
  key_links:
    - "HEAT_PAINT -> HeatMap.svelte row swatch (unchanged colour constant is the swatch source)"
    - "heatmapState -> HeatMap.svelte `shown` derivation (availability still gates row rendering)"
  prohibitions:
    - "NO change to either HEAT_PAINT line-color value. D-02/D-13 lock #ff8c00 and #ff0000."
    - "NO dark casing / under-stroke layer, NO second line layer per year, NO runtime-tunable paint knob — all three were offered to Kurt and declined."
    - "NO change to `available` that hides the DC34 row while the artifact is empty. That would turn currently-GREEN probe assertions 9, 10 and 12 red and remove the feature's visible promise five days before the con."
    - "NO call to any fitBounds / flyTo / easeTo / setCenter in this file. The restore path must never move the camera (Phase 70 landmine, truth #18)."

---

<objective>
Close the phase's headline verification failure: at `line-opacity 0.25` the DC33 flame
stack is not faint, it is invisible, so "every run a translucent line ... so overlap =
heat" is false of the shipped build. Kurt locked the fix on 2026-07-31 as **D-13** —
opacity `0.25 -> 0.70`, width stays 3, colours unchanged.

While in this file, close the two studio-side review findings that travel with it:
**WR-07** (the DC34 checkbox latches ON, draws nothing, gives no feedback, and re-fetches
the whole artifact on every subsequent toggle) and **IN-05** (`remove()` leaves the store
stale; `whenStyleReady()` can never settle).

Purpose: this is the one change that makes the feature visible to a runner at all. Every
other plan in this gap-closure set protects availability; this one restores the product.

Output: a legible DC33 stack, a DC34 row that behaves honestly while its year is empty,
and a layer class that cannot wedge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-CONTEXT.md
@.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise HEAT_STROKE line-opacity to 0.70 and rewrite the paint rationale (D-13)</name>

  <read_first>
    - `apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts` lines 71-98
      (the `HeatStroke` type, the `HEAT_STROKE` constant and its justification comment, and
      `HEAT_PAINT`). The comment currently argues FOR 0.25 by citing `public-overlays.ts`
      `addAggregate()` at 0.15/width 2 — that argument is now falsified by measurement and
      must be replaced, not merely edited around.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-CONTEXT.md`
      section `## Gap-Closure Decisions (user-locked 2026-07-31)` -> **D-13**. This is the
      binding text; the new comment must reproduce its reasoning, including the accepted
      trade-off and the known-but-unfixed basemap-contrast factor.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-VERIFICATION.md`
      gap #1 — the controlled-capture evidence (`shot-dc33-SHIPPED-0.25-invisible.png` vs
      `shot-dc33-DIAG-0.70-legible.png`) that justifies the number.
  </read_first>

  <files>apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts</files>

  <action>
Change the `HEAT_STROKE` constant's `'line-opacity'` from `0.25` to `0.70`. Leave
`'line-width': 3` exactly as it is. Do NOT touch either `'line-color'` value in
`HEAT_PAINT` — per D-13 the year identity is unchanged.

Replace the JSDoc block above `HEAT_STROKE` with an honest rationale that records, in
prose, all of the following (no code fences, no fenced blocks):

  - the value is 0.70 and was confirmed by Kurt on 2026-07-31 (D-13) after a measured
    0.25 / 0.45 / 0.70 sweep at a 40-run hotspot with every non-heat layer hidden;
  - the shipped 0.25 was not faint but perceptually absent over the Mapbox basemap — the
    controlled capture at 0.25 is indistinguishable from a bare basemap, the same camera
    and the same data at 0.70 shows a real density gradient;
  - the accepted trade-off: at 0.70 one line is fairly opaque, so overlap saturates sooner
    and the gradient is coarser than a true low-alpha stack. Legibility beat fidelity;
  - a known contributing factor that is deliberately NOT being fixed: the DC33 hue sits on
    top of the basemap's own orange road casings, so DC33 contrast is imperfect at any
    opacity. A colour change, a dark casing/under-stroke and a runtime-tunable paint knob
    were each offered and declined — do not add them;
  - the discretion note: exact opacity/width tuning was always Claude's Discretion in
    71-CONTEXT.md, with 0.25 only a suggested start, so 0.70 is a tuning, not a reversal.

Delete the now-false sentence that cites `addAggregate()`'s 0.15/width-2 as the reason the
value sits low. That comparison is what produced the invisible render; leaving it in place
invites the next reader to lower the number again.
  </action>

  <acceptance_criteria>
    - `grep -c "'line-opacity': 0.7" apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts` returns 1.
      (Can go red: the literal is currently `0.25`.)
    - Comment-stripped source still carries width 3 and neither colour moved. Run:
      `grep -v '^\s*[/*]' apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts | grep -c "'line-width': 3"` returns 1;
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "#ff8c00"` returns 1;
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "#ff0000"` returns 1.
      (Can go red: any colour edit, any width edit, or a colour moved into a comment.)
    - Comment-stripped source contains exactly ONE `line-opacity` VALUE assignment:
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -cE "'line-opacity': [0-9]"` returns 1.
      (Can go red: adding a casing layer or a second stroke would introduce a second value.
      NOTE: do NOT grep for the bare token `line-opacity` and expect 1 — the `HeatStroke`
      type declares `'line-opacity': number;` as a field, so a CORRECT implementation has
      two non-comment occurrences and a bare-token count of 1 would false-red.)
    - `grep -c "0.25" .../heatmap-layer.ts` returns 0 outside the new rationale prose — i.e.
      the old value survives only as narrative history, never as an assignment. Verify with
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "0.25"` returning 0.
    - `grep -c "D-13" .../heatmap-layer.ts` returns >= 1 — the decision is cited at the
      constant so a future reader finds the authority before re-tuning.
    - **The ROADMAP goal and the shipped constant agree.** `.planning/ROADMAP.md`'s Phase 71
      `**Goal:**` line states 70% opacity and cites D-13, and carries no unqualified "~25%"
      claim: `grep -c "25% opacity" .planning/ROADMAP.md` returns 0.
      (Already corrected during planning — this criterion exists so the executor CONFIRMS the
      agreement rather than assuming it. Can go red if the ROADMAP is regenerated or reverted.
      This matters: the goal sentence is what the next verifier reads, and a ROADMAP saying
      25% against a shipped 0.70 would reproduce the exact confusion that caused this gap —
      a discretionary value being read as locked.)
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output human 2>&1 | tail -5</automated>
  </verify>

  <done>`HEAT_STROKE` reads width 3 / opacity 0.70; both `HEAT_PAINT` colours byte-identical to the pre-change file; the rationale comment cites D-13, the measurement, the trade-off and the three declined alternatives; svelte-check shows no NEW error on this file versus the recorded 26+4 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: An empty-but-valid year is a legitimate layer, not a broken latch (WR-07)</name>

  <read_first>
    - `apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts` lines
      177-186 (`isFeatureCollection`, whose `features.length > 0` clause is the root cause),
      lines 202-238 (`loadMeta`, where `available` is derived), lines 254-299
      (`ensureGeometry`, which early-returns WITHOUT setting `built`), and lines 307-321
      (`setVisible`, which persists `visible: true` regardless).
    - `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/HeatMap.svelte`
      lines 39-45 (`detail()` builds the hint string; `shown` filters on `available`).
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md`
      section WR-07 for the exact four-step failure trace.
  </read_first>

  <files>
apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts,
apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/HeatMap.svelte
  </files>

  <action>
Two coordinated edits. The goal is: a year whose artifact exists but currently holds zero
runs behaves as an empty layer that the runner understands — NOT as a checkbox that latches
on, paints nothing, says nothing and re-downloads the artifact every time it is clicked.

(a) In `heatmap-layer.ts`, relax `isFeatureCollection` so it accepts a FeatureCollection
with an empty `features` array. Keep every other structural check exactly as it is: the
value must be a non-array object, `type` must equal the FeatureCollection literal, and
`features` must be an array. Only the length clause goes. Update the function's JSDoc to
say why: an empty year is a valid artifact awaiting data, and treating it as malformed is
what left `built` false and made every toggle re-fetch hundreds of KB. Emphasise that this
is a relaxation of a LIVENESS check, not of a STRUCTURAL one — the type and array checks
are the untrusted-input gate (T-71-21) and must not be loosened further.

With that clause gone, `ensureGeometry` will now create the source and layer for an empty
year, call `applyVisibility`, and set `built[year] = true` — which is the fix. Verify by
reading that no other early-return in `ensureGeometry` fires for an empty artifact.

(b) In `HeatMap.svelte`, make the zero-run case legible to the user. Extend `detail()` so
that when `s.runCount` is 0 the hint string ends with an explicit clause telling the runner
there is nothing to draw yet and that the layer fills in during the con — keep the existing
"Last built <exact> · N runs · N.N km" prefix intact so probe assertion 10, which requires
the hint to contain the word `runs` and a four-digit year, keeps passing. Do NOT change the
`shown` derivation and do NOT gate `available` on `runCount` — see the prohibition; hiding
the DC34 row would turn probe assertions 9, 10 and 12 red and delete the feature's
pre-con visibility.

Leave `available = meta !== null` in `loadMeta` exactly as it is.
  </action>

  <acceptance_criteria>
    - The length clause is gone from the structural gate:
      `grep -v '^\s*[/*]' apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts | grep -c "features.length > 0"` returns 0.
      (Can go red: it is present today at line 185.)
    - The structural half survives — comment-stripped source still checks the type and the
      array: `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "Array.isArray(o.features)"` returns 1
      AND `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "o.type === 'FeatureCollection'"` returns 1.
      (Can go red: an over-eager rewrite that deletes the whole guard.)
    - `available` is NOT content-gated:
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -cE "available *=.*runCount"` returns 0.
      (Can go red: this is precisely the tempting-but-wrong fix WR-07 suggests.)
    - The zero-run case produces different hint text than the populated case. Confirm by
      reading `detail()` and checking a `runCount === 0` branch exists:
      `grep -v '^\s*[/*]' apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/HeatMap.svelte | grep -cE "runCount === 0|runCount > 0"` returns >= 1.
    - The existing hint contract is unbroken — comment-stripped `HeatMap.svelte` still
      contains the `Last built` prefix and a `runs` token:
      `grep -v '^\s*[/*]' .../HeatMap.svelte | grep -c "Last built"` returns 1 and
      `grep -v '^\s*[/*]' .../HeatMap.svelte | grep -c "runs"` returns >= 1.
      (Can go red: a rewrite of `detail()` that drops the prefix breaks probe assertion 10.)
    - The `shown` derivation is untouched:
      `grep -v '^\s*[/*]' .../HeatMap.svelte | grep -c "ROWS.filter((r) => \$heatmapState\[r.year\].available)"` returns 1.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output human 2>&1 | tail -5</automated>
  </verify>

  <done>An empty artifact builds a real (empty) source+layer and sets `built`, so a second toggle issues no second fetch; the DC34 row still renders and still toggles; its hint tells the runner why nothing is drawn; svelte-check shows no NEW error on either changed file.</done>
</task>

<task type="auto">
  <name>Task 3: remove() clears the store and whenStyleReady() always settles (IN-05)</name>

  <read_first>
    - `apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts` lines
      116-134 (`blankState`, `blankFlags`, the `heatmapState` writable), lines 197-200
      (`whenStyleReady`), lines 323-332 (`remove`).
    - `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte`
      — the `map.onLoad` block that calls `remove()` then re-creates the layer and
      `void loadMeta()`s. Read it to confirm the teardown ordering before changing `remove()`.
    - `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-REVIEW.md`
      section IN-05.
  </read_first>

  <files>apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts</files>

  <action>
Two small robustness fixes, both in `heatmap-layer.ts`.

(a) `remove()` currently resets the private `built` and `visible` flag maps but leaves the
exported `heatmapState` store holding the torn-down layer's availability, so between a
teardown and the next `loadMeta()` the HEAT MAP section renders rows for layers that no
longer exist. Have `remove()` finish by setting `heatmapState` back to a blank state via
the existing `blankState()` helper. Add a one-line comment saying the store is documented
as the section's source of truth, so a teardown that leaves it populated makes that
documentation false.

(b) `whenStyleReady()` resolves only from a one-shot map idle listener. If the map never
reaches idle — a style load that fails, a tab backgrounded before first idle — the promise
never settles, the await inside `ensureGeometry` never returns, and that year's toggle
never persists, with no timeout and no rejection. Race the idle listener against a timeout
so the caller always makes progress: on timeout, resolve (do NOT reject — the callers treat
this as a readiness hint and a rejection would turn a slow style into a silent catch).
Choose a bound in the 8-15 second range and name it as a module-level constant with a
comment explaining that resolving-on-timeout is deliberate and that the subsequent
`getSource`/`getLayer` guards already tolerate a not-quite-ready style.

Do not otherwise change `ensureGeometry`, `setVisible` or `applyVisibility`.
  </action>

  <acceptance_criteria>
    - `remove()` resets the store — comment-stripped source contains a `heatmapState.set(blankState())`:
      `grep -v '^\s*[/*]' apps/run.gpx/gpx-studio/website/src/lib/components/map/heatmap-layer.ts | grep -c "heatmapState.set(blankState())"` returns >= 2
      (one in the existing `loadMeta` initialiser path is `heatmapState.set(next)`, so this
      specific literal appearing at all is new — a return of 0 means the fix is absent).
      Re-express precisely: the literal `blankState()` must appear inside the `remove` method
      body. Confirm by reading `remove()` and by
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "blankState()"` returning >= 3
      (declaration site, `heatmapState = writable<...>(blankState())`, and the new call).
      (Can go red: today the count is 2.)
    - `whenStyleReady` no longer has a lone unraced promise — comment-stripped source
      contains a timeout in that method:
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -cE "setTimeout"` returns >= 1.
      (Can go red: today the count is 0.)
    - The timeout resolves rather than rejects:
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -c "reject"` returns 0.
      (Can go red: a `new Promise((resolve, reject) =>` that rejects on timeout would turn
      the awaiting `ensureGeometry` into a silent catch and lose the layer.)
    - No camera call was introduced anywhere in the file:
      `grep -v '^\s*[/*]' .../heatmap-layer.ts | grep -cE "fitBounds|flyTo|easeTo|setCenter|jumpTo"` returns 0.
      (Can go red: this is the Phase 70 restore landmine and the file must stay clean of it.)
  </acceptance_criteria>

  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output human 2>&1 | tail -5</automated>
  </verify>

  <done>`remove()` blanks `heatmapState`; `whenStyleReady()` races idle against a named timeout and always resolves; no camera-moving call exists in the file; svelte-check shows no NEW error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public artifact JSON -> studio | Unauthenticated, attacker-influenceable-if-S3-is-breached JSON reaches `addSource` |
| persisted localStorage -> layer restore | A user-writable store drives which layers auto-enable on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-09-01 | Tampering | `isFeatureCollection` relaxation in `heatmap-layer.ts` | medium | mitigate | Only the LENGTH clause is removed. The `type === "FeatureCollection"` and `Array.isArray(features)` checks stay, and no feature attribute is ever read by this module, so a tampered artifact still cannot inject a value into the DOM. Pinned by an acceptance grep that both surviving checks are present in comment-stripped source. |
| T-71-09-02 | Denial of Service | `whenStyleReady` unsettled promise | low | mitigate | A never-idle map today wedges the toggle forever. Task 3 races a named timeout that resolves, so the caller always progresses. |
| T-71-09-03 | Information disclosure | new hint text for the zero-run case | low | accept | The hint renders only `runCount`, `totalKm` and `generatedAt`, all already public at `?meta=1`. No new field is surfaced. |
</threat_model>

<verification>
1. `cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json`
   — DELTA gate, not an absolute count. Compare against the recorded pre-existing baseline
   (26 errors + 4 env-dependent, D-71-E). Zero NEW errors attributable to the two files
   this plan touches.
2. `./apps/run.gpx/build-frontend.sh` completes with exit 0 — the studio bundle is what
   ships, and a svelte-check pass does not prove the Vite build.
3. Re-read the diff and confirm no `line-color` byte moved.
</verification>

<success_criteria>
- `HEAT_STROKE` is width 3 / opacity 0.70; both colours unchanged; D-13 cited in-file.
- An empty-but-valid year builds its layer, latches `built`, and stops re-fetching.
- The DC34 row still renders, still toggles, and now explains its own emptiness.
- `remove()` blanks the store; `whenStyleReady()` cannot hang.
- svelte-check delta is 0 new errors; `build-frontend.sh` exits 0.
</success_criteria>

<output>
Create `.planning/phases/71-heat-map-layers-dc33-dc34-flame-stacks-gpx-studio/71-09-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)

| Kind | Name | File |
|------|------|------|
| changed constant | `HEAT_STROKE['line-opacity']` 0.25 -> 0.70 | `heatmap-layer.ts` |
| relaxed predicate | `isFeatureCollection` (length clause removed) | `heatmap-layer.ts` |
| new constant | style-readiness timeout (name chosen by executor, 8-15s) | `heatmap-layer.ts` |
| changed method | `HeatmapLayer.remove()` now blanks `heatmapState` | `heatmap-layer.ts` |
| changed function | `detail()` zero-run branch | `HeatMap.svelte` |
</output>
