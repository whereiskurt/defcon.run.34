---
phase: 65-ctf-single-use-otp-flag-option
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - apps/run.human/webapp/src/lib/ctf-judge.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-judge-otp-singleuse.test.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-otp-singleuse-covert-invariant.test.ts
autonomous: true
requirements: [CTFT-15, CTFT-17]

must_haves:
  truths:
    - "Two concurrent submissions of the SAME valid code by two different logged-in players yield EXACTLY ONE scoring solve and one NON_SOLVE — proven by a race test over the create-if-absent claim seam (SC1)."
    - "`otp.singleUse` absent/false preserves today's SHARED behavior exactly — multiple distinct players submitting the same valid code EACH score (regression test); the existing repeatable time-bucket path is untouched (SC2)."
    - "A used/consumed code (and the winner re-submitting within the window) returns the SAME NON_SOLVE + same `ctfJudgeLog('no-solve')` a wrong code yields, with NO double-accrue; the guess/codeHash is never passed to the logger (SC4)."
    - "The claim row carries a DynamoDB TTL = floor(now/1000)+period·(skew+2); the winner scores through the EXISTING recordScoreEvent(bucket=codeHash)/accrue path; enforcement lives ONLY in judgeSolve — no resolver/.mjs change (SC3)."
    - "The covert CSS path stays byte-identical (grep gate); static/otp(shared)/wordlist flags are unaffected by the new branch (SC4)."
  artifacts:
    - "apps/run.human/webapp/src/lib/ctf-judge.ts — JudgeOtp.singleUse + narrowCtf otp passthrough + CtfStore.claimOtpCode? + defaultStore.claimOtpCode + the single-use OTP finalize block before isRepeatable."
    - "apps/run.human/webapp/src/lib/__tests__/ctf-judge-otp-singleuse.test.ts — race, cross-user second⇒NON_SOLVE, winner-resubmit⇒NON_SOLVE no double-accrue, TTL set, shared-OTP regression, indistinguishable non-solve."
    - "apps/run.human/webapp/src/lib/__tests__/ctf-otp-singleuse-covert-invariant.test.ts — covert-file grep gate for singleUse/CtfOtpClaim/claimOtpCode."
  key_links:
    - "The single-use finalize runs AFTER the shared step-4 verifyTotp gate — so an invalid code is a NON_SOLVE BEFORE any claim; the claim only ever consumes a currently-valid code."
    - "The finalize keys the ledger row by codeHash (bucket=codeHash) through the EXISTING upsert recordScoreEvent — the same shape the wordlist finalize uses — so no new scoring/accrue code is introduced."
    - "The judge test fake's claimOtpCode backs onto 65-01's pure `applyOtpClaim`, so the offline race model and the pure gate share one source of truth."
---

<objective>
Wire the judge's single-use OTP path (CTFT-17 + the JudgeOtp half of CTFT-15): add `singleUse` to `JudgeOtp`, carry it through `narrowCtf`, add the optional atomic `claimOtpCode` store op (a `CtfOtpClaim.create` create-if-absent conditional put with catch discipline mirroring `claimSolve`), and add a dedicated single-use OTP finalize block guarded by `answerType === "otp" && otp?.singleUse === true`, placed BEFORE the generic `isRepeatable` block and running AFTER the existing step-4 `verifyTotp` gate. The winner scores through the existing `recordScoreEvent(bucket=codeHash)`/`accrue` path with a claim-row TTL; a lost/consumed code (and a winner re-submit) is a NON_SOLVE indistinguishable from a wrong answer. Enforcement is judge-only; the resolver Lambda / `.mjs` mirrors are never touched. Default-off (singleUse absent) leaves today's shared repeatable OTP path byte-for-byte unchanged — proven by a regression test.

Purpose: Deliver the concurrency-correct heart of the feature — first-come-first-served global single-use for a rotating-OTP flag — additively, reusing the shipped wordlist finalize shape verbatim, without disturbing shared OTP / static / wordlist flags or the covert channel.
Output: `JudgeOtp.singleUse` + `narrowCtf` passthrough + `CtfStore.claimOtpCode?` + `defaultStore.claimOtpCode` + the single-use OTP finalize in `judgeSolve`; a single-use OTP judge test (race + cross-user + winner-resubmit + TTL + shared regression + indistinguishability) and a covert-invariant grep gate.
</objective>

<artifacts_produced>
New symbols this plan introduces:
- `JudgeOtp.singleUse?: boolean` — the scoring-time view of the config flag (mirrors `Ctf.otp.singleUse` from 65-01).
- `CtfStore.claimOtpCode?(args: { challenge; codeHash; user; claimedAt; ttl }): Promise<{ claimed: boolean; claimedBy?: string }>` — optional/additive atomic single-use claim (absent op ⇒ locked NON_SOLVE, same discipline as `claimCode`/`claimScoreEvent`).
- `defaultStore.claimOtpCode` — `CtfOtpClaim.create({ challenge, codeHash, claimedBy: user, claimedAt, ttl })` (ElectroDB `create` ⇒ `attribute_not_exists` on the key ⇒ conditional put). On a condition failure, read the existing row: present ⇒ `{ claimed: false, claimedBy }`; absent ⇒ rethrow (degrade to NON_SOLVE) — mirrors `claimSolve`.
- The single-use OTP finalize block in `judgeSolve` (guarded, placed before `isRepeatable`).
- `narrowCtf` otp coercion widened to carry `singleUse`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Vitest requires Node 22.12 in this worktree (per 65-CONTEXT — NOT 22.1.0 / 23.6.0). If 65-01 already ran `npm ci` in this worktree it is installed; otherwise:
`cd apps/run.human/webapp && nvm use 22.12.0 && npm ci`
Targeted run:
`npx vitest run src/lib/__tests__/ctf-judge-otp-singleuse.test.ts src/lib/__tests__/ctf-otp-singleuse-covert-invariant.test.ts`
Run the FULL judge suite before finishing (no regression on shared OTP / static / wordlist):
`npx vitest run src/lib/__tests__/ctf-judge.test.ts src/lib/__tests__/ctf-judge-gates.test.ts src/lib/__tests__/ctf-judge-wordlist.test.ts src/lib/__tests__/ctf-flag-types.test.ts src/lib/__tests__/ctf-reward-covert-invariant.test.ts src/lib/__tests__/ctf-wordlist-covert-invariant.test.ts`
`npx tsc --noEmit` may carry pre-existing out-of-scope errors — assert NO NEW errors on `ctf-judge.ts`.
Depends on 65-01: `CtfOtpClaim` entity, the `ctf-otp-claim` pure helper (`otpCodeHash`/`otpClaimTtlSeconds`/`applyOtpClaim`), and `Ctf.otp.singleUse` must exist first.
Do NOT switch or create git branches. Do NOT touch the resolver Lambda or any `.mjs` mirror.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-ctf-single-use-otp-flag-option/65-CONTEXT.md
@docs/superpowers/specs/2026-07-18-single-use-otp-ctf-design.md

# The judge: the step-4 verifyTotp gate (~:374), the codeHash line (~:371), the SHIPPED wordlist
# finalize (~:409-438, the verbatim analog), the isRepeatable block (~:447), CtfStore (~:145),
# defaultStore.claimCode (~:797, the create/patch + catch analog), narrowCtf otp (~:594).
@apps/run.human/webapp/src/lib/ctf-judge.ts
@apps/run.human/webapp/src/lib/ctf-hash.ts
@apps/run.human/webapp/src/lib/ctf-otp-claim.ts
@apps/run.human/webapp/src/lib/ctf-otp.ts
@apps/run.human/webapp/src/entities/ctf.ts
@apps/run.human/webapp/src/lib/__tests__/ctf-judge-gates.test.ts
@apps/run.human/webapp/src/lib/__tests__/ctf-wordlist-covert-invariant.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JudgeOtp.singleUse, narrowCtf passthrough, and the atomic claimOtpCode store op + race proof</name>
  <files>apps/run.human/webapp/src/lib/ctf-judge.ts, apps/run.human/webapp/src/lib/__tests__/ctf-judge-otp-singleuse.test.ts</files>
  <behavior>
    - Two concurrent `judgeSolve` calls (Promise.all) by two DIFFERENT users with the SAME valid current TOTP code on a `singleUse` flag → EXACTLY one `solved:true` (points>0) and one NON_SOLVE; exactly one `accrue`; exactly one ledger row keyed by codeHash. (The fake `claimOtpCode` backs onto 65-01's `applyOtpClaim` over a shared Map — presence-check and set with NO await between, modeling attribute_not_exists on the key.)
    - `claimOtpCode` for a codeHash already claimed → `{ claimed: false, claimedBy: <winner> }`.
    - The claim args carry a `ttl` equal to `otpClaimTtlSeconds(now, otp)` (asserted by capturing the fake's received args).
  </behavior>
  <read_first>
    - `apps/run.human/webapp/src/lib/ctf-judge.ts` — the `CtfStore` interface incl. the OPTIONAL `claimCode?` op (~:234-255) as the shape/discipline template for `claimOtpCode?`; `defaultStore.claimCode` (~:797) as the create/patch + catch-discipline analog (single-use OTP uses `.create()` create-if-absent, NOT `.patch()`); the `JudgeOtp` interface (~:94); `narrowCtf`'s inline `otp` param type (~:594) and its `otp: row.otp` passthrough (~:637).
    - `apps/run.human/webapp/src/lib/ctf-otp-claim.ts` (65-01) — `otpCodeHash`, `otpClaimTtlSeconds`, `applyOtpClaim`, `resolveOtpClaimOutcome`.
    - `apps/run.human/webapp/src/lib/__tests__/ctf-judge-gates.test.ts` — the `makeStore` in-memory fake (~:72), `OTP_SECRET`/`OTP_PERIOD`, `totpAt`, and `wrongOtpCode` (~:163). Copy these to build a single-use OTP fixture + fake.
    - `apps/run.human/webapp/src/entities/ctf.ts` — `CtfOtpClaim` (65-01) and its `(challenge, codeHash)` key + `ttl`.
  </read_first>
  <action>
    Add `singleUse?: boolean;` to the `JudgeOtp` interface (~:94) with a one-line comment: true ⇒ the first-come single-use finalize; absent/false ⇒ the shared repeatable path (unchanged). Widen `narrowCtf`'s inline `otp` param type (~:594) to include `singleUse?: boolean` — the existing `otp: row.otp` passthrough (~:637) then carries it verbatim (confirm no coercion is needed; `singleUse` rides along on the map).

    Extend the `CtfStore` interface with an OPTIONAL op, documented like `claimCode`: `claimOtpCode?(args: { challenge: string; codeHash: string; user: string; claimedAt: string; ttl: number }): Promise<{ claimed: boolean; claimedBy?: string }>`. Document it as the ATOMIC single-use OTP claim — a create-if-absent conditional put on `(challenge, codeHash)` (unlike `claimCode`, which is patch-if-exists over a pre-loaded pool; single-use OTP has NO pool). The FIRST concurrent claimer of a code wins (`claimed:true`); a consumed code, or an absent op, reads as `claimed:false` (the indistinguishable NON_SOLVE). Catch discipline mirrors `claimSolve`: a claim collision ⇒ `{claimed:false, claimedBy}`; any OTHER error rethrows so `judgeSolve` degrades to a non-solve. Keep it OPTIONAL so a store built for the static/otp-shared path stays type-clean. NEVER log the codeHash/guess.

    Implement `defaultStore.claimOtpCode`: `try { await CtfOtpClaim.create({ challenge, codeHash, claimedBy: user, claimedAt, ttl }).go(); return { claimed: true }; } catch (err) { const existing = await CtfOtpClaim.get({ challenge, codeHash }).go(); if (!existing.data) throw err; return { claimed: false, claimedBy: existing.data.claimedBy }; }`. Place it beside `claimCode` (~:797). Add a comment: `create` adds `attribute_not_exists` on the key ⇒ two concurrent claimers of the same code collide and EXACTLY one wins (no read-then-write race); a condition failure with a present row is a legitimate loss, an absent row means the failure was NOT a claim collision ⇒ rethrow. Never log codeHash/guess.

    Import `otpCodeHash`, `otpClaimTtlSeconds`, and `applyOtpClaim` from `./ctf-otp-claim` at the top of `ctf-judge.ts` (add `applyOtpClaim` only if the finalize/tests reference it directly; the finalize itself uses `otpCodeHash`/`otpClaimTtlSeconds`, so at minimum import those two — the test file imports `applyOtpClaim`).

    Create `src/lib/__tests__/ctf-judge-otp-singleuse.test.ts`. Build a `singleUseOtpCtf()` fixture (answerType "otp", `otp: { secret: OTP_SECRET, digits: 6, period: OTP_PERIOD, skew: 1, singleUse: true }`, flat scoring). Build an in-memory store fake modeled on `makeStore` but whose `claimOtpCode` backs onto a shared `Map` via 65-01's `applyOtpClaim` (capturing the received `ttl`), and whose `recordScoreEvent`/`accrue`/`allocateOrdinal` count calls + rows. In THIS task cover ONLY the claim seam: `Promise.all` of two `judgeSolve` calls by `u1`/`u2` with the SAME valid current code → exactly one `solved:true` + one NON_SOLVE, exactly one `accrue`, exactly one ledger row keyed by the codeHash; and assert the captured `ttl` equals `otpClaimTtlSeconds(now, otp)`. (The finalize consumed here lands in Task 2 — write these RED-first against the intended behavior, then Task 2 turns them green.)
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 22.12.0 && npx vitest run src/lib/__tests__/ctf-judge-otp-singleuse.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>`JudgeOtp.singleUse` + `narrowCtf` passthrough + `CtfStore.claimOtpCode` + `defaultStore.claimOtpCode` exist; the race test asserts exactly-one-winner under concurrent same-code submission and the correct TTL on the claim args.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the single-use OTP finalize block (validate-by-verifyTotp, then atomic claim + score)</name>
  <files>apps/run.human/webapp/src/lib/ctf-judge.ts, apps/run.human/webapp/src/lib/__tests__/ctf-judge-otp-singleuse.test.ts</files>
  <behavior>
    - answerType "otp" + singleUse + a valid current code, unclaimed → solved:true, points>0 (via computePoints), one CtfScoreEvent ledger row keyed by codeHash, one accrue; the result carries `effect` only when points>0.
    - A SECOND player submitting the SAME valid code → the EXACT NON_SOLVE a wrong code returns + the SAME `ctfJudgeLog('no-solve')` payload (no extra fields; guess/codeHash never in the log args).
    - The WINNER re-submitting the same code within its window → NON_SOLVE, and accrue is called EXACTLY ONCE total (no double-accrue).
    - An invalid/wrong code on a singleUse flag → NON_SOLVE and `claimOtpCode` is NEVER called (verifyTotp gate short-circuits before any claim).
    - globalMax exceeded on a singleUse claim → solved:true/points:0/capped, no accrue.
    - REGRESSION (default-off): a shared OTP flag (`singleUse` absent) with two DIFFERENT users submitting the SAME valid code → BOTH score (each writes its own time-bucket CtfScoreEvent); `claimOtpCode` is never called.
  </behavior>
  <read_first>
    - `apps/run.human/webapp/src/lib/ctf-judge.ts` — the step-4 answer-validation dispatch (the `if (ctf.answerType === "otp")` verifyTotp branch, ~:374) and the shared `if (!ok)` NON_SOLVE (~:402); the `scoredAt` line (~:407); the SHIPPED WORDLIST finalize (~:409-438) — the VERBATIM shape to mirror (allocateOrdinal → globalMax → computePoints → recordScoreEvent(bucket) → accrue → effect-only-when-points>0); the generic `if (isRepeatable(ctf))` block (~:447) the new block must precede.
    - `apps/run.human/webapp/src/lib/ctf-otp-claim.ts` — `otpCodeHash`, `otpClaimTtlSeconds`.
  </read_first>
  <action>
    Add a dedicated single-use OTP finalize block in `judgeSolve`, placed AFTER the shared `if (!ok)` NON_SOLVE gate and the `scoredAt` line, and BEFORE the `if (isRepeatable(ctf))` block (adjacent to / right after the wordlist finalize — order is irrelevant since the guards are mutually exclusive). Guard it exactly: `if (ctf.answerType === "otp" && ctf.otp?.singleUse === true) { ... }`. Because it sits after the step-4 gate, `ok` is already true here — the code is a currently-valid TOTP; the claim only ever consumes a valid code. Inside:
    - `const codeHash = otpCodeHash(guess ?? "");` — the raw code (OTP has no covert `guessHash` path; verifyTotp needs the raw code, per the existing step-4 comment). Never log it.
    - `const ttl = otpClaimTtlSeconds(now, ctf.otp);`
    - `const claim = store.claimOtpCode ? await store.claimOtpCode({ challenge, codeHash, user, claimedAt: scoredAt, ttl }) : { claimed: false };`
    - `if (!claim.claimed) { log(ctfJudgeLog({ challenge, result: "no-solve" })); return NON_SOLVE; }` — a consumed code, the winner re-submitting (row exists ⇒ claimed:false), or an absent op ALL fall here, indistinguishable from a wrong answer. This is the winner-re-submit ⇒ NON_SOLVE / no-double-accrue decision (65-CONTEXT gray area) — do NOT re-accrue on any path.
    - Then mirror the wordlist finalize R3/R4 VERBATIM: `const n = await store.allocateOrdinal(challenge);` → if `(ctf.globalMax ?? 0) > 0 && n > ctf.globalMax` ⇒ `log('capped')` + return `{ solved:true, points:0, ordinal:n, firstBlood:false, capped:true }`; else `const points = computePoints(n, ctf, now); const capped = points === 0; const firstBlood = n === 1; const tierCeiling = activeTierCeiling(now, ctf.timeTiers) ?? ctf.pointMax;` → `if (store.recordScoreEvent) await store.recordScoreEvent({ challenge, user, bucket: codeHash, points, tierCeiling, channel });` → `await store.accrue({ user, points });` → `log(ctfJudgeLog({ challenge, result: capped ? "capped" : "solve" }));` → `return { solved:true, points, ordinal:n, firstBlood, capped, effect: points > 0 ? ctf.effect : undefined };`.

    Add a block comment mirroring the wordlist finalize note: this path bypasses `claimScoreEvent` — the `CtfOtpClaim` conditional create IS its single-use guard; `recordScoreEvent` MUST upsert (defaultStore.recordScoreEvent already upserts, so no row need pre-exist); no `perPlayerMax` gate (a globally single-use code is the natural bound — mirrors the wordlist finalize, per the 65-CONTEXT gray-area decision); never pass guess/codeHash to the logger. Do NOT modify the step-4 verifyTotp branch or the shared repeatable block — default-off (singleUse absent) must fall through to the UNCHANGED `isRepeatable` path.

    ⚠ COMMENT-TEXT DISCIPLINE: the covert grep gate (Task 3) negative-greps the covert source files for the tokens `singleUse`, `CtfOtpClaim`, and `claimOtpCode`. Those tokens legitimately appear here in `ctf-judge.ts` (NOT a covert file), so they are fine in this file — but do NOT introduce them into any covert file (covert-egg.ts, EggTrigger.tsx, CtfCelebration.tsx, ctf-covert-css.ts, assets/theme/route.ts).

    Extend `ctf-judge-otp-singleuse.test.ts` to green: valid code scores (points>0, one ledger row keyed by codeHash, one accrue, effect present when authored); a second user with the same code → the exact wrong-code NON_SOLVE + identical `ctfJudgeLog('no-solve')` payload (assert structural equality of the logged object and that neither guess nor codeHash appears in it); the winner re-submitting → NON_SOLVE with accrue called exactly once total; a wrong code → NON_SOLVE with `claimOtpCode` never called (spy/count); a globalMax-exceeded claim → capped/points:0/no-accrue. Add a SHARED-OTP REGRESSION test: reuse the `ctf-judge-gates.test.ts` shared-OTP fixture/fake (or a `singleUse:false`/absent variant) and assert two different users submitting the SAME valid code BOTH score and `claimOtpCode` is never called — the default-off path is byte-for-byte the existing repeatable behavior.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 22.12.0 && npx vitest run src/lib/__tests__/ctf-judge-otp-singleuse.test.ts src/lib/__tests__/ctf-judge-gates.test.ts src/lib/__tests__/ctf-judge-wordlist.test.ts src/lib/__tests__/ctf-judge.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>The single-use OTP finalize scores a valid unclaimed code via the codeHash-keyed ledger+accrue path, returns an indistinguishable NON_SOLVE (same shape + same log, no leak) for a consumed code and for the winner re-submit (no double-accrue), never claims on a wrong code, honors globalMax, and leaves the shared OTP path unchanged (regression green). Static/otp/wordlist suites stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Covert-invariant grep gate for the single-use OTP path</name>
  <files>apps/run.human/webapp/src/lib/__tests__/ctf-otp-singleuse-covert-invariant.test.ts</files>
  <read_first>
    - `apps/run.human/webapp/src/lib/__tests__/ctf-wordlist-covert-invariant.test.ts` — copy the disk-read + token-absence structure verbatim (the `COVERT_FILES` list at ~:23, the readFileSync loop, the per-file token-absence assertions).
  </read_first>
  <action>
    Create `ctf-otp-singleuse-covert-invariant.test.ts` following the `ctf-wordlist-covert-invariant.test.ts` structure. Reuse the SAME `COVERT_FILES` list (covert-egg.ts, EggTrigger.tsx, CtfCelebration.tsx, ctf-covert-css.ts, assets/theme/route.ts). Assert each covert file is readable AND references NONE of the single-use OTP tokens: `singleUse`, `CtfOtpClaim`, `claimOtpCode`. This proves the single-use OTP logic is confined to the visible judge/store path and never leaked into the covert CSS channel (SC4 — the covert path stays byte-identical; a used/consumed code stays indistinguishable; T-53-04-01 intact). Add a short header comment tying this to SC4 / the covert invariant.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 22.12.0 && npx vitest run src/lib/__tests__/ctf-otp-singleuse-covert-invariant.test.ts src/lib/__tests__/ctf-wordlist-covert-invariant.test.ts src/lib/__tests__/ctf-reward-covert-invariant.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>The new grep gate is green: no covert source file references any singleUse/CtfOtpClaim/claimOtpCode token; the existing wordlist + reward covert invariants stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player OTP code → judge | Untrusted rolling code crosses here; verified by verifyTotp, then hashed and atomically claimed — only for a credentialed session.user.id. |
| public resolver → (NOT crossed) | The single-use claim MUST NOT live in the public q.defcon.run resolver — anonymous traffic must never trigger a DynamoDB write. |
| judge → covert CSS channel | The covert path reads only solved+points; no single-use reason (used/consumed) may leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-02-01 | Tampering | double-claim race on one code | high | mitigate | The claim is a single `CtfOtpClaim.create` create-if-absent (`attribute_not_exists` on the key) — exactly one concurrent winner; proven by the Task 1/2 race test (SC1). |
| T-65-02-02 | Information disclosure | consumed-code distinguishability (covert channel) | high | mitigate | A consumed code, a winner re-submit, and an absent op ALL return the shared NON_SOLVE + identical `ctfJudgeLog('no-solve')`; guess/codeHash never passed to the logger; covert grep gate (Task 3) proves no leak (SC4 / T-53-04-01). |
| T-65-02-03 | Elevation of privilege | anonymous code consumption / DDB write from public traffic | high | mitigate | Enforcement lives ONLY in judgeSolve (authenticated); the resolver Lambda / `.mjs` mirrors are untouched (SC3). No DynamoDB write from public traffic. |
| T-65-02-04 | Elevation of privilege | double-accrue (winner re-submit) | high | mitigate | The claim row is the sole idempotency guard; a winner re-submit fails the create ⇒ NON_SOLVE with NO accrue; test asserts accrue called exactly once (SC4). |
| T-65-02-05 | Tampering | shared OTP flags silently change scoring | high | mitigate | The finalize is guarded by `otp.singleUse === true`; a default-off regression test proves multiple users still each score a shared code (SC2). |
| T-65-02-06 | Repudiation/Info | guess/codeHash logged | medium | mitigate | The finalize reuses `ctfJudgeLog` (no value param); tests assert guess/codeHash never appear in the log payload. |
| T-65-SC | Tampering (supply chain) | package installs | low | accept | No new packages — reuses `electrodb` + node `crypto` (via hashAnswer/otpCodeHash); legitimacy gate N/A. |
</threat_model>

<verification>
- `npx vitest run` on the new single-use test + covert gate + the full judge suite (ctf-judge, gates, wordlist, flag-types, both existing covert invariants) — all green.
- `npx tsc --noEmit` — no NEW errors on `ctf-judge.ts` (pre-existing out-of-scope excluded).
- Manual read-through: the single-use finalize returns BEFORE the `isRepeatable` block; it is guarded by `answerType === "otp" && otp?.singleUse === true`; it calls `claimOtpCode` (create-if-absent), NOT `claimScoreEvent`; it never calls `accrue` on a lost/re-submit path; `git diff` touches NO `.mjs` file and NO resolver path.
</verification>

<success_criteria>
- SC1: concurrent same-code submissions by two players → exactly one scoring solve + one NON_SOLVE (race test).
- SC2: default-off (singleUse absent) preserves shared behavior — multiple users each score the same code (regression); the repeatable path is untouched.
- SC3: claim row carries a DynamoDB TTL; enforcement is judge-only (no resolver/.mjs change); winner scores via recordScoreEvent(bucket=codeHash)/accrue.
- SC4: used/consumed/re-submit = indistinguishable NON_SOLVE (same log, no leak); covert path byte-identical (grep gate); static/otp-shared/wordlist unaffected.
- CTFT-17 satisfied; the JudgeOtp half of CTFT-15 satisfied.
</success_criteria>

<output>
Create `.planning/phases/65-ctf-single-use-otp-flag-option/65-02-SUMMARY.md` when done.
</output>
