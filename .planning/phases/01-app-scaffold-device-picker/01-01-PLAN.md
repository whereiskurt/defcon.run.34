---
phase: 01-app-scaffold-device-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.flash/webapp/package.json
  - apps/run.flash/webapp/tsconfig.json
  - apps/run.flash/webapp/next.config.ts
  - apps/run.flash/webapp/tailwind.config.js
  - apps/run.flash/webapp/postcss.config.mjs
  - apps/run.flash/webapp/eslint.config.mjs
  - apps/run.flash/webapp/src/styles/globals.css
  - apps/run.flash/webapp/src/config/fonts.ts
  - apps/run.flash/webapp/src/config/site.ts
  - apps/run.flash/webapp/src/config/auth.ts
  - apps/run.flash/webapp/src/app/providers.tsx
  - apps/run.flash/webapp/src/app/layout.tsx
  - apps/run.flash/webapp/src/app/page.tsx
  - apps/run.flash/webapp/src/app/signin/page.tsx
  - apps/run.flash/webapp/src/app/api/auth/[...nextauth]/route.ts
  - apps/run.flash/webapp/src/middleware.ts
  - apps/run.flash/webapp/.env.local
  - apps/run.auth/webapp/src/config/oidc.ts
  - apps/run.auth/webapp/src/config/index.ts
autonomous: true
requirements:
  - BRWS-02

must_haves:
  truths:
    - "Visiting localhost:3004 without a session redirects to auth.defcon.run signin"
    - "After OIDC login, user returns to the flash app with a valid session"
    - "The app renders a dark-themed page with DCR34 branding"
  artifacts:
    - path: "apps/run.flash/webapp/package.json"
      provides: "Next.js 16 project with all dependencies"
      contains: "next"
    - path: "apps/run.flash/webapp/src/config/auth.ts"
      provides: "OIDC client config for auth.defcon.run"
      exports: ["auth", "handlers", "signIn", "signOut"]
    - path: "apps/run.flash/webapp/src/middleware.ts"
      provides: "Auth middleware protecting all routes"
      contains: "auth"
    - path: "apps/run.flash/webapp/src/app/layout.tsx"
      provides: "Root layout with HeroUI providers and dark theme"
      contains: "SessionProvider"
    - path: "apps/run.auth/webapp/src/config/oidc.ts"
      provides: "Flash tool OIDC client registration"
      contains: "flashTool"
  key_links:
    - from: "apps/run.flash/webapp/src/middleware.ts"
      to: "apps/run.flash/webapp/src/config/auth.ts"
      via: "import { auth }"
      pattern: "import.*auth.*from.*config/auth"
    - from: "apps/run.flash/webapp/src/config/auth.ts"
      to: "apps/run.auth/webapp/src/config/oidc.ts"
      via: "OIDC client registration"
      pattern: "flashTool"
    - from: "apps/run.flash/webapp/src/app/layout.tsx"
      to: "apps/run.flash/webapp/src/app/providers.tsx"
      via: "Providers component"
      pattern: "Providers"
---

<objective>
Bootstrap the run.flash Next.js 16 application with full OIDC authentication.

Purpose: Create the foundational app shell that all subsequent plans build upon. Users visiting flash.defcon.run without a session are redirected to auth.defcon.run for login. After login, they return to a dark-themed shell ready for the wizard UI.

Output: A running Next.js 16 app at localhost:3004 with HeroUI + Tailwind 4 dark theme, OIDC authentication via auth.defcon.run, and OIDC client registration in run.auth.
</objective>

<execution_context>
@/Users/khundeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khundeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-scaffold-device-picker/01-CONTEXT.md
@.planning/phases/01-app-scaffold-device-picker/01-RESEARCH.md

Source files to copy patterns from:
@apps/run.gpx/webapp/src/config/auth.ts (OIDC client config pattern -- copy and adapt)
@apps/run.gpx/webapp/src/middleware.ts (Auth middleware pattern)
@apps/run.gpx/webapp/src/app/signin/page.tsx (Auto-redirect signin page)
@apps/run.gpx/webapp/next.config.ts (Next.js config with basePath)
@apps/run.gpx/webapp/tsconfig.json (TypeScript config)
@apps/run.human/webapp/src/app/providers.tsx (HeroUI + ThemeProvider)
@apps/run.human/webapp/src/config/fonts.ts (Font declarations)
@apps/run.human/webapp/src/styles/globals.css (Glass-card, noise-overlay, terminal-block CSS)
@apps/run.human/webapp/tailwind.config.js (HeroUI dark theme with DCR34 colors)
@apps/run.auth/webapp/src/config/oidc.ts (OIDC client registration -- add flash client)
@apps/run.auth/webapp/src/config/index.ts (Config pattern -- add flash client credentials)

<interfaces>
<!-- OIDC client registration in run.auth config/index.ts -- add flashTool alongside existing clients -->
From apps/run.auth/webapp/src/config/index.ts:
```typescript
oidc: {
  clients: {
    runHuman: { clientId: process.env.OIDC_RUNHUMAN_CLIENT_ID!, clientSecret: process.env.OIDC_RUNHUMAN_SECRET! },
    cmsStrapi: { clientId: process.env.OIDC_CMS_CLIENT_ID!, clientSecret: process.env.OIDC_CMS_SECRET! },
    gpxStudio: { clientId: process.env.OIDC_GPXSTUDIO_CLIENT_ID!, clientSecret: process.env.OIDC_GPXSTUDIO_SECRET! },
    // ADD: flashTool: { clientId: process.env.OIDC_FLASH_CLIENT_ID!, clientSecret: process.env.OIDC_FLASH_SECRET! },
  },
}
```

From apps/run.human/webapp/src/app/providers.tsx:
```typescript
export function Providers({ children, themeProps }: ProvidersProps) {
  const router = useRouter();
  return (
    <NextThemesProvider {...themeProps}>
      <HeroUIProvider navigate={router.push}>{children}</HeroUIProvider>
    </NextThemesProvider>
  );
}
```

From apps/run.gpx/webapp/src/config/auth.ts:
```typescript
export const { handlers, auth, signIn, signOut } = NextAuth({
  ...authConfig,
  redirectProxyUrl,
  secret: process.env.AUTH_JWT_SECRET?.split(","),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 16 app with deps, configs, theme, and layout</name>
  <files>
    apps/run.flash/webapp/package.json
    apps/run.flash/webapp/tsconfig.json
    apps/run.flash/webapp/next.config.ts
    apps/run.flash/webapp/tailwind.config.js
    apps/run.flash/webapp/postcss.config.mjs
    apps/run.flash/webapp/eslint.config.mjs
    apps/run.flash/webapp/src/styles/globals.css
    apps/run.flash/webapp/src/config/fonts.ts
    apps/run.flash/webapp/src/config/site.ts
    apps/run.flash/webapp/src/app/providers.tsx
    apps/run.flash/webapp/src/app/layout.tsx
    apps/run.flash/webapp/src/app/page.tsx
  </files>
  <action>
    Create the full app scaffold at `apps/run.flash/webapp/`:

    1. **package.json**: Create with `npm init -y`, then install ALL dependencies:
       ```bash
       cd apps/run.flash/webapp
       npm init -y
       npm install next@^16.1.6 react@19.2.3 react-dom@19.2.3 next-auth@5.0.0-beta.30 \
         @heroui/react@^2.8.8 next-themes@^0.4.6 framer-motion@^12.23.26 \
         clsx@2.1.1 lucide-react@^0.561.0 \
         @react-aria/ssr@3.9.10 @react-aria/visually-hidden@3.8.28 @react-types/shared@^3.32.1
       npm install -D tailwindcss@^4 @tailwindcss/postcss@^4 typescript@^5 \
         @types/node@^25 @types/react@^19 @types/react-dom@^19 \
         eslint@^9 eslint-config-next@16.1.6
       ```
       Add scripts: `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`, `"lint": "eslint ."`.

    2. **tsconfig.json**: Copy EXACTLY from `apps/run.gpx/webapp/tsconfig.json`. Path alias: `"@/*": ["./src/*"]`. Strict: true.

    3. **next.config.ts**: Adapt from `apps/run.gpx/webapp/next.config.ts`:
       - `output: "standalone"`
       - Conditionally apply `basePath: "/${REGION_SHORT}"` in production only
       - Conditionally apply `assetPrefix` in production only
       - `WEBAPP_ORIGIN = process.env.WEBAPP_ORIGIN || "flash.defcon.run"`
       - Expose `NEXT_PUBLIC_REGION_SHORT` via `env` config
       - No rewrites needed (unlike run.gpx)

    4. **tailwind.config.js**: Copy EXACTLY from `apps/run.human/webapp/tailwind.config.js`. This includes the full HeroUI dark theme (teal primary #00d4aa, surface colors, animations). Content paths should reference `./src/components/**/*` and `./src/app/**/*` and `./node_modules/@heroui/theme/dist/**/*`. Add a custom green accent color for the hacker/cyberpunk feel: `'matrix-green': '#00ff41'` in `theme.extend.colors`.

    5. **postcss.config.mjs**: Standard Tailwind 4 PostCSS config:
       ```javascript
       export default { plugins: { "@tailwindcss/postcss": {} } };
       ```

    6. **eslint.config.mjs**: Standard Next.js ESLint 9 flat config following monorepo pattern.

    7. **src/styles/globals.css**: Copy from `apps/run.human/webapp/src/styles/globals.css` (includes glass-card, noise-overlay, terminal-block, glass-nav, glow-focus utilities). Update `@config` path to reference `../../tailwind.config.js`. This gives us the full DCR34 visual system. Add additional flasher-specific CSS classes:
       - `.matrix-text` for the hacker green-on-dark terminal text effect (color: #00ff41, text-shadow glow)
       - `.cyber-border` for a subtle animated border glow effect

    8. **src/config/fonts.ts**: Copy from `apps/run.human/webapp/src/config/fonts.ts`. Include Fira_Code (mono), Inter (sans), MuseoModerno (museo), and Atkinson_Hyperlegible. These are the DCR34 brand fonts.

    9. **src/config/site.ts**: Create site metadata:
       ```typescript
       export const siteConfig = {
         name: "flash.defcon.run",
         description: "DCR34 Meshtastic Device Flasher",
         navItems: [], // Wizard is the entire app -- no navigation
       };
       ```

    10. **src/app/providers.tsx**: Copy EXACTLY from `apps/run.human/webapp/src/app/providers.tsx`. HeroUIProvider + NextThemesProvider + router integration + @react-types/shared module declaration.

    11. **src/app/layout.tsx**: Create root layout adapted from run.human's protected layout:
        - Import fonts, providers, SessionProvider, globals.css
        - `html lang="en" suppressHydrationWarning`
        - body: `"min-h-screen bg-background font-sans antialiased"` + font variables
        - Providers with `defaultTheme: "dark"`
        - SessionProvider with basePath computed from REGION_SHORT
        - `noise-overlay` class on wrapper div for DCR34 texture
        - NO MapBackground component (flasher has simpler dark background)
        - NO Header (header will be added in Plan 02 as part of the wizard shell)
        - Main content area: `container mx-auto max-w-6xl px-6 pt-4`

    12. **src/app/page.tsx**: Minimal placeholder page (server component):
        ```typescript
        export default function FlashPage() {
          return (
            <div className="flex items-center justify-center min-h-[60vh]">
              <h1 className="text-2xl font-mono text-primary">flash.defcon.run</h1>
            </div>
          );
        }
        ```
        This will be replaced by the browser gate + wizard in Plan 02.
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js 16 app builds successfully at apps/run.flash/webapp/ with HeroUI dark theme, Tailwind 4, DCR34 fonts, and noise-overlay styling. `npm run dev` starts on port 3004 and renders a dark-themed placeholder page.</done>
</task>

<task type="auto">
  <name>Task 2: OIDC authentication with auth.defcon.run and client registration</name>
  <files>
    apps/run.flash/webapp/src/config/auth.ts
    apps/run.flash/webapp/src/app/api/auth/[...nextauth]/route.ts
    apps/run.flash/webapp/src/middleware.ts
    apps/run.flash/webapp/src/app/signin/page.tsx
    apps/run.flash/webapp/.env.local
    apps/run.auth/webapp/src/config/oidc.ts
    apps/run.auth/webapp/src/config/index.ts
  </files>
  <action>
    Set up OIDC authentication so unauthenticated users are redirected to auth.defcon.run:

    1. **src/config/auth.ts**: Adapt from `apps/run.gpx/webapp/src/config/auth.ts` with these specific changes:
       - `LOCAL_FLASH_PORT = process.env.LOCAL_FLASH_PORT || "3004"` (not 3003)
       - Cookie names: `sess_flash`, `csrf_flash`, `callback_flash`, `state_flash` (CRITICAL: service-specific to avoid conflicts)
       - `redirectProxyUrl` construction using flash.defcon.run URLs
       - Logging prefix: `[run.flash]` (not `[run.gpx]`)
       - Copy the FULL jwt callback with `fetchFreshClaims()`, `validateAndUpdateClaims()`, session invalidation, and claims refresh logic. Do NOT simplify -- this handles lockouts and session revocation.
       - Copy the FULL session callback that exposes services to the client
       - Cookie domain in production: `.${siteDomain}` (leading dot)
       - Session strategy: jwt, maxAge: 1 day (same as run.gpx)
       - Export: `{ handlers, auth, signIn, signOut }`
       - Logger: suppress `env-url-basepath-mismatch` warning (same as run.gpx)
       - The flash app does NOT need service-specific access control (unlike gpxstudio). Do NOT check for a "flash" service claim -- all authenticated DCR34 users can use the flasher.

    2. **src/app/api/auth/[...nextauth]/route.ts**: Standard Auth.js catch-all route:
       ```typescript
       import { handlers } from "@/config/auth";
       export const { GET, POST } = handlers;
       ```

    3. **src/middleware.ts**: Protect ALL routes except API, static, signin, favicon, img, data:
       ```typescript
       import { auth } from "@/config/auth";
       import { NextResponse } from "next/server";

       export default auth((req) => {
         if (!req.auth?.user) {
           const signinUrl = req.nextUrl.clone();
           signinUrl.pathname = "/signin";
           signinUrl.searchParams.set("callbackUrl", req.url);
           return NextResponse.redirect(signinUrl);
         }
         return NextResponse.next();
       });

       export const config = {
         matcher: ["/((?!api|_next/static|_next/image|favicon.ico|signin|img|data).*)"],
       };
       ```
       Note: No gpxstudio-specific service claim check (unlike run.gpx). All authenticated users can access the flasher.

    4. **src/app/signin/page.tsx**: Copy from `apps/run.gpx/webapp/src/app/signin/page.tsx` and adapt:
       - Change callback URL from `/studio` to `/` (the root page IS the wizard)
       - Keep the region detection logic for production path prefix
       - Keep the `signIn("run.defcon.run", { callbackUrl })` call
       - Dark background styling consistent with DCR34 theme

    5. **.env.local**: Create local development environment file:
       ```
       # Auth
       OIDC_CLIENT_ID=flash-dev-client-id
       OIDC_CLIENT_SECRET=flash-dev-client-secret
       AUTH_JWT_SECRET=flash-dev-jwt-secret-change-me
       AUTH_INTERNAL_SECRET=dev-internal-secret
       LOCAL_FLASH_PORT=3004
       LOCAL_AUTH_PORT=3002
       ```

    6. **apps/run.auth/webapp/src/config/index.ts**: Add flash tool client credentials to the `oidc.clients` object:
       ```typescript
       flashTool: {
         clientId: process.env.OIDC_FLASH_CLIENT_ID!,
         clientSecret: process.env.OIDC_FLASH_SECRET!,
       },
       ```

    7. **apps/run.auth/webapp/src/config/oidc.ts**: Add flash tool OIDC client registration to the `clients` array, following the EXACT pattern of the gpxStudio client:
       ```typescript
       // Flash tool client (flash.{siteDomain}/{region})
       {
         client_id: config.oidc.clients.flashTool.clientId,
         client_secret: config.oidc.clients.flashTool.clientSecret,
         redirect_uris: [
           `https://flash.${siteDomain}/api/auth/callback/run.${siteDomain}`,
           `https://flash.${siteDomain}/use1/api/auth/callback/run.${siteDomain}`,
           `https://flash.${siteDomain}/cac1/api/auth/callback/run.${siteDomain}`,
           ...(config.isDev ? [
             `http://localhost:${LOCAL_FLASH_PORT}/api/auth/callback/run.${siteDomain}`,
           ] : []),
         ],
         post_logout_redirect_uris: [
           `https://flash.${siteDomain}/use1`,
           `https://flash.${siteDomain}/cac1`,
           ...(config.isDev ? [
             `http://localhost:${LOCAL_FLASH_PORT}`,
           ] : []),
         ],
         grant_types: ["authorization_code", "refresh_token"],
         response_types: ["code"],
         scope: "openid profile email services",
         token_endpoint_auth_method: "client_secret_post",
       },
       ```
       Add `const LOCAL_FLASH_PORT = process.env.LOCAL_FLASH_PORT || "3004";` at the top with the other port declarations.

    Also add `OIDC_FLASH_CLIENT_ID=flash-dev-client-id` and `OIDC_FLASH_SECRET=flash-dev-client-secret` to the run.auth `.env.local` if it exists (or document in .env.local of run.flash that run.auth needs these set too).
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>OIDC authentication is fully configured. Visiting localhost:3004 without a session redirects to the signin page, which auto-redirects to auth.defcon.run OIDC provider. After login, user returns to flash app with valid `sess_flash` cookie. Flash tool is registered as OIDC client in run.auth with correct redirect URIs.</done>
</task>

</tasks>

<verification>
1. `cd apps/run.flash/webapp && npm run build` succeeds with no errors
2. `cd apps/run.flash/webapp && PORT=3004 npm run dev` starts the dev server
3. Visiting http://localhost:3004 shows a dark-themed page (if auth server is running) or redirects to signin
4. Cookie names in auth.ts are `sess_flash`, `csrf_flash`, `callback_flash`, `state_flash`
5. Flash tool client is registered in `apps/run.auth/webapp/src/config/oidc.ts`
6. Flash tool credentials are in `apps/run.auth/webapp/src/config/index.ts`
</verification>

<success_criteria>
- Next.js 16 app builds and runs at localhost:3004
- HeroUI dark theme renders with DCR34 teal primary (#00d4aa) and dark background
- OIDC authentication redirects unauthenticated users to auth.defcon.run
- Flash tool registered as OIDC client in run.auth with all required redirect URIs
- Cookie names are service-specific (sess_flash, csrf_flash, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-scaffold-device-picker/01-01-SUMMARY.md`
</output>
