---
phase: 54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr
plan: 03
type: execute
wave: 2
depends_on: ["54-02"]
files_modified:
  - apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx
  - apps/run.human/webapp/src/lib/ctf-otp-enroll.ts
  - apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx
  - apps/run.human/webapp/src/lib/__tests__/ctf-otp-enroll.test.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-reward-covert-invariant.test.ts
autonomous: true
requirements: [CTFT-08]
must_haves:
  truths:
    - "When a non-covert solve response carries effect.kind==='otp-enroll', ClaimClient renders the CtfOtpEnroll reward card"
    - "CtfOtpEnroll draws a scannable QR of the otpauth string, shows the current rolling code with a live countdown + adjacent codes, exposes an Add-to-Authenticator otpauth deep link, and names effect.nextFlag when present"
    - "A missing or unparseable effect.otpauth makes the renderer no-op silently — the base solve-success card still shows"
    - "The covert path (covert-egg.ts, EggTrigger.tsx, CtfCelebration.tsx, /assets/theme route, ctf-covert-css.ts) is byte-unchanged and carries no reward payload"
  artifacts:
    - apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx
    - apps/run.human/webapp/src/lib/ctf-otp-enroll.ts
    - apps/run.human/webapp/src/lib/__tests__/ctf-otp-enroll.test.ts
    - apps/run.human/webapp/src/lib/__tests__/ctf-reward-covert-invariant.test.ts
  key_links:
    - "CtfOtpEnroll is imported ONLY on the non-covert ClaimClient result path (and later the admin reveal-preview) — never by EggTrigger/covert-egg"
    - "The renderer computes rolling codes via adjacentCodesAsync from ctf-otp-client.ts and parses the otpauth via parseOtpauth from ctf-otp-core.ts; QR via the already-present qrcode dep"
---

<objective>
Build the first client-side `effect.kind` handler: the `otp-enroll` reward renderer. A credited non-covert solve already carries `result.effect` all the way to the `ClaimClient` result card (the server claim page passes the full `JudgeResult`); today `ClaimClient` ignores it. Add a pure effect-narrowing predicate, a self-contained `CtfOtpEnroll.tsx` card (QR of the `otpauth://` seed via the existing `qrcode` dep, rolling previous/current/next code with a live countdown via `adjacentCodesAsync`, an "Add to Authenticator" deep link, a "Copy setup link" affordance, and an optional next-flag line), and a small dispatch at the `ClaimClient` credited-solve branch. Keep the covert CSS solve path untouched and prove it with a regression test.

Purpose: Deliver CTFT-08 — the daily-chain reward reveal — on the visible, authenticated solve path only, honoring the hard invariant that the covert channel stays byte-identical and reward-free (T-53-04-01 / SC-5).
Output: `CtfOtpEnroll.tsx`, `ctf-otp-enroll.ts` (pure predicate), a `ClaimClient.tsx` dispatch, and two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-CONTEXT.md
@.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure effect-narrowing predicate + covert-invariant regression test</name>
  <files>apps/run.human/webapp/src/lib/ctf-otp-enroll.ts, apps/run.human/webapp/src/lib/__tests__/ctf-otp-enroll.test.ts, apps/run.human/webapp/src/lib/__tests__/ctf-reward-covert-invariant.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/ctf-judge.ts (the OtpEnrollEffect interface lines ~59-65 — the exact shape {kind:"otp-enroll", otpauth, nextFlag?}; JudgeResult.effect is `unknown` and must be narrowed before use)
    - apps/run.human/webapp/src/lib/covert-egg.ts (the covert client — must contain no reward reference)
    - apps/run.human/webapp/src/components/EggTrigger.tsx (the covert trigger — win:boolean only, never effect)
    - apps/run.human/webapp/src/lib/ctf-covert-css.ts and apps/run.human/webapp/src/app/(ctf)/assets/theme/route.ts (the covert CSS channel — reward-free)
  </read_first>
  <behavior>
    - asOtpEnrollEffect(effect: unknown) returns an OtpEnrollEffect ({kind:"otp-enroll", otpauth:string, nextFlag?:string}) when the input is an object with kind==="otp-enroll" and a non-empty string otpauth that parseOtpauth accepts; otherwise returns null. A malformed/missing otpauth, a wrong kind, a non-object, null/undefined all return null.
    - The predicate never throws (parseOtpauth is wrapped in try/catch).
    - Covert-invariant test: reads the covert source files from disk and asserts none of them reference "otp-enroll", "CtfOtpEnroll", or import the reward renderer; asserts CtfOtpEnroll is imported only by ClaimClient (and, after 54-04, the admin form) — i.e. it does not appear in the covert modules.
  </behavior>
  <action>
    Create ctf-otp-enroll.ts exporting the OtpEnrollEffect type (import it from ctf-judge is fine — it is a type-only import, no runtime electro pull; prefer `import type`) and asOtpEnrollEffect(effect): OtpEnrollEffect | null, validating shape and running parseOtpauth (from "@/lib/ctf-otp-core") inside try/catch to confirm the otpauth is well-formed. Create the covert-invariant test using node:fs to read covert-egg.ts, EggTrigger.tsx, CtfCelebration.tsx, ctf-covert-css.ts, and the assets/theme route.ts, asserting each source string does NOT contain the reward-kind token or the renderer component name. Create the predicate test covering valid effect, wrong kind, missing otpauth, unparseable otpauth, null, and a non-object.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run src/lib/__tests__/ctf-otp-enroll.test.ts src/lib/__tests__/ctf-reward-covert-invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Predicate test: valid otp-enroll effect → object; every malformed case → null; no throw.
    - Covert-invariant test: each covert source file asserted free of the reward-kind token and the renderer name; test is green (proving covert files carry no reward reference at author time).
    - `import type` (not a runtime import) is used for OtpEnrollEffect so ctf-otp-enroll.ts stays free of any electro/server pull.
  </acceptance_criteria>
  <done>asOtpEnrollEffect narrows the reward effect safely; the covert-invariant test encodes the byte-free-of-reward guarantee for the covert modules; both tests green under Node 23.6.0.</done>
</task>

<task type="auto">
  <name>Task 2: CtfOtpEnroll.tsx reward card (QR + rolling code + deep link + next flag)</name>
  <files>apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx</files>
  <read_first>
    - apps/run.human/webapp/src/lib/ctf-otp-client.ts (adjacentCodesAsync — the client rolling-code source from 54-02)
    - apps/run.human/webapp/src/lib/ctf-otp-core.ts (parseOtpauth — to read secret/digits/period for adjacentCodesAsync)
    - apps/run.human/webapp/src/components/qr/renderStyledQr.ts (the proven client-side qrcode usage pattern — `import * as qr from "qrcode"`, `qr.create` / toDataURL)
    - apps/run.human/webapp/src/components/admin/qr-ui.ts (cls.* tokens: card, btn, btnPrimary, mono, label — reuse these; the design forbids new one-off Tailwind)
    - .planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-UI-SPEC.md (Surface B spec + the LOCKED reward-renderer copy table + the Rolling-code hero type rule + Accessibility rules)
  </read_first>
  <action>
    Create a "use client" CtfOtpEnroll.tsx accepting props { otpauth: string; nextFlag?: string } (the narrowed effect). Behavior: parse the otpauth via parseOtpauth (secret/digits/period); render the QR client-side from the RAW otpauth string via the qrcode dep to a data URL, drawn ~200-240px on a WHITE quiet-zone background (high-contrast regardless of theme) with aria-label "Authenticator enrollment QR code". Compute rolling codes with adjacentCodesAsync(secret, Math.floor(Date.now()/1000), {digits, period}) in an effect; show Previous / Current / Next with Current as the 28px mono primary hero and previous/next at 13px mono text-default-400 (per D4 / the type rule). Run a single setInterval(1000) that decrements the displayed countdown ("New code in {n}s") and, when the period rolls (remaining hits 0), re-invokes adjacentCodesAsync; clear the interval on unmount. Primary action "Add to Authenticator" is an anchor whose href is the otpauth string (cls.btnPrimary); secondary "Copy setup link" copies the otpauth string via navigator.clipboard and shows a transient "Copied" toast/label (cls.btn). When nextFlag is present render the line "This unlocks: {nextFlag}"; when absent omit it. Use the LOCKED copy strings from the UI-SPEC reward-renderer table verbatim. Guard against flash: render the code+countdown region first; the QR appears when its data URL resolves (no spinner needed). Do NOT import ctf-otp.ts (node:crypto) — only ctf-otp-client.ts + ctf-otp-core.ts. Do NOT touch CtfCelebration/EggTrigger/covert-egg. Accessibility: mark the per-second seconds span aria-hidden and announce only on period roll (aria-live polite), per the UI-SPEC.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "CtfOtpEnroll" | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "node:crypto|from \"qrcode\"|adjacentCodesAsync|parseOtpauth|otpauth" src/components/ctf/CtfOtpEnroll.tsx`: shows the qrcode import, adjacentCodesAsync, parseOtpauth, and otpauth deep-link usage; shows NO node:crypto.
    - `grep -c "CtfCelebration\|covert" src/components/ctf/CtfOtpEnroll.tsx` returns 0 (renderer is independent of the covert/confetti path).
    - The LOCKED reward copy strings ("You unlocked an authenticator code", "Scan with your authenticator app", "Add to Authenticator", "Copy setup link", "Current code", "New code in", "This unlocks:") are present verbatim.
    - `npx tsc --noEmit` reports no error referencing CtfOtpEnroll.
  </acceptance_criteria>
  <done>CtfOtpEnroll renders a scannable QR, a live rolling code with countdown + adjacent codes, an otpauth deep link, a copy affordance, and a conditional next-flag line — all from the existing qrcode dep and the 54-02 client TOTP, with no covert coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch the reward at the ClaimClient credited-solve branch</name>
  <files>apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx</files>
  <read_first>
    - apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx (ResultBody — the credited-solve branch `result.solved && result.points > 0`; the reward renders under that success block)
    - apps/run.human/webapp/src/app/(ctf)/ctf/claim/page.tsx (confirms the server passes the full JudgeResult incl. effect into ClaimClient — no server change needed)
    - apps/run.human/webapp/src/lib/ctf-otp-enroll.ts (asOtpEnrollEffect — the narrowing gate from Task 1)
  </read_first>
  <action>
    In ClaimClient's ResultBody, inside the credited-solve branch (solved && points > 0), narrow result.effect via asOtpEnrollEffect; when it returns a non-null OtpEnrollEffect, render CtfOtpEnroll below the existing "Flag captured!" block, passing otpauth + nextFlag. When it returns null, render exactly as today (no visual change, no error). Do NOT alter the capped or non-award branches, the signin branch, or the empty branch. Do NOT read effect on any covert path — this dispatch lives solely in the visible ClaimClient result card. Keep the change additive and minimal (an import + a narrowed conditional render).
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run src/lib/__tests__/ctf-reward-covert-invariant.test.ts && git diff --stat -- src/lib/covert-egg.ts src/components/EggTrigger.tsx src/components/CtfCelebration.tsx "src/app/(ctf)/assets/theme/route.ts" src/lib/ctf-covert-css.ts | grep -q . && echo "COVERT_TOUCHED_FAIL" || echo "COVERT_UNTOUCHED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - ClaimClient imports CtfOtpEnroll + asOtpEnrollEffect and renders the reward only inside the credited-solve branch.
    - `git diff --stat` shows covert-egg.ts, EggTrigger.tsx, CtfCelebration.tsx, the assets/theme route, and ctf-covert-css.ts are UNCHANGED by this phase (COVERT_UNTOUCHED_OK).
    - `npx tsc --noEmit -p tsconfig.json` reports no error referencing ClaimClient.
  </acceptance_criteria>
  <done>A non-covert credited solve with an otp-enroll effect shows the reward card; every other result state is visually unchanged; the covert files are provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server solve response → client render | result.effect (a per-challenge authored payload) crosses to the browser on the visible claim page |
| non-covert vs covert channels | the reward must appear ONLY on the visible/authenticated path; the covert CSS channel stays reward-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-03-01 | Information disclosure | covert channel reward leak | high | mitigate | Reward dispatch lives solely in ClaimClient's visible result branch; the covert-invariant test + git-diff-stat gate prove covert-egg/EggTrigger/CtfCelebration/assets-theme/ctf-covert-css are untouched and reward-free (T-53-04-01 / SC-5) |
| T-54-03-02 | Tampering | malformed effect payload | medium | mitigate | asOtpEnrollEffect validates shape + parseOtpauth in try/catch; a malformed effect renders nothing (base success still shows), never a crash |
| T-54-03-03 | Denial of service | rolling-code interval | low | mitigate | Single setInterval(1000) cleared on unmount; recompute only on period roll — no unbounded timers |
| T-54-03-SC | Tampering | package installs | low | accept | No package installs; QR uses the already-present qrcode dep, rolling code uses the built-in Web Crypto via 54-02 |
</threat_model>

<verification>
- `nvm use 23.6.0 && npx vitest run src/lib/__tests__/ctf-otp-enroll.test.ts src/lib/__tests__/ctf-reward-covert-invariant.test.ts` green.
- `npx tsc --noEmit -p tsconfig.json` clean for CtfOtpEnroll + ClaimClient.
- `git diff --stat` shows the covert modules untouched.
- Manual (human) UI check deferred to phase verification: on a real non-covert solve carrying an otp-enroll effect, the QR scans into an authenticator, the current code matches, the countdown ticks and rolls, "Add to Authenticator" opens the otpauth link, and the next-flag line shows when present.
</verification>

<success_criteria>
The first client effect.kind handler ships: an otp-enroll solve reveals a scannable QR + live rolling code + deep link + next-flag on the visible claim card, malformed effects no-op, and the covert channel is provably byte-unchanged and reward-free.
</success_criteria>

<output>
Create `.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-03-SUMMARY.md` when done.
</output>
