---
phase: 54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/components/admin/ctf-form-model.ts
  - apps/run.human/webapp/src/components/admin/__tests__/ctf-form-model.test.ts
autonomous: true
requirements: [CTFT-07]
must_haves:
  truths:
    - "presetToAdvanced maps each challenge-type preset to a concrete set of Advanced scoring knobs"
    - "previewPoints returns exactly what the judge's computePoints returns for identical inputs (parity, not re-implementation)"
    - "redactCtfSecrets strips otp.secret and the full effect payload from a loaded record and reports hasOtpSecret / hasEffect booleans"
    - "inferAnswerType and inferChallengeType recover the edit-mode selections from a stored record, defaulting answerType=static and challenge-type=Custom"
  artifacts:
    - apps/run.human/webapp/src/components/admin/ctf-form-model.ts
    - apps/run.human/webapp/src/components/admin/__tests__/ctf-form-model.test.ts
  key_links:
    - "previewPoints imports computePoints from src/lib/ctf-scoring.ts (the pure, client-safe scorer) — NEVER from ctf-judge.ts, which pulls the electro client"
---

<objective>
Build the pure, client-safe model layer the redesigned CtfForm (Plan 54-04) binds to: a challenge-type preset → Advanced-knobs map, a live-scoring-preview adapter that delegates to the judge's real `computePoints` (guaranteeing preview===judge parity), edit-mode type/answer-type inference, and a server-side secret redactor. All functions are pure (no React, no I/O, no node-only imports) so they are unit-tested in the vitest node env and importable from both the client form and the server edit page.

Purpose: Isolate every piece of form LOGIC into a tested, parallel-safe seam BEFORE the large `CtfForm.tsx` restructure, so the UI plan is layout+wiring only and the SC-3/SC-2 test obligations (preset→Advanced mapping, preview-vs-computePoints, masked-secret non-prefill) are satisfied at the pure-function level (this repo has no jsdom/testing-library — component DOM tests are not available).
Output: `ctf-form-model.ts` + its test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-CONTEXT.md
@.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: presetToAdvanced map + previewPoints (binds computePoints) + inference helpers</name>
  <files>apps/run.human/webapp/src/components/admin/ctf-form-model.ts, apps/run.human/webapp/src/components/admin/__tests__/ctf-form-model.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/ctf-scoring.ts (computePoints + ScoringConfig + TimeTier — the SINGLE source of truth for scoring; it imports nothing, so it is client-safe and MUST be the import target for the preview)
    - apps/run.human/webapp/src/components/admin/CtfForm.tsx (the CtfRecord shape + the existing numeric string-state fields the form uses; the model must speak the same shape)
    - .planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-UI-SPEC.md (Component Inventory §2 challenge-type presets; Interaction & State Contract rows "Preset → Advanced", "Live preview parity", "Edit-mode inference")
  </read_first>
  <behavior>
    - presetToAdvanced("flat-points" | "first-blood-race" | "timed-drop" | "easter-egg" | "custom") returns a concrete partial of Advanced scoring knobs (pointMax, pointFloor, maxSolves, firstBloodBonus, and where meaningful maxAttempts / rateLimitWindow / timeTiers). "custom" returns an empty/no-op partial (it never overwrites the admin's manual knobs). Each non-custom preset returns numeric values, not strings.
    - The five preset ids are exhaustively enumerated by a PRESET_IDS constant so the form's segmented control and the test iterate the same list (no drift).
    - previewPoints(config, n, now?) delegates to computePoints — for ANY config and n, previewPoints(config, n) === computePoints(n, config, now). Prove parity with a table of configs × n (including n=1 first-blood, n=maxSolves floor, n>maxSolves ⇒ 0, and an active timeTier). previewPoints is a thin adapter (maps the form's may-be-string numeric fields to a numeric ScoringConfig, then calls computePoints); it does NOT re-derive the curve.
    - inferAnswerType(record) returns record.answerType when it is "otp", else "static" (absent ⇒ static, matching the backend narrowCtf default).
    - inferChallengeType(record) returns the preset id whose presetToAdvanced output matches the record's stored scoring shape, else "custom" when nothing matches.
  </behavior>
  <action>
    Create ctf-form-model.ts as a plain module (NO "use client" directive, so the server edit page can import it too; import ONLY from ctf-scoring.ts and local types). Export: the PRESET_IDS tuple and a ChallengeTypePreset type; an AdvancedKnobs interface (the numeric scoring subset); presetToAdvanced(preset) returning AdvancedKnobs partials per the UI-SPEC preset semantics (Flat points = flat-ish curve / low decline; First-blood race = large firstBloodBonus; Timed drop = timeTier-oriented ceiling; Easter egg = small fixed award; Custom = {} no-op — document each choice inline as it is Claude's discretion within the design's intent); previewPoints(config, n, now?) delegating to computePoints imported from "@/lib/ctf-scoring"; inferAnswerType(record) and inferChallengeType(record). Keep every field numeric-coerced at the boundary via a small local toNum helper (blank/NaN ⇒ undefined ⇒ treated as 0 by the ScoringConfig mapping, mirroring the form's numOrUndef). Do NOT import ctf-judge.ts (it pulls the electro client and would poison the client bundle).
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run src/components/admin/__tests__/ctf-form-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts previewPoints === computePoints across a config×n table (parity), including first-blood, floor, over-cap-zero, and an active-tier case.
    - Test asserts every PRESET_IDS entry (except custom) yields numeric AdvancedKnobs, and custom yields an empty partial.
    - Test asserts inferAnswerType(absent)==="static", inferAnswerType({answerType:"otp"})==="otp".
    - Test asserts inferChallengeType round-trips a preset (presetToAdvanced(p) → record → inferChallengeType === p) and defaults to "custom" on an unmatched shape.
    - `grep -n "ctf-judge" src/components/admin/ctf-form-model.ts` returns nothing (no server import leaked into the client model).
  </acceptance_criteria>
  <done>ctf-form-model.ts exports presetToAdvanced, previewPoints, inferAnswerType, inferChallengeType, PRESET_IDS; the test file is green under Node 23.6.0; the module imports computePoints from ctf-scoring and never from ctf-judge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: redactCtfSecrets — strip write-only secrets before a record becomes a client prop</name>
  <files>apps/run.human/webapp/src/components/admin/ctf-form-model.ts, apps/run.human/webapp/src/components/admin/__tests__/ctf-form-model.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/qr-admin.ts (getCtf returns the RAW Ctf row incl. otp.secret and effect — lines ~186-189; CtfInput/ctfAttributes show the otp map + effect fields that carry secrets)
    - apps/run.human/webapp/src/app/(protected)/admin/qr/ctf/[challenge]/page.tsx (the server edit page that today casts the raw record to CtfRecord and passes it as a client prop — the leak site 54-04 will fix using this redactor)
    - .planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-UI-SPEC.md (Interaction & State Contract row "Secret / answer masking": answer + OTP secret + reward-otpauth are empty, placeholder-masked, and NEVER populated from the loaded record)
  </read_first>
  <behavior>
    - redactCtfSecrets(record) returns a shallow-cloned record with: otp.secret removed (otp.digits/period/algorithm preserved for the read-only OTP summary); effect removed entirely; plus two derived booleans hasOtpSecret (true iff the input had a non-empty otp.secret) and hasEffect (true iff the input had a defined effect). answerHash presence is left as-is (it already only drives the "leave blank to keep" hint and carries no plaintext).
    - Redaction is non-mutating: the input object is not modified.
    - A record with no otp and no effect returns unchanged shape with hasOtpSecret=false, hasEffect=false.
  </behavior>
  <action>
    Add redactCtfSecrets(record) to ctf-form-model.ts. Define the RedactedCtfRecord return type: the loaded fields the form needs (challenge, scoring knobs, timeTiers, maxAttempts, rateLimitWindow, enabled, answerType, unlockAfter, perPlayerIntervalHours, perPlayerMax, globalMax, answerHash, otp WITHOUT secret) plus hasOtpSecret and hasEffect. Deep-avoid the secret: build the returned otp (when present) from only {digits, period, algorithm}; never copy secret. Never copy effect. This function runs on the SERVER (the edit page) but is pure and node-free so it stays in this shared model module and is unit-tested here. Document loudly that this is the write-only-secret boundary (SC-2 / the design's "secrets never round-tripped to the client").
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run src/components/admin/__tests__/ctf-form-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test feeds a record with otp.secret set + effect set and asserts the output has NO otp.secret, NO effect, otp.digits/period/algorithm preserved, hasOtpSecret===true, hasEffect===true.
    - Test asserts the input object is not mutated (its otp.secret and effect still present after the call).
    - Test asserts a plain static record (no otp/effect) yields hasOtpSecret===false and hasEffect===false and keeps its scoring + limit fields.
  </acceptance_criteria>
  <done>redactCtfSecrets is exported, tested green, non-mutating, and provably drops every secret field while surfacing the presence booleans the form needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server edit page → client form prop | A loaded Ctf row (server) is serialized as a prop to the "use client" CtfForm; anything on it crosses to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-01-01 | Information disclosure | redactCtfSecrets / edit page prop | high | mitigate | redactCtfSecrets strips otp.secret + effect before the record can become a client prop; unit test proves no secret survives; 54-04 wires the edit page through it |
| T-54-01-02 | Tampering | previewPoints parity | low | mitigate | previewPoints delegates to the real computePoints (no duplicate curve to drift); parity test guards it |
| T-54-01-SC | Tampering | package installs | low | accept | No package installs in this plan (qrcode already present); no new dependency introduced |
</threat_model>

<verification>
- `nvm use 23.6.0 && npx vitest run src/components/admin/__tests__/ctf-form-model.test.ts` is green.
- `grep -rn "ctf-judge" src/components/admin/ctf-form-model.ts` returns nothing.
- No other test file regresses: `nvm use 23.6.0 && npx vitest run src/lib/__tests__/ctf-scoring.test.ts` stays green (computePoints untouched).
</verification>

<success_criteria>
The pure model layer exists and is fully tested: preset→Advanced mapping, preview-vs-computePoints parity, edit-mode inference, and secret redaction — the four logic obligations the UI plan (54-04) will consume without re-deriving any of them.
</success_criteria>

<output>
Create `.planning/phases/54-ctf-flag-types-slice-1b-frontend-admin-form-redesign-otp-enr/54-01-SUMMARY.md` when done.
</output>
