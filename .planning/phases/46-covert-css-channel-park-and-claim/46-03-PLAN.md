---
phase: 46-covert-css-channel-park-and-claim
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/components/CtfCelebration.tsx
  - apps/run.human/webapp/src/styles/globals.css
autonomous: true
requirements: [CTF-09]

must_haves:
  truths:
    - "CtfCelebration renders an overlay confetti/rain burst when active=true and nothing when active=false."
    - "The overlay is pointer-events:none so it never blocks the UI underneath."
    - "The celebration is a USER-TRIGGERED effect and is NOT suppressed by prefers-reduced-motion (documented prod cash-rain lesson) — a reduced-motion user still sees their celebration."
    - "The burst self-terminates after a bounded duration (does not animate forever)."
  artifacts:
    - apps/run.human/webapp/src/components/CtfCelebration.tsx
  key_links:
    - "CtfCelebration is the DC33-style celebration the egg client (46-04) mounts and toggles on a computed-style win."
    - "Its animation CSS lives in globals.css and must NOT sit inside a prefers-reduced-motion:reduce suppression block."
---

<objective>
Port the DC33 award vibe into run.human as a self-contained, reusable celebration
component: an overlay confetti/rain burst the egg client toggles on a covert win.

Purpose: CTF-09's visible payoff. Isolated in wave 1 (independent of the codec and
route) so it is ready when the egg client wires it. Carries forward the prod lesson
that a user-triggered celebration must not be silently reduced-motion-gated.
Output: `CtfCelebration.tsx` + its keyframes in globals.css.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-covert-css-channel-park-and-claim/46-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces 1 of the phase's 6 artifacts: the celebration module (a run.human
port of the run.bib CashRain lineage). The egg client + trigger (46-04) consume it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CtfCelebration overlay component + non-reduced-motion-gated keyframes</name>
  <files>apps/run.human/webapp/src/components/CtfCelebration.tsx, apps/run.human/webapp/src/styles/globals.css</files>
  <read_first>
    - apps/run.bib/webapp/src/components/CashRain.tsx (the lineage to port: active-gated overlay, mounted-guard for SSR, negative-delay burst, bounded RAIN_DURATION_MS self-terminate)
    - apps/run.bib/webapp/src/styles/globals.css (lines ~223-263: .cash-bill / .cash-rain keyframes AND the explicit comment that this user-initiated effect is NOT gated by prefers-reduced-motion — mirror that rationale)
    - apps/run.human/webapp/src/styles/globals.css (target file — find where to append the celebration keyframes; confirm no existing celebration class collision)
  </read_first>
  <action>
    Create `CtfCelebration.tsx` (`"use client"`) exporting `CtfCelebration({ active }: { active:
    boolean })`. Port the CashRain shape: a `mounted` guard (generate the random layout
    client-side only, no SSR hydration mismatch), a bounded self-terminating burst
    (a duration timer armed when `active` flips true, clears the pieces when it elapses so
    nothing animates forever), and an overlay `<div>` that is `aria-hidden` and
    `pointer-events: none`. Render `null` when `!active || !mounted || expired`.

    Do NOT depend on the run.bib dollar data-uri asset (absent in run.human). Use self-contained
    confetti pieces — a set of small colored spans (DC34 palette: mint-teal + magenta accents)
    positioned by randomized CSS custom properties — so the component needs no image asset.

    Add the keyframes + `.ctf-celebration` / `.ctf-confetti` (pick distinct class names, confirm
    no collision) rules to `src/styles/globals.css`. CRITICAL: place these rules OUTSIDE any
    `@media (prefers-reduced-motion: reduce)` suppression block — a user-INITIATED celebration
    must keep animating for reduced-motion users (the documented prod cash-rain lesson). Add a
    short comment recording that rationale (reworded so it does not name a grep gate token).

    Keep the component &lt;100 lines. Only the new component must be tsc-clean.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp &amp;&amp; test $(grep -n 'ctf-celebration\|ctf-confetti' src/styles/globals.css | wc -l) -ge 1 &amp;&amp; ! awk '/@media \(prefers-reduced-motion: reduce\)/{depth=1} depth&amp;&amp;/ctf-celebration|ctf-confetti/{print "GATED"; found=1} /}/&amp;&amp;depth{depth--} END{exit found?1:0}' src/styles/globals.css</automated>
  </verify>
  <done>CtfCelebration exists as an active-gated, pointer-events-none, self-terminating overlay; its keyframes are present in globals.css and are NOT inside a prefers-reduced-motion:reduce block; new file is tsc-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| celebration overlay → page UI | A decorative overlay must not capture input or block the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-09 | Denial of Service | overlay blocks clicks / never stops | low | mitigate | `pointer-events: none` overlay + bounded self-terminating timer (ported CashRain contract). |
| T-46-10 | Tampering | accessibility regression | low | accept | Overlay is `aria-hidden`; purely decorative; reduced-motion intentionally NOT suppressed per the user-initiated-effect exception. |
</threat_model>

<verification>
- globals.css contains the celebration keyframes and they are not inside a reduced-motion suppression block (verify gate).
- `npx tsc --noEmit` adds no NEW errors beyond the 2 pre-existing unrelated ones.
- Operator visual check (non-blocking): mounting `<CtfCelebration active />` shows a confetti burst that fades after the bounded duration and does not block clicks; still fires with OS Reduce Motion on.
</verification>

<success_criteria>
- Reusable, self-contained, pointer-events-none, self-terminating celebration overlay.
- Animation survives prefers-reduced-motion (user-triggered exception honored).
</success_criteria>

<output>
Create `.planning/phases/46-covert-css-channel-park-and-claim/46-03-SUMMARY.md` when done.
</output>
