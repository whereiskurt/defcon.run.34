---
phase: 46-covert-css-channel-park-and-claim
plan: 04
type: execute
wave: 3
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - apps/run.human/webapp/src/lib/covert-egg.ts
  - apps/run.human/webapp/src/components/EggTrigger.tsx
  - apps/run.human/webapp/src/lib/__tests__/covert-egg.test.ts
  - apps/run.human/webapp/src/app/(public)/page.tsx
autonomous: true
requirements: [CTF-09]

must_haves:
  truths:
    - "The egg client injects a <link rel=stylesheet> to the covert path with the flag encoded as a build-date-looking ?v= (via encodeFlag), and reads back the award via getComputedStyle — never by parsing a fetch response body."
    - "The celebration fires ONLY when the computed-style marker (AWARD_PROP) reads non-empty and non-zero; an absent/zero marker → no celebration."
    - "A reduced-motion user still gets their user-triggered celebration (CtfCelebration is not reduced-motion-gated)."
    - "One working trigger (the !!! egg) is wired on the run.defcon.run landing so the full signed-in loop (trigger → covert hit → credit → celebration) is demonstrable in-app."
  artifacts:
    - apps/run.human/webapp/src/lib/covert-egg.ts
    - apps/run.human/webapp/src/components/EggTrigger.tsx
  key_links:
    - "AWARD_PROP is imported from ctf-covert-css.ts so the read token matches exactly what the route writes."
    - "The href path /use1/assets/theme matches 46-02's route spelling; the read is getComputedStyle on document.documentElement (invisible to a network watcher)."
    - "EggTrigger mounts CtfCelebration (46-03) and toggles it via the covert-egg read-back."
---

<objective>
Build the egg-side client: a reusable module that, on the `!!!` trigger, injects the
covert stylesheet link with the flag encoded into a build-date-looking `?v=`, reads the
award back via `getComputedStyle`, and fires the DC33 celebration on a win only — plus
wire one working `!!!` trigger on the run.defcon.run landing to demonstrate the full loop.

Purpose: CTF-09 end-to-end. The read MUST be a computed-style read (not a parsed fetch
body) so a network watcher sees only a stylesheet load.
Output: `covert-egg.ts` (encode + inject + read-back + shouldCelebrate), `EggTrigger.tsx`
(the `!!!` listener that mounts CtfCelebration), and the wiring into `(public)/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-covert-css-channel-park-and-claim/46-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces 2 of the phase's 6 artifacts (egg client lib + the run.defcon.run
trigger wiring) and consumes the codec (46-01), the covert route (46-02), and the
celebration (46-03). NOTED FOLLOW-UP (out of scope unless trivial): embedding the same
covert-egg module into the separate static landing app (`apps/static/landing/`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: covert-egg client — encode, inject link, getComputedStyle read-back, shouldCelebrate</name>
  <files>apps/run.human/webapp/src/lib/covert-egg.ts, apps/run.human/webapp/src/lib/__tests__/covert-egg.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/ctf-covert-codec.ts (encodeFlag — 46-01)
    - apps/run.human/webapp/src/lib/ctf-covert-css.ts (AWARD_PROP — 46-01; import the exact token name)
    - docs/superpowers/specs/2026-07-13-ctf-judge-and-covert-channel-design.md (§7.2 read-back: getComputedStyle(document.documentElement).getPropertyValue(AWARD_PROP); computed-style read, not a network event)
  </read_first>
  <behavior>
    - buildCovertUrl(challenge, guess) returns `<basePath>/assets/theme?v=<encodeFlag(...)>` — the v segment is pure decimal (build-date-looking), and the path is the 46-02 spelling.
    - shouldCelebrate(marker: string): true iff marker trims to a non-empty numeric value > 0; false for "", whitespace, "0", or non-numeric.
    - The read helper calls getComputedStyle on document.documentElement and reads AWARD_PROP — asserted via a jsdom test that stubs getComputedStyle to return a value and confirms shouldCelebrate gates the outcome (marker present&gt;0 → celebrate true; absent/0 → false).
    - fireEgg resolves to a boolean win (or invokes an onWin callback) derived ONLY from the computed-style read, never from reading the injected sheet's rules or any fetch body.
  </behavior>
  <action>
    Create `covert-egg.ts` (`"use client"`-safe, browser-only guards) exporting:
    - `buildCovertUrl(challenge: string, guess: string): string` — region/basePath-prefixed
      (`isDev ? "" : "/" + region`) + `/assets/theme?v=` + `encodeFlag(challenge, guess)`.
      Mirror the region/basePath pattern already used in `(public)/page.tsx` (whoamiUrl).
    - `shouldCelebrate(marker: string): boolean` — pure, testable gate (non-empty numeric &gt; 0).
    - `readAward(): string` — `getComputedStyle(document.documentElement).getPropertyValue(AWARD_PROP)`.
    - `fireEgg(challenge, guess, onResult: (win: boolean) => void): void` — inject a
      `<link rel="stylesheet" href={buildCovertUrl(...)}>` into `<head>`; on its `load` event
      (with a short timeout fallback so a slow/failed load resolves to no-win), call
      `onResult(shouldCelebrate(readAward()))`, then remove the injected link. The win decision
      derives SOLELY from the computed-style read.

    Do NOT read the stylesheet's CSSOM rules and do NOT fetch()+parse the body — the covert
    contract is a computed-style read so a network watcher sees only a stylesheet load.
    Keep &lt;100 lines. Only new files must be tsc-clean.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp &amp;&amp; nvm use 23.6.0 >/dev/null 2>&amp;1; npx vitest run src/lib/__tests__/covert-egg.test.ts</automated>
  </verify>
  <done>buildCovertUrl produces the 46-02 path with a decimal ?v=; shouldCelebrate gates win-only on a non-empty non-zero numeric marker; the read-back uses getComputedStyle (no CSSOM-rule / fetch-body parse); tests green on Node 23.6.0.</done>
</task>

<task type="auto">
  <name>Task 2: EggTrigger (!!! listener + CtfCelebration) wired on the run.defcon.run landing</name>
  <files>apps/run.human/webapp/src/components/EggTrigger.tsx, apps/run.human/webapp/src/app/(public)/page.tsx</files>
  <read_first>
    - apps/run.human/webapp/src/components/CtfCelebration.tsx (46-03 — the celebration to mount/toggle)
    - apps/run.human/webapp/src/lib/covert-egg.ts (Task 1 — fireEgg/onResult)
    - apps/run.human/webapp/src/app/(public)/page.tsx (the run.defcon.run landing — "use client" page; region/whoamiUrl pattern at the top; where to mount the trigger)
  </read_first>
  <action>
    Create `EggTrigger.tsx` (`"use client"`) — a headless listener component that detects the
    `!!!` easter-egg gesture (three `!` characters typed in quick succession, and/or a triple-tap
    on touch) using a short rolling window/debounce. It holds a baked demo `challenge` + `guess`
    (a known covert challenge for the demo loop; document it inline). On trigger it calls
    `fireEgg(challenge, guess, (win) => setCelebrating(win))` and renders `<CtfCelebration
    active={celebrating} />`, auto-clearing `celebrating` after a bounded delay so it can re-fire.

    Mount `<EggTrigger />` in `(public)/page.tsx` (the run.defcon.run landing) so the full
    signed-in loop is demonstrable in-app: trigger → covert hit → (server credits) → win sheet →
    computed-style read → celebration. Keep the page's existing content/behavior intact; add only
    the trigger mount.

    Keep EggTrigger &lt;100 lines. Only the new/edited files must be tsc-clean.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp &amp;&amp; test $(grep -c 'EggTrigger' "src/app/(public)/page.tsx") -ge 1 &amp;&amp; test -f src/components/EggTrigger.tsx &amp;&amp; grep -q 'CtfCelebration' src/components/EggTrigger.tsx</automated>
  </verify>
  <done>EggTrigger detects the !!! gesture, fires the covert hit, and toggles CtfCelebration on the computed-style win; it is mounted on the run.defcon.run landing so the full signed-in loop is demonstrable; new/edited files tsc-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| egg client → covert route | Same-origin request carrying `sess_run`; the read-back must be computed-style, not a network-observable body parse. |
| injected `<link>` → DOM | A transient stylesheet element must be cleaned up and must not carry a network-observable win tell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-11 | Information Disclosure | read-back mechanism | high | mitigate | Win derived ONLY from getComputedStyle on document.documentElement; no CSSOM-rule read, no fetch+parse — a network watcher sees a stylesheet load only (Task 1). |
| T-46-12 | Information Disclosure | injected link residue | low | mitigate | The injected `<link>` is removed after read; carries only the build-date-looking `?v=`. |
| T-46-13 | Tampering | celebration on non-win | medium | mitigate | shouldCelebrate gate: non-empty numeric &gt; 0 only; absent/zero marker → no celebration (Task 1 tests). |
</threat_model>

<verification>
- covert-egg vitest suite green on Node 23.6.0; EggTrigger mounted in `(public)/page.tsx` (verify gate).
- `npx tsc --noEmit` adds no NEW errors beyond the 2 pre-existing unrelated ones.
- Operator end-to-end (non-blocking here; full network-tab proof lands after Phase 48 exposure): signed-in, fire `!!!` on the landing → a stylesheet request appears (200 text/css) and a celebration fires; signed-out → stylesheet loads, no celebration (flag parked); a reduced-motion user still sees the celebration on a win.
</verification>

<success_criteria>
- Egg fires the DC33 celebration ONLY on a computed-style win, via getComputedStyle (not a parsed body).
- A reduced-motion user still gets the user-triggered celebration.
- One working `!!!` trigger is wired on run.defcon.run demonstrating the full signed-in loop.
</success_criteria>

<output>
Create `.planning/phases/46-covert-css-channel-park-and-claim/46-04-SUMMARY.md` when done.
</output>