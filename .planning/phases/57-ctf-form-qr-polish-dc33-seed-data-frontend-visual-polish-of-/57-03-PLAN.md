---
phase: 57-ctf-form-qr-polish-dc33-seed-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/lib/ctf-seed-rows.ts
  - apps/run.human/webapp/src/lib/__tests__/ctf-seed-rows.test.ts
  - apps/run.human/webapp/scripts/seed-ctf.mts
autonomous: true
requirements: [CTFP-04]
user_setup:
  - service: aws-dynamodb (prod seed run — operator step, NOT part of the build)
    why: "A prod --confirm run writes the six Ctf rows to the shared run-human-electro table and MUST use prod's CTF_ANSWER_SALT so hashes verify."
    env_vars:
      - name: AWS_PROFILE
        source: "dc34-application (SSO); default AWS provider chain (D4). Do NOT --env-file the localhost:8888 .env for a prod run."
      - name: CTF_ANSWER_SALT
        source: "prod value — hashes computed under any other salt will never verify against player guesses."
      - name: RUN_DYNAMODB_REGION
        source: "us-east-1"
    dashboard_config:
      - task: "Before any prod --confirm: run DRY-RUN and confirm the composed Ctf pk/sk + __edb_e__/__edb_v__ markers match a real Ctf row (D4 landmine)."
        location: "apps/run.human/webapp — npx tsx scripts/seed-ctf.mts (no --confirm)"
must_haves:
  truths:
    - "A pure row-builder maps each of the six DC33 starter names to a complete Ctf attribute set, with answers salted-hashed via ctf-hash and every row enabled:false."
    - "seed-ctf.mts mirrors reset-ctf-user.mts: raw DynamoDBDocument (not the ESM entities), DRY-RUN by default, --confirm to write, --remove to bulk-delete the seeded set, SSO-cred fallback via the default provider chain."
    - "The seed set is idempotent by challenge name (re-running --confirm rewrites the same rows by the same key, never duplicates)."
    - "The DRY-RUN prints the composed pk/sk + entity markers for each row so the operator can confirm the key shape against a real Ctf row before writing."
    - "The pure row-builder is unit-tested (Node >=22.12): the six starters produce their documented fields, all enabled:false, goldstein carries the otp-enroll effect + nextFlag goldstein-otp, goldstein-otp is answerType:otp with unlockAfter:goldstein, grace-hopper has the DEF CON 34 timeTier ceiling."
  artifacts:
    - "apps/run.human/webapp/src/lib/ctf-seed-rows.ts (pure buildSeedRows() row-builder)"
    - "apps/run.human/webapp/src/lib/__tests__/ctf-seed-rows.test.ts"
    - "apps/run.human/webapp/scripts/seed-ctf.mts (raw-SDK DRY-RUN/--confirm/--remove writer)"
  key_links:
    - "hashAnswer (src/lib/ctf-hash.ts) -> answerHash on each static/flat seed row"
    - "buildSeedRows() -> seed-ctf.mts (script imports the pure builder; no logic duplicated)"
    - "Ctf entity key shape (pk composite [challenge], sk composite [], entity 'Ctf' v1 service 'run') -> composed raw pk/sk in seed-ctf.mts"
  prohibitions:
    - "MUST NOT import the ElectroDB entities into the .mts script (they pull @auth/dynamodb-adapter ESM which a tsx CJS run cannot require) — mirror reset-ctf-user.mts and use the raw @aws-sdk DynamoDBDocument client."
    - "MUST NOT write outside DRY-RUN unless --confirm is passed; MUST NOT --env-file the localhost .env for a prod run (D4 landmine)."
    - "MUST NOT change the judge, scoring, entities, /api/admin/qr, or the covert channel — this slice only ADDS a script + pure builder + test."
    - "MUST NOT store raw plaintext answers where a hash suffices — static/flat rows store answerHash (goldstein-otp stores otp.secret, no answer)."
    - "MUST NOT add any npm dependency."
    - "MUST NOT switch or create git branches (git.branching_strategy=none)."
---

<objective>
Add `scripts/seed-ctf.mts` — a raw-SDK operator script that seeds a curated set of six REAL DC33 CTF starter flags (one per flag type, all `enabled:false`, deletable via the existing admin Delete button) — plus a PURE, unit-tested row-builder (CTFP-04, D3/D4).

Purpose: Give the CTF admin a one-command way to populate an empty challenge list with real, illustrative starters covering every flag type (static + otp-enroll reward, chained rotating OTP, first-blood race, flat, timed-drop tier, easter-egg confetti). The script mirrors the proven `reset-ctf-user.mts` pattern (raw `DynamoDBDocument`, DRY-RUN default, `--confirm`, `--remove`, SSO-cred fallback) so it is safe to run against prod. The actual prod `--confirm` run is a gated operator step (D4 landmine: confirm pk/sk against a real row first) — this slice ships the tool, verified by unit tests + DRY-RUN.

Output: `src/lib/ctf-seed-rows.ts` (pure `buildSeedRows()`), its vitest, and `scripts/seed-ctf.mts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-CONTEXT.md
@apps/run.human/webapp/scripts/reset-ctf-user.mts
@apps/run.human/webapp/scripts/migrate-ctf-answerhash.mts
@apps/run.human/webapp/src/lib/ctf-hash.ts
@apps/run.human/webapp/src/entities/qr.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure buildSeedRows() row-builder + unit test</name>
  <files>apps/run.human/webapp/src/lib/ctf-seed-rows.ts, apps/run.human/webapp/src/lib/__tests__/ctf-seed-rows.test.ts</files>
  <read_first>
    apps/run.human/webapp/src/lib/ctf-hash.ts (hashAnswer — salted SHA-256 of trim+lowercased answer)
    apps/run.human/webapp/src/entities/qr.ts lines 116-211 (the Ctf attribute set the rows must populate)
    apps/run.human/webapp/src/components/admin/__tests__/ctf-form-model.test.ts (vitest style + `@/` alias usage)
    57-CONTEXT.md D3 (the six flags with exact field values) and D4 (hashing/salt requirement)
  </read_first>
  <behavior>
    - buildSeedRows() returns exactly 6 rows, one per starter name: goldstein, goldstein-otp, mudge, condor, grace-hopper, turing.
    - Every row has enabled:false and the anti-spam defaults maxAttempts:5, rateLimitWindow:60.
    - goldstein: static; answerHash === hashAnswer("hackers4evr"); pointMax/points 100 (flat); effect {kind:"otp-enroll", otpauth:"otpauth://totp/Emmanuel%20Goldstein?secret=GZRGQNKGKN4DINQ&issuer=Defcon.run&algorithm=SHA1&digits=6&period=120", nextFlag:"goldstein-otp"}.
    - goldstein-otp: answerType "otp"; otp {secret:"GZRGQNKGKN4DINQ", digits:6, period:120, algorithm:"SHA1", skew:1}; unlockAfter:"goldstein"; perPlayerIntervalHours:24; no plaintext answer.
    - mudge: first-blood race; answerHash === hashAnswer("0g3l33t"); pointMax 1000, pointFloor 100, maxSolves 100, firstBloodBonus 250.
    - condor: flat; answerHash === hashAnswer("fr33k3v1n"); award 100.
    - grace-hopper: timed drop; answerHash === hashAnswer("d3bugth3system") (hashAnswer lowercases the D3 "d3bugth3sYstem"); pointMax 100, pointFloor 1, timeTiers:[{from,to,ceiling:500}] spanning the DEF CON 34 window.
    - turing: easter egg; answerHash === hashAnswer("3n1gim@"); award 10; effect {kind:"confetti", intensity:11}.
    - No row stores a raw plaintext `answer` when an answerHash is present.
  </behavior>
  <action>
    Create `src/lib/ctf-seed-rows.ts` exporting a pure `buildSeedRows()` that returns an array of plain `Ctf`-attribute objects (typed against the Ctf attributes in `src/entities/qr.ts`), with NO AWS/entity imports — it imports ONLY `hashAnswer` from `@/lib/ctf-hash` (or `./ctf-hash`). Encode the six starters exactly per 57-CONTEXT.md D3 (values enumerated in the behavior block above). Compute each static/flat/race/timed/easter answer field as `answerHash: hashAnswer(<answer>)` — never store the plaintext. Set `enabled:false` and the anti-spam defaults on every row. Keep the builder free of DynamoDB key-composition (that lives in the .mts script); each object carries only its `challenge` NAME plus attributes.

    The grace-hopper answer in D3 contains an uppercase letter; hashAnswer trim+lowercases, so assert against the lowercased form and document that normalization in a code comment.

    Write `src/lib/__tests__/ctf-seed-rows.test.ts` (vitest, `@/` alias) asserting the full behavior contract: row count, per-name field values, answerHash equality against `hashAnswer(...)`, all `enabled:false`, the goldstein effect + nextFlag, goldstein-otp's otp map + unlockAfter, and grace-hopper's timeTier ceiling.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/lib/__tests__/ctf-seed-rows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - buildSeedRows() returns 6 rows matching the behavior contract; test file green under Node >=22.12.
    - No AWS/entity imports in ctf-seed-rows.ts; only hashAnswer imported.
    - Every row enabled:false with maxAttempts:5/rateLimitWindow:60; no plaintext answers stored.
  </acceptance_criteria>
  <done>buildSeedRows() returns the 6 documented DC33 starters with hashed answers and enabled:false; ctf-seed-rows.test.ts green under Node >=22.12; no AWS/entity imports.</done>
</task>

<task type="auto">
  <name>Task 2: seed-ctf.mts raw-SDK writer (DRY-RUN / --confirm / --remove)</name>
  <files>apps/run.human/webapp/scripts/seed-ctf.mts</files>
  <read_first>
    apps/run.human/webapp/scripts/reset-ctf-user.mts (the raw DynamoDBDocument client, creds() SSO fallback, DRY-RUN gate, scanAll, env contract — mirror all of it)
    apps/run.human/webapp/scripts/migrate-ctf-answerhash.mts (scanning Ctf rows by the __edb_e__ marker + writing by pk/sk)
    apps/run.human/webapp/src/entities/qr.ts lines 212-219 (Ctf primary index: pk composite [challenge], sk composite [], entity "Ctf" version "1" service "run")
  </read_first>
  <action>
    Create `scripts/seed-ctf.mts` mirroring `reset-ctf-user.mts`: same imports (`@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb`), same `creds()` SSO-fallback helper, same `RUN_ELECTRO_*`/`RUN_DYNAMODB_REGION` env contract, same DRY-RUN-by-default gate (`--confirm` to write). Import the pure builder via a RELATIVE path (`../src/lib/ctf-seed-rows`) — same style as migrate-ctf-answerhash.mts importing `../src/lib/ctf-migration` — so a `tsx` CJS run resolves it without the ESM entity chain. Do NOT import the ElectroDB entities.

    Compose each row's raw DynamoDB key to match the Ctf entity: `pk = "$run#challenge_" + challenge` (lowercased challenge), `sk = "$ctf_1"`, plus the ElectroDB markers `__edb_e__: "Ctf"` and `__edb_v__: "1"`, merged with the builder's attributes and the entity defaults (`solveCount: 0`, `createdAt`/`updatedAt` ISO strings).

    Ordering so DRY-RUN works offline: FIRST compose + print each of the six rows (pk/sk/markers + full attribute set it WOULD put), THEN attempt a best-effort parity scan (one existing Ctf row via the `__edb_e__` marker) wrapped in try/catch — print its pk/sk/markers next to a composed sample when reachable, and on any scan error print a note that key-parity must be confirmed manually before `--confirm` (never abort DRY-RUN on scan failure). Because a mis-composed key is the primary landmine (D4), keep the composed-key print unconditional.

    `--confirm`: `put` each composed row (idempotent — same key overwrites, never duplicates). `--remove`: `delete` each of the six rows by composed key (bulk-delete the seeded set). Keep the localhost-endpoint override optional (`RUN_ELECTRO_ENDPOINT`) exactly as reset-ctf-user.mts does, and put a PROD RUN RECIPE header comment (AWS_PROFILE=dc34-application, no --env-file of localhost .env, CTF_ANSWER_SALT must be prod's). Fail loud (non-zero exit) on missing region.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; RUN_DYNAMODB_REGION=us-east-1 npx tsx scripts/seed-ctf.mts 2>&1 | grep -Eq "DRY-RUN" && RUN_DYNAMODB_REGION=us-east-1 npx tsx scripts/seed-ctf.mts 2>&1 | grep -q "challenge_goldstein" && echo SEED_DRYRUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/seed-ctf.mts` (no flags) prints DRY-RUN, composes + prints all six rows (including `$run#challenge_goldstein` / `$ctf_1` + __edb_e__/__edb_v__ markers), and writes nothing even when DynamoDB is unreachable.
    - --confirm puts by composed key (idempotent); --remove deletes the six by composed key; entities are NOT imported.
    - Missing RUN_DYNAMODB_REGION exits non-zero; PROD RUN RECIPE + salt/localhost-env landmine documented in the header.
  </acceptance_criteria>
  <done>seed-ctf.mts DRY-RUNs offline printing six composed rows; --confirm puts / --remove deletes by composed key; raw SDK only (no entity imports); region-guard + recipe header present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> prod run-human-electro (DynamoDB) | The script writes/deletes real Ctf rows under operator SSO creds; a mis-composed key or wrong salt corrupts or shadows challenge data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57C-01 | Tampering | composed Ctf pk/sk | high | mitigate | DRY-RUN default prints composed keys + best-effort real-row parity; gated operator step confirms pk/sk against a real row before --confirm (D4). |
| T-57C-02 | Spoofing (unverifiable answers) | answerHash under wrong salt | high | mitigate | user_setup mandates prod CTF_ANSWER_SALT; header recipe forbids --env-file of the localhost .env; hashing flows through the shared ctf-hash seam. |
| T-57C-03 | Elevation of Privilege | seeded rows scored live | medium | mitigate | Every seeded row is enabled:false (asserted in the unit test); admin must explicitly enable before it scores. |
| T-57C-04 | Repudiation | accidental bulk write | medium | mitigate | Writes only under explicit --confirm; --remove reverses the seeded set by the same keys; idempotent by challenge name. |
</threat_model>

<verification>
- `ctf-seed-rows.test.ts` green under Node >=22.12 (`nvm use 23.6.0`).
- `npx tsx scripts/seed-ctf.mts` DRY-RUN prints the six composed rows offline and writes nothing.
- Operator ship-gate (documented, not automated): DRY-RUN against prod confirms composed pk/sk == a real Ctf row before any `--confirm`.
</verification>

<success_criteria>
CTFP-04 met: `scripts/seed-ctf.mts` writes six real DC33 Ctf starter rows covering every flag type (incl. the goldstein -> goldstein-otp chain and a timed-drop tier), DRY-RUN by default with `--confirm`/`--remove`, answers hashed via ctf-hash under prod salt, idempotent by challenge name, all `enabled:false`, backed by a unit-tested pure row-builder.
</success_criteria>

<artifacts>
This phase produces (this plan): `apps/run.human/webapp/src/lib/ctf-seed-rows.ts` (pure `buildSeedRows()`), `apps/run.human/webapp/src/lib/__tests__/ctf-seed-rows.test.ts`, and `apps/run.human/webapp/scripts/seed-ctf.mts`.
</artifacts>

<output>
Create `.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-03-SUMMARY.md` when done.
</output>
