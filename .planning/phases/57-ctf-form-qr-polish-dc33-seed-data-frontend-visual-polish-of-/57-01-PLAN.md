---
phase: 57-ctf-form-qr-polish-dc33-seed-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/components/admin/qr-ui.ts
  - apps/run.human/webapp/src/components/admin/CtfForm.tsx
autonomous: false
requirements: [CTFP-01]
must_haves:
  truths:
    - "Admin CtfForm challenge-type segments show a two-line label (name over a one-line descriptor) with an accent selection glow on the active segment (D1)."
    - "The Live scoring preview renders as a stat-tile row (bordered tiles, mono tabular-nums 20px value over a tiny UPPERCASE mono key) inside a subtly tinted container, not two loose 28px numbers."
    - "The timed-drop / scoring-window explanatory callout renders in amber (text-secondary/text-warning)."
    - "Surface A renders correctly in BOTH light and dark app themes with no raw mockup darks leaked."
    - "The existing ctf-form-model unit tests stay green and tsc is clean (restyle changed no logic)."
  artifacts:
    - "apps/run.human/webapp/src/components/admin/qr-ui.ts (restyled cls tokens)"
    - "apps/run.human/webapp/src/components/admin/CtfForm.tsx (restyled segments, stat-tiles, amber note, tighter rhythm)"
  key_links:
    - "cls token layer in qr-ui.ts → consumed by CtfForm.tsx segments/tiles/labels"
    - "PRESET_IDS / presetToAdvanced → challenge-type segment descriptors (copy only, no map change)"
    - "previewPoints(previewConfig, n) → stat-tile values (unchanged call)"
  prohibitions:
    - "MUST NOT change presets, presetToAdvanced, previewPoints, ctf_upsert/ctf_delete payloads, or any validation — restyle only."
    - "MUST NOT touch the judge (ctf-judge.ts), scoring (ctf-scoring.ts/computePoints), the Ctf/CtfSolve/CtfScoreEvent/CtfCode entities, /api/admin/qr + upsertCtf/ctfAttributes, or the covert channel (covert-egg.ts, ctf-covert-*)."
    - "MUST NOT hardcode the mockup's raw dark palette (#0b0d10 / #15181d etc.) on Surface A — use HeroUI semantic tokens only (bg-content1/content2/background, border-divider, text-foreground/default-400/default-500, text-primary/danger/secondary/warning)."
    - "MUST NOT add any npm dependency."
    - "MUST NOT change qr.toDataURL params or recolor/invert QR modules (the framed QR preview reuses CtfOtpEnroll unchanged)."
    - "MUST NOT switch or create git branches (git.branching_strategy=none) — stay on the current worktree branch."
---

<objective>
Restyle Surface A — the admin CTF form (`CtfForm.tsx`) and its `cls` token layer (`qr-ui.ts`) — to the approved mockup treatment WHILE staying inside HeroUI semantic tokens and rendering correctly in both light and dark app themes (CTFP-01, D1).

Purpose: The form already HAS the correct structure (segmented picker, `presetToAdvanced` pre-fill, answer-type segments, Advanced drawer, `previewPoints` live preview, Delete/`ctf_delete`). This slice adds ZERO structure — it is a visual restyle: segment selection glow, two-line challenge-type labels, a stat-tile live-scoring preview, an amber window note, mono/uppercase labels (already present), and tighter card rhythm.

Output: Restyled `qr-ui.ts` (`cls` tokens) + `CtfForm.tsx`, theme-aware, with all existing form/preset/preview unit tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-CONTEXT.md
@.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle cls tokens + challenge-type / answer-type segments</name>
  <files>apps/run.human/webapp/src/components/admin/qr-ui.ts, apps/run.human/webapp/src/components/admin/CtfForm.tsx</files>
  <read_first>
    apps/run.human/webapp/src/components/admin/qr-ui.ts (the full `cls` map — segment/segmentActive/segmentIdle/label/chip already exist)
    apps/run.human/webapp/src/components/admin/CtfForm.tsx lines 442-493 (Section 2 challenge-type radiogroup + Section 3 answer-type radiogroup)
    57-UI-SPEC.md — "Component Contracts › Surface A" and "Copywriting Contract › Challenge-type two-line descriptors / Answer-type descriptors"
  </read_first>
  <action>
    In `qr-ui.ts`, strengthen the selection-glow tokens per the UI-SPEC (D1): `cls.segmentActive` becomes a soft inner glow — accent border plus `bg-primary/10` and an inset primary ring (theme-aware translation of the mockup's `inset 0 0 0 1px` primary-at-35% + `bg-primary/[.09]`), keeping the existing `h-9`/`px-3.5` control rhythm. `cls.segmentIdle` stays neutral (`bg-content1 hover:bg-content2`). Keep `cls.label` mono/uppercase/11px/`text-default-400`. Do not remove or rename any existing token key (CtfForm and CtfOtpEnroll consume them).

    In `CtfForm.tsx` Section 2 (challenge-type radiogroup, ~line 445), convert each preset button to a TWO-LINE label: line 1 = preset name (13.5px/600), line 2 = a one-line descriptor (11px, `text-default-400`). Use the descriptors from the UI-SPEC Copywriting table (flat-points → "Everyone earns the same"; first-blood-race → "Early solvers earn more"; timed-drop → "Value shifts over a window"; easter-egg → "Small fixed reward + effect"; custom → "Tune every knob yourself"). Source the descriptor from a local const map keyed by preset id; do NOT alter `PRESET_IDS`, `PRESET_LABEL`, `applyPreset`, or `presetToAdvanced`. Preserve `role="radiogroup"`/`role="radio"`/`aria-checked`. The answer-type row (Section 3) keeps single-line labels; optionally add the single-line descriptors ("One fixed code" / "6-digit code from an authenticator" / "Pool of single-use codes") as helper subtext per the UI-SPEC, without changing the `AnswerType` values or `setAnswerType`.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/components/admin/__tests__/ctf-form-model.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Active challenge-type segment shows accent border + soft inner glow via `cls.segmentActive`; idle segments stay neutral.
    - Each challenge-type segment renders name over descriptor (two lines); descriptors match the UI-SPEC copy verbatim.
    - No change to `PRESET_IDS`/`PRESET_LABEL`/`applyPreset`/`presetToAdvanced` or answer-type values; radiogroup/radio/aria-checked roles preserved.
    - `ctf-form-model.test.ts` passes.
  </acceptance_criteria>
  <done>Active challenge-type segment glows, segments show two-line descriptors matching UI-SPEC copy, no preset/answer-type logic changed, ctf-form-model.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Stat-tile live-scoring preview + amber window note + card rhythm</name>
  <files>apps/run.human/webapp/src/components/admin/CtfForm.tsx</files>
  <read_first>
    apps/run.human/webapp/src/components/admin/CtfForm.tsx lines 1035-1083 (Section 7 Live scoring preview) and the window-gated chip block
    57-UI-SPEC.md — "Surface A › Live scoring preview" row + Typography "Live-preview stat value/key" rows + Color "Amber window note"
  </read_first>
  <action>
    In Section 7 (Live scoring preview, ~line 1035), keep the exact `previewConfig` object and the `previewPoints(previewConfig, n)` calls UNCHANGED. Restyle the two big numbers into a STAT-TILE ROW: each tile is a `bg-content1`/`bg-background` card with `border-divider`, `rounded-lg`, holding a mono `tabular-nums` value at 20px `text-primary` (down from 28px) over a tiny UPPERCASE mono key at 10.5px `text-default-400`. Wrap the tiles in a subtly tinted container (`bg-content2` + `border-divider`, `rounded-lg`) — the theme-aware translation of the mockup's gradient strip. Keep the existing "First solve (n=1)" and conditional "Last solve (n=N)" tiles and the existing `window-gated` chip row.

    Render the timed-drop / scoring-window explanatory callout in amber via `text-secondary`/`text-warning` as a soft callout (matching the mockup's amber window-note). Tighten overall card rhythm toward the mockup density using the UI-SPEC Spacing Scale (existing Tailwind half-step values); style the `<code>…</code>` help chips as a subtle mono chip on `bg-content2`. Do not change any input, drawer, action, or Delete behavior — only class strings / wrappers.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Live scoring preview renders as bordered stat tiles (20px mono tabular-nums value over UPPERCASE mono key) inside a tinted container; `previewConfig`/`previewPoints` calls unchanged.
    - `window-gated` chip preserved; window/timed-drop note renders amber.
    - `tsc --noEmit` clean; Delete button still `cls.btnDanger` with `ctf_delete` intact.
  </acceptance_criteria>
  <done>Live scoring preview renders as bordered stat tiles inside a tinted container with previewConfig/previewPoints unchanged; window note amber; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Surface A in light + dark themes</name>
  <action>Human confirms the restyled admin CtfForm renders correctly in both light and dark app themes (segment glow, two-line descriptors, stat tiles, amber note) with no raw mockup darks leaked and AA-legible text. Claude has already completed the restyle; this is verification only.</action>
  <what-built>Surface A restyle: two-line glowing challenge-type segments, stat-tile live-scoring preview, amber window note, tighter rhythm — all in HeroUI semantic tokens.</what-built>
  <how-to-verify>
    1. Start the app: from `apps/run.human/webapp`, `PORT=3001 npm run dev`.
    2. Open the admin CTF form (`/admin/qr` → create/edit a challenge).
    3. Confirm in LIGHT theme: active challenge-type segment has an accent glow, descriptors read on line two, the live preview shows bordered stat tiles, the timed-drop/window note is amber, and NO harsh near-black mockup panels appear (colors track the rest of /admin).
    4. Toggle to DARK theme and confirm the same surfaces render correctly with AA-legible text.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any theme/contrast issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → CtfForm | Admin-only surface (already gated upstream); this slice adds no new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57A-01 | Tampering | CtfForm payload (`ctf_upsert`) | low | accept | Restyle touches only class strings; payload/validation frozen (prohibition) — no new mutation surface. |
| T-57A-02 | Information Disclosure | theme-aware contrast | low | mitigate | Human-verify checkpoint confirms AA legibility in light + dark; no secrets rendered by the form. |
</threat_model>

<verification>
- `ctf-form-model.test.ts` green (preset/preview logic untouched).
- `tsc --noEmit` clean.
- Human-verify: light + dark theme render pass; no raw mockup darks leaked.
</verification>

<success_criteria>
CTFP-01 met: the admin CtfForm adopts the mockup treatment (segment glow, two-line labels, stat-tile preview, amber note, framed QR via reused CtfOtpEnroll) within HeroUI tokens, theme-aware, with zero change to presets/previewPoints/ctf_upsert payload/validation and all existing form tests green.
</success_criteria>

<artifacts>
This phase produces (this plan): restyled `apps/run.human/webapp/src/components/admin/qr-ui.ts` and `apps/run.human/webapp/src/components/admin/CtfForm.tsx`. No new files.
</artifacts>

<output>
Create `.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-01-SUMMARY.md` when done.
</output>
