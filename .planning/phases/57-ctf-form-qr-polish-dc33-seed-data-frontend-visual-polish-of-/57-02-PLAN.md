---
phase: 57-ctf-form-qr-polish-dc33-seed-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx
  - apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx
autonomous: false
requirements: [CTFP-02, CTFP-03]
must_haves:
  truths:
    - "The OTP reward reveal renders as a self-contained bespoke-dark card regardless of the viewer's app theme (D1)."
    - "Header reads '✓ Correct — {name} solved' (falls back to '✓ Correct' when the name is unavailable)."
    - "The rolling-code hero shows the CURRENT code at ~40px mono tabular-nums cyan, flanked by prev/next from the existing codes.previous/current/next."
    - "A gradient progress bar (width = remaining/period) replaces the plain 'New code in Ns' line, driven by the EXISTING remaining/period state, and keeps an accessible countdown name/value."
    - "The primary action is a full-width mint '＋ Add to Authenticator' button (href = raw otpauth://); 'Copy setup link' stays the neutral secondary affordance."
    - "The chain callout '🔗 This unlocks: {nextFlag}' renders in cyan ONLY when nextFlag is present (unchanged conditional)."
    - "Existing ctf-otp-enroll and ctf-otp-client unit tests stay green."
  artifacts:
    - "apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx (bespoke-dark restyle)"
    - "apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx (tightened solved-branch success card framing)"
  key_links:
    - "adjacentCodesAsync → codes.previous/current/next → rolling-code hero + flanks (unchanged call)"
    - "remaining/period state → progress-bar width (no new timing logic)"
    - "asOtpEnrollEffect(result.effect) → CtfOtpEnroll otpauth/nextFlag props (unchanged in ClaimClient)"
  prohibitions:
    - "MUST NOT change qr.toDataURL params — keep width 220, margin 2, errorCorrectionLevel 'M', color light '#ffffff' / dark '#000000'. Never recolor/invert modules or restyle pupils; polish is framing only (CTFP-03, D2)."
    - "MUST NOT alter any logic: preserve parseOtpauth, adjacentCodesAsync, isSupportedAlgorithm fallback (WR-02), the silent no-op on unparseable seed (`if (!parsed) return null`), the aria-live roll announcement + aria-hidden on decorative code spans, and the nextFlag conditional."
    - "MUST NOT change the ClaimClient solved/capped/not-solved branch logic, the asOtpEnrollEffect narrowing, or the nonce cookie effects — success-card framing only."
    - "MUST NOT add any npm dependency (QR via existing qrcode dep; no new icon set)."
    - "MUST NOT touch the judge, scoring, entities, /api/admin/qr, or the covert channel."
    - "MUST NOT switch or create git branches (git.branching_strategy=none)."
---

<objective>
Restyle Surface B — the player-facing OTP reward reveal (`CtfOtpEnroll.tsx`) and the `ClaimClient` solved-branch success card — to full bespoke-dark per the approved mockup (CTFP-02), while keeping the enrollment QR byte-for-byte scannable (CTFP-03, D2) and preserving 100% of the existing logic.

Purpose: This is a standalone dramatic moment that commits to a scoped dark "terminal" palette regardless of the viewer's app theme. It replaces the plain "New code in Ns" line with a gradient progress-ring/bar countdown, promotes the current code to a 40px cyan hero, adds a mint "＋ Add to Authenticator" button and a cyan chain callout — all as a restyle over already-shipped, correctly-structured code.

Output: Restyled `CtfOtpEnroll.tsx` (bespoke-dark, scoped hardcoded hex allowed here only) + a tightened `ClaimClient` solved-branch card, with QR params UNCHANGED and all OTP unit tests green. The real-authenticator scan is a manual ship-gate (cannot be automated).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-CONTEXT.md
@.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bespoke-dark reveal card — hero code, framed QR, mint CTA, chain callout</name>
  <files>apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx</files>
  <read_first>
    apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx (entire file — note the qr.toDataURL block lines 58-75, the algoSupported gate, the codes/remaining rendering lines 129-205, and the WR-02 fallback)
    57-UI-SPEC.md — "Surface B palette" table, "Component Contracts › Surface B", Typography "Surface B" rows, "Copywriting Contract", and "Accessibility"
  </read_first>
  <action>
    Convert the reveal card to a self-contained dark card using the UI-SPEC Surface B palette (the ONE place raw hex is allowed): outer `--bg #0b0d10`, card surface `--card #15181d`, `1px --line #262b33` border, 14px radius, ~18px padding, centered column, `max-w-[380px]`. Because the card renders dark over EITHER app theme, apply the hex via inline `style` or a scoped local class map — do NOT rely on HeroUI theme tokens for the dark chrome, and do NOT drag in the mockup darks anywhere outside this component.

    Header: replace "You unlocked an authenticator code" with "✓ Correct — {name} solved" in `--mint #4ade80`. Derive `{name}` from a new OPTIONAL prop (e.g. `flagName?`) passed by ClaimClient; when absent, fall back to "✓ Correct". Adding an optional prop is allowed (it carries display copy only) — no behavior change.

    QR: keep the `qr.toDataURL(otpauth, { width:220, margin:2, errorCorrectionLevel:"M", color:{ light:"#ffffff", dark:"#000000" } })` call and the white quiet-zone card (`bg-white p-2`, `min-h/min-w-[220px]`, `<img>` 200×200) EXACTLY as-is. Add ONLY framing: rounded border + an optional soft OUTER glow (box-shadow mint/cyan at ~14% alpha) AROUND the white frame. Modules and pupils untouched (CTFP-03/D2).

    Rolling-code hero: promote `codes.current` to mono ~40px/700 `--cyan #38bdf8` tabular-nums with `letter-spacing:.12em`; flank with prev/next (12px `--muted #8b95a3`, from existing `codes.previous`/`codes.next`). Keep the `aria-live="polite"` announcement span and `aria-hidden` on the visual code/label spans.

    Progress countdown: REPLACE the "New code in {remaining}s" text line with a bar — 6px height, `rounded-full`, track `--line2 #333a44`, fill `linear-gradient(90deg,#38bdf8,#4ade80)`, `width = (remaining / period) * 100%`. Drive width from the EXISTING `remaining` state and the parsed `period` — add NO new timer/interval. Retain an accessible countdown: give the bar `role="progressbar"` with `aria-valuenow`/`aria-valuemin`/`aria-valuemax` (or an sr-only "New code in {remaining}s") so the countdown stays available to AT.

    Actions: make "＋ Add to Authenticator" the full-width primary — border `--mint`, bg `rgba(74,222,128,.14)`, text `--mint`, href = the raw `otpauth://` (existing anchor). Keep "Copy setup link" as the neutral secondary (ghost/`--line` border, `--muted` text, existing copy→"Copied" transient).

    Chain callout: render "🔗 This unlocks: {nextFlag}" (only when `nextFlag` present) as cyan text on `rgba(56,189,248,.07)` fill, `1px` border `rgba(56,189,248,.25)`, radius 8px, `{nextFlag}` in mono.

    Preserve verbatim: `parseOtpauth`, the `if (!parsed) return null` no-op, `isSupportedAlgorithm` gate and the WR-02 fallback note (restyled to `--muted` on the dark card, copy unchanged), and `adjacentCodesAsync`.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx vitest run src/lib/__tests__/ctf-otp-enroll.test.ts src/lib/__tests__/ctf-otp-client.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Card is self-contained dark (scoped hex), header "✓ Correct — {name} solved" (or "✓ Correct" fallback) in mint.
    - qr.toDataURL params byte-identical; QR gains only a rounded border + optional outer glow; modules/pupils unchanged.
    - Current code is 40px cyan tabular-nums hero with prev/next flanks; aria-live + aria-hidden preserved.
    - "New code in Ns" text replaced by a gradient bar (width=remaining/period) with role=progressbar/aria-valuenow (or sr-only equivalent); no new timer.
    - Full-width mint "＋ Add to Authenticator" (href raw otpauth://) + neutral "Copy setup link"; chain callout cyan and conditional on nextFlag.
    - WR-02 fallback + unparseable no-op preserved; ctf-otp-enroll + ctf-otp-client tests green; tsc clean.
  </acceptance_criteria>
  <done>Bespoke-dark reveal card with cyan hero + gradient countdown bar + mint CTA + chain callout; qr.toDataURL params unchanged; all OTP logic preserved; OTP tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten ClaimClient solved-branch success card + pass flag name</name>
  <files>apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx</files>
  <read_first>
    apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx (ResultBody, lines 87-113 — the solved && points>0 branch that mounts CtfOtpEnroll)
    57-UI-SPEC.md — "Surface B › ClaimClient solved-branch card" row
  </read_first>
  <action>
    In `ResultBody`, tighten ONLY the surrounding success card on the credited-solve branch (`result.solved && result.points > 0`) so it frames the dark reveal cleanly: keep the `Trophy`, "Flag captured!" museo heading, the first-blood `Chip`, the `+{result.points}` display (stays `text-primary` bold), and the ordinal line. Adjust spacing/wrapping only — no branch, logic, or `asOtpEnrollEffect` change.

    Pass the solved flag/challenge display name into the reward card via the new optional prop from Task 1 (e.g. `flagName={...}`) IF a name is already available on `result` (do NOT add a new judge/API field — only wire an existing value; if none exists, omit the prop so CtfOtpEnroll falls back to "✓ Correct"). Leave the capped and not-solved branches, the signin branch, and the nonce cookie effects unchanged.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null 2>&1; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Solved-branch card framing tightened; Trophy/"Flag captured!"/first-blood Chip/`+{points}`/ordinal preserved; `+{points}` stays text-primary bold.
    - CtfOtpEnroll receives `flagName` only from an existing `result` value (or omitted → "✓ Correct" fallback); no new judge/API field introduced.
    - Capped / not-solved / signin branches and nonce cookie effects unchanged; tsc clean.
  </acceptance_criteria>
  <done>Solved-branch success card framing tightened, flagName wired from an existing result value (or omitted), all other branches and nonce effects unchanged; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify dark reveal legibility + real-authenticator QR scan</name>
  <action>Human verifies the bespoke-dark reveal renders legibly over a light-theme device AND scans the rendered enrollment QR in a real Google Authenticator / Authy install to confirm it enrolls and produces a working rolling code (CTFP-03/D2 HARD ship gate). Claude has already completed the restyle; this is verification only and cannot be automated.</action>
  <what-built>Bespoke-dark OTP reward reveal: cyan 40px rolling-code hero, gradient countdown bar, mint "＋ Add to Authenticator", cyan chain callout, framed (unchanged) enrollment QR.</what-built>
  <how-to-verify>
    1. From `apps/run.human/webapp`, `PORT=3001 npm run dev`; trigger the credited otp-enroll solve so the reveal card renders (e.g. the goldstein → goldstein-otp path, or the admin reveal preview inside CtfForm).
    2. Confirm the reveal card renders full-dark and legible even when the app is in LIGHT theme (D1 risk), current code is a large cyan hero, and the countdown bar shrinks smoothly as the period ticks.
    3. QR SCAN GATE (CTFP-03/D2, HARD): scan the rendered enrollment QR with a REAL Google Authenticator / Authy install. Confirm it enrolls and produces a working rolling 6-digit code that matches the on-screen current code. If it does NOT scan/enroll, the QR framing regressed the modules — stop and fix.
  </how-to-verify>
  <resume-signal>Type "approved" once the QR enrolls in a real authenticator and the dark card is legible over a light theme; otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticator app ← rendered QR | The enrollment seed crosses into the user's authenticator; a corrupted QR silently breaks enrollment. |
| player browser → CtfOtpEnroll | Client-only render; otpauth already narrowed upstream by asOtpEnrollEffect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57B-01 | Tampering | enrollment QR modules | high | mitigate | qr.toDataURL params frozen (prohibition); framing-only; blocking human-verify requires a real-authenticator scan before ship (D2). |
| T-57B-02 | Denial of Service (usability) | dark card over light theme | medium | mitigate | Human-verify confirms legibility over a light-theme device (D1 risk). |
| T-57B-03 | Information Disclosure | rolling-code render | low | accept | Codes are the player's own enrollment; aria-live/aria-hidden hygiene preserved, no logging added. |
</threat_model>

<verification>
- `ctf-otp-enroll.test.ts` + `ctf-otp-client.test.ts` green; `tsc --noEmit` clean.
- Human-verify: dark card legible over light theme AND enrollment QR scans + enrolls in a real authenticator (CTFP-03 ship gate).
</verification>

<success_criteria>
CTFP-02 + CTFP-03 met: the OTP reveal + ClaimClient solved-branch are restyled to bespoke-dark (rolling-code hero, gradient countdown, mint CTA, chain callout) with all existing logic preserved, and the enrollment QR stays dark-on-white with unchanged qr.toDataURL params, verified scannable in a real authenticator before ship.
</success_criteria>

<artifacts>
This phase produces (this plan): restyled `apps/run.human/webapp/src/components/ctf/CtfOtpEnroll.tsx` and `apps/run.human/webapp/src/app/(ctf)/ctf/claim/ClaimClient.tsx`. No new files.
</artifacts>

<output>
Create `.planning/phases/57-ctf-form-qr-polish-dc33-seed-data-frontend-visual-polish-of-/57-02-SUMMARY.md` when done.
</output>
