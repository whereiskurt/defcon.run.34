---
phase: 33-oidc-silent-sso
plan: 06
type: execute
wave: 3
depends_on: ["33-01", "33-02", "33-03"]
files_modified:
  - apps/run.gpx/e2e/silent-sso.spec.ts
  - apps/run.flash/e2e/package.json
  - apps/run.flash/e2e/playwright.config.ts
  - apps/run.flash/e2e/tsconfig.json
  - apps/run.flash/e2e/lib/cookie-jar.ts
  - apps/run.flash/e2e/silent-sso-smoke.spec.ts
  - apps/run.bib/e2e/package.json
  - apps/run.bib/e2e/playwright.config.ts
  - apps/run.bib/e2e/tsconfig.json
  - apps/run.bib/e2e/lib/cookie-jar.ts
  - apps/run.bib/e2e/silent-sso-smoke.spec.ts
autonomous: true
requirements: [SSO-08]

must_haves:
  truths:
    - "Full gpx e2e: a warm-session visit shows the authenticated view with the top-level URL never changing and no /login page rendered; the app session cookie (sess_gpx) is set via the iframe"
    - "Full gpx e2e: a logged-out visit posts login_required, the parent stays logged-out, and there is no redirect loop"
    - "Smoke e2e on flash and bib: a warm-session visit reaches the authenticated view without a top-level navigation to /login"
  artifacts:
    - apps/run.gpx/e2e/silent-sso.spec.ts
    - apps/run.flash/e2e/silent-sso-smoke.spec.ts
    - apps/run.bib/e2e/silent-sso-smoke.spec.ts
  key_links:
    - "The gpx spec joins the existing run.gpx/e2e package (existing @playwright/test + cookie-jar that loads run.auth cookies) — no new dep there"
    - "flash/bib e2e packages mirror run.gpx/e2e (same pinned @playwright/test + typescript versions already vetted in-repo) — introduces no NEW third-party package"
    - "The no-loop assertion watches that the top-level URL does not oscillate to /login and back (the core regression the whole phase guards against)"
    - "SSO-08 is proven by the EXECUTED warm-session assertion (authenticated view renders, no top-level /login nav, sess_gpx set via iframe), not by playwright --list; when a warm run.auth cookie jar is present CI runs it, otherwise the manual warm-session run is the real gate"
    - "This plan depends on 33-01 (the IdP interactions.url / loadExistingGrant / remember:true changes) being live — the warm-session invariant cannot hold without the IdP change"
---

<objective>
Add the browser-level end-to-end proof of the silent-SSO behavior: a full spec on run.gpx (both warm and logged-out flows) and smoke specs on run.flash and run.bib.

Purpose: This is the goal-level verification — it asserts the user-visible invariant (no visible bouncing, no login render for a warm user; no loop for a logged-out user) that unit and integration tests cannot observe.
Output: `run.gpx/e2e/silent-sso.spec.ts` plus minimal flash/bib e2e packages each with a warm-session smoke spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-oidc-silent-sso/33-CONTEXT.md
@.planning/phases/33-oidc-silent-sso/33-PATTERNS.md
@docs/superpowers/specs/2026-07-03-oidc-silent-sso-design.md
@apps/run.gpx/e2e/cloud-storage.spec.ts
@apps/run.gpx/e2e/lib/cookie-jar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full silent-SSO e2e on run.gpx (warm + logged-out)</name>
  <read_first>
    - apps/run.gpx/e2e/cloud-storage.spec.ts lines 1-45 (BASE_URL, region prefix, cookie loading, screenshot helper conventions)
    - apps/run.gpx/e2e/lib/cookie-jar.ts (loadAuthCookies / hasAuthCookieJar — how the run.auth warm session is injected)
    - apps/run.gpx/e2e/playwright.config.ts (baseURL default localhost:3003, serial, workers 1)
    - apps/run.gpx/webapp/src/components/SilentSSO.tsx (the iframe + message behavior being exercised)
  </read_first>
  <files>apps/run.gpx/e2e/silent-sso.spec.ts</files>
  <action>
    Create `silent-sso.spec.ts` in the existing run.gpx/e2e package (reuse its @playwright/test dep, region-prefix pattern, and `loadAuthCookies` from lib/cookie-jar — add no dependency). 

    Warm-session test: load a valid run.auth session via loadAuthCookies, capture the current top-level URL, navigate to a gpx public route, and assert: (a) the top-level URL never navigates to a `/login`/`/signin` page (record page URL over the flow / assert no navigation to a login path occurred — e.g. track `page.on('framenavigated')` for the main frame and assert none match a login path); (b) the authenticated view renders (an authed-only element/text is visible); (c) the `sess_gpx` cookie becomes present in the context after the iframe completes (poll context.cookies()). 

    Logged-out test: use a fresh context with NO auth cookies, navigate to the same gpx public route, and assert: (a) the parent stays in the logged-out view (authed-only element absent / a logged-out marker present); (b) there is NO redirect loop — the main-frame URL does not oscillate to a login page and back (assert the login path is not visited, and the page settles). Give the iframe/timeout room using the existing 30s expect timeout.
  </action>
  <verify>
    <automated>cd apps/run.gpx/e2e && npx playwright test silent-sso.spec.ts --list && echo "SPEC_DISCOVERED"</automated>
    <automated>cd apps/run.gpx/e2e && if ls ../../run.auth/e2e/.auth/cookies-*accounta.json >/dev/null 2>&1; then npx playwright test silent-sso.spec.ts -g "warm" && echo "WARM_INVARIANT_PROVEN"; else echo "WARM_GATE_SKIPPED: no warm run.auth cookie jar (run.auth/e2e/.auth/cookies-*accounta.json) present — SSO-08 NOT proven by CI; run the warm-session spec manually with a live session (see note)"; fi</automated>
  </verify>
  <acceptance_criteria>
    - The spec defines a warm-session test asserting no top-level login navigation, authed view visible, and sess_gpx set via the iframe.
    - The spec defines a logged-out test asserting the parent stays logged-out with no redirect loop.
    - `playwright test --list` discovers both tests (spec is valid and registered).
    - When a warm run.auth cookie jar is present, the warm-session test EXECUTES and passes (the real SSO-08 gate — asserts the authenticated view renders, no top-level /login navigation, sess_gpx set via the iframe). A green `--list` alone does NOT prove SSO-08; the executed warm-session assertion (or the manual warm-session run noted below) is the real gate.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Smoke silent-SSO e2e packages for run.flash and run.bib</name>
  <read_first>
    - apps/run.gpx/e2e/package.json (the e2e package shape: name, type module, scripts, pinned @playwright/test + typescript to reuse)
    - apps/run.gpx/e2e/playwright.config.ts and apps/run.gpx/e2e/tsconfig.json (config to mirror; change only the default BASE_URL port per app)
    - apps/run.gpx/e2e/lib/cookie-jar.ts (the run.auth cookie-jar loader to copy so the smoke test can inject a warm session)
  </read_first>
  <files>apps/run.flash/e2e/package.json, apps/run.flash/e2e/playwright.config.ts, apps/run.flash/e2e/tsconfig.json, apps/run.flash/e2e/lib/cookie-jar.ts, apps/run.flash/e2e/silent-sso-smoke.spec.ts, apps/run.bib/e2e/package.json, apps/run.bib/e2e/playwright.config.ts, apps/run.bib/e2e/tsconfig.json, apps/run.bib/e2e/lib/cookie-jar.ts, apps/run.bib/e2e/silent-sso-smoke.spec.ts</files>
  <action>
    Create minimal e2e packages under `apps/run.flash/e2e/` and `apps/run.bib/e2e/` mirroring run.gpx/e2e: copy package.json (rename to `run-flash-e2e` / `run-bib-e2e`, keep the SAME pinned @playwright/test and typescript versions — reuse the in-repo-vetted versions, add no other dependency), copy tsconfig.json, and copy playwright.config.ts changing only the default BASE_URL to each app's dev port (flash 3004, bib per its dev port). Copy `lib/cookie-jar.ts` (it already resolves the run.auth cookie jar via a relative path; adjust only the app-local paths if needed so it loads the same run.auth session).

    In each package add `silent-sso-smoke.spec.ts`: load a valid run.auth session via the copied cookie-jar loader, navigate to the app's public route, and assert the warm-session smoke invariant — the authenticated view renders and the top-level URL never navigated to a `/login`/`/signin` path (same main-frame navigation check as Task 1, trimmed to the warm case only). Keep each smoke spec small (single test).
  </action>
  <verify>
    <automated>cd apps/run.flash/e2e && npx playwright test silent-sso-smoke.spec.ts --list && cd ../../run.bib/e2e && npx playwright test silent-sso-smoke.spec.ts --list && echo "SMOKE_DISCOVERED"</automated>
  </verify>
  <acceptance_criteria>
    - flash and bib each have an e2e package that mirrors gpx/e2e and pins the same @playwright/test/typescript versions (no new package).
    - Each smoke spec loads a warm session and asserts authed view + no top-level login navigation.
    - `playwright test --list` discovers the smoke spec in each package.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (real iframe) → parent | The e2e is the only layer that exercises the real cross-frame postMessage + cookie flow end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-11 | Denial of Service | redirect loop for logged-out user | high | mitigate | The logged-out gpx test asserts no oscillation to /login and back — the concrete regression guard for the "no loop" invariant. |
| T-33-12 | Elevation of Privilege | silent auth without server session | medium | mitigate | The logged-out test asserts the parent stays logged-out (no authed view), confirming the iframe cannot fabricate a session. |
| T-33-SC | Tampering | package installs | low | accept | flash/bib e2e packages reuse the exact @playwright/test + typescript versions already locked in run.gpx/e2e and run.auth/e2e — no NEW third-party package. |
</threat_model>

<verification>
- `playwright test --list` discovers the gpx full spec and both smoke specs (specs valid and registered).
- When a warm run.auth cookie jar is present, the gpx warm-session test EXECUTES and passes (the real SSO-08 gate). A green `--list` alone does NOT prove SSO-08.
- NOTE (SSO-08 gate): green CI from `--list` does not prove the warm-session invariant. The real gate is the executed warm-session assertion (authed view renders, no top-level /login navigation, sess_gpx set via the iframe) — run with a live run.auth session (dev servers up + warm cookie jar), either via the second verify step when the jar is present or manually. This plan also depends on 33-01 (IdP change) being live for the invariant to hold.
- Executing runs (with dev servers + a warm cookie jar) demonstrate: warm → authed view, no top-level login nav, sess_ cookie set; logged-out → stays logged-out, no loop.
</verification>

<success_criteria>
- Full gpx e2e covers warm (no nav, no login render, cookie set, authed view) and logged-out (stays logged-out, no loop).
- flash + bib smoke specs cover the warm-session invariant.
</success_criteria>

<output>
Create `.planning/phases/33-oidc-silent-sso/33-06-SUMMARY.md` when done.
</output>
