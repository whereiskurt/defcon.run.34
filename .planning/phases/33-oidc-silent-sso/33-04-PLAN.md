---
phase: 33-oidc-silent-sso
plan: 04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/run.auth/e2e/tests/silent-sso.spec.ts
autonomous: true
requirements: [SSO-06]

must_haves:
  truths:
    - "A prompt=none authorize request with a live provider session returns a 302 carrying an authorization code, with no interaction render"
    - "A prompt=none authorize request with no session returns login_required"
    - "interactions.url resolves to the /{region}/api/oidc/interaction/{uid} route (not the /login page)"
    - "An authenticated interaction completes without rendering /login; an unauthenticated interaction still reaches /login"
  artifacts:
    - apps/run.auth/e2e/tests/silent-sso.spec.ts
  key_links:
    - "The integration suite reuses run.auth/e2e's existing @playwright/test package + cookie-jar/setup infra (loadCookiesForUser) — no new package, no new e2e project"
    - "The warm-session cases depend on plan-01's loadExistingGrant auto-consent + interactions.url repoint being live"
---

<objective>
Add IdP-level integration tests (Playwright, in the existing run.auth/e2e package) that prove the five locked provider behaviors from the design's Testing Strategy: silent code issuance for a warm session, login_required for no session, the interactions.url target, and the authenticated-vs-unauthenticated interaction render split.

Purpose: These assert the plan-01 IdP changes behave correctly at the HTTP/redirect level, independent of any RP wiring — the layer between the loadExistingGrant unit test and the full browser e2e.
Output: `apps/run.auth/e2e/tests/silent-sso.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-oidc-silent-sso/33-CONTEXT.md
@.planning/phases/33-oidc-silent-sso/33-PATTERNS.md
@docs/superpowers/specs/2026-07-03-oidc-silent-sso-design.md
@apps/run.auth/e2e/tests/session-valid.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Silent-code + no-session integration cases (prompt=none)</name>
  <read_first>
    - apps/run.auth/e2e/tests/session-valid.spec.ts (test harness shape: BASE_URL, region prefix, cookie loading)
    - apps/run.auth/e2e/lib/cookie-jar.ts lines 1-70 (loadCookiesForUser / hasCookieJarForUser / getEmailForRole — how a warm session is injected)
    - apps/run.auth/e2e/playwright.config.ts (baseURL, workers, project config)
    - apps/run.auth/webapp/src/config/index.ts lines 49-56,60-81 (issuer, routePrefix, client ids used to build the authorize URL)
  </read_first>
  <files>apps/run.auth/e2e/tests/silent-sso.spec.ts</files>
  <action>
    Create `tests/silent-sso.spec.ts` in the run.auth/e2e package (reuse its @playwright/test dep, region-prefix pattern, and cookie-jar helpers — introduce no new dependency or e2e project). Build a `prompt=none` authorization URL against the provider `/api/oidc/auth` endpoint for a registered first-party client (client_id from OIDC_* env used by the app; include response_type=code, a redirect_uri registered for that client, scope openid, state, nonce, PKCE code_challenge, and `prompt=none`). Use `request`/`page` with `waitUntil` set so redirects are NOT auto-followed, so the raw 302 Location can be inspected.

    Case A (warm session): load a valid session via `loadCookiesForUser` into the browser context, issue the prompt=none authorize, and assert the resulting redirect Location targets the client redirect_uri carrying a `code` query param (silent success) and that no interaction/login HTML was rendered along the way. Case B (no session): with a fresh context (no cookies), issue the same prompt=none authorize and assert the redirect Location carries `error=login_required` (or an equivalent prompt=none negative) rather than a code.
  </action>
  <verify>
    <automated>cd apps/run.auth/e2e && npx playwright test tests/silent-sso.spec.ts --grep "prompt=none"</automated>
  </verify>
  <acceptance_criteria>
    - Warm-session prompt=none yields a redirect containing `code=` at the client redirect_uri, with no rendered interaction page.
    - No-session prompt=none yields `error=login_required` (or documented equivalent), no code.
    - The spec imports only from the existing run.auth/e2e lib and @playwright/test.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: interactions.url target + authenticated vs unauthenticated interaction render</name>
  <read_first>
    - apps/run.auth/webapp/src/pages/api/oidc/interaction/[uid].ts lines 26-60 (auth check + redirect-to-login fallback; interaction completion)
    - apps/run.auth/webapp/src/config/oidc.ts interactions block (post plan-01 edit — the /{region}/api/oidc/interaction target)
    - apps/run.auth/e2e/lib/cookie-jar.ts (session injection for the authenticated case)
  </read_first>
  <files>apps/run.auth/e2e/tests/silent-sso.spec.ts</files>
  <action>
    Extend `silent-sso.spec.ts` with three interaction-layer cases. Case C: trigger a normal (interactive) authorize with NO session and assert the provider redirects into the server interaction route path `/api/oidc/interaction/{uid}` (region-prefixed in prod / bare in dev) — i.e. interactions.url now targets the interaction route, not `?oidc=` on `/login`. Case D (authenticated): with a valid session loaded, drive the interaction and assert the flow completes (redirect continues the OIDC flow / lands with a code) WITHOUT ever rendering the `/login` page — assert the login page markup / URL never appears. Case E (unauthenticated): with no session, assert the interaction route still redirects to `/{region}/login?oidc={uid}` (the preserved fallback). Reuse the authorize-URL builder and region-prefix helper from Task 1; keep the cases in the same describe block.
  </action>
  <verify>
    <automated>cd apps/run.auth/e2e && npx playwright test tests/silent-sso.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Case C asserts a redirect whose path contains `/api/oidc/interaction/`.
    - Case D asserts the authenticated interaction completes and `/login` is never rendered.
    - Case E asserts the unauthenticated interaction redirects to `/{region}/login?oidc=`.
    - Full `playwright test tests/silent-sso.spec.ts` passes all five cases.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → provider authorize/interaction endpoints | Tests assert the security-relevant redirect behavior (no silent grant without session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-09 | Elevation of Privilege | silent code without session | high | mitigate | Case B/E are the regression guard: no session must yield login_required and still reach /login — proves the invisible path cannot mint a code for an unauthenticated user. |
| T-33-SC | Tampering | package installs | low | accept | No new package or e2e project — reuses run.auth/e2e's existing @playwright/test and helpers. |
</threat_model>

<verification>
- `npx playwright test tests/silent-sso.spec.ts` passes all five locked integration cases.
</verification>

<success_criteria>
- Warm session → silent code; no session → login_required.
- interactions.url → interaction route; authenticated completes without /login; unauthenticated still reaches /login.
</success_criteria>

<output>
Create `.planning/phases/33-oidc-silent-sso/33-04-SUMMARY.md` when done.
</output>
