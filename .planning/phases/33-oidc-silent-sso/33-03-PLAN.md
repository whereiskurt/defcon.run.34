---
phase: 33-oidc-silent-sso
plan: 03
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - apps/run.flash/webapp/src/lib/silent-sso.ts
  - apps/run.flash/webapp/src/app/api/auth/silent-signin/route.ts
  - apps/run.flash/webapp/src/app/api/auth/auto-signin/route.ts
  - apps/run.flash/webapp/src/app/silent-callback/page.tsx
  - apps/run.flash/webapp/src/components/SilentSSO.tsx
  - apps/run.flash/webapp/src/app/layout.tsx
  - apps/run.flash/webapp/src/config/auth.ts
  - apps/run.bib/webapp/src/lib/silent-sso.ts
  - apps/run.bib/webapp/src/app/api/auth/silent-signin/route.ts
  - apps/run.bib/webapp/src/app/api/auth/auto-signin/route.ts
  - apps/run.bib/webapp/src/app/silent-callback/page.tsx
  - apps/run.bib/webapp/src/components/SilentSSO.tsx
  - apps/run.bib/webapp/src/app/providers.tsx
  - apps/run.bib/webapp/src/config/auth.ts
autonomous: true
requirements: [SSO-05]

must_haves:
  truths:
    - "The five silent-SSO unit files exist byte-identically in run.flash and run.bib at the same relative paths as the run.gpx canonical copy"
    - "<SilentSSO /> is mounted app-wide inside each app's SessionProvider (flash via layout.tsx, bib via providers.tsx)"
    - "Each app's config/auth.ts routes pages.error to its region-prefixed /silent-callback bridge"
  artifacts:
    - apps/run.flash/webapp/src/components/SilentSSO.tsx
    - apps/run.bib/webapp/src/components/SilentSSO.tsx
  key_links:
    - "The five unit files are copied verbatim from run.gpx (plan 02) — NO edits, so the plan-05 parity test passes"
    - "Only the mount point (layout vs providers) and the pages.error region literal differ per app; these are app-shell glue, not part of the parity-guarded unit"
---

<objective>
Place the canonical silent-SSO unit (authored in run.gpx by plan 02) byte-identically into run.flash and run.bib, and wire each app's shell (mount + pages.error) the same way gpx was wired.

Purpose: The prototype's delivery constraint is "authored once, placed identically." This plan performs the placement; plan 05's parity test guards against future drift.
Output: Five identical unit files in each of flash and bib, `<SilentSSO />` mounted in each, and `pages.error` routed to each app's bridge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-oidc-silent-sso/33-CONTEXT.md
@.planning/phases/33-oidc-silent-sso/33-PATTERNS.md
@apps/run.gpx/webapp/src/lib/silent-sso.ts
@apps/run.gpx/webapp/src/components/SilentSSO.tsx
@apps/run.gpx/webapp/src/app/silent-callback/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the five unit files into run.flash and mount + wire the flash shell</name>
  <read_first>
    - apps/run.gpx/webapp/src/lib/silent-sso.ts, src/app/api/auth/silent-signin/route.ts, src/app/api/auth/auto-signin/route.ts, src/app/silent-callback/page.tsx, src/components/SilentSSO.tsx (the canonical sources to copy verbatim)
    - apps/run.flash/webapp/src/app/layout.tsx lines 44-57 (the SessionProvider wrapper inside Providers — flash's mount point)
    - apps/run.flash/webapp/src/config/auth.ts lines 118-130 (pages block) and the region var near line 5
  </read_first>
  <files>apps/run.flash/webapp/src/lib/silent-sso.ts, apps/run.flash/webapp/src/app/api/auth/silent-signin/route.ts, apps/run.flash/webapp/src/app/api/auth/auto-signin/route.ts, apps/run.flash/webapp/src/app/silent-callback/page.tsx, apps/run.flash/webapp/src/components/SilentSSO.tsx, apps/run.flash/webapp/src/app/layout.tsx, apps/run.flash/webapp/src/config/auth.ts</files>
  <action>
    Copy the five canonical unit files from run.gpx into the SAME relative paths under `apps/run.flash/webapp/src/`. Copy them VERBATIM — byte-for-byte, no edits (they contain no app-specific literals by construction, so they need none). Because all three apps use the `@/* → ./src/*` alias, imports resolve unchanged.

    Mount `<SilentSSO />` in flash's app shell: flash wraps `<SessionProvider basePath={authBasePath}>{children}</SessionProvider>` inside layout.tsx (lines ~49-57). Add `<SilentSSO />` as a sibling of `{children}` inside that SessionProvider; import from `@/components/SilentSSO`. In flash's `config/auth.ts`, add `pages.error` pointing at the region-prefixed `/silent-callback` (mirror the existing signIn page region derivation; keep signIn intact).
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp && diff <(cat src/lib/silent-sso.ts src/app/api/auth/silent-signin/route.ts src/app/api/auth/auto-signin/route.ts src/app/silent-callback/page.tsx src/components/SilentSSO.tsx) <(cat ../../run.gpx/webapp/src/lib/silent-sso.ts ../../run.gpx/webapp/src/app/api/auth/silent-signin/route.ts ../../run.gpx/webapp/src/app/api/auth/auto-signin/route.ts ../../run.gpx/webapp/src/app/silent-callback/page.tsx ../../run.gpx/webapp/src/components/SilentSSO.tsx) && grep -q "SilentSSO" src/app/layout.tsx && grep -q "silent-callback" src/config/auth.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The concatenated flash unit files are byte-identical to the gpx originals (diff is empty).
    - `<SilentSSO />` renders inside flash's SessionProvider; pages.error → region-prefixed silent-callback.
    - `npm run build` succeeds for run.flash/webapp.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Copy the five unit files into run.bib and mount + wire the bib shell</name>
  <read_first>
    - the five canonical run.gpx unit files (as in Task 1)
    - apps/run.bib/webapp/src/app/providers.tsx lines 26-38 (the SessionProvider inside Providers — bib's mount point)
    - apps/run.bib/webapp/src/config/auth.ts lines 118-130 (pages block) and the region var near line 5
  </read_first>
  <files>apps/run.bib/webapp/src/lib/silent-sso.ts, apps/run.bib/webapp/src/app/api/auth/silent-signin/route.ts, apps/run.bib/webapp/src/app/api/auth/auto-signin/route.ts, apps/run.bib/webapp/src/app/silent-callback/page.tsx, apps/run.bib/webapp/src/components/SilentSSO.tsx, apps/run.bib/webapp/src/app/providers.tsx, apps/run.bib/webapp/src/config/auth.ts</files>
  <action>
    Copy the same five canonical unit files from run.gpx into the SAME relative paths under `apps/run.bib/webapp/src/`, VERBATIM (byte-for-byte).

    Mount `<SilentSSO />` in bib's app shell: bib's `Providers` (providers.tsx) already wraps `<SessionProvider basePath={authBasePath}>{children}</SessionProvider>`. Add `<SilentSSO />` as a sibling of `{children}` inside that SessionProvider; import from `@/components/SilentSSO`. In bib's `config/auth.ts`, add `pages.error` pointing at the region-prefixed `/silent-callback` (mirror signIn region derivation; keep signIn intact).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && diff <(cat src/lib/silent-sso.ts src/app/api/auth/silent-signin/route.ts src/app/api/auth/auto-signin/route.ts src/app/silent-callback/page.tsx src/components/SilentSSO.tsx) <(cat ../../run.gpx/webapp/src/lib/silent-sso.ts ../../run.gpx/webapp/src/app/api/auth/silent-signin/route.ts ../../run.gpx/webapp/src/app/api/auth/auto-signin/route.ts ../../run.gpx/webapp/src/app/silent-callback/page.tsx ../../run.gpx/webapp/src/components/SilentSSO.tsx) && grep -q "SilentSSO" src/app/providers.tsx && grep -q "silent-callback" src/config/auth.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The concatenated bib unit files are byte-identical to the gpx originals (diff is empty).
    - `<SilentSSO />` renders inside bib's SessionProvider; pages.error → region-prefixed silent-callback.
    - `npm run build` succeeds for run.bib/webapp.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copied unit → flash/bib runtime | Same cross-frame postMessage boundary as gpx, now in two more apps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering | copy drift across apps | medium | mitigate | Copies are byte-identical (per-task diff gate); plan 05's parity test enforces this continuously so a future divergent edit fails CI. |
| T-33-04b | Spoofing | flash/bib parent listeners | high | mitigate | Inherited from the identical unit — origin check via decideParentAction against window.location.origin holds in every app. |
| T-33-SC | Tampering | package installs | low | accept | No packages installed — verbatim source copies only; builds use each app's existing toolchain. |
</threat_model>

<verification>
- Per-app diff gate: flash and bib unit files byte-match gpx.
- `npm run build` passes for run.flash/webapp and run.bib/webapp.
</verification>

<success_criteria>
- Byte-identical silent-SSO unit present in gpx, flash, bib.
- Mounted + pages.error wired in flash and bib; both build.
</success_criteria>

<output>
Create `.planning/phases/33-oidc-silent-sso/33-03-SUMMARY.md` when done.
</output>
