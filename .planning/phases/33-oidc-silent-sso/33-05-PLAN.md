---
phase: 33-oidc-silent-sso
plan: 05
type: execute
wave: 3
depends_on: ["33-03"]
files_modified:
  - apps/run.bib/webapp/src/__tests__/silent-sso-parity.test.ts
  - apps/run.bib/webapp/src/__tests__/silent-sso-unit.test.ts
autonomous: true
requirements: [SSO-05, SSO-07]

must_haves:
  truths:
    - "A parity test fails if the five silent-SSO unit files diverge across run.gpx, run.flash, run.bib"
    - "resolveSilentStatus maps an error-free / param-less callback landing to success (next-auth already consumed the code at its own callback) and every next-auth error param / prompt=none negative to login_required — success is NOT keyed on a code param"
    - "decideParentAction ignores foreign-origin or wrong-type messages and only maps same-origin silent-sso messages to authenticated / stay-logged-out"
    - "The timeout constant is in the ~4-5s target range"
  artifacts:
    - apps/run.bib/webapp/src/__tests__/silent-sso-parity.test.ts
    - apps/run.bib/webapp/src/__tests__/silent-sso-unit.test.ts
  key_links:
    - "Tests run in run.bib's EXISTING node-env vitest (bib has vitest; no jsdom needed because the logic lives in pure helpers) — no new package anywhere"
    - "Because parity guarantees the three copies are identical, testing bib's copy of lib/silent-sso.ts is equivalent to testing all three"
---

<objective>
Add the parity test that guards the copied silent-SSO unit against drift, plus unit tests for the pure decision logic (`resolveSilentStatus`, `decideParentAction`) — both in run.bib's existing vitest runner, node environment.

Purpose: Parity is the delivery constraint's safety net (authored once, placed identically). The logic tests satisfy the locked "RP client unit" testing intent (success/negative mapping + same-origin gating) without booting a DOM, because the logic was extracted into pure helpers in plan 02.
Output: `silent-sso-parity.test.ts` and `silent-sso-unit.test.ts` under run.bib's `src/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-oidc-silent-sso/33-CONTEXT.md
@.planning/phases/33-oidc-silent-sso/33-PATTERNS.md
@apps/run.bib/webapp/vitest.config.ts
@apps/run.bib/webapp/src/lib/silent-sso.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity test — the five unit files are byte-identical across gpx, flash, bib</name>
  <read_first>
    - apps/run.bib/webapp/vitest.config.ts (node env; include glob src/__tests__/**; @ alias)
    - apps/run.bib/webapp/src/__tests__/runner-code.test.ts (an existing node-env vitest test to model imports/structure on)
  </read_first>
  <files>apps/run.bib/webapp/src/__tests__/silent-sso-parity.test.ts</files>
  <action>
    Create `src/__tests__/silent-sso-parity.test.ts` (vitest, node env). Using `node:fs` + `node:path`, read the five unit files from each of the three apps at their fixed relative paths (`src/lib/silent-sso.ts`, `src/app/api/auth/silent-signin/route.ts`, `src/app/api/auth/auto-signin/route.ts`, `src/app/silent-callback/page.tsx`, `src/components/SilentSSO.tsx`) — resolve the three webapp roots relative to the test file (bib is `../../..`, its siblings are `run.gpx/webapp` and `run.flash/webapp`). For each of the five relative paths, assert the run.flash and run.bib contents equal the run.gpx content exactly (byte-for-byte string equality). Fail with a message naming the divergent file. Treat run.gpx as the canonical source of truth.
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/silent-sso-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test compares all five unit files across gpx/flash/bib and passes when identical.
    - A deliberate one-character change in any copy makes the test fail (drift is detected).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unit tests for resolveSilentStatus, decideParentAction, and the timeout constant</name>
  <read_first>
    - apps/run.bib/webapp/src/lib/silent-sso.ts (the helper exports under test — resolveSilentStatus, decideParentAction, SILENT_SSO_MESSAGE_TYPE, SILENT_SSO_TIMEOUT_MS). Read the ACTUAL implemented `resolveSilentStatus` contract before writing tests: success is keyed on the ABSENCE of a next-auth error param (next-auth already consumed the `code` at its own callback and lands on `/silent-callback` param-less), NOT on the presence of a `code` param. Encode the exact next-auth@5 error param name that plan 02 confirmed and implemented — read it from the source, do not invent one.
    - apps/run.bib/webapp/src/__tests__/runner-code.test.ts (describe/it/expect import style)
  </read_first>
  <files>apps/run.bib/webapp/src/__tests__/silent-sso-unit.test.ts</files>
  <action>
    Create `src/__tests__/silent-sso-unit.test.ts` importing the helpers via the `@` alias from `@/lib/silent-sso`. Cover: (1) `resolveSilentStatus` SUCCESS CONTRACT — returns "success" for a param-less URLSearchParams (the real next-auth success landing, where the code was already consumed at next-auth's own callback), and also for params that carry no error (e.g. a benign non-error param). It must NOT be keyed on a `code` param — do not assert `code` ⇒ success; the real success shape is error-free. Returns "login_required" for each of `error=login_required`, `error=interaction_required`, `error=consent_required`, `error=access_denied`, and for whatever next-auth@5 error param plan 02 confirmed (e.g. an `error=<next-auth error code>`) — every error/negative outcome → login_required. (2) `decideParentAction` returns "ignore" when `evt.origin` differs from the expected origin even if the type matches; returns "ignore" when the origin matches but `evt.data.type` is not `SILENT_SSO_MESSAGE_TYPE`; returns "authenticated" for a same-origin `{ type, status: "success" }`; returns "stay-logged-out" for a same-origin `{ type, status: "login_required" }`. (3) assert `SILENT_SSO_TIMEOUT_MS` is a number within the inclusive range 4000-5000 (the ~4-5s target).
  </action>
  <verify>
    <automated>cd apps/run.bib/webapp && npx vitest run src/__tests__/silent-sso-unit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - resolveSilentStatus success is asserted from an error-free / param-less input (NOT from a `code` param), and login_required from every error/negative including the four named OIDC negatives and the confirmed next-auth@5 error param.
    - decideParentAction proves the same-origin gate (foreign origin → ignore) and the type gate.
    - The timeout constant is asserted within 4000-5000ms.
    - `npx vitest run` passes both new suites.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → unit source | Encodes the security invariants (origin gate, negative normalization) as regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-10 | Spoofing | origin-gate regression | high | mitigate | The decideParentAction test pins the foreign-origin → ignore behavior so a future edit that drops the origin check fails CI. |
| T-33-08b | Tampering | copy drift | medium | mitigate | The parity test is the continuous enforcement of the "placed identically" constraint. |
| T-33-SC | Tampering | package installs | low | accept | No new package — reuses run.bib's existing node-env vitest; no jsdom (logic is pure). |
</threat_model>

<verification>
- `npx vitest run` in run.bib/webapp passes the parity and unit suites.
</verification>

<success_criteria>
- Parity test enforces byte-identical copies across the three apps.
- Pure-logic tests cover status mapping, the same-origin gate, and the timeout window.
</success_criteria>

<output>
Create `.planning/phases/33-oidc-silent-sso/33-05-SUMMARY.md` when done.
</output>
