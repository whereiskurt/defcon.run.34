---
phase: 41-abuse-detection
plan: 05
type: execute
wave: 3
depends_on: ["41-01", "41-02", "41-03", "41-04"]
files_modified:
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/region/us-east-1/abuse-detection/terragrunt.hcl
autonomous: false
requirements: [AD-08]
must_haves:
  truths:
    - "site.hcl carries an abuse_detection block with enabled=false at merge and every threshold (cron_minutes, lookback_hours, session_hours, session_gap_min, posts_per_5min, requests_per_5min, escalation_multiplier, digest_hour_utc) as a one-line knob (AD-08)."
    - "The terragrunt unit excludes itself when abuse_detection.enabled is false (ships dark) and derives the REAL ALB-log bucket name from the network unit's alb_logs_bucket_name output — never a guessed name (Phase 40 lesson #2)."
    - "A scoped `terragrunt plan` (modules=abuse-detection, region=us-east-1) validates the module — NOT bare `terraform validate` (Phase 40 lesson #1) — and a synthetic/real request burst is flagged end-to-end: query flags the IP, SNS emails, findings.jsonl is written (Phase 40 lesson #3)."
  artifacts:
    - infra/terraform/live/site/region/us-east-1/abuse-detection/terragrunt.hcl
    - infra/terraform/live/site/site.hcl
  key_links:
    - "The unit's `dependency \"network\"` feeds alb_logs_bucket_name into the module — the seam that makes the Glue table read the real bucket."
    - "site.hcl.abuse_detection.enabled gates BOTH the terragrunt exclude AND the EventBridge rule state (schedule_enabled) so 'dark' is dark everywhere."
    - "The reused SNS topic dcr-admin-reports-tripwire must exist (admin-reports enabled) before an end-to-end alert can fire — a checkpoint precondition."
---

<objective>
Wire the abuse-detection module into the live infrastructure: add the `abuse_detection` block to `site.hcl` (mirroring `admin_reports`, tight pre-con defaults, `enabled=false`), author the us-east-1 terragrunt unit (exclude-if-disabled, `dependency "network"` for the real ALB-log bucket name, providers include, inputs mapping site.hcl → module vars), then run the blocking deploy/verify checkpoint that enables it, plans it with a SCOPED `terragrunt plan`, applies, and confirms a burst is flagged end-to-end.

Purpose: AD-08 (site.hcl thresholds + enabled gate) plus the real end-to-end verification the whole phase exists to deliver.
Output: the `abuse_detection` site.hcl block, the terragrunt unit, and a verified live detection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41-abuse-detection/41-CONTEXT.md
@docs/superpowers/specs/2026-07-05-abuse-detection-design.md

# The admin_reports block to mirror (structure, enabled gate, thresholds, alert_email):
@infra/terraform/live/site/site.hcl
# The terragrunt unit to mirror (exclude-if-disabled, dependency+mock, providers include, inputs):
@infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl
# The network output that supplies the REAL ALB-log bucket name:
@infra/terraform/modules/network/v1.0.0/outputs.tf
# The module inputs this unit must satisfy:
@infra/terraform/modules/abuse-detection/v1.0.0/variables.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: abuse_detection block in site.hcl (AD-08)</name>
  <files>infra/terraform/live/site/site.hcl</files>
  <read_first>
    - infra/terraform/live/site/site.hcl (the `admin_reports = {` block ~line 418: enabled gate, alert_email via TF_VAR_ADMIN_EMAIL, thresholds map) — mirror its shape and placement.
  </read_first>
  <action>
    Using Edit (scoped — never rewrite the whole file), add a new `abuse_detection = { ... }` local block immediately after the `admin_reports` block's closing brace inside the same `locals`. Mirror the admin_reports conventions with a header comment explaining it ships dark. Keys (AD-08 pre-con defaults):
    - `enabled = false` (gate the whole unit; the unit excludes itself and the EventBridge rule ships DISABLED). Comment: flip true only during the deploy checkpoint after a manual Athena query confirms the ALB-log schema parses.
    - `cron_minutes = 30`, `lookback_hours = 3`, `session_hours = 2`, `session_gap_min = 15`, `posts_per_5min = 30`, `requests_per_5min = 100`, `escalation_multiplier = 3`, `digest_hour_utc = 13`.
    - `athena_bytes_scanned_cutoff = 10737418240` (10 GiB scan cap).
    - `sns_topic_name = "dcr-admin-reports-tripwire"` (reuse the Phase 40 topic; comment that no second topic is created).
    Add a comment mapping each key to its module variable (like admin_reports maps thresholds to the module). Do not touch the admin_reports block or any other local.
  </action>
  <acceptance_criteria>
    - `terragrunt hclfmt --terragrunt-check --terragrunt-working-dir infra/terraform/live/site` (or `terraform fmt -check` on the file if hclfmt unavailable) leaves site.hcl formatted; at minimum the file still parses (see verify).
    - site.hcl contains a `abuse_detection` block with `enabled = false` and keys `posts_per_5min`, `requests_per_5min`, `session_hours`, `sns_topic_name` (grep each).
    - The admin_reports block is unchanged: `grep -c 'dcr-admin-reports-tripwire' site.hcl` is >= 1 and the admin_reports `enabled` line still present (spot-grep).
  </acceptance_criteria>
  <verify>
    <automated>F=infra/terraform/live/site/site.hcl; grep -q 'abuse_detection' "$F" && grep -q 'posts_per_5min' "$F" && grep -q 'session_hours' "$F" && grep -q 'sns_topic_name' "$F" && grep -q 'admin_reports' "$F"</automated>
  </verify>
  <done>site.hcl exposes the abuse_detection block with enabled=false and all eight thresholds + scan cap + reused topic name as one-line knobs, admin_reports untouched.</done>
</task>

<task type="auto">
  <name>Task 2: us-east-1 terragrunt unit (exclude-if-disabled + network dependency + inputs)</name>
  <files>infra/terraform/live/site/region/us-east-1/abuse-detection/terragrunt.hcl</files>
  <read_first>
    - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl (exclude block, dependency+mock_outputs, include "providers", terraform source, inputs mapping site.hcl locals)
    - infra/terraform/modules/network/v1.0.0/outputs.tf lines 139-146 (alb_logs_bucket_name output)
  </read_first>
  <action>
    Author the new unit at region/us-east-1/abuse-detection/terragrunt.hcl, mirroring admin-reports (us-east-1 only, self-contained, no skip include needed). Include:
    - `locals { site_vars = read_terragrunt_config(find_in_parent_folders("site.hcl")) }`.
    - `exclude { if = !try(local.site_vars.locals.abuse_detection.enabled, false) actions = ["all"] }` — ships dark, mirrors admin-reports/cloudtrail exclude-if-disabled.
    - `dependency "network" { config_path = "../network" mock_outputs = { alb_logs_bucket_name = "logs-alb-use1-dc34-mockmock" } mock_outputs_allowed_terraform_commands = ["init","validate","plan","destroy"] }` — this is the seam that feeds the REAL ALB-log bucket name (Phase 40 lesson #2: derive, never guess). The mock lets plan render before network state exists.
    - `include "providers" { path = "${find_in_parent_folders("providers")}/regional.hcl" }` — this generates provider.tf (with required_providers); the module MUST NOT declare its own (Phase 40 lesson #1).
    - `terraform { source = "${dirname(find_in_parent_folders("AGENTS.md"))}/infra/terraform/modules/abuse-detection/v1.0.0" }`.
    - `inputs = { ... }` mapping: `site` and `region` objects from site_vars/region.hcl (mirror how admin-reports/other units pass site.label; for `region` build `{ label = "use1", full = "us-east-1" }` or read region.hcl), `alb_logs_bucket_name = dependency.network.outputs.alb_logs_bucket_name`, and every threshold/gate from `local.site_vars.locals.abuse_detection.*` (cron_minutes, lookback_hours, session_hours, session_gap_min, posts_per_5min, requests_per_5min, escalation_multiplier, digest_hour_utc, athena_bytes_scanned_cutoff, sns_topic_name), plus `schedule_enabled = local.site_vars.locals.abuse_detection.enabled` (so enabling the unit also enables the cron), and tags {Site, Component=abuse-detection, ManagedBy=terragrunt}.
    Add a header comment: us-east-1 only; validation is scoped `terragrunt plan` (modules=abuse-detection, region=us-east-1), not bare `terraform validate`.
  </action>
  <acceptance_criteria>
    - The unit references `dependency.network.outputs.alb_logs_bucket_name` and does NOT hardcode a real bucket (`grep -c 'logs-alb-use1-dc34-[a-z0-9]' terragrunt.hcl` counts only the mock, not a real suffix — the real value comes from the dependency).
    - The unit has an `exclude` block keyed on `abuse_detection.enabled` (grep both `exclude` and `abuse_detection.enabled`).
    - The unit maps `schedule_enabled` to `abuse_detection.enabled` and passes `sns_topic_name` (grep both).
    - The terraform source points at `modules/abuse-detection/v1.0.0` (grep present).
    - `terragrunt hclfmt --terragrunt-check --terragrunt-working-dir infra/terraform/live/site/region/us-east-1/abuse-detection` passes (or the file parses via a terragrunt render).
  </acceptance_criteria>
  <verify>
    <automated>F=infra/terraform/live/site/region/us-east-1/abuse-detection/terragrunt.hcl; test -f "$F" && grep -q 'dependency.network.outputs.alb_logs_bucket_name' "$F" && grep -q 'abuse_detection.enabled' "$F" && grep -q 'schedule_enabled' "$F" && grep -q 'sns_topic_name' "$F" && grep -q 'modules/abuse-detection/v1.0.0' "$F"</automated>
  </verify>
  <done>The us-east-1 unit excludes itself when disabled, wires the real ALB-log bucket from the network dependency, maps every site.hcl threshold + the enabled→schedule gate into the module, and sources the versioned module.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Deploy + end-to-end burst verification (scoped terragrunt plan, NOT terraform validate)</name>
  <action>
    Enable the unit (temporarily set abuse_detection.enabled=true), validate via a SCOPED terragrunt plan (modules=abuse-detection, region=us-east-1) rather than bare terraform validate, apply, then fire a synthetic-or-real request burst and confirm the detection flags the IP end-to-end (Athena query → SNS email → findings.jsonl). Confirm dedup (no second same-day email) and restore the dark posture (enabled=false) for the committed state unless the operator elects to leave it live. Full steps in how-to-verify below.
  </action>
  <what-built>
    The complete abuse-detection module (Glue table alb_access_logs, workgroup dcr-abuse-analysis, abuse-detector Lambda + cron, dedup/escalation, reused-SNS alert, findings.jsonl + digest), the site.hcl abuse_detection block (enabled=false), and the us-east-1 terragrunt unit (exclude-if-disabled, network-derived ALB-log bucket).
  </what-built>
  <how-to-verify>
    PRECONDITION: the reused SNS topic `dcr-admin-reports-tripwire` must exist (admin-reports enabled/applied) and its email subscription confirmed — otherwise the end-to-end alert cannot deliver. Confirm before enabling.

    1. Scoped VALIDATION (Phase 40 lesson #1 — bare `terraform validate` misses provider/dep issues): run the module's plan through terragrunt, NOT terraform validate. Temporarily set `abuse_detection.enabled = true` in site.hcl (local, do not commit yet), then from `infra/terraform/live/site/region/us-east-1/abuse-detection` run `terragrunt plan` (or trigger the CI "Infra: Terragrunt Plan" workflow with region=us-east-1, modules=abuse-detection). Confirm the plan renders with NO "Duplicate required providers configuration" error and shows the Glue table, workgroup, results bucket, Lambda, IAM role, and EventBridge rule to CREATE.
    2. Confirm the plan's Glue table `storage.location.template` resolves to the REAL ALB-log bucket from the network dependency (Phase 40 lesson #2), not a mock/guess.
    3. APPLY the unit (with enabled=true) via terragrunt apply / CI apply.
    4. END-TO-END burst (Phase 40 lesson #3 — real detection, not plan-only): either (a) replay a burst of >`posts_per_5min` POSTs (or sustained >2h activity, or lower a threshold temporarily) against a prod ALB endpoint from a known source IP, or (b) backfill a synthetic ALB-log partition with lines that trip Q1 and Q2. Then invoke the Lambda manually (or wait a cron cycle) and confirm ALL THREE seams fire:
       - the detection query flags the source IP (check the Lambda logs / Athena result),
       - an SNS email arrives at the operator inbox with the IP + UA + rule + counts,
       - `s3://<abuse-detection bucket>/abuse/YYYY-MM-DD/findings.jsonl` contains a finding in the fixed schema for that IP.
    5. Confirm dedup: a second Lambda run the same UTC day for the same IP sends NO second email (unless it crosses escalation_multiplier).
    6. ROLLBACK posture: set `abuse_detection.enabled = false` again for the committed state (ship dark) unless the operator decides to leave it live. Confirm the unit then excludes (plan shows nothing / destroy of the created resources if you want it fully dark).
    Record which verification path (real burst vs synthetic partition) was used and the observed IP in the summary.
  </how-to-verify>
  <resume-signal>Type "approved" once the scoped terragrunt plan is clean AND a burst was flagged end-to-end (query → SNS email → findings.jsonl), or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site.hcl enabled gate → live resources | A mis-set gate could turn on cost/emails unexpectedly. |
| Operator → terragrunt apply | Applying the module to prod; must be reviewed via scoped plan first. |
| network dependency → module input | The real ALB-log bucket name must come from state, not a literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-09 | Spoofing/Misconfig | enabled gate | medium | mitigate | `enabled=false` at merge; the unit excludes-if-disabled AND the EventBridge rule state derives from the same flag, so a merged-but-dark unit provisions nothing and pages no one until deliberately enabled. |
| T-41-10 | Tampering | terragrunt apply to prod | medium | mitigate | Blocking human checkpoint; scoped `terragrunt plan` reviewed before apply (Phase 40 lesson #1); real end-to-end burst verified (lesson #3). |
| T-41-14 | Information Disclosure | wrong ALB-log bucket | low | mitigate | Bucket name derived from the network dependency output, verified in the plan to resolve to the real bucket (lesson #2), never a hardcoded guess. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | No installs anywhere in the phase; the blocking checkpoint is the human gate for the apply itself. |
</threat_model>

<verification>
- Scoped `terragrunt plan` (modules=abuse-detection, region=us-east-1) renders clean with no duplicate-provider error (Phase 40 lesson #1).
- Plan resolves the real ALB-log bucket from the network dependency (lesson #2).
- End-to-end burst flagged: query → SNS email → findings.jsonl (lesson #3).
- Dedup confirmed (no second same-day email).
- Committed state ships dark (enabled=false) unless the operator elects otherwise.
</verification>

<success_criteria>
AD-08 satisfied and the phase proven live: thresholds + enabled gate are site.hcl knobs, the unit wires the real ALB-log bucket and ships dark, and a real burst was detected end-to-end through the reused Phase 40 alert path with an automation-ready finding written to S3.
</success_criteria>

## Artifacts this phase produces (Plan 05 slice)

- `infra/terraform/live/site/site.hcl` — new `abuse_detection` local block (enabled=false, 8 thresholds, scan cap, reused topic name).
- `infra/terraform/live/site/region/us-east-1/abuse-detection/terragrunt.hcl` — us-east-1 unit (exclude-if-disabled, network dependency for real ALB-log bucket, providers include, full inputs mapping, enabled→schedule gate).

<output>
Create `.planning/phases/41-abuse-detection/41-05-SUMMARY.md` when done.
</output>
