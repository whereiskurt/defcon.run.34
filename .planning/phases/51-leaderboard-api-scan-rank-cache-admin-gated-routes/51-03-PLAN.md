---
phase: 51-leaderboard-api-scan-rank-cache-admin-gated-routes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/route.ts
  - apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/__tests__/route.test.ts
autonomous: true
requirements: [LDBR-08]

must_haves:
  truths:
    - "A non-admin (no session, or session without an admin group, or failing fresh-claims revalidation) receives a bare 404 on GET /api/leaderboard/[userId]/accomplishments — never a 403, never a body (SC #1)."
    - "An admin receives 200 JSON of the runner's accomplishments including metadata.polyline for GPX/check-in runs (SC #4)."
    - "A clearly-marked, currently-no-op privacy-filter hook sits between the fetch and the response, where spec §9's launch-time filter will slot in."
  artifacts:
    - "apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/route.ts"
    - "apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/__tests__/route.test.ts"
  key_links:
    - "route imports requireAdmin + revalidateAdmin from @/lib/admin-gate and getAccomplishmentsByUser from @/entities/accomplishment — all shipped, none re-implemented."
    - "revalidateAdmin is called with session.user.authUserId (the OIDC sub), NOT session.user.id (the adapter uuid) — the Phase-43 identity landmine."
---

<objective>
Ship the lazy per-user runs route: `GET /api/leaderboard/[userId]/accomplishments` — admin-gated (denial → 404), returning a runner's accomplishments (incl. `metadata.polyline`) via the Phase-49 `getAccomplishmentsByUser`, with a marked no-op privacy hook for launch (spec §9). This plan is independent of the ranking/cache core (plan 01) — it reuses only already-shipped helpers, so it runs in wave 1 alongside it.

Purpose: The accordion (Phase 52) lazy-loads one runner's runs on row expand. This is that endpoint. It is a thin shell over shipped code; the only real work is the exact admin-gate contract (404 non-disclosure + fresh-claims revalidation) and marking the privacy seam so launch doesn't lose it.
Output: the route handler + an importable-handler vitest proving 404-on-non-admin / 200-with-admin / polyline-present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-leaderboard-api-scan-rank-cache-admin-gated-routes/51-CONTEXT.md

# Reuse targets + the exact patterns to mirror
@apps/run.human/webapp/src/lib/admin-gate.ts
@apps/run.human/webapp/src/entities/accomplishment.ts
@apps/run.human/webapp/src/app/api/admin/users/route.ts
@apps/run.human/webapp/src/app/api/internal/accomplishment/__tests__/route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-user accomplishments route + admin-gate/polyline test</name>
  <files>apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/route.ts, apps/run.human/webapp/src/app/api/leaderboard/[userId]/accomplishments/__tests__/route.test.ts</files>
  <behavior>
    Route test mocks `@/config/auth` (exporting `auth` + `revalidateAdmin`) and `@/entities/accomplishment` (`getAccomplishmentsByUser`), imports `GET` from `../route`, and drives three branches (mirror the Phase-50 route-test convention):
    - Non-admin → 404: `auth()` resolves a session with no admin group (e.g. `{ user: { services: [] } }`) → response status 404, empty body, and getAccomplishmentsByUser is NEVER called.
    - Admin happy path → 200: `auth()` resolves `{ user: { services: ["admin"], authUserId: "sub-1" } }`, `revalidateAdmin` resolves true, `getAccomplishmentsByUser` returns one row whose `metadata.polyline` is a `[{lat,lng}]` list → status 200, JSON `{ accomplishments: [...] }`, and the returned row still carries `metadata.polyline` (SC #4) plus type/source/name/completedAt/year.
    - Stale-admin → 404: admin group present but `revalidateAdmin` resolves false → 404 (fresh-claims deny).
    `params` is passed as a Promise (`{ params: Promise.resolve({ userId }) }`) to match the Next 16 dynamic-route signature.
  </behavior>
  <action>
    Create the route handler. Set `export const runtime = "nodejs"` and `export const dynamic = "force-dynamic"` (mirror `app/api/admin/users/route.ts` — the ElectroDB scan/query pipeline needs Node, and this is always a live read).

    Gate EXACTLY like `app/api/admin/users/route.ts` (the LOCKED 404 non-disclosure contract, both routes of this phase):
    - `const session = await auth();`
    - `const gate = requireAdmin(session); if (!gate.ok) return new Response(null, { status: 404 });`
    - `const authUserId = session?.user?.authUserId; if (!authUserId || !(await revalidateAdmin(authUserId))) return new Response(null, { status: 404 });`
    Use a single `NOT_FOUND = () => new Response(null, { status: 404 })` helper. NEVER return 403/401. Import `requireAdmin` + `revalidateAdmin` from `@/lib/admin-gate` and `auth` from `@/config/auth`.

    IDENTITY LANDMINE (Phase 43): revalidateAdmin MUST receive `session.user.authUserId` (the auth.defcon.run OIDC sub), not `session.user.id` (the Auth.js adapter uuid) — the run.auth validate endpoint is keyed by the OIDC sub; the adapter id silently fails and 404s a real admin.

    Signature: `export async function GET(_request: Request, { params }: { params: Promise<{ userId: string }> })`. After the gate, `const { userId } = await params;` then `const accomplishments = await getAccomplishmentsByUser(userId);` (Phase-49 helper, reused as-is).

    PRIVACY HOOK (spec §9, LOCKED "mark the hook point"): add a small local helper `applyPrivacyFilter(items)` that TODAY returns `items` unchanged (identity no-op), with a comment block stating: admin-only board shows all runs now; at launch this filters other runners' `isPrivate` check-ins and share-ineligible GPX when the gate relaxes from admin-only to signed-in. Call it as `const visible = applyPrivacyFilter(accomplishments);` so the seam is named and obvious, not buried in a comment.

    Shape the response after the DC33 per-user route: map `visible` to `{ type, source, name, description, completedAt, year, metadata }` (KEEP `metadata` whole so `metadata.polyline` survives for the Phase-52 renderer) and return `Response.json({ accomplishments: rows }, { headers: { "Cache-Control": "no-store" } })`.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && source ~/.nvm/nvm.sh && nvm use 23.6.0 && npx vitest run "src/app/api/leaderboard/[userId]/accomplishments/__tests__/route.test.ts" 2>&1 | tail -20</automated>
  </verify>
  <done>All three route branches pass: non-admin + stale-admin → 404 (getAccomplishmentsByUser not called on the non-admin path); admin → 200 with `{accomplishments}` including `metadata.polyline`. Denial is always a bare 404, never 403.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route handler | Untrusted HTTP request with a session cookie crosses into an admin-only read of another runner's runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-03 | Elevation of Privilege | GET /api/leaderboard/[userId]/accomplishments | high | mitigate | requireAdmin gates entry; a non-admin/anonymous caller gets 404 and no data-layer call fires. Proven by the non-admin branch test. |
| T-51-04 | Information Disclosure | route existence + fresh claims | high | mitigate | Denial maps to 404 (never 403) so the hidden board's route existence is not advertised; revalidateAdmin(authUserId) denies a just-revoked admin inside the JWT staleness window (fail-closed). |
| T-51-05 | Information Disclosure | others' private runs at launch | low | accept | Privacy filter deferred to launch (spec §9) — the surface is admin-only today, so all runs are shown. The named `applyPrivacyFilter` no-op hook records the debt so it is not lost. |
</threat_model>

<verification>
- The route vitest above passes (Node ≥22.12 via `nvm use 23.6.0`, from apps/run.human/webapp).
- `npx tsc --noEmit` introduces no NEW errors: PASS iff only the two KNOWN pre-existing out-of-scope errors remain (`dropdown-user.tsx` svg, `__tests__/checkin.test.ts` `.model`) and none reference the new route file.
</verification>

<success_criteria>
The per-user route is admin-gated with the 404 non-disclosure + fresh-claims contract, returns a runner's accomplishments including `metadata.polyline`, and carries a marked no-op privacy hook. Satisfies Phase-51 SC #1 (non-admin 404) and SC #4 (per-user runs incl. polyline) for LDBR-08.
</success_criteria>

<output>
Create `.planning/phases/51-leaderboard-api-scan-rank-cache-admin-gated-routes/51-03-SUMMARY.md` when done.
</output>
