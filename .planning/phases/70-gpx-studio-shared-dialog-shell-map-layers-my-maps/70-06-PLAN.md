---
phase: 70-gpx-studio-shared-dialog-shell-map-layers-my-maps
plan: 06
type: execute
wave: 4
depends_on: ["70-01", "70-02", "70-03", "70-04", "70-05"]
files_modified:
  - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/dialog-shell-probe.cjs
  - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-pre-deploy.txt
  - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt
autonomous: true
requirements: [DLGS-06]

must_haves:
  truths:
    - "The run.gpx webapp vitest suite on Node 22.12 is either green, or its failing set is proven identical to the origin/main baseline — a NEW failure is the only vitest condition that stops the run"
    - "svelte-check reports zero errors for every file this phase touched (the ~26-30 pre-existing upstream errors are untouched and not counted)"
    - "build-frontend.sh completes cleanly"
    - "The feature branch is merged to main via a squash merge and the merge is confirmed MERGED before any release is dispatched"
    - "buildpub.yml is dispatched for run.gpx/use1 only after confirming no in-flight run.gpx build (immutable-ECR-tag race)"
    - "deploy.yml is dispatched for us-east-1 with pr_number skip and cache invalidation, and both CI runs are watched to a successful conclusion"
    - "Before any post-deploy probing, a bounded roll-verification gate (12 attempts x 30s) proves the NEW bundle is actually being served by fetching the live JS chunks and matching a new-bundle-only string literal; on exhaustion the run stops and escalates instead of probing an unrolled deploy"
    - "A headless Playwright probe against https://gpx.defcon.run proves, on the LIVE site: the layers button click-opens the dialog, hovering the button does not open it, zero native tooltip attributes inside layer rows and file rows, section order, hint-bar text change on hover, and My Maps order plus footer Add run button"
    - "The same probe run before the deploy fails its new-UI assertions, proving the probe actually discriminates old bundle from new"
  artifacts:
    - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/dialog-shell-probe.cjs
    - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-pre-deploy.txt
    - .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt
  key_links:
    - "Deploy path is GitHub Actions ONLY: local tooling never runs terragrunt and the release is never dispatched with --with-terragrunt (AGENTS.md Essential Rule 4)"
    - "CI green is NOT proof the new task is serving (ECS rolling replace + CloudFront) — the step-7 sentinel chunk grep is the actual roll proof, and chunk fetches must never use --max-time or truncation manufactures a phantom not-deployed verdict"
    - "Probe script + pre-deploy transcript are committed BEFORE the squash-merge; only the post-deploy transcript goes on the docs/70-post-deploy-probe branch, because it cannot exist until after the deploy — that branch is pushed and its PR OPENED for the user, since the ONE authorized --admin merge is the phase PR"
    - "Because the release runs in CI, the env.local.sh worktree landmine does not apply — no local build.sh invocation is part of this plan"
    - "The probe's route stubs must be registered catch-all FIRST and specific LAST: Playwright resolves the most recently registered matching route"
    - "The mapbox token MUST be read from SSM with decryption, or the map never fires load and every DOM assertion times out"
---

<objective>
Ship the phase autonomously and prove it live (DLGS-06): run the quality gates,
author a discriminating Playwright prod probe, open and squash-merge the PR,
release through buildpub.yml, deploy through deploy.yml, and re-run the probe
against https://gpx.defcon.run until every assertion passes.

Purpose: the user asked for fully autonomous delivery — no human step between
"plan approved" and "probe green" other than the standing PR-merge authorization
granted for this phase in CONTEXT.md.

Output: three committed probe artifacts (script + pre/post transcripts), a merged
phase PR, a released and deployed gpx build on use1, a green probe transcript, and
an OPEN evidence PR carrying that transcript.

## Standing authorization (this phase only)

CONTEXT.md `<decisions>` "Autonomous delivery contract" grants, FOR THIS PHASE:
create the PR, squash-merge it with `--admin`, dispatch `buildpub.yml` and
`deploy.yml`, and verify live. ALL IMPLEMENTATION lives on the current branch
`design/gpx-dialog-shell`, which tracks `origin/main` — never move application
code to another branch.

EXACTLY ONE `--admin` MERGE IS AUTHORIZED, AND IT IS THE PHASE PR. The grant in
CONTEXT.md covers one phase PR; it does not extend to a second merge, so nothing
in this plan may merge anything else.

Exactly one additional BRANCH is permitted: the post-deploy evidence branch in
task 3 step 10 (`docs/70-post-deploy-probe`). It exists only because the
post-deploy probe transcript cannot physically be produced until after the merge
and deploy have happened, and committing it onto the already-merged branch would
leave the DLGS-06 evidence stranded off main. That branch is PUSHED and its PR is
OPENED, then left for the user to merge — the plan does not merge it. It carries
planning artifacts only: zero application code, zero release impact. No other
branch may be created.

The phase goal is "probe green", and it is fully MET at task 3 step 8 — the
12/12 post-deploy probe against the live site. Steps 9-10 are evidence
bookkeeping: they make the transcript reviewable on a PR without changing whether
the phase succeeded.

cac1 is explicitly out of scope (use1 only, matching current gpx release practice).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-05-SUMMARY.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-04-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

New files created by THIS plan (all planning artifacts, no application code):

| Artifact | Kind |
|---|---|
| `70-06-probes/dialog-shell-probe.cjs` | Playwright prod probe script |
| `70-06-probes/transcript-pre-deploy.txt` | probe output against the OLD live bundle |
| `70-06-probes/transcript-post-deploy.txt` | probe output against the NEW live bundle |

Application symbols asserted by the probe were created by sibling plans:
`data-dc34-layers-btn` (plan 05), `data-dc34-dialog`, `data-section-label`,
`data-layer-row`, `data-hint-out`, `Hover a row for details` (plan 01),
`data-file-row` (plan 04).

<!-- planner-discipline-allow: title -->

<tasks>

<task type="auto">
  <name>Task 1: Quality gates</name>
  <files>(no files written — verification only)</files>
  <read_first>
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-01-SUMMARY.md`, `70-02-SUMMARY.md`, `70-03-SUMMARY.md`, `70-04-SUMMARY.md`, `70-05-SUMMARY.md` (what actually landed, including any deviations to re-check)
- `AGENTS.md` (Essential Rule 4 and the Releases & Deploys section — read before running anything that touches CI)
- `apps/run.gpx/build-frontend.sh` (the only sanctioned way to build the studio frontend)
  </read_first>
  <action>
Run, in order, from the repo root of this worktree:

1. `nvm use 22.12.0` (vitest needs Node ≥ 22.12).
2. Webapp regression suite, MEASURED AGAINST A BASELINE (this phase changed no
   webapp code, so the question is never "is the suite green" but "did this phase
   make it worse" — a pre-existing red suite must not dead-end the autonomous run):
   a. Run on this branch:
      `cd apps/run.gpx/webapp && npx vitest run 2>&1 | tee /tmp/70-vitest-branch.txt`.
   b. If it reports zero failures, record that and PROCEED to step 3. No baseline
      run is needed.
   c. If it reports any failure, measure the baseline before judging it. Create a
      scratch checkout of the merge base so the working tree is never disturbed:
      `git worktree add --detach /tmp/70-baseline origin/main`. `--detach` is
      mandatory: without it git DWIMs a local branch named `main` tracking the
      remote, which either creates an unwanted branch or fails outright when `main`
      is already checked out in another worktree — and either way the baseline run
      never happens. Register the cleanup in the SAME shell before running anything
      in it:
      `trap 'git worktree remove /tmp/70-baseline --force 2>/dev/null || true; git worktree prune' EXIT`.
      `package.json` and
      `package-lock.json` are asserted byte-unchanged by step 6, so the installed
      dependency tree is identical and you may copy it instead of reinstalling:
      `cp -R apps/run.gpx/webapp/node_modules /tmp/70-baseline/apps/run.gpx/webapp/node_modules`
      (fall back to `cd /tmp/70-baseline/apps/run.gpx/webapp && npm ci` if the copy
      fails). Then
      `cd /tmp/70-baseline/apps/run.gpx/webapp && npx vitest run 2>&1 | tee /tmp/70-vitest-baseline.txt`.
   d. Extract the sorted set of failing test names from each transcript and diff
      the two sets. If the branch set is IDENTICAL to the baseline set, the failures
      are pre-existing: record both sets plus the diff result in the SUMMARY and
      PROCEED. If the branch set contains any test absent from the baseline set,
      those are NEW failures — stop and escalate, naming them.
   e. Clean up UNCONDITIONALLY — success, new-failure escalation, or abort:
      `git worktree remove /tmp/70-baseline --force || true; git worktree prune`.
      The `|| true` and the `prune` are both required: a removal that fails (or a
      directory someone deleted by hand) must not abort the run, and must not leave
      a stale worktree registration behind that poisons the next
      `git worktree add`. The step-2c `trap … EXIT` is the belt to this braces —
      whichever fires first, the registration is gone.
3. Studio type gate — write the transcript to a FILE and prove it is a real run
   before counting anything:
   `cd apps/run.gpx/gpx-studio/website && npx svelte-kit sync && npx svelte-check --tsconfig ./tsconfig.json --output machine >/tmp/70-svelte-check.txt 2>&1`.
   Then, in order:
   a. LIVENESS: `[ -s /tmp/70-svelte-check.txt ]` must hold, and
      `TOT=$(grep -c ' ERROR ' /tmp/70-svelte-check.txt || true); [ "${TOT:-0}" -ge 1 ]`
      must hold. An empty transcript, or one with zero ` ERROR ` lines, means
      svelte-check crashed or never ran — NOT that the tree is clean. The
      documented ~26-30 upstream baseline is what makes this a valid liveness
      probe: a genuine run on this repo always emits errors.
   b. FILTERED COUNT: `N=$(grep ' ERROR ' /tmp/70-svelte-check.txt | grep -cE 'dialog-shell/(DialogShell|HintBar|Section|Row|Chips|Chip)\.svelte|dialog-shell/index\.ts|StravaStrip\.svelte|layer-control/(PublicOverlays|MyConRuns|CommunityRoutes|BasemapSection|LayerControl)\.svelte|logic/basemap-tree-pure\.ts|cloud/CloudStorage\.svelte' || true); [ "${N:-0}" -eq 0 ] && echo "OK: 0 new errors (total $TOT)" || { echo "FAIL: $N"; exit 1; }`
      must print `OK: 0 new errors (total N)`. The `|| true` is load-bearing —
      `grep -c` exits 1 when it matches nothing, which is the success case — and is
      only SAFE because step 3a already proved the transcript is a real run.
   c. Record `$TOT` in the SUMMARY. It should sit in the ~26-30 upstream-baseline
      range; a materially larger number means a touched file broke an upstream
      file's inference and must be investigated.
   Every per-task svelte-check gate in plans 01-05 follows this same
   write-to-file → liveness → filtered-count shape, for the same reason.
4. Compile gate: `cd apps/run.gpx && ./build-frontend.sh` must exit 0. Never
   `npm install` inside `gpx-studio/website` — that fails on the local `gpx`
   package ordering; this script is the only supported path.
5. Cross-cutting DLGS-04 gates (run from `apps/run.gpx/gpx-studio/website`):
   `grep -c 'title=' src/lib/components/map/layer-control/PublicOverlays.svelte src/lib/components/map/layer-control/MyConRuns.svelte src/lib/components/map/layer-control/CommunityRoutes.svelte src/lib/components/map/layer-control/LayerControl.svelte src/lib/components/cloud/CloudStorage.svelte || true`
   must print `:0` for all five; and
   `grep -cE 'onmouseenter|onpointerenter' src/lib/components/map/layer-control/LayerControl.svelte || true`
   must print 0. Both trailing `|| true` guards are required — a multi-file
   `grep -c` and a single-file `grep -c` each exit 1 when nothing matches, which
   is precisely the passing condition for these two gates.
6. Supply-chain gate: `git status --porcelain apps/run.gpx/gpx-studio/website/package.json apps/run.gpx/gpx-studio/website/package-lock.json apps/run.gpx/webapp/package.json apps/run.gpx/webapp/package-lock.json`
   must print nothing — this phase adds zero dependencies, so there is no package
   to audit and no legitimacy checkpoint to clear.
7. Working-tree sanity: `git status --porcelain` shows only intended files, and
   `git log --oneline origin/main..HEAD` lists the phase's commits.

If any gate fails, fix it on this branch and re-run the failing gate before
moving to task 2. Do not proceed to any CI dispatch with a red gate.
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output machine >/tmp/70-06-t1-check.txt 2>&1; [ -s /tmp/70-06-t1-check.txt ] || { echo "FAIL: svelte-check wrote no output (crashed) — an empty transcript is not a pass"; exit 1; }; TOT=$(grep -c ' ERROR ' /tmp/70-06-t1-check.txt || true); [ "${TOT:-0}" -ge 1 ] || { echo "FAIL: 0 total ERROR lines — svelte-check did not really run (documented upstream baseline is ~26-30)"; exit 1; }; N=$(grep ' ERROR ' /tmp/70-06-t1-check.txt | grep -cE 'dialog-shell/|StravaStrip\.svelte|layer-control/(PublicOverlays|MyConRuns|CommunityRoutes|BasemapSection|LayerControl)\.svelte|basemap-tree-pure\.ts|CloudStorage\.svelte' || true); [ "${N:-0}" -eq 0 ] && echo "OK: 0 new errors (total $TOT)" || { echo "FAIL: $N new errors"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
- `npx vitest run` in `apps/run.gpx/webapp` either reports 0 failed tests, OR its failing-test set is proven IDENTICAL to the `origin/main` baseline set measured in step 2c and both sets are recorded in the SUMMARY. Any failing test absent from the baseline set is a hard stop.
- The filtered svelte-check command prints `OK` (0 new errors) for the fifteen touched files.
- Total svelte-check error count is recorded in the SUMMARY and is 32 or fewer.
- `./build-frontend.sh` exits 0.
- `grep -c 'title=' <the five touched surface files> || true` prints `:0` for every one.
- `grep -cE 'onmouseenter|onpointerenter' src/lib/components/map/layer-control/LayerControl.svelte || true` prints 0.
- `git status --porcelain` over the four package/lock files prints nothing.
  </acceptance_criteria>
  <done>Vitest either green or proven identical to the origin/main baseline, zero new svelte-check errors on touched files, clean frontend build, DLGS-04 source gates green, zero dependency drift.</done>
</task>

<task type="auto">
  <name>Task 2: Author the prod probe and prove it discriminates (pre-deploy run)</name>
  <files>
.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/dialog-shell-probe.cjs,
.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-pre-deploy.txt
  </files>
  <read_first>
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md` `<decisions>` "Autonomous delivery contract" (the probe recipe of record)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` §8 (verification hooks)
- `apps/run.gpx/gpx-studio/website/src/lib/stores/auth.ts` lines 53-100 (the `/api/auth/session` URL and the exact session JSON shape the store expects: `session.user.services` must include `gpxstudio`)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/Map.svelte` lines 31-49 (the `/api/user/mapbox-token` fetch and its `data.token` shape, which must start with `pk.`) — this file covers the TOKEN half of the handshake only
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/map.ts` around line 203 (`window._map = map; // entry point for extensions`) — this is where the global the probe waits on is actually assigned, so it is the authoritative reference for the `waitForFunction(() => !!window._map)` gate and for what is safe to call on it (`setTerrain`)
- `apps/run.gpx/gpx-studio/website/src/lib/cloud-sync.ts` lines 160-220 (the `/api/gpx/files` → `data.files` and `/api/gpx/folders` → `data.folders` / `data.globalFolders` shapes)
- `apps/run.gpx/gpx-studio/website/src/lib/components/Menu.svelte` lines 600-608 (Ctrl/Cmd+O opens My Maps — the probe's way in without clicking through the menubar)
  </read_first>
  <action>
Write `70-06-probes/dialog-shell-probe.cjs` — a CommonJS Node script (`.cjs` so
`require` works without any package.json wiring) that drives the LIVE site and
prints one `PASS`/`FAIL` line per assertion plus a final
`RESULT: n/12 assertions passed`, exiting non-zero on any failure.

SCORING CONTRACT (the denominator is fixed, never computed):
- The script declares `const TOTAL = 12;` and the final line is always
  `RESULT: ${passed}/${TOTAL} assertions passed`. It must NEVER divide by a count
  of checks that happened to execute — a shrinking denominator would make the
  `12/12` ship gate in task 3 unreachable, or worse, reachable while asserting
  nothing.
- Every one of the twelve assertions prints exactly one line, always. There is no
  third outcome besides PASS and FAIL.
- An assertion whose subject legitimately does not exist in prod data counts as a
  PASS and prints `PASS (skipped: no <section> in prod data)`. This applies where
  the guards intentionally hide empty sections (assertion 5's per-section pairs)
  and where prod may carry no described route row (assertion 7's fallback). A skip
  is a pass because the corresponding UI contract is vacuously satisfied — but it
  is labelled in the transcript so a reader can see which sub-checks had no data.

Setup requirements:
- `const { chromium } = require('/Users/khundeck/working/defcon.run.34/apps/run.auth/e2e/node_modules/playwright-core');`
- `executablePath` = `${process.env.HOME}/Library/Caches/ms-playwright/chromium_headless_shell-1208/chrome-headless-shell-mac-arm64/chrome-headless-shell`
- launch args `--use-gl=swiftshader`, `--enable-unsafe-swiftshader`, `--no-sandbox`
  (WebGL2 must be available or `Map.svelte` bails before creating the map)
- target URL `https://gpx.defcon.run/use1/studio/app`
- viewport 1280x900
- read the mapbox token from the environment variable `MAPBOX_TOKEN`, which the
  caller sets from SSM:
  `MAPBOX_TOKEN=$(aws ssm get-parameter --name /dc34/secrets/use1/mapbox/public_token --with-decryption --query Parameter.Value --output text --profile dc34-application --region us-east-1)`
  — the decryption flag is load-bearing: without it you get KMS ciphertext, the
  token does not start with `pk.`, mapbox never fires `load`, and every assertion
  times out. If that profile is not configured, retry with `--profile application`.
  Fail fast with a clear message when `MAPBOX_TOKEN` is missing or does not start
  with `pk.`.

Route stubs, registered in EXACTLY this order (Playwright resolves the most
recently registered matching handler first, so catch-alls go first):
1. `**/use1/api/gpx/**` → fulfil 200 JSON `{}`
2. `**/use1/api/user/**` → fulfil 200 JSON `{}`
3. `**/use1/api/auth/**` → fulfil 200 JSON `{}`
4. `**/use1/api/gpx/public/**` → `route.continue()` (real public route data, so the
   DEF CON 34 / Rabbit route-group sections have content)
5. `**/use1/api/user/mapbox-token*` → fulfil `{ token: process.env.MAPBOX_TOKEN }`
6. `**/use1/api/auth/session*` → fulfil a session whose `user` has
   `services: ['gpxstudio']` plus id/email/name and `hasStrava: false`
7. `**/use1/api/gpx/files*` → fulfil `{ files: [ one file object with fileId,
   fileName ending in `.gpx`, fileSize, version 1, trackCount 1, createdAt and
   updatedAt as epoch millis, and no conDay ] }`
8. `**/use1/api/gpx/folders*` → fulfil
   `{ folders: [], globalFolders: [ { folderId: 'probe-global', folderName: 'DEF CON 34 Maps' } ] }`

Boot sequence: `goto` with `waitUntil: 'domcontentloaded'`, then
`page.waitForFunction(() => !!window._map)`, then
`page.evaluate(() => window._map.setTerrain(null))` (terrain on swiftshader is the
usual hang), then wait for `[data-dc34-layers-btn]`.

Assertions, in this order, each printed as its own PASS/FAIL line:
1. `[data-dc34-layers-btn]` is present.
2. HOVER-DOES-NOT-OPEN: hover the layers button, wait 500ms, and
   `[data-dc34-dialog="layers"]` count is still 0 (this is the direct DLGS-04
   no-hover-open proof).
3. CLICK OPENS: click the button; `[data-dc34-dialog="layers"]` matches
   `[role="dialog"]` and is visible.
4. NO NATIVE TOOLTIPS IN LAYER ROWS:
   `document.querySelectorAll('[data-layer-row] [title]').length === 0` AND
   `document.querySelectorAll('[data-layer-row][title]').length === 0`.
5. SECTION ORDER: collect
   `[...document.querySelectorAll('[data-dc34-dialog="layers"] [data-section-label]')].map(e => e.textContent.trim())`.
   Assert the first entry matches `/^basemap$/i`. Then, for every pair present in
   the collected list, assert relative order follows: Basemap, User Check-ins,
   route groups (labels ending in `Routes` that are not `Community Routes`),
   My DEF CON Runs, Community Routes. A section absent from prod data contributes
   no pair and is reported as `PASS (skipped: no <section> in prod data)` per the
   scoring contract above — the guards intentionally hide empty sections, so a
   missing section is a satisfied contract, not a failure and not a subtraction
   from the denominator. The assertion as a whole still emits exactly one
   PASS/FAIL line and still counts as one of the twelve.
6. HINT BAR DEFAULT: `[data-dc34-dialog="layers"] [data-hint-out]` text equals
   `Hover a row for details`.
7. HINT BAR UPDATES: hover the first `[data-layer-row]` that carries a non-empty
   `data-hint`, then assert `[data-hint-out]` text is non-empty and different from
   the default. If prod returns no route row with a description, fall back to any
   `[data-hint]` element inside the dialog body and assert the same — the fallback
   is preferred over skipping, because Section headers always carry a hint. Only if
   the dialog body contains NO `[data-hint]` element whatsoever does this become a
   skip, printed as `PASS (skipped: no hinted element in prod data)` per the
   scoring contract.
8. ESC CLOSES: press Escape; `[data-dc34-dialog="layers"]` count returns to 0.
9. MY MAPS OPENS: press `Control+o`; `[data-dc34-dialog="mymaps"]` is visible.
10. MY MAPS ORDER: collect
    `[data-dc34-dialog="mymaps"] [data-section-label]` texts; the index of the entry
    matching `/my files/i` is less than the index matching `/shared with you/i`.
11. FOOTER: `[data-dc34-dialog="mymaps"]` contains a `button` whose textContent
    includes `Add run`.
12. NO NATIVE TOOLTIPS IN FILE ROWS:
    `document.querySelectorAll('[data-file-row] [title]').length === 0` AND
    `document.querySelectorAll('[data-file-row][title]').length === 0`.

Also capture a screenshot of each open dialog next to the transcript for the
record, and always close the browser in a `finally`.

Then RUN it against current prod and save the output to
`70-06-probes/transcript-pre-deploy.txt`. Expect FAILURES — the live bundle is
still the old hover popover, so at minimum assertions 1 and 3 fail. That failing
run is the point: it proves the probe discriminates the old bundle from the new
one, so a green post-deploy run cannot be a false positive. Record in the
transcript header the git SHA, the timestamp, and the live gpx version observed.
Do not "fix" the probe to make the pre-deploy run pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/dialog-shell-probe.cjs && grep -c 'RESULT:' .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-pre-deploy.txt</automated>
  </verify>
  <acceptance_criteria>
- `70-06-probes/dialog-shell-probe.cjs` exists and `node --check` on it exits 0.
- `grep -c 'chrome-headless-shell' dialog-shell-probe.cjs` is 1 and `grep -c 'swiftshader' dialog-shell-probe.cjs` is at least 1.
- `grep -c 'gpx.defcon.run/use1/studio/app' dialog-shell-probe.cjs` is 1.
- `grep -n 'api/gpx/\*\*' dialog-shell-probe.cjs` reports a SMALLER line number than `grep -n 'api/gpx/public/\*\*' dialog-shell-probe.cjs` (catch-all registered before the specific continue-handler).
- `grep -c 'with-decryption' dialog-shell-probe.cjs` is at least 1 (the SSM command is documented in the script header) and the script hard-fails when `MAPBOX_TOKEN` does not start with `pk.`.
- `grep -c 'setTerrain(null)' dialog-shell-probe.cjs` is 1 and `grep -c '_map' dialog-shell-probe.cjs` is at least 2.
- All twelve assertions are present: `grep -c 'PASS\|FAIL' dialog-shell-probe.cjs` is at least 1 and the script contains 12 distinct assertion labels.
- The denominator is a fixed literal, not a computed count: `grep -c 'const TOTAL = 12' dialog-shell-probe.cjs` is 1, and the `RESULT:` template divides by `TOTAL`.
- A skipped sub-check scores as a pass and says so: `grep -c 'PASS (skipped' dialog-shell-probe.cjs` is at least 1.
- `transcript-pre-deploy.txt` exists, contains a `RESULT:` line whose denominator is 12, and that line shows FEWER than 12 assertions passed (the probe discriminates the old bundle).
- The transcript header records a git SHA and a timestamp.
  </acceptance_criteria>
  <done>A self-contained CommonJS probe drives live prod with correctly ordered route stubs and a decrypted mapbox token, asserts all twelve DLGS-02/03/04 hooks, and its pre-deploy run fails on the old bundle — proving it is not a no-op.</done>
</task>

<task type="auto">
  <name>Task 3: PR, squash-merge, CI release, CI deploy, green probe</name>
  <files>.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt</files>
  <read_first>
- `AGENTS.md` (Releases & Deploys — the exact standing flow, the immutable-ECR warning, and the rule that the deploy always runs in GitHub Actions)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md` `<decisions>` "Autonomous delivery contract"
- `.github/workflows/buildpub.yml` (the `apps` and `regions` dispatch inputs)
- `.github/workflows/deploy.yml` (the `region`, `pr_number`, `invalidate_cache` dispatch inputs; `pr_number: skip` deploys without merging anything)
  </read_first>
  <action>
Execute the standing ship flow, in order, verifying each step before the next.

0. COMMIT THE PRE-DEPLOY EVIDENCE BEFORE THE MERGE. Stage and commit task 2's
   artifacts — `70-06-probes/dialog-shell-probe.cjs`, `transcript-pre-deploy.txt`
   and the pre-deploy screenshots — on this branch now, so the probe script and its
   discriminating pre-deploy run travel to main with the phase PR. Anything
   committed after step 4 lands on a branch that has already been squash-merged and
   therefore never reaches main; that is why the post-deploy transcript needs the
   separate follow-up PR in step 10 and why everything that CAN be committed
   pre-merge MUST be.
1. Push the branch as-is (never create or switch branches at this stage):
   `git push -u origin design/gpx-dialog-shell`.
2. Open the PR against main with `gh pr create --base main --head design/gpx-dialog-shell`,
   titled for the phase (for example
   `feat(gpx-studio): shared dialog shell — Map Layers + My Maps (phase 70)`) and a
   body summarising DLGS-01..DLGS-05, the gates from task 1, and the pre-deploy
   probe result. Capture the PR number.
3. Squash-merge with the standing authorization:
   `gh pr merge <PR#> --squash --admin`. Then CONFIRM the merge landed before
   going further: `gh pr view <PR#> --json state --jq .state` must print `MERGED`.
   A failed merge that is treated as success is how a release ends up building an
   unintended commit.
4. Immutable-ECR race check BEFORE dispatching the release:
   `gh run list --workflow=buildpub.yml --limit 10 --json status,conclusion,displayTitle,createdAt,url`.
   If any run is `in_progress` or `queued` AND involves run.gpx, WAIT for it to
   finish (`gh run watch <id>`) before continuing — ECR repos are immutable, so two
   concurrent run.gpx builds race on the same tag. Concurrent builds of a
   DIFFERENT app are fine.
5. Release through CI (this builds and pushes the image and auto-merges the
   version-bump Release PR):
   `gh workflow run buildpub.yml -f apps=run.gpx -f regions=use1`.
   Find the new run id (`gh run list --workflow=buildpub.yml --limit 3`) and watch
   it to completion with `gh run watch <id> --exit-status`. Never pass
   `--with-terragrunt` to anything and never run terragrunt locally: the deploy is
   CI-only (AGENTS.md Essential Rule 4). Because the build runs in CI, the
   `env.local.sh` worktree landmine does not apply here.
6. Deploy through CI:
   `gh workflow run deploy.yml -f region=us-east-1 -f pr_number=skip -f invalidate_cache=true`
   (`skip` because buildpub already merged the Release PR). Watch it with
   `gh run watch <id> --exit-status`. Record the deploy run URL now — step 7 needs
   it for its escalation path.
7. ROLL-VERIFICATION GATE — prove the new bundle is actually being served BEFORE
   running the probe. A green deploy run is NOT that proof: ECS does a rolling
   replace (the old task keeps serving while the new one health-checks) and
   CloudFront caches on top of it, so a probe fired the instant CI turns green is
   measuring the old bundle and its failures are meaningless. Poll until a
   new-bundle-only string literal is observably served, bounded at 12 attempts
   30 seconds apart (6 minutes total):
   - Each attempt: `CB=$(date +%s)`, fetch
     `curl -fsS "https://gpx.defcon.run/use1/studio/app?cb=$CB"`, and collect the
     JS chunk paths it references (`grep -oE '/use1/[^"]+\.js'`, deduped).
   - The studio is a client-rendered SPA, so the sentinel will NOT be in the page
     HTML — it lives in a chunk. Walk the chunks: fetch each with the same
     cache-buster, and if a chunk is not itself a hit, harvest the relative module
     paths it imports (match static `from"./<name>.js"` and dynamic
     `import("./<name>.js")` forms with `grep -oE '\./[A-Za-z0-9_.-]+\.js'`,
     resolved against that chunk's own directory) and fetch those too. Bound the
     walk to depth 2 and dedupe by URL; the studio lazy-loads the map subtree, so
     the sentinel commonly sits one hop below the entry chunk.
   - SENTINEL: grep the fetched chunk bodies for `data-dc34-layers-btn`, and treat
     `Hover a row for details` as a second acceptable hit. Both are string literals
     introduced by this phase, and string literals survive minification while
     function names do not — neither can exist anywhere in the pre-phase bundle, so
     a hit is positive proof of the new build and cannot be a false positive from a
     cached old asset.
   - NEVER pass `--max-time` to these chunk fetches. A truncated chunk body silently
     drops the sentinel and manufactures a phantom "not deployed" verdict, which is
     a recorded landmine on this exact site.
   - On a hit: proceed to step 8 immediately.
   - On exhaustion of all 12 attempts with no hit: STOP AND ESCALATE. Record the
     deploy run URL from step 6, the buildpub run URL, the chunk URLs actually
     probed and the attempt count in the SUMMARY, and hand off. Do NOT edit
     application source, do NOT edit the probe, and do NOT run the probe anyway to
     "see what happens" — a bundle that never rolled is an infrastructure condition,
     not a UI defect, and treating it as one corrupts the evidence.
8. Re-run the probe against live prod exactly as in task 2 — same script, no
   edits — writing output to `70-06-probes/transcript-post-deploy.txt` with the
   same header fields plus the buildpub and deploy run URLs, the newly released
   gpx version, and the sentinel hit recorded in step 7 (which chunk URL matched,
   on which attempt). ALL TWELVE assertions must pass, denominator fixed at 12 per
   the task-2 scoring contract. Because step 7 already proved the new bundle is
   serving, a failure here is a real UI defect and not a timing artifact.
9. Verify no token leaked into the artifacts before committing them:
   `grep -rc 'pk\.' 70-06-probes/ || true` must print `:0` for every file.
10. OPEN the post-deploy evidence PR and STOP THERE — do not merge it. The phase
    branch was squash-merged in step 3, so committing there now would strand the
    evidence off main; put it on the one permitted extra branch instead:
    `git checkout -b docs/70-post-deploy-probe origin/main`, add
    `transcript-post-deploy.txt` plus the post-deploy screenshots, commit as
    `docs(70): post-deploy prod probe transcript (12/12)`, `git push -u origin docs/70-post-deploy-probe`,
    then `gh pr create --base main --head docs/70-post-deploy-probe` with a body
    that quotes the `RESULT: 12/12` line and links the buildpub and deploy runs.
    Record the PR NUMBER and URL in the SUMMARY and in the final report so the user
    can review and merge it.
    Do NOT run `gh pr merge` on this PR. The standing authorization in CONTEXT.md
    covers ONE `--admin` merge and that merge was the phase PR in step 3; a second
    admin merge would exceed the grant. Leaving this PR open costs nothing —
    the phase goal (a green live probe) was already met at step 8, and this branch
    carries planning artifacts only, so it triggers no release and blocks nothing.

If the post-deploy probe fails on a UI assertion, fix the source on a new commit
on `design/gpx-dialog-shell` and repeat steps 0-8 — do NOT weaken the probe. If it
fails because step 7's sentinel never appeared, that is the escalation path in step
7, not a fix-and-retry loop.
  </action>
  <verify>
    <automated>P=.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt; grep -qE 'RESULT: 12/12' "$P" && ! grep -q '^FAIL' "$P" && echo "OK: 12/12 post-deploy, roll-verified" || { echo "FAIL: post-deploy probe not 12/12"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
- The PHASE PR is merged: `gh pr view <phasePR#> --json state --jq .state` printed `MERGED` and the PR number is recorded in the SUMMARY. This is the ONE `--admin` merge this plan performs.
- The probe script and `transcript-pre-deploy.txt` were committed BEFORE the squash-merge, so they are present on main: `git show origin/main:.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/dialog-shell-probe.cjs | head -1` succeeds after step 3.
- ROLL-VERIFIED BEFORE PROBING: the SUMMARY records the sentinel hit from step 7 — which chunk URL served `data-dc34-layers-btn` (or `Hover a row for details`) and on which of the 12 attempts. A post-deploy probe run with no recorded sentinel hit is not acceptable evidence, however green it looks.
- `transcript-post-deploy.txt` is committed on `docs/70-post-deploy-probe`, that branch is pushed, and an evidence PR is OPEN against main: `gh pr view <evidencePR#> --json state --jq .state` prints `OPEN`, and its number AND url are recorded in the SUMMARY and in the final report. Its merge is the user's call — exactly ONE `--admin` merge happens in this plan and it is the phase PR.
- The buildpub.yml run for run.gpx/use1 concluded `success` (run URL recorded in the SUMMARY), and `gh run list` showed no in-flight run.gpx build at dispatch time.
- The deploy.yml run for us-east-1 with `pr_number=skip` and `invalidate_cache=true` concluded `success` (run URL recorded).
- `transcript-post-deploy.txt` contains a `RESULT: 12/12 assertions passed` line (denominator the fixed literal 12) and zero `FAIL` lines.
- `git log --oneline origin/main -1` shows the squashed phase commit; the evidence commit lives on the pushed branch, not on main: `git log --oneline origin/docs/70-post-deploy-probe -1` shows `docs(70): post-deploy prod probe transcript (12/12)`. `git status --porcelain` is clean after the final push.
- No local terragrunt was executed: `history`-independent check — the SUMMARY explicitly records that deployment happened via workflow dispatch only, and no `--with-terragrunt` flag appears in any command run.
  </acceptance_criteria>
  <done>The phase is merged to main (the one authorized `--admin` merge), released to use1 via buildpub.yml, deployed via deploy.yml, and a 12/12 green Playwright probe against https://gpx.defcon.run proves the new dialogs are live — with the post-deploy transcript pushed on `docs/70-post-deploy-probe` and its evidence PR left OPEN for the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workstation → GitHub Actions | Workflow dispatches trigger a production build and deploy |
| CI → ECR / ECS / CloudFront | The release pushes an immutable image tag and rolls the ECS service |
| SSM Parameter Store → local probe process | The mapbox public token is read with decryption into an environment variable |
| live prod → probe browser | The probe stubs authenticated endpoints locally; it never writes to prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-SC | Tampering (supply chain / release integrity) | buildpub.yml dispatch | high | mitigate | `gh run list --workflow=buildpub.yml` is checked for in-flight run.gpx builds BEFORE dispatch (immutable-ECR-tag race), and the squash merge is confirmed `MERGED` before the release is dispatched so CI cannot build an unintended commit. No package-manager install occurs in this phase, so no `[ASSUMED]`/`[SUS]` package legitimacy checkpoint applies |
| T-70-15 | Tampering (unreviewed infrastructure change) | deploy path | high | mitigate | Deployment is workflow-dispatch only; no local `terragrunt apply`, and `--with-terragrunt` is never passed to any release command (AGENTS.md Essential Rule 4), asserted in the acceptance criteria |
| T-70-16 | Information disclosure | mapbox token in the probe | medium | mitigate | The token is a PUBLIC `pk.` mapbox token read at run time into an env var; it is never written into the committed probe script or into either transcript — transcripts are grep-checked for `pk.` before commit |
| T-70-17 | Repudiation (false green) | prod verification | medium | mitigate | The identical probe is run BEFORE the deploy and must fail; only then is a post-deploy 12/12 accepted, so a probe that silently asserts nothing cannot pass |
| T-70-18 | Denial of service | ECS rolling replace | low | accept | The deploy is a standard rolling replace with CloudFront invalidation; the phase is presentation-layer only and adds no new request path |
</threat_model>

<verification>
- `P=.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt; grep -c 'FAIL' "$P" || true` prints 0.
- `grep -E 'RESULT: 12/12' .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/transcript-post-deploy.txt` matches.
- `grep -rc 'pk\.' .planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-probes/ || true` prints `:0` for every file (no token leaked into committed artifacts).
- The roll-verification gate ran and hit: the SUMMARY names the chunk URL that served a new-bundle-only sentinel literal and the attempt number, dated between the deploy run's completion and the post-deploy probe's start timestamp.
- `gh pr list --state merged --limit 5` includes the phase PR (the one authorized `--admin` merge), and `gh pr list --state open --limit 5` includes the `docs/70-post-deploy-probe` evidence PR still awaiting the user's merge; `gh run list --workflow=buildpub.yml --limit 3` and `gh run list --workflow=deploy.yml --limit 3` show `success` for the two dispatched runs.
- `git status --porcelain` is clean and `git log --oneline @{u}..HEAD` is empty (everything pushed — the session is not complete until push succeeds, per AGENTS.md "Landing the Plane").
</verification>

<success_criteria>
- Vitest either green or proven identical to the `origin/main` baseline failing set, zero new svelte-check errors on the fifteen touched files, clean `build-frontend.sh`.
- The phase PR is merged to main by squash with the standing authorization, confirmed `MERGED` — and it is the ONLY `--admin` merge performed.
- buildpub.yml (run.gpx/use1) and deploy.yml (us-east-1, pr_number skip, invalidate cache) both concluded success, dispatched in that order and only after the in-flight race check.
- The roll-verification gate hit a new-bundle-only sentinel literal in a live JS chunk BEFORE the post-deploy probe ran, with the chunk URL and attempt number recorded.
- A 12/12 Playwright probe against https://gpx.defcon.run proves click-to-open, no hover-open, zero native tooltips in layer and file rows, section order, hint-bar update, My Maps order, and the footer Add run button.
- Pre-deploy transcript shows the same probe failing, so the green run is meaningful — and it was committed before the merge so it lives on main.
- Everything committed and pushed, including the post-deploy transcript on the pushed `docs/70-post-deploy-probe` branch with its evidence PR OPEN (number + url reported) for the user to merge.
</success_criteria>

<output>
Create `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-06-SUMMARY.md` when done.
</output>