---
phase: 70-gpx-studio-shared-dialog-shell-map-layers-my-maps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/DialogShell.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/HintBar.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Section.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Row.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chips.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chip.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/index.ts
autonomous: true
requirements: [DLGS-01]

must_haves:
  truths:
    - "One shell renders a centered ~420px dark card with header (icon + title + optional subtitle + close), scrollable body, optional footer, and a bottom hint bar"
    - "Every section header shows exactly ONE collapse affordance: a single chevron glyph that rotates -90 degrees when closed (no icon node swap)"
    - "Section trailing controls appear in the fixed order count badge, overflow menu, master checkbox (rightmost)"
    - "A section whose master checkbox is off renders its label at ~55% opacity"
    - "Hovering or keyboard-focusing an element carrying a hint attribute updates the hint bar text; leaving the body restores 'Hover a row for details'"
    - "Layer rows expose a stable data attribute so a DOM probe can assert zero native tooltip attributes inside them"
  artifacts:
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/DialogShell.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/HintBar.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Section.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Row.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chips.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chip.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/index.ts
  key_links:
    - "DialogShell composes HintBar and owns the delegated hover/focus listeners on its scrollable body element"
    - "Section takes collapsed as an input prop plus an ontoggle callback so consumers keep driving collapse from their existing master-visibility effects"
    - "index.ts is the single import surface: consumers import DialogShell/Section/Row/Chips/Chip from $lib/components/dialog-shell/index.js"
    - "DialogShell forwards a dialogId prop onto Dialog.Content as data-dc34-dialog for the prod probe selector"
---

<objective>
Build the shared dialog kit that both Phase 70 surfaces are assembled from (DLGS-01):
five presentational Svelte 5 runes components plus a barrel, living in a new
`lib/components/dialog-shell/` directory in gpx-studio.

Purpose: the Map Layers dialog and the My Maps dialog must share ONE shell, ONE
collapse idiom, ONE row vocabulary, ONE chip vocabulary, and ONE hint bar — the
hint bar being the replacement for every native tooltip in these surfaces.

Output: 6 new components + `index.ts` barrel, type-clean under svelte-check. No
existing file is touched by this plan, so nothing changes visually yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md
@.planning/sketches/006-shared-dialog-shell/DESIGN.md
@AGENTS.md
</context>

## Artifacts this phase produces

New symbols created by THIS plan (all under
`apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/`):

| Symbol | Kind |
|---|---|
| `DialogShell.svelte` (component `DialogShell`) | Svelte component |
| `HintBar.svelte` (component `HintBar`) | Svelte component |
| `Section.svelte` (component `Section`) | Svelte component |
| `Row.svelte` (component `Row`) | Svelte component |
| `Chips.svelte` (component `Chips`) | Svelte component |
| `Chip.svelte` (component `Chip`) | Svelte component |
| `index.ts` | barrel re-exporting the six components |
| `DEFAULT_HINT` | exported const in `index.ts` (`'Hover a row for details'`) |
| `data-dc34-dialog` | data attribute on the dialog content element |
| `data-dialog-body` | data attribute on the scrollable body |
| `data-hint` | data attribute read by the hint bar delegation |
| `data-hint-bar`, `data-hint-out` | data attributes on the hint strip |
| `data-section`, `data-section-chevron`, `data-section-label` | data attributes on Section parts |
| `data-layer-row` | data attribute on every `Row` |

Symbols created by sibling plans in this phase (do not treat as pre-existing
drift): `flattenLayerTree` and `lib/logic/basemap-tree-pure.ts` (plan 05),
`BasemapSection.svelte` (plan 05), `data-dc34-layers-btn` (plan 05),
`data-file-row` and `handleExportFile` (plan 04).

<tasks>

<task type="auto">
  <name>Task 1: DialogShell + HintBar + barrel</name>
  <files>
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/DialogShell.svelte,
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/HintBar.svelte,
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/index.ts
  </files>
  <read_first>
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md` (sections "DialogShell.svelte" and "HintBar.svelte" — the dialog-host, scrollable-body, footer, header-subtitle and delegated-listener patterns, with the exact class strings to copy)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` (§1 shell, §3 hint bar)
- `.planning/sketches/006-shared-dialog-shell/index.html` (CSS blocks `.dlg`, `.dlg-head`, `.dlg-body`, `.dlg-foot`, `.hintbar`, and the `mouseover` delegation at the bottom — the visual reference of record)
- `apps/run.gpx/gpx-studio/website/src/lib/components/ui/dialog/dialog-content.svelte` (base classes to override, built-in close button placement `end-4 top-4`, `restProps` passthrough)
- `apps/run.gpx/gpx-studio/website/src/lib/components/ui/dialog/index.ts` (exported primitive names)
- `apps/run.gpx/gpx-studio/website/src/lib/components/cloud/CloudStorage.svelte` lines 545-552 and 929-942 (the live dialog-host + hand-rolled footer precedent)
  </read_first>
  <action>
Create `HintBar.svelte` first (runes mode, `lang="ts"`). Props:
`let { text }: { text: string } = $props();`. It renders a single div with
attribute `data-hint-bar` and class
`flex flex-shrink-0 items-center gap-2 border-t bg-black/25 px-4 py-2 text-xs text-muted-foreground min-h-[33px]`,
containing (a) a `<span class="font-mono text-[10px] text-primary/70" aria-hidden="true">` holding the
glyph `ⓘ`, and (b) a `<span data-hint-out aria-live="polite" class="min-w-0 truncate">{text}</span>`.
`aria-live="polite"` is required because UI-SPEC §7 says the hint bar must serve
keyboard focus, not just mouse hover. No other markup, no logic.

Create `DialogShell.svelte` (runes mode, `lang="ts"`). Imports:
`import * as Dialog from '$lib/components/ui/dialog/index.js';`,
`import HintBar from './HintBar.svelte';`, `import type { Snippet } from 'svelte';`.
Props (type the destructure inline, matching the PublicOverlays convention):
`let { open, onOpenChange, dialogId, title, subtitle, icon, footer, children }:
{ open: boolean; onOpenChange: (o: boolean) => void; dialogId: string; title: string;
subtitle?: string; icon?: Snippet; footer?: Snippet; children: Snippet } = $props();`

Declare `const DEFAULT_HINT = 'Hover a row for details';` and
`let hint = $state(DEFAULT_HINT);` plus
`function readHint(e: Event) { const el = (e.target as HTMLElement | null)?.closest?.('[data-hint]') as HTMLElement | null; hint = el?.dataset.hint || DEFAULT_HINT; }`.
Use the `?.closest?.()` optional-call defensiveness of LayerControl.svelte line 352.

Structure:
`<Dialog.Root {open} {onOpenChange}>` wrapping
`<Dialog.Content data-dc34-dialog={dialogId} class="!max-w-[420px] !w-[94vw] !gap-0 !p-0 max-h-[72vh] overflow-hidden flex flex-col rounded-xl">`.
The `!`-prefixed overrides are mandatory: dialog-content.svelte ships
`sm:max-w-lg`, `p-6` and `gap-4`, all of which must lose to the 420px carded
layout. Keep the built-in close button (do NOT pass `showCloseButton={false}`) —
it is absolutely positioned at `end-4 top-4`, which lands inside the header band.

Inside Content, three or four stacked children:
1. A hand-rolled header div (do NOT use `Dialog.Header`, whose `flex-col gap-2`
   fights a single row): class
   `flex flex-shrink-0 items-center gap-2.5 border-b px-4 py-3.5 pe-10`. First child:
   `{#if icon}<span class="shrink-0 text-[17px] leading-none">{@render icon()}</span>{/if}`
   (17px glyph per UI-SPEC §1). Then a `<div class="min-w-0 flex-1">` holding
   `<Dialog.Title class="truncate text-lg font-bold leading-tight">{title}</Dialog.Title>`
   and, when `subtitle` is set,
   `<Dialog.Description class="mt-0.5 text-xs text-muted-foreground">{subtitle}</Dialog.Description>`;
   when it is not set, still render
   `<Dialog.Description class="sr-only">{title}</Dialog.Description>` so bits-ui
   always has an accessible description and never logs its missing-description warning.
2. The scrollable body div: attribute `data-dialog-body`, class
   `flex min-h-0 flex-1 flex-col gap-2.5 overflow-y-auto p-3`, with
   `onmouseover={readHint}`, `onfocusin={readHint}` and
   `onmouseleave={() => (hint = DEFAULT_HINT)}` attached as Svelte 5 attributes
   (attributes, not manual addEventListener: Svelte attaches and detaches them
   with the portalled node, so there is no cleanup to get wrong). Immediately
   above this div place the pragma comment
   `<!-- svelte-ignore a11y_mouse_events_have_key_events -->` so the mouse-over
   handler on a static element does not add a NEW svelte-check finding. Body
   content is `{@render children()}`. `gap-2.5` is the ~10px inter-card gap from
   UI-SPEC §2; `p-3` is the ~12px body padding. Use plain overflow, NOT
   `ScrollArea` — PATTERNS.md records that the bits-ui viewport wrapper fights
   `flex-1 min-h-0`.
3. `{#if footer}` a footer div with class
   `flex flex-shrink-0 items-center justify-between gap-3 border-t px-4 py-3`
   rendering `{@render footer()}` (hand-rolled, because `Dialog.Footer` is
   `sm:justify-end` and the spec wants helper-text-left / primary-right).
4. `<HintBar text={hint} />` — always last, below the footer (UI-SPEC §5).

Create `index.ts` re-exporting all six components as named exports
(`export { default as DialogShell } from './DialogShell.svelte';` and the same
shape for HintBar, Section, Row, Chips, Chip — Section/Row/Chips/Chip land in
tasks 2 and 3, so write the full barrel now and finish the plan with all six
present) plus `export const DEFAULT_HINT = 'Hover a row for details';`. Consumers
import from `$lib/components/dialog-shell/index.js`, matching the `ui/` barrel
convention.

Never render hinted text or labels through raw-HTML interpolation anywhere in
this directory — every string is a text interpolation, which is what keeps a
CMS-authored route description from becoming an XSS vector (threat T-70-01).
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-kit sync >/dev/null 2>&1; npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'dialog-shell'</automated>
  </verify>
  <acceptance_criteria>
- The three files exist at the paths listed in `<files>`.
- `npx svelte-check --tsconfig ./tsconfig.json --output machine` reports 0 lines containing ` ERROR ` whose filename matches `dialog-shell/DialogShell.svelte` or `dialog-shell/HintBar.svelte` or `dialog-shell/index.ts`.
- `grep -c 'data-dc34-dialog' DialogShell.svelte` is at least 1, and the attribute value is the `dialogId` prop (not a hardcoded string).
- `grep -E '\!max-w-\[420px\]' DialogShell.svelte` matches, and `grep -E '\!p-0' DialogShell.svelte` matches.
- `grep -c 'data-dialog-body' DialogShell.svelte` is 1 and `grep -c 'onfocusin' DialogShell.svelte` is at least 1 (keyboard focus feeds the hint bar).
- `grep -c 'ScrollArea' DialogShell.svelte` is 0.
- `grep -c 'Hover a row for details' index.ts` is 1 and the same default string appears in DialogShell.svelte.
- `grep -c 'data-hint-out' HintBar.svelte` is 1 and `grep -c 'aria-live' HintBar.svelte` is 1.
- `grep -c 'export { default as DialogShell }' index.ts` is 1; the barrel also names HintBar, Section, Row, Chips and Chip.
- `grep -rc '{@html' apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/ | grep -v ':0' | wc -l` prints 0 (no raw-HTML interpolation anywhere in the kit).
  </acceptance_criteria>
  <done>DialogShell renders a 420px carded dialog with header/body/optional footer/hint bar, the hint bar updates from delegated hover and focus events, and the barrel exposes the whole kit.</done>
</task>

<task type="auto">
  <name>Task 2: Section — the one collapse idiom</name>
  <files>apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Section.svelte</files>
  <read_first>
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md` (section "Section.svelte" — the chevron + master-checkbox header markup to unify, the MUST-PRESERVE master-collapse effect, and the node-swap landmine explanation)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` (§2 Section component)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/PublicOverlays.svelte` lines 140-170 (the header markup being unified; note the real `<button type="button">` with `aria-label`, and the uncontrolled `checked=` + `onchange=` pattern)
- `.planning/sketches/006-shared-dialog-shell/index.html` (`.sec`, `.sec-head`, `.chev`, `.sec-label`, `.sec-count`, `.v-card .sec` rules and the `change` delegation showing master-off semantics)
  </read_first>
  <action>
Create `Section.svelte` (runes mode, `lang="ts"`). Import
`import { ChevronDown } from '@lucide/svelte';` and `import type { Snippet } from 'svelte';`.

Props: `let { label, count, master = null, onmaster, collapsed = false, ontoggle,
variant = 'card', collapsible = true, hint, menu, children }: { label: string;
count?: number | string; master?: boolean | null; onmaster?: (v: boolean) => void;
collapsed?: boolean; ontoggle?: (c: boolean) => void; variant?: 'card' | 'plain';
collapsible?: boolean; hint?: string; menu?: Snippet; children?: Snippet } = $props();`

`collapsed` is a plain input prop plus an `ontoggle` callback — deliberately NOT
`$bindable`. Consumers own the collapse state because their existing
master-visibility effects (PublicOverlays.svelte lines 48-56 and its clones) must
keep writing it; a bindable would fight those effects.

Derive `const showChevron = $derived(collapsible && !!children);` and
`const isOpen = $derived(!collapsed || !collapsible);`.

Markup — outer wrapper: `<div data-section class={variant === 'card' ? 'overflow-hidden rounded-md border bg-muted/20' : ''}>`.
`rounded-md` + `border` + `bg-muted/20` is the Variant B carded treatment (~8px
radius, 1px border, surface-2-equivalent).

Header row div: `data-hint={hint}` and class
`flex items-center gap-2 ` plus `variant === 'card' ? 'px-3 py-2.5' : 'py-1.5 pr-1'`
plus, when `isOpen && showChevron && variant === 'card'`, `border-b` (UI-SPEC §2:
open sections show a divider under the header). Header children, in this exact order:

1. Chevron: when `showChevron`, a real
   `<button type="button" data-section-chevron aria-label={collapsed ? 'Expand' : 'Collapse'} aria-expanded={!collapsed}
   class="grid size-[18px] shrink-0 place-items-center text-muted-foreground transition-transform duration-150 hover:text-foreground"`
   with `-rotate-90` appended to the class when `collapsed`, rendering a single
   `<ChevronDown size="14" />` and `onclick={() => ontoggle?.(!collapsed)}`. ONE
   rotating glyph — never swap between two icon components. That node swap is
   exactly what forced the old hand-rolled outside-click handler (PATTERNS.md
   "Node-swap landmine"). When `showChevron` is false render
   `<span class="size-[18px] shrink-0"></span>` so labels stay aligned.
2. Label: a `<button type="button" data-section-label class="min-w-0 flex-1 truncate text-left text-[11px] font-bold uppercase tracking-[0.12em] text-muted-foreground">`
   with `opacity-55` appended when `master === false` (the dim state), text
   `{label}`, `onclick={() => ontoggle?.(!collapsed)}`, and `disabled={!showChevron}`.
   The label toggles collapse too, per UI-SPEC §2.
3. Count: `{#if count !== undefined}<span class="shrink-0 font-mono text-[11px] text-muted-foreground">{count}</span>{/if}`.
4. Menu slot: `{@render menu?.()}` — the overflow-menu position, always left of the master checkbox.
5. Master checkbox: `{#if master !== null}` an `<input type="checkbox" checked={master} class="size-[15px] shrink-0 cursor-pointer accent-primary" aria-label={'Toggle all in ' + label} onchange={(e) => onmaster?.(e.currentTarget.checked)} />`.
   Rightmost, uncontrolled (`checked=` + `onchange=`, never `bind:`), matching
   PublicOverlays.svelte line 159-164.

Body: `{#if isOpen && children}<div class={variant === 'card' ? 'py-1' : 'pl-4'}>{@render children()}</div>{/if}`.

Section itself performs NO cascade and NO auto-collapse: `onmaster` hands the new
boolean to the consumer, which calls into its layer and lets its own
prev-visible effect drive `collapsed`. That is what preserves today's
group-off-collapses behavior byte-for-byte.
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'dialog-shell/Section.svelte'</automated>
  </verify>
  <acceptance_criteria>
- `Section.svelte` exists and svelte-check reports 0 ` ERROR ` lines for it.
- `grep -c 'ChevronRight' Section.svelte` is 0 (single rotating glyph only, no icon-node swap).
- `grep -c '\-rotate-90' Section.svelte` is at least 1 and `grep -c 'transition-transform' Section.svelte` is at least 1.
- `grep -c 'tracking-\[0.12em\]' Section.svelte` is 1 and `grep -c 'uppercase' Section.svelte` is at least 1.
- `grep -c 'opacity-55' Section.svelte` is at least 1 (master-off dim).
- `grep -c 'bind:' Section.svelte` is 0 (uncontrolled master checkbox and collapse).
- In the header markup the `count` span appears before `{@render menu` which appears before the master `input type="checkbox"` (trailing order per UI-SPEC §2): `grep -n 'sec\|count\|menu\|checkbox' Section.svelte` shows increasing line numbers for count, menu render, checkbox input.
- `grep -c 'aria-label' Section.svelte` is at least 2 (chevron + master checkbox).
- `grep -c 'data-section-chevron' Section.svelte` is 1 and `grep -c 'data-section-label' Section.svelte` is 1.
  </acceptance_criteria>
  <done>Section renders card and plain variants with one rotating chevron, an uppercase tracked label that also toggles collapse, ordered trailing count/menu/master controls, and a dimmed label when master is off — with zero cascade logic of its own.</done>
</task>

<task type="auto">
  <name>Task 3: Row + Chips + Chip</name>
  <files>
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Row.svelte,
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chips.svelte,
apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Chip.svelte
  </files>
  <read_first>
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md` (sections "Row.svelte" and "Chips.svelte" — the layer-row analog, the colour-dot idiom, the segmented and multi-tint chip markup, and the `${color}22` hex-alpha tint)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` (§3 Row / chips / hint bar)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/PublicOverlays.svelte` lines 83-134 and 173-187 (the chip and row markup being unified — and the native tooltip attributes being retired)
- `.planning/sketches/006-shared-dialog-shell/index.html` (`.row`, `.dot`, `.chiprow`, `.seg`, `.chip` rules)
  </read_first>
  <action>
Create `Row.svelte` (runes mode, `lang="ts"`). Props:
`let { control = 'checkbox', name, checked = false, onchange, color, icon, label, meta, hint, trailing }:
{ control?: 'checkbox' | 'radio' | 'none'; name?: string; checked?: boolean;
onchange?: (v: boolean) => void; color?: string; icon?: Snippet; label: string;
meta?: string; hint?: string; trailing?: Snippet } = $props();`

Render a `<label data-layer-row data-hint={hint} class="group/row flex cursor-pointer items-center gap-2.5 px-3 py-1.5 text-sm transition-colors hover:bg-foreground/5">`
containing, in order:
- for `control === 'radio'`: `<input type="radio" {name} checked={checked} class="size-[15px] shrink-0 cursor-pointer accent-primary" onchange={(e) => { if (e.currentTarget.checked) onchange?.(true); }} />`
- for `control === 'checkbox'`: the same input with `type="checkbox"` and `onchange={(e) => onchange?.(e.currentTarget.checked)}`
- for `'none'`: no input
- `{#if color}<span class="inline-block h-2.5 w-2.5 shrink-0 rounded-full" style="background-color: {color}"></span>{/if}` — the exact 10px colour-dot idiom already used at PublicOverlays.svelte lines 181-184; keep the inline `style` because the colours are runtime data
- `{#if icon}<span class="shrink-0">{@render icon()}</span>{/if}`
- `<span class="min-w-0 flex-1 truncate">{label}</span>` — the `min-w-0 flex-1` + `truncate` pair is mandatory; dropping either breaks truncation inside a flex row
- `{#if meta}<span class="shrink-0 text-[11px] text-muted-foreground">{meta}</span>{/if}`
- `{@render trailing?.()}`

`data-layer-row` is on EVERY Row: it is the selector the prod probe uses to prove
no native tooltip attribute survives inside a layer row (UI-SPEC §8). Row is the
layers/list primitive; My Maps file rows are composed separately in CloudStorage.

Create `Chips.svelte` — the segmented single-select group. Props:
`let { options, value, onselect, hint }: { options: { key: string; label: string }[];
value: string; onselect: (key: string) => void; hint?: string } = $props();`
Render `<div class="flex flex-wrap gap-1.5 px-3 py-1" data-hint={hint}>` wrapping a
`<span class="inline-flex overflow-hidden rounded-full border border-border">`, and
inside it `{#each options as o, i (o.key)}` a
`<button type="button" aria-pressed={value === o.key} class="px-3 py-1 text-xs transition-colors">`
with `i > 0 ? 'border-l border-border' : ''` appended (joined pills) and, for the
selected one, `bg-primary/15 font-semibold text-primary`, otherwise
`text-muted-foreground hover:bg-accent`. `onclick={() => onselect(o.key)}`.
Selected state is weight AND tint, never colour alone (UI-SPEC §7).

Create `Chip.svelte` — one multi-select pill with optional type colour. Props:
`let { label, on = false, color, onclick, hint }: { label: string; on?: boolean;
color?: string; onclick: () => void; hint?: string } = $props();`
Render `<button type="button" aria-pressed={on} data-hint={hint} {onclick} class="rounded-full border px-2.5 py-1 text-xs transition-colors">`
with: `font-semibold` when `on`; `border-border text-muted-foreground hover:bg-accent`
when not `on`; `border-primary bg-primary/15 text-primary` when `on` and no
`color` is given; and when `on` AND `color` is given, the inline
`style={`border-color:${color};background:${color}22;color:${color}`}` form — the
`22` hex-alpha suffix is the ~13% tint the spec calls for and is already the
shipped idiom at PublicOverlays.svelte lines 115-117.

All three components interpolate `label`/`meta` as text only — no raw-HTML
interpolation (threat T-70-01).
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -cE 'dialog-shell/(Row|Chips|Chip)\.svelte'</automated>
  </verify>
  <acceptance_criteria>
- The three files exist and svelte-check reports 0 ` ERROR ` lines for any of them.
- `grep -c 'data-layer-row' Row.svelte` is 1.
- `grep -c 'h-2.5 w-2.5 shrink-0 rounded-full' Row.svelte` is 1 (colour-dot idiom preserved verbatim).
- `grep -c 'min-w-0 flex-1 truncate' Row.svelte` is 1 (truncation pair intact).
- `grep -c 'accent-primary' Row.svelte` is at least 1.
- `grep -c 'aria-pressed' Chips.svelte` is 1 and `grep -c 'aria-pressed' Chip.svelte` is 1.
- `grep -c 'font-semibold' Chips.svelte` is at least 1 (selected state is not colour-only).
- `grep -F -c '}22' Chip.svelte` is at least 1 (hex-alpha tint).
- `grep -rc '{@html' Row.svelte Chips.svelte Chip.svelte` prints `:0` for all three.
- Whole-kit gate: `npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'dialog-shell'` prints 0.
  </acceptance_criteria>
  <done>Row renders checkbox/radio/none layer rows with colour dot, truncating label, meta and trailing slots; Chips renders the joined segmented group; Chip renders a type-coloured multi-select pill. The kit is complete and type-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS/API → studio DOM | Route titles, `shortDescription`, folder names and user file names are fetched from `/api/gpx/**` and rendered by this kit |
| user pointer/keyboard → dialog | Hover and focus events select which hint string is displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-01 | Injection (XSS) | HintBar, Row, Section, Chip labels | medium | mitigate | Every external string is a Svelte text interpolation; zero raw-HTML interpolation in `dialog-shell/` — grep-gated in tasks 1 and 3 |
| T-70-02 | Information disclosure | `data-hint` attributes in the DOM | low | accept | Hint text is the same CMS description already rendered as a native tooltip today; no secrets, tokens or S3 keys are placed in hint text |
| T-70-03 | Tampering (supply chain) | npm dependency graph | high | mitigate | This plan adds ZERO dependencies: `package.json` and `package-lock.json` under `apps/run.gpx/gpx-studio/website` must be byte-unchanged (asserted in `<verification>`); no package-manager install task exists, so no legitimacy checkpoint is required |
| T-70-04 | Spoofing (a11y/affordance) | Section chevron + master checkbox | low | mitigate | Chevron and label are real `<button type="button">` with `aria-label`/`aria-expanded`; master checkbox carries `aria-label`; chips carry `aria-pressed` |
</threat_model>

<verification>
- `cd apps/run.gpx/gpx-studio/website && npx svelte-kit sync && npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'dialog-shell'` prints 0.
- `git status --porcelain apps/run.gpx/gpx-studio/website/package.json apps/run.gpx/gpx-studio/website/package-lock.json` prints nothing (zero new dependencies).
- `ls apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/` lists exactly: Chip.svelte, Chips.svelte, DialogShell.svelte, HintBar.svelte, Row.svelte, Section.svelte, index.ts.
- No existing file was modified: `git status --porcelain -- apps/run.gpx/gpx-studio/website/src` shows only additions under `dialog-shell/`.
</verification>

<success_criteria>
- Six components + barrel exist, are runes-mode, and are svelte-check-clean.
- Section implements exactly one collapse affordance with no icon-node swap and no internal cascade.
- DialogShell owns the hint bar and its hover/focus delegation, and forwards `dialogId` as `data-dc34-dialog`.
- Zero new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-01-SUMMARY.md` when done.
</output>
