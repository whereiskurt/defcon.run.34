---
phase: 70-gpx-studio-shared-dialog-shell-map-layers-my-maps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte
autonomous: true
requirements: [DLGS-05]

must_haves:
  truths:
    - "An imported-but-untagged Strava card shows an amber chip that reads as an action to take, not a status"
    - "A never-imported Strava card shows a quiet import chip, so two identical-looking cards no longer behave differently with no visible difference"
    - "A tagged card is unchanged: check glyph plus weekday short name, card dimmed and disabled"
    - "Clicking any card still opens the same popover in the same mode as before (assign for imported, import for fresh)"
  artifacts:
    - apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte
  key_links:
    - "isTagged / isUntaggedImport (lines 192-198) and openPopover's mode selection (lines 203-213) are untouched — the chips are pure copy inside the existing card button"
---

<objective>
Make every non-tagged Strava card state read as an explicit action (DLGS-05):
the imported-but-untagged amber chip becomes an actionable label, and
never-imported cards gain a quiet import chip. Tagged cards are untouched.

Purpose: today an imported-untagged card and a never-imported card look
identical but do different things on click; only one of them carries a chip.

Output: one modified component, copy/markup only — zero behavioural change to
import or assign logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

This plan creates no new files or exported symbols. It changes two chip literals
and adds one `{:else}` arm inside the existing card button in
`StravaStrip.svelte`. New strings introduced: the chip copy `Pick a day` and
`+ Import`.

Symbols created by sibling plans in this phase (not drift): everything under
`lib/components/dialog-shell/` (plan 01), `flattenLayerTree` /
`lib/logic/basemap-tree-pure.ts` / `BasemapSection.svelte` /
`data-dc34-layers-btn` (plan 05), `data-file-row` / `handleExportFile` (plan 04).

<!-- planner-discipline-allow: Assign a day -->

<tasks>

<task type="auto">
  <name>Task 1: Three explicit card chip states</name>
  <files>apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte</files>
  <read_first>
- `apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` lines 185-215 (the `isTagged` / `isUntaggedImport` predicates and `openPopover`'s mode selection — read them to confirm you are changing nothing there)
- `apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` lines 538-592 (the card button and the current chip block being edited)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md` (section "StravaStrip.svelte" — the exact current chip block and the quiet-chip token to mirror)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` (§6 StravaStrip chips)
  </read_first>
  <action>
In the chip block inside the card `<button>` (currently around lines 567-579,
immediately after the `{a.type}` badge span):

1. Keep the `{#if tagged}` arm exactly as it is: the green
   `text-[10px] font-semibold text-green-600 dark:text-green-400` span rendering
   `✓ {weekdayShort(a.conDay as string)}`.
2. In the `{:else if untaggedImport}` arm keep the amber classes verbatim
   (`rounded bg-amber-500/15 px-1.5 py-0.5 text-[10px] font-semibold text-amber-600 dark:text-amber-500`)
   and change ONLY the chip text to `Pick a day` — an imperative, so the chip
   reads as the action the card performs rather than a description of its state.
3. Add a new `{:else}` arm for never-imported activities rendering a quiet chip:
   a `<span class="rounded bg-accent px-1.5 py-0.5 text-[10px] font-semibold text-muted-foreground">`
   whose text is `+ Import`. Mirror the existing `{a.type}` badge token on line
   564 for the quiet look, minus its `uppercase` (the chip is sentence-cased
   copy, not a type code).

Do not touch `isTagged`, `isUntaggedImport`, `openPopover`, `popoverMode`, the
card `<button>`'s `onclick`, its `disabled={tagged}` guard, or its dim classes.
Both new chips live inside that same button, so they need no handlers of their
own — the card's existing click already routes to the correct popover mode.
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'StravaStrip.svelte' || true); [ "${N:-0}" -eq 0 ] && echo "OK: 0 new errors" || { echo "FAIL: $N new errors"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'Pick a day' apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` is 1.
- `grep -c '+ Import' apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` is 1.
- `grep -c 'Assign a day' apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` is 0 (old copy fully replaced).
- `grep -c 'bg-amber-500/15' apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` is 1 (amber token untouched).
- `git diff -U0 apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte | grep -cE '^[-+][^-+]'` is 16 or fewer (copy-only change; no logic churn). The `[^-+]` second character is required so the `---`/`+++` file headers are not counted as changed lines.
- `git diff apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte | grep -cE '^[-+].*(isTagged|isUntaggedImport|popoverMode|openPopover)'` is 0 (predicates and mode selection untouched).
- `N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'StravaStrip.svelte' || true); [ "${N:-0}" -eq 0 ] && echo OK` prints `OK`. (The `|| true` is load-bearing: `grep -c` exits 1 when it matches nothing, so the bare pipeline would fail exactly when the task succeeded.)
  </acceptance_criteria>
  <done>All three card states carry an explicit chip (✓ weekday / Pick a day / + Import), the amber and green tokens are unchanged, and no predicate or popover-mode code was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strava API → studio DOM | Activity names/types already rendered on the card; this change adds only static literal chip copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-05 | Injection (XSS) | StravaStrip chip block | low | accept | Both new chips render hardcoded literals — no external data enters the new markup; the surrounding `{a.name}` / `{a.type}` interpolations are unchanged text interpolations |
| T-70-06 | Elevation of privilege | card click → popover mode | medium | mitigate | `openPopover`, `popoverMode` and `disabled={tagged}` are asserted unchanged by a git-diff gate, so a tagged (already-scored) activity cannot become re-assignable via this change |
| T-70-03 | Tampering (supply chain) | npm dependency graph | high | mitigate | Zero dependencies added; `package.json` / `package-lock.json` asserted byte-unchanged in `<verification>` |
</threat_model>

<verification>
- `cd apps/run.gpx/gpx-studio/website && N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'StravaStrip.svelte' || true); [ "${N:-0}" -eq 0 ] && echo OK` prints `OK`.
- `git status --porcelain apps/run.gpx/gpx-studio/website/package.json apps/run.gpx/gpx-studio/website/package-lock.json` prints nothing.
- OWN-FILES SCOPE (not a whole-tree exclusivity claim): this plan shares one worktree with its wave-1 sibling 70-01, which is creating `lib/components/dialog-shell/` concurrently, so any assertion that the whole `src` tree contains only `StravaStrip.svelte` is false by construction and must never be written. Scope instead to this plan's own path: `git status --porcelain -uall -- apps/run.gpx/gpx-studio/website/src/lib/components/StravaStrip.svelte` prints either that one path or nothing (nothing once the executor has committed it) — and no other path.
- NEGATIVE GATES on files no plan in this phase touches: `W=apps/run.gpx/gpx-studio/website; git status --porcelain -- $W/src/lib/components/map/layer-control/LayerTree.svelte $W/src/lib/components/map/layer-control/LayerTreeNode.svelte $W/src/lib/components/map/layer-control/LayerControlSettings.svelte $W/src/lib/components/map/public-overlays.ts $W/src/lib/components/map/my-con-runs.ts $W/src/lib/components/map/community-routes.ts $W/src/lib/cloud-sync.ts $W/src/routes` prints nothing.
</verification>

<success_criteria>
- Imported-untagged cards read `Pick a day`; never-imported cards read `+ Import`; tagged cards unchanged.
- Diff is copy/markup only: no predicate, handler or mode change.
- svelte-check adds no error for the touched file.
</success_criteria>

<output>
Create `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-02-SUMMARY.md` when done.
</output>
