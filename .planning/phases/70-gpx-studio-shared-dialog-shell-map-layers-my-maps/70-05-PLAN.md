---
phase: 70-gpx-studio-shared-dialog-shell-map-layers-my-maps
plan: 05
type: execute
wave: 3
depends_on: ["70-01", "70-03"]
files_modified:
  - apps/run.gpx/gpx-studio/website/src/lib/logic/basemap-tree-pure.ts
  - apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/BasemapSection.svelte
  - apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte
autonomous: true
requirements: [DLGS-02, DLGS-04]

must_haves:
  truths:
    - "Clicking the layers control opens a centered Map Layers dialog; the control never opens on hover and never closes on mouse-leave"
    - "The dialog body renders sections in the order BASEMAP, USER CHECK-INS, the public route groups (DEF CON 34 ROUTES then RABBIT ROUTES), MY DEF CON RUNS, COMMUNITY ROUTES"
    - "Basemap selection is a flat list of radio rows with no second collapse affordance and no node labelled as a panel title; picking one still records the previous basemap and switches the live map style"
    - "Empty sections still render nothing"
    - "QuickStart hub actions routes and runners still reveal their layers and open the dialog"
    - "Every layer instance created in map.onLoad, the async-auth loads, the day-assign popup bridge and the run-reload subscription behave exactly as before"
    - "Esc and overlay click close the dialog and focus returns to the layers button"
  artifacts:
    - apps/run.gpx/gpx-studio/website/src/lib/logic/basemap-tree-pure.ts
    - apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/BasemapSection.svelte
    - apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte
  key_links:
    - "LayerControl keeps CustomControl for map-corner placement; the dialog is rendered as a SIBLING of CustomControl and portals itself to body"
    - "LayerControl.svelte lines 1-444 are script-only and preserved: setStyle + mode effect, overlay effects, the whole map.onLoad block, the onMount day-assign bridge, hasOverlays, quickStartAction and myConRunsRefresh subscriptions"
    - "LayerTree.svelte itself is NOT modified — it still serves the overlays tree and LayerControlSettings"
    - "flattenLayerTree keeps the recursive tree walk out of the component so basemap rows stay a flat radio list"
---

<objective>
Turn the LayerControl hover popover into the click-opened Map Layers dialog
(DLGS-02) and delete the hover wiring that causes the second reported stutter
(DLGS-04), plus replace the basemap tree with flat radio rows so the dialog has
exactly one collapse affordance per section and no node label masquerading as a
panel title.

Purpose: the panel currently opens on raw mouse-enter, closes on mouse-leave,
needs a hand-rolled window click listener to survive its own chevron icon swap,
and presents "Basemaps" as if it were the panel's title.

Output: two new files (a pure tree-flattener and a BasemapSection component) plus
a rewritten LayerControl TEMPLATE. Every line of LayerControl's script that owns
layer lifecycle is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-CONTEXT.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-01-SUMMARY.md
@.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-03-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

New symbols created by THIS plan:

| Symbol | Kind |
|---|---|
| `apps/run.gpx/gpx-studio/website/src/lib/logic/basemap-tree-pure.ts` | new module |
| `flattenLayerTree` | exported pure function in that module |
| `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/BasemapSection.svelte` | new component |
| `data-dc34-layers-btn` | data attribute on the layers control button (prod-probe selector) |
| `basemapCollapsed`, `overlaysCollapsed`, `layersBtn` | new local state in LayerControl |

Consumed from sibling plans: `DialogShell` / `Section` / `Row` and
`data-dc34-dialog` (plan 01); the Section-emitting `PublicOverlays`, `MyConRuns`
and `CommunityRoutes` (plan 03); `data-file-row` / `handleExportFile` exist in
CloudStorage from plan 04.

<!-- planner-discipline-allow: onmouseenter -->
<!-- planner-discipline-allow: onmouseleave -->
<!-- planner-discipline-allow: onpointerenter -->
<!-- planner-discipline-allow: cancelEvents -->
<!-- planner-discipline-allow: svelte:window -->
<!-- planner-discipline-allow: ScrollArea -->

<tasks>

<task type="auto">
  <name>Task 1: flattenLayerTree + BasemapSection</name>
  <files>
apps/run.gpx/gpx-studio/website/src/lib/logic/basemap-tree-pure.ts,
apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/BasemapSection.svelte
  </files>
  <read_first>
- `apps/run.gpx/gpx-studio/website/src/lib/assets/layers.ts` line 757 (`LayerTreeType` definition) and lines 833-880 (`defaultBasemapTree` — note the nested `basemaps.world` / `basemaps.countries.<country>` shape and that only `true` leaves are meant to render)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerTreeNode.svelte` (the label-resolution chain you must mirror: custom layer name, then extension name, then the i18n key — plus the radio input it renders)
- `apps/run.gpx/gpx-studio/website/src/lib/logic/strava-strip-pure.ts` (the existing `*-pure.ts` module precedent: dependency-free logic, type-only imports)
- `apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/Section.svelte` and `Row.svelte`
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte` lines 479-489 (the basemap `onselect` callback pair that must be preserved)
  </read_first>
  <action>
Create `lib/logic/basemap-tree-pure.ts`: a dependency-free module exporting
`export function flattenLayerTree(tree: LayerTreeType | undefined): string[]`
with a type-only import (`import type { LayerTreeType } from '$lib/assets/layers';`).
It walks the tree depth-first in key-insertion order and returns the ids of every
leaf whose value is `true`; object values recurse; `false` leaves are skipped;
`undefined` input returns an empty array. Insertion order is what makes the
`basemaps.world` group come out before the per-country groups, matching the order
the tree renders today. It must not import any Svelte or store module — this is
the codebase's "testable without a component runner" pattern (gpx-studio has no
test runner, so its gate is svelte-check plus the prod probe).

Create `lib/components/map/layer-control/BasemapSection.svelte` (runes mode,
`lang="ts"`). Imports: `Section` and `Row` from
`$lib/components/dialog-shell/index.js`, `flattenLayerTree` from
`$lib/logic/basemap-tree-pure`, `import { i18n } from '$lib/i18n.svelte';`,
`import { settings } from '$lib/logic/settings';`, and
`import { extensionAPI } from '$lib/components/map/layer-control/extension-api';`.

Props: `let { collapsed = false, ontoggle }: { collapsed?: boolean; ontoggle?: (c: boolean) => void } = $props();`

Body: destructure `const { currentBasemap, previousBasemap, selectedBasemapTree, customLayers } = settings;`
and `const { isLayerFromExtension, getLayerName } = extensionAPI;`. Derive
`const ids = $derived(flattenLayerTree($selectedBasemapTree));`. Add
`function labelFor(id: string): string` that mirrors LayerTreeNode exactly:
return `$customLayers[id].name` when `$customLayers.hasOwnProperty(id)`, else
`$getLayerName(id)` when `$isLayerFromExtension(id)`, else
`i18n._('layers.label.' + id)`. Add
`function select(id: string) { $previousBasemap = $currentBasemap; $currentBasemap = id; }`
— this is the exact callback pair currently passed to `LayerTree` as `onselect`
at LayerControl.svelte lines 484-487, and it must stay semantically identical
because `previousBasemap` is what the rest of the app reads to restore a style.

Template: `<Section label="Basemap" {collapsed} {ontoggle} hint="Choose the background map style.">`
containing `{#each ids as id (id)}` a
`<Row control="radio" name="basemap" checked={$currentBasemap === id}
label={labelFor(id)} onchange={() => select(id)} />`. Compare against
`$currentBasemap` (the user's choice), NOT the effective style key — dark mode
swaps the rendered style but must not move the radio.

Do NOT modify `LayerTree.svelte` or `LayerTreeNode.svelte`: LayerTree still
renders the overlays tree in LayerControl and is used by `LayerControlSettings`
via Menu.svelte, so it stays intact.
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && npx svelte-kit sync >/dev/null 2>&1; N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -cE '(basemap-tree-pure.ts|BasemapSection.svelte)' || true); [ "${N:-0}" -eq 0 ] && echo "OK: 0 new errors" || { echo "FAIL: $N new errors"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
- Both files exist and svelte-check reports 0 ` ERROR ` lines for either.
- `grep -c 'export function flattenLayerTree' src/lib/logic/basemap-tree-pure.ts` is 1.
- `grep -c 'import type' src/lib/logic/basemap-tree-pure.ts` is 1 and `grep -c '^import {' src/lib/logic/basemap-tree-pure.ts` is 0 (type-only import; the module is runtime-dependency-free).
- `grep -c 'control="radio"' BasemapSection.svelte` is 1 and `grep -c 'name="basemap"' BasemapSection.svelte` is 1.
- `grep -c 'previousBasemap' BasemapSection.svelte` is 2 or more and `grep -c '\$currentBasemap = id' BasemapSection.svelte` is 1 (the onselect pair preserved).
- `grep -c 'layers.label.' BasemapSection.svelte` is 1 and `grep -c 'isLayerFromExtension' BasemapSection.svelte` is 2 (label chain mirrors LayerTreeNode).
- `git status --porcelain -- apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerTree.svelte apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerTreeNode.svelte` prints nothing (both untouched).
  </acceptance_criteria>
  <done>A pure flattener plus a BasemapSection that renders the enabled basemaps as one flat radio list inside a shared Section, preserving the previous/current basemap write pair and the custom-layer/extension/i18n label chain.</done>
</task>

<task type="auto">
  <name>Task 2: LayerControl — click-only trigger and dialog host</name>
  <files>apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte</files>
  <read_first>
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte` (whole file, 549 lines — you must know exactly which ranges are script-preserved and which template lines die)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-PATTERNS.md` (section "LayerControl.svelte" — the line-exact PRESERVE VERBATIM table, the DELETE list, the CustomControl click-trigger analog, the quickStartAction bridge, and the section conditional guards)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/spot-control/SpotControl.svelte` lines 50-60 (the click-triggered CustomControl button analog)
- `apps/run.gpx/gpx-studio/website/src/lib/components/map/custom-control/CustomControl.svelte` (its `class` prop is required; its container is moved into the mapbox corner element, which is why the dialog must be a sibling)
- `apps/run.gpx/gpx-studio/website/src/lib/components/dialog-shell/DialogShell.svelte` (snippet prop names and the controlled open/onOpenChange contract)
- `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-UI-SPEC.md` (§4 Map Layers dialog)
  </read_first>
  <action>
PRESERVE VERBATIM — do not edit, reorder or "clean up" any of these script
ranges (per the PATTERNS.md table): lines 43-136 (`darkBasemapFor`, `setStyle`,
the mode-tracking effect), 138-243 (`hasValidTileUrls`, `addOverlay`,
`updateOverlays` and their effects), 245-340 (the ENTIRE `map.onLoad` block —
OverpassLayer, PublicOverlaysLayer, MyConRunsLayer, CommunityRoutesLayer, the
`isAuthenticated.subscribe` async-auth load, Ghost/Rabbit/RainbowArch/CoffeeCup/
TheSpot/PayPhone/Deuce instantiation and their one-shot subscriptions, and the
`style.import.load` first-only hook), 342-382 (the `onMount` delegated
document-click → `dc34-open-day-assign` bridge and its cleanup), 384-391
(`hasOverlays`), 407-419 (the `quickStartAction` subscription), 421-444 (the
`myConRunsRefresh` subscription with its reveal capture), and the trailing
`{#if assignDialogFile}<ConDaySaveDialog …>` block. The subscriptions without
cleanup are intentional and documented in their comments — leave them.

Script edits:
- Imports: add `import { DialogShell, Section } from '$lib/components/dialog-shell/index.js';`
  and `import BasemapSection from './BasemapSection.svelte';`. Remove the
  `Separator` import and the `ScrollArea` import (the popover chrome they served
  is gone; the shell body scrolls itself). Keep every other import, including
  `LayerTree` (still used for the overlays tree) and `Layers`.
- Delete `let container: HTMLDivElement;` (line 50) — nothing binds to it any
  more — and add `let layersBtn: HTMLButtonElement | undefined;` for focus return.
- Delete the hover-open state and helpers: the two one-line functions that set
  `open` true/false (lines 394-399) and the `cancelEvents` state (line 400). Keep
  `let open = $state(false);` — `quickStartAction` writes it directly and must
  keep working.
- Add `let basemapCollapsed = $state(true);` (the basemap tree renders
  `defaultState="closed"` today, so the new section starts collapsed) and
  `let overlaysCollapsed = $state(false);`.

Template — replace EVERYTHING from line 447 to line 540 (the CustomControl
wrapper with its hover handlers, the reveal-transition grid, the scroll-area
popover body, and the whole window-level click listener block) with two siblings:

1. `<CustomControl class="w-[29px] h-[29px] shrink-0">` containing a single
   `<button type="button" bind:this={layersBtn} data-dc34-layers-btn
   aria-label="Map layers" aria-expanded={open}
   class="flex size-full items-center justify-center"
   onclick={() => (open = true)}>` wrapping `<Layers size="20" />`. Click-only:
   the mouse-enter, mouse-leave and pointer-enter handlers at lines 452-462, the
   500ms `cancelEvents` guard, and the grid-rows/grid-cols reveal transition at
   lines 464-475 are all deleted. So is the `svelte:window` outside-click handler
   at lines 529-540 — the shell's overlay owns dismissal now, and the rotating
   single-glyph chevron removed the icon-node-swap hazard that listener existed
   to work around.
2. `<DialogShell dialogId="layers" title="Map Layers" {open}
   onOpenChange={(o) => { open = o; if (!o) layersBtn?.focus(); }}>` with
   `{#snippet icon()}<Layers class="h-[17px] w-[17px]" />{/snippet}` and, as its
   body, the sections in UI-SPEC §4 order:
   - `<BasemapSection collapsed={basemapCollapsed} ontoggle={(c) => (basemapCollapsed = c)} />`
   - `{#if hasOverlays}` a `<Section label="Overlays" collapsed={overlaysCollapsed}
     ontoggle={(c) => (overlaysCollapsed = c)}>` wrapping the existing
     `<LayerTree layerTree={$selectedOverlayTree} name="overlays" multiple={true}
     bind:checked={$currentOverlays} />` inside a `<div class="px-3 py-1">`. Keep
     the guard — for DEF CON it is false, so the band stays hidden.
   - `{#if $publicOverlayGroups.length > 0 || $publicAggregate.available}<PublicOverlays layer={publicOverlaysLayer} />{/if}`
   - `{#if $myConRunGroups.length > 0}<MyConRuns layer={myConRunsLayer} />{/if}`
   - `{#if $communityRoutes.length > 0}<CommunityRoutes layer={communityRoutesLayer} />{/if}`
   Each child now emits its own Section card(s) (plan 03), so there is no `p-2 ml-1`
   wrapper div, no `Separator`, and no external `My DEF CON Runs` label div (the
   one at lines 509-513 is deleted — MyConRuns owns that label now). The four
   `{#if}` guards ARE the "empty sections stay hidden" behavior and must survive
   exactly as written.

Keep the dialog as a SIBLING of `<CustomControl>`: CustomControl's container is
relocated into the mapbox corner element, and bits-ui's portal already escapes to
`<body>`, so sibling placement keeps the control markup clean. `+page.svelte`
mount order (`SpotControl` then `LayerControl`) is unchanged — do not touch that
file. `Embedding.svelte` also renders `LayerControl`, so the click trigger must
work with no app-page-only assumptions.
  </action>
  <verify>
    <automated>cd apps/run.gpx/gpx-studio/website && N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'LayerControl.svelte' || true); [ "${N:-0}" -eq 0 ] && echo "OK: 0 new errors" || { echo "FAIL: $N new errors"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
- `grep -cE 'onmouseenter|onmouseleave|onpointerenter' LayerControl.svelte` is 0.
- `grep -c 'svelte:window' LayerControl.svelte` is 0 and `grep -c 'composedPath' LayerControl.svelte` is 0.
- `grep -c 'cancelEvents' LayerControl.svelte` is 0.
- `grep -c 'ScrollArea' LayerControl.svelte` is 0 and `grep -c 'Separator' LayerControl.svelte` is 0.
- `grep -c 'data-dc34-layers-btn' LayerControl.svelte` is 1 and `grep -c 'aria-expanded={open}' LayerControl.svelte` is 1.
- `grep -c '<DialogShell' LayerControl.svelte` is 1 and `grep -c 'dialogId="layers"' LayerControl.svelte` is 1.
- `grep -c '<BasemapSection' LayerControl.svelte` is 1.
- `grep -c 'My DEF CON Runs' LayerControl.svelte` is 0 (the label moved into MyConRuns).
- Section guards intact: `grep -c '{#if \$publicOverlayGroups.length > 0 || \$publicAggregate.available}' LayerControl.svelte` is 1, `grep -c '{#if \$myConRunGroups.length > 0}' LayerControl.svelte` is 1, `grep -c '{#if \$communityRoutes.length > 0}' LayerControl.svelte` is 1, `grep -c '{#if hasOverlays}' LayerControl.svelte` is 1.
- Layer wiring untouched: `git diff -U0 -- apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControl.svelte | grep '^-' | grep -cE 'new (OverpassLayer|PublicOverlaysLayer|MyConRunsLayer|CommunityRoutesLayer|GhostLayer|RabbitLayer|RainbowArch|CoffeeCup|TheSpot|PayPhone|DeuceLayer)\(' ` is 0.
- Bridges untouched: `git diff -U0 -- .../LayerControl.svelte | grep '^-' | grep -cE 'dc34-open-day-assign|isAuthenticated.subscribe|myConRunsReveal|quickStartAction.subscribe|style.import.load'` is 0.
- `grep -c 'open = true' LayerControl.svelte` is 3 (the button click plus the two quickStartAction branches).
- `grep -c 'layersBtn?.focus()' LayerControl.svelte` is 1 (focus returns to the trigger on close).
- `git status --porcelain -- 'apps/run.gpx/gpx-studio/website/src/routes' ` prints nothing (`+page.svelte` untouched).
- `N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -c 'LayerControl.svelte' || true); [ "${N:-0}" -eq 0 ] && echo OK` prints `OK`. (The `|| true` is load-bearing: `grep -c` exits 1 when it matches nothing, so the bare pipeline would fail exactly when the task succeeded.)
  </acceptance_criteria>
  <done>The layers control is a single click-only button that opens the Map Layers dialog with six ordered section cards; all hover-open, reveal-transition and window-listener code is gone; every layer lifecycle line, bridge and guard is byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/gpx/public/**` → studio DOM | Route group and route metadata rendered by the child sections inside this dialog |
| raw-HTML Mapbox popups → LayerControl | The `data-dc34-assign` delegated document-click bridge accepts a fileId from popup HTML |
| user pointer/keyboard → dialog dismissal | Overlay click and Esc now own dismissal instead of a hand-rolled window listener |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-12 | Tampering (regression risk) | LayerControl script lines 43-444 | high | mitigate | Git-diff gates assert that no layer-instantiation line, no `map.onLoad` bridge line and no subscription line was removed; the PRESERVE VERBATIM ranges are enumerated in the action |
| T-70-13 | Spoofing (event-target confusion) | dialog dismissal | medium | mitigate | The hand-rolled `composedPath` window listener is deleted in favour of the bits-ui overlay, and Section uses one rotating glyph so no click target is detached mid-dispatch |
| T-70-01 | Injection (XSS) | route/basemap labels in the dialog | medium | mitigate | Labels render through Row's text interpolation and `i18n._`; no raw-HTML interpolation is introduced |
| T-70-14 | Information disclosure | `data-dc34-assign` popup bridge | low | accept | Unchanged from today: the bridge only forwards a fileId it then looks up in the already-fetched `cloudFiles` store, and falls back to a minimal stub |
| T-70-03 | Tampering (supply chain) | npm dependency graph | high | mitigate | Zero dependencies added; lockfile asserted byte-unchanged in `<verification>` |
</threat_model>

<verification>
- `cd apps/run.gpx/gpx-studio/website && N=$(npx svelte-check --tsconfig ./tsconfig.json --output machine 2>&1 | grep ' ERROR ' | grep -cE '(LayerControl|BasemapSection)\.svelte|basemap-tree-pure\.ts' || true); [ "${N:-0}" -eq 0 ] && echo OK` prints `OK`.
- `grep -c 'title=' src/lib/components/map/layer-control/LayerControl.svelte || true` prints 0.
- `git status --porcelain apps/run.gpx/gpx-studio/website/package.json apps/run.gpx/gpx-studio/website/package-lock.json` prints nothing.
- `git diff --stat -- apps/run.gpx/gpx-studio/website/src/lib/components/map/public-overlays.ts apps/run.gpx/gpx-studio/website/src/lib/components/map/my-con-runs.ts apps/run.gpx/gpx-studio/website/src/lib/components/map/community-routes.ts apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerTree.svelte apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerTreeNode.svelte apps/run.gpx/gpx-studio/website/src/lib/components/map/layer-control/LayerControlSettings.svelte` prints nothing.
- `cd apps/run.gpx && ./build-frontend.sh` completes with exit status 0 (first full compile gate for the whole phase — all six touched components are now imported from the app route, so the bundler type-checks and tree-shakes them for real).
</verification>

<success_criteria>
- The layers control opens the dialog on click only; hover does nothing.
- Six sections render in UI-SPEC §4 order, empty ones hidden.
- Basemap radios switch the live map and record the previous basemap.
- QuickStart routes/runners still open the dialog with their layers revealed.
- `build-frontend.sh` builds clean.
</success_criteria>

<output>
Create `.planning/phases/70-gpx-studio-shared-dialog-shell-map-layers-my-maps/70-05-SUMMARY.md` when done.
</output>
