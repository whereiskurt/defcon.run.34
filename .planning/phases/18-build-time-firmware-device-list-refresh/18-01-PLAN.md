---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.flash/webapp/src/config/firmware.ts
  - apps/run.flash/webapp/next.config.ts
  - apps/run.flash/README.md
autonomous: true
requirements: [FLSH-07, FLSH-08, DPLY-06]

must_haves:
  truths:
    - "src/config/firmware.ts reads FIRMWARE_VERSION from process.env.NEXT_PUBLIC_FIRMWARE_VERSION — no hardcoded version string remains"
    - "getFactoryFilename() returns firmware-{target}-{version}.factory.bin (not the app-only .bin)"
    - "Running `next build` with NEXT_PUBLIC_FIRMWARE_VERSION unset or empty exits non-zero with a clear message"
    - "apps/run.flash/README.md contains a release checklist covering offline-verify + FLSH-08 hardware boot test"
  artifacts:
    - path: "apps/run.flash/webapp/src/config/firmware.ts"
      provides: "env-injected FIRMWARE_VERSION + .factory.bin filename generator"
      contains: "process.env.NEXT_PUBLIC_FIRMWARE_VERSION"
    - path: "apps/run.flash/webapp/next.config.ts"
      provides: "build-time assertion that NEXT_PUBLIC_FIRMWARE_VERSION is non-empty in production"
      contains: "NEXT_PUBLIC_FIRMWARE_VERSION"
    - path: "apps/run.flash/README.md"
      provides: "release checklist — offline verify + Recommended device boot test"
      contains: "hardware boot test"
  key_links:
    - from: "apps/run.flash/webapp/src/config/firmware.ts"
      to: "process.env.NEXT_PUBLIC_FIRMWARE_VERSION"
      via: "module-scope constant"
      pattern: "process\\.env\\.NEXT_PUBLIC_FIRMWARE_VERSION"
    - from: "apps/run.flash/webapp/src/config/firmware.ts"
      to: "*.factory.bin"
      via: "getFactoryFilename return value"
      pattern: "\\.factory\\.bin"
    - from: "apps/run.flash/webapp/next.config.ts"
      to: "throw / process.exit on empty NEXT_PUBLIC_FIRMWARE_VERSION"
      via: "production-only assertion"
      pattern: "NEXT_PUBLIC_FIRMWARE_VERSION"
---

<objective>
Establish the code-side contract for Phase 18: FIRMWARE_VERSION is read from a
build-injected env var (never hardcoded), the factory image filename replaces
the app-only .bin filename, `next build` fails loudly on an empty version, and
the run.flash README carries the release-time verification checklist.

Purpose: Downstream plans (18-02 dev scripts, 18-03 Dockerfile) mirror this
contract. Doing the code contract first means the Dockerfile plumbing has a
definite target env var name and filename to match — no scavenger hunt.

Output: firmware.ts, next.config.ts, and README changes committed; no
regressions in `next dev` when a local .env.local (populated by 18-02) sets
NEXT_PUBLIC_FIRMWARE_VERSION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Read CONTEXT.md before starting — decisions D-01 (factory image), D-03
(env-var injection), and D-05 (offline README checklist) are locked there.

@.planning/phases/18-build-time-firmware-device-list-refresh/18-CONTEXT.md

Current shape of the constant being replaced (do NOT keep the fallback string):

apps/run.flash/webapp/src/config/firmware.ts:9
`export const FIRMWARE_VERSION = "2.6.11.60ec05e";`

Downstream consumers that use FIRMWARE_VERSION as a re-exported constant (no
signature change required — they will keep working once the source reads env):
- src/components/flash/flash-step.tsx (imports FIRMWARE_VERSION, getFactoryFilename)
- src/hooks/use-flash.ts (calls loadFirmware(device) — uses default arg)

next.config.ts already gates production behavior with `isDev = process.env.NODE_ENV !== "production"` — reuse that gate to scope the assertion to `next build`, so `next dev` without the env var remains a hard error only after 18-02 has produced .env.local.
</context>

<interfaces>
<!-- Exports that must remain callable by existing consumers (no signature changes). -->
<!-- flash-step.tsx and use-flash.ts import these — do not rename or change arity. -->

From src/config/firmware.ts (current — DO NOT change signatures):
```typescript
export const FIRMWARE_VERSION: string;              // becomes env-read, still a string
export const FIRMWARE_BASE_PATH: string;            // unchanged
export function getFactoryFilename(device: DeviceHardware, version?: string): string;
export function loadFirmware(device: DeviceHardware, version?: string):
  Promise<{ data: string; size: number; filename: string }>;
export function formatBytes(bytes: number): string;
```

Only two things change inside these:
1. `FIRMWARE_VERSION`'s initializer: string literal → `process.env.NEXT_PUBLIC_FIRMWARE_VERSION ?? ""`
2. `getFactoryFilename`'s return template: `.bin` → `.factory.bin`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite firmware.ts — env-injected version and factory-image filename</name>
  <files>apps/run.flash/webapp/src/config/firmware.ts</files>
  <behavior>
    - FIRMWARE_VERSION resolves to `process.env.NEXT_PUBLIC_FIRMWARE_VERSION ?? ""` at module load (no string literal fallback)
    - getFactoryFilename returns `firmware-{platformioTarget}-{version}.factory.bin`
    - Existing consumers (flash-step.tsx, use-flash.ts) compile without edits because signatures are preserved
    - The stale TODO/placeholder JSDoc block on the old constant is removed; a one-line comment naming the injection source (Dockerfile builder ARG) replaces it
  </behavior>
  <action>
    Per D-01 and D-03: delete the hardcoded `export const FIRMWARE_VERSION = "2.6.11.60ec05e";` and its
    multi-line TODO JSDoc; replace with `export const FIRMWARE_VERSION = process.env.NEXT_PUBLIC_FIRMWARE_VERSION ?? "";`
    (single-line comment above naming the source — Dockerfile builder ARG /
    local .env.local from download-firmware.sh). Change `getFactoryFilename` return
    from `firmware-${device.platformioTarget}-${version}.bin` to
    `firmware-${device.platformioTarget}-${version}.factory.bin`. Keep the export
    signatures and the `version: string = FIRMWARE_VERSION` default arg on both
    getFactoryFilename and loadFirmware unchanged (their signatures are consumed by
    flash-step.tsx and use-flash.ts). Do not touch FIRMWARE_BASE_PATH,
    uint8ToBinaryString, loadFirmware body, or formatBytes.
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp &amp;&amp; grep -q 'process.env.NEXT_PUBLIC_FIRMWARE_VERSION' src/config/firmware.ts &amp;&amp; grep -q '.factory.bin' src/config/firmware.ts &amp;&amp; ! grep -qE 'FIRMWARE_VERSION\s*=\s*"[0-9]' src/config/firmware.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>firmware.ts reads FIRMWARE_VERSION from env, returns .factory.bin from getFactoryFilename, contains no hardcoded version literal, and TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add production-only NEXT_PUBLIC_FIRMWARE_VERSION assertion to next.config.ts</name>
  <files>apps/run.flash/webapp/next.config.ts</files>
  <behavior>
    - When NODE_ENV=production (i.e., `next build`) and NEXT_PUBLIC_FIRMWARE_VERSION is unset or empty, the config module throws with a message naming the env var and pointing to the Dockerfile builder ARG / download-firmware.sh
    - When NODE_ENV!=production (i.e., `next dev`), no throw — dev flow remains usable while 18-02 wires up the .env.local writer
    - The env var is also exposed via `env: { NEXT_PUBLIC_FIRMWARE_VERSION: ... }` so it is embedded into the client bundle at build time (mirroring the existing NEXT_PUBLIC_REGION_SHORT pattern already in this file)
  </behavior>
  <action>
    Per D-03: near the top of next.config.ts (after the existing `const isDev = ...`
    line), add a production-gated assertion — `if (!isDev &amp;&amp; !process.env.NEXT_PUBLIC_FIRMWARE_VERSION) throw new Error(...)`.
    The error message must name NEXT_PUBLIC_FIRMWARE_VERSION and point to
    `Dockerfile.webapp` builder ARG or `scripts/download-firmware.sh` as remediation.
    In the `env: { ... }` block, add `NEXT_PUBLIC_FIRMWARE_VERSION: process.env.NEXT_PUBLIC_FIRMWARE_VERSION || ""`
    alongside the existing NEXT_PUBLIC_REGION_SHORT entry so the value is baked
    into the client bundle. Do not touch basePath / assetPrefix / turbopack /
    trailingSlash / output logic.
  </action>
  <verify>
    <automated>cd apps/run.flash/webapp &amp;&amp; grep -q 'NEXT_PUBLIC_FIRMWARE_VERSION' next.config.ts &amp;&amp; NODE_ENV=production node -e "try { require('./next.config.ts'); process.exit(1) } catch (e) { if (String(e).includes('NEXT_PUBLIC_FIRMWARE_VERSION')) process.exit(0); process.exit(2) }" 2>/dev/null || NODE_ENV=production NEXT_PUBLIC_FIRMWARE_VERSION= npx --yes tsx -e "import('./next.config.ts').then(() => process.exit(1)).catch(e => { if (String(e).includes('NEXT_PUBLIC_FIRMWARE_VERSION')) process.exit(0); process.exit(2) })"</automated>
  </verify>
  <done>next.config.ts throws in production build mode when NEXT_PUBLIC_FIRMWARE_VERSION is empty, and the value is added to the `env:` client-bundle map.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add release-time verification checklist to run.flash README</name>
  <files>apps/run.flash/README.md</files>
  <behavior>
    - README contains a "Release verification checklist" section with:
      (a) Grep-based offline verification: after `docker build`, confirm the built image contains no `api.meshtastic.org` or `github.com/meshtastic` string in `.next/standalone` or `.next/static` (matches the Dockerfile grep gate 18-03 will add)
      (b) Hardware boot test: flash one device from `RECOMMENDED_SLUGS` end-to-end using the built image and confirm the device boots and connects — the FLSH-08 gate STATE.md flags as the highest open risk
    - Both steps are one-command / one-action checkboxes a release engineer can walk through
  </behavior>
  <action>
    Per D-05 (offline guarantee documentation) and the FLSH-08 open risk from STATE.md:
    if apps/run.flash/README.md exists, append a new `## Release verification checklist`
    section; if it does not exist, create it with a minimal H1 title and this section
    only (do not scaffold unrelated content). The checklist has two subsections:
    "Offline guarantee (DPLY-06)" — copy-pasteable grep commands against
    `.next/standalone` and `.next/static` for `api.meshtastic.org` and
    `github.com/meshtastic` (`grep -rE 'api\.meshtastic\.org|github\.com/meshtastic'`);
    and "Hardware boot test (FLSH-08)" — run the built image locally, connect a
    Recommended device (e.g. HELTEC_V3, TBEAM, RAK4631), flash end-to-end, confirm
    the device boots and joins the Meshtastic UI. Reference the RECOMMENDED_SLUGS
    set in `src/config/devices.ts` as the authoritative device list.
  </action>
  <verify>
    <automated>test -f apps/run.flash/README.md &amp;&amp; grep -q 'Release verification checklist' apps/run.flash/README.md &amp;&amp; grep -q 'api.meshtastic.org' apps/run.flash/README.md &amp;&amp; grep -q 'FLSH-08' apps/run.flash/README.md</automated>
  </verify>
  <done>apps/run.flash/README.md contains the Release verification checklist with the offline grep step and the FLSH-08 hardware boot step, referencing RECOMMENDED_SLUGS.</done>
</task>

</tasks>

<verification>
- Grep confirms no hardcoded firmware version literal remains anywhere in `apps/run.flash/webapp/src/`
- `npx tsc --noEmit` on the webapp passes
- Setting NEXT_PUBLIC_FIRMWARE_VERSION= (empty) and running `next build` fails with a message naming the env var
- Setting NEXT_PUBLIC_FIRMWARE_VERSION=2.x.y.hash and running `next build` succeeds
- README grep gates all pass
</verification>

<success_criteria>
- FIRMWARE_VERSION comes exclusively from process.env.NEXT_PUBLIC_FIRMWARE_VERSION
- getFactoryFilename() returns `.factory.bin`
- Empty NEXT_PUBLIC_FIRMWARE_VERSION → hard build failure
- README release checklist covers offline grep + FLSH-08 hardware boot test
- No downstream import breaks: flash-step.tsx and use-flash.ts still typecheck
</success_criteria>

<output>
Create `.planning/phases/18-build-time-firmware-device-list-refresh/18-01-SUMMARY.md` when done.
</output>
