---
phase: 18
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - apps/run.flash/webapp/Dockerfile.webapp
autonomous: false
requirements: [FLSH-06, FLSH-07, FLSH-08, DEVC-06, DPLY-06]

must_haves:
  truths:
    - "A clean `docker build -f apps/run.flash/webapp/Dockerfile.webapp .` with no build-args resolves FIRMWARE_VERSION from api.meshtastic.org/github/firmware/list (releases.stable[0].id) and echoes it on a grep-able build-log line"
    - "Passing `--build-arg FIRMWARE_VERSION=x.y.z.hash` skips the API call and pins the version — used for rollback"
    - "If the API is unreachable AND no build-arg is given, the build fails (no hardcoded fallback anywhere in Dockerfile.webapp)"
    - "Stage 1 extracts firmware-{arch}-{version}.factory.bin (not the app-only .bin) into /firmware/ for all four ESP32-family archs"
    - "A hardware-list regeneration step (extension of Stage 1 or a distinct stage) fetches api.meshtastic.org/resource/deviceHardware, filters to esp32-family architectures, and overwrites public/data/hardware-list.json before the Next.js build reads it"
    - "The builder stage sets ENV NEXT_PUBLIC_FIRMWARE_VERSION=$FIRMWARE_VERSION before `npm run build` so next.config.ts's assertion passes and the value is embedded in the client bundle"
    - "A post-`npm run build` grep in the builder stage over `.next/standalone` and `.next/static` for `api.meshtastic.org` and `github.com/meshtastic` fails the build if any hit is found"
    - "A human has flashed one Recommended ESP32-family device end-to-end from the built image and confirmed it boots + connects (FLSH-08 gate)"
  artifacts:
    - path: "apps/run.flash/webapp/Dockerfile.webapp"
      provides: "build-time version resolution, factory-image vendoring, hardware-list regen, env-var plumbing, and offline grep gate"
      contains: "api.meshtastic.org/github/firmware/list"
  key_links:
    - from: "Dockerfile.webapp Stage 1"
      to: "api.meshtastic.org/github/firmware/list"
      via: "curl (default path)"
      pattern: "api\\.meshtastic\\.org/github/firmware/list"
    - from: "Dockerfile.webapp Stage 1 (or 1b)"
      to: "api.meshtastic.org/resource/deviceHardware"
      via: "curl + jq filter"
      pattern: "api\\.meshtastic\\.org/resource/deviceHardware"
    - from: "Dockerfile.webapp builder"
      to: "NEXT_PUBLIC_FIRMWARE_VERSION env in `next build`"
      via: "ARG + ENV plumbing"
      pattern: "NEXT_PUBLIC_FIRMWARE_VERSION"
    - from: "Dockerfile.webapp builder (post-build)"
      to: ".next/standalone + .next/static grep gate"
      via: "grep -RE ... && exit 1"
      pattern: "grep -R.*meshtastic"
---

<objective>
Rewrite the Dockerfile so a clean `docker build` with no source edits and no
build-args produces a run.flash image on the current Meshtastic stable release,
vendoring the correct factory images and the regenerated ESP32-only hardware
list, with NEXT_PUBLIC_FIRMWARE_VERSION plumbed into the Next build and a
grep gate that fails the build if any meshtastic hostname leaks into the
client bundle.

Then verify by hand that a Recommended ESP32-family device flashed from the
built image boots and connects — STATE.md flags this as the highest open risk
for v1.4 (FLSH-08), and the version-resolution work cannot be trusted until
this gate is green.

Purpose: All five ROADMAP success criteria for Phase 18 pass here. 18-01
established the code contract; 18-02 the dev-parity scripts; this plan is
where CI produces the shippable image.

Output: Updated Dockerfile.webapp + a completed human-verified boot test on
one Recommended device. Autonomous: false because the boot test is a
hardware-in-the-loop checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Read CONTEXT.md before starting — decisions D-01 (factory image), D-02
(mandatory API + build-arg override, no fallback), D-03 (NEXT_PUBLIC_FIRMWARE_VERSION),
D-04 (hardware-list Dockerfile stage), and D-05 (offline grep gate) all
converge here.

@.planning/phases/18-build-time-firmware-device-list-refresh/18-CONTEXT.md

Depends on 18-01 (firmware.ts reads env var, .factory.bin filename, next.config assertion)
and 18-02 (dev-parity scripts) — the Dockerfile mirrors those shapes so
dev-vs-prod drift is impossible.

Current Dockerfile.webapp shape (do NOT preserve the obsolete pieces):
- Stage 1 `firmware`: grepped `FIRMWARE_VERSION` from `src/config/firmware.ts`
  (that constant no longer exists after 18-01) — must be replaced.
- Stage 1 extracts `firmware-*.bin` — must become `firmware-*.factory.bin`.
- Stage 2 `builder`: already threads NEXT_PUBLIC_ASSET_PREFIX, VERSION_NGINX,
  VERSION_WEBAPP via ARG/ENV — add NEXT_PUBLIC_FIRMWARE_VERSION alongside
  using the same pattern (see current lines 55–68 for the template).
- Stage 3 `runner`: unchanged.

Upstream endpoints (build-time only; NEVER runtime):
- `https://api.meshtastic.org/github/firmware/list` → jq path `.releases.stable[0].id` → strip leading `v`
- `https://api.meshtastic.org/resource/deviceHardware` → jq filter on `architecture`

Alpine node image already has `apk` — install `jq` alongside the existing
`curl` + `unzip` install line in Stage 1.

Recommended device slugs (for the boot-test checkpoint):
`HELTEC_V3`, `TBEAM`, `TLORA_V2_1_1P6`, `RAK4631`, `STATION_G2` (from
`src/config/devices.ts` — do NOT modify).
</context>

<interfaces>
<!-- The contract Dockerfile changes must honor (from 18-01 and 18-02). -->

Contract from 18-01 (do NOT re-litigate):
- `FIRMWARE_VERSION` is `process.env.NEXT_PUBLIC_FIRMWARE_VERSION ?? ""` in src/config/firmware.ts
- getFactoryFilename returns `firmware-{platformioTarget}-{version}.factory.bin`
- next.config.ts throws in production if NEXT_PUBLIC_FIRMWARE_VERSION is empty

Contract from 18-02 (mirror the shape):
- Same API endpoints, same jq paths, same ESP32-family architecture filter
- If dev script uses `sed 's/^v//'` on the version, Dockerfile uses the same
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite Stage 1 — API version resolution, factory extraction, hardware-list regen</name>
  <files>apps/run.flash/webapp/Dockerfile.webapp</files>
  <behavior>
    - Stage 1 installs `curl unzip jq` (add jq to the existing `apk add` line)
    - `ARG FIRMWARE_VERSION=""` remains; the version-resolution RUN block is rewritten:
      - If `$FIRMWARE_VERSION` is empty, `curl -fsSL https://api.meshtastic.org/github/firmware/list | jq -r '.releases.stable[0].id' | sed 's/^v//'` populates it
      - If curl or jq fails (unreachable API + no build-arg), the RUN exits non-zero with an error message naming the endpoint
      - No hardcoded fallback anywhere — do NOT copy `src/config/firmware.ts` into the stage
    - Echoes `Resolved Meshtastic stable: ${FIRMWARE_VERSION}` on a dedicated build-log line
    - The extract loop over `esp32 esp32s3 esp32c3 esp32c6` fetches
      `firmware-{arch}-{version}.zip` and extracts `firmware-*.factory.bin` (not the app-only .bin)
    - The `*-update.bin` cleanup line remains as defense in depth
    - A second RUN block (still in Stage 1) fetches `https://api.meshtastic.org/resource/deviceHardware`,
      pipes through `jq` with the ESP32-family filter, and writes to
      `/hardware/hardware-list.json` (a new WORKDIR under /hardware, or /tmp — pick one and be consistent)
    - The resolved FIRMWARE_VERSION is persisted to `/firmware/VERSION.txt` so Stage 2 can read it deterministically without re-invoking the API
  </behavior>
  <action>
    Per D-01, D-02, D-04: replace Stage 1 wholesale. Start from the existing
    `FROM node:current-alpine AS firmware` line. Change the apk install to
    include `jq` (`RUN apk add --no-cache curl unzip jq`). Delete the
    `COPY src/config/firmware.ts /tmp/firmware.ts` line — that constant is
    gone after 18-01. In the version-resolution RUN block: if `$FIRMWARE_VERSION`
    is empty, resolve via `curl -fsSL https://api.meshtastic.org/github/firmware/list`
    piped through `jq -r '.releases.stable[0].id'` and `sed 's/^v//'`; on
    empty result after both paths, `echo "ERROR: Could not resolve Meshtastic stable version from api.meshtastic.org and no --build-arg FIRMWARE_VERSION provided" &amp;&amp; exit 1`.
    Add a dedicated echo line: `echo "Resolved Meshtastic stable: ${FW_VER}"`.
    Change the download loop's `unzip -q -o "/tmp/${ZIP}" "firmware-*.bin"` to
    `unzip -q -o "/tmp/${ZIP}" "firmware-*.factory.bin"`. Persist the resolved
    version: `echo "${FW_VER}" > /firmware/VERSION.txt` at the end of the RUN.
    Add a new RUN block for hardware-list regeneration:
    `mkdir -p /hardware &amp;&amp; curl -fsSL https://api.meshtastic.org/resource/deviceHardware | jq '[.[] | select(.architecture == "esp32" or .architecture == "esp32-s3" or .architecture == "esp32-c3" or .architecture == "esp32-c6")]' > /hardware/hardware-list.json &amp;&amp; jq -e 'length > 0' /hardware/hardware-list.json`.
    Keep the "Extracted N binaries" count echo.
  </action>
  <verify>
    <automated>grep -q 'api.meshtastic.org/github/firmware/list' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'api.meshtastic.org/resource/deviceHardware' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q '\.factory\.bin' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'Resolved Meshtastic stable' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; ! grep -q 'COPY src/config/firmware.ts' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; ! grep -qE 'FW_VER=[0-9]' apps/run.flash/webapp/Dockerfile.webapp</automated>
  </verify>
  <done>Stage 1 resolves the stable version via API by default, allows a build-arg override, extracts .factory.bin files for all four ESP32-family archs, regenerates the hardware list, and contains no hardcoded fallback.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stage 2 — NEXT_PUBLIC_FIRMWARE_VERSION plumbing + copy regenerated hardware-list + post-build offline grep gate</name>
  <files>apps/run.flash/webapp/Dockerfile.webapp</files>
  <behavior>
    - Builder reads `/firmware/VERSION.txt` from Stage 1 (via COPY --from=firmware) and sets `NEXT_PUBLIC_FIRMWARE_VERSION` to that value in ENV before `npm run build` runs
    - Builder copies `/hardware/hardware-list.json` from Stage 1 into `./public/data/hardware-list.json`, overwriting the tracked snapshot
    - Existing NEXT_PUBLIC_ASSET_PREFIX / VERSION_NGINX / VERSION_APP / REGION_SHORT plumbing is preserved unchanged
    - After `npm run build`, a grep gate runs against `.next/standalone` and `.next/static` for `api.meshtastic.org` and `github.com/meshtastic` — any hit fails the build with a clear error naming the offending path
    - The build fails deterministically if VERSION.txt is missing (no silent empty NEXT_PUBLIC_FIRMWARE_VERSION reaching next.config.ts)
  </behavior>
  <action>
    Per D-03 and D-05: in Stage 2 (`builder`), after the existing
    `COPY --from=firmware /firmware/ ./public/firmware/` line, add:
    `COPY --from=firmware /firmware/VERSION.txt /tmp/FIRMWARE_VERSION` and
    `COPY --from=firmware /hardware/hardware-list.json ./public/data/hardware-list.json`.
    Immediately before `RUN npm run build`, add a RUN block that reads the
    version file and exports it as an env var for the same shell invocation:
    `RUN FW_VER=$(cat /tmp/FIRMWARE_VERSION) &amp;&amp; test -n "$FW_VER" &amp;&amp; export NEXT_PUBLIC_FIRMWARE_VERSION="$FW_VER" &amp;&amp; echo "Building with NEXT_PUBLIC_FIRMWARE_VERSION=$NEXT_PUBLIC_FIRMWARE_VERSION" &amp;&amp; npm run build`
    (combining the export and build in one RUN so the env is in scope).
    Alternative if the combined RUN is awkward: use `ENV NEXT_PUBLIC_FIRMWARE_VERSION` fed by a build-arg populated from Stage 1 via a shared ARG — but the combined-RUN form is simpler and keeps Stage 1 the single source of truth.
    Directly after the `npm run build` step (same or new RUN block), add the
    grep gate: `RUN if grep -rE 'api\.meshtastic\.org|github\.com/meshtastic' .next/standalone .next/static 2>/dev/null; then echo "ERROR: meshtastic hostname leaked into client bundle — offline guarantee (DPLY-06) violated" &amp;&amp; exit 1; fi`.
    Leave Stage 3 (runner) untouched.
  </action>
  <verify>
    <automated>grep -q 'VERSION.txt' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'NEXT_PUBLIC_FIRMWARE_VERSION' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'public/data/hardware-list.json' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'grep -rE' apps/run.flash/webapp/Dockerfile.webapp &amp;&amp; grep -q 'DPLY-06' apps/run.flash/webapp/Dockerfile.webapp</automated>
  </verify>
  <done>Stage 2 threads NEXT_PUBLIC_FIRMWARE_VERSION from the Stage 1 resolved value, overwrites the tracked hardware-list.json, and runs the offline grep gate against the built .next output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FLSH-08 hardware boot verification — flash one Recommended device end-to-end</name>
  <what-built>Docker image built from the rewritten Dockerfile.webapp vendors the .factory.bin for each ESP32-family arch and serves it via the flash pipeline (writeFlash at 0x0, MD5 verify at 0x0 in src/hooks/use-flash.ts). This checkpoint validates that the factory-image switch actually produces a bootable device — STATE.md flags this as the highest open risk for v1.4.</what-built>
  <how-to-verify>
    1. Build the image locally: `docker build -t run-flash-p18 -f apps/run.flash/webapp/Dockerfile.webapp apps/run.flash/webapp/`
       - Expected: Build log shows `Resolved Meshtastic stable: X.Y.Z.hash` on a dedicated line
       - Expected: `docker inspect run-flash-p18` shows the image built cleanly
    2. Run the container: `docker run --rm -p 3000:3000 run-flash-p18` (or use the local dev flow with the updated scripts if easier)
    3. Open http://localhost:3000/ in Chrome or Edge (Web Serial required)
    4. Connect one device from the Recommended set — pick any ONE of: HELTEC_V3, TBEAM, TLORA_V2_1_1P6, RAK4631, STATION_G2 (physical device, USB cable, hold BOOT if required)
    5. Walk the full flow: Pick Device → Connect → Flash → Configure → Done
    6. After flashing completes, unplug the device and plug it back in
    7. Confirm the device boots (LED / display shows Meshtastic boot sequence, no bootloop)
    8. Confirm the device connects (Meshtastic Web/mobile UI shows the node, or the flasher's post-flash configure step succeeds)
    9. Record the tested device slug, the resolved firmware version from the build log, and the outcome in 18-03-SUMMARY.md
  </how-to-verify>
  <resume-signal>Reply "approved: {device_slug} at {version}" once the device boots and connects, OR "failed: {reason}" if the factory image does not produce a bootable device (which would require reverting Decision 1 and revisiting FLSH-08).</resume-signal>
</task>

</tasks>

<verification>
- `docker build` with no build-args succeeds and log shows a resolved version
- `docker build --build-arg FIRMWARE_VERSION=2.6.11.60ec05e` skips the API call and pins the version
- `docker build` with no network to api.meshtastic.org AND no build-arg fails at Stage 1 with a clear error
- Grep gate has been exercised (either passes on the actual build or a synthetic test string in the assets triggers it — validated during Task 2 by inspecting `docker build` log)
- One Recommended ESP32-family device flashed from the built image boots and connects (Task 3 checkpoint)
</verification>

<success_criteria>
- All five ROADMAP.md Phase 18 success criteria pass:
  1. Factory image at 0x00, boot-verified — Task 3 checkpoint green
  2. Clean docker build produces flasher on current stable, no hardcoded version — Task 1 grep gate
  3. FIRMWARE_VERSION build-injected, visible in the compiled bundle — Task 2 (UI placement is Phase 19)
  4. ESP32-only hardware list regenerated at build — Task 1 hardware-regen RUN
  5. Zero runtime network calls — Task 2 grep gate
- All five requirements (FLSH-06, FLSH-07, FLSH-08, DEVC-06, DPLY-06) covered
</success_criteria>

<output>
Create `.planning/phases/18-build-time-firmware-device-list-refresh/18-03-SUMMARY.md` when done — include the tested device slug, resolved firmware version, and boot-test outcome from Task 3.
</output>
