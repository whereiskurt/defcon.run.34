---
phase: 66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [MRAD-01, MRAD-08]
repo: monorepo
files_modified:
  - apps/run.human/webapp/src/entities/mesh-radio.ts
  - apps/run.human/webapp/src/entities/__tests__/mesh-radio-key-parity.test.ts
must_haves:
  truths:
    - "A MeshRadio ElectroDB entity exists on the shared run-human-electro table, keyed pk=nodeId, with a byUser GSI on userId."
    - "MeshRadio models FULL radio state (nodeNum, userId, publicKey, privateKey, verified, verificationCode, verifiedAt, verificationAttempts, resendAttempts, impersonate, showOnMap, source, createdAt, updatedAt) — not just decrypt keys."
    - "A vitest parity test asserts the EXACT composed keys pk='$run#nodeid_!433d1cec', sk='$meshradio_1', byUser gsi1pk='$run#userid_<userId>'."
    - "Server-side helpers upsertMeshRadio / deleteMeshRadio / getMeshRadio / getMeshRadiosByUser wrap the entity so every write funnels through one module (L10)."
    - "The module documents the ElectroDB-composed key format so meshtk (plan 66-07) hardcodes a byte-identical Go GetItem key."
  artifacts:
    - apps/run.human/webapp/src/entities/mesh-radio.ts
    - apps/run.human/webapp/src/entities/__tests__/mesh-radio-key-parity.test.ts
  key_links:
    - "MeshRadio.byUser overlays the pre-provisioned gsi1pk-gsi1sk-index (ElectroDB scopes GSIs by entity) — NO terraform change (MRAD-08)."
    - "The parity-locked pk/sk strings are the contract meshtk's Go GetItem (plan 66-07) composes byte-identically."
  prohibitions:
    - "NEVER add a physical global_secondary_index in terraform — gsi1/gsi2/gsi3 are already provisioned (RESEARCH L6)."
    - "NEVER change entity/version/service after the parity test locks — any drift silently 404s every meshtk decrypt (L1)."
    - "NEVER store uppercase hex in nodeId — ElectroDB lowercases composite values; keep the entity contract lowercase (L12)."
---

<objective>
Create the first-class `MeshRadio` ElectroDB entity that becomes the SINGLE SOURCE OF TRUTH for Meshtastic radios (per LOCKED hard-switch decision in CONTEXT), replacing the embedded `RunUser.meshtasticRadios[]` list. Keyed by `nodeId` for meshtk's direct `GetItem` hot path, with a `byUser` GSI for per-user listing / admin / the deferred Layer-2 join. Deliver the entity, its server-side CRUD helpers, and a key-parity test locking the exact ElectroDB-composed keys.

Purpose: Everything else in this phase (register write, readers, backfill, meshtk decrypt) targets this entity. The pk/sk format it composes is the phase's biggest landmine (L1) — lock it here.
Output: `mesh-radio.ts` (entity + helpers), `mesh-radio-key-parity.test.ts`, and a documented canonical key format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-CONTEXT.md
@.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-RESEARCH.md
@docs/authoritative-pubkey-ddb-design.md
@apps/run.human/webapp/src/entities/accomplishment.ts
@apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeshRadio entity + CRUD helpers</name>
  <read_first>
    - apps/run.human/webapp/src/entities/accomplishment.ts (clone template — GSI-bearing entity on the shared table; model/indexes/client-table close, createdAt default + updatedAt watch pattern)
    - apps/run.human/webapp/src/entities/run-user.ts lines 86-107 (the embedded meshtasticRadios map fields to model as full attributes)
    - apps/run.human/webapp/src/entities/client.ts (electroClient, ELECTRO_TABLE exports)
  </read_first>
  <files>apps/run.human/webapp/src/entities/mesh-radio.ts</files>
  <action>Create the MeshRadio ElectroDB entity by cloning accomplishment.ts's structure. model: entity "MeshRadio", version "1", service "run". Attributes (per CONTEXT LOCKED hard-switch — model FULL radio state): nodeId (string, required — the canonical "!hex" id), nodeNum (number, required — uint32 form so meshtk composes its key from the integer without reversing hex), userId (string, required — owning RunUser adapter userId), publicKey (string — the 0x hex authoritative X25519 pubkey), privateKey (string), verified (boolean, default false), verificationCode (string), verifiedAt (number), verificationAttempts (number, default 0), resendAttempts (number, default 0), impersonate (boolean, default false), showOnMap (boolean, default false), source (enum ["flash","sync","manual"] as const), createdAt (number, default Date.now, readOnly), updatedAt (number, watch all, set Date.now, readOnly). indexes: primary with pk field "pk" composite ["nodeId"] and sk field "sk" composite []; byUser with index "gsi1pk-gsi1sk-index", pk field "gsi1pk" composite ["userId"], sk field "gsi1sk" composite ["nodeId"]. Close with client electroClient, table ELECTRO_TABLE. Export type MeshRadioItem via EntityItem. Add SERVER-ONLY helpers that are the single write funnel (L10): upsertMeshRadio(input) using MeshRadio.put (put upserts by nodeId), deleteMeshRadio(nodeId), getMeshRadio(nodeId) via MeshRadio.get, getMeshRadiosByUser(userId) via MeshRadio.query.byUser paginated all. Header comment must record that byUser overlays the pre-provisioned generic gsi1 (ElectroDB scopes by entity — no terraform change, MRAD-08) and that the pk/sk format is parity-locked against meshtk plan 66-07. Do NOT wire any route here (plan 66-02 owns that).</action>
  <verify>
    <automated>cd apps/run.human/webapp && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'mesh-radio' || echo "no mesh-radio type errors"</automated>
  </verify>
  <acceptance_criteria>
    - mesh-radio.ts compiles clean; entity declares pk composite [nodeId], sk composite [], byUser on gsi1pk-gsi1sk-index.
    - All full-state attributes present including nodeNum (number) and source enum ["flash","sync","manual"].
    - upsertMeshRadio / deleteMeshRadio / getMeshRadio / getMeshRadiosByUser exported; no route imports them yet.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Key-parity test (TS side of L1)</name>
  <read_first>
    - apps/run.human/webapp/src/entities/__tests__/qr-key-parity.test.ts (clone — asserts exact composed pk/sk via .params({table}) offline, no network)
  </read_first>
  <files>apps/run.human/webapp/src/entities/__tests__/mesh-radio-key-parity.test.ts</files>
  <action>Clone qr-key-parity.test.ts for MeshRadio. Assert MeshRadio.get({ nodeId: "!433d1cec" }).params({ table: "run-human-electro" }).Key equals { pk: "$run#nodeid_!433d1cec", sk: "$meshradio_1" }. Assert the byUser GSI: MeshRadio.query.byUser({ userId: "user-abc" }).params({ table }) has IndexName "gsi1pk-gsi1sk-index" and ExpressionAttributeValues at ":pk" equal to "$run#userid_user-abc". Add a put-parity case asserting MeshRadio.put with nodeId "!433d1cec", nodeNum 1128733676, userId "user-abc", publicKey "0x" plus 64 hex chars, source "flash" produces Item.pk === "$run#nodeid_!433d1cec" and Item.sk === "$meshradio_1". The test header must state these literal strings are the contract meshtk's Go GetItem (plan 66-07) reproduces.</action>
  <verify>
    <automated>cd apps/run.human/webapp && source ~/.nvm/nvm.sh && nvm use 22.12.0 && npx vitest run src/entities/__tests__/mesh-radio-key-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Parity test passes asserting pk="$run#nodeid_!433d1cec", sk="$meshradio_1", byUser gsi1pk="$run#userid_user-abc".
    - Test header documents these strings as the Go-side contract (L1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Canonical key helper + MRAD-08 no-terraform doc</name>
  <read_first>
    - infra/terraform/modules/dynamodb/v1.0.0/main.tf lines 24-53 (confirms gsi1/gsi2/gsi3 pre-provisioned, projection ALL)
  </read_first>
  <files>apps/run.human/webapp/src/entities/mesh-radio.ts</files>
  <action>Add an exported meshRadioKeyFor(nodeId) helper to mesh-radio.ts that returns the composed { pk, sk } by calling MeshRadio.get({nodeId}).params().Key — one programmatic source of the canonical key strings so the Go format is never hand-guessed. In the module header record the confirmed format for the Go side: pk equals "$run#nodeid_" followed by the lowercased nodeId; sk equals "$meshradio_1"; byUser gsi1pk equals "$run#userid_" followed by userId. Also record the MRAD-08 decision inline: the shared run-human-electro table pre-provisions generic gsi1/gsi2/gsi3 (projection ALL); MeshRadio.byUser overlays gsi1 as an ElectroDB entity-scoped logical index, so there is no terraform change for this phase (the config half of MRAD-08 lives in plan 66-07).</action>
  <verify>
    <automated>cd apps/run.human/webapp && source ~/.nvm/nvm.sh && nvm use 22.12.0 && npx vitest run src/entities/__tests__/mesh-radio-key-parity.test.ts && grep -q 'meshradio_1' src/entities/mesh-radio.ts && grep -qi 'no terraform' src/entities/mesh-radio.ts && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - meshRadioKeyFor exported and returns the parity-locked pk/sk.
    - Module header documents the Go-side key format and the MRAD-08 no-terraform decision.
    - GATE_OK printed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app code → DynamoDB | ElectroDB-composed keys must match meshtk's Go-composed keys; a mismatch is a silent availability failure (every decrypt 404s). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | MeshRadio entity key composition | high | mitigate | Parity test locks pk/sk to exact strings; meshRadioKeyFor is the single programmatic source (Task 2/3). |
| T-66-02 | Denial of Service | GSI slot collision on shared gsi1 | low | accept | ElectroDB scopes GSIs by entity; Accomplishment/RunUser already co-tenant gsi1 safely — documented, no infra change. |

No package installs this plan (no T-66-SC).
</threat_model>

<verification>
- `npx vitest run src/entities/__tests__/mesh-radio-key-parity.test.ts` passes (Node 22.12).
- `mesh-radio.ts` type-checks; helpers exported; MRAD-08 doc + Go key format present in header.
</verification>

<success_criteria>
- MeshRadio entity exists with full-state attributes, pk=nodeId, byUser GSI on gsi1.
- Parity test green, locking pk="$run#nodeid_!...", sk="$meshradio_1".
- No terraform change; decision documented inline.
</success_criteria>

<output>
Create `.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-01-SUMMARY.md` when done.
</output>
