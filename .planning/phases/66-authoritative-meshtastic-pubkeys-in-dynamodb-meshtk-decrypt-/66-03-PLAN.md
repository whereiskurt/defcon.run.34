---
phase: 66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt
plan: 03
type: execute
wave: 3
depends_on: [66-01, 66-02]
autonomous: true
requirements: [MRAD-04]
repo: monorepo
files_modified:
  - apps/run.human/webapp/src/app/api/meshtastic-radios/route.ts
  - apps/run.human/webapp/src/app/api/meshtastic-radios/resend/route.ts
  - apps/run.human/webapp/src/app/api/internal/mesh-map/route.ts
  - apps/run.human/webapp/src/app/api/user/route.ts
  - apps/run.human/webapp/src/app/whoami/page.tsx
  - apps/run.human/webapp/src/components/profile/MeshtasticRadios.tsx
  - apps/run.human/webapp/src/entities/run-user.ts
  - apps/run.human/webapp/src/entities/run-user.test.ts
  - apps/run.human/webapp/src/app/api/internal/mesh-map/route.test.ts
must_haves:
  truths:
    - "EVERY reader/writer of RunUser.meshtasticRadios[] enumerated in RESEARCH §1 now reads/writes MeshRadio instead."
    - "The embedded meshtasticRadios attribute, updateMeshtasticRadios(), the MeshtasticRadio type, and sanitizeRadio() are removed from run-user.ts (hard-switch)."
    - "User-facing radio management (list/add/verify/resend/delete/impersonate/showOnMap) behaves identically to before — no UI regression."
    - "The mesh-map feed emits verified+showOnMap radios sourced from MeshRadio, not the embedded list."
    - "Per-user radio quota derives from a byUser count, not meshtasticRadios.length."
  artifacts:
    - apps/run.human/webapp/src/entities/mesh-radio.ts
  key_links:
    - "All CRUD routes → upsertMeshRadio/deleteMeshRadio/getMeshRadiosByUser (plan 66-01) — the single funnel."
    - "whoami UI radios prop now sourced from MeshRadio byUser query."
  prohibitions:
    - "NEVER leave any reader on the embedded list — a missed site means the retired attribute breaks it (grep must return zero references)."
    - "NEVER reintroduce dual-write — MeshRadio is the SINGLE source of truth (LOCKED hard-switch)."
    - "The mesh-map feed may enumerate MeshRadio via an entity scan or per-user byUser query; the meshtk no-Scan constraint applies ONLY to meshtk's decrypt hot path, not this low-frequency app feed."
---

<objective>
Execute the LOCKED hard-switch: migrate EVERY reader and writer of `RunUser.meshtasticRadios[]` (RESEARCH §1 enumeration, ~9 read sites incl. the whole whoami radio UI) onto the `MeshRadio` entity, then retire the embedded list, its write helper, its type, and its sanitizer from `run-user.ts`. No user-facing regression; no dual-write. This is the largest and highest-risk plan (spec §8a main risk) — it must land atomically so no reader is left pointing at a removed attribute.

Purpose: `MeshRadio` becomes the single source of truth the whole app agrees on, so meshtk's DDB reads and the app's writes never diverge.
Output: All radio CRUD + map feed + whoami UI on MeshRadio; embedded list fully removed; tests migrated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-CONTEXT.md
@.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-RESEARCH.md
@apps/run.human/webapp/src/entities/mesh-radio.ts
@apps/run.human/webapp/src/entities/run-user.ts
@apps/run.human/webapp/src/app/api/meshtastic-radios/route.ts
@apps/run.human/webapp/src/app/api/internal/mesh-map/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate user-facing CRUD + resend onto MeshRadio</name>
  <read_first>
    - apps/run.human/webapp/src/app/api/meshtastic-radios/route.ts (full GET/POST/PATCH/DELETE — the CRUD to migrate; note derivePublicKey returns base64, validateAndFormatNodeId does NOT pad)
    - apps/run.human/webapp/src/app/api/meshtastic-radios/resend/route.ts (resend — touches verification flow, not keys)
    - apps/run.human/webapp/src/entities/mesh-radio.ts (helpers from plan 66-01)
    - apps/run.human/webapp/src/lib/mesh-radio-canonical.ts (canonicalization + base64→hex from plan 66-02)
  </read_first>
  <files>apps/run.human/webapp/src/app/api/meshtastic-radios/route.ts, apps/run.human/webapp/src/app/api/meshtastic-radios/resend/route.ts</files>
  <action>Rewrite the CRUD route against MeshRadio helpers. GET: list via getMeshRadiosByUser(session.user.id); keep the same response shape { radios, quota } the UI expects (map MeshRadio items to the fields MeshtasticRadios.tsx reads; strip verificationCode). POST (add): canonicalize nodeId via normalizeNodeId (fixes the unpadded manual-add path, L2); duplicate-check via getMeshRadio; convert any provided/derived base64 publicKey to 0x hex via publicKeyBase64ToHex before storing (derivePublicKey still returns base64 — convert it, L3); upsertMeshRadio with source "manual", verified false, a generated verificationCode, explicit nodeNum. PATCH (verify/keys/impersonate/showOnMap): load the row by nodeId (the UI's radioId key must map to nodeId now — use nodeId as the stable id; adjust the client if it keyed on the old uuid, see Task 2), apply the same verification-attempt limit and field updates, convert publicKey base64→hex when keys change, upsertMeshRadio. DELETE: deleteMeshRadio(nodeId); preserve the "quota not restored on delete" behavior. Quota: derive current count from getMeshRadiosByUser length where the old code read meshtasticRadios.length. resend/route.ts: load the row via getMeshRadio, bump resendAttempts / regenerate code, upsertMeshRadio — no key change. Preserve all existing status codes, lockout gates (assertNotLockedLive), and quota consume/restore semantics.</action>
  <verify>
    <automated>cd apps/run.human/webapp && source ~/.nvm/nvm.sh && nvm use 22.12.0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GET/POST/PATCH/DELETE + resend operate on MeshRadio; response shapes unchanged for the client.
    - Manual-add nodeId is canonicalized pad-8; any base64 pubkey is stored as 0x hex.
    - Quota derives from a byUser count; lockout + status codes preserved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate readers — mesh-map, /api/user, whoami, MeshtasticRadios.tsx</name>
  <read_first>
    - apps/run.human/webapp/src/app/api/internal/mesh-map/route.ts (full — enumerates verified+showOnMap radios; hexToNodeNum)
    - apps/run.human/webapp/src/app/api/user/route.ts lines 40,80-99 (getRunUser spread → meshtasticRadios carried to whoami)
    - apps/run.human/webapp/src/app/whoami/page.tsx lines 46,420 (radios prop)
    - apps/run.human/webapp/src/components/profile/MeshtasticRadios.tsx lines 31-62,126-350 (CRUD client component; note which id field it sends on PATCH/DELETE)
  </read_first>
  <files>apps/run.human/webapp/src/app/api/internal/mesh-map/route.ts, apps/run.human/webapp/src/app/api/user/route.ts, apps/run.human/webapp/src/app/whoami/page.tsx, apps/run.human/webapp/src/components/profile/MeshtasticRadios.tsx</files>
  <action>mesh-map/route.ts: instead of scanAllRunUsers → u.meshtasticRadios, enumerate MeshRadio rows and emit { nodeNum, displayName, userType, pinIcon, pinColor } for verified && showOnMap. Prefer a MeshRadio entity scan (or per-user byUser query) plus a user-identity lookup for displayName/prefs; nodeNum comes straight from the stored MeshRadio.nodeNum (no hexToNodeNum needed). This app map feed is low-frequency — the meshtk no-Scan rule does NOT apply here. Keep the same output contract and no-store headers. api/user/route.ts: replace the implicit meshtasticRadios spread with an explicit radios field sourced from getMeshRadiosByUser(userId), presentation-safe (strip verificationCode/privateKey per the existing redaction posture). whoami/page.tsx: source the radios prop from the new user field. MeshtasticRadios.tsx: adjust only what the data-shape change requires — ensure PATCH/DELETE send nodeId (the new stable key) rather than the retired uuid id; keep all UI behavior identical.</action>
  <verify>
    <automated>cd apps/run.human/webapp && source ~/.nvm/nvm.sh && nvm use 22.12.0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mesh-map emits MeshRadio-sourced entries with correct nodeNum; contract unchanged.
    - /api/user + whoami surface radios from MeshRadio byUser; MeshtasticRadios.tsx keys on nodeId.
    - No component or route references user.meshtasticRadios.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Retire embedded list from run-user.ts + migrate tests</name>
  <read_first>
    - apps/run.human/webapp/src/entities/run-user.ts lines 86-107 (attr), 352-357 (updateMeshtasticRadios), 449-486 (MeshtasticRadio type + sanitizeRadio), 502 (RunUserItem.meshtasticRadios)
    - apps/run.human/webapp/src/entities/run-user.test.ts (sanitizeRadio unit tests to remove/migrate)
    - apps/run.human/webapp/src/app/api/internal/mesh-map/route.test.ts line 28 (fixture using meshtasticRadios)
  </read_first>
  <files>apps/run.human/webapp/src/entities/run-user.ts, apps/run.human/webapp/src/entities/run-user.test.ts, apps/run.human/webapp/src/app/api/internal/mesh-map/route.test.ts</files>
  <action>Remove the embedded meshtasticRadios attribute from the RunUser entity, the updateMeshtasticRadios() helper, the MeshtasticRadio type export, the sanitizeRadio() helper, and the meshtasticRadios field from RunUserItem — nothing should reference them after Tasks 1-2. Delete or migrate the sanitizeRadio unit tests in run-user.test.ts (the sanitizer is gone; its empty-string→NULL concern is now handled by MeshRadio's typed attributes). Update mesh-map/route.test.ts to build fixtures as MeshRadio rows instead of embedded-list users, matching the Task-2 rewrite. After edits, grep the whole run.human src for any remaining reference to meshtasticRadios / updateMeshtasticRadios / sanitizeRadio and confirm zero (outside comments/this SUMMARY).</action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -rn --include='*.ts' --include='*.tsx' -E 'meshtasticRadios|updateMeshtasticRadios|sanitizeRadio' src | grep -v '^\s*//' | grep -v -c 'mesh-radio' ; echo "expect 0 real refs above" && source ~/.nvm/nvm.sh && nvm use 22.12.0 && npx vitest run src/entities/run-user.test.ts src/app/api/internal/mesh-map/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - meshtasticRadios attr, updateMeshtasticRadios, MeshtasticRadio type, sanitizeRadio, RunUserItem.meshtasticRadios all removed.
    - grep for the retired symbols returns zero live references in run.human src.
    - run-user + mesh-map tests pass against the migrated shapes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user session → run.human CRUD → DynamoDB (MeshRadio) | User may only mutate their own radios; assertNotLockedLive still gates mutating routes. |
| internal secret → mesh-map feed → run.gpx | Only presentation-safe fields emitted; never key material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-06 | Denial of Service | half-migrated readers referencing a removed attr | high | mitigate | Atomic plan; grep gate proves zero residual references before retire lands. |
| T-66-07 | Information Disclosure | mesh-map / user route leaking key material | medium | mitigate | Emit only presentation-safe fields; strip verificationCode/privateKey (existing redaction preserved). |
| T-66-08 | Tampering | user mutating another user's radio | medium | accept | CRUD scoped by session.user.id via byUser; assertNotLockedLive unchanged. |

No package installs this plan (no T-66-SC).
</threat_model>

<verification>
- run.human `npm run build` passes.
- `vitest run` passes for run-user + mesh-map tests.
- grep for meshtasticRadios/updateMeshtasticRadios/sanitizeRadio returns zero live references.
- Manual UI parity: list/add/verify/resend/delete/impersonate/showOnMap all still function (checker/UAT).
</verification>

<success_criteria>
- Every enumerated reader/writer migrated to MeshRadio; embedded list fully retired.
- No user-facing radio-management regression.
- MeshRadio is the single source of truth (no dual-write).
</success_criteria>

<output>
Create `.planning/phases/66-authoritative-meshtastic-pubkeys-in-dynamodb-meshtk-decrypt-/66-03-SUMMARY.md` when done.
</output>
