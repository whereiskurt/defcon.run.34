---
phase: 55-ctf-flag-types-slice-2-scoring-windows-day-time-tz-gating-de
plan: 03
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - apps/run.human/webapp/src/components/admin/CtfForm.tsx
  - apps/run.human/webapp/src/lib/qr-admin.ts
  - apps/run.human/webapp/src/lib/__tests__/qr-admin.test.ts
autonomous: true
requirements: [CTFT-11]
must_haves:
  truths:
    - "An admin can set the day/time/tz window in the Scoring window & limits section (weekday multi-select + Opens/Closes times + PT/ET/UTC selector) and the enable toggle hides/clears it"
    - "The 'DEF CON run hours' quick set fills days Thu–Sun, 06:00–08:00, PT in one click and every field stays editable afterward"
    - "The stored tz is an IANA id; the window round-trips through save→edit (rehydrates on the edit page from the stored scoreWindow)"
    - "The write path passes scoreWindow through to the Ctf row only when provided (no-clobber on partial edits)"
  artifacts:
    - apps/run.human/webapp/src/components/admin/CtfForm.tsx
    - apps/run.human/webapp/src/lib/qr-admin.ts
    - apps/run.human/webapp/src/lib/__tests__/qr-admin.test.ts
  key_links:
    - "CtfForm imports formStateToScoreWindow / scoreWindowToFormState + TZ_OPTIONS + DEFCON_RUN_HOURS (55-01)"
    - "onSave sends ctf.scoreWindow; qr-admin.ctfAttributes emits it only when provided; the edit page's redacted record.scoreWindow rehydrates the picker"
---

<objective>
Replace the Phase-54 Slice-2 placeholder note in `CtfForm.tsx`'s **Scoring window & limits** section with the real day/time/tz picker (CTFT-11): an enable toggle, a weekday multi-select, Opens/Closes time inputs, a PT/ET/UTC timezone selector (stored as an IANA id), and the **"DEF CON run hours"** one-click quick-set chip (Thu–Sun 06:00–08:00 America/Los_Angeles). Wire the picker to the pure 55-01 bridge helpers, include `scoreWindow` in the save payload, rehydrate it on edit, and pass it through the `qr-admin` write path with the section's no-clobber discipline. Per the UI-SPEC there is deliberately NO player-facing window UI (a closed window is silently non-scoring).

Purpose: Slice 2's authoring half — the only place an operator configures a window. The round-trip contract is proven at the pure seam (55-01 tests) + the qr-admin passthrough test here, since the form has no jsdom.

Output: the rebuilt Scoring-window picker in CtfForm, `CtfInput.scoreWindow` + `ctfAttributes` passthrough, and a qr-admin test asserting no-clobber passthrough.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-ctf-flag-types-slice-2-scoring-windows-day-time-tz-gating-de/55-CONTEXT.md
@.planning/phases/55-ctf-flag-types-slice-2-scoring-windows-day-time-tz-gating-de/55-UI-SPEC.md

# The form to extend — Section 4 "Scoring window & limits" holds the placeholder note to replace (~lines 522-576); onSave assembles the ctf payload (~lines 223-295); state hooks live ~lines 159-190:
@apps/run.human/webapp/src/components/admin/CtfForm.tsx

# The pure bridge helpers + constants this picker binds to (built in 55-01):
@apps/run.human/webapp/src/components/admin/ctf-form-model.ts

# The write path — CtfInput type (~lines 127-161) + ctfAttributes no-clobber passthrough (~lines 328-375):
@apps/run.human/webapp/src/lib/qr-admin.ts

# The design system tokens the picker reuses (cls.segment/segmentActive/segmentIdle/chip/input/label/card/cardPad):
@apps/run.human/webapp/src/components/admin/qr-ui.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Day/time/tz picker in the Scoring window & limits section (CtfForm.tsx)</name>
  <files>apps/run.human/webapp/src/components/admin/CtfForm.tsx</files>
  <action>Import `formStateToScoreWindow`, `scoreWindowToFormState`, and `type ScoreWindowFormState` from `./ctf-form-model`, and `TZ_OPTIONS` + `DEFCON_RUN_HOURS` from `@/lib/ctf-score-window`. Seed the picker's state from the loaded record: `const swInit = scoreWindowToFormState(initial?.scoreWindow)` and add React state for `windowEnabled` (swInit.enabled), `windowDays` (number[]), `windowFrom`, `windowTo`, and `windowTzLabel` — so an existing flag rehydrates on edit (SC3 round-trip) and a new flag starts disabled ("Scorable any time."). REPLACE the placeholder note block (the `<div>` reading "Day / time / timezone windows arrive in a later update…" inside Section 4, currently ~lines 572-575) with the picker, reusing the section's existing `cls.*` tokens and matching the sibling controls' styling (do NOT introduce a new design system or dependency):
    - An enable toggle (a checkbox/`Switch`) labelled "Restrict scoring to a time window"; when off, render the helper "Scorable any time." and hide the rest of the picker (off ⇒ no scoreWindow persisted).
    - When on: (a) 7 weekday toggle chips Sun–Sat (reuse the `cls.segment`/`segmentActive`/`segmentIdle` pattern from the challenge-type presets, `role="group"`, each an accessible toggle) bound to `windowDays` (0=Sun..6=Sat); (b) two time inputs `type="time"` (24h HH:mm) labelled "Opens" / "Closes" bound to `windowFrom`/`windowTo`; (c) a timezone `<select>` (or HeroUI Select) listing exactly `TZ_OPTIONS` (PT/ET/UTC labels) bound to `windowTzLabel` — the stored value is the IANA id, resolved by the 55-01 helper on save; (d) the **"DEF CON run hours"** quick-set chip/button (accent `primary` via `cls.chip`) with sub-hint "Thu–Sun, 6–8 AM PT" whose onClick fills state from `DEFCON_RUN_HOURS` (map its tz back to the "PT" label via `scoreWindowToFormState(DEFCON_RUN_HOURS)`) AND leaves every field individually editable afterward (presets pre-fill, never lock — mirror `applyPreset`).
    - Copy exactly per the UI-SPEC: section helper "Only credit solves during this window. Outside it, a correct answer silently doesn't score — players can't tell the window is closed."; tz note "Times use this timezone; daylight saving is handled automatically."
    - In `onSave`, compute `const scoreWindow = formStateToScoreWindow({ enabled: windowEnabled, days: windowDays, from: windowFrom, to: windowTo, tzLabel: windowTzLabel })` and add `...(scoreWindow ? { scoreWindow } : {})` to the `ctf` payload object (so toggling off sends nothing → no-clobber leaves stored, and an operator who wants to CLEAR a window uses delete+recreate, consistent with the section's existing fields). Ensure every control has an accessible label and ≥40px touch targets consistent with the surface. If the live scoring preview is present and a window is enabled, surface a small non-blocking "window-gated" annotation near the preview (the preview still shows the point value — the window affects WHETHER it scores, not HOW much); do not add player-facing countdowns.
    Change nothing in the covert path and add no runtime dependency.</action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null && npx tsc --noEmit 2>&1 | grep -v "dropdown-user.tsx\|checkin.test.ts" | grep -c "CtfForm.tsx" | grep -qx 0 && echo "CtfForm tsc-clean"</automated>
  </verify>
  <done>The Section-4 placeholder note is gone; the picker renders enable toggle + weekday chips + Opens/Closes + PT/ET/UTC select + DEF CON run hours chip with the UI-SPEC copy; state rehydrates from initial.scoreWindow; onSave includes scoreWindow when enabled; CtfForm.tsx is tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 2: scoreWindow write-path passthrough + no-clobber test (qr-admin.ts)</name>
  <files>apps/run.human/webapp/src/lib/qr-admin.ts, apps/run.human/webapp/src/lib/__tests__/qr-admin.test.ts</files>
  <behavior>
    - ctfAttributes emits scoreWindow ONLY when input.scoreWindow is provided (an omitted scoreWindow leaves the key absent so a patch preserves the stored value — no-clobber).
    - ctfAttributes with a provided scoreWindow returns it verbatim ({days, from, to, tz}).
    - scoreWindow is NOT a flag-type field: it does not affect isRepeatable / mergeFlagTypeNextState (a scoreWindow-only edit must not trip the CTFT-06 static↔repeatable guard).
  </behavior>
  <action>Add `scoreWindow?: { days?: number[]; from?: string; to?: string; tz?: string }` to the `CtfInput` interface, alongside the other Slice-1a additive passthrough fields, with a comment: additive Slice-2 passthrough; no transform; no-clobber. In `ctfAttributes`, add `...(input.scoreWindow !== undefined ? { scoreWindow: input.scoreWindow } : {})` to the returned attribute object, next to the `perPlayerMax`/`globalMax` passthrough lines. Do NOT touch `mergeFlagTypeNextState` / `assertAnswerTypeTransition` — scoreWindow is not part of the repeatable-ness decision, so a window-only edit of a solved flag must not be rejected. Extend the existing `ctfAttributes` describe block in `__tests__/qr-admin.test.ts` with tests for all three behavior bullets (provided ⇒ emitted verbatim; omitted ⇒ key absent; and a case asserting a scoreWindow-only input produces no answerType/perPlayerMax keys).</action>
  <verify>
    <automated>cd apps/run.human/webapp && nvm use 23.6.0 >/dev/null && npx vitest run src/lib/__tests__/qr-admin.test.ts</automated>
  </verify>
  <done>ctfAttributes emits scoreWindow only when provided and verbatim when present; the no-clobber and non-flag-type cases pass; the existing qr-admin tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → server write (postQrAction ctf_upsert) | The admin-authed form submits scoreWindow; the write path passes it through untransformed onto the Ctf row. |
| server edit-page redacted record → client picker | The rehydration reads the non-secret scoreWindow off the redacted record (55-01 preserves it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-03-01 | Tampering | ctfAttributes scoreWindow passthrough | low | mitigate | Passthrough is no-clobber (emitted only when provided) and does not touch the CTFT-06 flip guard, so a window-only edit of a solved flag can't corrupt scoring-history semantics; asserted by test. |
| T-55-03-02 | Elevation of Privilege | /admin/qr/ctf route | low | accept | The picker lives on the existing admin-gated CtfForm route; no new endpoint or auth surface is added by this plan. |
| T-55-03-03 | Information Disclosure | player-facing leak | medium | mitigate | Per UI-SPEC, NO player-facing window/countdown UI is added — the window is enforced silently in the judge; the form copy states the covert-safe behavior to the admin only. |
| T-55-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies — HeroUI + existing qr-ui tokens only. |
</threat_model>

<verification>
- `cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run src/lib/__tests__/qr-admin.test.ts src/components/admin/__tests__/ctf-form-model.test.ts` — green (the round-trip proof lives in the 55-01 form-model test; passthrough here).
- `cd apps/run.human/webapp && nvm use 23.6.0 && npx tsc --noEmit` — no NEW errors (dropdown-user.tsx + checkin.test.ts pre-existing only).
- `cd apps/run.human/webapp && nvm use 23.6.0 && npx vitest run` — full webapp suite green.
- Manual visual check (post-execution, non-blocking): open /admin/qr/ctf/new, enable the window, click "DEF CON run hours" → chips show Thu·Fri·Sat·Sun, Opens 06:00, Closes 08:00, tz PT; every field still editable.
</verification>

<success_criteria>
- CTFT-11 satisfied: the day/time/tz picker + DEF CON run hours quick set replace the placeholder; stored tz is an IANA id; the window round-trips through save→edit (rehydration + the 55-01 pure round-trip proof).
- SC3 met: one-click quick set fills Thu–Sun 6–8 AM PT and fields stay editable; the value round-trips and stores an IANA id.
- No player-facing window UI; no new dependency; covert path untouched.
</success_criteria>

<output>
Create `.planning/phases/55-ctf-flag-types-slice-2-scoring-windows-day-time-tz-gating-de/55-03-SUMMARY.md` when done.
</output>
