---
phase: 40-admin-activity-reports
plan: 05
type: execute
wave: 2
depends_on: ["40-04"]
files_modified:
  - infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md
autonomous: true
requirements: [AR-08]
must_haves:
  truths:
    - "The runbook documents the three Mapbox account controls and why URL-restriction is the critical one"
    - "The runbook explains gpx.map.view as the real-time leading indicator vs Mapbox's ~24h-lagged dashboard"
    - "The runbook tells the operator how to read the admin reports and bump site.hcl thresholds for con-week"
  artifacts:
    - "infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md"
  key_links:
    - "Runbook references the DefconRun/Activity metrics + admin/* queries produced by 40-04"
---

<objective>
Write the operator runbook for the account-side controls that have no code lever, primarily the
Mapbox account-hardening checklist (AR-08b — no Mapbox usage API exists, so the mitigation is
account configuration + our own gpx.map.view leading indicator), plus a short "reading the reports"
guide so the operator knows how to use the dashboard, the admin/* saved queries, and the site.hcl
threshold knobs.

Purpose: capture the human-only account controls and the operational knowledge that the Terraform
and app code cannot encode.
Output: a single RUNBOOK.md in the admin-reports module directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-admin-activity-reports/40-CONTEXT.md
@docs/superpowers/specs/2026-07-05-admin-activity-reports-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapbox account-hardening checklist</name>
  <files>infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/40-admin-activity-reports/40-CONTEXT.md (AR-08b: the four Mapbox mitigations)
    - docs/superpowers/specs/2026-07-05-admin-activity-reports-design.md (section 3.3 Mapbox rationale, ~24h lag, scrapeable token)
  </read_first>
  <action>
    Create RUNBOOK.md with a Mapbox section documenting, as a numbered operator checklist with exact
    dashboard locations: (1) URL-restrict the public token to `*.defcon.run` origins — flag this as the
    critical control because the token is scrapeable from client JS; (2) issue a dedicated per-app token
    (gpx at minimum) so the Mapbox per-token usage filter attributes traffic; (3) set a spending cap on
    the Mapbox account; (4) explain that the `gpx.map.view` event metric (MapViews in DefconRun/Activity)
    is the real-time leading indicator because our logs are live while Mapbox's stats lag ~24h and expose
    no usage API. Note the optional code lever (splitting the gpx public Mapbox token env into a dedicated
    per-app var) and where it would live, so a follow-up can pick it up. Prose only — no fenced code needed.
  </action>
  <verify>
    <automated>test -f infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md && grep -c 'defcon.run' infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - RUNBOOK.md exists and covers all four Mapbox mitigations (grep: URL-restrict, per-app token, spending cap, gpx.map.view / MapViews)
    - The URL-restriction control is explicitly called out as the critical one
  </acceptance_criteria>
  <done>Operator can perform the Mapbox hardening steps from the runbook without further research.</done>
</task>

<task type="auto">
  <name>Task 2: Reading-the-reports operator guide</name>
  <files>infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/40-admin-activity-reports/40-04-PLAN.md (metric names + admin/* query list to reference)
    - .planning/phases/40-admin-activity-reports/40-CONTEXT.md (distinct-active-users widget is the headline number; pre-con noisy posture; site.hcl is the volume knob)
  </read_first>
  <action>
    Append a "Reading the reports" section to RUNBOOK.md: how to open the admin-reports dashboard;
    what the distinct-active-users-last-hour widget and top-IPs widget mean; how to run each admin/*
    saved query (user-activity / ip-activity edit-the-placeholder workflow); and how to bump the
    pre-con thresholds to con-week values by editing the admin_reports block in site.hcl (one-line change,
    then terragrunt apply the us-east-1/admin-reports unit). State the pre-con posture explicitly:
    Signups>=1/hr firing is expected signal, not noise.
  </action>
  <verify>
    <automated>grep -c 'distinct\|admin/\|site.hcl' infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - RUNBOOK.md references the distinct-active-users widget, the admin/* queries, and the site.hcl threshold-bump workflow
    - The pre-con vs con-week threshold posture is documented
  </acceptance_criteria>
  <done>Operator can interpret the dashboard/queries and adjust thresholds from the runbook alone.</done>
</task>

</tasks>

<verification>
- RUNBOOK.md exists in the module dir with Mapbox checklist + reading-the-reports guide
- All four Mapbox controls and the threshold-bump workflow are present
</verification>

<success_criteria>
The account-side controls with no code lever are captured as an operator runbook alongside the module.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `infra/terraform/modules/admin-reports/v1.0.0/RUNBOOK.md` — Mapbox hardening checklist + reading-the-reports guide
</artifacts>

<output>
Create `.planning/phases/40-admin-activity-reports/40-05-SUMMARY.md` when done
</output>
