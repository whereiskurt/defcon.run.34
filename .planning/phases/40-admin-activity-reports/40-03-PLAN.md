---
phase: 40-admin-activity-reports
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/run.human/webapp/src/lib/log-event.ts
  - apps/run.human/webapp/src/lib/log-event.test.ts
  - apps/run.human/webapp/src/entities/checkin.ts
  - apps/run.human/webapp/src/entities/user-upload.ts
autonomous: true
requirements: [AR-01, AR-02]
must_haves:
  truths:
    - "A checkin write emits exactly one evt=human.checkin line"
    - "A user-upload record creation emits exactly one evt=human.upload line"
    - "logEvent never throws and extracts the first x-forwarded-for hop as ip"
  artifacts:
    - "apps/run.human/webapp/src/lib/log-event.ts + .test.ts"
  key_links:
    - "checkin write path (createCheckIn) request-boundary caller -> logEvent(human.checkin)"
    - "user-upload create path request-boundary caller -> logEvent(human.upload)"
---

<objective>
Add the `logEvent` helper to run.human and wire the two event call sites: `human.checkin` on the
checkin write path and `human.upload` on the user-upload create path. Because the entity write
functions do not hold request headers, the emit is placed at the nearest request-boundary caller
(the API route/action invoking the write) so the first x-forwarded-for hop is available.

Purpose: real-time signals for the two highest-value human actions (AR-02).
Output: logEvent helper + unit in run.human, plus two instrumented write paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-admin-activity-reports/40-CONTEXT.md
@apps/run.human/webapp/src/entities/checkin.ts
@apps/run.human/webapp/src/entities/user-upload.ts
</context>

## Event line contract (LOCKED — identical to 40-01)

logEvent emits one `JSON.stringify` line to stdout: keys `evt`, `userId`, `email`, `ip` (first
`x-forwarded-for` hop), `ua`, `meta`. Wrap in try/catch, return void, never block the request path.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: logEvent helper + vitest unit in run.human</name>
  <files>apps/run.human/webapp/src/lib/log-event.ts, apps/run.human/webapp/src/lib/log-event.test.ts</files>
  <read_first>
    - apps/run.human/webapp/src/lib/api.ts (existing src/lib style)
    - apps/run.human/webapp/src/lib/__tests__ (existing test location + vitest config)
    - .planning/phases/40-admin-activity-reports/40-01-PLAN.md ("Event line contract" — copy shape byte-for-byte)
  </read_first>
  <behavior>
    - `x-forwarded-for: "8.8.8.8, 172.16.0.1"` -> emitted ip is `8.8.8.8`
    - meta round-trips through JSON.parse unchanged
    - undefined headers/fields -> emits, does NOT throw
    - circular meta -> swallowed, does NOT throw
  </behavior>
  <action>
    Create the same `logEvent(evt, opts)` helper as 40-01 (single JSON.stringify line; first
    x-forwarded-for hop; user-agent as ua; try/catch swallow; void). Match run.human's src/lib and
    test conventions (place the unit where the app's other tests live — see __tests__). Cover each
    Behavior case in the vitest unit.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && npx vitest run src/lib/log-event.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest unit passes
    - `grep -c "x-forwarded-for" src/lib/log-event.ts` >= 1
    - helper uses `JSON.stringify` and `console.log`
  </acceptance_criteria>
  <done>logEvent exists in run.human, single-line JSON, first-hop ip, never throws, unit green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire human.checkin and human.upload at their request boundaries</name>
  <files>apps/run.human/webapp/src/entities/checkin.ts, apps/run.human/webapp/src/entities/user-upload.ts</files>
  <read_first>
    - apps/run.human/webapp/src/entities/checkin.ts (createCheckIn ~line 208; CheckIn.create ~233)
    - apps/run.human/webapp/src/entities/user-upload.ts (UserUpload create path; record created at presigned-URL generation)
    - the API route/server-action that CALLS createCheckIn and the one that creates the UserUpload record (grep `createCheckIn(` and `UserUpload.create` / the presign route)
    - apps/run.human/webapp/src/lib/log-event.ts (Task 1)
  </read_first>
  <action>
    Emit `human.checkin` right after the checkin write succeeds, and `human.upload` right after the
    user-upload record is created (at presigned-URL generation). Place each logEvent call at the
    request-boundary caller (the API route or server action) where `request.headers` and the session
    userId are available — NOT inside the ElectroDB entity function, which has no headers. Pass headers,
    userId, and a meta with the checkinId / uploadId. If the write path is only reachable through a
    single caller, thread the Headers into that caller and emit there. Fire-and-forget; do not change
    entity signatures beyond what is needed to reach the request boundary, and do not alter write
    behavior or response bodies.
  </action>
  <verify>
    <automated>cd apps/run.human/webapp && grep -rc 'human.checkin\|human.upload' src | grep -v ':0' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - a `human.checkin` logEvent emit exists on the checkin write path caller (grep >=1)
    - a `human.upload` logEvent emit exists on the upload-record create caller (grep >=1)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Checkin and upload each emit their evt line at the request boundary; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> run.human | Attacker-controlled headers reach logEvent ip/ua |
| run.human -> CloudWatch | stdout lines feed metric filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07 | Tampering | checkin/upload event line | medium | mitigate | Single JSON.stringify; `$.evt` structured match resists field injection |
| T-40-08 | Denial of Service | logEvent in write path | high | mitigate | try/catch swallow + fire-and-forget; write behavior unchanged |
| T-40-09 | Spoofing | x-forwarded-for ip | low | accept | Best-effort attribution only, documented |
</threat_model>

<verification>
- vitest unit green; both events wired at request boundaries; typecheck clean
- Prod fire-of-each-event deferred to 40-07
</verification>

<success_criteria>
run.human emits human.checkin and human.upload with the locked field shape via the shared logEvent helper.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `apps/run.human/webapp/src/lib/log-event.ts` + `.test.ts`
- Events: `human.checkin`, `human.upload`
</artifacts>

<output>
Create `.planning/phases/40-admin-activity-reports/40-03-SUMMARY.md` when done
</output>
