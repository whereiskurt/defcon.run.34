---
phase: 40-admin-activity-reports
plan: 07
type: execute
wave: 3
depends_on: ["40-01", "40-02", "40-03", "40-06"]
files_modified: []
autonomous: false
requirements: [AR-01, AR-02, AR-03, AR-04, AR-05, AR-07, AR-08]
must_haves:
  truths:
    - "Firing one of each event in prod increments the matching DefconRun/Activity metric"
    - "The distinct-active-users-last-hour widget shows the test user"
    - "A tripwire alarm email actually arrives in the operator inbox"
  artifacts:
    - ".planning/phases/40-admin-activity-reports/40-07-SUMMARY.md (verification evidence)"
  key_links:
    - "deployed instrumented apps -> awslogs -> metric filters -> dashboard + alarms end-to-end"
---

<objective>
Deploy the instrumented apps and the admin-reports module to us-east-1, then verify the whole chain
end-to-end by firing one of each event in the running environment and confirming the metric increments,
the dashboard widget updates, and a tripwire email arrives. Per CONTEXT this acceptance is explicitly
NOT satisfied by `terragrunt plan` alone — real events must flow through to a real alarm email.

Purpose: prove the producer->metric-filter->dashboard/alarm pipeline works in production.
Output: deployed apps + applied module + a SUMMARY capturing the verification evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-admin-activity-reports/40-CONTEXT.md
@apps/build.sh
@apps/deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy instrumented apps + apply admin-reports (us-east-1)</name>
  <files>(no source files — deploy + apply commands)</files>
  <read_first>
    - apps/build.sh and apps/deploy.sh (build/push to ECR + ECS deploy flow)
    - apps/release-all.sh (multi-region release wrapper; us-east-1 only is live)
    - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl (the unit to apply)
    - .claude/commands.md (full command reference for build/deploy)
  </read_first>
  <action>
    Build, push, and deploy run.auth, run.gpx, and run.human to us-east-1 so the logEvent instrumentation
    is live (use the repo's build.sh/deploy.sh or release-all.sh path; us-east-1 only — skip_regions
    excludes cac1/apse1). Then `terragrunt apply` the region/us-east-1/admin-reports unit to create the
    metric filters, retention adoption (import), query definitions, dashboard, SNS topic, and alarms.
    Confirm the SNS email subscription is CONFIRMED (the operator must click the AWS confirmation email
    once). If any AWS/ECR auth step fails, surface an auth checkpoint rather than fabricating success.
    Capture the applied resource ids (dashboard name, SNS topic arn, alarm names) for the SUMMARY.
  </action>
  <verify>
    <automated>aws cloudwatch list-dashboards --query "DashboardEntries[?DashboardName=='admin-reports']" --output text | grep -q admin-reports && aws logs describe-metric-filters --metric-namespace DefconRun/Activity --query 'length(metricFilters)' --output text</automated>
  </verify>
  <acceptance_criteria>
    - The `admin-reports` dashboard exists (`aws cloudwatch list-dashboards`)
    - `aws logs describe-metric-filters --metric-namespace DefconRun/Activity` returns >=7 filters
    - The three apps' ECS services are running the new task definitions (deploy reported success)
    - The SNS email subscription status is `Confirmed`
  </acceptance_criteria>
  <done>Instrumented apps are live and the admin-reports module is applied in us-east-1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fire one of each event in prod and confirm the pipeline</name>
  <action>
    Human-verify the deployed pipeline end-to-end by firing one of each event in production and
    confirming the metric increments, the distinct-active-users widget updates, and a tripwire email
    arrives. Follow the numbered how-to-verify steps below; do not mark complete on `terragrunt plan` alone.
  </action>
  <what-built>
    logEvent instrumentation in run.auth/run.gpx/run.human, plus the admin-reports metric filters,
    dashboard, saved queries, and four SNS tripwire alarms — all deployed to us-east-1.
  </what-built>
  <how-to-verify>
    1. Sign up a fresh test account at auth.defcon.run (new email) -> a signup happens.
    2. Sign in again with that account -> a login happens.
    3. At gpx.defcon.run: create a gpx file, publish it, request a share, and open the public map.
    4. At run.defcon.run: perform a checkin and a file upload.
    5. Open the CloudWatch `admin-reports` dashboard (us-east-1). Confirm within a few minutes:
       - DefconRun/Activity metrics Signups, Logins, GpxUploads, GpxShares, MapViews, Checkins, Uploads each ticked up.
       - The "distinct active users, last hour" widget shows the test user (count includes your test userId).
       - The "top IPs" widget shows your client IP (first x-forwarded-for hop).
    6. Confirm a tripwire email arrived at the operator inbox (the Signups>=1/hr alarm should fire on the test signup).
    7. Run the `admin/user-activity` saved query with the test userId placeholder -> it returns the events you just fired.
  </how-to-verify>
  <resume-signal>Type "approved" once metrics incremented, the distinct-users widget shows the test user, and the tripwire email arrived — or describe what did not fire.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> prod deploy | Deploy/apply touches live us-east-1 services and log groups |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-16 | Denial of Service | ECS redeploy of three apps | medium | mitigate | Rolling ECS deploy; verify each service healthy before firing test events; retention uses import (no group recreate) |
| T-40-17 | Repudiation | verification evidence | low | mitigate | SUMMARY records dashboard/metric/alarm ids + screenshots-of-record so the pass is auditable |
</threat_model>

<verification>
- Dashboard + >=7 metric filters live; SNS subscription confirmed
- Human checkpoint: each event increments its metric, distinct-users widget shows the test user, tripwire email arrives, admin/user-activity returns the events
</verification>

<success_criteria>
The full producer->filter->dashboard/alarm chain is proven in production: real events increment real
metrics and a real tripwire email lands in the operator inbox.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- Deployed instrumented run.auth / run.gpx / run.human (us-east-1)
- Applied admin-reports module (dashboard, filters, retention, queries, SNS + alarms)
- `.planning/phases/40-admin-activity-reports/40-07-SUMMARY.md` — end-to-end verification evidence
</artifacts>

<output>
Create `.planning/phases/40-admin-activity-reports/40-07-SUMMARY.md` when done
</output>
