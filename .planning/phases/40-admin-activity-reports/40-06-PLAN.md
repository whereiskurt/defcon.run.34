---
phase: 40-admin-activity-reports
plan: 06
type: execute
wave: 2
depends_on: ["40-04"]
files_modified:
  - infra/terraform/modules/admin-reports/v1.0.0/dashboard.tf
  - infra/terraform/modules/admin-reports/v1.0.0/alarms.tf
  - infra/terraform/modules/admin-reports/v1.0.0/variables.tf
  - infra/terraform/live/site/site.hcl
  - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl
autonomous: true
requirements: [AR-03, AR-05, AR-07]
must_haves:
  truths:
    - "An admin-reports CloudWatch dashboard shows ALB, CloudFront, DefconRun/Activity, distinct-active-users, and top-IPs"
    - "An SNS topic with an email subscription exists, address parameterized in site.hcl"
    - "Four alarms fire on their thresholds, all thresholds parameterized in site.hcl"
  artifacts:
    - "infra/terraform/modules/admin-reports/v1.0.0/dashboard.tf + alarms.tf"
    - "admin_reports thresholds + sns_email in site.hcl"
  key_links:
    - "alarms reference DefconRun/Activity metrics (Signups, GpxUploads) from 40-04"
    - "dashboard Logs Insights widgets query the /ecs/* app groups for count_distinct(userId)"
---

<objective>
Complete the admin-reports module presentation + alerting plane: the `admin-reports` CloudWatch
dashboard (AR-05) and the SNS tripwire alarms (AR-07). Thresholds and the SNS email are parameterized
in site.hcl so pre-con -> con-week is a one-line bump. Depends on 40-04, which created the module and
the DefconRun/Activity metrics these resources reference.

Purpose: give the operator the single-glance dashboard (headline number: distinct active users, last
hour) and get emailed the moment anyone does anything unusual (pre-con: anything at all).
Output: dashboard.tf, alarms.tf, threshold/email variables, and site.hcl wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-admin-activity-reports/40-CONTEXT.md
@infra/terraform/modules/site/v1.0.0/waf/dashboard.tf
@infra/terraform/modules/network/v1.0.0/alb.tf
@infra/terraform/modules/cloudfront/v1.0.0/main.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-reports dashboard</name>
  <files>infra/terraform/modules/admin-reports/v1.0.0/dashboard.tf</files>
  <read_first>
    - infra/terraform/modules/site/v1.0.0/waf/dashboard.tf (aws_cloudwatch_dashboard jsonencode widget pattern to mirror)
    - infra/terraform/modules/network/v1.0.0/alb.tf (target-group names for RequestCount / 4XX / 5XX / TargetResponseTime)
    - infra/terraform/modules/cloudfront/v1.0.0/main.tf (per-domain distributions for Requests / 4xxErrorRate / 5xxErrorRate; all six domains, metrics in us-east-1)
    - infra/terraform/modules/admin-reports/v1.0.0/outputs.tf (DefconRun/Activity metric names from 40-04)
  </read_first>
  <action>
    Create one `aws_cloudwatch_dashboard` named `admin-reports` (mirror the waf dashboard's jsonencode
    widget structure). Widgets: ALB per-target-group RequestCount, HTTPCode_Target_4XX_Count,
    HTTPCode_Target_5XX_Count, TargetResponseTime; CloudFront Requests + 4xxErrorRate/5xxErrorRate per
    distribution (all six domains auth/run/cms/gpx/flash/bib); the custom DefconRun/Activity event
    metrics stacked per hour; a Logs Insights widget for distinct active users last hour
    (count_distinct(userId) across the app /ecs/* groups) — the headline number; a Logs Insights widget
    for top IPs by event count; and a Strava rate-limit widget (StravaRateLimitUsage). Widget geometry
    and exact Logs Insights syntax are Claude's discretion provided the widgets answer the questions.
  </action>
  <verify>
    <automated>cd infra/terraform/modules/admin-reports/v1.0.0 && terraform init -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` passes
    - `grep -c 'admin-reports' dashboard.tf` >= 1 and `grep -c 'count_distinct' dashboard.tf` >= 1
    - dashboard references ALB (RequestCount), CloudFront (Requests), and DefconRun/Activity metrics (grep each)
  </acceptance_criteria>
  <done>The admin-reports dashboard validates with ALB, CloudFront, event, distinct-users, top-IP, and Strava widgets.</done>
</task>

<task type="auto">
  <name>Task 2: SNS topic + four parameterized tripwire alarms</name>
  <files>infra/terraform/modules/admin-reports/v1.0.0/alarms.tf, infra/terraform/modules/admin-reports/v1.0.0/variables.tf</files>
  <read_first>
    - infra/terraform/modules/admin-reports/v1.0.0/variables.tf (add threshold + email vars here)
    - infra/terraform/modules/admin-reports/v1.0.0/metrics.tf (DefconRun/Activity Signups + GpxUploads for the metric alarms)
    - infra/terraform/modules/network/v1.0.0/alb.tf (ALB RequestCount + HTTPCode_Target_5XX_Count dimensions for the ALB alarms)
    - .planning/phases/40-admin-activity-reports/40-CONTEXT.md (AR-07 alarm list + pre-con defaults)
  </read_first>
  <action>
    Add variables: `sns_alarm_email`, `threshold_signups_per_hour` (default 1), `threshold_gpx_uploads_per_hour`
    (default 5), `threshold_alb_5xx_per_5min` (default 10). Create an `aws_sns_topic` + an
    `aws_sns_topic_subscription` (protocol email, endpoint = sns_alarm_email). Create four
    `aws_cloudwatch_metric_alarm` resources, all alarm_actions -> the SNS topic:
    (a) Signups >= threshold_signups_per_hour over 1h on DefconRun/Activity Signups;
    (b) GpxUploads >= threshold_gpx_uploads_per_hour over 1h;
    (c) ALB total RequestCount ANOMALY-DETECTION alarm (aws_cloudwatch_metric_alarm with an
    anomaly-detection band / ANOMALY_DETECTION_BAND expression, comparison GreaterThanUpperThreshold);
    (d) ALB HTTPCode_Target_5XX_Count >= threshold_alb_5xx_per_5min over 5min. Every threshold comes from
    a variable so con-week is a site.hcl one-liner. Do not hardcode the email or thresholds in the module.
  </action>
  <verify>
    <automated>cd infra/terraform/modules/admin-reports/v1.0.0 && terraform init -backend=false && terraform validate</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` passes
    - `grep -oc 'aws_cloudwatch_metric_alarm' alarms.tf` == 4 AND `grep -c 'aws_sns_topic' alarms.tf` >= 1
    - `grep -c 'ANOMALY_DETECTION_BAND\|anomaly' alarms.tf` >= 1 (ALB RequestCount anomaly alarm)
    - all four thresholds + the email are `var.` references, not literals (grep `var.threshold` >=3, `var.sns_alarm_email` >=1)
  </acceptance_criteria>
  <done>SNS topic + email sub + four alarms exist, every threshold and the email fully parameterized.</done>
</task>

<task type="auto">
  <name>Task 3: Surface thresholds + email in site.hcl and pass to the unit</name>
  <files>infra/terraform/live/site/site.hcl, infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl</files>
  <read_first>
    - infra/terraform/live/site/site.hcl (the admin_reports block from 40-04; alert_email pattern ~line 399 get_env TF_VAR_ADMIN_EMAIL)
    - infra/terraform/live/site/region/us-east-1/admin-reports/terragrunt.hcl (inputs from 40-04)
    - infra/terraform/modules/admin-reports/v1.0.0/variables.tf (Task 2 variable names)
  </read_first>
  <action>
    Extend the admin_reports block in site.hcl with `sns_alarm_email` (default via
    get_env("TF_VAR_ADMIN_EMAIL", ...) matching the existing alert_email pattern) and the four
    threshold keys with their pre-con defaults (signups=1, gpx_uploads=5, alb_5xx=10). Pass these into
    the us-east-1/admin-reports terragrunt.hcl inputs alongside the 40-04 inputs. Keep pre-con defaults;
    the RUNBOOK (40-05) documents bumping them for con-week.
  </action>
  <verify>
    <automated>cd infra/terraform/live/site/region/us-east-1/admin-reports && terragrunt validate 2>&1 | tail -20; terragrunt plan 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sns_alarm_email\|threshold_signups' site.hcl` >= 1
    - `terragrunt plan` renders the dashboard, SNS topic + email subscription, and four alarms with the site.hcl threshold values
    - `terragrunt plan` shows no destroy/recreate of the 40-04 filters/queries/retention (additive change)
  </acceptance_criteria>
  <done>Dashboard + alarms plan cleanly with thresholds and email sourced from site.hcl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudWatch alarm -> SNS email | Alarm notifications carry activity data to an operator inbox |
| site.hcl -> alarm thresholds | Threshold config controls alert sensitivity (pre-con vs con-week) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-13 | Information Disclosure | SNS alarm email | medium | mitigate | Email endpoint is operator-owned, parameterized in site.hcl; topic not public; content is aggregate counts |
| T-40-14 | Denial of Service | pre-con alarm noise | low | accept | Noisy Signups>=1 firing is intentional signal; site.hcl thresholds are the volume knob for con-week |
| T-40-15 | Tampering | anomaly-detection baseline | low | accept | Anomaly band self-trains; a slow-ramp attacker could shift the baseline — event-count alarms backstop it |
</threat_model>

<verification>
- Module `terraform validate` clean with dashboard + alarms
- `terragrunt plan` renders dashboard, SNS+subscription, four alarms; additive over 40-04
- Live alarm email + metric increment confirmed in 40-07
</verification>

<success_criteria>
The admin-reports dashboard and four site.hcl-parameterized tripwire alarms (with SNS email) plan
cleanly on top of the 40-04 data plane.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `infra/terraform/modules/admin-reports/v1.0.0/dashboard.tf` — `admin-reports` dashboard
- `infra/terraform/modules/admin-reports/v1.0.0/alarms.tf` — SNS topic + email subscription + 4 metric alarms
- Variables sns_alarm_email, threshold_signups_per_hour, threshold_gpx_uploads_per_hour, threshold_alb_5xx_per_5min
- site.hcl admin_reports thresholds + email
</artifacts>

<output>
Create `.planning/phases/40-admin-activity-reports/40-06-SUMMARY.md` when done
</output>
